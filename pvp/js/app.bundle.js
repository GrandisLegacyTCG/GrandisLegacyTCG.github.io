/* Grandis Legacy shared gameplay application v3.0 — PvP v3.02 / VS AI v6.1 shared battlefield.
   One Source Authority v1.5.0 + Runtime Foundation v1.82 / Runtime Core v0.50 / Runtime Data v0.12.7.
   This gameplay/UI bundle is the next shared authority for Local AI and the future PvP rebuild; only intent controller and network transport may differ. */
(function(){
  'use strict';
  var GL_APP_MODE=String((typeof window!=='undefined'&&window.GL_APP_MODE)||'LOCAL_AI').toUpperCase();
  var IS_PVP_APP=GL_APP_MODE==='PVP';
  var GL_VERSION=IS_PVP_APP?'Grandis Legacy PvP v3.02 · VS AI v6.1 Battlefield · One Source v1.5.0 · Runtime Data v0.12.7 · Foundation v1.82 · Core v0.50':'Grandis Legacy VS AI v6.1 · Shared Gameplay Bundle v3.0 · One Source v1.5.0 · Runtime Data v0.12.7 · Foundation v1.82 · Core v0.50';
  var PHASES=['Draw','Deploy','Battle','Reform','End'];
  var LANE_ORDER=['LEFT','CENTER','RIGHT'];
  var EXP_MAX_TOTAL=700;
  var OPENING_HAND_SIZE=6;
  var RACIAL_TOKEN_CAP=2;
  var GL_SHARED_CARD_BASE='https://grandislegacytcg.github.io/shared/season1/v1/cards/';
  var GL_ASSET_REV='gl-pvp-3.02-osa-1.5';
  var GL_CARD_ZOOM_ID=null;
  var GL_LAST_PLAYER_TURN_BANNER_KEY='', GL_PLAYER_TURN_BANNER_TIMER=null;
  var GL_MODAL_HOVER_GUARD_BOUND=false;
  var GL_MODAL_STACK_BOUND=false, GL_MODAL_STACK_SEQUENCE=12000;
  function bringModalToFront(overlay){
    if(!overlay) return 0;
    GL_MODAL_STACK_SEQUENCE+=1;
    if(overlay.style&&typeof overlay.style.setProperty==='function') overlay.style.setProperty('z-index',String(GL_MODAL_STACK_SEQUENCE),'important'); else if(overlay.style) overlay.style.zIndex=String(GL_MODAL_STACK_SEQUENCE);
    overlay.setAttribute('data-modal-stack-order',String(GL_MODAL_STACK_SEQUENCE));
    return GL_MODAL_STACK_SEQUENCE;
  }
  function bindModalStackManager(){
    if(GL_MODAL_STACK_BOUND || !animationDocumentReady()) return;
    GL_MODAL_STACK_BOUND=true;
    var overlays=Array.prototype.slice.call(document.querySelectorAll('#choiceOverlay,#infoOverlay,#responseOverlay,#previewOverlay'));
    overlays.forEach(function(overlay){
      if(overlay.classList.contains('open')) bringModalToFront(overlay);
      if(typeof MutationObserver!=='undefined'){
        new MutationObserver(function(){ if(overlay.classList.contains('open')) bringModalToFront(overlay); }).observe(overlay,{attributes:true,attributeFilter:['class']});
      }
    });
  }
  function nextVisualFrame(fn){
    if(typeof requestAnimationFrame==='function') return requestAnimationFrame(fn);
    return setTimeout(fn,0);
  }
  var GL_DECODED_VISUAL_ASSETS=Object.create(null),GL_VISUAL_DECODE_OBSERVER=null;
  function visualAssetKey(src){return String(src||'').split('#')[0];}
  function markVisualImageReady(img,key,cache){
    if(!img)return false;
    if(cache&&key)GL_DECODED_VISUAL_ASSETS[key]=true;
    if(img.classList){img.classList.remove('gl-decode-pending');img.classList.add('gl-image-ready');}
    if(img.style)img.style.visibility='';
    return true;
  }
  function prepareImageForPaint(img){
    if(!img)return Promise.resolve(false);
    var src=(img.getAttribute&&img.getAttribute('src'))||img.src||'',key=visualAssetKey(src);
    try{img.decoding='async';img.draggable=false;if(img.setAttribute){img.setAttribute('draggable','false');img.setAttribute('data-gl-image','1');}}catch(e){}
    if(img._glDecodeKey===key&&img._glDecodePromise)return img._glDecodePromise;
    img._glDecodeKey=key;
    if(GL_DECODED_VISUAL_ASSETS[key]){markVisualImageReady(img,key,false);return Promise.resolve(true);}
    if(img.classList){img.classList.remove('gl-image-ready');img.classList.add('gl-decode-pending');}
    img._glDecodePromise=new Promise(function(resolve){
      var settled=false,timer=null;
      function done(ok){if(settled)return;settled=true;if(timer)clearTimeout(timer);markVisualImageReady(img,key,!!ok);resolve(!!ok);}
      try{
        if(img.complete&&Number(img.naturalWidth||0)>0){
          if(typeof img.decode==='function'){Promise.resolve(img.decode()).then(function(){done(true);},function(){done(true);});}
          else done(true);
          return;
        }
        if(img.addEventListener){img.addEventListener('load',function(){done(true);},{once:true});img.addEventListener('error',function(){done(false);},{once:true});}
        if(typeof img.decode==='function'){Promise.resolve().then(function(){return img.decode();}).then(function(){done(true);},function(){});}
      }catch(e){}
      timer=setTimeout(function(){done(!!(img.complete&&Number(img.naturalWidth||0)>0));},1600);
    });
    return img._glDecodePromise;
  }
  function prepareRenderedImages(root){
    if(!root)return Promise.resolve([]);
    var list=[];
    if(String(root.tagName||'').toUpperCase()==='IMG')list=[root];
    else if(root.querySelectorAll)list=Array.prototype.slice.call(root.querySelectorAll('img'));
    return Promise.all(list.map(prepareImageForPaint));
  }
  function preloadVisualAsset(src){
    var key=visualAssetKey(src);if(!key||GL_DECODED_VISUAL_ASSETS[key])return Promise.resolve(true);
    if(!animationDocumentReady())return Promise.resolve(false);
    var img=document.createElement('img');img.src=src;return prepareImageForPaint(img);
  }
  function preloadCriticalVisualAssets(){
    return Promise.all([
      'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Head.webp','https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Tail.webp',
      'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp','https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Legacy-Deck.webp',
      'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Mana-Shard-Thumb.webp'
    ].map(preloadVisualAsset));
  }
  function installVisualDecodeObserver(){
    if(GL_VISUAL_DECODE_OBSERVER||typeof MutationObserver==='undefined'||!animationDocumentReady()||!document.body)return;
    GL_VISUAL_DECODE_OBSERVER=new MutationObserver(function(records){
      records.forEach(function(record){
        if(record.type==='attributes'&&record.target)prepareImageForPaint(record.target);
        Array.prototype.forEach.call(record.addedNodes||[],function(node){if(node&&node.nodeType===1)prepareRenderedImages(node);});
      });
    });
    GL_VISUAL_DECODE_OBSERVER.observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['src']});
  }
  var STARTER_DECK_OPTIONS = {"starter_01_elemental_lord_conqueror_renegade":{"label":"Starter 1 — Elemental Lord / Conqueror / Renegade","file":"starter_deck_examples/starter_01_elemental_lord_conqueror_renegade_GL_DECK_1_0.json","deck":{"schema_version":"GL-DECK-1.0","builder_version":"2.1-public-deck-builder","deck_name":"Starter 1 — Elemental Lord / Conqueror / Renegade","format":"One Source Authority v1.4 + Public Deck Builder v2.1","main_deck_count":60,"legacy_package_count":3,"legacy_deck_count":12,"legacy_deck_label":"Legacy Deck","legacy_deck_package_slots":[{"progression":"S1-WAR-H001","legacy":"S1-WAR-L001"},{"progression":"S1-MAG-H001","legacy":"S1-MAG-L001"},{"progression":"S1-THF-H001","legacy":"S1-THF-L002"}],"legacy_deck_expanded":[{"card_id":"S1-WAR-H001","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-WAR-H002","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-WAR-H003","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-WAR-L001","card_name":"Warrior’s Relic","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-MAG-H001","card_name":"Vaelis Stormweave","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-MAG-H002","card_name":"Vaelis Stormweave","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-MAG-H003","card_name":"Vaelis Stormweave","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-MAG-L001","card_name":"Ancestral Tome","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-THF-H001","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"},{"card_id":"S1-THF-H002","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"},{"card_id":"S1-THF-H003","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"},{"card_id":"S1-THF-L002","card_name":"Hidden Archives","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"}],"side_package_count":3,"side_deck_count":12,"side_deck_package_slots":[{"progression":"S1-WAR-H001","legacy":"S1-WAR-L001"},{"progression":"S1-MAG-H001","legacy":"S1-MAG-L001"},{"progression":"S1-THF-H001","legacy":"S1-THF-L002"}],"side_deck_expanded":[{"card_id":"S1-WAR-H001","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-WAR-H002","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-WAR-H003","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-WAR-L001","card_name":"Warrior’s Relic","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Human Warrior — Warrior;Gladiator;Conqueror + Warrior’s Relic"},{"card_id":"S1-MAG-H001","card_name":"Vaelis Stormweave","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-MAG-H002","card_name":"Vaelis Stormweave","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-MAG-H003","card_name":"Vaelis Stormweave","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-MAG-L001","card_name":"Ancestral Tome","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Elf Mage — Mage;Elementalist;Elemental Lord + Ancestral Tome"},{"card_id":"S1-THF-H001","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"},{"card_id":"S1-THF-H002","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"},{"card_id":"S1-THF-H003","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"},{"card_id":"S1-THF-L002","card_name":"Hidden Archives","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Halfling Thief — Thief;Rogue;Renegade + Hidden Archives"}],"is_valid":true,"validation_issues":[],"validation_warnings":[],"main_deck":[{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-EVT-010","card_name":"Déjà vu","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":2},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-016","card_name":"Chain Mail","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":2},{"card_id":"S1-MAG-004","card_name":"Mana Shield","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":2},{"card_id":"S1-MAG-006","card_name":"Meditation","quantity":2},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":1},{"card_id":"S1-MAG-008","card_name":"Frostbite","quantity":2},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":2},{"card_id":"S1-MAG-011","card_name":"Ice Block","quantity":1},{"card_id":"S1-MAG-013","card_name":"Wildfire","quantity":1},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":1},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":2},{"card_id":"S1-MAG-018","card_name":"Double Casting","quantity":1},{"card_id":"S1-THF-004","card_name":"Smoke Screen","quantity":1},{"card_id":"S1-THF-005","card_name":"Steal","quantity":1},{"card_id":"S1-THF-007","card_name":"Horizontal Slash","quantity":1},{"card_id":"S1-THF-008","card_name":"Vertical Slash","quantity":1},{"card_id":"S1-THF-009","card_name":"X Cross Slash","quantity":1},{"card_id":"S1-THF-010","card_name":"Back Stab","quantity":1},{"card_id":"S1-THF-011","card_name":"Elusive Escape","quantity":2},{"card_id":"S1-THF-012","card_name":"Poison Mist","quantity":2},{"card_id":"S1-THF-013","card_name":"Venom Strike","quantity":1},{"card_id":"S1-THF-014","card_name":"Viper’s Fang","quantity":2},{"card_id":"S1-THF-015","card_name":"Venom Sovereign","quantity":1},{"card_id":"S1-THF-017","card_name":"Venom Binding","quantity":1},{"card_id":"S1-THF-018","card_name":"Venom Detonation","quantity":1},{"card_id":"S1-WAR-002","card_name":"Power Slash","quantity":1},{"card_id":"S1-WAR-004","card_name":"Cover Up","quantity":1},{"card_id":"S1-WAR-007","card_name":"Upward Thrust","quantity":2},{"card_id":"S1-WAR-008","card_name":"Wild Swing","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":2},{"card_id":"S1-WAR-010","card_name":"Rage Blast","quantity":1},{"card_id":"S1-WAR-011","card_name":"Side Step","quantity":1},{"card_id":"S1-WAR-012","card_name":"Deflect","quantity":2},{"card_id":"S1-WAR-014","card_name":"Final Grit","quantity":1},{"card_id":"S1-WAR-015","card_name":"Charge Attack","quantity":1},{"card_id":"S1-WAR-016","card_name":"Whirlwind","quantity":1},{"card_id":"S1-WAR-017","card_name":"Fiery Thrust","quantity":1},{"card_id":"S1-WAR-018","card_name":"Execute","quantity":1}],"default_formation":{"LEFT":"S1-MAG-H001","CENTER":"S1-WAR-H001","RIGHT":"S1-THF-H001"},"source_database_version":"One Source Authority v1.4 · registry b455b1537434e454ab873372bb9da715b779e64525a01a8bc3995ed9267ecfa0 · Starter60 v1.2","builder_version_note":"Normalized for Grandis Legacy PvP v2.6.3 while preserving the approved deck contents, Legacy packages, and default Formation.","display_name":"Starter 1 — Elemental Lord / Conqueror / Renegade","preset_id":"starter_01_elemental_lord_conqueror_renegade","main_deck_expanded":[{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-EVT-010","card_name":"Déjà vu","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":1},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-016","card_name":"Chain Mail","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-MAG-004","card_name":"Mana Shield","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":1},{"card_id":"S1-MAG-006","card_name":"Meditation","quantity":1},{"card_id":"S1-MAG-006","card_name":"Meditation","quantity":1},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":1},{"card_id":"S1-MAG-008","card_name":"Frostbite","quantity":1},{"card_id":"S1-MAG-008","card_name":"Frostbite","quantity":1},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":1},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":1},{"card_id":"S1-MAG-011","card_name":"Ice Block","quantity":1},{"card_id":"S1-MAG-013","card_name":"Wildfire","quantity":1},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":1},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":1},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":1},{"card_id":"S1-MAG-018","card_name":"Double Casting","quantity":1},{"card_id":"S1-THF-004","card_name":"Smoke Screen","quantity":1},{"card_id":"S1-THF-005","card_name":"Steal","quantity":1},{"card_id":"S1-THF-007","card_name":"Horizontal Slash","quantity":1},{"card_id":"S1-THF-008","card_name":"Vertical Slash","quantity":1},{"card_id":"S1-THF-009","card_name":"X Cross Slash","quantity":1},{"card_id":"S1-THF-010","card_name":"Back Stab","quantity":1},{"card_id":"S1-THF-011","card_name":"Elusive Escape","quantity":1},{"card_id":"S1-THF-011","card_name":"Elusive Escape","quantity":1},{"card_id":"S1-THF-012","card_name":"Poison Mist","quantity":1},{"card_id":"S1-THF-012","card_name":"Poison Mist","quantity":1},{"card_id":"S1-THF-013","card_name":"Venom Strike","quantity":1},{"card_id":"S1-THF-014","card_name":"Viper’s Fang","quantity":1},{"card_id":"S1-THF-014","card_name":"Viper’s Fang","quantity":1},{"card_id":"S1-THF-015","card_name":"Venom Sovereign","quantity":1},{"card_id":"S1-THF-017","card_name":"Venom Binding","quantity":1},{"card_id":"S1-THF-018","card_name":"Venom Detonation","quantity":1},{"card_id":"S1-WAR-002","card_name":"Power Slash","quantity":1},{"card_id":"S1-WAR-004","card_name":"Cover Up","quantity":1},{"card_id":"S1-WAR-007","card_name":"Upward Thrust","quantity":1},{"card_id":"S1-WAR-007","card_name":"Upward Thrust","quantity":1},{"card_id":"S1-WAR-008","card_name":"Wild Swing","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":1},{"card_id":"S1-WAR-010","card_name":"Rage Blast","quantity":1},{"card_id":"S1-WAR-011","card_name":"Side Step","quantity":1},{"card_id":"S1-WAR-012","card_name":"Deflect","quantity":1},{"card_id":"S1-WAR-012","card_name":"Deflect","quantity":1},{"card_id":"S1-WAR-014","card_name":"Final Grit","quantity":1},{"card_id":"S1-WAR-015","card_name":"Charge Attack","quantity":1},{"card_id":"S1-WAR-016","card_name":"Whirlwind","quantity":1},{"card_id":"S1-WAR-017","card_name":"Fiery Thrust","quantity":1},{"card_id":"S1-WAR-018","card_name":"Execute","quantity":1}]}},"starter_02_saint_crusader_grand_ranger":{"label":"Starter 2 — Saint / Crusader / Grand Ranger","file":"starter_deck_examples/starter_02_saint_crusader_grand_ranger_GL_DECK_1_0.json","deck":{"schema_version":"GL-DECK-1.0","builder_version":"2.1-public-deck-builder","deck_name":"Starter 2 — Saint / Crusader / Grand Ranger","format":"One Source Authority v1.4 + Public Deck Builder v2.1","main_deck_count":60,"legacy_package_count":3,"legacy_deck_count":12,"legacy_deck_label":"Legacy Deck","legacy_deck_package_slots":[{"progression":"S1-CLE-H004","legacy":"S1-CLE-L002"},{"progression":"S1-WAR-H004","legacy":"S1-WAR-L001"},{"progression":"S1-ARC-H001","legacy":"S1-ARC-L001"}],"legacy_deck_expanded":[{"card_id":"S1-CLE-H004","card_name":"Thrain Sunborn","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-CLE-H005","card_name":"Thrain Sunborn","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-CLE-H006","card_name":"Thrain Sunborn","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-CLE-L002","card_name":"Wand of First Light","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-WAR-H004","card_name":"Aurex Sunsworn","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-WAR-H005","card_name":"Aurex Sunsworn","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-WAR-H006","card_name":"Aurex Sunsworn","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-WAR-L001","card_name":"Warrior’s Relic","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-ARC-H001","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"},{"card_id":"S1-ARC-H002","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"},{"card_id":"S1-ARC-H003","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"},{"card_id":"S1-ARC-L001","card_name":"Golden Arrows","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"}],"side_package_count":3,"side_deck_count":12,"side_deck_package_slots":[{"progression":"S1-CLE-H004","legacy":"S1-CLE-L002"},{"progression":"S1-WAR-H004","legacy":"S1-WAR-L001"},{"progression":"S1-ARC-H001","legacy":"S1-ARC-L001"}],"side_deck_expanded":[{"card_id":"S1-CLE-H004","card_name":"Thrain Sunborn","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-CLE-H005","card_name":"Thrain Sunborn","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-CLE-H006","card_name":"Thrain Sunborn","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-CLE-L002","card_name":"Wand of First Light","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Dwarf Cleric — THRAIN + Wand of First Light"},{"card_id":"S1-WAR-H004","card_name":"Aurex Sunsworn","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-WAR-H005","card_name":"Aurex Sunsworn","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-WAR-H006","card_name":"Aurex Sunsworn","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-WAR-L001","card_name":"Warrior’s Relic","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Dragonborn Warrior — AUREX + Warrior’s Relic"},{"card_id":"S1-ARC-H001","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"},{"card_id":"S1-ARC-H002","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"},{"card_id":"S1-ARC-H003","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"},{"card_id":"S1-ARC-L001","card_name":"Golden Arrows","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Golden Arrows"}],"is_valid":true,"validation_issues":[],"validation_warnings":[],"main_deck":[{"card_id":"S1-ARC-002","card_name":"Poison Arrow","quantity":2},{"card_id":"S1-ARC-003","card_name":"Escape Arrow","quantity":2},{"card_id":"S1-ARC-005","card_name":"Reload","quantity":1},{"card_id":"S1-ARC-008","card_name":"Ambush Shot","quantity":2},{"card_id":"S1-ARC-010","card_name":"Arrow Barrage","quantity":1},{"card_id":"S1-ARC-011","card_name":"Back Step","quantity":2},{"card_id":"S1-ARC-012","card_name":"Deflect Arrow","quantity":1},{"card_id":"S1-ARC-015","card_name":"Power Shot","quantity":1},{"card_id":"S1-ARC-016","card_name":"Soul Blast Shot","quantity":2},{"card_id":"S1-ARC-017","card_name":"Dual Arrow","quantity":1},{"card_id":"S1-ARC-018","card_name":"Charged Shot","quantity":1},{"card_id":"S1-CLE-003","card_name":"Brilliant Radiance","quantity":1},{"card_id":"S1-CLE-006","card_name":"Blessing of Might","quantity":1},{"card_id":"S1-CLE-009","card_name":"Holy Ring","quantity":2},{"card_id":"S1-CLE-012","card_name":"Binding Light","quantity":1},{"card_id":"S1-CLE-015","card_name":"Resurrection","quantity":1},{"card_id":"S1-CLE-016","card_name":"Sanctuary","quantity":1},{"card_id":"S1-CLE-017","card_name":"Lay on Hands","quantity":2},{"card_id":"S1-CLE-019","card_name":"Divine Punishment","quantity":1},{"card_id":"S1-CLE-020","card_name":"Holy Slash","quantity":2},{"card_id":"S1-CLE-021","card_name":"Hammer of Justice","quantity":2},{"card_id":"S1-CLE-022","card_name":"Sacred Bulwark","quantity":1},{"card_id":"S1-CLE-023","card_name":"Holy Light","quantity":2},{"card_id":"S1-CLE-024","card_name":"Divine Judgement","quantity":1},{"card_id":"S1-CLE-025","card_name":"Blessing of Divinity","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-EVT-010","card_name":"Déjà vu","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":2},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-013","card_name":"Ring of Grace","quantity":1},{"card_id":"S1-ITM-016","card_name":"Chain Mail","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":2},{"card_id":"S1-WAR-005","card_name":"Taunt","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":2},{"card_id":"S1-WAR-020","card_name":"Shield Bash","quantity":2},{"card_id":"S1-WAR-021","card_name":"Sacred Stormblade","quantity":2},{"card_id":"S1-WAR-022","card_name":"Unbroken Stand","quantity":2},{"card_id":"S1-WAR-023","card_name":"Divine Slash","quantity":1},{"card_id":"S1-WAR-024","card_name":"Heroic Charge","quantity":1}],"default_formation":{"LEFT":"S1-CLE-H004","CENTER":"S1-WAR-H004","RIGHT":"S1-ARC-H001"},"source_database_version":"One Source Authority v1.4 · registry b455b1537434e454ab873372bb9da715b779e64525a01a8bc3995ed9267ecfa0","builder_version_note":"Normalized for Grandis Legacy PvP v2.6.3 while preserving the approved deck contents, Legacy packages, and default Formation.","display_name":"Starter 2 — Saint / Crusader / Grand Ranger","preset_id":"starter_02_saint_crusader_grand_ranger","main_deck_expanded":[{"card_id":"S1-ARC-002","card_name":"Poison Arrow","quantity":1},{"card_id":"S1-ARC-002","card_name":"Poison Arrow","quantity":1},{"card_id":"S1-ARC-003","card_name":"Escape Arrow","quantity":1},{"card_id":"S1-ARC-003","card_name":"Escape Arrow","quantity":1},{"card_id":"S1-ARC-005","card_name":"Reload","quantity":1},{"card_id":"S1-ARC-008","card_name":"Ambush Shot","quantity":1},{"card_id":"S1-ARC-008","card_name":"Ambush Shot","quantity":1},{"card_id":"S1-ARC-010","card_name":"Arrow Barrage","quantity":1},{"card_id":"S1-ARC-011","card_name":"Back Step","quantity":1},{"card_id":"S1-ARC-011","card_name":"Back Step","quantity":1},{"card_id":"S1-ARC-012","card_name":"Deflect Arrow","quantity":1},{"card_id":"S1-ARC-015","card_name":"Power Shot","quantity":1},{"card_id":"S1-ARC-016","card_name":"Soul Blast Shot","quantity":1},{"card_id":"S1-ARC-016","card_name":"Soul Blast Shot","quantity":1},{"card_id":"S1-ARC-017","card_name":"Dual Arrow","quantity":1},{"card_id":"S1-ARC-018","card_name":"Charged Shot","quantity":1},{"card_id":"S1-CLE-003","card_name":"Brilliant Radiance","quantity":1},{"card_id":"S1-CLE-006","card_name":"Blessing of Might","quantity":1},{"card_id":"S1-CLE-009","card_name":"Holy Ring","quantity":1},{"card_id":"S1-CLE-009","card_name":"Holy Ring","quantity":1},{"card_id":"S1-CLE-012","card_name":"Binding Light","quantity":1},{"card_id":"S1-CLE-015","card_name":"Resurrection","quantity":1},{"card_id":"S1-CLE-016","card_name":"Sanctuary","quantity":1},{"card_id":"S1-CLE-017","card_name":"Lay on Hands","quantity":1},{"card_id":"S1-CLE-017","card_name":"Lay on Hands","quantity":1},{"card_id":"S1-CLE-019","card_name":"Divine Punishment","quantity":1},{"card_id":"S1-CLE-020","card_name":"Holy Slash","quantity":1},{"card_id":"S1-CLE-020","card_name":"Holy Slash","quantity":1},{"card_id":"S1-CLE-021","card_name":"Hammer of Justice","quantity":1},{"card_id":"S1-CLE-021","card_name":"Hammer of Justice","quantity":1},{"card_id":"S1-CLE-022","card_name":"Sacred Bulwark","quantity":1},{"card_id":"S1-CLE-023","card_name":"Holy Light","quantity":1},{"card_id":"S1-CLE-023","card_name":"Holy Light","quantity":1},{"card_id":"S1-CLE-024","card_name":"Divine Judgement","quantity":1},{"card_id":"S1-CLE-025","card_name":"Blessing of Divinity","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-EVT-010","card_name":"Déjà vu","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-013","card_name":"Ring of Grace","quantity":1},{"card_id":"S1-ITM-016","card_name":"Chain Mail","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-WAR-005","card_name":"Taunt","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":1},{"card_id":"S1-WAR-020","card_name":"Shield Bash","quantity":1},{"card_id":"S1-WAR-020","card_name":"Shield Bash","quantity":1},{"card_id":"S1-WAR-021","card_name":"Sacred Stormblade","quantity":1},{"card_id":"S1-WAR-021","card_name":"Sacred Stormblade","quantity":1},{"card_id":"S1-WAR-022","card_name":"Unbroken Stand","quantity":1},{"card_id":"S1-WAR-022","card_name":"Unbroken Stand","quantity":1},{"card_id":"S1-WAR-023","card_name":"Divine Slash","quantity":1},{"card_id":"S1-WAR-024","card_name":"Heroic Charge","quantity":1}]}},"starter_03_renegade_arcane_duelist_elemental_lord":{"label":"Starter 3 — Renegade / Arcane Duelist / Elemental Lord","file":"starter_deck_examples/starter_03_renegade_arcane_duelist_elemental_lord_GL_DECK_1_0.json","deck":{"schema_version":"GL-DECK-1.0","builder_version":"2.1-public-deck-builder","deck_name":"Starter 3 — Renegade / Arcane Duelist / Elemental Lord","format":"One Source Authority v1.4 + Public Deck Builder v2.1","main_deck_count":60,"legacy_package_count":3,"legacy_deck_count":12,"legacy_deck_label":"Legacy Deck","legacy_deck_package_slots":[{"progression":"S1-MAG-H004","legacy":"S1-MAG-L002"},{"progression":"S1-THF-H001","legacy":"S1-THF-L001"},{"progression":"S1-THF-H004","legacy":"S1-THF-L002"}],"legacy_deck_expanded":[{"card_id":"S1-MAG-H004","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H005","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H006","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-L002","card_name":"Arcane Wand","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-THF-H001","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-H002","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-H003","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-L001","card_name":"Hidden Stash","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-H004","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H005","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H006","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-L002","card_name":"Hidden Archives","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"}],"side_package_count":3,"side_deck_count":12,"side_deck_package_slots":[{"progression":"S1-MAG-H004","legacy":"S1-MAG-L002"},{"progression":"S1-THF-H001","legacy":"S1-THF-L001"},{"progression":"S1-THF-H004","legacy":"S1-THF-L002"}],"side_deck_expanded":[{"card_id":"S1-MAG-H004","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H005","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H006","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-L002","card_name":"Arcane Wand","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-THF-H001","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-H002","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-H003","card_name":"Finnian Copperpot","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-L001","card_name":"Hidden Stash","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Halfling Thief — FINNIAN + Hidden Stash"},{"card_id":"S1-THF-H004","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H005","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H006","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-L002","card_name":"Hidden Archives","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Elf Thief — LUCIEN + Hidden Archives"}],"is_valid":true,"validation_issues":[],"validation_warnings":[],"main_deck":[{"card_id":"S1-EVT-001","card_name":"Begin Anew","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":2},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":2},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-008","card_name":"Forged Alliance","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":2},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":2},{"card_id":"S1-ITM-017","card_name":"Flashpowder Bomb","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":2},{"card_id":"S1-MAG-003","card_name":"Fireball","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":2},{"card_id":"S1-MAG-006","card_name":"Meditation","quantity":2},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":2},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":2},{"card_id":"S1-MAG-014","card_name":"Lightning Strike","quantity":1},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":2},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":1},{"card_id":"S1-MAG-019","card_name":"Flame Dragon's Wrath","quantity":1},{"card_id":"S1-MAG-020","card_name":"Aether Ball","quantity":1},{"card_id":"S1-MAG-021","card_name":"Mana Absorption","quantity":2},{"card_id":"S1-MAG-022","card_name":"Aether Infusion","quantity":2},{"card_id":"S1-MAG-023","card_name":"Aether Slash","quantity":2},{"card_id":"S1-MAG-024","card_name":"Aether Sweep","quantity":1},{"card_id":"S1-MAG-025","card_name":"Mana Void","quantity":1},{"card_id":"S1-THF-002","card_name":"Jump Slash","quantity":1},{"card_id":"S1-THF-003","card_name":"Evasion","quantity":2},{"card_id":"S1-THF-004","card_name":"Smoke Screen","quantity":1},{"card_id":"S1-THF-006","card_name":"Sixth Sense","quantity":2},{"card_id":"S1-THF-010","card_name":"Back Stab","quantity":2},{"card_id":"S1-THF-011","card_name":"Elusive Escape","quantity":2},{"card_id":"S1-THF-016","card_name":"Finishing Strike","quantity":2},{"card_id":"S1-THF-017","card_name":"Venom Binding","quantity":2},{"card_id":"S1-THF-018","card_name":"Venom Detonation","quantity":1},{"card_id":"S1-THF-020","card_name":"Sword Slash","quantity":2},{"card_id":"S1-THF-022","card_name":"Step In","quantity":2},{"card_id":"S1-THF-023","card_name":"Flash Slash","quantity":1},{"card_id":"S1-THF-024","card_name":"Sword Combo","quantity":1}],"default_formation":{"LEFT":"S1-THF-H001","CENTER":"S1-THF-H004","RIGHT":"S1-MAG-H004"},"source_database_version":"One Source Authority v1.4 · registry b455b1537434e454ab873372bb9da715b779e64525a01a8bc3995ed9267ecfa0","builder_version_note":"Normalized for Grandis Legacy PvP v2.6.3 while preserving the approved deck contents, Legacy packages, and default Formation.","display_name":"Starter 3 — Renegade / Arcane Duelist / Elemental Lord","preset_id":"starter_03_renegade_arcane_duelist_elemental_lord","main_deck_expanded":[{"card_id":"S1-EVT-001","card_name":"Begin Anew","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-008","card_name":"Forged Alliance","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":1},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":1},{"card_id":"S1-ITM-017","card_name":"Flashpowder Bomb","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-MAG-003","card_name":"Fireball","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":1},{"card_id":"S1-MAG-006","card_name":"Meditation","quantity":1},{"card_id":"S1-MAG-006","card_name":"Meditation","quantity":1},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":1},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":1},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":1},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":1},{"card_id":"S1-MAG-014","card_name":"Lightning Strike","quantity":1},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":1},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":1},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":1},{"card_id":"S1-MAG-019","card_name":"Flame Dragon's Wrath","quantity":1},{"card_id":"S1-MAG-020","card_name":"Aether Ball","quantity":1},{"card_id":"S1-MAG-021","card_name":"Mana Absorption","quantity":1},{"card_id":"S1-MAG-021","card_name":"Mana Absorption","quantity":1},{"card_id":"S1-MAG-022","card_name":"Aether Infusion","quantity":1},{"card_id":"S1-MAG-022","card_name":"Aether Infusion","quantity":1},{"card_id":"S1-MAG-023","card_name":"Aether Slash","quantity":1},{"card_id":"S1-MAG-023","card_name":"Aether Slash","quantity":1},{"card_id":"S1-MAG-024","card_name":"Aether Sweep","quantity":1},{"card_id":"S1-MAG-025","card_name":"Mana Void","quantity":1},{"card_id":"S1-THF-002","card_name":"Jump Slash","quantity":1},{"card_id":"S1-THF-003","card_name":"Evasion","quantity":1},{"card_id":"S1-THF-003","card_name":"Evasion","quantity":1},{"card_id":"S1-THF-004","card_name":"Smoke Screen","quantity":1},{"card_id":"S1-THF-006","card_name":"Sixth Sense","quantity":1},{"card_id":"S1-THF-006","card_name":"Sixth Sense","quantity":1},{"card_id":"S1-THF-010","card_name":"Back Stab","quantity":1},{"card_id":"S1-THF-010","card_name":"Back Stab","quantity":1},{"card_id":"S1-THF-011","card_name":"Elusive Escape","quantity":1},{"card_id":"S1-THF-011","card_name":"Elusive Escape","quantity":1},{"card_id":"S1-THF-016","card_name":"Finishing Strike","quantity":1},{"card_id":"S1-THF-016","card_name":"Finishing Strike","quantity":1},{"card_id":"S1-THF-017","card_name":"Venom Binding","quantity":1},{"card_id":"S1-THF-017","card_name":"Venom Binding","quantity":1},{"card_id":"S1-THF-018","card_name":"Venom Detonation","quantity":1},{"card_id":"S1-THF-020","card_name":"Sword Slash","quantity":1},{"card_id":"S1-THF-020","card_name":"Sword Slash","quantity":1},{"card_id":"S1-THF-022","card_name":"Step In","quantity":1},{"card_id":"S1-THF-022","card_name":"Step In","quantity":1},{"card_id":"S1-THF-023","card_name":"Flash Slash","quantity":1},{"card_id":"S1-THF-024","card_name":"Sword Combo","quantity":1}]}},"starter_04_grand_ranger_conqueror_grand_arbalest":{"label":"Starter 4 — Grand Ranger / Conqueror / Grand Arbalest","file":"starter_deck_examples/starter_04_grand_ranger_conqueror_grand_arbalest_GL_DECK_1_0.json","deck":{"schema_version":"GL-DECK-1.0","builder_version":"2.1-public-deck-builder","deck_name":"Starter 4 — Grand Ranger / Conqueror / Grand Arbalest","format":"One Source Authority v1.4 + Public Deck Builder v2.1","main_deck_count":60,"legacy_package_count":3,"legacy_deck_count":12,"legacy_deck_label":"Legacy Deck","legacy_deck_package_slots":[{"progression":"S1-ARC-H004","legacy":"S1-ARC-L001"},{"progression":"S1-WAR-H001","legacy":"S1-WAR-L001"},{"progression":"S1-ARC-H001","legacy":"S1-ARC-L002"}],"legacy_deck_expanded":[{"card_id":"S1-ARC-H004","card_name":"Alden Sterling","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-ARC-H005","card_name":"Alden Sterling","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-ARC-H006","card_name":"Alden Sterling","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-ARC-L001","card_name":"Golden Arrows","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-WAR-H001","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-WAR-H002","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-WAR-H003","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-WAR-L001","card_name":"Warrior’s Relic","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-ARC-H001","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"},{"card_id":"S1-ARC-H002","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"},{"card_id":"S1-ARC-H003","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"},{"card_id":"S1-ARC-L002","card_name":"Falconer’s Whistle","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"}],"side_package_count":3,"side_deck_count":12,"side_deck_package_slots":[{"progression":"S1-ARC-H004","legacy":"S1-ARC-L001"},{"progression":"S1-WAR-H001","legacy":"S1-WAR-L001"},{"progression":"S1-ARC-H001","legacy":"S1-ARC-L002"}],"side_deck_expanded":[{"card_id":"S1-ARC-H004","card_name":"Alden Sterling","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-ARC-H005","card_name":"Alden Sterling","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-ARC-H006","card_name":"Alden Sterling","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-ARC-L001","card_name":"Golden Arrows","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Human Archer — ALDEN + Golden Arrows"},{"card_id":"S1-WAR-H001","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-WAR-H002","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-WAR-H003","card_name":"Draxen Blacksand","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-WAR-L001","card_name":"Warrior’s Relic","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Human Warrior — DRAXEN + Warrior’s Relic"},{"card_id":"S1-ARC-H001","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"},{"card_id":"S1-ARC-H002","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"},{"card_id":"S1-ARC-H003","card_name":"Korvak Ironfang","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"},{"card_id":"S1-ARC-L002","card_name":"Falconer’s Whistle","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Beastman Archer — KORVAK + Falconer’s Whistle"}],"is_valid":true,"validation_issues":[],"validation_warnings":[],"main_deck":[{"card_id":"S1-ARC-002","card_name":"Poison Arrow","quantity":2},{"card_id":"S1-ARC-003","card_name":"Escape Arrow","quantity":2},{"card_id":"S1-ARC-005","card_name":"Reload","quantity":2},{"card_id":"S1-ARC-008","card_name":"Ambush Shot","quantity":2},{"card_id":"S1-ARC-010","card_name":"Arrow Barrage","quantity":2},{"card_id":"S1-ARC-011","card_name":"Back Step","quantity":2},{"card_id":"S1-ARC-012","card_name":"Deflect Arrow","quantity":1},{"card_id":"S1-ARC-014","card_name":"Replenish","quantity":1},{"card_id":"S1-ARC-016","card_name":"Soul Blast Shot","quantity":1},{"card_id":"S1-ARC-017","card_name":"Dual Arrow","quantity":1},{"card_id":"S1-ARC-018","card_name":"Charged Shot","quantity":1},{"card_id":"S1-ARC-019","card_name":"Long Range Shot","quantity":2},{"card_id":"S1-ARC-020","card_name":"Piercing Bolt","quantity":1},{"card_id":"S1-ARC-021","card_name":"Aura Infusion Bolt","quantity":1},{"card_id":"S1-ARC-022","card_name":"Singularity Reload","quantity":1},{"card_id":"S1-ARC-023","card_name":"Cross Bolt","quantity":2},{"card_id":"S1-ARC-024","card_name":"Colossal Dreadbolt","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-008","card_name":"Forged Alliance","quantity":1},{"card_id":"S1-EVT-010","card_name":"Déjà vu","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":1},{"card_id":"S1-ITM-016","card_name":"Chain Mail","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-THF-026","card_name":"Dodge Instinct","quantity":2},{"card_id":"S1-THF-027","card_name":"Camouflage","quantity":2},{"card_id":"S1-THF-029","card_name":"Flash Combo","quantity":2},{"card_id":"S1-THF-030","card_name":"Crescent Moon Slash","quantity":1},{"card_id":"S1-WAR-004","card_name":"Cover Up","quantity":1},{"card_id":"S1-WAR-005","card_name":"Taunt","quantity":1},{"card_id":"S1-WAR-007","card_name":"Upward Thrust","quantity":2},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":2},{"card_id":"S1-WAR-010","card_name":"Rage Blast","quantity":1},{"card_id":"S1-WAR-011","card_name":"Side Step","quantity":1},{"card_id":"S1-WAR-012","card_name":"Deflect","quantity":2},{"card_id":"S1-WAR-014","card_name":"Final Grit","quantity":1},{"card_id":"S1-WAR-015","card_name":"Charge Attack","quantity":1},{"card_id":"S1-WAR-016","card_name":"Whirlwind","quantity":1},{"card_id":"S1-WAR-017","card_name":"Fiery Thrust","quantity":2},{"card_id":"S1-WAR-018","card_name":"Execute","quantity":1}],"default_formation":{"LEFT":"S1-ARC-H001","CENTER":"S1-WAR-H001","RIGHT":"S1-ARC-H004"},"source_database_version":"One Source Authority v1.4 · registry b455b1537434e454ab873372bb9da715b779e64525a01a8bc3995ed9267ecfa0","builder_version_note":"Normalized for Grandis Legacy PvP v2.6.3 while preserving the approved deck contents, Legacy packages, and default Formation.","display_name":"Starter 4 — Grand Ranger / Conqueror / Grand Arbalest","preset_id":"starter_04_grand_ranger_conqueror_grand_arbalest","main_deck_expanded":[{"card_id":"S1-ARC-002","card_name":"Poison Arrow","quantity":1},{"card_id":"S1-ARC-002","card_name":"Poison Arrow","quantity":1},{"card_id":"S1-ARC-003","card_name":"Escape Arrow","quantity":1},{"card_id":"S1-ARC-003","card_name":"Escape Arrow","quantity":1},{"card_id":"S1-ARC-005","card_name":"Reload","quantity":1},{"card_id":"S1-ARC-005","card_name":"Reload","quantity":1},{"card_id":"S1-ARC-008","card_name":"Ambush Shot","quantity":1},{"card_id":"S1-ARC-008","card_name":"Ambush Shot","quantity":1},{"card_id":"S1-ARC-010","card_name":"Arrow Barrage","quantity":1},{"card_id":"S1-ARC-010","card_name":"Arrow Barrage","quantity":1},{"card_id":"S1-ARC-011","card_name":"Back Step","quantity":1},{"card_id":"S1-ARC-011","card_name":"Back Step","quantity":1},{"card_id":"S1-ARC-012","card_name":"Deflect Arrow","quantity":1},{"card_id":"S1-ARC-014","card_name":"Replenish","quantity":1},{"card_id":"S1-ARC-016","card_name":"Soul Blast Shot","quantity":1},{"card_id":"S1-ARC-017","card_name":"Dual Arrow","quantity":1},{"card_id":"S1-ARC-018","card_name":"Charged Shot","quantity":1},{"card_id":"S1-ARC-019","card_name":"Long Range Shot","quantity":1},{"card_id":"S1-ARC-019","card_name":"Long Range Shot","quantity":1},{"card_id":"S1-ARC-020","card_name":"Piercing Bolt","quantity":1},{"card_id":"S1-ARC-021","card_name":"Aura Infusion Bolt","quantity":1},{"card_id":"S1-ARC-022","card_name":"Singularity Reload","quantity":1},{"card_id":"S1-ARC-023","card_name":"Cross Bolt","quantity":1},{"card_id":"S1-ARC-023","card_name":"Cross Bolt","quantity":1},{"card_id":"S1-ARC-024","card_name":"Colossal Dreadbolt","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-007","card_name":"Intercept","quantity":1},{"card_id":"S1-EVT-008","card_name":"Forged Alliance","quantity":1},{"card_id":"S1-EVT-010","card_name":"Déjà vu","quantity":1},{"card_id":"S1-ITM-001","card_name":"Health Potion","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-008","card_name":"Phoenix Feather","quantity":1},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":1},{"card_id":"S1-ITM-016","card_name":"Chain Mail","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-THF-026","card_name":"Dodge Instinct","quantity":1},{"card_id":"S1-THF-026","card_name":"Dodge Instinct","quantity":1},{"card_id":"S1-THF-027","card_name":"Camouflage","quantity":1},{"card_id":"S1-THF-027","card_name":"Camouflage","quantity":1},{"card_id":"S1-THF-029","card_name":"Flash Combo","quantity":1},{"card_id":"S1-THF-029","card_name":"Flash Combo","quantity":1},{"card_id":"S1-THF-030","card_name":"Crescent Moon Slash","quantity":1},{"card_id":"S1-WAR-004","card_name":"Cover Up","quantity":1},{"card_id":"S1-WAR-005","card_name":"Taunt","quantity":1},{"card_id":"S1-WAR-007","card_name":"Upward Thrust","quantity":1},{"card_id":"S1-WAR-007","card_name":"Upward Thrust","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":1},{"card_id":"S1-WAR-009","card_name":"Rage Swing","quantity":1},{"card_id":"S1-WAR-010","card_name":"Rage Blast","quantity":1},{"card_id":"S1-WAR-011","card_name":"Side Step","quantity":1},{"card_id":"S1-WAR-012","card_name":"Deflect","quantity":1},{"card_id":"S1-WAR-012","card_name":"Deflect","quantity":1},{"card_id":"S1-WAR-014","card_name":"Final Grit","quantity":1},{"card_id":"S1-WAR-015","card_name":"Charge Attack","quantity":1},{"card_id":"S1-WAR-016","card_name":"Whirlwind","quantity":1},{"card_id":"S1-WAR-017","card_name":"Fiery Thrust","quantity":1},{"card_id":"S1-WAR-017","card_name":"Fiery Thrust","quantity":1},{"card_id":"S1-WAR-018","card_name":"Execute","quantity":1}]}},"starter_05_elemental_lord_arcane_duelist_saint":{"label":"Starter 5 — Elemental Lord / Arcane Duelist / Saint","file":"starter_deck_examples/starter_05_elemental_lord_arcane_duelist_saint_GL_DECK_1_0.json","deck":{"schema_version":"GL-DECK-1.0","builder_version":"2.1-public-deck-builder","deck_name":"Starter 5 — Elemental Lord / Arcane Duelist / Saint","format":"One Source Authority v1.4 + Public Deck Builder v2.1","main_deck_count":60,"legacy_package_count":3,"legacy_deck_count":12,"legacy_deck_label":"Legacy Deck","legacy_deck_package_slots":[{"progression":"S1-CLE-H001","legacy":"S1-CLE-L002"},{"progression":"S1-THF-H004","legacy":"S1-THF-L002"},{"progression":"S1-MAG-H004","legacy":"S1-MAG-L002"}],"legacy_deck_expanded":[{"card_id":"S1-CLE-H001","card_name":"Elara Heavens","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-CLE-H002","card_name":"Elara Heavens","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-CLE-H003","card_name":"Elara Heavens","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-CLE-L002","card_name":"Wand of First Light","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-THF-H004","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H005","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H006","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-L002","card_name":"Hidden Archives","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-MAG-H004","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H005","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H006","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-L002","card_name":"Arcane Wand","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"}],"side_package_count":3,"side_deck_count":12,"side_deck_package_slots":[{"progression":"S1-CLE-H001","legacy":"S1-CLE-L002"},{"progression":"S1-THF-H004","legacy":"S1-THF-L002"},{"progression":"S1-MAG-H004","legacy":"S1-MAG-L002"}],"side_deck_expanded":[{"card_id":"S1-CLE-H001","card_name":"Elara Heavens","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-CLE-H002","card_name":"Elara Heavens","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-CLE-H003","card_name":"Elara Heavens","card_type":"Hero","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-CLE-L002","card_name":"Wand of First Light","card_type":"Legacy","package_id":"CUSTOM-SLOT-1","package_name":"Elf Cleric — ELARA + Wand of First Light"},{"card_id":"S1-THF-H004","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H005","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-H006","card_name":"Lucien Voss","card_type":"Hero","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-THF-L002","card_name":"Hidden Archives","card_type":"Legacy","package_id":"CUSTOM-SLOT-2","package_name":"Elf Thief — LUCIEN + Hidden Archives"},{"card_id":"S1-MAG-H004","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H005","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-H006","card_name":"Aldric Ashford","card_type":"Hero","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"},{"card_id":"S1-MAG-L002","card_name":"Arcane Wand","card_type":"Legacy","package_id":"CUSTOM-SLOT-3","package_name":"Human Mage — ALDRIC + Arcane Wand"}],"is_valid":true,"validation_issues":[],"validation_warnings":[],"main_deck":[{"card_id":"S1-CLE-007","card_name":"Blessing of Wisdom","quantity":2},{"card_id":"S1-CLE-009","card_name":"Holy Ring","quantity":1},{"card_id":"S1-CLE-010","card_name":"Holy Blast","quantity":1},{"card_id":"S1-CLE-011","card_name":"Holy Barrier","quantity":2},{"card_id":"S1-CLE-012","card_name":"Binding Light","quantity":2},{"card_id":"S1-CLE-014","card_name":"Wrathful Radiance","quantity":1},{"card_id":"S1-CLE-015","card_name":"Resurrection","quantity":2},{"card_id":"S1-CLE-017","card_name":"Lay on Hands","quantity":2},{"card_id":"S1-CLE-018","card_name":"Heaven’s Fury","quantity":1},{"card_id":"S1-EVT-001","card_name":"Begin Anew","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":2},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":2},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-008","card_name":"Forged Alliance","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":2},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":2},{"card_id":"S1-ITM-017","card_name":"Flashpowder Bomb","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":2},{"card_id":"S1-MAG-003","card_name":"Fireball","quantity":2},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":2},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":2},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":2},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":2},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":2},{"card_id":"S1-MAG-019","card_name":"Flame Dragon's Wrath","quantity":1},{"card_id":"S1-MAG-020","card_name":"Aether Ball","quantity":2},{"card_id":"S1-MAG-021","card_name":"Mana Absorption","quantity":2},{"card_id":"S1-MAG-022","card_name":"Aether Infusion","quantity":2},{"card_id":"S1-MAG-023","card_name":"Aether Slash","quantity":2},{"card_id":"S1-MAG-025","card_name":"Mana Void","quantity":1},{"card_id":"S1-THF-019","card_name":"Dash Slash","quantity":1},{"card_id":"S1-THF-020","card_name":"Sword Slash","quantity":2},{"card_id":"S1-THF-022","card_name":"Step In","quantity":2},{"card_id":"S1-THF-023","card_name":"Flash Slash","quantity":2},{"card_id":"S1-THF-024","card_name":"Sword Combo","quantity":2}],"default_formation":{"LEFT":"S1-MAG-H004","CENTER":"S1-THF-H004","RIGHT":"S1-CLE-H001"},"source_database_version":"One Source Authority v1.4 · registry b455b1537434e454ab873372bb9da715b779e64525a01a8bc3995ed9267ecfa0","builder_version_note":"Normalized for Grandis Legacy PvP v2.6.3 while preserving the approved deck contents, Legacy packages, and default Formation.","display_name":"Starter 5 — Elemental Lord / Arcane Duelist / Saint","preset_id":"starter_05_elemental_lord_arcane_duelist_saint","main_deck_expanded":[{"card_id":"S1-CLE-007","card_name":"Blessing of Wisdom","quantity":1},{"card_id":"S1-CLE-007","card_name":"Blessing of Wisdom","quantity":1},{"card_id":"S1-CLE-009","card_name":"Holy Ring","quantity":1},{"card_id":"S1-CLE-010","card_name":"Holy Blast","quantity":1},{"card_id":"S1-CLE-011","card_name":"Holy Barrier","quantity":1},{"card_id":"S1-CLE-011","card_name":"Holy Barrier","quantity":1},{"card_id":"S1-CLE-012","card_name":"Binding Light","quantity":1},{"card_id":"S1-CLE-012","card_name":"Binding Light","quantity":1},{"card_id":"S1-CLE-014","card_name":"Wrathful Radiance","quantity":1},{"card_id":"S1-CLE-015","card_name":"Resurrection","quantity":1},{"card_id":"S1-CLE-015","card_name":"Resurrection","quantity":1},{"card_id":"S1-CLE-017","card_name":"Lay on Hands","quantity":1},{"card_id":"S1-CLE-017","card_name":"Lay on Hands","quantity":1},{"card_id":"S1-CLE-018","card_name":"Heaven’s Fury","quantity":1},{"card_id":"S1-EVT-001","card_name":"Begin Anew","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-002","card_name":"Market Bargain","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-003","card_name":"Scouting","quantity":1},{"card_id":"S1-EVT-004","card_name":"Relentless Leveling","quantity":1},{"card_id":"S1-EVT-008","card_name":"Forged Alliance","quantity":1},{"card_id":"S1-EVT-009","card_name":"Tactical Adaptation","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-002","card_name":"Greater Health Potion","quantity":1},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":1},{"card_id":"S1-ITM-012","card_name":"Spectral Grappling Hook","quantity":1},{"card_id":"S1-ITM-017","card_name":"Flashpowder Bomb","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-ITM-018","card_name":"Magic Compass","quantity":1},{"card_id":"S1-MAG-003","card_name":"Fireball","quantity":1},{"card_id":"S1-MAG-003","card_name":"Fireball","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":1},{"card_id":"S1-MAG-005","card_name":"Mirror Image","quantity":1},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":1},{"card_id":"S1-MAG-007","card_name":"Tornado","quantity":1},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":1},{"card_id":"S1-MAG-009","card_name":"Fire Blast","quantity":1},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":1},{"card_id":"S1-MAG-016","card_name":"Nova Strike","quantity":1},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":1},{"card_id":"S1-MAG-017","card_name":"Frostfire Nova","quantity":1},{"card_id":"S1-MAG-019","card_name":"Flame Dragon's Wrath","quantity":1},{"card_id":"S1-MAG-020","card_name":"Aether Ball","quantity":1},{"card_id":"S1-MAG-020","card_name":"Aether Ball","quantity":1},{"card_id":"S1-MAG-021","card_name":"Mana Absorption","quantity":1},{"card_id":"S1-MAG-021","card_name":"Mana Absorption","quantity":1},{"card_id":"S1-MAG-022","card_name":"Aether Infusion","quantity":1},{"card_id":"S1-MAG-022","card_name":"Aether Infusion","quantity":1},{"card_id":"S1-MAG-023","card_name":"Aether Slash","quantity":1},{"card_id":"S1-MAG-023","card_name":"Aether Slash","quantity":1},{"card_id":"S1-MAG-025","card_name":"Mana Void","quantity":1},{"card_id":"S1-THF-019","card_name":"Dash Slash","quantity":1},{"card_id":"S1-THF-020","card_name":"Sword Slash","quantity":1},{"card_id":"S1-THF-020","card_name":"Sword Slash","quantity":1},{"card_id":"S1-THF-022","card_name":"Step In","quantity":1},{"card_id":"S1-THF-022","card_name":"Step In","quantity":1},{"card_id":"S1-THF-023","card_name":"Flash Slash","quantity":1},{"card_id":"S1-THF-023","card_name":"Flash Slash","quantity":1},{"card_id":"S1-THF-024","card_name":"Sword Combo","quantity":1},{"card_id":"S1-THF-024","card_name":"Sword Combo","quantity":1}]}}};
  var selectedDeckKey={PLAYER:'starter_01_elemental_lord_conqueror_renegade', AI:'starter_02_saint_crusader_grand_ranger'};
  var importedDecks={PLAYER:null, AI:null};
  var decks={PLAYER:clone(STARTER_DECK_OPTIONS[selectedDeckKey.PLAYER].deck), AI:clone(STARTER_DECK_OPTIONS[selectedDeckKey.AI].deck)};
  var SUPPRESS_RENDER=false;
  var matchStarted=false;
  var setupError='';
  var appState=null;
  var STARTUP_SHUFFLE_ENABLED=true;
  var selectedDiscardIndexes=[];
  function $(id){ return document.getElementById(id); }
  function clone(v){ return JSON.parse(JSON.stringify(v)); }
  var GL_ANIMATION_SEQUENCE=0, GL_ANIMATION_QUEUE=[], GL_ANIMATION_RUNNING=false;
  var GL_CARD_SOUND_STORAGE_KEY='grandis_legacy_card_motion_sound_v1';
  var GL_AUDIO_PRELOAD_CACHE={};
  var GL_CARD_SOUND_ENABLED=(function(){ try{ var v=window.localStorage&&window.localStorage.getItem(GL_CARD_SOUND_STORAGE_KEY); return v===null?true:v==='on'; }catch(e){ return true; } })();
  function primeAudioAsset(src){
    if(!src||typeof Audio==='undefined'||GL_AUDIO_PRELOAD_CACHE[src]) return GL_AUDIO_PRELOAD_CACHE[src]||null;
    try{var a=new Audio(src);a.preload='auto';if(typeof a.load==='function')a.load();GL_AUDIO_PRELOAD_CACHE[src]=a;return a;}catch(e){return null;}
  }
  function playPreloadedAudio(src,volume){
    if(!GL_CARD_SOUND_ENABLED||SUPPRESS_RENDER||typeof Audio==='undefined')return false;
    try{var base=primeAudioAsset(src),a=(base&&typeof base.cloneNode==='function')?base.cloneNode(true):new Audio(src);a.preload='auto';a.volume=Number(volume||.55);a.currentTime=0;var pr=a.play();if(pr&&typeof pr.catch==='function')pr.catch(function(){});return true;}catch(e){return false;}
  }
  function cardMotionSoundLabel(){ return GL_CARD_SOUND_ENABLED?'Sound ON':'Sound OFF'; }
  function setCardMotionSoundEnabled(enabled){ GL_CARD_SOUND_ENABLED=!!enabled; try{ if(window.localStorage) window.localStorage.setItem(GL_CARD_SOUND_STORAGE_KEY,GL_CARD_SOUND_ENABLED?'on':'off'); }catch(e){} ['soundToggleButton','mobileSoundToggleButton'].forEach(function(id){ var b=$(id); if(b){ b.textContent=cardMotionSoundLabel(); b.setAttribute('aria-pressed',GL_CARD_SOUND_ENABLED?'true':'false'); } }); }
  function toggleCardMotionSound(){ setCardMotionSoundEnabled(!GL_CARD_SOUND_ENABLED); }
  function playCardMotionSound(){
    if(!animationDocumentReady())return false;
    return playPreloadedAudio('assets/audio/freesound_community-flipcard-91468.mp3',.55);
  }

  var GL_BATTLE_FEEDBACK_QUEUE=[],GL_BATTLE_FEEDBACK_ACTIVE=0,GL_BATTLE_FEEDBACK_SEQUENCE=0,GL_LAST_BATTLE_AUDIO={src:'',at:0};
  var GL_BATTLE_VFX={
    pAttack:'assets/battle/P.Attack.png',
    mAttack:'assets/battle/M.Attack.png',
    pDefense:'assets/battle/P.Defense.png',
    mDefense:'assets/battle/M.Defense.png',
    heal:'assets/battle/Heal.png'
  };
  var GL_BATTLE_AUDIO={
    pAttack:'assets/audio/battle/P.Atk.mp3',
    mAttack:'assets/audio/battle/M.Atk.mp3',
    pDefense:'assets/audio/battle/P.Def.mp3',
    mDefense:'assets/audio/battle/M.Def.mp3',
    dodge:'assets/audio/battle/Dodge.mp3',
    heal:'assets/audio/battle/Heal.mp3'
  };
  function battleFeedbackBusy(){return !!(GL_BATTLE_FEEDBACK_ACTIVE||GL_BATTLE_FEEDBACK_QUEUE.length);}
  function battleAttackVisualKind(cardId){
    var c=card(cardId),sub=String(cardSubtype(c)||'');
    if(/Magical Attack/i.test(sub))return'M';
    if(/Physical Attack/i.test(sub))return'P';
    return currentDamageType(c)==='Magical'?'M':'P';
  }
  function battleDefenseVisualKind(hero){
    var family=String(baseFamilyForHero(hero)||'');
    return /^(Mage|Cleric)$/i.test(family)?'M':'P';
  }
  function battleHeroAnchor(side,lane){
    if(!animationDocumentReady())return null;
    return document.querySelector('.hero-panel[data-side="'+String(side||'')+'"][data-lane="'+String(lane||'')+'"] .hero-card-anchor')||
      document.querySelector('.hero-panel[data-side="'+String(side||'')+'"][data-lane="'+String(lane||'')+'"] .heroImg');
  }
  function battleHeroRect(side,lane){
    var anchor=battleHeroAnchor(side,lane);
    if(!anchor||typeof anchor.getBoundingClientRect!=='function')return null;
    var r=anchor.getBoundingClientRect();
    if(!r||!isFinite(r.left)||!isFinite(r.top)||r.width<1||r.height<1)return null;
    return{left:r.left,top:r.top,width:r.width,height:r.height};
  }
  function battleFeedbackLayer(){
    if(!animationDocumentReady()||!document.body)return null;
    var layer=document.getElementById('glBattleFeedbackLayer');
    if(!layer){
      layer=document.createElement('div');
      layer.id='glBattleFeedbackLayer';
      layer.className='gl-battle-feedback-layer';
      layer.setAttribute('aria-hidden','true');
      document.body.appendChild(layer);
    }
    return layer;
  }
  function battlePlayAudio(src,volume){
    if(!src)return false;
    var now=Date.now();
    if(GL_LAST_BATTLE_AUDIO.src===src&&now-GL_LAST_BATTLE_AUDIO.at<180)return false;
    GL_LAST_BATTLE_AUDIO={src:src,at:now};
    return playPreloadedAudio(src,volume||.58);
  }
  function battleVfxTier(cls){
    if(/pattack|mattack/.test(String(cls||'')))return 11854;
    if(/pdef|mdef/.test(String(cls||'')))return 11853;
    return 11852;
  }
  function battleVfxClip(rect,zIndex){
    var layer=battleFeedbackLayer();if(!layer||!rect)return null;
    var clip=document.createElement('div');
    clip.className='gl-battle-vfx-clip';
    clip.style.zIndex=String(zIndex||11852);
    clip.style.left=rect.left+'px';clip.style.top=rect.top+'px';clip.style.width=rect.width+'px';clip.style.height=rect.height+'px';
    layer.appendChild(clip);return clip;
  }
  function battleVfxNode(src,cls,rect,scale,clipToCard){
    var layer=battleFeedbackLayer();if(!layer||!rect)return null;
    var tier=battleVfxTier(cls);
    var host=clipToCard===false?layer:battleVfxClip(rect,tier);if(!host)return null;
    var node=document.createElement('img');
    node.src=src;node.className='gl-battle-vfx '+cls;node.style.zIndex=String(tier);
    if(clipToCard===false){
      var w=Math.max(54,rect.width*Number(scale||1.18)),h=Math.max(72,rect.height*Number(scale||1.18));
      node.style.left=(rect.left+rect.width/2-w/2)+'px';
      node.style.top=(rect.top+rect.height/2-h/2)+'px';
      node.style.width=w+'px';node.style.height=h+'px';
    }else{
      var insideScale=Math.max(.1,Number(scale||1));
      node.style.width=(insideScale*100)+'%';node.style.height=(insideScale*100)+'%';
      node.style.left=(((1-insideScale)*50))+'%';node.style.top=(((1-insideScale)*50))+'%';
    }
    node.draggable=false;host.appendChild(node);prepareImageForPaint(node);node._battleHost=host;return node;
  }
  function battleHealNode(src,cls,rect,side){
    var host=battleVfxClip(rect,11852);if(!host)return null;
    var node=document.createElement('img');node.src=src;node.className='gl-battle-vfx '+cls;
    node.style.width='46%';node.style.height='58%';node.style.top='34%';node.style.left=(side==='right'?'50%':'4%');
    node.draggable=false;host.appendChild(node);prepareImageForPaint(node);node._battleHost=host;return node;
  }
  function battleRemoveLater(node,ms){if(!node)return;setTimeout(function(){var host=node._battleHost;removeVisualNode(node);if(host&&host!==node&&host.parentNode)removeVisualNode(host);},Math.max(50,Number(ms||700)));}
  function battlePulseHeroClass(side,lane,cls,ms){
    var anchor=battleHeroAnchor(side,lane);if(!anchor)return false;
    anchor.classList.remove(cls);void anchor.offsetWidth;anchor.classList.add(cls);
    setTimeout(function(){try{anchor.classList.remove(cls);}catch(ignore){}},Math.max(100,Number(ms||650)));return true;
  }
  function runBattleFeedback(evt){
    if(!evt||SUPPRESS_RENDER||!animationDocumentReady())return false;
    var rect=battleHeroRect(evt.side,evt.lane);if(!rect)return false;
    GL_BATTLE_FEEDBACK_ACTIVE++;
    var duration=720,attackKind=evt.attack_kind||'P',attackSrc=attackKind==='M'?GL_BATTLE_VFX.mAttack:GL_BATTLE_VFX.pAttack;
    if(evt.kind==='heal'){
      duration=920;
      var h1=battleHealNode(GL_BATTLE_VFX.heal,'gl-battle-heal gl-battle-heal--left',rect,'left');
      var h2=battleHealNode(GL_BATTLE_VFX.heal,'gl-battle-heal gl-battle-heal--right',rect,'right');
      if(evt.play_sound!==false)battlePlayAudio(GL_BATTLE_AUDIO.heal,.52);
      battleRemoveLater(h1,duration);battleRemoveLater(h2,duration+140);
    }else if(evt.outcome==='dodge'){
      duration=720;
      var dodgeAttack=battleVfxNode(attackSrc,attackKind==='M'?'gl-battle-mattack':'gl-battle-pattack',rect,.72,true);
      battleRemoveLater(dodgeAttack,650);
      battlePulseHeroClass(evt.side,evt.lane,'gl-battle-dodge-card',duration);
      if(evt.play_sound!==false)battlePlayAudio(GL_BATTLE_AUDIO.dodge,.62);
    }else{
      var attackNode=battleVfxNode(attackSrc,attackKind==='M'?'gl-battle-mattack':'gl-battle-pattack',rect,.72,true);
      battleRemoveLater(attackNode,650);
      if(evt.has_damage)battlePulseHeroClass(evt.side,evt.lane,'gl-battle-damage-card',520);
      if(evt.outcome==='block'){
        duration=820;
        var defKind=evt.defense_kind||'P',defSrc=defKind==='M'?GL_BATTLE_VFX.mDefense:GL_BATTLE_VFX.pDefense;
        setTimeout(function(){
          var fresh=battleHeroRect(evt.side,evt.lane)||rect;
          var isMagicDefense=defKind==='M';
          var defNode=battleVfxNode(defSrc,isMagicDefense?'gl-battle-mdef':'gl-battle-pdef',fresh,1.08,isMagicDefense?false:true);
          battleRemoveLater(defNode,640);
        },105);
        if(evt.play_sound!==false)battlePlayAudio(defKind==='M'?GL_BATTLE_AUDIO.mDefense:GL_BATTLE_AUDIO.pDefense,.6);
      }else{
        if(evt.play_sound!==false)battlePlayAudio(attackKind==='M'?GL_BATTLE_AUDIO.mAttack:GL_BATTLE_AUDIO.pAttack,.6);
      }
    }
    setTimeout(function(){
      GL_BATTLE_FEEDBACK_ACTIVE=Math.max(0,GL_BATTLE_FEEDBACK_ACTIVE-1);
      if(!battleFeedbackBusy()&&appState&&matchStarted&&!SUPPRESS_RENDER)render();
    },duration+40);
    return true;
  }
  function queueBattleFeedback(evt){
    if(!evt||SUPPRESS_RENDER)return false;
    evt=Object.assign({id:++GL_BATTLE_FEEDBACK_SEQUENCE,play_sound:true},evt);
    GL_BATTLE_FEEDBACK_QUEUE.push(evt);return true;
  }
  function flushBattleFeedbackQueue(){
    if(!GL_BATTLE_FEEDBACK_QUEUE.length||SUPPRESS_RENDER||!animationDocumentReady())return false;
    var items=GL_BATTLE_FEEDBACK_QUEUE.splice(0);
    items.forEach(function(evt,index){setTimeout(function(){var ok=runBattleFeedback(evt);if(!ok&&!battleFeedbackBusy()&&appState&&matchStarted&&!SUPPRESS_RENDER)render();},index*130);});
    return true;
  }
  function queueResolvedAttackFeedback(state,rw,result,responseKindChosen,responseOption,block,dodged,targetLane,playSound){
    if(!state||!rw)return false;
    var lane=targetLane||rw.target_lane,target=sideHeroes(state,rw.target_side)[lane];if(!target)return false;
    var blockLike=!!responseOption&&!dodged&&(
      responseKindChosen==='block'||responseKindChosen==='area_reduce'||responseKindChosen==='immunity'||
      responseKindChosen==='negate_return'||responseKindChosen==='dragon_scale'||responseKindChosen==='legacy_reduce'||
      Number(block||0)>0
    );
    return queueBattleFeedback({
      kind:'attack',outcome:dodged?'dodge':(blockLike?'block':'hit'),
      side:rw.target_side,lane:lane,card_id:rw.card_id,
      attack_kind:battleAttackVisualKind(rw.card_id),
      defense_kind:blockLike?battleDefenseVisualKind(target):null,
      has_damage:Number(result&&result.hp_damage||result&&result.damage||0)>0,
      play_sound:playSound!==false
    });
  }
  function queueHealFeedback(side,lane){
    return queueBattleFeedback({kind:'heal',side:side,lane:lane,play_sound:true});
  }
  function primeBattleFeedbackAssets(){
    Object.keys(GL_BATTLE_VFX).forEach(function(k){preloadVisualAsset(GL_BATTLE_VFX[k]);});
    Object.keys(GL_BATTLE_AUDIO).forEach(function(k){primeAudioAsset(GL_BATTLE_AUDIO[k]);});
  }
  var GL_AI_TURN_DIRECTOR={active:false,token:0,timer:null};
  function aiTurnDirectorAvailable(){ return !SUPPRESS_RENDER && !window.GL_PVP_SHARED_BOARD_ACTIVE && typeof window!=='undefined' && typeof setTimeout==='function'; }
  function cancelAITurnDirector(){ GL_AI_TURN_DIRECTOR.token++; GL_AI_TURN_DIRECTOR.active=false; if(GL_AI_TURN_DIRECTOR.timer){ clearTimeout(GL_AI_TURN_DIRECTOR.timer); GL_AI_TURN_DIRECTOR.timer=null; } if(appState) appState.aiPresentationStatus=''; }
  function aiDirectorSchedule(fn,delay){ var token=GL_AI_TURN_DIRECTOR.token; if(GL_AI_TURN_DIRECTOR.timer) clearTimeout(GL_AI_TURN_DIRECTOR.timer); GL_AI_TURN_DIRECTOR.timer=setTimeout(function(){ GL_AI_TURN_DIRECTOR.timer=null; if(!GL_AI_TURN_DIRECTOR.active || token!==GL_AI_TURN_DIRECTOR.token) return; fn(); },Math.max(0,Number(delay||0))); }
  function aiDirectorStatus(state,text){ if(!state) return; state.aiPresentationStatus=text||''; if(!SUPPRESS_RENDER) render(); }
  function aiDirectorWaitForAnimations(state,next,settleDelay){ var token=GL_AI_TURN_DIRECTOR.token; function check(){ if(!GL_AI_TURN_DIRECTOR.active || token!==GL_AI_TURN_DIRECTOR.token) return; if(state.pending || state.responseWindow) return; if(animationBusy()||battleFeedbackBusy()){ GL_AI_TURN_DIRECTOR.timer=setTimeout(check,80); return; } aiDirectorSchedule(next,typeof settleDelay==='number'?settleDelay:320); } check(); }
  function animationBusy(){ return !!(GL_ANIMATION_RUNNING || GL_ANIMATION_QUEUE.length); }
  function gameplayInputLocked(){return animationBusy()||!!(appState&&appState.preGame);}
  var GL_HELD_CARD_VISUALS={}, GL_PENDING_HELD_RELEASES={};
  var GL_ANIMATION_SCROLL_LOCKED=false, GL_PAGE_SCROLL_LOCK_REQUESTED=false;
  function setPageAnimationScrollLock(requested){
    GL_PAGE_SCROLL_LOCK_REQUESTED=!!requested;
    syncAnimationScrollLock();
  }
  function syncAnimationScrollLock(){
    if(!animationDocumentReady() || typeof document.documentElement==='undefined') return;
    /* Only the opening-hand batch after the coin flip owns the whole-page
       mobile viewport lock. Tribute, Rank Up, mandatory draw, and effect draw
       animations preserve the current page position. */
    var shouldLock=animationBusy()&&GL_PAGE_SCROLL_LOCK_REQUESTED;
    if(shouldLock===GL_ANIMATION_SCROLL_LOCKED) return;
    GL_ANIMATION_SCROLL_LOCKED=shouldLock;
    document.documentElement.classList.toggle('gl-animation-scroll-locked',shouldLock);
    if(document.body) document.body.classList.toggle('gl-animation-scroll-locked',shouldLock);
  }
  var GL_HIDDEN_DRAW_TOKENS={PLAYER:[],AI:[]};
  function animationDocumentReady(){ return typeof document!=='undefined' && document && typeof document.querySelector==='function' && typeof document.createElement==='function'; }
  function captureVisualRect(selector){
    if(!animationDocumentReady() || !selector) return null;
    try{ var el=document.querySelector(selector); if(!el || typeof el.getBoundingClientRect!=='function') return null; var r=el.getBoundingClientRect(); if(!r || !isFinite(r.left) || !isFinite(r.top)) return null; return {left:r.left,top:r.top,width:Math.max(1,r.width||70),height:Math.max(1,r.height||98)}; }catch(e){ return null; }
  }
  function visualRectCenter(r){ return r?{x:r.left+r.width/2,y:r.top+r.height/2}:null; }
  function captureAIHandAnimationRect(){
    return captureVisualRect('.hand-area--opponent .opponent-hand .back:last-child') ||
      captureVisualRect('.hand-area--opponent .opponent-hand') ||
      captureVisualRect('.hand-area--opponent');
  }
  function visualLayer(){
    if(!animationDocumentReady()) return null;
    var layer=document.getElementById('glAnimationLayer');
    if(!layer && document.body){ layer=document.createElement('div'); layer.id='glAnimationLayer'; layer.className='gl-animation-layer'; layer.setAttribute('aria-hidden','true'); document.body.appendChild(layer); }
    return layer;
  }
  function removeVisualNode(node){ try{ if(node&&node.parentNode) node.parentNode.removeChild(node); }catch(e){} }
  function runVisualItemCallback(item){ if(item&&typeof item.onFinish==='function'){ try{ item.onFinish(); }catch(e){} } }
  function runVisualAnimationQueue(){
    if(GL_ANIMATION_RUNNING || !GL_ANIMATION_QUEUE.length) return;
    var item=GL_ANIMATION_QUEUE.shift(), layer=visualLayer();
    if(item&&item.parallel_items&&item.parallel_items.length){ runParallelVisualAnimationItem(item,layer); return; }
    if(!layer || !item || !item.from || !item.steps || !item.steps.length){ GL_ANIMATION_RUNNING=false; syncAnimationScrollLock(); runVisualItemCallback(item); if(appState&&matchStarted&&!SUPPRESS_RENDER) render(); setTimeout(runVisualAnimationQueue,0); return; }
    GL_ANIMATION_RUNNING=true; syncAnimationScrollLock(); if(appState&&matchStarted&&!SUPPRESS_RENDER) render();
    var img=document.createElement('img'); img.className='gl-flying-card'; img.src=item.src||'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp'; img.alt=''; img.dataset.animationId=String(item.id);
    var w=Math.min(86,Math.max(54,item.from.width||72)), h=Math.round(w*1.4); img.style.width=w+'px'; img.style.height=h+'px'; img.style.left=(item.from.left+(item.from.width-w)/2)+'px'; img.style.top=(item.from.top+(item.from.height-h)/2)+'px'; layer.appendChild(img);
    var origin=visualRectCenter({left:parseFloat(img.style.left)||0,top:parseFloat(img.style.top)||0,width:w,height:h});
    var frames=[{transform:'translate(0px,0px) scale(1)',opacity:1}], lastRect=item.from;
    item.steps.forEach(function(step,idx){ var c=visualRectCenter(step.rect); if(!c) return; lastRect=step.rect; var isLast=idx===item.steps.length-1; frames.push({transform:'translate('+(c.x-origin.x)+'px,'+(c.y-origin.y)+'px) scale('+(isLast?(item.hold_key?.88:.78):.92)+')',opacity:isLast?(item.hold_key?1:.12):1,offset:(idx+1)/item.steps.length}); });
    var total=Math.max(220,Number(item.duration||520));
    function done(){
      if(item.hold_key){
        img.classList.add('gl-held-card');
        GL_HELD_CARD_VISUALS[item.hold_key]={img:img,src:item.src,rect:lastRect};
      } else removeVisualNode(img);
      GL_ANIMATION_RUNNING=false; syncAnimationScrollLock(); runVisualItemCallback(item); if(appState&&matchStarted&&!SUPPRESS_RENDER) render();
      if(item.hold_key && GL_PENDING_HELD_RELEASES[item.hold_key]){ var dest=GL_PENDING_HELD_RELEASES[item.hold_key]; delete GL_PENDING_HELD_RELEASES[item.hold_key]; setTimeout(function(){ releaseHeldCardMotion(item.hold_key,dest); },0); }
      setTimeout(runVisualAnimationQueue,10);
    }
    function startVisualAnimation(){
      if(item.play_sound!==false) playCardMotionSound();
      if(typeof img.animate==='function'){ try{ var a=img.animate(frames,{duration:total,easing:'cubic-bezier(.2,.75,.2,1)',fill:'forwards'}); a.onfinish=done; a.oncancel=done; return; }catch(e){} }
      var last=frames[frames.length-1]; img.style.transition='transform '+total+'ms cubic-bezier(.2,.75,.2,1), opacity '+total+'ms ease';
      img.style.transform=last.transform; img.style.opacity=String(last.opacity); setTimeout(done,total+40);
    }
    prepareImageForPaint(img).then(function(){nextVisualFrame(startVisualAnimation);});
  }
  function queueVisualCardMotion(src,from,steps,duration,options){
    options=options||{}; var filtered=(steps||[]).filter(function(x){return x&&x.rect;});
    if(!from || !filtered.length){ if(typeof options.onFinish==='function') options.onFinish(); return false; }
    GL_ANIMATION_QUEUE.push({id:++GL_ANIMATION_SEQUENCE,src:src,from:from,steps:filtered,duration:duration||560,hold_key:options.hold_key||null,onFinish:options.onFinish||null,play_sound:options.play_sound!==false}); syncAnimationScrollLock(); if(appState&&matchStarted&&!SUPPRESS_RENDER) render(); setTimeout(runVisualAnimationQueue,0); return true;
  }
  function runParallelVisualAnimationItem(item,layer){
    if(!layer||!item){GL_ANIMATION_RUNNING=false;syncAnimationScrollLock();runVisualItemCallback(item);setTimeout(runVisualAnimationQueue,0);return;}
    GL_ANIMATION_RUNNING=true;syncAnimationScrollLock();if(appState&&matchStarted&&!SUPPRESS_RENDER)render();
    var parts=(item.parallel_items||[]).filter(function(x){return x&&x.from&&x.to;}),remaining=parts.length,total=Math.max(220,Number(item.duration||390));
    if(!remaining){GL_ANIMATION_RUNNING=false;syncAnimationScrollLock();runVisualItemCallback(item);setTimeout(runVisualAnimationQueue,0);return;}
    function oneDone(img){removeVisualNode(img);remaining--;if(remaining>0)return;GL_ANIMATION_RUNNING=false;syncAnimationScrollLock();runVisualItemCallback(item);if(appState&&matchStarted&&!SUPPRESS_RENDER)render();setTimeout(runVisualAnimationQueue,10);}
    var prepared=parts.map(function(part,idx){
      var img=document.createElement('img');img.className='gl-flying-card';img.src=part.src||'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp';img.alt='';img.dataset.animationId=String(item.id)+'-'+idx;
      var w=Math.min(86,Math.max(54,part.from.width||72)),h=Math.round(w*1.4);img.style.width=w+'px';img.style.height=h+'px';img.style.left=(part.from.left+(part.from.width-w)/2)+'px';img.style.top=(part.from.top+(part.from.height-h)/2)+'px';layer.appendChild(img);
      var origin=visualRectCenter({left:parseFloat(img.style.left)||0,top:parseFloat(img.style.top)||0,width:w,height:h}),dest=visualRectCenter(part.to),frames=[{transform:'translate(0px,0px) scale(1)',opacity:1},{transform:'translate('+(dest.x-origin.x)+'px,'+(dest.y-origin.y)+'px) scale(.78)',opacity:.12}];
      return {img:img,frames:frames,ready:prepareImageForPaint(img)};
    });
    Promise.all(prepared.map(function(entry){return entry.ready;})).then(function(){nextVisualFrame(function(){
      if(item.play_sound!==false) playCardMotionSound();
      prepared.forEach(function(entry){
        var img=entry.img,frames=entry.frames,doneCalled=false;
        function done(){if(doneCalled)return;doneCalled=true;oneDone(img);}
        if(typeof img.animate==='function'){try{var a=img.animate(frames,{duration:total,easing:'cubic-bezier(.2,.75,.2,1)',fill:'forwards'});a.onfinish=done;a.oncancel=done;return;}catch(e){}}
        img.style.transition='transform '+total+'ms cubic-bezier(.2,.75,.2,1), opacity '+total+'ms ease';img.style.transform=frames[1].transform;img.style.opacity='.12';setTimeout(done,total+40);
      });
    });});
  }
  function queueParallelVisualCardMotions(items,duration,options){options=options||{};var valid=(items||[]).filter(function(x){return x&&x.from&&x.to;});if(!valid.length){if(typeof options.onFinish==='function')options.onFinish();return false;}GL_ANIMATION_QUEUE.push({id:++GL_ANIMATION_SEQUENCE,parallel_items:valid,duration:duration||390,onFinish:options.onFinish||null,play_sound:options.play_sound!==false});syncAnimationScrollLock();if(appState&&matchStarted&&!SUPPRESS_RENDER)render();setTimeout(runVisualAnimationQueue,0);return true;}

  function multiTargetAttackAnimationUsesSource(cardId,action){
    var c=card(cardId);
    if(!c || !isAttackCard(c)) return false;
    if(action && action.triple_shot_area) return true;
    if(action && Array.isArray(action.target_lanes) && action.target_lanes.length>1) return true;
    return !isSingleTargetAttack(c);
  }
  function capturePlayedCardMotion(cardId,side,action){
    if(!animationDocumentReady()) return null;
    action=action||{};
    var from=side==='PLAYER'?captureVisualRect('.hand-card[data-hand-index="'+Number(action.hand_index)+'"] .hand-art'):captureAIHandAnimationRect();
    var sourceAnchor=multiTargetAttackAnimationUsesSource(cardId,action);
    var sourceTarget=(action.source_side&&action.source_lane)?captureVisualRect('.hero-panel[data-side="'+action.source_side+'"][data-lane="'+action.source_lane+'"] .heroImg'):null;
    var targetLane=action.target_lane||((action.target_lanes&&action.target_lanes.length)?action.target_lanes[0]:null);
    var target=sourceAnchor?sourceTarget:((action.target_side&&targetLane)?captureVisualRect('.hero-panel[data-side="'+action.target_side+'"][data-lane="'+targetLane+'"] .heroImg'):null);
    if(!target && cardFamily(card(cardId))==='Event') target=captureVisualRect('.controls') || captureVisualRect('#app');
    if(!target) target=sourceTarget;
    var discard=captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Discard Pile"] .zoneCard');
    return {card_id:cardId,side:side,from:from,target:target,discard:discard,animation_anchor:sourceAnchor?'SOURCE':'TARGET'};
  }
  function commitPlayedCardMotion(snapshot,persistent,hostSide,hostLane,slot){
    if(!snapshot || !snapshot.from) return false;
    var end=persistent?captureVisualRect('.slot[data-attachment-side="'+hostSide+'"][data-attachment-lane="'+hostLane+'"][data-attachment-slot="'+Number(slot)+'"]'):snapshot.discard;
    var steps=[]; if(snapshot.target) steps.push({rect:snapshot.target}); if(end) steps.push({rect:end});
    return queueVisualCardMotion(thumbFor(snapshot.card_id),snapshot.from,steps,steps.length>1?620:430);
  }
  function commitAuthoritativePlayedCardMotion(snapshot,destination){
    if(!snapshot || !snapshot.from) return false;
    destination=destination||{type:'target'};
    var end=null;
    if(destination.type==='attachment') end=captureVisualRect('.slot[data-attachment-side="'+destination.side+'"][data-attachment-lane="'+destination.lane+'"][data-attachment-slot="'+Number(destination.slot||0)+'"]');
    else if(destination.type==='discard') end=captureVisualRect('[data-zone-side="'+destination.side+'"][data-zone-type="Discard Pile"] .zoneCard');
    else if(destination.type==='hand') end=destination.side==='PLAYER'?captureVisualRect('.handPanel'):captureVisualRect('.strip .backs');
    var steps=[]; if(snapshot.target) steps.push({rect:snapshot.target}); if(end) steps.push({rect:end});
    if(!steps.length) return false;
    return queueVisualCardMotion(thumbFor(snapshot.card_id),snapshot.from,steps,steps.length>1?620:430);
  }
  function beginHeldTargetCardMotion(snapshot,holdKey){
    if(!snapshot || !snapshot.from || !snapshot.target || !holdKey) return false;
    return queueVisualCardMotion(thumbFor(snapshot.card_id),snapshot.from,[{rect:snapshot.target}],420,{hold_key:holdKey});
  }
  function heldDestinationRect(destination){
    destination=destination||{};
    if(destination.type==='hand') return destination.side==='PLAYER'?captureVisualRect('.handPanel'):captureVisualRect('.strip .backs');
    if(destination.type==='attachment') return captureVisualRect('.slot[data-attachment-side="'+destination.side+'"][data-attachment-lane="'+destination.lane+'"][data-attachment-slot="'+Number(destination.slot)+'"]');
    if(destination.type==='hero') return captureVisualRect('.hero-panel[data-side="'+destination.side+'"][data-lane="'+destination.lane+'"] .heroImg');
    return captureVisualRect('[data-zone-side="'+destination.side+'"][data-zone-type="Discard Pile"] .zoneCard');
  }
  function releaseHeldCardMotion(holdKey,destination){
    if(!holdKey) return false;
    var held=GL_HELD_CARD_VISUALS[holdKey];
    if(!held){ GL_PENDING_HELD_RELEASES[holdKey]=destination||{}; return false; }
    delete GL_HELD_CARD_VISUALS[holdKey];
    var from=held.rect;
    try{ if(held.img&&typeof held.img.getBoundingClientRect==='function'){ var r=held.img.getBoundingClientRect(); if(r&&isFinite(r.left)) from={left:r.left,top:r.top,width:Math.max(1,r.width||70),height:Math.max(1,r.height||98)}; } }catch(e){}
    removeVisualNode(held.img);
    var to=heldDestinationRect(destination);
    if(!to){ syncAnimationScrollLock(); return false; }
    var queued=queueVisualCardMotion(held.src,from,[{rect:to}],360); syncAnimationScrollLock(); return queued;
  }
  function nextAnimationHoldKey(prefix,cardId){ return String(prefix||'card')+'-'+String(cardId||'unknown')+'-'+(++GL_ANIMATION_SEQUENCE); }
  function addHiddenDrawToken(side,cardId,handIndex,eventId){var token={token:'draw-'+(++GL_ANIMATION_SEQUENCE),card_id:cardId,hand_index:Number(handIndex),event_id:eventId||null};(GL_HIDDEN_DRAW_TOKENS[side]||(GL_HIDDEN_DRAW_TOKENS[side]=[])).push(token);return token;}
  function removeHiddenDrawToken(side,token,options){options=options||{};var list=GL_HIDDEN_DRAW_TOKENS[side]||[];GL_HIDDEN_DRAW_TOKENS[side]=list.filter(function(x){return x.token!==token.token;});if(!options.suppressRender&&appState&&matchStarted&&!SUPPRESS_RENDER)render();}
  function hiddenTokenByIndex(side,index){return (GL_HIDDEN_DRAW_TOKENS[side]||[]).find(function(x){return Number(x.hand_index)===Number(index);})||null;}
  function presentationHandEntries(side,hand){hand=hand||[];return hand.map(function(id,index){var tok=hiddenTokenByIndex(side,index);return{id:id,index:index,hidden:!!tok,token:tok};});}
  function presentationHandCount(side,hand){return (hand||[]).length;}
  function fanMetrics(index,count,side){count=Math.max(1,Number(count||1));index=Math.max(0,Number(index||0));var max=count<=1?0:(count<=3?2.5:(count<=6?4:(count<=8?3:2)));var t=count<=1?0:(index/(count-1))*2-1;var angle=t*max,y=Math.abs(t)*(count<=6?3:2),overlap=count<=3?-5:(count<=6?-11:(count<=8?-16:-20));if(side==='AI')y=-y;return{angle:angle,y:y,overlap:overlap,z:index+1};}
  function fanStyle(index,count,side){if(side==='PLAYER'&&isMobileViewport())return'--fan-angle:0deg;--fan-y:0px;--fan-overlap:0px;--fan-z:'+(index+1)+';';var f=fanMetrics(index,count,side);return'--fan-angle:'+f.angle.toFixed(2)+'deg;--fan-y:'+f.y.toFixed(2)+'px;--fan-overlap:'+f.overlap+'px;--fan-z:'+f.z+';';}
  function captureHandSlotRect(side,index){
    return captureVisualRect('[data-hand-side="'+side+'"][data-hand-slot-index="'+Number(index)+'"]')||(side==='PLAYER'?captureVisualRect('.handPanel'):captureVisualRect('.strip .backs'));
  }
  function isMobileViewport(){return typeof window!=='undefined'&&window.matchMedia&&window.matchMedia('(max-width: 760px)').matches;}
  var GL_LOCAL_MOBILE_HAND_SCROLL_LEFT=0;
  function localMobileHandScroller(){if(typeof document==='undefined')return null;return document.querySelector('.v96-app .hand-area--player .handPanel')||document.querySelector('.hand-area--player .handPanel');}
  function captureLocalMobileHandScroll(){if(!isMobileViewport())return null;var hand=localMobileHandScroller();if(!hand)return null;GL_LOCAL_MOBILE_HAND_SCROLL_LEFT=Number(hand.scrollLeft||0);return{left:GL_LOCAL_MOBILE_HAND_SCROLL_LEFT};}
  function restoreLocalMobileHandScroll(saved){if(!isMobileViewport())return false;var desired=Number(saved&&saved.left);if(!Number.isFinite(desired))desired=Number(GL_LOCAL_MOBILE_HAND_SCROLL_LEFT||0);function apply(){var hand=localMobileHandScroller();if(!hand)return;var max=Math.max(0,Number(hand.scrollWidth||0)-Number(hand.clientWidth||0));hand.scrollLeft=Math.max(0,Math.min(max,desired));GL_LOCAL_MOBILE_HAND_SCROLL_LEFT=Number(hand.scrollLeft||0);}apply();if(typeof requestAnimationFrame==='function')requestAnimationFrame(function(){requestAnimationFrame(apply);});else setTimeout(apply,0);return true;}
  function bindLocalMobileHandScroll(){var hand=localMobileHandScroller();if(!hand||hand.getAttribute('data-local-scroll-bound')==='1')return false;hand.setAttribute('data-local-scroll-bound','1');hand.addEventListener('scroll',function(){GL_LOCAL_MOBILE_HAND_SCROLL_LEFT=Number(hand.scrollLeft||0);},{passive:true});return true;}
  function focusMobilePlayerHand(options){
    options=options||{};if(!isMobileViewport()||typeof document==='undefined')return false;
    var hand=document.querySelector('.v96-app .hand-area--player .handPanel')||document.querySelector('.hand-area--player .handPanel');
    if(!hand)return false;
    var targetIndex=Number(options.targetIndex),hasTarget=Number.isFinite(targetIndex);
    var movePage=options.ensureVisible===true;
    function apply(){
      /* Whole-page movement is reserved for the opening-hand batch after the
         coin flip. Mid-match draws preserve the current Hero/action context. */
      if(movePage){
        try{hand.scrollIntoView({behavior:options.smooth?'smooth':'auto',block:'end',inline:'nearest'});}catch(e){try{window.scrollTo(0,Math.max(0,document.documentElement.scrollHeight-window.innerHeight));}catch(ignore){}}
      }
      var left=Math.max(0,hand.scrollWidth-hand.clientWidth);
      if(hasTarget){
        var slot=hand.querySelector('[data-hand-side="PLAYER"][data-hand-slot-index="'+targetIndex+'"]');
        if(slot)left=Math.max(0,slot.offsetLeft+slot.offsetWidth-hand.clientWidth+10);
      }
      try{hand.scrollLeft=left;}catch(ignore2){}
    }
    apply();
    if(typeof requestAnimationFrame==='function')requestAnimationFrame(function(){requestAnimationFrame(apply);});else setTimeout(apply,0);
    return true;
  }
  function queueDrawMotion(side,cardId,handIndex,options){options=options||{};if(!animationDocumentReady())return false;var from=captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Main Deck"] .zoneCard');if(!from)return false;var token=addHiddenDrawToken(side,cardId,handIndex,options.event_id);if(appState&&matchStarted&&!SUPPRESS_RENDER)render();var to=captureHandSlotRect(side,handIndex);if(!to){removeHiddenDrawToken(side,token);return false;}return queueVisualCardMotion('https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp',from,[{rect:to}],Number(options.duration||390),{play_sound:options.play_sound!==false,onFinish:function(){removeHiddenDrawToken(side,token);if(typeof options.onFinish==='function')options.onFinish();}});}
  function queueDrawEvents(events,state,options){
    options=options||{};
    events=(events||[]).filter(function(e){return e&&e.type==='CARD_DRAWN';});
    var openingViewportBatch=events.some(function(e){return e&&e.side==='PLAYER'&&e.reason==='OPENING_HAND';});
    var latestPlayerDraw=events.filter(function(e){return e&&e.side==='PLAYER';}).sort(function(a,b){return Number(a.hand_index)-Number(b.hand_index);}).pop()||null;
    function finishSequence(){
      if(latestPlayerDraw)focusMobilePlayerHand({targetIndex:Number(latestPlayerDraw.hand_index),ensureVisible:false});
      if(typeof window!=='undefined'&&typeof window.GL_PVP_NOTIFY_DRAW_COMPLETE==='function'&&latestPlayerDraw){try{window.GL_PVP_NOTIFY_DRAW_COMPLETE(Number(latestPlayerDraw.hand_index));}catch(ignore){}}
      if(openingViewportBatch)setPageAnimationScrollLock(false);
      if(typeof options.onComplete==='function'){try{options.onComplete();}catch(e){}}
    }
    if(!events.length||!animationDocumentReady()){finishSequence();return false;}
    if(openingViewportBatch)setPageAnimationScrollLock(true);
    var tokenById={};
    events.forEach(function(e){tokenById[e.id]=addHiddenDrawToken(e.side,e.card_id,e.hand_index,e.id);});
    if(appState&&matchStarted&&!SUPPRESS_RENDER)render();
    var playerEvents=events.filter(function(e){return e.side==='PLAYER';});
    var openingPlayerEvents=playerEvents.filter(function(e){return e.reason==='OPENING_HAND';});
    if(openingPlayerEvents.length)focusMobilePlayerHand({targetIndex:Number(openingPlayerEvents[0].hand_index),ensureVisible:true});
    var groups=[],byKey={};
    events.forEach(function(e){
      var key=(e.reason==='OPENING_HAND'&&e.group_id)?String(e.group_id):('SEQ:'+String(e.id));
      if(!byKey[key]){byKey[key]=[];groups.push(byKey[key]);}
      byKey[key].push(e);
    });
    var queued=false,index=0;
    function revealGroup(group){
      group.forEach(function(e){removeHiddenDrawToken(e.side,tokenById[e.id],{suppressRender:true});});
      if(appState&&matchStarted&&!SUPPRESS_RENDER)render();
      var latest=group.filter(function(e){return e.side==='PLAYER';}).sort(function(a,b){return Number(a.hand_index)-Number(b.hand_index);}).pop();
      if(latest)focusMobilePlayerHand({targetIndex:Number(latest.hand_index),ensureVisible:false});
    }
    function runNext(){
      if(index>=groups.length){finishSequence();return;}
      var group=groups[index++],latest=group.filter(function(e){return e.side==='PLAYER';}).sort(function(a,b){return Number(a.hand_index)-Number(b.hand_index);}).pop();
      function animateVisibleTarget(){
        var parts=group.map(function(e){return{event:e,src:'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp',from:captureVisualRect('[data-zone-side="'+e.side+'"][data-zone-type="Main Deck"] .zoneCard'),to:captureHandSlotRect(e.side,e.hand_index)};});
        function done(){revealGroup(group);setTimeout(runNext,50);}
        if(parts.every(function(part){return part.from&&part.to;})){
          if(parts.length>1)queueParallelVisualCardMotions(parts.map(function(part){return{src:part.src,from:part.from,to:part.to};}),360,{play_sound:true,onFinish:done});
          else queueVisualCardMotion(parts[0].src,parts[0].from,[{rect:parts[0].to}],360,{play_sound:true,onFinish:done});
          queued=true;
        }else done();
      }
      if(latest&&isMobileViewport()){
        var openingGroup=group.some(function(e){return e&&e.reason==='OPENING_HAND';});
        focusMobilePlayerHand({targetIndex:Number(latest.hand_index),ensureVisible:openingGroup});
        nextVisualFrame(function(){nextVisualFrame(animateVisibleTarget);});
      }else animateVisibleTarget();
    }
    if(playerEvents.length&&isMobileViewport())nextVisualFrame(function(){nextVisualFrame(runNext);});else runNext();
    return queued;
  }

  function captureAuthoritativeDrawMotions(side,cardIds,count){var hand=sideHand(appState||{},side)||[],ids=Array.isArray(cardIds)?cardIds.slice():[],n=Math.max(1,Number(count||ids.length||1)),out=[];while(ids.length<n)ids.push('__HIDDEN_CARD_BACK__');for(var i=0;i<n;i++)out.push({type:'CARD_DRAWN',id:'external-'+(++GL_ANIMATION_SEQUENCE),side:side,card_id:ids[i],hand_index:Math.max(0,hand.length-n+i),reason:'CARD_EFFECT'});return{events:out};}
  function queueCapturedAuthoritativeDrawMotions(plan){return queueDrawEvents(plan&&plan.events||[],appState);}
  function captureAuthoritativeRankUpMotion(side,lane,toCardId,expCardIds){
    if(!animationDocumentReady())return null;
    return {side:side,lane:lane,to_card_id:toCardId,exp_card_ids:(expCardIds||[]).slice(),legacy_from:captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Legacy Deck"] .zoneCard'),hero_rect:captureVisualRect('.hero-panel[data-side="'+side+'"][data-lane="'+lane+'"] .heroImg'),discard_to:captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Discard Pile"] .zoneCard')};
  }
  function queueCapturedAuthoritativeRankUpMotion(plan){
    if(!plan)return false;var ok=false;
    if(plan.legacy_from&&plan.hero_rect&&plan.to_card_id)ok=queueVisualCardMotion(thumbFor(plan.to_card_id),plan.legacy_from,[{rect:plan.hero_rect}],470)||ok;
    if(plan.hero_rect&&plan.discard_to)(plan.exp_card_ids||[]).forEach(function(id){ok=queueVisualCardMotion(thumbFor(id),plan.hero_rect,[{rect:plan.discard_to}],360)||ok;});
    return ok;
  }
  function captureTributeCardMotion(side,handIndex,cardId,lane){
    if(!animationDocumentReady()) return null;
    var from=side==='PLAYER'?captureVisualRect('.hand-card[data-hand-index="'+Number(handIndex)+'"] .hand-art'):captureAIHandAnimationRect();
    var target=captureVisualRect('.hero-panel[data-side="'+side+'"][data-lane="'+lane+'"] .heroImg');
    return {side:side,card_id:cardId,from:from,target:target};
  }
  function queueTributeCardMotion(snapshot){ if(!snapshot) return false; return queueVisualCardMotion(thumbFor(snapshot.card_id),snapshot.from,[{rect:snapshot.target}],430); }
  function queueRankUpDiscardMotion(side,lane,cardIds){
    if(!animationDocumentReady() || !cardIds || !cardIds.length) return false;
    var from=captureVisualRect('.hero-panel[data-side="'+side+'"][data-lane="'+lane+'"] .heroImg');
    var to=captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Discard Pile"] .zoneCard');
    if(!from || !to) return false;
    cardIds.forEach(function(id){ queueVisualCardMotion(thumbFor(id),from,[{rect:to}],360); }); return true;
  }
  function queueLegacyDeckToFieldMotion(side,lane,cardId){
    if(!animationDocumentReady() || !cardId) return false;
    var from=captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Legacy Deck"] .zoneCard');
    var to=captureVisualRect('.hero-panel[data-side="'+side+'"][data-lane="'+lane+'"] .heroImg');
    return queueVisualCardMotion(thumbFor(cardId),from,[{rect:to}],470);
  }
  function queueLegacyFieldToDeckMotion(side,lane,cardId){
    if(!animationDocumentReady() || !cardId) return false;
    var from=captureVisualRect('.hero-panel[data-side="'+side+'"][data-lane="'+lane+'"] .heroImg');
    var to=captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Legacy Deck"] .zoneCard');
    return queueVisualCardMotion(thumbFor(cardId),from,[{rect:to}],470);
  }
  function captureHandDiscardMotion(side,handIndex,cardId){
    if(!animationDocumentReady() || !cardId) return null;
    var from=side==='PLAYER'?captureVisualRect('.hand-card[data-hand-index="'+Number(handIndex)+'"] .hand-art'):captureAIHandAnimationRect();
    var to=captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Discard Pile"] .zoneCard');
    return from&&to?{card_id:cardId,from:from,to:to}:null;
  }
  function queueHandDiscardMotion(snapshot){ if(!snapshot) return false; return queueVisualCardMotion(thumbFor(snapshot.card_id),snapshot.from,[{rect:snapshot.to}],390); }

  function queueAttachmentDiscardMotion(side,lane,slot,cardId){
    if(!animationDocumentReady() || !cardId) return false;
    var from=captureVisualRect('.slot[data-attachment-side="'+side+'"][data-attachment-lane="'+lane+'"][data-attachment-slot="'+Number(slot)+'"]');
    var to=captureVisualRect('[data-zone-side="'+side+'"][data-zone-type="Discard Pile"] .zoneCard');
    if(!from||!to) return false;
    return queueVisualCardMotion(thumbFor(cardId),from,[{rect:to}],390);
  }

  function shuffleDeck(deckArray){
    var arr=(deckArray||[]).slice();
    for(var i=arr.length-1;i>0;i--){
      var j=Math.floor(Math.random()*(i+1));
      var tmp=arr[i]; arr[i]=arr[j]; arr[j]=tmp;
    }
    return arr;
  }
  function esc(value){ return String(value ?? '').replace(/[&<>"']/g, function(ch){ return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[ch]; }); }
  function rankLabel(value){ var raw=String(value||'').trim(); if(!raw) return ''; return /^Rank\s+/i.test(raw)?raw:('Rank '+raw); }
  function cleanDisplayText(value, fallback){
    fallback=fallback||'';
    if(value===null || typeof value==='undefined') return fallback;
    if(typeof value==='string'){
      var t=value.trim();
      if(!t || t==='{}' || t==='[]' || /^null$/i.test(t) || /^undefined$/i.test(t) || /^\{\s*\"text\"\s*:\s*\"\"/i.test(t) || /^\[object Object\]$/i.test(t)) return fallback;
      return t;
    }
    if(Array.isArray(value)){ return value.map(function(v){ return cleanDisplayText(v,''); }).filter(Boolean).join('\n') || fallback; }
    if(typeof value==='object'){
      if(value.text) return cleanDisplayText(value.text,fallback);
      if(value.full) return cleanDisplayText(value.full,fallback);
      if(value.short) return cleanDisplayText(value.short,fallback);
      if(value.description) return cleanDisplayText(value.description,fallback);
      if(value.effect_text) return cleanDisplayText(value.effect_text,fallback);
      if(value.player_facing_label) return cleanDisplayText(value.player_facing_label,fallback);
      if(value.name) return cleanDisplayText(value.name,fallback);
      if(value.effect){ var eff=value.effect; var bits=[]; if(eff.draw) bits.push('Draw '+eff.draw+' card'+(Number(eff.draw)===1?'':'s')+'.'); if(eff.mana_regen) bits.push('Gain '+eff.mana_regen+' Mana Regen.'); if(eff.heal) bits.push('Heal '+eff.heal+'.'); if(eff.damage) bits.push('Deal '+eff.damage+' damage.'); if(bits.length) return bits.join(' '); }
      var bits2=[]; ['kind','target','status','amount','damage','duration','phase','trigger'].forEach(function(k){ if(value[k]!==null && typeof value[k]!=='undefined' && value[k]!=='' && typeof value[k]!=='object') bits2.push(k+': '+value[k]); });
      return bits2.join(' · ') || fallback;
    }
    return String(value);
  }
  function formatInfoBody(body){
    var txt=cleanDisplayText(body,'');
    if(!txt) txt='No additional detail available.';
    return txt.split(/\n{2,}/).map(function(par){ return '<p>'+esc(par).replace(/\n/g,'<br>')+'</p>'; }).join('');
  }
  function rankUpBonusText(c){
    var rank=parseRank((c.identity||{}).rank);
    var direct=cleanDisplayText(c.rankup&&c.rankup.text,'');
    if(direct) return direct.replace(/mana regen/ig,'Mana Regen');
    if(c.rankup && c.rankup.action && c.rankup.action.effect){ return cleanDisplayText(c.rankup.action,''); }
    if(rank===1) return 'Rank II: After Rank Up, draw 2 cards and gain +1 Mana Regen. Mana Pool does not increase immediately.';
    if(rank===2) return 'Rank III: After Rank Up, draw 3 cards and gain +1 Mana Regen. Mana Pool does not increase immediately.';
    if(rank>=3) return 'No further Rank Up bonus available at Rank III.';
    return 'Rank Up Bonus follows runtime rule: Rank II draws 2, Rank III draws 3, and each Rank Up gains +1 Mana Regen.';
  }
  function flattenCards(defs){
    var root=(defs&&Array.isArray(defs.cards))?defs.cards:[];
    if(root.length) return root.slice();
    var out=[], fams=(defs&&defs.families)||{};
    Object.keys(fams).forEach(function(fam){ ((fams[fam]&&fams[fam].cards)||[]).forEach(function(c){ out.push(c); }); });
    return out;
  }
  function assertCanonicalCardMirror(defs){
    var root=(defs&&Array.isArray(defs.cards))?defs.cards:[], fam=[];
    Object.keys((defs&&defs.families)||{}).forEach(function(k){ (((defs.families[k]||{}).cards)||[]).forEach(function(c){fam.push(c);}); });
    if(root.length!==198 || fam.length!==198) throw new Error('Canonical card source requires 198 root cards and 198 family mirrors.');
    var byId={}; fam.forEach(function(c){byId[c.card_id]=c;});
    root.forEach(function(c){ if(!byId[c.card_id] || JSON.stringify(c)!==JSON.stringify(byId[c.card_id])) throw new Error('Card mirror divergence: '+c.card_id); });
    return true;
  }
  var CARDS=[], CARD_BY_ID={};

  function sanitizeStaleSkillRuntimePayloads(){return true;}
  function assertActiveRuntimeSourceStack(){var stack=window.GL_SOURCE_STACK||{},defs=window.GL_CARD_DEFINITIONS||{},recipes=window.GL_EFFECT_RECIPES||{},gate=window.GRANDIS_LEGACY_ONE_SOURCE_READY||{},cardCount=Array.isArray(defs.cards)?defs.cards.length:flattenCards(defs).length,effectCount=Array.isArray(recipes.effect_recipes)?recipes.effect_recipes.length:0,hash='b455b1537434e454ab873372bb9da715b779e64525a01a8bc3995ed9267ecfa0';if(stack.runtime_data!=='v0.12.7'||defs.version!=='v0.12.7'||cardCount!==198)throw new Error('Active Runtime Data guard failed; v0.12.7 / 198 cards required.');if(stack.effect_checkpoint!=='v0.11.6'||stack.effect_recipe!=='v0.11.7'||recipes.version!=='v0.11.7'||effectCount!==198)throw new Error('Active Effect guard failed.');if(stack.runtime_foundation!=='v1.82'||stack.runtime_core!=='v0.50'||stack.application_runtime_sync!=='v2.44')throw new Error('Active Runtime source stack guard failed.');if(!window.GL_RUNTIME_AUTHORITY||window.GL_RUNTIME_AUTHORITY.version!=='v1.81-browser')throw new Error('Browser Runtime Authority v1.78 is not loaded.');if(gate.canonical_registry_hash!==hash||defs.canonical_registry_hash!==hash||recipes.canonical_registry_hash!==hash)throw new Error('One Source Authority v1.5 hash mismatch.');assertCanonicalCardMirror(defs);return true;}
  function initCards(){assertActiveRuntimeSourceStack();CARDS=flattenCards(window.GL_CARD_DEFINITIONS||{});CARD_BY_ID={};CARDS.forEach(function(c){CARD_BY_ID[c.card_id]=c;});}
  function card(id){ return CARD_BY_ID[id] || {card_id:id, name:id, family:'Unknown'}; }
  function cardName(c){ return c.name || c.card_name || c.card_id || 'Unknown'; }
  function cardFamily(c){ return c.family || c.classification || c.card_category || 'Card'; }
  function cardSubtype(c){ return c.classification || c.action_category || (c.identity && c.identity.display_class) || ''; }
  function cardCost(c){if(!c)return'';var cost=c.cost||{};if(typeof cost.mana!=='undefined'&&cost.mana!==null)return cost.mana;if(cost.mana_by_class){var vals=Object.values(cost.mana_by_class);return vals.length?Math.min.apply(null,vals):'';}return'';}
  function cardPhase(c){ return (c.timing && (c.timing.phase || c.timing.raw_phase || c.timing.phase_or_window)) || ''; }
  function manifestEntry(cardId){ return window.GL_ASSET_MANIFEST && window.GL_ASSET_MANIFEST.cards && window.GL_ASSET_MANIFEST.cards[cardId]; }
  function cardAssetEntry(cardId){ var c=CARD_BY_ID && CARD_BY_ID[cardId]; return c && c.asset ? c.asset : null; }
  function withAssetRevision(path){
    path=String(path||'');
    if(!path || /^(?:data:|blob:)/i.test(path) || path.indexOf(GL_SHARED_CARD_BASE)===0 || /[?&]v=gl-pvp-/i.test(path)) return path;
    return path+(path.indexOf('?')===-1?'?':'&')+'v='+encodeURIComponent(GL_ASSET_REV);
  }
  function thumbFor(cardId){ if(cardId==='__HIDDEN_CARD_BACK__'||cardId==='__HIDDEN_CARD__') return withAssetRevision('https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp'); var e=manifestEntry(cardId), a=cardAssetEntry(cardId); var p=(e && (e.local_thumb_path || e.fallback_thumb_path || e.thumb_url)) || (a && (a.local_thumbnail_path || a.thumbnail_url || a.image_url || a.artwork_url)) || 'https://grandislegacytcg.github.io/shared/season1/v1/cards/thumbs/'+cardId+'.webp'; return withAssetRevision(p); }
  function fullFor(cardId){ var e=manifestEntry(cardId), a=cardAssetEntry(cardId), p=''; if(!e && !a) return thumbFor(cardId); var staleFullArt={ 'S1-ARC-008': true }; if(staleFullArt[cardId] && e && e.local_thumb_path) p=e.local_thumb_path; else p=(e && ((e.verified_local_full_path || '') || e.local_full_path || e.local_thumb_path || e.fallback_thumb_path || e.thumb_url || e.full_url)) || (a && (a.local_thumbnail_path || a.image_url || a.artwork_url || a.thumbnail_url)) || ''; return p?withAssetRevision(p):thumbFor(cardId); }
  function heroIdentity(c){ var id=c.identity || {}; var bits=[]; if(id.race) bits.push(id.race); if(id.display_class || id.class) bits.push(id.display_class || id.class); if(id.rank) bits.push(rankLabel(id.rank)); return bits.join(' · ') || 'Hero'; }
  function legacyAbilitySummary(c){
    var a=c&&c.ability; if(!a) return '';
    var text=cleanDisplayText(a.text||'', ''); if(text) return text;
    var bits=[];
    if(a.timing) bits.push(cleanDisplayText(a.timing,'Deploy Phase'));
    if(a.once_per_turn) bits.push('once/turn');
    var cost=legacyCostSpec(a), fam=legacyCostFamily(a), cnt=legacyCostCount(a);
    if(cost && cnt) bits.push('discard '+cnt+' '+(fam?fam+' ':'')+'Skill from hand');
    var eff=a.effect||{};
    if(eff.draw) bits.push('draw '+eff.draw+' card'+(Number(eff.draw)>1?'s':''));
    if(eff.gain_mana) bits.push('gain '+eff.gain_mana+' Mana Shard'+(Number(eff.gain_mana)>1?'s':''));
    if(eff.heal) bits.push('heal 1 own Hero by '+eff.heal);
    if(eff.damage_reduction) bits.push('reduce incoming damage by '+eff.damage_reduction);
    if(eff.opponent_mana_discard) bits.push('discard '+eff.opponent_mana_discard+' Mana Shard from opponent Mana Pool');
    if(eff.return_from_discard) bits.push('return '+eff.return_from_discard+' card from discard to hand');
    if(eff.look_at_top_deck) bits.push('look at top '+eff.look_at_top_deck+' deck card(s)');
    return bits.length?bits.join(' → ')+'.':'Legacy ability data is present but has no player-facing text in the current lock.';
  }
  function summarizeCard(c){
    if(c.card_text) return cleanDisplayText(c.card_text,'');
    if(isLegacyDefinitionCard(c) && c.ability) return legacyAbilitySummary(c);
    if(c.effect_strings_by_class) return Object.keys(c.effect_strings_by_class).map(function(k){ return k+': '+cleanDisplayText(c.effect_strings_by_class[k],'No effect text available.'); }).join('\n');
    if(Array.isArray(c.effect)) return c.effect.map(function(e){ return cleanDisplayText(e,''); }).filter(Boolean).join('\n') || 'Effect data is present but has no player-facing text in the current lock.';
    if(c.attack) return cleanDisplayText(c.attack,'Attack effect uses runtime damage/type values from the card lock.');
    if(c.class_ability && c.class_ability.text) return cleanDisplayText(c.class_ability.text,'');
    if(c.racial_ability && c.racial_ability.text) return cleanDisplayText(c.racial_ability.text,'');
    return 'Effect text unavailable in the current card lock.';
  }
  function deckEntriesToIds(entries){
    return normalizeCardIdEntries(entries,'deck',[]).ids;
  }
  function normalizeCardIdEntries(entries,path,errors){
    var ids=[]; var normalized=[];
    function add(id,qty,extra){
      var cid=String(id||'').trim(); var q=Number(qty||1);
      if(!cid){ errors.push('Deck validation failed: missing card_id at '+path+'.'); return; }
      if(!Number.isFinite(q) || q<1) q=1;
      q=Math.floor(q);
      var obj={card_id:cid, quantity:q};
      if(extra && typeof extra==='object') Object.keys(extra).forEach(function(k){ if(!(k in obj)) obj[k]=extra[k]; });
      normalized.push(obj);
      for(var i=0;i<q;i++) ids.push(cid);
    }
    if(Array.isArray(entries)){
      entries.forEach(function(e,i){
        if(typeof e==='string') add(e,1,{});
        else if(e && typeof e==='object'){
          if(!e.card_id){ errors.push('Deck validation failed: missing card_id at '+path+'['+i+'].'); return; }
          add(e.card_id, e.quantity||1, e);
        } else errors.push('Deck validation failed: invalid entry at '+path+'['+i+'].');
      });
    } else if(entries && typeof entries==='object'){
      Object.keys(entries).forEach(function(k){ add(k, entries[k], {}); });
    } else if(entries == null){
      errors.push('Deck validation failed: missing '+path+'.');
    } else errors.push('Deck validation failed: invalid '+path+' shape.');
    return {ids:ids, entries:normalized};
  }
  function normalizeLegacyExpanded(entries,path,errors){
    var ids=[];
    if(Array.isArray(entries)){
      entries.forEach(function(e,i){
        if(typeof e==='string') ids.push(e);
        else if(e && typeof e==='object'){
          if(!e.card_id){ errors.push('Deck validation failed: missing card_id at '+path+'['+i+'].'); return; }
          ids.push(String(e.card_id));
        } else errors.push('Deck validation failed: invalid entry at '+path+'['+i+'].');
      });
    } else if(entries && typeof entries==='object'){
      Object.keys(entries).forEach(function(k){ var q=Number(entries[k]||1); for(var i=0;i<q;i++) ids.push(k); });
    } else errors.push('Deck validation failed: missing '+path+'.');
    return ids;
  }
  function normalizePackageSlots(rawSlots, formation, legacyExpandedRaw){
    var slots=Array.isArray(rawSlots) ? rawSlots.slice() : [];
    var expanded=Array.isArray(legacyExpandedRaw)?legacyExpandedRaw:[];
    return slots.map(function(p,i){
      var copy=Object.assign({}, p||{});
      if(!copy.slot){
        var progression=copy.progression;
        var inferred=LANE_ORDER.find(function(l){ return formation && formation[l]===progression; });
        copy.slot=inferred || LANE_ORDER[i] || ('SLOT_'+(i+1));
      }
      var progressionId=copy.progression || (formation && copy.slot && formation[copy.slot]);
      var progCard=progressionId ? card(progressionId) : null;
      var inferredLineage=(progCard && progCard.identity && progCard.identity.fixed_class_lineage_id) || '';
      if(!copy.lineage || /^S1-[A-Z]+-H\d+/i.test(String(copy.lineage)) || String(copy.lineage).indexOf(';')!==-1 || /\+/.test(String(copy.lineage))){
        copy.lineage=inferredLineage || copy.lineage || copy.package_name || copy.progression || copy.legacy || ('Package '+(i+1));
      }
      var packageEntry=expanded.find(function(e){
        if(!e || typeof e!=='object') return false;
        if(copy.legacy && e.card_id===copy.legacy) return true;
        if(copy.progression && e.card_id===copy.progression) return true;
        return !!(copy.package_name && e.package_name===copy.package_name);
      });
      if(!copy.package_id) copy.package_id=(packageEntry&&packageEntry.package_id) || (copy.slot?copy.slot+'-PACKAGE':'PACKAGE-'+(i+1));
      if(!copy.package_name && packageEntry&&packageEntry.package_name) copy.package_name=packageEntry.package_name;
      return copy;
    });
  }
  function normalizeDeck(raw){
    if(!raw || typeof raw!=='object') throw new Error('Invalid deck file.');
    var errors=[]; var deck=clone(raw);
    if(!deck.default_formation || typeof deck.default_formation!=='object') throw new Error('Invalid deck file: missing default_formation.');
    var main=normalizeCardIdEntries(deck.main_deck,'main_deck',errors);
    var legacyExpandedRaw=clone(deck.legacy_deck_expanded||[]);
    var legacyIds=normalizeLegacyExpanded(deck.legacy_deck_expanded,'legacy_deck_expanded',errors);
    if(errors.length) throw new Error(errors[0]);
    deck.main_deck=main.entries;
    deck.legacy_deck_expanded=legacyIds;
    deck.legacy_deck_package_slots=normalizePackageSlots(deck.legacy_deck_package_slots, deck.default_formation, legacyExpandedRaw);
    deck.main_deck_count=main.ids.length;
    deck.legacy_deck_count=legacyIds.length;
    deck.legacy_package_count=deck.legacy_deck_package_slots.length;
    deck.deck_name=deck.deck_name || deck.display_name || deck.preset_id || 'Imported Deck';
    deck.display_name=deck.display_name || deck.deck_name;
    return deck;
  }
  function validateDeck(deck, side){
    var errors=[]; var d;
    try{ d=normalizeDeck(deck); } catch(err){ return {ok:false, deck:deck, errors:[err.message||'Invalid deck file.']}; }
    var expectedSlots=['LEFT','CENTER','RIGHT']; var formation=d.default_formation||{};
    expectedSlots.forEach(function(slot){ if(!formation[slot]) errors.push('Deck validation failed: missing '+side+' '+slot+' hero at default_formation.'+slot+'.'); });
    var packageSlots=d.legacy_deck_package_slots||[];
    if(packageSlots.length!==3){
      var found={}; packageSlots.forEach(function(p){ if(p&&p.slot) found[p.slot]=true; });
      var missing=expectedSlots.filter(function(s){ return !found[s]; });
      errors.push('Deck validation failed: legacy package count '+packageSlots.length+' does not match expected 3.'+(missing.length?' Missing '+missing.join('/')+'.':''));
    }
    if((d.legacy_deck_expanded||[]).length!==12) errors.push('Deck validation failed: legacy_deck_expanded must contain 12 cards, found '+(d.legacy_deck_expanded||[]).length+'.');
    var mainIds=deckEntriesToIds(d.main_deck);
    if(!mainIds.length) errors.push('Deck validation failed: main_deck must contain playable cards.');
    var ultimateCounts={}; mainIds.forEach(function(id){ var c=CARD_BY_ID[id]; if(c && isUltimateCard(c)){ var key=(c.name||id).toLowerCase(); ultimateCounts[key]=(ultimateCounts[key]||0)+1; } });
    Object.keys(ultimateCounts).forEach(function(k){ if(ultimateCounts[k]>1) errors.push('Deck validation failed: Ultimate card max 1 per name. Duplicate: '+k+' x'+ultimateCounts[k]+'.'); });
    var allIds=[]; expectedSlots.forEach(function(slot){ if(formation[slot]) allIds.push(formation[slot]); }); allIds=allIds.concat(mainIds,d.legacy_deck_expanded||[]);
    var missingIds=[]; allIds.forEach(function(id,idx){ if(typeof id!=='string' || !id.trim()) missingIds.push('allIds['+idx+']'); else if(!CARD_BY_ID[id]) missingIds.push(id); });
    if(missingIds.length) errors.push('Deck validation failed: unknown card_id '+missingIds.slice(0,12).join(', ')+(missingIds.length>12?'…':'')+'.');
    return {ok:errors.length===0, deck:d, errors:errors, report:{selected_preset_name:d.deck_name, selected_preset_file:d.source_file||d.file||d.preset_file||'', main_deck_count:mainIds.length, legacy_deck_count:(d.legacy_deck_expanded||[]).length, legacy_package_count:packageSlots.length, default_formation:d.default_formation}};
  }
  function heroState(id, side, lane, legacyInfo){
    var c=card(id); var hp=Number(c.hp||100); var h={card_id:id, side:side, lane:lane, instance_id:'HERO:'+side+':'+lane+':'+heroProgressionId({card_id:id}), hp:hp, maxHp:hp, exhausted:false, exp_cards:[], exp_total:0, attachments:[null,null], statuses:[]};
    if(legacyInfo){ h.assigned_legacy_card_id=legacyInfo.legacy||legacyInfo.legacy_card_id||null; h.legacy_lineage_id=legacyInfo.lineage||legacyInfo.progression||legacyInfo.lineage_id||null; h.legacy_package_id=legacyInfo.package_id||legacyInfo.slot||lane; }
    return h;
  }
  function progressionContainsHeroId(progressionId, heroId){
    if(!progressionId || !heroId) return false;
    if(progressionId===heroId) return true;
    return rankHeroIdFromProgression(progressionId,2)===heroId || rankHeroIdFromProgression(progressionId,3)===heroId;
  }
  function packageSlotMatchesHero(slot, heroId){
    if(!slot || !heroId) return false;
    if(slot.progression && progressionContainsHeroId(slot.progression,heroId)) return true;
    if(slot.hero===heroId || slot.card_id===heroId) return true;
    var heroCard=card(heroId), slotCard=slot.progression?card(slot.progression):null;
    var heroLineage=(heroCard.identity&&heroCard.identity.fixed_class_lineage_id)||'';
    var slotLineage=(slotCard&&slotCard.identity&&slotCard.identity.fixed_class_lineage_id)||slot.fixed_class_lineage_id||'';
    return !!(heroLineage && slotLineage && heroLineage===slotLineage);
  }
  function legacyInfoForFormationHero(deck, lane, heroId){
    var slots=(deck&&deck.legacy_deck_package_slots)||[];
    // Exact progression/package binding must win before lane or base-class fallback.
    // This is required when a deck contains both Archer progressions at the same time.
    return slots.find(function(p){ return packageSlotMatchesHero(p,heroId); }) ||
      slots.find(function(p){ return p && p.slot===lane; }) || null;
  }
  function buildHeroMap(deck, side){ var out={}; LANE_ORDER.forEach(function(lane){ var hid=deck.default_formation[lane]; out[lane]=heroState(hid,side,lane,legacyInfoForFormationHero(deck,lane,hid)); }); return out; }
  function buildInitialMatchState(){
    var p=normalizeDeck(decks.PLAYER),a=normalizeDeck(decks.AI),pRaw=deckEntriesToIds(p.main_deck),aRaw=deckEntriesToIds(a.main_deck),pMain=STARTUP_SHUFFLE_ENABLED?shuffleDeck(pRaw):pRaw.slice(),aMain=STARTUP_SHUFFLE_ENABLED?shuffleDeck(aRaw):aRaw.slice();
    var state={phase:'Opening Coin Flip',turn:null,round:1,gameOver:false,winner:null,pending:null,preGame:{stage:'COIN_FLIP'},openingCoinFlip:null,drawPhaseResolvedFor:null,mana:0,manaRegen:2,racial:2,aiMana:0,aiManaRegen:2,aiRacial:2,playerDeck:pMain,aiDeck:aMain,playerHand:[],aiHand:[],playerDiscard:[],aiDiscard:[],playerHeroes:buildHeroMap(p,'PLAYER'),aiHeroes:buildHeroMap(a,'AI'),playerLegacy:p.legacy_deck_expanded.slice(),aiLegacy:a.legacy_deck_expanded.slice(),playerLegacyPackageSlots:(p.legacy_deck_package_slots||[]).slice(),aiLegacyPackageSlots:(a.legacy_deck_package_slots||[]).slice(),tributeUsedThisReform:false,activeAttachments:[],playerDeckName:p.deck_name,aiDeckName:a.deck_name,log:['Match initialized. Main Decks are shuffled. Opening Coin Flip must resolve before opening hands are drawn.'],lastOpponentAction:null,opponentPlayedEvents:[],selectedOpponentEventId:null,pvpActionEventsBySide:{PLAYER:[],AI:[]},presentationEvents:[],presentationEventSequence:0,cardsDrawnThisTurn:{PLAYER:0,AI:0},lastDrawnCardBySide:{}};
    syncCounts(state);return state;
  }
  function syncCounts(state){
    if(!state) return state;
    state.racial=Math.max(0,Math.min(RACIAL_TOKEN_CAP,Number(state.racial||0)));
    state.aiRacial=Math.max(0,Math.min(RACIAL_TOKEN_CAP,Number(state.aiRacial||0)));
    state.playerDeckCount=(state.playerDeck||[]).length; state.aiDeckCount=(state.aiDeck||[]).length;
    state.playerLegacyCount=(state.playerLegacy||[]).length; state.aiLegacyCount=(state.aiLegacy||[]).length;
    state.aiHandCount=(state.aiHand||[]).length;
    return state;
  }
  function runtimeStateCheckpoint(state,reason){
    if(!state) return state;
    sanitizeAllHeroSlots(state);
    if(window.GL_RUNTIME_AUTHORITY){
      window.GL_RUNTIME_AUTHORITY.bumpRevision(state,reason||'runtime checkpoint');
      var report=window.GL_RUNTIME_AUTHORITY.inspect(state,{attachmentCardId:attachmentCardId,isLegacyModeHero:isLegacyModeHero});
      if(!report.ok){ state.runtimeInvariantErrors=report.errors.slice(); throw new Error('Runtime invariant failed: '+report.errors.join(' | ')); }
      state.runtimeInvariantErrors=[];
    }
    syncCounts(state);
    return state;
  }
  function pushLog(state,msg){ state.log=state.log||[]; state.log.unshift(msg); if(state.log.length>20) state.log.length=20; }
  function restoreStateSnapshot(target,snapshot){
    Object.keys(target||{}).forEach(function(k){ delete target[k]; });
    Object.keys(snapshot||{}).forEach(function(k){ target[k]=clone(snapshot[k]); });
    return target;
  }
  function committedCardCustodyList(state){ state.committedCardCustody=Array.isArray(state.committedCardCustody)?state.committedCardCustody:[]; return state.committedCardCustody; }
  function physicalCardCountForSide(state,side,cardId){
    if(!state || !cardId) return 0;
    var count=0;
    [sideHand(state,side),deckForSide(state,side),sideDiscard(state,side)].forEach(function(zone){ (zone||[]).forEach(function(id){ if(id===cardId) count++; }); });
    LANE_ORDER.forEach(function(lane){ var h=sideHeroes(state,side)[lane]; if(!h) return; (h.exp_cards||[]).forEach(function(id){ if(id===cardId) count++; }); (h.attachments||[]).forEach(function(id){ if(attachmentCardId(id)===cardId) count++; }); });
    return count;
  }
  function objectReferencesCommittedCard(obj,side,cardId,depth){
    if(!obj || depth>4) return false;
    if(Array.isArray(obj)) return obj.some(function(x){ return objectReferencesCommittedCard(x,side,cardId,depth+1); });
    if(typeof obj!=='object') return false;
    var sourceSide=obj.source_side||obj.side||obj.attacking_player_id||obj.player_id||null;
    var ids=[obj.card_id,obj.source_card_id,obj.deferred_discard_card_id,obj.drawn_card_id];
    if(ids.indexOf(cardId)!==-1 && (!sourceSide || sourceSide===side)) return true;
    return ['action','post','next_pending','multi_sequence'].some(function(k){ return objectReferencesCommittedCard(obj[k],side,cardId,depth+1); });
  }
  function committedCardHasActiveReference(state,entry){
    if(!state || !entry) return false;
    if(objectReferencesCommittedCard(state.pending,entry.side,entry.card_id,0)) return true;
    if(objectReferencesCommittedCard(state.responseWindow,entry.side,entry.card_id,0)) return true;
    if((state.pendingCastings||[]).some(function(x){ return x&&x.card_id===entry.card_id&&x.side===entry.side; })) return true;
    if((state.activeAttachments||[]).some(function(x){ return x&&x.card_id===entry.card_id&&x.side===entry.side; })) return true;
    return false;
  }
  function repairReferencedCommittedAttachment(state,entry){
    var refs=[];
    (state.pendingCastings||[]).forEach(function(x){ if(x&&x.card_id===entry.card_id&&x.side===entry.side) refs.push(x); });
    (state.activeAttachments||[]).forEach(function(x){ if(x&&x.card_id===entry.card_id&&x.side===entry.side) refs.push(x); });
    refs.some(function(x){
      var lane=x.source_lane||x.lane, slot=Number(typeof x.attachmentSlot!=='undefined'?x.attachmentSlot:x.slot), h=lane&&sideHeroes(state,entry.side)[lane];
      if(!h || !Array.isArray(h.attachments) || slot<0 || slot>1) return false;
      if(!h.attachments[slot]){ h.attachments[slot]=entry.card_id; pushLog(state,'Card custody restored '+cardName(card(entry.card_id))+' to its active Attachment Slot.'); }
      return attachmentCardId(h.attachments[slot])===entry.card_id;
    });
  }
  function beginCommittedCardCustody(state,side,cardId,beforePhysical,commitToken){
    committedCardCustodyList(state).push({side:side,card_id:cardId,before_physical:Number(beforePhysical||0),started_round:Number(state.round||0),started_phase:state.phase||'',token:String(commitToken||('card:'+side+':'+cardId+':'+Date.now()+':'+Math.random()))});
  }
  function activeCommitTokenFor(state,side,cardId){ var list=committedCardCustodyList(state); for(var i=list.length-1;i>=0;i--){ if(list[i].side===side&&list[i].card_id===cardId) return list[i].token; } return null; }
  function commitDestinationLedger(state){ state.resolvedCardCommitDestinations=state.resolvedCardCommitDestinations&&typeof state.resolvedCardCommitDestinations==='object'?state.resolvedCardCommitDestinations:{}; return state.resolvedCardCommitDestinations; }
  function finalizeCommittedSourceCard(state,holder,side,cardId,destination,detail){
    holder=holder||{};
    var token=holder.commit_token||activeCommitTokenFor(state,side,cardId)||('card:'+side+':'+cardId+':'+Date.now()+':'+Math.random());
    var authority=window.GL_RUNTIME_AUTHORITY;
    if(!authority) throw new Error('Runtime Authority unavailable for card finalization.');
    var result=authority.finalizeCard(state,{commit_token:token,side:side,card_id:cardId,destination:destination,detail:detail||null},{
      toDiscard:function(){ sideDiscard(state,side).push(cardId); },
      toHand:function(){ sideHand(state,side).push(cardId); }
    });
    if(!result.applied) return false;
    completeCommittedCardCustody(state,side,cardId,token); holder.commit_token=token; holder.source_card_finalized=true;
    return true;
  }
  function reconcileCommittedCardCustody(state,reason){
    if(!state || !Array.isArray(state.committedCardCustody) || !state.committedCardCustody.length) return false;
    var keep=[],changed=false;
    state.committedCardCustody.forEach(function(entry){
      repairReferencedCommittedAttachment(state,entry);
      if(state.resolvedCardCommitDestinations&&state.resolvedCardCommitDestinations[entry.token]){ changed=true; return; }
      var physical=physicalCardCountForSide(state,entry.side,entry.card_id);
      if(physical>=Number(entry.before_physical||0)){ changed=true; return; }
      if(committedCardHasActiveReference(state,entry)){ keep.push(entry); return; }
      finalizeCommittedSourceCard(state,{commit_token:entry.token},entry.side,entry.card_id,'discard',{reason:'custody safeguard'});
      pushLog(state,'Card custody safeguard moved '+cardName(card(entry.card_id))+' to Discard because resolution ended without a legal destination'+(reason?' ('+reason+')':'')+'.');
      changed=true;
    });
    state.committedCardCustody=keep;
    return changed;
  }
  function visibleOpponentEventTypes(){ return ['ATK','DEF','ITEM','EVENT','RACIAL','ABILITY','LEGACY','TRIBUTE','RANK UP','REPOSITION','CAST','RESOLVE','CANCELED','SUPPORT','TACTICAL','SKILL','ACTION','TICK']; }
  function actionTargetLine(evt){
    if(!evt) return '';
    var side=evt.target_side || '', lane=evt.target_lane || '';
    var snap=evt.target_snapshot || evt.metric_before || null;
    if(side && lane){
      var snapName=snap && (snap.name || (snap.card_id&&cardName(card(snap.card_id))));
      var snapMeta=snap&&snap.class_name?(' — '+snap.class_name+(snap.rank?' Rank '+snap.rank:'')):'';
      if(snapName) return side+' '+lane+' / '+snapName+snapMeta;
      var h=(appState?sideHeroes(appState,side)[lane]:null);
      return side+' '+lane+(h&&h.card_id?' / '+cardName(card(h.card_id)):'');
    }
    return evt.target_label || '';
  }
  function lockEventTargetSnapshot(state,evt){
    if(!state||!evt||!evt.target_side||!evt.target_lane)return evt;
    if(!evt.target_snapshot) evt.target_snapshot=clone(evt.metric_before||heroEventSnapshot(state,evt.target_side,evt.target_lane));
    return evt;
  }
  function normalizeLines(lines){
    if(!lines) return [];
    if(typeof lines==='string') return lines.split(/\n+/).map(function(x){return cleanDisplayText(x,'');}).filter(Boolean);
    if(Array.isArray(lines)) return lines.map(function(x){return cleanDisplayText(x,'');}).filter(Boolean);
    return [cleanDisplayText(lines,'')].filter(Boolean);
  }
  function orderPlayedResultLines(lines){
    var normalized=normalizeLines(lines), hpChanges=[], other=[];
    normalized.forEach(function(line){
      if(/\bHP:\s*-?\d+\s*->\s*-?\d+/i.test(line)) hpChanges.push(line);
      else other.push(line);
    });
    return other.concat(hpChanges);
  }
  function heroEventSnapshot(state, side, lane){
    if(!state || !side || !lane) return null;
    var h=sideHeroes(state,side)[lane]; if(!h) return null;var id=heroIdFrom(h);
    return {side:side,lane:lane,card_id:id,name:cardName(card(id)),class_name:heroClass(h),rank:heroRank(h),hp:Number(h.hp||0),exp:Number(h.exp_total||0)};
  }
  function heroEventSnapshotAfter(state, before, fallbackSide, fallbackLane){
    if(state && before && before.card_id){
      var heroes=sideHeroes(state,before.side||fallbackSide)||{};
      for(var i=0;i<LANE_ORDER.length;i++){ var lane=LANE_ORDER[i], h=heroes[lane]; if(h && heroIdFrom(h)===before.card_id) return heroEventSnapshot(state,before.side||fallbackSide,lane); }
    }
    return heroEventSnapshot(state,fallbackSide,fallbackLane);
  }
  function opponentMetricChangeLine(before, after, metric){
    if(!before || !after) return '';
    var key=metric==='EXP'?'exp':'hp';
    return (before.name||after.name||'Hero')+' '+metric+': '+Number(before[key]||0)+' -> '+Number(after[key]||0);
  }

  function opponentEventDetail(evt){
    if(!evt) return 'Opponent Played\n\nNo opponent action yet.';
    var out=['Opponent Played',''];
    if(evt.action_line) out.push(evt.action_line); else out.push('Action: '+cleanDisplayText(evt.title||evt.label,'AI action resolved.')+'.');
    var target=actionTargetLine(evt); if(target) out.push('Target: '+target+'.');
    var responseLines=normalizeLines(evt.response_lines||evt.chain_lines);
    if(responseLines.length){ out.push('', 'Response:'); responseLines.forEach(function(l){ out.push('- '+l); }); }
    var resultLines=orderPlayedResultLines(evt.result_lines || evt.summary);
    if(resultLines.length){ out.push('', 'Result:'); resultLines.forEach(function(l){ out.push('- '+l.replace(/^Result:\s*/i,'')); }); }
    var reasonLines=normalizeLines(evt.reason_lines);
    if(reasonLines.length){ out.push('', 'Reason:'); reasonLines.forEach(function(l){ out.push('- '+l); }); }
    return out.join('\n');
  }
  function recordOpponentAction(state,label,cardId,title,summary,extra){
    if(!state) return null;
    var type=String(label||'ACTION').toUpperCase();
    if(visibleOpponentEventTypes().indexOf(type)===-1) type='ACTION';
    var evt=Object.assign({
      id:'op_'+Date.now()+'_'+Math.floor(Math.random()*1000000),
      label:type,
      card_id:cardId||null,
      title:title||cardName(card(cardId)),
      action_line:null,
      target_side:null,
      target_lane:null,
      target_label:null,
      summary:summary||'',
      detail_lines:[],
      result_lines:normalizeLines(summary),
      response_lines:[],
      reason_lines:[],
      round:Number(state.round||1),
      phase:state.phase,
      turn:state.turn,
      timestamp:Date.now()
    }, extra||{});
    evt.result_lines=normalizeLines(evt.result_lines && evt.result_lines.length ? evt.result_lines : evt.summary);
    lockEventTargetSnapshot(state,evt);
    state.opponentPlayedEvents=state.opponentPlayedEvents||[];
    state.opponentPlayedEvents.unshift(evt);
    if(state.opponentPlayedEvents.length>120) state.opponentPlayedEvents.length=120;
    state.lastOpponentAction=evt;
    state.selectedOpponentEventId=evt.id;
    return evt;
  }
  function recordLocalPlayerAction(state,label,cardId,title,summary,extra){
    if(!state) return null;
    var type=String(label||'ACTION').toUpperCase();
    if(visibleOpponentEventTypes().indexOf(type)===-1) type='ACTION';
    var evt=Object.assign({
      id:'you_'+Date.now()+'_'+Math.floor(Math.random()*1000000),
      label:type,card_id:cardId||null,title:title||cardName(card(cardId)),
      action_line:null,target_side:null,target_lane:null,target_label:null,
      summary:summary||'',detail_lines:[],result_lines:normalizeLines(summary),
      response_lines:[],reason_lines:[],round:Number(state.round||1),
      phase:state.phase,turn:state.turn,source_side:'PLAYER',timestamp:Date.now()
    },extra||{});
    evt.result_lines=normalizeLines(evt.result_lines&&evt.result_lines.length?evt.result_lines:evt.summary);
    lockEventTargetSnapshot(state,evt);
    state.playerPlayedEvents=state.playerPlayedEvents||[];
    state.playerPlayedEvents.unshift(evt);
    if(state.playerPlayedEvents.length>120) state.playerPlayedEvents.length=120;
    state.lastPlayerAction=evt;
    state.selectedPlayerEventId=evt.id;
    return evt;
  }
  function findLocalPlayerEventById(state,eventId){
    if(!state||!eventId) return null;
    return (state.playerPlayedEvents||[]).find(function(e){return e.id===eventId;})||null;
  }
  function findLocalPlayerEventForAction(state,action,label){
    if(!state||!action) return null;
    if(action.player_event_id){var byId=findLocalPlayerEventById(state,action.player_event_id);if(byId)return byId;}
    var type=String(label||'ATK').toUpperCase();
    return (state.playerPlayedEvents||[]).find(function(e){
      return e.label===type&&e.card_id===action.card_id&&
        (!e.source_side||e.source_side===action.source_side)&&(!e.source_lane||e.source_lane===action.source_lane)&&
        (!e.target_side||e.target_side===action.target_side)&&(!e.target_lane||e.target_lane===action.target_lane);
    })||null;
  }
  function appendLocalPlayerEventResult(state,evtOrId,lines){
    if(!state)return null;
    var evt=(evtOrId&&typeof evtOrId==='object')?evtOrId:findLocalPlayerEventById(state,evtOrId);
    if(!evt)return null;
    evt.result_lines=(evt.result_lines||[]).concat(normalizeLines(lines));
    evt.summary=evt.result_lines.join('\n');state.lastPlayerAction=evt;state.selectedPlayerEventId=evt.id;return evt;
  }
  function appendLocalPlayerEventResponse(state,evt,responseLines,resultLines){
    if(!state||!evt)return null;
    evt.response_lines=(evt.response_lines||[]).concat(normalizeLines(responseLines));
    evt.result_lines=(evt.result_lines||[]).concat(normalizeLines(resultLines));
    evt.summary=evt.result_lines.join('\n');state.lastPlayerAction=evt;state.selectedPlayerEventId=evt.id;return evt;
  }
  function appendCardPlayedResultForSide(state,side,playerEvt,aiEvt,lines){
    if(side==='PLAYER'&&playerEvt)return appendLocalPlayerEventResult(state,playerEvt,lines);
    if(side==='AI'&&aiEvt)return appendOpponentEventResult(state,aiEvt,lines);
    return null;
  }
  function castingSnapshotLines(pc){
    var lines=[];
    if(Number.isFinite(Number(pc&&pc.printed_damage))) lines.push('Printed/base damage: '+Number(pc.printed_damage)+'.');
    (pc&&pc.modifier_breakdown||[]).forEach(function(m){lines.push('Committed modifier: '+(Number(m.amount||0)>=0?'+':'')+Number(m.amount||0)+' from '+String(m.source_name||'effect')+'.');});
    if(Number.isFinite(Number(pc&&pc.committed_damage))) lines.push('Committed damage snapshot: '+Number(pc.committed_damage)+'.');
    return lines;
  }
  function recordCastingStartEvent(state,pc,mode){
    if(!state||!pc)return null;
    var c=card(pc.card_id),name=cardName(c),locked=pc.locked_target_lane||pc.target_lane,side=pc.side==='AI'?'AI':'PLAYER';
    var lines=[mode==='counter_draw'?'Counter Casting started.':'Casting started.','Card entered Attachment Slot '+(Number(pc.attachmentSlot)+1)+'.'];
    if(mode==='counter_draw') lines.push('Required draw counters: '+requiredDrawCountersForCasting(c)+'.');
    if(pc.target_side&&locked) lines.push('Locked target position: '+pc.target_side+' '+locked+'.');
    lines=lines.concat(castingSnapshotLines(pc));
    var common={action_line:'Action: '+(side==='PLAYER'?'You':'AI')+' started casting '+name+'.',source_side:side,source_lane:pc.original_source_lane||pc.source_lane,target_side:pc.target_side,target_lane:locked,result_lines:lines,casting_action_id:pc.commit_token||null,casting_stage:'STARTED'};
    var evt=side==='PLAYER'?recordLocalPlayerAction(state,'CAST',pc.card_id,'Casting Started — '+name,lines.join('\n'),common):recordOpponentAction(state,'CAST',pc.card_id,'Casting Started — '+name,lines.join('\n'),common);
    if(state.pvpHumanVsHuman) recordPvpSideAction(state,side,'CAST',pc.card_id,'Casting Started — '+name,lines.join('\n'),common);
    return evt;
  }
  function recordCastingExitEvent(state,pc,outcome,reason,extraLines){
    if(!state||!pc)return null;
    var resolved=String(outcome||'').toUpperCase()==='RESOLVED',side=pc.side==='AI'?'AI':'PLAYER',c=card(pc.card_id),name=cardName(c),locked=pc.locked_target_lane||pc.target_lane;
    var label=resolved?'RESOLVE':'CANCELED',title=(resolved?'Casting Resolved — ':'Casting Canceled — ')+name;
    var lines=[resolved?'Casting card left its Attachment Slot for release.':'Casting card left its Attachment Slot because it was canceled.','Attachment Slot '+(Number(pc.attachmentSlot)+1)+' is now empty.'];
    if(pc.target_side&&locked) lines.push('Locked target position: '+pc.target_side+' '+locked+'.');
    lines=lines.concat(castingSnapshotLines(pc));
    if(reason) lines.push((resolved?'Result: ':'Cancel reason: ')+reason);
    lines=lines.concat(normalizeLines(extraLines));
    var common={action_line:'Action: '+(side==='PLAYER'?'Your':'AI')+' casting '+name+' '+(resolved?'resolved.':'was canceled.'),source_side:side,source_lane:pc.original_source_lane||pc.source_lane,target_side:pc.target_side,target_lane:locked,result_lines:lines,reason_lines:resolved?[]:(reason?[reason]:[]),casting_action_id:pc.commit_token||null,related_casting_start_event_id:pc.casting_start_event_id||null,casting_stage:resolved?'RESOLVED':'CANCELED'};
    var evt=side==='PLAYER'?recordLocalPlayerAction(state,label,pc.card_id,title,lines.join('\n'),common):recordOpponentAction(state,label,pc.card_id,title,lines.join('\n'),common);
    if(state.pvpHumanVsHuman) recordPvpSideAction(state,side,label,pc.card_id,title,lines.join('\n'),common);
    return evt;
  }
  function recordPvpSideAction(state, side, label, cardId, title, summary, extra){
    if(!state || !state.pvpHumanVsHuman) return null;
    side=side==='AI'?'AI':'PLAYER';
    var type=String(label||'ACTION').toUpperCase();
    if(visibleOpponentEventTypes().indexOf(type)===-1) type='ACTION';
    var evt=Object.assign({
      id:'pvp_'+side+'_'+Date.now()+'_'+Math.floor(Math.random()*1000000),
      label:type,
      card_id:cardId||null,
      title:title||cardName(card(cardId)),
      action_line:null,
      target_side:null,
      target_lane:null,
      target_label:null,
      summary:summary||'',
      detail_lines:[],
      result_lines:normalizeLines(summary),
      response_lines:[],
      reason_lines:[],
      round:Number(state.round||1),
      phase:state.phase,
      turn:state.turn,
      source_side:side,
      timestamp:Date.now()
    }, extra||{});
    evt.result_lines=normalizeLines(evt.result_lines && evt.result_lines.length ? evt.result_lines : evt.summary);
    lockEventTargetSnapshot(state,evt);
    state.pvpActionEventsBySide=state.pvpActionEventsBySide||{PLAYER:[],AI:[]};
    state.pvpActionEventsBySide.PLAYER=state.pvpActionEventsBySide.PLAYER||[];
    state.pvpActionEventsBySide.AI=state.pvpActionEventsBySide.AI||[];
    state.pvpActionEventsBySide[side].unshift(evt);
    if(state.pvpActionEventsBySide[side].length>120) state.pvpActionEventsBySide[side].length=120;
    return evt;
  }
  function applyPvpLocalOpponentPlayedTimeline(state){
    if(!state || !state.pvpHumanVsHuman) return;
    state.pvpActionEventsBySide=state.pvpActionEventsBySide||{PLAYER:[],AI:[]};
    var events=(state.pvpActionEventsBySide.AI||[]).slice(0,120);
    if(!events.length && state.lastOpponentAction && state.lastOpponentAction.source_side==='AI') events=[state.lastOpponentAction];
    state.opponentPlayedEvents=events;
    state.lastOpponentAction=events[0]||null;
    state.selectedOpponentEventId=(events[0]&&events[0].id)||null;
  }
  function appendOpponentEventResult(state, evtOrCardId, lines){
    if(!state) return;
    var evt=null;
    if(typeof evtOrCardId==='object' && evtOrCardId && evtOrCardId.id) evt=evtOrCardId;
    else evt=(state.opponentPlayedEvents||[]).find(function(e){ return e.card_id===evtOrCardId; }) || state.lastOpponentAction;
    if(!evt) return;
    evt.result_lines=(evt.result_lines||[]).concat(normalizeLines(lines));
    evt.summary=evt.result_lines.join('\n');
  }
  function findOpponentEventForAction(state, action, label){
    if(!state || !action) return null;
    if(action.opponent_event_id){var byId=(state.opponentPlayedEvents||[]).find(function(e){return e.id===action.opponent_event_id;});if(byId)return byId;}
    var type=String(label||'ATK').toUpperCase();
    return (state.opponentPlayedEvents||[]).find(function(e){
      return e.label===type && e.card_id===action.card_id &&
        (!e.source_side || e.source_side===action.source_side) && (!e.source_lane || e.source_lane===action.source_lane) &&
        (!e.target_side || e.target_side===action.target_side) && (!e.target_lane || e.target_lane===action.target_lane);
    }) || null;
  }
  function appendOpponentEventResponse(state, evt, responseLines, resultLines){
    if(!state || !evt) return null;
    evt.response_lines=(evt.response_lines||[]).concat(normalizeLines(responseLines));
    evt.result_lines=(evt.result_lines||[]).concat(normalizeLines(resultLines));
    evt.summary=evt.result_lines.join('\n');
    state.lastOpponentAction=evt;
    state.selectedOpponentEventId=evt.id;
    return evt;
  }
  function findOpponentEventById(state, eventId){
    if(!state || !eventId) return null;
    return (state.opponentPlayedEvents||[]).find(function(e){ return e.id===eventId; }) || null;
  }
  function removeOpponentEventById(state, eventId){
    if(!state || !eventId) return null;
    var list=state.opponentPlayedEvents||[], idx=list.findIndex(function(e){ return e.id===eventId; });
    if(idx<0) return null;
    var removed=list.splice(idx,1)[0];
    if(state.lastOpponentAction && state.lastOpponentAction.id===eventId) state.lastOpponentAction=list[0]||null;
    if(state.selectedOpponentEventId===eventId) state.selectedOpponentEventId=(list[0]&&list[0].id)||null;
    return removed;
  }
  function sideHeroes(state, side){ return side==='PLAYER'?state.playerHeroes:state.aiHeroes; }
  function sideHand(state, side){ return side==='PLAYER'?state.playerHand:state.aiHand; }
  function sideDiscard(state, side){ return side==='PLAYER'?state.playerDiscard:state.aiDiscard; }
  function heroClass(hero){ var c=card(heroIdFrom(hero)); var id=c.identity||{}; return id.display_class || id.class || ''; }
  function parseRank(value){
    if(typeof value==='number') return value;
    var s=String(value||'1').toLowerCase();
    if(/iii|rank\s*3|3/.test(s)) return 3;
    if(/ii|rank\s*2|2/.test(s)) return 2;
    return 1;
  }
  function heroRank(hero){ var c=card(heroIdFrom(hero)); var id=c.identity||{}; return parseRank(id.rank||1); }
  function inherentRankExp(hero){ var r=heroRank(hero); return r>=3?700:(r>=2?300:0); }
  function heroLineageId(hero){ var c=card(heroIdFrom(hero)); var id=c.identity||{}; return id.fixed_class_lineage_id || ''; }
  function heroProgressionId(hero){
    var id=heroIdFrom(hero),m=String(id||'').match(/^(S1-[A-Z]+-H)(\d{3})$/);
    if(!m)return String(id||'');var n=Number(m[2]),base=n-((n-1)%3);return m[1]+String(base).padStart(3,'0');
  }
  function castingSourceMatchesHero(pc,hero){
    if(!pc||!hero)return false;
    var current={card_id:heroIdFrom(hero),progression_id:heroProgressionId(hero),instance_id:hero.instance_id||''};
    var locked=pc.source_progression_id||heroProgressionId({card_id:pc.source_hero_card_id});
    if(window.GL_RUNTIME_AUTHORITY&&window.GL_RUNTIME_AUTHORITY.castingSourceMatches)return window.GL_RUNTIME_AUTHORITY.castingSourceMatches(Object.assign({},pc,{source_progression_id:locked}),current);
    if(pc.source_instance_id&&current.instance_id)return String(pc.source_instance_id)===String(current.instance_id);
    return locked?locked===current.progression_id:String(pc.source_hero_card_id||'')===String(current.card_id||'');
  }
  function cardBaseSkillClass(c){
    var req=(c&&c.requirement)||{};
    if(req.base_skill_class) return req.base_skill_class;
    if(req.source_package) return req.source_package;
    var id=String((c&&c.card_id)||'');
    if(/^S1-MAG-/i.test(id)) return 'Mage';
    if(/^S1-THF-/i.test(id)) return 'Thief';
    if(/^S1-ARC-/i.test(id)) return 'Archer';
    if(/^S1-WAR-/i.test(id)) return 'Warrior';
    if(/^S1-CLE-/i.test(id)) return 'Cleric';
    return '';
  }
  function hybridRankOneFallbackBaseClass(c, hero){
    if(!c || !hero || cardFamily(c)!=='Skill') return null;
    var req=c.requirement||{}, gate=req.rank_gate||{};
    if(gate.do_not_fallback_to_base_family_for_rank_locked_cards) return null;
    var minRank=gate.min_active_rank ? Number(gate.min_active_rank) : 1;
    if(minRank>1) return null;
    var base=cardBaseSkillClass(c), hc=heroClass(hero);
    if((hc==='Spell Blade' || hc==='Arcane Duelist') && (base==='Thief' || base==='Mage')) return base;
    if((hc==='Arbalest' || hc==='Grand Arbalest') && (base==='Archer' || base==='Thief')) return base;
    return null;
  }
  function allowsHybridRankOneFallback(c, hero){
    var base=hybridRankOneFallbackBaseClass(c, hero);
    if(!base) return false;
    var req=(c&&c.requirement)||{}, legal=req.legal_active_classes || (req.rank_gate&&req.rank_gate.eligible_active_classes) || [];
    return !legal.length || legal.indexOf(base)!==-1;
  }
  function allowsCrusaderPaladinLineage(c, hero){
    if(!c || !hero || heroClass(hero)!=='Crusader') return false;
    var req=c.requirement||{}, legal=req.legal_active_classes || (req.rank_gate&&req.rank_gate.eligible_active_classes) || [];
    return legal.indexOf('Paladin')!==-1;
  }
  function isSkillCard(c){ return cardFamily(c)==='Skill'; }
  function isUltimateCard(c){ return !!(c.requirement && c.requirement.ultimate && c.requirement.ultimate.is_ultimate); }
  function tributeExpValue(c){ return Number((c.tribute&&c.tribute.exp_value)||100); }
  function attachmentCardId(v){ return typeof v==='string'?v:(v&&v.card_id)||null; }
  function attachmentRuntimeLockForCard(c){
    if(!c) return null;
    return c.attachment_policy || (c.lifecycle&&c.lifecycle.attachment_policy) || (c.canonical_execution&&c.canonical_execution.attachment_policy) || null;
  }
  function attachmentPolicyForCard(c, sourceHero){
    if(!c || c.card_id==='S1-ARC-018' || isReloadCard(c)) return null;
    var policy=attachmentRuntimeLockForCard(c); if(!policy || policy.persistent!==true) return null;
    var cls=String(heroClass(sourceHero)||'').toLowerCase();
    if(c.card_id==='S1-CLE-009' && cls && cls!=='priest' && cls!=='saint') return null;
    if(c.card_id==='S1-WAR-022' && cls && cls!=='paladin' && cls!=='crusader') return null;
    var count=policy.remaining_count;
    if(count==='dynamic'){
      if(c.card_id==='S1-CLE-018') count=cls==='priest'?2:1;
      else if(c.card_id==='S1-MAG-018') count=cls==='elementalist'?2:1;
      else count=1;
    }
    return {tick_phase:policy.tick_phase,remaining_count:Number(count||1),counter_mode:policy.counter_mode||'countdown',role:policy.role||'ongoing',host:policy.host==='target_hero'?'target':'source',exact_once_zone_movement:policy.exact_once_zone_movement!==false};
  }
  function requiresAttachmentSlot(c,sourceHero){ return !!attachmentPolicyForCard(c,sourceHero); }
  function isPersistentAttachment(c){ return !!attachmentPolicyForCard(c,null); }
  function isPersistentAttachmentForSource(c,sourceHero){ return !!attachmentPolicyForCard(c,sourceHero); }
  function attachmentSlotIndex(hero){ if(!hero) return -1; hero.attachments=hero.attachments||[null,null]; for(var i=0;i<hero.attachments.length;i++){ if(!hero.attachments[i]) return i; } return -1; }
  function attachmentHostForAction(state,c,action,sourceHero){ var p=attachmentPolicyForCard(c,sourceHero); if(!p)return null; var side=p.host==='target'?(action.target_side||action.source_side||action.side):(action.source_side||action.side); var lane=p.host==='target'?action.target_lane:action.source_lane; var hero=side&&lane?sideHeroes(state,side)[lane]:null; return hero?{hero:hero,side:side,lane:lane,policy:p}:null; }
  function activeAttachmentExpiryName(policy){ if(!policy)return null; if(policy.tick_phase==='END_PHASE')return 'end_phase'; if(policy.tick_phase==='DRAW_PHASE_START')return 'draw_phase_start'; if(policy.tick_phase==='DRAW_EVENT')return 'counter_draw'; return 'casting'; }
  function registerLocalAttachment(state,side,lane,slot,cardId,policy,holder,extra){
    if(!policy) throw new Error('Attachment policy missing for '+cardId+'.');
    var hero=sideHeroes(state,side)[lane]; if(!hero || isLegacyModeHero(hero)) throw new Error('Attachment host is not an active Hero: '+side+' '+lane+'.');
    var token=(holder&&holder.commit_token)||activeCommitTokenFor(state,side,cardId)||('attachment:'+side+':'+lane+':'+slot+':'+cardId+':'+Date.now());
    var attachmentMeta=Object.assign({
      created_phase:String(state.phase||''),
      created_turn:String(state.turn||''),
      created_round:Number(state.round||0)
    }, (holder&&holder.attachment_effect)||{}, extra||{});
    var rec=window.GL_RUNTIME_AUTHORITY.placeAttachment(state,{side:side,lane:lane,slot:Number(slot),card_id:cardId,commit_token:token,tick_phase:policy.tick_phase,remaining_count:Number(policy.remaining_count||1),counter_mode:policy.counter_mode,role:policy.role,expires:activeAttachmentExpiryName(policy)},hero,attachmentMeta);
    // The Attachment record is the sole ongoing-effect authority. Hero status badges are reserved
    // for actual statuses (Poison, Burn, Freeze, Bleed, Stun, and similar state conditions).
    finalizeCommittedSourceCard(state,holder||{commit_token:token},side,cardId,'attachment',{side:side,lane:lane,slot:Number(slot),attachment_id:rec.attachment_id});
    return rec;
  }
  function findLocalAttachment(state,side,lane,slot,cardId){
    return (state&&state.activeAttachments||[]).find(function(a){ return a&&a.side===side&&a.lane===lane&&Number(a.slot)===Number(slot)&&a.card_id===cardId; })||null;
  }
  function heroRuntimeLocation(state,hero){
    if(!state||!hero) return null;
    var sides=['PLAYER','AI'];
    for(var si=0;si<sides.length;si++){
      var side=sides[si], heroes=sideHeroes(state,side)||{};
      for(var li=0;li<LANE_ORDER.length;li++){
        var lane=LANE_ORDER[li];
        if(heroes[lane]===hero) return {side:side,lane:lane};
      }
    }
    return null;
  }
  function activeAttachmentsForHero(state,hero){
    var loc=heroRuntimeLocation(state,hero); if(!loc) return [];
    return (state.activeAttachments||[]).filter(function(a){return a&&a.side===loc.side&&a.lane===loc.lane;});
  }
  function activeAttachmentForHero(state,hero,cardId){
    return activeAttachmentsForHero(state,hero).find(function(a){return !cardId||a.card_id===cardId;})||null;
  }
  function hasActiveAttachmentForHero(state,hero,cardId){ return !!activeAttachmentForHero(state,hero,cardId); }
  function activeAttachmentForSide(state,side,cardId){
    return (state&&state.activeAttachments||[]).find(function(a){return a&&a.side===side&&(!cardId||a.card_id===cardId);})||null;
  }
  function releaseActiveAttachmentForHero(state,hero,cardId,reason){
    var att=activeAttachmentForHero(state,hero,cardId); return att?releaseLocalAttachment(state,att,reason||'Attachment effect consumed'):false;
  }
  function heroHasSingleTargetProtection(state,hero){
    return ['S1-THF-027','S1-ITM-015','S1-CLE-009'].some(function(id){return hasActiveAttachmentForHero(state,hero,id);});
  }
  function attachmentModifierAmount(state,hero,cardId,fallback){
    var att=activeAttachmentForHero(state,hero,cardId); if(!att) return 0;
    return Number(att.modifier_amount||att.amount||att.bonus||fallback||0);
  }
  function syntheticAttachmentRecord(state,side,lane,slot,cardId){
    state.runtimeSyntheticAttachmentSequence=Number(state.runtimeSyntheticAttachmentSequence||0)+1;
    return {attachment_id:'ATT:RECOVER:'+state.runtimeSyntheticAttachmentSequence+':'+side+':'+lane+':'+slot+':'+cardId,side:side,lane:lane,slot:Number(slot),card_id:cardId};
  }
  function releaseLocalAttachment(state,attachment,reason){
    if(!state||!attachment) return false;
    var hero=sideHeroes(state,attachment.side)[attachment.lane], cardId=attachment.card_id;
    var result=window.GL_RUNTIME_AUTHORITY.releaseAttachment(state,attachment,hero,function(){ sideDiscard(state,attachment.side).push(cardId); },reason||'attachment release');
    if(result.applied) queueAttachmentDiscardMotion(attachment.side,attachment.lane,attachment.slot,cardId);
    return result.applied;
  }
  function releaseLocalAttachmentAt(state,side,lane,slot,cardId,reason){
    var hero=sideHeroes(state,side)[lane], visualId=hero&&hero.attachments&&attachmentCardId(hero.attachments[slot]);
    var rec=findLocalAttachment(state,side,lane,slot,cardId);
    if(!rec && visualId===cardId) rec=syntheticAttachmentRecord(state,side,lane,slot,cardId);
    return rec?releaseLocalAttachment(state,rec,reason):false;
  }
  function discardCardExactOnce(state,side,cardId,beforeCount){ var d=sideDiscard(state,side), target=Number(beforeCount||0)+1; while(d.filter(function(id){return id===cardId;}).length>target){ var last=d.lastIndexOf(cardId); if(last>=0)d.splice(last,1); else break; } if(d.filter(function(id){return id===cardId;}).length<target)d.push(cardId); }
  function completeCommittedCardCustody(state,side,cardId,commitToken){ if(!state||!Array.isArray(state.committedCardCustody))return; for(var i=state.committedCardCustody.length-1;i>=0;i--){var e=state.committedCardCustody[i];if(e.side===side&&e.card_id===cardId&&(!commitToken||e.token===commitToken)){state.committedCardCustody.splice(i,1);return;}} }
  function attachmentRuntimeProtected(state, side, lane, slot, cardId){
    if(!state || !cardId) return false;
    if(state.responseWindow){ var rw=state.responseWindow, post=rw.post||{}; if(rw.source_side===side && rw.source_lane===lane && Number(post.attachmentSlot)===Number(slot) && rw.card_id===cardId) return true; }
    if(state.pendingCastings && state.pendingCastings.some(function(pc){ return pc.side===side && pc.source_lane===lane && Number(pc.attachmentSlot)===Number(slot) && pc.card_id===cardId; })) return true;
    if(state.activeAttachments && state.activeAttachments.some(function(a){ return a.side===side && a.lane===lane && Number(a.slot)===Number(slot) && a.card_id===cardId; })) return true;
    return false;
  }
  function cleanupResolvedAttachmentSlots(state, reason){
    if(!state || state.pending || state.responseWindow) return 0;
    var cleaned=0;
    ['PLAYER','AI'].forEach(function(side){
      var heroes=sideHeroes(state,side)||{};
      LANE_ORDER.forEach(function(lane){
        var h=heroes[lane]; if(!h || !h.attachments) return;
        h.attachments.forEach(function(v,idx){
          var id=attachmentCardId(v); if(!id) return;
          if(attachmentRuntimeProtected(state,side,lane,idx,id)) return;
          var c=card(id), fam=cardFamily(c);
          // Attachment Slot is a host slot, not a card family. Only cards with active runtime anchors may remain here.
          // Instant/normal Attack, instant Skill, resolved Item/Event staging, and canceled cards must leave the slot.
          if(fam==='Skill' || fam==='Item' || fam==='Event'){
            if(releaseLocalAttachmentAt(state,side,lane,idx,id,'explicit attachment recovery'+(reason?' · '+reason:''))){
              cleaned++;
              pushLog(state, side+' '+lane+' Attachment Slot recovery: '+cardName(c)+' moved to Discard Pile after resolution'+(reason?' · '+reason:'')+'.');
            }
          }
        });
      });
    });
    return cleaned;
  }
  function cardPlayPhaseAllowed(c, phase){
    if(!phase || phase==='Setup') return false;
    var text=(cardPhase(c)||'').toLowerCase();
    if(!text){
      if(cardFamily(c)==='Event' || cardFamily(c)==='Item') return phase==='Deploy' || phase==='Reform';
      return false;
    }
    return text.indexOf(phase.toLowerCase())!==-1;
  }
  function runtimeTagList(c){
    var tags=c&&c.runtime_tags;
    if(Array.isArray(tags)) return tags.map(function(t){ return String(t||'').trim(); }).filter(Boolean);
    return String(tags||'').split(/[;,|]/).map(function(t){ return t.trim(); }).filter(Boolean);
  }
  function isResponseOnly(c){
    if(!c) return false;
    var timing=c.timing||{};
    var text=[cardSubtype(c), c.action_category, timing.action_category, timing.phase, timing.raw_phase, timing.phase_or_window].join(' ');
    if(/response|reactive|defen[cs]e skill|defend skill|DEF Response|Area Attack Response/i.test(text||'')) return true;
    var tags=runtimeTagList(c).map(function(t){ return t.toUpperCase(); });
    if(tags.some(function(t){ return t==='DEFEND' || t==='DEFENSE_RESPONSE' || t==='RESPONSE' || t==='REACTIVE'; })) return true;
    if(Array.isArray(c.effect) && c.effect.some(function(e){
      var k=String(e&&e.kind||'').toLowerCase();
      return k==='block_damage' || k==='dodge_incoming_attack_damage' || k==='dodge_incoming_damage' || k==='negate_incoming_attack' || k==='redirect_incoming_single_target_attack_by_cover_up' || k==='area_defense_exception_damage_reduction' || k==='cancel_response_skill';
    })) return true;
    return false;
  }
  function isAttackCard(c){ return /attack/i.test(cardSubtype(c)||'') || /attack/i.test((c&&c.action_category)||'') || !!c.attack || (Array.isArray(c.effect)&&c.effect.some(function(e){return /damage/i.test(e.kind||'') && /attack/i.test(e.attack_type||'');})); }
  function isHealCard(c){ return !!(c && Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='heal' || e.kind==='heal_target_hero' || e.kind==='heal_allied_heroes' || e.kind==='heal_all_allied_heroes'; })); }
  function isDrawCard(c){ return Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='draw_cards'; }); }
  function hasInflictStatusEffect(c){ return Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='inflict_status'; }); }
  function isStatusTactical(c){ return hasInflictStatusEffect(c) && !isAttackCard(c) && !isResponseOnly(c); }
  function requiresSourceHero(c){
    // Final Grit is a tactical revive/legacy-recovery action and must not require an active Warrior source.
    // Otherwise it becomes unplayable exactly when the Warrior line is already in Legacy Mode.
    if(isFinalGritCard(c)) return false;
    if(cardFamily(c)==='Skill') return true;
    if(c.requirement && c.requirement.source_validator_required) return true;
    return false;
  }
  function sourceCompatible(c, hero){
    return sourceCompatibleForMode(c, hero, {response:false});
  }
  function sourceCompatibleForResponse(c, hero){
    return sourceCompatibleForMode(c, hero, {response:true});
  }
  function normalizedClassToken(value){return String(value||'').trim().toLowerCase();}
  function splitCanonicalClassList(value){
    if(Array.isArray(value)) return value.map(function(v){return String(v||'').trim();}).filter(Boolean);
    if(value==null||value==='') return [];
    return String(value).split(/[;,]/).map(function(v){return v.trim();}).filter(Boolean);
  }
  function canonicalCardLegalClasses(c){
    c=c||{};var req=c.requirement||{},src=c.source_requirement||{},canon=c.canonical_legality||{},rank=req.rank_gate||src.rank_gate||{},values=[c.legal_active_classes,req.legal_active_classes,src.legal_active_classes,rank.eligible_active_classes,c.runtime_legality&&c.runtime_legality.legal_active_classes,c.structured_legality&&c.structured_legality.legal_active_classes,canon.legal_active_classes,canon.source_requirement&&canon.source_requirement.legal_active_classes,canon.runtime_legality&&canon.runtime_legality.legal_active_classes],out=[];
    values.forEach(function(v){out=out.concat(splitCanonicalClassList(v));});
    return out.filter(function(v,i,a){var token=normalizedClassToken(v);return token&&a.findIndex(function(x){return normalizedClassToken(x)===token;})===i;});
  }
  function canonicalMinimumSourceRank(c){
    c=c||{};var req=c.requirement||{},src=c.source_requirement||{},canon=c.canonical_legality||{};
    var gates=[req.rank_gate,src.rank_gate,canon.rank_gate,canon.source_requirement&&canon.source_requirement.rank_gate].filter(Boolean);
    var ranks=gates.map(function(g){return Number(g.min_active_rank||0);}).filter(function(n){return Number.isFinite(n)&&n>0;});
    return ranks.length?Math.max.apply(Math,ranks):1;
  }
  function lineageContainsAnyClass(hero,classes){
    var lineage=activeLineageForHero(hero).map(normalizedClassToken), legal=(classes||[]).map(normalizedClassToken);
    return !legal.length || legal.some(function(cls){return lineage.indexOf(cls)!==-1;});
  }
  function sourceCompatibleForMode(c, hero, opts){
    opts=opts||{};
    var exhaustedAllowed=!!(c && c.card_id==='S1-THF-027' && !opts.response);
    if(!hero || hero.hp<=0 || (!opts.response && hero.exhausted && !exhaustedAllowed) || hasStatus(hero,'Stun')) return false;
    if(!opts.response && c && cardFamily(c)==='Skill' && hero.stamina_no_proactive_skill_turn===runtimeTurnStamp(appState)) return false;
    if(isLegacyModeHero(hero)) return false;
    if(window.GL_RULES_RUNTIME && window.GL_RULES_RUNTIME.cardLegalForLineage && !window.GL_RULES_RUNTIME.cardLegalForLineage(c,hero,card(heroIdFrom(hero)),heroClass(hero))) return false;
    var legal=canonicalCardLegalClasses(c);
    if(!lineageContainsAnyClass(hero,legal)) return false;
    if(requiresAttachmentSlot(c,hero) && attachmentSlotIndex(hero)===-1) return false;
    var req=c.requirement||{},src=c.source_requirement||{},ultimate=req.ultimate||src.ultimate||{};
    var minRank=canonicalMinimumSourceRank(c);
    if(minRank && heroRank(hero)<minRank) return false;
    if(ultimate && ultimate.is_ultimate){
      var owners=ultimate.owner_lineage_card_ids||[];
      if(!owners.length || owners.indexOf(heroIdFrom(hero))===-1) return false;
    }
    return true;
  }
  function cardPlayCostForSource(c, sourceHero, state, sourceSide){
    if(isExecuteEffect(c)) return executeCostBySource(c, sourceHero);
    if(isArrowBarrageCard(c)) return 1;
    state=state||appState; sourceSide=sourceSide||'PLAYER';
    return window.GL_RULES_RUNTIME.dynamicManaCost(c,sourceHero,state,sourceSide,{printedCost:Number(cardCost(c)||0),heroRank:sourceHero?heroRank(sourceHero):1,heroClass:sourceHero?heroClass(sourceHero):'',heroCard:sourceHero?card(heroIdFrom(sourceHero)):null,manaRegenForSide:function(st,side){return Number(st&&st[side==='PLAYER'?'manaRegen':'aiManaRegen']||0);}});
  }
  function minimumPlayableCostForLegalSource(state,c){
    var lanes=state?legalSourceLanes(state,c.card_id):[];
    if(!lanes.length) return Number(cardCost(c)||0);
    var heroes=sideHeroes(state,'PLAYER'), best=Infinity;
    lanes.forEach(function(lane){ best=Math.min(best, cardPlayCostForSource(c,heroes[lane])); });
    return best===Infinity?Number(cardCost(c)||0):best;
  }
  function legalSourceLanes(state, cardId){
    var c=card(cardId); var out=[]; var heroes=sideHeroes(state,'PLAYER');
    LANE_ORDER.forEach(function(lane){ if(sourceCompatible(c,heroes[lane])) out.push(lane); });
    return out;
  }
  function legalPlayState(state, cardId){
    var c=card(cardId); var reasons=[];
    if(!matchStarted || !state) reasons.push('match not started');
    if(state && (state.turn!=='PLAYER')) reasons.push('not player turn');
    if(state && state.pending) reasons.push('another action pending');
    if(isResponseOnly(c)) reasons.push('response card waits for Response Window');
    if(state && !cardPlayPhaseAllowed(c,state.phase)) reasons.push('wrong phase');
    var cost=state?minimumPlayableCostForLegalSource(state,c):Number(cardCost(c)||0); if(state && cost>Number(state.mana||0)) reasons.push('not enough mana');
    if(state && requiredDiscardSpecificIds(c).length && !handHasRequiredDiscards(sideHand(state,'PLAYER'),c)) reasons.push('required discard cards missing from hand');
    if(state && requiresSourceHero(c) && !legalSourceLanes(state,cardId).length) reasons.push('no legal source hero');
    if(state){ var pre=validateCardPreconditions(state,'PLAYER',cardId,null,null,{preplay:true}); if(!pre.ok) reasons=reasons.concat(pre.reasons); }
    return {can:reasons.length===0, reasons:reasons, cost:cost};
  }
  function oppositeSide(side){ return side==='PLAYER'?'AI':'PLAYER'; }

  function isLegacyCardId(id){ return /^S1-[A-Z]{3}-L\d+/i.test(String(id||'')); }
  function isLegacyDefinitionCard(c){ return !!(c && (c.identity_mode==='Legacy Mode Definition' || cardFamily(c)==='LegacyModeDefinition' || c.legacy_deck_entry===true || isLegacyCardId(c.card_id))); }
  function isLegacyModeHero(hero){
    if(!hero) return false;
    var id=heroIdFrom(hero) || hero.active_legacy_card_id;
    var c=card(id);
    return !!(hero.legacy_mode || hero.legacy || hero.active_legacy_card_id || isLegacyCardId(id) || isLegacyDefinitionCard(c));
  }
  function heroSlotMode(hero){ return isLegacyModeHero(hero) ? 'LEGACY' : 'HERO'; }
  function isHeroSlot(hero){ return !!(hero && heroSlotMode(hero)==='HERO'); }
  function isLegacySlot(hero){ return !!(hero && heroSlotMode(hero)==='LEGACY'); }
  function isActiveHeroSlot(hero){ return !!(isHeroSlot(hero) && Number(hero.hp||0)>0); }
  function sanitizeLegacySlot(hero){
    if(!isLegacySlot(hero)) return hero;
    delete hero.hp; delete hero.maxHp; delete hero.exp_total; delete hero.exp_cards; delete hero.statuses; delete hero.exhausted; delete hero.exhaust_reason; delete hero.attachments;
    hero.legacy_mode=true;
    return hero;
  }
  function sanitizeHeroSlotsForSide(state, side){
    var heroes=state&&sideHeroes(state,side); if(!heroes) return;
    LANE_ORDER.forEach(function(l){ sanitizeLegacySlot(heroes[l]); });
  }
  function sanitizeAllHeroSlots(state){ sanitizeHeroSlotsForSide(state,'PLAYER'); sanitizeHeroSlotsForSide(state,'AI'); return state; }
  function legacyDefeatedOverlayName(hero){
    var snap=hero && (hero.defeated_hero_snapshot || hero.legacy_overlay);
    return snap && snap.card_id ? cardName(card(snap.card_id)) : 'Unknown Hero';
  }
  function legacyOriginalHeroClass(hero){
    if(hero && hero.defeated_hero_snapshot){ return heroClass(hero.defeated_hero_snapshot); }
    return heroClass(hero);
  }

  function firstPlayerAttackRestrictedNow(state, side, cardObj){
    var attackLike=!!(cardObj && (/Attack/i.test(String(cardSubtype(cardObj)||cardObj.classification||cardObj.action_category||'')) || /ATTACK/i.test(String(cardObj.runtime_tags||'')) || isAttackCard(cardObj)));
    return !!(state && cardObj && attackLike && state.phase==='Battle' && Number(state.round||1)===1 && openingFirstSide(state)===side);
  }
  function validateCardPreconditions(state, side, cardId, sourceLane, targetLane, context){
    var c=card(cardId), reasons=[]; context=context||{};
    if(!c || !c.card_id) reasons.push('unknown card');
    var opp=oppositeSide(side);
    if(firstPlayerAttackRestrictedNow(state, side, c)) reasons.push('First player cannot play Attack Skill Cards on their first turn.');
    if(isFinalGritCard(c) && state && (!legalTargetLanesFor(state,side,cardId,sourceLane).length)) reasons.push('Final Grit requires a valid allied Legacy Mode Gladiator/Conqueror target');
    if(isScoutingCard(c) && state && !hasLegalScoutingExpTarget(state,side)) reasons.push('Scouting requires opponent non-Ultimate EXP card in an active Hero EXP stack');
    if(isRemoveStatusCard(c) && state && !legalTargetLanesFor(state,side,cardId,sourceLane).length) reasons.push(cardName(c)+' requires an allied active Hero with a negative status.');
    if(isStealCard(c) && state && Number(state[manaPoolKeyForSide(opp)]||0)<=0) reasons.push('Steal requires opponent Mana Pool > 0');
    if(isVenomDetonationCard(c) && state){ var hasPoison=false; LANE_ORDER.forEach(function(l){ var h=sideHeroes(state,opp)[l]; if(h && hasStatus(h,'Poison')) hasPoison=true; }); if(!hasPoison) reasons.push('Venom Detonation requires at least one poisoned opponent Hero'); }
    if(isReplenishCard(c) && state){ var d=sideDiscard(state,side)||[]; if(d.filter(function(id){ return archerNonUltimateSkillCard(card(id)); }).length<3) reasons.push('Replenish requires 3 legal Archer non-Ultimate Skill cards in discard'); }
    if(isLastResortCard(c)&&state){ var active=LANE_ORDER.filter(function(l){var h=sideHeroes(state,side)[l];return h&&Number(h.hp||0)>0&&!isLegacyModeHero(h);}); if(active.length!==1) reasons.push('Last Resort requires exactly 1 active allied Hero'); }
    if(isForgedAllianceCard(c)&&state&&Number(state[side==='PLAYER'?'racial':'aiRacial']||0)>=RACIAL_TOKEN_CAP) reasons.push('Forged Alliance cannot be used at the Racial Token cap');
    if(isRelentlessLevelingCard(c)&&state){ var source=sourceLane&&sideHeroes(state,side)[sourceLane], hand=sideHand(state,side)||[]; var hasLegal=hand.some(function(id){return id!==cardId&&nonUltimateSkillCard(card(id))&&(!source||heroCanReceiveExpCard(source,card(id)));}); if(!hasLegal) reasons.push('Relentless Leveling requires a legal non-Ultimate Skill in hand'); }
    if(isDejaVuCard(c)&&state && !(sideDiscard(state,side)||[]).some(function(id){return nonUltimateSkillCard(card(id));})) reasons.push('Déjà vu requires a non-Ultimate Skill in Discard Pile');
    if(c&&c.card_id==='S1-CLE-013'&&state){ var gh=(sideHand(state,side)||[]).filter(function(id){return id!==cardId;}); if(!gh.length) reasons.push("God's Blessing requires 1 other card in hand to discard"); }
    if(sourceLane){ var sh=sideHeroes(state,side)[sourceLane]; if(isLegacyModeHero(sh)) reasons.push('Legacy Mode cannot use Skill, Item, Racial, Class, Reposition, or Tribute actions'); }
    if(targetLane && targetSideForActor(c,side)===side){ var th=sideHeroes(state,side)[targetLane]; if((isHeroAttachmentItem(c)||isStaminaPotionCard(c)||isElixirCard(c)||isBuffCard(c)) && isLegacyModeHero(th)) reasons.push('Legacy Mode cannot receive attachments or staged effects'); if(isPoisonVialCard(c) && !poisonVialHostEligible(th)) reasons.push('Poison Vial can only be attached to Thief/Archer lineage Heroes.'); }
    return {ok:reasons.length===0, reasons:reasons};
  }
  function validateSourceHero(state, side, sourceLane, cardId, context){
    var c=card(cardId), h=sourceLane?sideHeroes(state,side)[sourceLane]:null, reasons=[], targetHostItem=!!(context&&context.target_host_item&&isHeroAttachmentItem(c));
    if(requiresSourceHero(c) && !sourceLane) reasons.push('source Hero required');
    if(sourceLane && !targetHostItem && !sourceCompatible(c,h)) reasons.push('illegal source Hero');
    if(h && isLegacyModeHero(h)) reasons.push('Legacy Mode cannot be an action source');
    return {ok:reasons.length===0, reasons:reasons};
  }
  function validateTarget(state, side, cardId, sourceLane, targetLane, context){
    var c=card(cardId), reasons=[], targetSide=targetSideForActor(c,side);
    if(needsCommitTargetForActor(c,side)){
      if(!targetLane) reasons.push('target required');
      else if(legalTargetLanesFor(state,side,cardId,sourceLane).indexOf(targetLane)===-1) reasons.push('illegal target');
    }
    return {ok:reasons.length===0, reasons:reasons};
  }
  function validateCost(state, side, cardId, sourceLane, context){
    var c=card(cardId), h=sourceLane?sideHeroes(state,side)[sourceLane]:null, cost=cardPlayCostForSource(c,h), reasons=[];
    if(Number(state[manaPoolKeyForSide(side)]||0)<cost) reasons.push('not enough mana');
    return {ok:reasons.length===0, reasons:reasons, cost:cost};
  }
  function validateAction(state, side, action, context){
    context=context||{}; action=action||{}; var reasons=[];
    if(!state) reasons.push('no state');
    if(state && state.turn!==side) reasons.push('not '+side+' turn');
    if(state && state.gameOver) reasons.push('game over');
    if(state && state.pending && !context.committingPending && !(state.pending.type==='response_window')) reasons.push('another action pending');
    if(action.type==='TRIBUTE_CARD'){
      var ts=legalTributeStateFor(state,side,action.card_id); if(!ts.can) reasons=reasons.concat(ts.reasons);
      if(action.target_lane && legalTributeTargetLanesFor(state,side,action.card_id).indexOf(action.target_lane)===-1) reasons.push('illegal Tribute target');
      return {ok:reasons.length===0, reasons:reasons, cost:0};
    }
    if(action.type==='PLAY_CARD' || !action.type){
      var c=card(action.card_id); if(!c || !c.card_id) reasons.push('unknown card');
      if(state && !cardPlayPhaseAllowed(c,state.phase)) reasons.push('wrong phase');
      if(isResponseOnly(c) && !(context && context.response)) reasons.push('response card waits for Response Window');
      var pre=validateCardPreconditions(state,side,action.card_id,action.source_lane,action.target_lane,context); if(!pre.ok) reasons=reasons.concat(pre.reasons);
      var sourceContext=Object.assign({},context,{target_host_item:!!(isHeroAttachmentItem(c)&&action.source_lane&&action.target_lane===action.source_lane&&(action.target_side||side)===side)}); var src=validateSourceHero(state,side,action.source_lane,action.card_id,sourceContext); if(!src.ok) reasons=reasons.concat(src.reasons);
      var tgt=validateTarget(state,side,action.card_id,action.source_lane,action.target_lane,context); if(!tgt.ok) reasons=reasons.concat(tgt.reasons);
      var cost=validateCost(state,side,action.card_id,action.source_lane,context); if(!cost.ok) reasons=reasons.concat(cost.reasons);
      return {ok:reasons.length===0, reasons:reasons, cost:cost.cost};
    }
    return {ok:reasons.length===0, reasons:reasons, cost:0};
  }
  function assertActionStillLegalBeforeCommit(state, action){ return validateAction(state, action.side||action.source_side||'PLAYER', action, {committingPending:true}); }

  function manaPoolKeyForSide(side){ return side==='PLAYER'?'mana':'aiMana'; }
  function targetSideForActor(c, side){
    if(isCamouflageCard(c)) return null;
    if(side==='PLAYER') return targetSideFor(c);
    if(isAttackCard(c)) return 'PLAYER';
    if(isFinalGritCard(c)) return 'AI';
    if(isHealAllCard(c) || isGodsBlessingCard(c) || isPoisonMistCard(c) || isWildfireCard(c) || isSixthSenseCard(c) || isStealCard(c) || isMagicCompassCard(c) || isMagicScopeCard(c) || isCrystalBallCard(c) || isBeginAnewCard(c) || isLastResortCard(c) || isForgedAllianceCard(c) || isDejaVuCard(c) || isCoordinationAttackCard(c) || isRelentlessLevelingCard(c)) return null;
    if(isHealCard(c) || isRemoveStatusCard(c) || isReviveCard(c) || isBuffCard(c) || isStaminaPotionCard(c) || isHeroAttachmentItem(c)) return 'AI';
    if(isStatusTactical(c)) return 'PLAYER';
    return null;
  }
  function needsTargetSideForResolution(c, side){ return !!targetSideForActor(c,side); }
  function isDualTargetAttack(c){ return attackType(c)==='Dual Target'; }
  function needsPlayerTargetSelection(c, state, side, sourceLane){
    if(!targetSideForActor(c,side)) return false;
    if(isAttackCard(c) && (isAreaAttack(c) || isAllTargetAttack(c))) return false;
    if(isDualTargetAttack(c)){
      var lanes=sourceLane?legalTargetLanesFor(state,side,c.card_id,sourceLane):[];
      return lanes.length>2;
    }
    return true;
  }
  function needsCommitTargetForActor(c, side){
    if(!targetSideForActor(c,side)) return false;
    if(isAttackCard(c) && (isAreaAttack(c) || isAllTargetAttack(c) || isDualTargetAttack(c))) return false;
    return true;
  }
  function needsTargetForActor(c, side){ return needsCommitTargetForActor(c,side); }
  function legalSourceLanesFor(state, side, cardId){
    var c=card(cardId), out=[]; if(!state) return out;
    var heroes=sideHeroes(state,side);
    LANE_ORDER.forEach(function(lane){ if(sourceCompatible(c,heroes[lane])) out.push(lane); });
    return out;
  }
  function minimumPlayableCostForLegalSourceFor(state, side, c){
    var lanes=state?legalSourceLanesFor(state,side,c.card_id):[];
    if(!lanes.length) return Number(cardCost(c)||0);
    var heroes=sideHeroes(state,side), best=Infinity;
    lanes.forEach(function(lane){ best=Math.min(best, cardPlayCostForSource(c,heroes[lane])); });
    return best===Infinity?Number(cardCost(c)||0):best;
  }
  function legalTargetLanesFor(state, side, cardId, sourceLane){
    var c=card(cardId); var targetSide=targetSideForActor(c,side); if(!state || !targetSide) return [];
    var heroes=sideHeroes(state,targetSide); var lanes=LANE_ORDER.slice(); var sourceHero=sideHeroes(state,side)[sourceLane];
    if(isAttackCard(c)){
      if(isChargedShotCard(c) || isAllTargetAttack(c) || isRangeAttackCard(c)) lanes=LANE_ORDER.slice();
      else if(isSingleTargetAttack(c) && (heroClass(sourceHero)==='Marksman' || heroClass(sourceHero)==='Grand Ranger')) lanes=LANE_ORDER.slice();
      else lanes=targetCoverage(sourceLane);
    }
    var policy=attachmentPolicyForCard(c,sourceHero);
    var legal=lanes.filter(function(lane){ var h=heroes[lane];
      if(isFinalGritCard(c)) return finalGritTargetValid(h);
      if(isPhoenixFeatherCard(c)) return !!(h && (Number(h.hp||0)<=0 || isLegacyModeHero(h)));
      if(isReviveCard(c)) return !!(h && (Number(h.hp||0)<=0 || isLegacyModeHero(h)));
      if(isStaminaPotionCard(c)) return !!(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && h.exhausted);
      if(isElixirCard(c)) return !!(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && h.statuses && h.statuses.some(function(st){ return isNegativeStatusName(statusName(st)); }));
      if(isHeroAttachmentItem(c)){ if(!heroMatchesLineageGate(h,itemLineageGate(c))) return false; return !!(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && attachmentSlotIndex(h)!==-1); }
      if(isAttackCard(c)){
        if(!(h && h.hp>0) || isLegacyModeHero(h)) return false;
        if(isSingleTargetAttack(c) && heroHasSingleTargetProtection(state,h)) return false;
        if(isExecuteEffect(c)) return executeTargetValid(h);
        if(requiresFrozenTarget(c) && !hasStatus(h,'Freeze')) return false;
        return true;
      }
      if(isHealCard(c)) return !!(h && !isLegacyModeHero(h) && healTargetValid(c,h));
      if(isRemoveStatusCard(c)) return !!(h && h.hp>0 && !isLegacyModeHero(h) && h.statuses && h.statuses.some(function(st){ return isNegativeStatusName(statusName(st)); }));
      if(isBuffCard(c)){
        if(!(h && h.hp>0) || isLegacyModeHero(h)) return false;
        if(policy&&policy.host==='target'&&attachmentSlotIndex(h)===-1) return false;
        if((isBlessingMightCard(c)||isBlessingWisdomCard(c)||isHeavensFuryCard(c)) && hasNonStackableAttackBuff(state,h)) return false;
        return true;
      }
      if(!(h && h.hp>0) || isLegacyModeHero(h)) return false;
      return true; });
    if(targetSide===oppositeSide(side) && isSingleTargetAttack(c)) legal=applyTauntTargeting(state,targetSide,legal);
    return legal;
  }
  function legalPlayStateFor(state, side, cardId){
    var c=card(cardId); var reasons=[];
    if(!matchStarted || !state) reasons.push('match not started');
    if(state && state.gameOver) reasons.push('game over');
    if(state && state.turn!==side) reasons.push('not '+side+' turn');
    if(state && (state.pending || state.responseWindow)) reasons.push('another action pending');
    if(isResponseOnly(c)) reasons.push('response card waits for Response Window');
    if(state && !cardPlayPhaseAllowed(c,state.phase)) reasons.push('wrong phase');
    var cost=state?minimumPlayableCostForLegalSourceFor(state,side,c):Number(cardCost(c)||0); if(state && cost>Number(state[manaPoolKeyForSide(side)]||0)) reasons.push('not enough mana');
    if(state && requiredDiscardSpecificIds(c).length && !handHasRequiredDiscards(sideHand(state,side),c)) reasons.push('required discard cards missing from hand');
    if(state && requiresSourceHero(c) && !legalSourceLanesFor(state,side,cardId).length) reasons.push('no legal source hero');
    if(state){ var pre=validateCardPreconditions(state,side,cardId,null,null,{preplay:true}); if(!pre.ok) reasons=reasons.concat(pre.reasons); }
    return {can:reasons.length===0, reasons:reasons, cost:cost};
  }
  function isAIChoiceHeavyCard(c){ return isSixthSenseCard(c) || isRelentlessLevelingCard(c) || isMagicCompassCard(c) || isDejaVuCard(c) || isReplenishCard(c) || isArrowBarrageCard(c) || isTripleShotCard(c) || isDualArrowCard(c) || isChargeSwapCard(c) || isSoulBlastShotCard(c); }
  function actionKey(a){ return [a&&a.type,a&&a.card_id,a&&a.hand_index,a&&a.source_side,a&&a.source_lane,a&&a.target_side,a&&a.target_lane].join('|'); }
  function getLegalActions(side, state){
    var actions=[]; side=side||'PLAYER';
    if(!matchStarted || !state || state.turn!==side || state.pending || state.responseWindow || state.gameOver) return actions;
    actions.push({type:'SKIP', side:side, label:'Skip '+state.phase});
    var hand=sideHand(state,side)||[];
    hand.forEach(function(cardId,idx){ var c=card(cardId), lp=legalPlayStateFor(state,side,cardId); if(!lp.can) return;
      if(requiresSourceHero(c)){
        legalSourceLanesFor(state,side,cardId).forEach(function(srcLane){
          var sourceHero=sideHeroes(state,side)[srcLane], cost=cardPlayCostForSource(c,sourceHero,state,side); if(cost>Number(state[manaPoolKeyForSide(side)]||0)) return;
          if(needsPlayerTargetSelection(c,state,side,srcLane)){
            legalTargetLanesFor(state,side,cardId,srcLane).forEach(function(tLane){ actions.push({type:'PLAY_CARD', side:side, card_id:cardId, hand_index:idx, source_side:side, source_lane:srcLane, source_card_id:heroIdFrom(sourceHero), target_side:targetSideForActor(c,side), target_lane:tLane, cost:cost}); });
          } else actions.push({type:'PLAY_CARD', side:side, card_id:cardId, hand_index:idx, source_side:side, source_lane:srcLane, source_card_id:heroIdFrom(sourceHero), target_side:targetSideForActor(c,side), target_lane:null, cost:cost});
        });
      } else if(needsPlayerTargetSelection(c,state,side,null)){
        legalTargetLanesFor(state,side,cardId,null).forEach(function(tLane){ actions.push({type:'PLAY_CARD', side:side, card_id:cardId, hand_index:idx, source_side:null, source_lane:null, target_side:targetSideForActor(c,side), target_lane:tLane, cost:lp.cost}); });
      } else actions.push({type:'PLAY_CARD', side:side, card_id:cardId, hand_index:idx, source_side:null, source_lane:null, target_side:targetSideForActor(c,side), target_lane:null, cost:lp.cost});
    });
    if(state.phase==='Reform' && !state.tributeUsedThisReform){
      hand.forEach(function(cardId,idx){ var ts=legalTributeStateFor(state,side,cardId); if(!ts.can) return; legalTributeTargetLanesFor(state,side,cardId).forEach(function(lane){ actions.push({type:'TRIBUTE_CARD', side:side, card_id:cardId, hand_index:idx, target_side:side, target_lane:lane, exp:ts.exp}); }); });
    }
    if(side==='AI' && (state.phase==='Deploy' || state.phase==='Reform')){
      repositionPairsForSide(state,'AI').forEach(function(pair){ actions.push({type:'REPOSITION', side:'AI', pair:pair.slice(), score:aiRepositionScore(state,pair), label:'AI Reposition '+pair.join(' ↔ ')}); });
    }
    return actions;
  }
  function legalPlayableAIActions(state){ return getLegalActions('AI',state).filter(function(a){ return a.type==='PLAY_CARD' && !isAIChoiceHeavyCard(card(a.card_id)); }); }
  function estimateAIDamage(state, action){ var src=sideHeroes(state,'AI')[action.source_lane], tgt=sideHeroes(state,'PLAYER')[action.target_lane]; return computedAttackDamageForAction(state,action,src,tgt)||Number(action.cost||0)||0; }
  function isRingOfGraceCard(c){ return !!(c && c.card_id==='S1-ITM-013'); }
  function isArcaneScrollCard(c){ return !!(c && c.card_id==='S1-ITM-010'); }
  function isHolyMedallionCard(c){ return !!(c && c.card_id==='S1-ITM-014'); }
  function isEnrageSetupCard(c){ return !!(c && c.card_id==='S1-WAR-013'); }
  function isAttackBuffSetupCard(c){ return !!(c && (isPoisonVialCard(c) || isCoordinationAttackCard(c) || isHolyMedallionCard(c) || isArcaneScrollCard(c) || isBlessingMightCard(c) || isBlessingWisdomCard(c) || isEnrageSetupCard(c) || isLastResortCard(c))); }
  function isTacticalFollowUpSetupCard(c){ return isAttackBuffSetupCard(c) || isRingOfGraceCard(c) || isDoubleCastingCard(c) || isWildfireCard(c); }
  function aiRemainingManaAfterAction(state, action){ return Math.max(0, Number(state.aiMana||0)-Number(action&&action.cost||0)); }
  function aiCloneHandWithoutActionCard(state, action){
    var simulatedHand=(state.aiHand||[]).slice(), setupIdx=Number(action&&action.hand_index);
    if(simulatedHand[setupIdx]!==action.card_id) setupIdx=simulatedHand.indexOf(action.card_id);
    if(setupIdx>=0) simulatedHand.splice(setupIdx,1);
    return simulatedHand;
  }
  function aiAttackMatchesSetup(state, setupAction, attackAction){
    var setupCard=card(setupAction&&setupAction.card_id), attackCard=card(attackAction&&attackAction.card_id);
    if(!setupCard || !attackCard || !isAttackCard(attackCard)) return false;
    var sourceHero=sideHeroes(state,'AI')[attackAction.source_lane], actualType=damageTypeForActionSource(attackCard,sourceHero);
    if(isPoisonVialCard(setupCard)) return attackAction.source_lane===setupAction.target_lane && isPoisonVialEligibleAttack(attackCard);
    if(isArcaneScrollCard(setupCard)) return attackAction.source_lane===setupAction.target_lane && actualType==='Magical';
    if(isHolyMedallionCard(setupCard)) return attackAction.source_lane===setupAction.target_lane && (actualType==='Physical' || actualType==='Magical');
    if(isBlessingMightCard(setupCard)) return attackAction.source_lane===setupAction.target_lane && actualType==='Physical';
    if(isBlessingWisdomCard(setupCard)) return attackAction.source_lane===setupAction.target_lane && actualType==='Magical';
    if(isEnrageSetupCard(setupCard)) return attackAction.source_lane===setupAction.source_lane && actualType==='Physical';
    if(isLastResortCard(setupCard)) return attackAction.source_lane===setupAction.source_lane;
    if(isCoordinationAttackCard(setupCard)) return isSingleTargetAttack(attackCard);
    return true;
  }
  function aiAttackPlanValue(state, setupAction, attackAction){
    var value=estimateAIDamage(state,attackAction), setupCard=card(setupAction.card_id), attackCard=card(attackAction.card_id);
    if(isPoisonVialCard(setupCard)){
      if(isAllTargetAttack(attackCard) || isAreaAttack(attackCard)) value+=45;
      else if(isDualArrowCard(attackCard)) value+=30;
      else value+=20;
    } else if(isArcaneScrollCard(setupCard) || isHolyMedallionCard(setupCard) || isBlessingMightCard(setupCard) || isBlessingWisdomCard(setupCard) || isEnrageSetupCard(setupCard)) value+=20;
    else if(isLastResortCard(setupCard)) value+=Math.max(20,estimateAIDamage(state,attackAction));
    else if(isCoordinationAttackCard(setupCard)) value+=coordinationAttackBonusAmount(state,'AI')||10;
    return value;
  }
  function coordinationAttackBonusAmount(state,side){
    var heroes=sideHeroes(state,side), bases=[];
    LANE_ORDER.forEach(function(lane){ var h=heroes&&heroes[lane]; if(h&&Number(h.hp||0)>0&&!isLegacyModeHero(h)){ var base=baseFamilyForHero(h); if(base&&bases.indexOf(base)<0) bases.push(base); } });
    return bases.length>=3?20:10;
  }
  function aiBestAttackAfterSetup(state, setupAction){
    if(!state || !setupAction) return null;
    var oldPhase=state.phase, oldMana=state.aiMana, oldPlan=state.aiPlan, savedHand=(state.aiHand||[]).slice();
    state.aiHand=aiCloneHandWithoutActionCard(state,setupAction);
    state.phase='Battle'; state.aiMana=aiRemainingManaAfterAction(state,setupAction); state.aiPlan=null;
    var legal=getLegalActions('AI',state).filter(function(a){
      return a.type==='PLAY_CARD' && isAttackCard(card(a.card_id)) && !isAIChoiceHeavyCard(card(a.card_id)) && aiAttackMatchesSetup(state,setupAction,a);
    });
    legal.sort(function(a,b){ return aiAttackPlanValue(state,setupAction,b)-aiAttackPlanValue(state,setupAction,a) || Number(b.cost||0)-Number(a.cost||0); });
    state.phase=oldPhase; state.aiMana=oldMana; state.aiPlan=oldPlan; state.aiHand=savedHand;
    return legal[0]||null;
  }
  function aiCanFollowSetupWithAttack(state, setupAction){ return !!aiBestAttackAfterSetup(state,setupAction); }
  function aiBestMageAttackAfterSetup(state, setupAction, options){
    options=options||{};
    if(!state||!setupAction||!setupAction.source_lane)return null;
    var setupSource=sideHeroes(state,'AI')[setupAction.source_lane];if(!setupSource)return null;
    var oldPhase=state.phase,oldMana=state.aiMana,oldPlan=state.aiPlan,savedHand=(state.aiHand||[]).slice(),oldExhausted=!!setupSource.exhausted,oldReason=setupSource.exhaust_reason;
    state.aiHand=aiCloneHandWithoutActionCard(state,setupAction);
    state.phase='Battle';state.aiMana=options.next_turn?Math.min(12,aiRemainingManaAfterAction(state,setupAction)+Number(state.aiManaRegen||0)):aiRemainingManaAfterAction(state,setupAction);state.aiPlan=null;
    if(options.next_turn){setupSource.exhausted=false;setupSource.exhaust_reason=null;}
    var legal=getLegalActions('AI',state).filter(function(a){
      var attackCard=card(a.card_id),attackTypeText=String(cardSubtype(attackCard)||attackCard.action_category||'');
      return a.type==='PLAY_CARD'&&isAttackCard(attackCard)&&a.source_lane===setupAction.source_lane&&!isAIChoiceHeavyCard(attackCard)&&(!options.magical_only||/Magical Attack/i.test(attackTypeText));
    });
    legal.sort(function(a,b){return estimateAIDamage(state,b)-estimateAIDamage(state,a)||Number(b.cost||0)-Number(a.cost||0);});
    setupSource.exhausted=oldExhausted;setupSource.exhaust_reason=oldReason;state.phase=oldPhase;state.aiMana=oldMana;state.aiPlan=oldPlan;state.aiHand=savedHand;
    return legal[0]||null;
  }
  function aiBestAttackAfterDoubleCasting(state, setupAction){
    if(!state||!setupAction||!setupAction.source_lane)return null;
    var source=sideHeroes(state,'AI')[setupAction.source_lane],cls=heroClass(source);
    if(cls!=='Elementalist'&&cls!=='Elemental Lord')return null;
    return aiBestMageAttackAfterSetup(state,setupAction,{magical_only:true,next_turn:cls==='Elementalist'});
  }
  function aiBestAttackAfterWildfire(state, setupAction){
    return aiBestMageAttackAfterSetup(state,setupAction,{magical_only:false,next_turn:true});
  }
  function aiFollowUpRoundForSetup(state,action){
    var c=card(action&&action.card_id),source=action&&action.source_lane?sideHeroes(state,'AI')[action.source_lane]:null;
    if(isWildfireCard(c))return Number(state.round||1)+1;
    if(isDoubleCastingCard(c)&&source&&heroClass(source)==='Elementalist')return Number(state.round||1)+1;
    return Number(state.round||1);
  }
  function aiHealActionTargetsLane(c,action,lane){
    if(!c || !isHealCard(c)) return false;
    if(isHealAllCard(c)) return true;
    return action.target_side==='AI' && action.target_lane===lane;
  }
  function aiBaseHealBeforeRing(state, action){
    var c=card(action.card_id), sourceHero=action.source_lane?sideHeroes(state,'AI')[action.source_lane]:null;
    var amount=firstHealAmountForSource(c,sourceHero);
    if(sourceHero && (heroClass(sourceHero)==='Priest' || heroClass(sourceHero)==='Saint')) amount+=20;
    return Math.max(0,Number(amount||0));
  }
  function aiRingHealPlanValue(state, setupAction, healAction){
    var target=sideHeroes(state,'AI')[setupAction.target_lane]; if(!target) return -1000;
    var missing=Math.max(0,Number(target.maxHp||100)-Number(target.hp||0));
    var base=aiBaseHealBeforeRing(state,healAction), realizedBonus=Math.min(20,Math.max(0,missing-base));
    if(realizedBonus<=0) return -1000;
    var expected=Math.min(missing,base+20), c=card(healAction.card_id);
    return realizedBonus*5+expected-Number(healAction.cost||0)+(isHealAllCard(c)?12:0);
  }
  function aiBestHealAfterRing(state, setupAction){
    if(!state || !setupAction || !setupAction.target_lane) return null;
    var target=sideHeroes(state,'AI')[setupAction.target_lane];
    if(!target || Number(target.hp||0)<=0 || isLegacyModeHero(target) || healBlockedByBleed(target) || Number(target.hp||0)>=Number(target.maxHp||100)) return null;
    var oldPhase=state.phase, oldMana=state.aiMana, oldPlan=state.aiPlan, savedHand=(state.aiHand||[]).slice();
    state.aiHand=aiCloneHandWithoutActionCard(state,setupAction);
    state.phase='Reform'; state.aiMana=aiRemainingManaAfterAction(state,setupAction); state.aiPlan=null;
    var legal=getLegalActions('AI',state).filter(function(a){
      var c=card(a.card_id); return a.type==='PLAY_CARD' && isHealCard(c) && !isAIChoiceHeavyCard(c) && aiHealActionTargetsLane(c,a,setupAction.target_lane) && aiRingHealPlanValue(state,setupAction,a)>-999;
    });
    legal.sort(function(a,b){ return aiRingHealPlanValue(state,setupAction,b)-aiRingHealPlanValue(state,setupAction,a); });
    state.phase=oldPhase; state.aiMana=oldMana; state.aiPlan=oldPlan; state.aiHand=savedHand;
    return legal[0]||null;
  }
  function aiCanFollowRingWithHeal(state, setupAction){ return !!aiBestHealAfterRing(state,setupAction); }
  function aiPlanForSetupAction(state,action){
    var c=card(action&&action.card_id);
    if(isDoubleCastingCard(c)){
      var doubleAttack=aiBestAttackAfterDoubleCasting(state,action); if(!doubleAttack) return null;
      var doubleRound=aiFollowUpRoundForSetup(state,action),doubleDelayed=doubleRound>Number(state.round||1);
      return {kind:'ATTACK_FOLLOWUP',followup_phase:'Battle',not_before_round:doubleRound,preferred_card_id:doubleAttack.card_id,preferred_source_lane:doubleAttack.source_lane||null,preferred_target_lane:doubleDelayed?null:(doubleAttack.target_lane||null),preferred_attack_source_lane:doubleAttack.source_lane||null,preferred_attack_card_id:doubleAttack.card_id,setup_card_id:action.card_id,setup_target_lane:null,remaining_mana:aiRemainingManaAfterAction(state,action),double_casting:true};
    }
    if(isWildfireCard(c)){
      var wildfireAttack=aiBestAttackAfterWildfire(state,action);if(!wildfireAttack)return null;
      return {kind:'ATTACK_FOLLOWUP',followup_phase:'Battle',not_before_round:Number(state.round||1)+1,preferred_card_id:wildfireAttack.card_id,preferred_source_lane:wildfireAttack.source_lane||null,preferred_target_lane:null,preferred_attack_source_lane:wildfireAttack.source_lane||null,preferred_attack_card_id:wildfireAttack.card_id,setup_card_id:action.card_id,setup_target_lane:null,remaining_mana:aiRemainingManaAfterAction(state,action),wildfire:true};
    }
    if(isRingOfGraceCard(c)){
      var heal=aiBestHealAfterRing(state,action); if(!heal) return null;
      return {kind:'HEAL_FOLLOWUP',followup_phase:'Reform',preferred_card_id:heal.card_id,preferred_source_lane:heal.source_lane||null,preferred_target_lane:action.target_lane,heal_all:isHealAllCard(card(heal.card_id)),setup_card_id:action.card_id,setup_target_lane:action.target_lane,remaining_mana:aiRemainingManaAfterAction(state,action)};
    }
    if(isAttackBuffSetupCard(c)){
      var attack=aiBestAttackAfterSetup(state,action); if(!attack) return null;
      return {kind:'ATTACK_FOLLOWUP',followup_phase:'Battle',preferred_card_id:attack.card_id,preferred_source_lane:attack.source_lane||null,preferred_target_lane:attack.target_lane||null,preferred_attack_source_lane:attack.source_lane||null,preferred_attack_card_id:attack.card_id,setup_card_id:action.card_id,setup_target_lane:action.target_lane||null,remaining_mana:aiRemainingManaAfterAction(state,action)};
    }
    return null;
  }
  function aiRankNeed(state,lane){ var h=state.aiHeroes&&state.aiHeroes[lane]; var t=nextRankThreshold(h); return t?Math.max(0,t-Number(h.exp_total||0)):99999; }
  function aiDeployActionScore(state, action){
    var c=card(action.card_id), score=0;
    if(isBeginAnewCard(c)){
      var playable=getLegalActions('AI',state).filter(function(a){ return a.type==='PLAY_CARD' && a.card_id!==action.card_id && !isAIChoiceHeavyCard(card(a.card_id)); });
      return playable.length ? -1000 : 5;
    }
    if(isDoubleCastingCard(c)){
      var doublePlan=aiBestAttackAfterDoubleCasting(state,action); if(!doublePlan) return -1000;
      score+=100+Math.max(0,Math.min(60,estimateAIDamage(state,doublePlan)));
    } else if(isWildfireCard(c)){
      var wildfirePlan=aiBestAttackAfterWildfire(state,action);if(!wildfirePlan)return -1000;
      score+=88+Math.max(0,Math.min(50,estimateAIDamage(state,wildfirePlan)));
    } else if(isRingOfGraceCard(c)){
      var healPlan=aiBestHealAfterRing(state,action); if(!healPlan) return -1000;
      score+=85+Math.max(0,aiRingHealPlanValue(state,action,healPlan));
    } else if(isAttackBuffSetupCard(c)){
      var attackPlan=aiBestAttackAfterSetup(state,action); if(!attackPlan) return -1000;
      score+=70+Math.max(0,Math.min(60,aiAttackPlanValue(state,action,attackPlan)));
    }
    if(isReloadCard(c)) score+=90;
    if(isManaCatalystCard(c)) score+=75;
    if(isCoordinationAttackCard(c)) score+=15;
    if(isForgedAllianceCard(c)) score+=65;
    if(isBuffCard(c) || isHeroAttachmentItem(c)) score+=55;
    if(isHealCard(c)) score+=20;
    if(cardFamily(c)==='Item') score+=35;
    if(cardFamily(c)==='Event') score+=25;
    score-=Number(action.cost||0);
    return score;
  }
  function aiPlannedFollowUpAction(state,phase,playable){
    var plan=state&&state.aiPlan; if(!plan) return null;
    if(Number(plan.not_before_round||0)>Number(state.round||0))return null;
    if(plan.followup_phase && plan.followup_phase!==phase) return null;
    var candidates=playable.filter(function(a){
      if(plan.kind==='ATTACK_FOLLOWUP' && !isAttackCard(card(a.card_id))) return false;
      if(plan.kind==='HEAL_FOLLOWUP' && !isHealCard(card(a.card_id))) return false;
      if(plan.preferred_card_id && a.card_id!==plan.preferred_card_id) return false;
      if(plan.preferred_source_lane && a.source_lane!==plan.preferred_source_lane) return false;
      if(plan.kind==='HEAL_FOLLOWUP' && !plan.heal_all && plan.preferred_target_lane && a.target_lane!==plan.preferred_target_lane) return false;
      if(plan.kind==='ATTACK_FOLLOWUP' && plan.preferred_target_lane && a.target_lane!==plan.preferred_target_lane) return false;
      return true;
    });
    if(!candidates.length) return null;
    if(plan.kind==='ATTACK_FOLLOWUP') candidates.sort(function(a,b){return estimateAIDamage(state,b)-estimateAIDamage(state,a);});
    return candidates[0];
  }
  function chooseAIAction(state, phase){
    var legal=getLegalActions('AI',state);
    var nonSkip=legal.filter(function(a){ return a.type!=='SKIP'; });
    var playable=nonSkip.filter(function(a){ return a.type==='PLAY_CARD' && !isAIChoiceHeavyCard(card(a.card_id)); });
    var futurePlan=state.aiPlan&&Number(state.aiPlan.not_before_round||0)>Number(state.round||0)?state.aiPlan:null;
    if(futurePlan&&futurePlan.preferred_card_id)playable=playable.filter(function(a){return a.card_id!==futurePlan.preferred_card_id;});
    var planned=aiPlannedFollowUpAction(state,phase,playable);
    if(planned){ state.aiPlan=null; return planned; }
    if(state.aiPlan && state.aiPlan.followup_phase===phase && Number(state.aiPlan.not_before_round||0)<=Number(state.round||0)) state.aiPlan=null;
    if(phase==='Reform'){
      var tributes=nonSkip.filter(function(a){ return a.type==='TRIBUTE_CARD'; });
      if(tributes.length) return tributes.sort(function(a,b){ return aiRankNeed(state,a.target_lane)-aiRankNeed(state,b.target_lane) || Number(b.exp||0)-Number(a.exp||0); })[0];
    }
    if(phase==='Battle'){
      var attacks=playable.filter(function(a){ return isAttackCard(card(a.card_id)); });
      if(attacks.length) return attacks.sort(function(a,b){ return estimateAIDamage(state,b)-estimateAIDamage(state,a) || Number(b.cost||0)-Number(a.cost||0); })[0];
    }
    if(phase==='Deploy'){
      var dueAttackPlan=state.aiPlan&&state.aiPlan.kind==='ATTACK_FOLLOWUP'&&Number(state.aiPlan.not_before_round||0)<=Number(state.round||0);
      var setup=playable.filter(function(a){ return !isAttackCard(card(a.card_id))&&!(dueAttackPlan&&isTacticalFollowUpSetupCard(card(a.card_id))); });
      var meaningful=setup.filter(function(a){ return aiDeployActionScore(state,a)>-999; });
      if(meaningful.length){
        var chosen=meaningful.sort(function(a,b){ return aiDeployActionScore(state,b)-aiDeployActionScore(state,a); })[0];
        if(isTacticalFollowUpSetupCard(card(chosen.card_id))){ var plan=aiPlanForSetupAction(state,chosen); if(plan) chosen.ai_setup_plan=plan; }
        return chosen;
      }
      var begin=setup.filter(function(a){ return isBeginAnewCard(card(a.card_id)); });
      if(begin.length) return begin[0];
      var safeFallback=setup.filter(function(a){ return !isTacticalFollowUpSetupCard(card(a.card_id)) || !!aiPlanForSetupAction(state,a); });
      if(safeFallback.length) return safeFallback[0];
    }
    if(phase==='Deploy' || phase==='Reform'){
      var reps=nonSkip.filter(function(a){ return a.type==='REPOSITION'; }).sort(function(a,b){ return Number(b.score||0)-Number(a.score||0); });
      if(reps.length && Number(reps[0].score||0)>0) return reps[0];
    }
    return {type:'SKIP', side:'AI', label:'Skip '+phase, reason:nonSkip.length?'Only choice-heavy or non-executable actions available to AI automation.':'No legal non-skip action is available.'};
  }
  function clearAIPlanAtTurnEnd(state){
    if(!state||!state.aiPlan)return;
    if(Number(state.aiPlan.not_before_round||0)>Number(state.round||0))return;
    state.aiPlan=null;
  }
  function assertActionStillLegal(state, side, action){
    var legal=getLegalActions(side,state).map(actionKey);
    return legal.indexOf(actionKey(action))!==-1;
  }
  function commitAITributeToHero(state, action){
    var ts=legalTributeStateFor(state,'AI',action.card_id);
    if(!ts.can || !assertActionStillLegal(state,'AI',action)){ var blockedTributeName=cardName(card(action.card_id)); pushLog(state,'AI could not complete the Tribute with '+blockedTributeName+' because it was no longer legal.'); recordOpponentAction(state,'BLOCKED',action.card_id,'AI Action Blocked','AI could not complete the Tribute.\nReason: the selected card or Hero was no longer legal.'); return false; }
    var hand=state.aiHand||[]; var idx=action.hand_index; if(hand[idx]!==action.card_id) idx=hand.indexOf(action.card_id);
    var hero=state.aiHeroes[action.target_lane]; if(idx<0 || !hero){ pushLog(state,'AI action was rejected by the runtime validator: tribute target or hand card missing.'); return false; }
    var beforeRank=heroRank(hero), beforeExp=Number(hero.exp_total||0), beforeHeroName=cardName(card(hero.card_id));
    var tributeVisual=captureTributeCardMotion('AI',idx,action.card_id,action.target_lane);
    var id=hand.splice(idx,1)[0], c=card(id), gained=tributeExpValue(c); queueTributeCardMotion(tributeVisual); hero.exp_cards=hero.exp_cards||[]; hero.exp_cards.push(id); var aiTributeExp=applyTributeExpToHero(hero,c); if(!aiTributeExp.legal){ hero.exp_cards.pop(); return false; } state.tributeUsedThisReform=true;
    var afterExp=Number(hero.exp_total||0);
    pushLog(state,'AI tributes '+cardName(c)+' to '+beforeHeroName+' for '+gained+' EXP. Printed Mana cost is ignored.');
    var tributeEvt=recordOpponentAction(state,'TRIBUTE',id,'AI Tribute','AI tributed '+cardName(c)+'.', {action_line:'Action: AI tributed '+cardName(c)+'.', target_side:'AI', target_lane:action.target_lane, metric_before:{side:'AI',lane:action.target_lane,card_id:hero.card_id,name:beforeHeroName,hp:Number(hero.hp||0),exp:beforeExp}, result_lines:[beforeHeroName+' EXP: '+beforeExp+' -> '+afterExp,'AI '+action.target_lane+' gained '+gained+' EXP.','Rank before Tribute: Rank '+beforeRank+'.']});
    var ranked=rankUpHero(state,'AI',action.target_lane);
    appendOpponentEventResult(state,tributeEvt,[ranked?'Rank Up triggered immediately. See RANK UP tile.':'No Rank Up occurred.']);
    syncCounts(state); return true;
  }
  function executeAIAction(state, action){
    if(!action || action.type==='SKIP'){
      var reason=(action&&action.reason)||'No executable legal action was selected.';
      pushLog(state,'AI legally skips '+state.phase+' Phase. Reason: '+reason);
      // Compact Opponent Played intentionally excludes ordinary AI Skip; full history/internal log keeps the skip reason.
      return true;
    }
    if(action.type==='TRIBUTE_CARD') return commitAITributeToHero(state, action);
    if(action.type==='REPOSITION') return performAIReposition(state, action.pair||[]);
    if(!assertActionStillLegal(state,'AI',action)){
      var blockedActionName=action.card_id?cardName(card(action.card_id)):(action.label||'the selected action');
      pushLog(state,'AI could not complete '+blockedActionName+' because it was no longer legal.');
      recordOpponentAction(state,'BLOCKED',action.card_id||null,'AI Action Blocked','AI could not complete '+blockedActionName+'.\nReason: the game state changed before commitment.');
      return false;
    }
    var beforePending=state.pending; var committed=commitPlayedCard(state,action);
    if(!committed){ var failedActionName=action.card_id?cardName(card(action.card_id)):(action.label||'the selected action'); pushLog(state,'AI could not commit '+failedActionName+'. No effect was resolved.'); return false; }
    if(action.ai_setup_plan){
      state.aiPlan=clone(action.ai_setup_plan);
      var followName=cardName(card(action.ai_setup_plan.preferred_card_id||action.ai_setup_plan.preferred_attack_card_id||''));
      var followDetail=action.ai_setup_plan.kind==='HEAL_FOLLOWUP'?'heal AI '+(action.ai_setup_plan.preferred_target_lane||'target'):(followName+' from '+(action.ai_setup_plan.preferred_source_lane||action.ai_setup_plan.preferred_attack_source_lane||'any source'));
      pushLog(state,'AI stores tactical setup plan: '+cardName(card(action.ai_setup_plan.setup_card_id))+' → '+followDetail+'.');
    }
    if(state.responseWindow && state.responseWindow.target_side==='PLAYER'){
      if(SUPPRESS_RENDER){ resolveResponseWindow(null); }
      else { state.aiControl={resume:'after_player_response', from_phase:state.phase, director:GL_AI_TURN_DIRECTOR.active}; state.aiPresentationStatus='Waiting for PLAYER response to '+cardName(card(action.card_id))+'.'; pushLog(state,'AI pauses for PLAYER Response Window. Resolve response to continue AI turn.'); syncCounts(state); render(); }
    }
    return beforePending!==state.pending || committed;
  }
  function beginPlayerTurnDrawPhase(state,logLine){
    if(!state||state.gameOver)return false;
    state.turn='PLAYER';state.phase='Draw';state.drawPhaseResolvedFor=null;state.aiControl=null;state.aiPresentationStatus='PLAYER begins Draw Phase.';
    removeStartOfTurnTargetPreventionForSide(state,'PLAYER');
    pushLog(state,logLine||('AI ends turn. PLAYER begins Draw Phase for Round '+state.round+'.'));
    syncCounts(state);
    if(SUPPRESS_RENDER){resolveDrawPhase(state,'PLAYER',{deferAnimation:true});autoAdvancePlayerDrawWhenReady(state);syncCounts(state);return true;}
    render();
    nextVisualFrame(function(){
      if(appState!==state||state.gameOver||state.turn!=='PLAYER'||state.phase!=='Draw'||state.drawPhaseResolvedFor==='PLAYER')return;
      resolveDrawPhase(state,'PLAYER');
      syncCounts(state);render();if(state.gameOver)showResult();else autoAdvancePlayerDrawWhenReady(state);
    });
    return true;
  }
  function finishAITurnFromBattleImmediate(state){
    expireBattleAttachments(state); removeTemporaryBattleBuffsForSide(state,'AI');
    state.phase='Reform'; state.tributeUsedThisReform=false; pushLog(state,'AI moves to Reform Phase and checks legal Tribute actions.');
    executeAIAction(state, chooseAIAction(state,'Reform')); if(state.pending || state.responseWindow || state.gameOver) return;
    state.phase='End'; cleanupHandLimit(state,'AI'); resolveEndPhaseCleanupForSide(state,'AI');
    clearAIPlanAtTurnEnd(state); advanceRoundAfterCompletedTurnPair(state,'AI'); beginPlayerTurnDrawPhase(state,'AI ends turn. PLAYER begins Draw Phase for Round '+state.round+'.'); if(state.gameOver) showResult();
  }
  function resumeAITurnAfterPlayerResponseImmediate(state){
    if(!state || state.turn!=='AI' || !state.aiControl || state.aiControl.resume!=='after_player_response' || state.pending || state.responseWindow) return false;
    var from=state.aiControl.from_phase||state.phase;
    pushLog(state,'AI resumes after the PLAYER response in '+from+' Phase.');
    if(from==='Deploy'){
      state.phase='Battle'; resolvePendingCastingsFor(state,'AI'); if(state.pending || state.responseWindow || state.gameOver) return true;
      pushLog(state,'AI checks legal Battle actions.');
      executeAIAction(state,chooseAIAction(state,'Battle')); if(state.pending || state.responseWindow || state.gameOver) return true;
      finishAITurnFromBattleImmediate(state); return true;
    }
    if(from==='Reform'){
      state.phase='End'; cleanupHandLimit(state,'AI'); resolveEndPhaseCleanupForSide(state,'AI');
      clearAIPlanAtTurnEnd(state); advanceRoundAfterCompletedTurnPair(state,'AI'); beginPlayerTurnDrawPhase(state,'AI ends turn. PLAYER begins Draw Phase for Round '+state.round+'.'); if(state.gameOver) showResult(); return true;
    }
    finishAITurnFromBattleImmediate(state); return true;
  }

  function targetCoverage(sourceLane){
    if(sourceLane==='LEFT') return ['LEFT','CENTER'];
    if(sourceLane==='RIGHT') return ['CENTER','RIGHT'];
    return ['LEFT','CENTER','RIGHT'];
  }
  function targetSideFor(c){
    // Camouflage is a source-owned self effect; the source choice is the host choice.
    // Do not open a second allied target picker just because v0.11.5 models it as attach_*.
    if(isCamouflageCard(c)) return null;
    if(isAttackCard(c)) return 'AI';
    if(isFinalGritCard(c)) return 'PLAYER';
    if(isHealAllCard(c) || isGodsBlessingCard(c) || isPoisonMistCard(c) || isWildfireCard(c) || isSixthSenseCard(c) || isStealCard(c) || isMagicCompassCard(c) || isMagicScopeCard(c) || isCrystalBallCard(c) || isBeginAnewCard(c) || isLastResortCard(c) || isForgedAllianceCard(c) || isDejaVuCard(c) || isCoordinationAttackCard(c) || isRelentlessLevelingCard(c)) return null;
    if(isHealCard(c) || isRemoveStatusCard(c) || isReviveCard(c) || isBuffCard(c) || isStaminaPotionCard(c) || isHeroAttachmentItem(c)) return 'PLAYER';
    if(isStatusTactical(c)) return 'AI';
    return null;
  }
  function legalTargetLanes(state, cardId, sourceLane){
    var side=(state&&state.turn==='AI')?'AI':'PLAYER';
    return legalTargetLanesFor(state, side, cardId, sourceLane);
  }
  function needsTarget(c, state, sourceLane){ return needsPlayerTargetSelection(c,state||appState,'PLAYER',sourceLane); }
  function ultimateMatchesReceivingHero(c, hero){
    if(c && c.card_id==='S1-CLE-024') return true; // Divine Judgement is not owner-lineage Tribute locked; only true Ultimate cards restrict Tribute lineage.
    if(!isUltimateCard(c)) return true;
    var ultimate=c.requirement.ultimate||{};
    var ownerIds=ultimate.owner_lineage_card_ids||[];
    if(!ownerIds.length) return false;
    return ownerIds.indexOf(heroIdFrom(hero))!==-1;
  }
  function tributeExpResolution(hero,c){
    if(!hero || isLegacyModeHero(hero)) return {legal:false,total:Number(hero&&hero.exp_total||0),applied:0,overflow:0};
    return window.GL_RULES_RUNTIME.resolveTributeExp(Number(hero.exp_total||0),tributeExpValue(c),heroRank(hero),isUltimateCard(c));
  }
  function heroCanReceiveExpCard(hero, c){ return !!tributeExpResolution(hero,c).legal; }
  function applyTributeExpToHero(hero,c){
    var result=tributeExpResolution(hero,c);
    if(!result.legal) return result;
    hero.exp_total=result.total;
    return result;
  }
  function canReceiveTributeFor(state, side, cardId, lane){
    var c=card(cardId), hero=state&&sideHeroes(state,side)&&sideHeroes(state,side)[lane];
    if(!hero || hero.hp<=0 || isLegacyModeHero(hero)) return false;
    if(!heroCanReceiveExpCard(hero,c)) return false;
    if(!ultimateMatchesReceivingHero(c, hero)) return false;
    return true;
  }
  function canReceiveTribute(state, cardId, lane){ return canReceiveTributeFor(state,'PLAYER',cardId,lane); }
  function legalTributeTargetLanesFor(state, side, cardId){
    if(!state) return [];
    return LANE_ORDER.filter(function(lane){ return canReceiveTributeFor(state, side, cardId, lane); });
  }
  function legalTributeTargetLanes(state, cardId){ return legalTributeTargetLanesFor(state,'PLAYER',cardId); }
  function legalTributeStateFor(state, side, cardId){
    var c=card(cardId); var reasons=[];
    if(!matchStarted || !state) reasons.push('match not started');
    if(state && state.gameOver) reasons.push('game over');
    if(state && state.turn!==side) reasons.push('not '+side+' turn');
    if(state && state.pending && !(state.pending.type==='tribute_target' && state.pending.card_id===cardId)) reasons.push('another action pending');
    if(state && state.phase!=='Reform') reasons.push('wrong phase');
    if(!isSkillCard(c)) reasons.push('not a Skill Card');
    if(c.tribute && c.tribute.can_be_tributed===false) reasons.push('card cannot be tributed');
    if(state && state.tributeUsedThisReform) reasons.push('normal Tribute already used this Reform Phase');
    if(state && isSkillCard(c) && !legalTributeTargetLanesFor(state,side,cardId).length) reasons.push('no legal receiving hero');
    return {can:reasons.length===0, reasons:reasons, exp:tributeExpValue(c)};
  }
  function legalTributeState(state, cardId){ return legalTributeStateFor(state,'PLAYER',cardId); }
  function beginTributeFromHand(index){
    if(!appState || appState.turn!=='PLAYER' || appState.pending || appState.gameOver) return;
    var cardId=appState.playerHand[index]; var ts=legalTributeState(appState,cardId);
    if(!ts.can){ showInfo('Cannot Tribute', ts.reasons.join('\n')); return; }
    appState.pending={type:'tribute_target', decision_side:'PLAYER', source_side:'PLAYER', card_id:cardId, hand_index:index, legal_targets:legalTributeTargetLanes(appState,cardId)};
    pushLog(appState,'Choose receiving Hero for Tribute: '+cardName(card(cardId))+' gives '+ts.exp+' EXP.'); render();
  }
  function beginPlayFromHand(index){
    if(!appState || appState.turn!=='PLAYER' || appState.pending) return;
    var cardId=appState.playerHand[index]; var c=card(cardId); var lp=legalPlayState(appState,cardId);
    if(!lp.can){ showInfo('Cannot Play', lp.reasons.join('\n')); return; }
    if(requiresSourceHero(c)){
      appState.pending={type:'source_selection', decision_side:'PLAYER', source_side:'PLAYER', card_id:cardId, hand_index:index, legal_sources:legalSourceLanes(appState,cardId)};
      pushLog(appState,'Choose acting Hero for '+cardName(c)+'.'); render(); return;
    }
    if(needsTarget(c,appState,null)){
      appState.pending={type:'target_selection', decision_side:'PLAYER', source_side:null, source_lane:null, card_id:cardId, hand_index:index, target_side:targetSideForActor(c,'PLAYER'), legal_targets:legalTargetLanesFor(appState,'PLAYER',cardId,null)};
      pushLog(appState,'Choose target for '+cardName(c)+'.'); render(); return;
    }
    commitPlayedCard(appState,{card_id:cardId, hand_index:index, source_side:null, source_lane:null, target_side:targetSideForActor(c,'PLAYER'), target_lane:null});
  }
  function chooseHeroFromBoard(side,lane){
    if(!appState || !appState.pending) return false;
    var p=appState.pending;
    if(p.type==='source_selection'){
      if(side!=='PLAYER' || p.legal_sources.indexOf(lane)===-1){ showInfo('Illegal Source','Choose a highlighted green PLAYER Hero as the acting Hero.'); return false; }
      var c=card(p.card_id);
      if(isDualArrowCard(c) && dualArrowRequiresPairChoice(appState,'PLAYER',lane)){
        openDualArrowExactTwoTargetChoice(appState,{card_id:p.card_id, hand_index:p.hand_index, source_side:'PLAYER', source_lane:lane, target_side:targetSideForActor(c,'PLAYER')}); return true;
      }
      if(isScoutingCard(c)){
        appState.pending={type:'scouting_target_selection', decision_side:'PLAYER', card_id:p.card_id, hand_index:p.hand_index, source_side:'PLAYER', source_lane:lane, target_side:'AI', legal_targets:legalScoutingTargetLanes(appState,'PLAYER')};
        pushLog(appState,'Choose opponent Hero with non-Ultimate EXP for '+cardName(c)+'.'); render(); return true;
      }
      if(needsTarget(c,appState,lane)){
        appState.pending={type:'target_selection', decision_side:'PLAYER', card_id:p.card_id, hand_index:p.hand_index, source_side:'PLAYER', source_lane:lane, source_card_id:heroIdFrom(sideHeroes(appState,'PLAYER')[lane]), target_side:targetSideForActor(c,'PLAYER'), legal_targets:legalTargetLanesFor(appState,'PLAYER',p.card_id,lane)};
        pushLog(appState,'Choose target for '+cardName(c)+'.'); render(); return true;
      }
      commitPlayedCard(appState,{card_id:p.card_id, hand_index:p.hand_index, source_side:'PLAYER', source_lane:lane, source_card_id:heroIdFrom(sideHeroes(appState,'PLAYER')[lane]), target_side:targetSideForActor(c,'PLAYER'), target_lane:null}); return true;
    }
    if(p.type==='exact_two_target_selection'){
      var dualTargetSide=p.target_side||oppositeSide(p.source_side||'PLAYER');
      if(side!==dualTargetSide || (p.legal_targets||[]).indexOf(lane)===-1){ showInfo('Illegal Target','Choose a highlighted opponent Hero or Legacy slot.'); return false; }
      p.selected_target_lanes=p.selected_target_lanes||[];
      var selectedIndex=p.selected_target_lanes.indexOf(lane);
      if(selectedIndex!==-1){
        p.selected_target_lanes.splice(selectedIndex,1);
        pushLog(appState,'Dual Arrow target removed: '+lane+'. Choose 2 distinct opponent field slots.');
        render(); return true;
      }
      if(p.selected_target_lanes.length>=2) return false;
      p.selected_target_lanes.push(lane);
      if(p.selected_target_lanes.length<2){
        pushLog(appState,'Dual Arrow target 1/2 selected: '+lane+'. Choose one more opponent Hero or Legacy slot.');
        render(); return true;
      }
      var selected=p.selected_target_lanes.slice();
      var hasHero=selected.some(function(targetLane){
        var h=sideHeroes(appState,dualTargetSide)[targetLane];
        return h && Number(h.hp||0)>0 && !isLegacyModeHero(h);
      });
      if(!hasHero){
        p.selected_target_lanes=[];
        showInfo('Dual Arrow','At least 1 selected slot must contain an active Hero.');
        render(); return false;
      }
      var dualAction={card_id:p.card_id,hand_index:p.hand_index,source_side:p.source_side,source_lane:p.source_lane,target_side:dualTargetSide,target_lanes:selected,target_lane:null};
      appState.pending=null; closeChoice();
      return commitPlayedCard(appState,dualAction);
    }
    if(p.type==='double_casting_target_selection'){
      if(side!==p.target_side || (p.legal_targets||[]).indexOf(lane)===-1){ showInfo('Illegal Target','Choose a highlighted legal target for the second Double Casting activation.'); return false; }
      return commitDoubleCastingSecondTarget(appState,p,lane);
    }
    if(p.type==='target_selection'){
      var c2=card(p.card_id); var targetSide=p.target_side||targetSideForActor(c2,'PLAYER');
      if(side!==targetSide || p.legal_targets.indexOf(lane)===-1){ showInfo('Illegal Target','Choose a highlighted green target on the board.'); return false; }
      if(isArrowBarrageCard(c2)){
        var spendAll=arrowBarrageSpendMax(appState,'PLAYER');
        commitPlayedCard(appState,{card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, source_card_id:p.source_card_id||null, target_side:side, target_lane:lane, mana_spent:spendAll}); return true;
      }
      if(isElixirCard(c2)){
        var targetHero=sideHeroes(appState,side)[lane], choices=negativeStatusChoices(targetHero);
        if(choices.length>1) return openStatusRemovalChoice(appState,{card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, target_side:side, target_lane:lane, choices:choices});
      }
      commitPlayedCard(appState,{card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, source_card_id:p.source_card_id||null, target_side:side, target_lane:lane}); return true;
    }
    if(p.type==='scouting_target_selection'){
      if(side!==p.target_side || p.legal_targets.indexOf(lane)===-1){ showInfo('Illegal Scouting Target','Choose an opponent Hero with at least 1 non-Ultimate EXP Card.'); return false; }
      return openScoutingExpChoice(appState,{card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, target_side:side, target_lane:lane});
    }
    if(p.type==='tribute_target'){
      if(side!=='PLAYER' || p.legal_targets.indexOf(lane)===-1){ showInfo('Illegal Tribute Target','Choose a highlighted green PLAYER Hero to receive EXP.'); return false; }
      return commitTributeToHero(appState,{card_id:p.card_id, hand_index:p.hand_index, target_lane:lane});
    }
    if(p.type==='racial_target_selection'){
      if(side!==p.target_side || p.legal_targets.indexOf(lane)===-1){ showInfo('Illegal Racial Target','Choose a highlighted valid target.'); return false; }
      return resolveActivatedRacialAbility(appState,p.source_side,p.source_lane,p.abilityId,lane);
    }
    if(p.type==='hero_ability_target_selection'){
      if(side!==p.target_side || p.legal_targets.indexOf(lane)===-1){ showInfo('Illegal Class Ability Target','Choose a highlighted valid allied damaged Hero.'); return false; }
      return resolveActivatedHeroAbility(appState,p.source_side,p.source_lane,p.abilityId,lane);
    }
    if(p.type==='legacy_hero_target_selection'){
      if(side!==p.target_side || p.legal_targets.indexOf(lane)===-1){ showInfo('Illegal Legacy Target','Choose a highlighted valid own damaged Hero.'); return false; }
      return resolveLegacyHeroTargetSelection(appState,p.source_side,p.source_lane,p.abilityId,lane);
    }
    return false;
  }
  function cancelPendingAction(){
    if(!appState || !appState.pending) return;
    if(!isCancelablePreCommitPending(appState.pending)){ showInfo('Cannot Cancel','This action is already committed or is waiting for a mandatory post-resolution choice.'); return; }
    var cid=appState.pending.card_id; appState.pending=null; pushLog(appState,'Cancelled pending action'+(cid?' for '+cardName(card(cid)):'')+'. No cost, Played marker, Exhaust, discard, or attachment staging occurred.'); render();
  }
  function branchClassKey(map, sourceHero){ return branchClassKeyForCard(map, sourceHero, null); }
  function branchClassKeyForCard(map, sourceHero, cardObj){
    return window.GL_RULES_RUNTIME.selectClassRow(map,sourceHero,cardObj,{heroClass:heroClass(sourceHero),heroCard:card(heroIdFrom(sourceHero))});
  }
  function classMappedNumber(map, sourceHero, cardObj){
    return window.GL_RULES_RUNTIME.mappedNumber(map,sourceHero,cardObj,{heroClass:heroClass(sourceHero),heroCard:card(heroIdFrom(sourceHero))});
  }
  function statusName(s){ return String((s&&s.name)||s||''); }
  function hasStatus(hero, name){
    var n=String(name||'').toLowerCase();
    return !!(hero && hero.statuses && hero.statuses.some(function(s){ return statusName(s).toLowerCase()===n; }));
  }
  function hasIceBlockDamageImmunity(hero){
    // Persistent immunity is owned by the Ice Block Attachment, not a duplicate Hero badge.
    return hasActiveAttachmentForHero(appState,hero,'S1-MAG-011') || hasStatus(hero,'Damage Immunity');
  }
  function addStatus(hero, name, duration, text, sourceInfo){
    if(!hero) return false;
    hero.statuses=hero.statuses||[];
    var incoming=Number(duration||1), n=String(name||'');
    var src=sourceInfo || {source_name:(text&&String(text).split(' · ')[0])||n, amount:incoming, note:text||''};
    var existing=hero.statuses.find(function(s){ return statusName(s).toLowerCase()===n.toLowerCase(); });
    if(existing){
      existing.duration=mergeStatusDuration(existing, incoming, n);
      if(/^Taunt$/i.test(n)) existing.owner_turns_until_expiry=2;
      if(/^(Untargetable by Single Target Attacks|Physical Damage -20)$/i.test(n)) existing.owner_turns_until_expiry=Math.max(Number(existing.owner_turns_until_expiry||0),incoming);
      existing.sources=existing.sources||[]; existing.sources.push(src);
      existing.text=text||((statusBadgeLabel(existing))+' · '+Number(existing.duration||0)+' turn(s)')||existing.text||name;
      return true;
    }
    var statusRecord={name:name, duration:incoming, text:text||((duration?name+' · '+incoming+' turn(s)':name)), sources:[src]};
    if(/^Taunt$/i.test(n)) statusRecord.owner_turns_until_expiry=2;
    if(/^(Untargetable by Single Target Attacks|Physical Damage -20)$/i.test(n)) statusRecord.owner_turns_until_expiry=incoming;
    hero.statuses.push(statusRecord);
    return true;
  }
  function mergeStatusDuration(existing, incoming, statusType){
    if(/^(Poison|Burn|Freeze|Stun)$/i.test(String(statusType||''))) return Number(existing&&existing.duration||0)+Number(incoming||0);
    return Math.max(Number(existing&&existing.duration||0), Number(incoming||0));
  }
  function statusBadgeLabel(s){
    var n=statusName(s), d=Number((s&&s.duration)||0);
    if(/^(Poison|Burn|Freeze|Stun|Bleed)$/i.test(n) && d>0) return n+' ('+d+')';
    return n;
  }
  function cleanStackSourceName(src, fallback){
    var raw=cleanDisplayText((src&&src.source_name)||((src&&src.source_card_id)?cardName(card(src.source_card_id)):fallback)||'Source','Source');
    raw=String(raw||'Source').split(' · ')[0].replace(/\s+-\s+final HP damage.*$/i,'').replace(/\s+if .*$/i,'').trim();
    return raw || fallback || 'Source';
  }
  function statusDetailText(st){
    var n=statusName(st), lines=[statusBadgeLabel(st)];
    if(st && Number(st.duration||0)>0) lines.push('Current duration/value: '+Number(st.duration||0));
    if(st && st.text) lines.push(cleanDisplayText(String(st.text||'').split(' · ')[0],''));
    if(st && st.sources && st.sources.length){ lines.push(''); lines.push('Sources:'); st.sources.forEach(function(src){ lines.push('- '+cleanStackSourceName(src,n)); }); }
    return lines.join('\n');
  }
  function expStackDetail(hero){
    var lines=['Total EXP: '+Number(hero&&hero.exp_total||0)];
    var cards=(hero&&hero.exp_cards)||[]; if(cards.length){ lines.push('', 'EXP stack cards:'); cards.forEach(function(id){ lines.push('- '+cardName(card(id))+' · '+tributeExpValue(card(id))+' EXP'); }); } else lines.push('No EXP stack cards.');
    return lines.join('\n');
  }
  var POISON_TICK_DAMAGE=10;
  var BURN_INCOMING_DAMAGE_BONUS=10;
  function burnIncomingDamageBonus(targetHero, attackConnected){
    if(!targetHero || !attackConnected) return 0;
    return hasStatus(targetHero,'Burn') ? BURN_INCOMING_DAMAGE_BONUS : 0;
  }
  function resolveStatusTicksForSide(state, side){
    if(!state) return;
    LANE_ORDER.forEach(function(lane){
      var h=sideHeroes(state,side)[lane]; if(!h || !h.statuses || Number(h.hp||0)<=0) return;
      (h.statuses||[]).forEach(function(st){
        if(statusName(st)==='Poison' && Number(st.duration||0)>0){
          if(activeAttachmentForSide(state,side,'S1-CLE-025')){
            var preventedLine=side+' '+lane+' Poison tick damage is prevented by Blessing of Divinity. Poison duration still decrements normally.';
            pushLog(state, preventedLine);
            if(side==='PLAYER') recordOpponentAction(state,'RESOLVE',null,'Status Tick Prevented','Status tick prevented.',{action_line:'Resolve: Status Tick', target_side:side, target_lane:lane, result_lines:[preventedLine]});
            if(state.pvpHumanVsHuman) recordPvpSideAction(state,side,'TICK',null,'Status Tick Prevented','Status tick prevented.',{action_line:'Resolve: '+side+' '+lane+' Status Tick', target_side:side, target_lane:lane, result_lines:[preventedLine]});
            return;
          }
          if(hasIceBlockDamageImmunity(h)){
            var iceLine=side+' '+lane+' Poison tick damage is prevented by Ice Block damage immunity. Poison duration still decrements normally.';
            pushLog(state, iceLine);
            if(side==='PLAYER') recordOpponentAction(state,'RESOLVE',null,'Status Tick Prevented','Status tick prevented.',{action_line:'Resolve: Status Tick', target_side:side, target_lane:lane, result_lines:[iceLine]});
            if(state.pvpHumanVsHuman) recordPvpSideAction(state,side,'TICK',null,'Status Tick Prevented','Status tick prevented.',{action_line:'Resolve: '+side+' '+lane+' Status Tick', target_side:side, target_lane:lane, result_lines:[iceLine]});
            return;
          }
          var before=Number(h.hp||0); h.hp=Math.max(0,before-POISON_TICK_DAMAGE);
          var tickLine=side+' '+lane+' suffers '+POISON_TICK_DAMAGE+' Poison damage at owner End Phase before duration decrement.';
          pushLog(state, tickLine);
          if(side==='PLAYER') recordOpponentAction(state,'RESOLVE',null,'Status Tick','Status tick damage resolved.',{action_line:'Resolve: Status Tick', target_side:side, target_lane:lane, result_lines:[tickLine]});
          if(state.pvpHumanVsHuman) recordPvpSideAction(state,side,'TICK',null,'Poison Tick','Poison tick damage resolved.',{action_line:'Resolve: '+side+' '+lane+' Poison Tick', target_side:side, target_lane:lane, result_lines:[tickLine]});
          applyDefeatCheck(state,side,lane);
        }
      });
    });
  }
  function clearExpiredAttachmentMarker(state, side, lane, cardId){
    var active=(state.activeAttachments||[]).filter(function(a){return a.side===side&&a.lane===lane&&a.card_id===cardId;});
    var removed=false; active.forEach(function(a){ if(releaseLocalAttachment(state,a,'registered status expiry')){removed=true;pushLog(state,cardName(card(cardId))+' expires from Attachment Slot and moves to Discard Pile.');} });
    var hero=sideHeroes(state,side)[lane]; if(!removed&&hero&&hero.attachments){ hero.attachments.forEach(function(v,idx){if(!removed&&attachmentCardId(v)===cardId) removed=releaseLocalAttachmentAt(state,side,lane,idx,cardId,'registered status expiry recovery');}); }
    return removed;
  }
  function tickStatusDurationsForSide(state, side){
    if(!state) return;
    LANE_ORDER.forEach(function(lane){
      var h=sideHeroes(state,side)[lane]; if(!h || !h.statuses) return;
      h.statuses=h.statuses.filter(function(st){
        if(typeof st.duration==='undefined' || st.duration===null) return true;
        st.duration=Number(st.duration||0)-1;
        return st.duration>0;
      });
    });
  }
  function tickAttachmentsForSideAtCheckpoint(state,side,checkpoint){
    if(!state) return 0;
    var ticked=0, active=(state.activeAttachments||[]).slice();
    active.forEach(function(att){
      if(!att || att.side!==side || att.tick_phase!==checkpoint) return;
      var outcome=window.GL_RUNTIME_AUTHORITY.tickAttachment(state,att,checkpoint);
      if(!outcome.ticked) return;
      ticked++;
      if(outcome.expired){
        if(releaseLocalAttachment(state,att,checkpoint+' expiry')) pushLog(state,cardName(card(att.card_id))+' expires at '+checkpoint+' and moves to Discard Pile.');
      } else pushLog(state,cardName(card(att.card_id))+' Attachment counter becomes ('+Number(att.remaining_count||0)+').');
    });
    return ticked;
  }
  function removeStartOfTurnTargetPreventionForSide(state, side){
    return tickAttachmentsForSideAtCheckpoint(state,side,'DRAW_PHASE_START');
  }
  function pendingCastingForHero(state, side, lane, hero){
    return (state&&state.pendingCastings||[]).find(function(pc){
      if(!pc || pc.side!==side) return false;
      return (pc.original_source_lane||pc.source_lane)===lane && castingSourceMatchesHero(pc,hero);
    })||null;
  }
  function clearExhaustForSide(state, side){
    if(!state) return;
    LANE_ORDER.forEach(function(lane){ var h=sideHeroes(state,side)[lane]; if(h){ var casting=pendingCastingForHero(state,side,lane,h); if(casting){ h.exhausted=true; h.exhaust_reason='Casting in progress.'; } else { h.exhausted=false; h.exhaust_reason=null; } } });
    pushLog(state, side+' Draw Phase cleanup clears Exhaust after Draw Phase.');
  }
  function expireEndPhaseAttachmentsForSide(state, side){
    return tickAttachmentsForSideAtCheckpoint(state,side,'END_PHASE');
  }
  function resolveEndPhaseCleanupForSide(state, side){
    if(!state)return;
    var cleanupKey=side+':'+Number(state.round||0)+':'+String(state.turn||'');
    var progress=state.endPhaseCleanupProgress;
    if(!progress||progress.key!==cleanupKey)progress=state.endPhaseCleanupProgress={key:cleanupKey,status_ticks_done:false};
    if(!progress.status_ticks_done){resolveStatusTicksForSide(state, side);progress.status_ticks_done=true;}
    if(state.pending){ syncCounts(state); return; }
    if(state.gameOver){ delete state.endPhaseCleanupProgress;syncCounts(state); return; }
    tickStatusDurationsForSide(state, side);
    expireEndPhaseAttachmentsForSide(state, side);
    checkGameEnd(state);
    delete state.endPhaseCleanupProgress;
    syncCounts(state);
  }
  function unbrokenStandStatusImmunityEligible(hero){var cls=String(heroClass(hero)||'').toLowerCase();return cls==='paladin'||cls==='crusader';}
  function statusImmune(hero){ return (unbrokenStandStatusImmunityEligible(hero)&&hasActiveAttachmentForHero(appState,hero,'S1-WAR-022')) || hasStatus(hero,'Status Immune') || hasStatus(hero,'Negative Status Immune'); }
  function durationForEffect(e, sourceHero){
    if(!e) return 1;
    if(e.duration_by_class) return classMappedNumber(e.duration_by_class, sourceHero)||1;
    if(e.duration_turns_by_class) return classMappedNumber(e.duration_turns_by_class, sourceHero)||1;
    if(typeof e.duration_turns!=='undefined') return Number(e.duration_turns||1);
    if(typeof e.duration!=='undefined') return Number(e.duration||1);
    return 1;
  }
  function applyStatusEffectToHero(e, targetHero, sourceHero, label, sourceCardObj){
    if(!e || !targetHero || e.kind!=='inflict_status') return null;
    if(statusImmune(targetHero)) return 'Prevented '+e.status+' by status immunity';
    var dur=durationForEffect(e, sourceHero);
    if(String(e.status||'').toLowerCase()==='poison' && sourceHero && /^(Rogue|Renegade)$/i.test(heroClass(sourceHero))) dur+=1;
    if(/^(Burn|Freeze)$/i.test(String(e.status||'')) && sourceHero && /^(Elementalist|Elemental Lord)$/i.test(heroClass(sourceHero))){
      var srcLoc=heroRuntimeLocation(appState,sourceHero), tgtLoc=heroRuntimeLocation(appState,targetHero);
      if(srcLoc&&tgtLoc&&srcLoc.side!==tgtLoc.side) dur+=1;
    }
    var sourceCardId=sourceCardObj&&sourceCardObj.card_id?sourceCardObj.card_id:null;
    var sourceCardName=sourceCardId?cardName(sourceCardObj):cleanDisplayText(label||e.status,e.status);
    addStatus(targetHero, e.status, dur, e.status+' · '+dur+' turn(s)'+(label?' · '+label:''), {source_card_id:sourceCardId, source_name:sourceCardName, amount:dur, note:e.status+' '+dur});
    return e.status+' '+dur;
  }
  function classBranchAllowsStatus(cardObj, status, sourceHero){
    if(!cardObj || !cardObj.effect_strings_by_class || !sourceHero || !status) return true;
    var branch=branchEffectText(cardObj, sourceHero);
    if(!branch) return true;
    // Card Definition Lock can carry all normalized effects at top level even when a class branch omits a status.
    // Runtime must follow the active class branch. Example: Shield Bash Warrior deals damage only; Paladin/Crusader adds Stun.
    return new RegExp('\\b'+String(status).replace(/[.*+?^${}()|[\]\\]/g,'\\$&')+'\\b','i').test(branch);
  }
  function applyAttackStatuses(cardObj, targetHero, sourceHero){
    if(!cardObj || !Array.isArray(cardObj.effect) || !targetHero) return [];
    var applied=[];
    cardObj.effect.forEach(function(e){
      if(e.kind==='inflict_status' && classBranchAllowsStatus(cardObj,e.status,sourceHero)){
        var r=applyStatusEffectToHero(e,targetHero,sourceHero,'attack-connected',cardObj); if(r) applied.push(r);
      }
    });
    return applied;
  }
  function applyNonAttackStatuses(cardObj, targetHero, sourceHero){
    if(!cardObj || !Array.isArray(cardObj.effect) || !targetHero) return [];
    var applied=[];
    cardObj.effect.forEach(function(e){
      if(e.kind==='inflict_status' && classBranchAllowsStatus(cardObj,e.status,sourceHero)){ var r=applyStatusEffectToHero(e,targetHero,sourceHero,'direct effect',cardObj); if(r) applied.push(r); }
    });
    return applied;
  }

  function isRemoveStatusCard(c){ return !!(c && Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='remove_status' || e.kind==='remove_negative_status'; })); }
  function isReviveCard(c){ return !!(c && Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='revive' || e.kind==='revive_hero' || (e.kind==='source_structured_effect_json' && e.payload && e.payload.revive_hero); })); }
  function reviveProfileFromCard(c, sourceHero){
    if(isPhoenixFeatherCard(c)) return {hp:10,exhausted:true,source_class:heroClass(sourceHero)||''};
    if(c && c.card_id==='S1-CLE-015'){
      var cls=heroClass(sourceHero), forced=(cls!=='Saint');
      return {hp:40,exhausted:forced,source_class:cls||'',reason:forced?'Priest Resurrection row':'Saint Resurrection row'};
    }
    var hp=30,exhausted=true;
    if(c && Array.isArray(c.effect)){
      for(var i=0;i<c.effect.length;i++){
        var e=c.effect[i]||{};
        if((e.kind==='revive'||e.kind==='revive_hero')){
          if(e.set_hp_by_class) hp=classMappedNumber(e.set_hp_by_class,sourceHero,c)||hp;
          else if(typeof e.set_hp!=='undefined'||typeof e.amount!=='undefined'||typeof e.raw_value!=='undefined') hp=Number(e.set_hp||e.amount||e.raw_value||hp);
          if(e.revived_exhausted_by_class){var k=branchClassKeyForCard(e.revived_exhausted_by_class,sourceHero,c);if(typeof e.revived_exhausted_by_class[k]!=='undefined')exhausted=!!e.revived_exhausted_by_class[k];}
        }
        if(e.kind==='source_structured_effect_json'&&e.payload){
          if(e.payload.set_hp_by_class) hp=classMappedNumber(e.payload.set_hp_by_class,sourceHero,c)||hp;
          else if(typeof e.payload.set_hp!=='undefined'||typeof e.payload.revive_hp!=='undefined') hp=Number(e.payload.set_hp||e.payload.revive_hp||hp);
          var ex=e.payload.revived_hero_exhausted_by_class||e.payload.revived_exhausted_by_class;
          if(ex){var ek=branchClassKeyForCard(ex,sourceHero,c);if(typeof ex[ek]!=='undefined')exhausted=!!ex[ek];}
        }
      }
    }
    return {hp:Math.max(1,Number(hp||30)),exhausted:exhausted,source_class:heroClass(sourceHero)||''};
  }
  function reviveHpFromCard(c, sourceHero){ return reviveProfileFromCard(c,sourceHero).hp; }
  function isBuffCard(c){ return !!(c && Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='buff' || e.kind==='buff_value' || e.kind==='damage_prevention_active_effect' || /^attach_/.test(e.kind||''); })); }
  function isHealAllCard(c){ return !!(c && (c.card_id==='S1-CLE-016' || Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='heal_allied_heroes' || e.kind==='heal_all_allied_heroes'; }))); }
  function isGodsBlessingCard(c){ return c && c.card_id==='S1-CLE-013'; }
  function isHeavensFuryCard(c){ return c && c.card_id==='S1-CLE-018'; }
  function isBlessingMightCard(c){ return c && c.card_id==='S1-CLE-006'; }
  function isBlessingWisdomCard(c){ return c && c.card_id==='S1-CLE-007'; }
  function isHolyRingCard(c){ return c && c.card_id==='S1-CLE-009'; }
  function isShieldBashCard(c){ return c && c.card_id==='S1-WAR-020'; }
  function isHolySlashCard(c){ return !!(c && (c.card_id==='S1-CLE-020' || cardName(c)==='Holy Slash')); }
  function isBrilliantRadianceCard(c){ return c && c.card_id==='S1-CLE-003'; }
  function isHolyBarrierCard(c){ return !!(c && (c.card_id==='S1-CLE-011' || cardName(c)==='Holy Barrier')); }
  function isSacredBulwarkCard(c){ return !!(c && (c.card_id==='S1-CLE-022' || cardName(c)==='Sacred Bulwark')); }
  function isBlessingDivinityCard(c){ return !!(c && (c.card_id==='S1-CLE-025' || cardName(c)==='Blessing of Divinity')); }
  function isAlliedSourceDefenseCard(c){ return isHolyBarrierCard(c) || isSacredBulwarkCard(c) || isBlessingDivinityCard(c); }
  function isThiefSkillCard(c){ return !!(c && /^S1-THF-/.test(c.card_id||'')); }
  function isStealCard(c){ return c && c.card_id==='S1-THF-005'; }
  function isSixthSenseCard(c){ return c && c.card_id==='S1-THF-006'; }
  function isPoisonMistCard(c){ return c && c.card_id==='S1-THF-012'; }
  function isWildfireCard(c){ return c && c.card_id==='S1-MAG-013'; }
  function isSmokeScreenCard(c){ return c && c.card_id==='S1-THF-004'; }
  function isThiefDodgeResponseCard(c){ return !!(c && (c.card_id==='S1-THF-003' || c.card_id==='S1-THF-011' || (Array.isArray(c.effect)&&c.effect.some(function(e){ return e.kind==='source_structured_effect_json' && e.payload && /Dodge/i.test(String(e.payload.defense||'')); })))); }
  function isThiefOptionalSwapAttack(c){ return c && (c.card_id==='S1-THF-002' || c.card_id==='S1-THF-019' || c.card_id==='S1-THF-023' || c.card_id==='S1-THF-025'); }
  function isXCrossSlashCard(c){ return c && c.card_id==='S1-THF-009'; }
  function isVenomSovereignCard(c){ return c && c.card_id==='S1-THF-015'; }
  function isVenomBindingCard(c){ return c && c.card_id==='S1-THF-017'; }
  function isVenomDetonationCard(c){ return c && c.card_id==='S1-THF-018'; }
  function isArcherSkillCard(c){ return !!(c && /^S1-ARC-/.test(c.card_id||'') && cardFamily(c)==='Skill'); }
  function isEyesHawkCard(c){ return c && c.card_id==='S1-ARC-004'; }
  function isReloadCard(c){ return c && c.card_id==='S1-ARC-005'; }
  function isArrowBarrageCard(c){ return c && c.card_id==='S1-ARC-010'; }
  function isTripleShotCard(c){ return c && c.card_id==='S1-ARC-013'; }
  function isReplenishCard(c){ return c && c.card_id==='S1-ARC-014'; }
  function isSoulBlastShotCard(c){ return c && c.card_id==='S1-ARC-016'; }
  function isDualArrowCard(c){ return c && c.card_id==='S1-ARC-017'; }
  function isCamouflageCard(c){ return c && c.card_id==='S1-THF-027'; }
  function isCalculatedPlanCard(c){ return c && c.card_id==='S1-THF-028'; }
  function isChargedShotCard(c){ return c && c.card_id==='S1-ARC-018'; }
  function isBeginAnewCard(c){ return c && c.card_id==='S1-EVT-001'; }
  function isScoutingCard(c){ return c && c.card_id==='S1-EVT-003'; }
  function hasLegalScoutingExpTarget(state, side){
    var opp=oppositeSide(side); var ok=false;
    LANE_ORDER.forEach(function(l){ var h=state&&sideHeroes(state,opp)[l]; if(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && (h.exp_cards||[]).some(function(id){ return !isUltimateCard(card(id)); })) ok=true; });
    return ok;
  }
  function legalScoutingTargetLanes(state, side){
    var opp=oppositeSide(side), out=[];
    LANE_ORDER.forEach(function(l){
      var h=state&&sideHeroes(state,opp)[l];
      if(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && (h.exp_cards||[]).some(function(id){ return !isUltimateCard(card(id)); })) out.push(l);
    });
    return out;
  }
  function legalScoutingExpChoices(state, side, targetLane){
    var opp=oppositeSide(side), h=state&&sideHeroes(state,opp)[targetLane];
    if(!h || !Array.isArray(h.exp_cards)) return [];
    return h.exp_cards.map(function(id,idx){ return {card_id:id, exp_index:idx, exp_value:tributeExpValue(card(id)), ultimate:isUltimateCard(card(id))}; }).filter(function(x){ return !x.ultimate; });
  }
  function isLastResortCard(c){ return c && c.card_id==='S1-EVT-005'; }
  function isDazedCard(c){ return c && c.card_id==='S1-EVT-006'; }
  function isForgedAllianceCard(c){ return c && c.card_id==='S1-EVT-008'; }
  function isDejaVuCard(c){ return c && c.card_id==='S1-EVT-010'; }
  function isCoordinationAttackCard(c){ return c && c.card_id==='S1-EVT-011'; }
  function isRelentlessLevelingCard(c){ return c && c.card_id==='S1-EVT-004'; }
  function isDefensiveFormationCard(c){ return c && c.card_id==='S1-EVT-012'; }
  function isTacticalAdaptationCard(c){ return c && c.card_id==='S1-EVT-009'; }
  function isInterceptCard(c){ return c && c.card_id==='S1-EVT-007'; }
  function isHealthPotionCard(c){ return c && c.card_id==='S1-ITM-001'; }
  function isGreaterHealthPotionCard(c){ return c && c.card_id==='S1-ITM-002'; }
  function isStaminaPotionCard(c){ return c && c.card_id==='S1-ITM-003'; }
  function isElixirCard(c){ return c && c.card_id==='S1-ITM-004'; }
  function isMagicScopeCard(c){ return c && c.card_id==='S1-ITM-006'; }
  function isPhoenixFeatherCard(c){ return c && c.card_id==='S1-ITM-008'; }
  function isCrystalBallCard(c){ return c && c.card_id==='S1-ITM-009'; }
  function isMagicCompassCard(c){ return c && c.card_id==='S1-ITM-018'; }
  function isManaCatalystCard(c){ return c && c.card_id==='S1-ITM-005'; }
  function isRingProtectionCard(c){ return c && c.card_id==='S1-ITM-007'; }
  function isChainMailCard(c){ return c && c.card_id==='S1-ITM-016'; }
  function isPoisonVialCard(c){ return c && c.card_id==='S1-ITM-011'; }
  function isPoisonVialEligibleAttack(c){ return !!(c && isAttackCard(c) && !isPoisonMistCard(c)); }
  function isSpectralGrapplingHookCard(c){ return c && c.card_id==='S1-ITM-012'; }
  function spectralGrapplingHookResponderValid(hero){ return heroMatchesLineageGate(hero,itemLineageGate(card('S1-ITM-012'))); }
  function chainMailResponderValid(hero){ return heroMatchesLineageGate(hero,itemLineageGate(card('S1-ITM-016'))); }
  function isFlashpowderBombCard(c){ return c && c.card_id==='S1-ITM-017'; }
  function isHeroAttachmentItem(c){ return !!(c && ['S1-ITM-010','S1-ITM-011','S1-ITM-013','S1-ITM-014','S1-ITM-015'].indexOf(c.card_id)!==-1); }
  function isPersistentItemAttachment(c){ return isHeroAttachmentItem(c); }
  function requiredDiscardSpecificIds(c){ var cost=c&&c.cost||{}, cj=cost.cost_json||{}, add=cost.additional_discard_from_hand||{}; if(Array.isArray(cj.discard_specific_from_hand)) return cj.discard_specific_from_hand.slice(); if(Array.isArray(add.exact_card_ids)) return add.exact_card_ids.slice(); var canonical=c&&c.canonical_cost&&Array.isArray(c.canonical_cost.additional_costs)?c.canonical_cost.additional_costs.find(function(x){return x&&x.kind==='discard_from_hand'&&Array.isArray(x.exact_card_ids);}):null; return canonical?canonical.exact_card_ids.slice():[]; }
  function handHasRequiredDiscards(hand,c){ var need=requiredDiscardSpecificIds(c); return need.every(function(id){ return (hand||[]).indexOf(id)!==-1; }); }
  function removeRequiredDiscardsFromHand(state,side,c){ var hand=sideHand(state,side), removed=[]; requiredDiscardSpecificIds(c).forEach(function(id){ var idx=hand.indexOf(id); if(idx>=0){ var motion=captureHandDiscardMotion(side,idx,id); hand.splice(idx,1); sideDiscard(state,side).push(id); queueHandDiscardMotion(motion); removed.push(id); } }); if(removed.length) pushLog(state, cardName(c)+' discards required cards: '+removed.map(function(id){return cardName(card(id));}).join(' + ')+'.'); return removed; }
  function poisonDurationOn(hero){ var total=0; (hero&&hero.statuses||[]).forEach(function(st){ if(statusName(st)==='Poison') total=Math.max(total, Number(st.duration||1)); }); return total; }
  function removePoison(hero){ return removeStatus(hero,'Poison'); }
  function healBlockedByBleed(hero){ return hasStatus(hero,'Bleed'); }
  function healTargetValid(cardObj, hero){
    if(!hero) return false;
    if(isReviveCard(cardObj)) return Number(hero.hp||0)<=0 || isLegacyModeHero(hero);
    if(Number(hero.hp||0)<=0) return false;
    if(!isHealCard(cardObj)) return true;
    if(healBlockedByBleed(hero)) return false;
    if(isGreaterHealthPotionCard(cardObj) && hero.exhausted) return false;
    return Number(hero.hp||0)<Number(hero.maxHp||100);
  }
  function firstHealAmountForSource(c, sourceHero){
    var e=(Array.isArray(c.effect)&&c.effect.find(function(x){return x.kind==='heal' || x.kind==='heal_target_hero' || x.kind==='heal_allied_heroes' || x.kind==='heal_all_allied_heroes';}));
    if(!e) return 0;
    if(e.amount_by_class) return classMappedNumber(e.amount_by_class, sourceHero);
    return Number(e.amount||0);
  }
  function healHeroIfLegal(state, targetHero, amount, sourceCard, sourceHero, reason){
    if(!targetHero || Number(targetHero.hp||0)<=0) return {healed:0, skipped:'not active'};
    if(healBlockedByBleed(targetHero)) return {healed:0, skipped:'Bleed prevents normal heal'};
    if(Number(targetHero.hp||0)>=Number(targetHero.maxHp||100)) return {healed:0, skipped:'full HP'};
    var bonus=attachmentModifierAmount(state,targetHero,'S1-ITM-013',20);
    if(sourceHero && (heroClass(sourceHero)==='Priest' || heroClass(sourceHero)==='Saint')) bonus+=20;
    var before=Number(targetHero.hp||0); targetHero.hp=Math.min(Number(targetHero.maxHp||100), before+Number(amount||0)+bonus);
    var diff=targetHero.hp-before;
    if(diff>0){
      pushLog(state, cardName(sourceCard)+' heals '+cardName(card(targetHero.card_id))+' for '+diff+(bonus?' including +'+bonus+' bonus':'')+(reason?' ('+reason+')':'')+'.');
      var healSide=null,healLane=null;
      ['PLAYER','AI'].some(function(side){return LANE_ORDER.some(function(lane){if(sideHeroes(state,side)[lane]===targetHero){healSide=side;healLane=lane;return true;}return false;});});
      if(healSide&&healLane)queueHealFeedback(healSide,healLane);
      if(sourceHero && heroClass(sourceHero)==='Saint') queueSaintPurifyChoice(state,targetHero,sourceCard);
    }
    return {healed:diff};
  }
  function healAllAlliedHeroes(state, side, amount, sourceCard, sourceHero, reason){
    var total=0; LANE_ORDER.forEach(function(lane){ var r=healHeroIfLegal(state, sideHeroes(state,side)[lane], amount, sourceCard, sourceHero, reason); total+=Number(r.healed||0); });
    if(total===0) pushLog(state, cardName(sourceCard)+' resolves but no allied Hero receives healing.');
    return total;
  }
  function removeOneStatusFromHero(state, hero, sourceCard){
    if(!hero || !hero.statuses || !hero.statuses.length){ pushLog(state, cardName(sourceCard)+' resolves but target has no status to remove.'); return false; }
    var removed=hero.statuses.shift(); pushLog(state, cardName(sourceCard)+' removes '+statusName(removed)+' from '+cardName(card(hero.card_id))+'.'); return true;
  }
  function isNegativeStatusName(name){ return /Poison|Burn|Freeze|Bleed|Decay|Stun|Dazed/i.test(String(name||'')); }
  function removeOneNegativeStatusFromHero(state, hero, sourceCard){
    if(!hero || !hero.statuses || !hero.statuses.length){ return false; }
    var idx=hero.statuses.findIndex(function(st){ return isNegativeStatusName(statusName(st)); });
    if(idx<0) return false;
    var removed=hero.statuses.splice(idx,1)[0]; pushLog(state, cardName(sourceCard)+' removes negative status '+statusName(removed)+' from '+cardName(card(hero.card_id))+'.'); return true;
  }
  function negativeStatusChoices(hero){
    var out=[]; if(!hero || !hero.statuses) return out;
    hero.statuses.forEach(function(st,idx){ if(isNegativeStatusName(statusName(st))) out.push({status:st, status_index:idx, label:statusBadgeLabel(st), detail:statusDetailText(st)}); });
    return out;
  }
  function removeChosenNegativeStatusFromHero(state, hero, sourceCard, statusIndex){
    if(!hero || !hero.statuses || !hero.statuses.length) return false;
    var idx=Number(statusIndex);
    if(!(idx>=0 && idx<hero.statuses.length && isNegativeStatusName(statusName(hero.statuses[idx])))) return removeOneNegativeStatusFromHero(state,hero,sourceCard);
    var removed=hero.statuses.splice(idx,1)[0];
    pushLog(state, cardName(sourceCard)+' removes chosen negative status '+statusName(removed)+' from '+cardName(card(hero.card_id))+'.');
    return true;
  }
  function addClericBuff(state, targetHero, c, sourceHero, action){
    if(!targetHero) return false;
    action=action||{};
    if(isBlessingMightCard(c)){ action.attachment_effect={modifier_amount:20,damage_type:'Physical',uses_actual_damage_type:true,non_stackable_buff:true}; pushLog(state, cardName(c)+' Attachment gives '+cardName(card(targetHero.card_id))+' +20 Physical Attack damage until End Phase.'); return true; }
    if(isBlessingWisdomCard(c)){ action.attachment_effect={modifier_amount:20,damage_type:'Magical',uses_actual_damage_type:true,non_stackable_buff:true}; pushLog(state, cardName(c)+' Attachment gives '+cardName(card(targetHero.card_id))+' +20 Magical Attack damage until End Phase.'); return true; }
    if(isHeavensFuryCard(c)){
      var next=heroClass(sourceHero)==='Priest'; action.attachment_effect={double_base_attack_damage:true,active_from_round:Number(state.round||1)+(next?1:0),active_timing:next?'next_turn':'this_turn'};
      pushLog(state, cardName(c)+' Attachment doubles '+cardName(card(targetHero.card_id))+' base Attack damage '+(next?'next turn':'this turn')+'.'); return true;
    }
    return false;
  }
  function consumeNamedStatus(hero,name){ return removeStatus(hero,name); }
  function requiresFrozenTarget(c){
    return !!(c && (c.card_id==='S1-MAG-015' || (c.effect_strings_by_class && Object.keys(c.effect_strings_by_class).some(function(k){ return /only be used on a target with Freeze status/i.test(c.effect_strings_by_class[k]); }))));
  }
  function dodgeResidualDamage(c, sourceHero){
    if(!c) return 0;
    var hc=heroClass(sourceHero);
    if(c.card_id==='S1-MAG-007' && (hc==='Elementalist' || hc==='Elemental Lord')) return 40;
    if(!Array.isArray(c.effect)) return 0;
    for(var i=0;i<c.effect.length;i++){ var e=c.effect[i]; var text=String(e.formula||e.raw_amount||'');
      var m=text.match(/Dodged residual\s+(\d+)/i);
      if(m && (hc==='Elementalist' || hc==='Elemental Lord')) return Number(m[1]);
    }
    return 0;
  }
  function hasEnrageBonus(hero){ return hasActiveAttachmentForHero(appState,hero,'S1-WAR-013'); }
  function consumeEnrageBonus(hero){ return releaseActiveAttachmentForHero(appState,hero,'S1-WAR-013','Enrage consumed'); }
  function attackCannotBeDodged(cardObj){
    if(cardObj && cardObj.card_id==='S1-THF-010') return true;
    return !!(cardObj && Array.isArray(cardObj.effect) && cardObj.effect.some(function(e){ return (e.kind==='attack_restriction' && /cannot be dodged/i.test(String(e.raw_value||e.text||''))) || (e.kind==='source_structured_effect_json' && e.payload && e.payload.cannot_be_dodged); }));
  }
  function attackCannotBeBlocked(cardObj){
    if(!cardObj) return false;
    var tags=Array.isArray(cardObj.runtime_tags)?cardObj.runtime_tags.join(';'):String(cardObj.runtime_tags||'');
    if(/(?:^|[;,_\s])UNBLOCKABLE(?:$|[;,_\s])/i.test(tags)) return true;
    return !!(Array.isArray(cardObj.effect) && cardObj.effect.some(function(e){
      return (e.kind==='attack_restriction' && (e.restriction==='cannot_be_blocked' || /cannot be blocked/i.test(String(e.raw_value||e.text||'')))) ||
        (e.kind==='source_structured_effect_json' && e.payload && e.payload.cannot_be_blocked);
    }));
  }
  function conditionalAttackBonus(cardObj, targetHero, sourceHero){
    var bonus=0;
    if(cardObj && Array.isArray(cardObj.effect)){
      cardObj.effect.forEach(function(e){
        if(e.kind==='attack_connected_conditional_damage' && /Bleed/i.test(String(e.condition||e.raw_condition||e.raw_value||JSON.stringify(e)))){ if(hasStatus(targetHero,'Bleed')) bonus+=Number(e.amount||e.bonus_damage||20); }
        if(e.kind==='source_structured_effect_json' && e.payload && e.payload.on_damage_lands_if_target_has_status){ var cond=e.payload.on_damage_lands_if_target_has_status; if(hasStatus(targetHero,cond.status)){ var by=cond.bonus_damage_by_class||{}; bonus+=classMappedNumber(by, sourceHero); } }
      });
    }
    return bonus;
  }
  function passiveAttackDamageBonus(c, sourceHero){
    if(!c || !sourceHero || cardFamily(c)!=='Skill' || !isAttackCard(c)) return 0;
    var heroCard=card(heroIdFrom(sourceHero));
    var action=heroCard&&heroCard.class_ability&&heroCard.class_ability.action||{};
    var effect=action.effect||{}, dm=action.damage_modifier||{}, amount=Number(dm.amount||effect.attack_damage_by_this_hero||effect.single_target_attack_damage_by_this_hero||0);
    if(!amount) return 0;
    var key=String(action.action_key||''), actualType=damageTypeForActionSource(c,sourceHero);
    if(key==='dead_eye'){
      if(actualType!=='Physical' || !isSingleTargetAttack(c)) return 0;
      if((action.exclusions||[]).indexOf(c.card_id)!==-1) return 0;
      return amount;
    }
    if(dm.damage_type) return actualType===dm.damage_type ? amount : 0;
    return 0;
  }
  function activeTimedAttachmentForHero(state,hero,cardId){
    var att=activeAttachmentForHero(state,hero,cardId); if(!att) return null;
    if(Number(att.active_from_round||0)>Number(state&&state.round||0)) return null;
    return att;
  }
  function hasNonStackableAttackBuff(state,hero){
    return ['S1-CLE-006','S1-CLE-007','S1-CLE-018','S1-EVT-005'].some(function(id){return !!activeAttachmentForHero(state,hero,id);});
  }
  function responseCostForSource(cardObj,sourceHero,state,side){
    return Number(cardPlayCostForSource(cardObj,sourceHero,state,side)||0);
  }
  function isBindingLightCard(c){ return !!(c&&c.card_id==='S1-CLE-012'); }
  function isDefendSkillCard(c){ return !!(c && cardFamily(c)==='Skill' && /(?:Defense|Defend) Skill/i.test(String(c.classification||c.action_category||c.card_subtype||''))); }

  function currentClassDamage(c, sourceHero, targetHero){
    var printed=0;
    if(c.attack){ if(c.attack.damage_by_class){ printed=classMappedNumber(c.attack.damage_by_class, sourceHero, c); } else if(typeof c.attack.damage!=='undefined') printed=Number(c.attack.damage||0); }
    if(!printed && Array.isArray(c.effect)){
      for(var i=0;i<c.effect.length;i++){ var e=c.effect[i]; if(e.kind==='deal_attack_damage'){ if(e.amount_by_class) printed=classMappedNumber(e.amount_by_class, sourceHero, c); else printed=Number(e.amount||0); break; } }
    }
    if(c && c.card_id==='S1-THF-010' && sourceHero){ var bst=heroClass(sourceHero); printed=(bst==='Thief')?40:((bst==='Rogue'||bst==='Renegade')?50:printed); }
    var base=printed;
    if(sourceHero && isSingleTargetAttack(c) && activeTimedAttachmentForHero(appState,sourceHero,'S1-CLE-018')) base*=2;
    if(sourceHero) base+=passiveAttackDamageBonus(c,sourceHero);
    if(sourceHero && hasEnrageBonus(sourceHero)) base+=20;
    var actualType=sourceHero?damageTypeForActionSource(c,sourceHero):currentDamageType(c);
    if(sourceHero && actualType==='Physical' && activeTimedAttachmentForHero(appState,sourceHero,'S1-CLE-006')) base+=20;
    if(sourceHero && actualType==='Magical' && activeTimedAttachmentForHero(appState,sourceHero,'S1-CLE-007')) base+=20;
    if(sourceHero && actualType==='Magical') base+=attachmentModifierAmount(appState,sourceHero,'S1-ITM-010',20);
    if(sourceHero && (actualType==='Physical'||actualType==='Magical')) base+=attachmentModifierAmount(appState,sourceHero,'S1-ITM-014',20);
    if(sourceHero && (actualType==='Physical'||actualType==='Magical')) base+=coordinationAttackBonus(appState,sourceHero);
    if(sourceHero && activeTimedAttachmentForHero(appState,sourceHero,'S1-EVT-005')) base*=2;
    return base;
  }
  function isExecuteEffect(c){ return !!(c && Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='defeat_target_if_hp_threshold'; })); }
  function executeCostBySource(c, sourceHero){
    var e=c && Array.isArray(c.effect) && c.effect.find(function(x){ return x.kind==='defeat_target_if_hp_threshold'; });
    if(e && e.mana_by_rank){ var key=branchClassKey(e.mana_by_rank, sourceHero); return Number(e.mana_by_rank[key]||cardCost(c)||0); }
    return Number(cardCost(c)||0);
  }
  function executeTargetValid(targetHero){ return !!(targetHero && Number(targetHero.hp||0)>0 && Number(targetHero.hp||0)<=Number(targetHero.maxHp||100)/2); }
  function currentDamageType(c){
    if(c.attack && c.attack.damage_type) return c.attack.damage_type;
    if(Array.isArray(c.effect)){ for(var i=0;i<c.effect.length;i++){ var e=c.effect[i]; if(e.kind==='deal_attack_damage' && e.damage_type) return e.damage_type; } }
    return 'Physical';
  }
  function hasAetherInfusion(hero){ return hasActiveAttachmentForHero(appState,hero,'S1-MAG-022'); }
  function physicalAttackConvertedByAether(c, sourceHero){ return !!(sourceHero && hasAetherInfusion(sourceHero) && /Physical Attack/i.test(cardSubtype(c)||'')); }
  function actionIsMagicalAttackForSource(c, sourceHero){ return !!(c && (/Magical Attack/i.test(cardSubtype(c)||'') || physicalAttackConvertedByAether(c,sourceHero))); }
  function damageTypeForActionSource(c, sourceHero){ return physicalAttackConvertedByAether(c,sourceHero)?'Magical':currentDamageType(c); }
  function magicalSurgeBonusAmount(sourceHero){ var hc=heroClass(sourceHero); if(hc==='Arcane Duelist') return 20; if(hc==='Spell Blade') return 10; return 0; }
  function magicalSurgeChoiceAvailable(state, action, sourceHero, c){
    if(!state || !action || !sourceHero || !actionIsMagicalAttackForSource(c,sourceHero)) return false;
    if(!magicalSurgeBonusAmount(sourceHero)) return false;
    var key=manaPoolKeyForSide(action.source_side||action.side||'PLAYER');
    return Number(state[key]||0)>=1;
  }
  function maybeApplyMagicalSurge(state, action, sourceHero, c){
    if(!state || !action || !sourceHero || !action.use_magical_surge || !actionIsMagicalAttackForSource(c,sourceHero)) return 0;
    var bonus=magicalSurgeBonusAmount(sourceHero); if(!bonus) return 0;
    var key=manaPoolKeyForSide(action.source_side||action.side||'PLAYER');
    if(Number(state[key]||0)<1){ pushLog(state,(heroClass(sourceHero)==='Arcane Duelist'?'Arcane Surge':'Mana Surge')+' was chosen but there is no additional Mana Shard available. No bonus applied.'); return 0; }
    state[key]=Math.max(0,Number(state[key]||0)-1);
    action.magical_surge_bonus=Number(action.magical_surge_bonus||0)+bonus;
    pushLog(state,(heroClass(sourceHero)==='Arcane Duelist'?'Arcane Surge':'Mana Surge')+' spends 1 additional Mana Shard. '+cardName(card(sourceHero.card_id))+' selected Magical Attack gains +'+bonus+' damage.');
    return bonus;
  }
  function archerSingleTargetAttackCard(c){ return !!(c && (c.card_id==='S1-ARC-002'||c.card_id==='S1-ARC-007') && isAttackCard(c) && isSingleTargetAttack(c)); }
  function tripleShotStatusFor(hero, cardId){
    var att=activeAttachmentForHero(appState,hero,'S1-ARC-013');
    return att && (!att.chosen_card_id || att.chosen_card_id===cardId) ? att : null;
  }
  function consumeTripleShotStatus(hero, cardId){
    var att=tripleShotStatusFor(hero,cardId); return att?releaseLocalAttachment(appState,att,'Triple Shot consumed'):false;
  }
  function attackActsAsTripleShotArea(action, sourceHero){ return !!(action && action.card_id && sourceHero && archerSingleTargetAttackCard(card(action.card_id)) && tripleShotStatusFor(sourceHero, action.card_id)); }
  function dualArrowDefaultLanes(sourceLane){
    if(sourceLane==='LEFT') return ['LEFT','CENTER'];
    if(sourceLane==='RIGHT') return ['CENTER','RIGHT'];
    return ['LEFT','CENTER','RIGHT'];
  }
  function dualArrowPairOptionsFor(state, side, sourceLane, targetSide){
    targetSide=targetSide||oppositeSide(side);
    var legal=LANE_ORDER.filter(function(lane){
      var h=sideHeroes(state,targetSide)[lane]; return !!h && (isLegacyModeHero(h) || Number(h.hp||0)>0);
    });
    var pairs=[];
    for(var i=0;i<legal.length;i++) for(var j=i+1;j<legal.length;j++){
      var pair=[legal[i],legal[j]];
      var containsHero=pair.some(function(lane){ var h=sideHeroes(state,targetSide)[lane]; return h && Number(h.hp||0)>0 && !isLegacyModeHero(h); });
      if(containsHero) pairs.push(pair);
    }
    return pairs;
  }
  function dualArrowRequiresPairChoice(state, side, sourceLane){ return dualArrowPairOptionsFor(state,side,sourceLane,oppositeSide(side)).length>=1; }
  function dualArrowAffectedLanesForAction(state, action){
    if(action && Array.isArray(action.target_lanes) && action.target_lanes.length) return action.target_lanes.slice();
    return dualArrowPairOptionsFor(state, action.source_side||'PLAYER', action.source_lane, action.target_side||oppositeSide(action.source_side||'PLAYER'))[0] || [];
  }
  function arrowBarrageSpendMax(state, side){ return Math.max(0, Number(state && state[manaPoolKeyForSide(side||'PLAYER')] || 0)); }
  function arrowBarrageSpendValid(state, side, amount){ amount=Number(amount||0); return amount>=1 && amount<=arrowBarrageSpendMax(state,side); }
  function targetOutsideDefaultCoverage(sourceLane,targetLane){ return targetCoverage(sourceLane).indexOf(targetLane)===-1; }
  function printedClassDamage(c, sourceHero){
    var base=0;
    if(c && c.attack){ if(c.attack.damage_by_class){ base=classMappedNumber(c.attack.damage_by_class, sourceHero, c); } else if(typeof c.attack.damage!=='undefined') base=Number(c.attack.damage||0); }
    if(!base && c && Array.isArray(c.effect)){
      for(var i=0;i<c.effect.length;i++){ var e=c.effect[i]; if(e.kind==='deal_attack_damage'){ if(e.amount_by_class) base=classMappedNumber(e.amount_by_class, sourceHero, c); else base=Number(e.amount||0); break; } }
    }
    if(c && c.card_id==='S1-THF-010' && sourceHero){ var bst=heroClass(sourceHero); base=(bst==='Thief')?40:((bst==='Rogue'||bst==='Renegade')?50:base); }
    return Number(base||0);
  }
  function castingModifierBreakdown(state,c,sourceHero,targetHero){
    var out=[],actual=damageTypeForActionSource(c,sourceHero),heroCard=card(heroIdFrom(sourceHero));
    function add(sourceType,sourceId,sourceName,amount,reason){amount=Number(amount||0);if(amount)out.push({source_type:sourceType,source_card_id:sourceId||null,source_name:sourceName||sourceId||sourceType,amount:amount,reason:reason||('+'+amount+' damage')});}
    var passive=passiveAttackDamageBonus(c,sourceHero); if(passive)add('Hero Ability',heroIdFrom(sourceHero),heroCard&&heroCard.class_ability&&heroCard.class_ability.name||cardName(heroCard),passive,'Active Hero ability modifies '+actual+' Attack damage');
    if(hasEnrageBonus(sourceHero))add('Attachment','S1-WAR-013','Enrage',20,'Enrage +20 Attack damage');
    if(actual==='Physical'&&activeTimedAttachmentForHero(state,sourceHero,'S1-CLE-006'))add('Attachment','S1-CLE-006','Blessing of Might',20,'Blessing of Might +20 Physical Attack damage');
    if(actual==='Magical'&&activeTimedAttachmentForHero(state,sourceHero,'S1-CLE-007'))add('Attachment','S1-CLE-007','Blessing of Wisdom',20,'Blessing of Wisdom +20 Magical Attack damage');
    if(actual==='Magical'){var arc=attachmentModifierAmount(state,sourceHero,'S1-ITM-010',20);if(arc)add('Attachment','S1-ITM-010','Arcane Scroll',arc,'Arcane Scroll +'+arc+' Magical Attack damage');}
    var holy=attachmentModifierAmount(state,sourceHero,'S1-ITM-014',20);if(holy)add('Attachment','S1-ITM-014','Holy Medallion',holy,'Holy Medallion +'+holy+' Attack damage');
    var coord=coordinationAttackBonus(state,sourceHero);if(coord)add('Attachment','S1-EVT-011','Coordination Attack',coord,'Coordination Attack +'+coord+' Attack damage');
    return out;
  }
  function castingSnapshotForAction(state,action,sourceHero,targetHero){
    var c=card(action.card_id),printed=printedClassDamage(c,sourceHero),committed=currentClassDamage(c,sourceHero,targetHero),breakdown=castingModifierBreakdown(state,c,sourceHero,targetHero),known=breakdown.reduce(function(n,x){return n+Number(x.amount||0);},0);
    if(activeTimedAttachmentForHero(state,sourceHero,'S1-CLE-018')) breakdown.push({source_type:'Attachment',source_card_id:'S1-CLE-018',source_name:'Heaven’s Fury',amount:committed-printed-known,reason:'Heaven’s Fury doubles the legal single-target Attack damage'});
    else if(activeTimedAttachmentForHero(state,sourceHero,'S1-EVT-005')) breakdown.push({source_type:'Attachment',source_card_id:'S1-EVT-005',source_name:'Last Resort',amount:committed-printed-known,reason:'Last Resort doubles full Attack Card damage'});
    else if(committed-printed-known) breakdown.push({source_type:'Runtime',source_card_id:null,source_name:'Other legal modifier',amount:committed-printed-known,reason:'Additional legal committed modifier'});
    return {printed_damage:Number(printed||0),committed_damage:Number(committed||0),damage_type:damageTypeForActionSource(c,sourceHero),action_profile:'Casting Attack',modifier_breakdown:breakdown};
  }
  function effectRowAuditForAttack(cardObj,sourceHero){
    if(!cardObj||!sourceHero)return null;var map=cardObj.effect_strings_by_class||cardObj.attack&&cardObj.attack.damage_by_class||{};if(!Object.keys(map||{}).length&&Array.isArray(cardObj.effect)){for(var i=0;i<cardObj.effect.length;i++){var e=cardObj.effect[i]||{};if(e.amount_by_class){map=e.amount_by_class;break;}if(e.damage_by_class){map=e.damage_by_class;break;}}}
    var row=branchClassKeyForCard(map,sourceHero,cardObj);if(!row)return null;var hc=heroClass(sourceHero),heroCard=card(heroIdFrom(sourceHero))||{},base=printedClassDamage(cardObj,sourceHero),dtype=damageTypeForActionSource(cardObj,sourceHero),rowText=cardObj.effect_strings_by_class&&cardObj.effect_strings_by_class[row]||'';
    return {source_card_id:heroIdFrom(sourceHero),source_name:cardName(heroCard),source_class:hc,source_rank:'Rank '+heroRank(sourceHero),row_used:row,match_type:row===hc?'Exact match':'Legal lineage fallback',base_label:base?('Base '+dtype+' damage'):'Base effect',base_value:Number(base||0),row_text:rowText};
  }
  function namedDamageModifiersForResponse(rw,sourceHero){
    var p=rw&&rw.post||{},mods=clone(p.modifier_breakdown||[]),c=card(rw&&rw.card_id);if(!mods.length&&sourceHero&&c){mods=castingModifierBreakdown(appState,c,sourceHero,sideHeroes(appState,rw.target_side)[rw.target_lane]);var printed=printedClassDamage(c,sourceHero),known=mods.reduce(function(n,m){return n+Number(m.amount||0);},0),delta=Number(rw.damage||0)-Number(printed||0)-known;if(delta){var surge=Number(rw.action&&rw.action.magical_surge_bonus||0);if(surge){mods.push({source_type:'Hero Ability',source_card_id:heroIdFrom(sourceHero),source_name:heroClass(sourceHero)==='Arcane Duelist'?'Arcane Surge':'Mana Surge',amount:surge,reason:(heroClass(sourceHero)==='Arcane Duelist'?'Arcane Surge':'Mana Surge')+' +'+surge+' Magical Attack damage'});delta-=surge;}if(delta)mods.push({source_type:'Runtime',source_card_id:null,source_name:'Other legal modifier',amount:delta,reason:'Other legal modifier '+(delta>=0?'+':'')+delta+' damage'});}}
    return mods;
  }
  function applyAttackAuditToEvent(evt,rw,result,sourceHero){
    if(!evt)return;var c=card(rw.card_id),printed=Number((rw.post&&rw.post.printed_damage)!=null?rw.post.printed_damage:printedClassDamage(c,sourceHero));evt.effect_row=evt.effect_row||effectRowAuditForAttack(c,sourceHero);evt.damage_modifiers=namedDamageModifiersForResponse(rw,sourceHero);evt.damage_summary={base_damage:printed,damage_after_modifiers:Number(rw.damage||0),conditional_damage_bonus:Number(result&&result.conditional_incoming_damage||0),conditional_damage_after_defense:Number(result&&result.conditional_damage||0),response_block:Number(result&&result.response_block||0),passive_reduction:Number(result&&result.passive_block||0),active_reduction:Number(result&&result.active_reduction||0),calculated_final_damage:Number(result&&result.calculated_final_damage!=null?result.calculated_final_damage:result&&result.damage||0),actual_hp_damage:Number(result&&result.hp_damage||0),overkill:Number(result&&result.overkill||0),target_hp_before:Number(result&&result.target_hp_before||0),target_hp_after:Number(result&&result.target_hp_after||0),damage_type:rw.damage_type||''};
  }
  function damageBreakdownLinesFromResponse(rw){
    var p=rw&&rw.post||{},lines=[];if(typeof p.printed_damage!=='undefined')lines.push('Base damage: '+Number(p.printed_damage||0)+'.');if(typeof rw.damage!=='undefined')lines.push('Damage after modifiers: '+Number(rw.damage||0)+'.');return lines;
  }

  function scalingFlatBonus(c, sourceHero, targetHero){
    // Scaling cards multiply only the printed/source class damage line. Existing flat buffs/passives are added after scaling.
    // This prevents Mana Void / Colossal Dreadbolt from multiplying external +damage modifiers.
    var printed=printedClassDamage(c,sourceHero), current=currentClassDamage(c,sourceHero,targetHero);
    return Math.max(0, Number(current||0)-Number(printed||0));
  }
  function computedAttackDamageForAction(state, action, sourceHero, targetHero){
    var c=card(action.card_id); var dmg=currentClassDamage(c,sourceHero,targetHero);
    if(c.card_id==='S1-ARC-024'){
      var drawn=Number(state && state.cardsDrawnThisTurn && state.cardsDrawnThisTurn[action.source_side||'PLAYER'] || 0);
      var cap=(heroClass(sourceHero)==='Grand Arbalest')?120:80;
      var printed=printedClassDamage(c,sourceHero);
      var flat=scalingFlatBonus(c,sourceHero,targetHero);
      var scaledResult=window.GL_RULES_RUNTIME.scalingDamage(c,sourceHero,state,action,{printedDamage:printed,flatBonus:flat,heroClass:heroClass(sourceHero),cardsDrawnForSide:function(st,side){return Number(st&&st.cardsDrawnThisTurn&&st.cardsDrawnThisTurn[side]||0);},manaRegenForSide:function(st,side){return Number(st&&st[side==='PLAYER'?'manaRegen':'aiManaRegen']||0);}}); var scaled=scaledResult.damage-flat; dmg=scaledResult.damage; action.damage_formula_log='Colossal Dreadbolt formula: '+heroClass(sourceHero)+' base '+printed+' × Draw This Turn '+drawn+' = '+(drawn*printed)+'; cap '+cap+' => '+scaled+(flat>0?'; flat bonus +'+flat:'')+'.';
    }
    if(c.card_id==='S1-MAG-025'){
      var opp=oppositeSide(action.source_side||'PLAYER');
      var regen=Number(state && state[(opp==='PLAYER'?'manaRegen':'aiManaRegen')] || 0);
      var printedMv=printedClassDamage(c,sourceHero);
      var flatMv=scalingFlatBonus(c,sourceHero,targetHero);
      dmg=window.GL_RULES_RUNTIME.scalingDamage(c,sourceHero,state,action,{printedDamage:printedMv,flatBonus:flatMv,heroClass:heroClass(sourceHero),cardsDrawnForSide:function(st,side){return Number(st&&st.cardsDrawnThisTurn&&st.cardsDrawnThisTurn[side]||0);},manaRegenForSide:function(st,side){return Number(st&&st[side==='PLAYER'?'manaRegen':'aiManaRegen']||0);}}).damage;
      action.damage_formula_log='Mana Void formula: '+heroClass(sourceHero)+' base '+printedMv+' × opponent Mana Regen '+regen+' = '+(regen*printedMv)+(flatMv>0?'; flat bonus +'+flatMv:'')+'.';
    }
    if(isArrowBarrageCard(c)){ var spend=Math.max(1, Number(action.mana_spent||1)); dmg=10*spend; }
    var surge=Number(action.magical_surge_bonus||0); if(surge>0){ dmg+=surge; if(action.damage_formula_log) action.damage_formula_log += ' Surge +'+surge+' => '+dmg+'.'; }
    if(isChargedShotCard(c) && action.source_lane && action.target_lane && targetOutsideDefaultCoverage(action.source_lane, action.target_lane)){
      dmg*=2;
      action.damage_formula_log='Charged Shot connected Range Attack full damage ×2 = '+dmg+'.';
      pushLog(state, cardName(c)+' targets outside default Attack Coverage and doubles full Attack Card damage after additive modifiers.');
    }
    return dmg;
  }
  function branchEffectText(c, sourceHero){
    if(!c || !c.effect_strings_by_class || !sourceHero) return '';
    var hc=heroClass(sourceHero); var map=c.effect_strings_by_class;
    var key=branchClassKeyForCard(map, sourceHero, c);
    if(map[key]) return cleanDisplayText(map[key],'');
    if(map[hc]) return cleanDisplayText(map[hc],'');
    if(hc==='Renegade' && map.Rogue && !map.Renegade) return cleanDisplayText(map.Rogue,'');
    return '';
  }
  function burnAmountFromPayload(c){
    if(!c || !Array.isArray(c.effect)) return null;
    for(var i=0;i<c.effect.length;i++){ var e=c.effect[i]; if(e.kind==='source_structured_effect_json' && e.payload && e.payload.status && e.payload.status.burn_amount) return Number(e.payload.status.burn_amount); }
    return null;
  }
  function statusEffectLine(c, e, sourceHero){
    var status=String(e.status||'').trim(); if(!status) return '';
    var dur=durationForEffect(e, sourceHero); var turns=' turn'+(Number(dur)===1?'':'s');
    if(/^burn$/i.test(status)){ var burn=burnAmountFromPayload(c); return 'Inflict '+(burn?burn+' ':'')+'Burn for '+dur+turns+'.'; }
    if(/^poison$/i.test(status)) return 'Inflict Poison for '+dur+turns+'.';
    return 'Inflict '+status+' for '+dur+turns+'.';
  }
  function activeEffectSummary(c, sourceHero){
    var prefix=sourceHero?heroClass(sourceHero)+': ':'';
    var lines=[]; var dmg=currentClassDamage(c,sourceHero);
    if(dmg) lines.push('Deal '+dmg+' '+currentDamageType(c)+' Attack damage.');
    if(c && Array.isArray(c.effect)){
      c.effect.forEach(function(e){
        if(e.kind==='inflict_status'){ var line=statusEffectLine(c,e,sourceHero); if(line && lines.indexOf(line)===-1) lines.push(line); }
        if(e.kind==='attack_restriction' && /cannot be dodged/i.test(String(e.raw_value||e.text||''))) lines.push('This attack cannot be dodged.');
        if(e.kind==='attack_restriction' && /cannot be blocked/i.test(String(e.raw_value||e.text||''))) lines.push('This attack cannot be blocked.');
        if(e.kind==='attack_connected_conditional_damage'){ var txt=cleanDisplayText(e.text||e.raw_value||e.condition||'',''); if(txt && lines.indexOf(txt)===-1) lines.push(txt); }
      });
    }
    var branch=branchEffectText(c,sourceHero);
    if(!lines.length && branch) lines.push(branch);
    else if(branch){
      // Preserve class-specific clauses that are not represented by normalized damage/status fields.
      branch.split(/(?<=\.)\s+/).forEach(function(part){ var clean=cleanDisplayText(part,''); if(clean && !lines.some(function(x){ return x.toLowerCase()===clean.toLowerCase(); }) && !/^Deal\s+\d+/i.test(clean)) lines.push(clean); });
    }
    if(!lines.length){
      var summary=summarizeCard(c).split('\n').filter(Boolean)[0]||'';
      if(summary) lines.push(summary);
    }
    return prefix+lines.join(' ');
  }
  function isDodgeResponseCard(rc){
    return !!(rc && Array.isArray(rc.effect) && rc.effect.some(function(e){
      return e.kind==='dodge_incoming_attack_damage' || e.kind==='dodge_incoming_damage' || (e.kind==='source_structured_effect_json' && e.payload && /Dodge/i.test(String(e.payload.defense||'')));
    }));
  }
  function responseExtraDiscardCount(rc){ var cost=rc&&rc.cost||{}, cj=cost.cost_json||{}, add=cost.additional_discard_from_hand||{}; if(cj.discard_any_from_hand!=null) return Number(cj.discard_any_from_hand||0); if(add.kind==='discard_from_hand') return Number(add.count||0); var canonical=rc&&rc.canonical_cost&&Array.isArray(rc.canonical_cost.additional_costs)?rc.canonical_cost.additional_costs.find(function(x){return x&&x.kind==='discard_from_hand'&&!Array.isArray(x.exact_card_ids);}):null; return Number(canonical&&canonical.count||0); }
  function handHasResponseExtraDiscard(hand, idx, rc){ return ((hand||[]).length-1)>=responseExtraDiscardCount(rc); }
  function payResponseExtraDiscard(state, side, responseIndex, rc, selectedIndex){
    var count=responseExtraDiscardCount(rc),hand=sideHand(state,side),discarded=[];
    if(count<=0)return 0;
    var idx=Number(selectedIndex);if(!Number.isInteger(idx)||idx<0||idx>=hand.length||idx===responseIndex){showInfo('Escape Arrow','Choose 1 other card in your hand to discard.');return 0;}
    var motion=captureHandDiscardMotion(side,idx,hand[idx]);var id=hand.splice(idx,1)[0];sideDiscard(state,side).push(id);queueHandDiscardMotion(motion);discarded.push(id);if(idx<responseIndex)responseIndex--;pushLog(state,cardName(rc)+' discards selected extra response cost: '+cardName(card(id))+'.');return 1;
  }
  function responseExtraDiscardCandidates(state,side,responseOption){
    var hand=sideHand(state,side)||[], responseIndex=Number(responseOption&&responseOption.hand_index), out=[];
    if(hand[responseIndex]!==responseOption.card_id) responseIndex=hand.indexOf(responseOption.card_id);
    hand.forEach(function(id,idx){ if(idx!==responseIndex) out.push({card_id:id,hand_index:idx}); });
    return out;
  }
  function openResponseExtraDiscardChoice(state,rw,responseOption){
    var side=rw.target_side||rw.response_owner||'PLAYER', candidates=responseExtraDiscardCandidates(state,side,responseOption);
    if(!candidates.length){showInfo('Escape Arrow','Choose 1 other card in your hand to discard, but no legal card remains.');return false;}
    if(!rw.response_window_token) rw.response_window_token='RW:'+String(rw.card_id||'')+':'+String(rw.source_side||'')+':'+String(rw.source_lane||'')+':'+String(rw.target_side||'')+':'+String(rw.target_lane||'')+':'+Date.now();
    state.pending={type:'response_extra_discard_choice',decision_side:side,side:side,response_owner:side,response_option:clone(responseOption),response_window_token:rw.response_window_token,candidates:candidates,selected_index:null,parent_response_hidden:true};
    closeResponseWindowUI();
    renderResponseExtraDiscardChoice();return true;
  }
  function renderResponseExtraDiscardChoice(){
    if(!appState||!appState.pending||appState.pending.type!=='response_extra_discard_choice')return;
    var p=appState.pending,selected=(Number.isInteger(Number(p.selected_index))?Number(p.selected_index):null),choices=p.candidates||[];
    if(!pvpLocalOwnsPending(p)){pvpHideChoiceForNonOwner();return;}if(SUPPRESS_RENDER||!$('choiceOverlay'))return;
    $('choiceTitle').textContent='Escape Arrow — Choose Discard';$('choiceConfirm').style.display='';$('choiceConfirm').textContent='Discard Selected Card';$('choiceConfirm').disabled=selected===null||!choices.length;
    $('choiceBody').innerHTML='<p class="choice-instruction">Choose exactly 1 other card from your hand to discard as Escape Arrow’s additional cost.</p><div class="choice-grid card-search-choice-grid">'+choices.map(function(x,idx){var sel=idx===selected;return '<article class="discard-choice '+(sel?'selected':'')+'"><button class="discard-preview" type="button" data-preview="'+esc(x.card_id)+'"><img src="'+esc(thumbFor(x.card_id))+'" alt="'+esc(cardName(card(x.card_id)))+' thumbnail"><span>'+esc(cardName(card(x.card_id)))+'</span></button><button class="discard-select" type="button" data-response-extra-discard-index="'+idx+'">'+(sel?'Selected':'Select')+'</button></article>';}).join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function selectResponseExtraDiscardChoice(idx){if(!appState||!appState.pending||appState.pending.type!=='response_extra_discard_choice')return false;idx=Number(idx);if(idx<0||idx>=(appState.pending.candidates||[]).length)return false;appState.pending.selected_index=idx;renderResponseExtraDiscardChoice();return true;}
  function commitResponseExtraDiscardChoice(){
    if(!appState||!appState.pending||appState.pending.type!=='response_extra_discard_choice')return false;
    var p=appState.pending,selected=Number(p.selected_index);
    if(!Number.isInteger(selected)){showInfo('Escape Arrow','Choose a card to discard.');return false;}
    var choice=(p.candidates||[])[selected],opt=clone(p.response_option||{}),side=p.side||p.response_owner||'PLAYER';
    if(!choice){showInfo('Escape Arrow','Choose a card to discard.');return false;}
    var rw=appState.responseWindow;
    if(!rw || (p.response_window_token && rw.response_window_token!==p.response_window_token)){
      showInfo('Escape Arrow','The incoming attack changed before the discard was confirmed. The response choice was canceled safely.');
      appState.pending=rw?{type:'response_window',card_id:rw.card_id,response_owner:rw.response_owner}:null;closeChoice();if(rw)renderResponseWindow();return false;
    }
    var hand=sideHand(appState,side)||[],responseIndex=Number(opt.hand_index);
    if(hand[responseIndex]!==opt.card_id)responseIndex=hand.indexOf(opt.card_id);
    var extraIndex=Number(choice.hand_index);
    if(extraIndex===responseIndex||hand[extraIndex]!==choice.card_id){
      extraIndex=-1;for(var i=0;i<hand.length;i++){if(i!==responseIndex&&hand[i]===choice.card_id){extraIndex=i;break;}}
    }
    if(responseIndex<0||extraIndex<0||extraIndex===responseIndex){showInfo('Escape Arrow','The selected discard card is no longer available. Choose again.');renderResponseExtraDiscardChoice();return false;}
    opt.hand_index=responseIndex;opt.selected_extra_discard_index=extraIndex;opt.selected_extra_discard_card_id=choice.card_id;
    // Restore the canonical response frame before resolving. This is the continuation contract
    // used by the attack resolver, Legacy gate, and AI turn director.
    appState.pending={type:'response_window',card_id:rw.card_id,response_owner:rw.response_owner,continuation_from:'escape_arrow_extra_discard'};
    closeChoice();
    var resolved=resolveResponseWindow(opt);
    if(!resolved){
      if(appState&&appState.responseWindow){appState.pending={type:'response_window',card_id:appState.responseWindow.card_id,response_owner:appState.responseWindow.response_owner};renderResponseWindow();}
      return false;
    }
    return true;
  }
  function responseKind(rc, incoming){
    var override=responseProfileOverride(rc,incoming);
    if(override===null) return null;
    if(override && override.kind) return override.kind;
    if(!rc || !Array.isArray(rc.effect)) return null;
    if(isCoverUpCard(rc)){ return incoming && incoming.attack_type==='Single Target' ? 'cover_up' : null; }
    if(isSmokeScreenCard(rc)) return incoming && incoming.attack_type==='Single Target' ? 'negate_return' : null;
    if(isSpectralGrapplingHookCard(rc)) return incoming && !incoming.cannot_dodge ? 'dodge' : null;
    if(isThiefDodgeResponseCard(rc) || isDodgeResponseCard(rc)) return incoming && !incoming.cannot_dodge ? 'dodge' : null;
    if(isBrilliantRadianceCard(rc)) return 'negate_return';
    if(incoming && incoming.cannot_block) return null;
    if(isSacredBulwarkCard(rc)) return 'block';
    if(isBlessingDivinityCard(rc)) return 'immunity';
    if(isDefensiveFormationCard(rc)) return incoming && /Area|All|Dual/i.test(String(incoming.attack_type||'')) ? 'area_reduce' : null;
    for(var i=0;i<rc.effect.length;i++){ var e=rc.effect[i];
      if(e.kind==='negate_incoming_attack' && (!e.damage_types || e.damage_types.indexOf(incoming.damage_type)!==-1)) return 'negate_return';
      if(e.kind==='damage_immunity') return 'immunity';
      if(e.kind==='damage_prevention_active_effect') return 'immunity';
      if(e.kind==='protective_def_response_block') return 'block';
      if(e.kind==='block_damage' && (!e.damage_types || e.damage_types.indexOf(incoming.damage_type)!==-1)) return 'block';
      if((e.kind==='dodge_incoming_attack_damage' || e.kind==='dodge_incoming_damage') && !incoming.cannot_dodge && (!e.damage_types || e.damage_types.indexOf(incoming.damage_type)!==-1)) return 'dodge';
    }
    return null;
  }
  function responseBlockAmount(rc, incoming, defenderHero){
    var override=responseProfileOverride(rc,incoming);
    if(override && override.kind==='block'){ if(override.amount_by_class) return classMappedNumber(override.amount_by_class, defenderHero, rc); return Number(override.amount||0); }
    var amount=0; if(!rc || !Array.isArray(rc.effect)) return 0;
    rc.effect.forEach(function(e){
      if((e.kind==='block_damage' || e.kind==='protective_def_response_block') && (!e.damage_types || e.damage_types.indexOf(incoming.damage_type)!==-1)){ if(e.amount_by_class){ amount=Math.max(amount, classMappedNumber(e.amount_by_class, defenderHero)); } else amount=Math.max(amount, Number(e.amount||0)); }
    });
    return amount;
  }
  function isDragonbornHero(hero){ return /Dragonborn/i.test(String((card(heroIdFrom(hero)).identity||{}).race||'')); }
  function dragonScaleResponseOptionsFor(state, responderSide, incoming){
    // PvP server-authoritative mode may evaluate either canonical side. Do not hard-code PLAYER.
    if(!state || Number(state[racialTokenKey(responderSide)]||0)<1 || !incoming) return [];
    if(['Physical','Magical'].indexOf(String(incoming.damage_type||''))===-1) return [];
    var lanes=responseAffectedLanes(state,responderSide,incoming), out=[];
    lanes.forEach(function(lane){
      var h=sideHeroes(state,responderSide)[lane];
      if(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && isDragonbornHero(h) && !racialAbilityUsedThisTurnByHero(state,h)){
        out.push({card_id:h.card_id, source_lane:lane, response_kind:'dragon_scale', racial_ability:'dragon_scale', label:'Dragon Scale — '+cardName(card(h.card_id))+' from '+lane, block:40, cost_racial:1, chosen_affected_lane:lane});
      }
    });
    return out;
  }
  function responseAffectedLanes(state, responderSide, incoming){
    var lanes=(incoming&&incoming.affected_lanes&&incoming.affected_lanes.length)?incoming.affected_lanes.slice():(incoming&&incoming.target_lane?[incoming.target_lane]:[]);
    return lanes.filter(function(l){ var h=sideHeroes(state,responderSide)[l]; return h && Number(h.hp||0)>0; });
  }
  function defensiveFormationReduction(state, responderSide){ return activeAlliedBaseFamiliesDifferent(state,responderSide)?40:20; }
  function responseOptionsFor(state, responderSide, incoming){
    if(!state || !responderSide) return [];
    var hand=sideHand(state,responderSide)||[], affected=responseAffectedLanes(state,responderSide,incoming), defender=sideHeroes(state,responderSide)[incoming.target_lane||affected[0]];
    var options=[], manaKey=manaPoolKeyForSide(responderSide);
    function affordable(rc,hero){return responseCostForSource(rc,hero,state,responderSide)<=Number(state[manaKey]||0);}
    hand.forEach(function(id,idx){ var rc=card(id); if(!isResponseOnly(rc)) return; if(!handHasResponseExtraDiscard(hand,idx,rc)) return;
      var casting=!!(incoming.casting||isCastingCard(card(incoming.card_id)));
      var area=!!(incoming.area||(incoming.affected_lanes||[]).length>1||/Area|All|Dual|Venom Detonation/i.test(String(incoming.attack_type||'')));
      if((isSmokeScreenCard(rc)||isBrilliantRadianceCard(rc)||rc.card_id==='S1-EVT-009')&&casting) return;
            if(isCoverUpCard(rc)){
        if(responseKind(rc,incoming)!=='cover_up') return;
        adjacentLanes(incoming.target_lane).forEach(function(srcLane){ var src=sideHeroes(state,responderSide)[srcLane]; if(sourceCompatibleForResponse(rc,src) && !hasStatus(src,'Freeze') && !hasStatus(defender,'Freeze') && affordable(rc,src)) options.push({card_id:id, hand_index:idx, card:rc, source_lane:srcLane, response_kind:'cover_up', label:cardName(rc)+' — '+cardName(card(src.card_id))+' from '+srcLane}); }); return;
      }
      var rk=responseKind(rc,incoming); if(!rk) return;
      if(isSpectralGrapplingHookCard(rc) && !spectralGrapplingHookResponderValid(defender)) return;
      if(isChainMailCard(rc) && !chainMailResponderValid(defender)) return;
      if((incoming.affected_lanes||[]).length>1 && !(isSacredBulwarkCard(rc) || isBlessingDivinityCard(rc) || isDefensiveFormationCard(rc) || isSpectralGrapplingHookCard(rc))) return;
      if(isDefensiveFormationCard(rc)){ if(affordable(rc,null)) options.push({card_id:id, hand_index:idx, card:rc, source_lane:null, response_kind:'area_reduce', affected_lanes:affected.slice()}); return; }
      if(isAlliedSourceDefenseCard(rc)){
        LANE_ORDER.forEach(function(srcLane){ var src=sideHeroes(state,responderSide)[srcLane]; if(sourceCompatibleForResponse(rc,src)&&affordable(rc,src)){ var pol=attachmentPolicyForCard(rc,src); if(!pol||attachmentSlotIndex(src)!==-1) options.push({card_id:id, hand_index:idx, card:rc, source_lane:srcLane, response_kind:rk, affected_lanes:(isHolyBarrierCard(rc)?null:affected.slice()), protected_lane:incoming.target_lane}); } }); return;
      }
      if(rk==='dodge' && hasStatus(defender,'Freeze')) return;
      if(requiresSourceHero(rc) && !sourceCompatibleForResponse(rc,defender)) return;
      if(!affordable(rc,defender)) return;
      options.push({card_id:id, hand_index:idx, card:rc, source_lane:incoming.target_lane, response_kind:rk});
    });
    options=options.concat(dragonScaleResponseOptionsFor(state,responderSide,incoming));
    options=options.concat(activeLegacyResponseOptionsFor(state,responderSide,incoming));
    return options;
  }
  function responseSourceLegalityReasons(state, responderSide, c, hero){
    var reasons=[];
    if(!hero){ reasons.push('No source Hero is available.'); return reasons; }
    if(Number(hero.hp||0)<=0){ reasons.push('The source Hero is defeated.'); return reasons; }
    if(isLegacyModeHero(hero)){ reasons.push('A Legacy card cannot be used as a Defense source.'); return reasons; }
    if(hasStatus(hero,'Stun')) reasons.push(cardName(card(hero.card_id))+' is Stunned and cannot use a Skill.');
    var heroCard=card(heroIdFrom(hero)), heroCls=heroClass(hero);
    if(window.GL_RULES_RUNTIME&&window.GL_RULES_RUNTIME.explainSourceLegality){
      var runtimeReasons=window.GL_RULES_RUNTIME.explainSourceLegality(c,hero,heroCard,heroCls,{hero_rank:heroRank(hero),attachment_available:attachmentSlotIndex(hero)!==-1});
      (runtimeReasons||[]).forEach(function(x){if(reasons.indexOf(x)===-1)reasons.push(x);});
    } else {
      if(window.GL_RULES_RUNTIME&&window.GL_RULES_RUNTIME.cardLegalForLineage&&!window.GL_RULES_RUNTIME.cardLegalForLineage(c,hero,heroCard,heroCls)) reasons.push('The Hero Class/Lineage cannot use this card.');
      var minRank=canonicalMinimumSourceRank(c); if(minRank&&heroRank(hero)<minRank) reasons.push('Requires Rank '+minRank+' or higher.');
      if(requiresAttachmentSlot(c,hero)&&attachmentSlotIndex(hero)===-1) reasons.push('The source Hero has no empty Attachment Slot.');
    }
    var cost=responseCostForSource(c,hero,state,responderSide), available=Number(state[manaPoolKeyForSide(responderSide)]||0);
    if(cost>available) reasons.push('Not enough Mana: needs '+cost+', available '+available+'.');
    return reasons;
  }
  function responseCardBaseClasses(c){
    c=c||{};var req=c.requirement||{},src=c.source_requirement||{},out=[];
    [req.base_skill_class,src.base_skill_class,req.source_package,src.source_package,c.class_family].forEach(function(v){if(v&&out.indexOf(String(v))===-1)out.push(String(v));});
    return out;
  }
  function responseCandidateHeroes(state,responderSide,c,incoming){
    if(!requiresSourceHero(c))return [];
    if(isCoverUpCard(c))return adjacentLanes(incoming.target_lane).map(function(l){return sideHeroes(state,responderSide)[l];}).filter(Boolean);
    if(isAlliedSourceDefenseCard(c))return LANE_ORDER.map(function(l){return sideHeroes(state,responderSide)[l];}).filter(Boolean);
    var defender=sideHeroes(state,responderSide)[incoming.target_lane];return defender?[defender]:[];
  }
  function responseHeroClassMatches(c,hero){
    if(!hero)return false;var lineage=activeLineageForHero(hero).map(normalizedClassToken),bases=responseCardBaseClasses(c).map(normalizedClassToken),legal=canonicalCardLegalClasses(c).map(normalizedClassToken);
    if(bases.length&&bases.some(function(x){return lineage.indexOf(x)!==-1;}))return true;
    return legal.some(function(x){return lineage.indexOf(x)!==-1;});
  }
  function responseHeroLineageRankMatches(c,hero){
    if(!hero||!responseHeroClassMatches(c,hero))return false;
    var legal=canonicalCardLegalClasses(c);if(legal.length&&!lineageContainsAnyClass(hero,legal))return false;
    return heroRank(hero)>=canonicalMinimumSourceRank(c);
  }
  function firstPrioritizedResponseReason(state,responderSide,incoming,c,rawReasons){
    rawReasons=(rawReasons||[]).filter(Boolean);var candidates=responseCandidateHeroes(state,responderSide,c,incoming);
    if(requiresSourceHero(c)&&candidates.length){
      if(!candidates.some(function(h){return responseHeroClassMatches(c,h);}))return 'This Hero’s class cannot use this card.';
      if(!candidates.some(function(h){return responseHeroLineageRankMatches(c,h);}))return 'This Hero’s lineage or rank cannot use this card.';
    }
    var mana=rawReasons.find(function(x){return /not enough mana|needs?\s*\d+.*available|requires?\s*\d+.*available/i.test(x);});
    if(mana){var nums=String(mana).match(/\d+/g)||[];return nums.length>=2?('Not enough Mana. Requires '+nums[0]+'; '+nums[1]+' available.'):'Not enough Mana.';}
    var blockDodge=rawReasons.find(function(x){return /cannot be dodged|cannot be blocked|does not match the incoming|response condition|cannot protect the full multi-target/i.test(x);});
    if(blockDodge)return 'This incoming attack cannot be Blocked or Dodged by this card.';
    var first=rawReasons[0]||'This response is not available in the current state.';
    return String(first).replace(/\s+/g,' ').trim();
  }
  function responseUnavailableReasonsFor(state,responderSide,incoming,cardId,handIndex,legalOptions){
    var c=card(cardId), reasons=[], defender=sideHeroes(state,responderSide)[incoming.target_lane], affected=responseAffectedLanes(state,responderSide,incoming);
    if((legalOptions||[]).some(function(o){return Number(o.hand_index)===Number(handIndex);} )) return [];
    if(!c||!isResponseOnly(c)) return ['This card is not a Defense/Response card.'];
    if(!handHasResponseExtraDiscard(sideHand(state,responderSide)||[],handIndex,c)) reasons.push('Requires 1 other card in Hand to discard as an additional cost.');
    var casting=!!(incoming.casting||isCastingCard(card(incoming.card_id)));
    if(casting&&(isSmokeScreenCard(c)||isBrilliantRadianceCard(c)||c.card_id==='S1-EVT-009')) reasons.push('This response cannot be used against a Casting Attack.');
    if(isCoverUpCard(c)&&String(incoming.attack_type||'')!=='Single Target') reasons.push('Cover Up only responds to a Single Target Attack.');
    if(isDefensiveFormationCard(c)&&!((incoming.affected_lanes||[]).length>1||/Area|All|Dual|Venom Detonation/i.test(String(incoming.attack_type||'')))) reasons.push('Defensive Formation only responds to an Area or multi-target Attack.');
    if((isThiefDodgeResponseCard(c)||isDodgeResponseCard(c))&&incoming.cannot_dodge) reasons.push('The incoming effect cannot be Dodged.');
    if((isThiefDodgeResponseCard(c)||isDodgeResponseCard(c))&&defender&&hasStatus(defender,'Freeze')) reasons.push(cardName(card(defender.card_id))+' is Frozen and cannot Dodge.');
    if(incoming.cannot_block&&Array.isArray(c.effect)&&c.effect.some(function(e){return e.kind==='block_damage'||e.kind==='protective_def_response_block';})) reasons.push('The incoming effect cannot be Blocked.');
    if(isSpectralGrapplingHookCard(c)&&!spectralGrapplingHookResponderValid(defender)) reasons.push('Spectral Grappling Hook has no legal reposition/response target for this defender.');
    if(isChainMailCard(c)&&!chainMailResponderValid(defender)) reasons.push('Chain Mail does not match this incoming damage or defender state.');
    if((incoming.affected_lanes||[]).length>1&&!(isSacredBulwarkCard(c)||isBlessingDivinityCard(c)||isDefensiveFormationCard(c)||isSpectralGrapplingHookCard(c))) reasons.push('This card cannot protect the full multi-target response frame.');
    var kind=responseKind(c,incoming);
    if(!kind&&!reasons.length) reasons.push('This card does not match the incoming '+String(incoming.damage_type||'')+' damage / '+String(incoming.attack_type||'Attack')+' response condition.');
    if(isCoverUpCard(c)){
      var adjacent=adjacentLanes(incoming.target_lane), viable=false, sourceReasons=[];
      adjacent.forEach(function(lane){var h=sideHeroes(state,responderSide)[lane];var rr=responseSourceLegalityReasons(state,responderSide,c,h);if(h&&hasStatus(h,'Freeze'))rr.push(cardName(card(h.card_id))+' is Frozen and cannot reposition for Cover Up.');if(defender&&hasStatus(defender,'Freeze'))rr.push(cardName(card(defender.card_id))+' is Frozen and cannot be repositioned.');if(!rr.length)viable=true;else sourceReasons=sourceReasons.concat(rr);});
      if(!viable) reasons.push(sourceReasons[0]||'No adjacent allied Hero can legally use Cover Up.');
    } else if(isAlliedSourceDefenseCard(c)){
      var any=false, sample=[];
      LANE_ORDER.forEach(function(lane){var h=sideHeroes(state,responderSide)[lane],rr=responseSourceLegalityReasons(state,responderSide,c,h);if(!rr.length){var pol=attachmentPolicyForCard(c,h);if(!pol||attachmentSlotIndex(h)!==-1)any=true;}else sample=sample.concat(rr);});
      if(!any) reasons.push(sample[0]||'No allied Hero meets this card’s Class, Rank, Lineage, status, Mana, or Attachment requirements.');
    } else if(requiresSourceHero(c)){
      var rr=responseSourceLegalityReasons(state,responderSide,c,defender); rr.forEach(function(x){reasons.push(x);});
    } else {
      var cost=responseCostForSource(c,defender,state,responderSide), available=Number(state[manaPoolKeyForSide(responderSide)]||0);if(cost>available)reasons.push('Not enough Mana: needs '+cost+', available '+available+'.');
    }
    var unique=[]; reasons.forEach(function(x){if(x&&unique.indexOf(x)===-1)unique.push(x);});
    return [firstPrioritizedResponseReason(state,responderSide,incoming,c,unique.length?unique:['No legal response source or target is available.'])];
  }
  function responseDisplayItemsFor(state,rw){
    var legal=rw.options||[], items=legal.map(function(o,i){return{available:true,option_index:i,card_id:o.card_id,hand_index:o.hand_index,option:o};});
    var side=rw.response_owner||rw.target_side, incoming=responseIncomingFromWindow(rw), hand=sideHand(state,side)||[];
    hand.forEach(function(id,idx){var c=card(id);if(!isResponseOnly(c))return;if(legal.some(function(o){return Number(o.hand_index)===Number(idx);} ))return;items.push({available:false,card_id:id,hand_index:idx,reasons:responseUnavailableReasonsFor(state,side,incoming,id,idx,legal)});});
    return items;
  }
  function showUnavailableResponseReason(idx){
    var rw=appState&&appState.responseWindow,item=rw&&rw.unavailable_response_cards&&rw.unavailable_response_cards[idx];if(!item)return false;
    var c=card(item.card_id),reasons=item.reasons||['This response is not currently legal.'];
    showInfoHtml('Response Unavailable — '+cardName(c),'<p class="response-unavailable-intro">This card stays visible so you can verify why it cannot be selected.</p><ul class="response-reason-list">'+reasons.map(function(r){return'<li>'+esc(r)+'</li>';}).join('')+'</ul>');return true;
  }
  function responseIncomingFromWindow(rw){
    return {damage:Number(rw&&rw.damage||0),damage_type:rw&&rw.damage_type,attack_type:(rw&&rw.attack_type)||'Single Target',cannot_dodge:!!(rw&&rw.cannot_dodge),cannot_block:!!(rw&&rw.cannot_block),card_id:rw&&rw.card_id,source_side:rw&&rw.source_side,source_lane:rw&&rw.source_lane,target_side:rw&&rw.target_side,target_lane:rw&&rw.target_lane,affected_lanes:rw&&rw.affected_lanes||null,casting:!!(rw&&rw.casting),area:!!(rw&&rw.area)};
  }
  function responseOptionKey(option){
    if(!option)return '';
    return [option.card_id||'',option.hand_index==null?'':option.hand_index,option.source_lane||'',option.response_kind||'',option.racial_ability||'',option.legacy_instance_id||''].join('|');
  }
  function refreshResponseWindowOptions(state,rw){
    if(!state||!rw||rw.kind!=='incoming_attack')return rw&&rw.options||[];
    var selectedKey=(typeof rw.selected==='number'&&rw.options&&rw.options[rw.selected])?responseOptionKey(rw.options[rw.selected]):'';
    rw.options=responseOptionsFor(state,rw.response_owner||rw.target_side,responseIncomingFromWindow(rw));
    rw.selected=selectedKey?rw.options.findIndex(function(o){return responseOptionKey(o)===selectedKey;}):null;
    if(rw.selected<0)rw.selected=null;
    return rw.options;
  }
  function reactiveCancelOptionsFor(state, incomingFamily, responderSide, sourceSide){
    responderSide=responderSide||'PLAYER'; if(sourceSide && sourceSide===responderSide) return [];
    var hand=sideHand(state,responderSide)||[], options=[], manaKey=manaPoolKeyForSide(responderSide);
    hand.forEach(function(id,idx){ var rc=card(id), source=null, cost=Number(cardCost(rc)||0); if(cost>Number(state[manaKey]||0)) return;
      if(incomingFamily==='Event' && isInterceptCard(rc)) options.push({card_id:id, hand_index:idx, card:rc, response_kind:'cancel_card'});
      if(incomingFamily==='Item' && isFlashpowderBombCard(rc)) options.push({card_id:id, hand_index:idx, card:rc, response_kind:'cancel_card'});
      if((incomingFamily==='DefendSkill'||incomingFamily==='DodgeDefenseSkill') && isBindingLightCard(rc)){
        LANE_ORDER.forEach(function(lane){var h=sideHeroes(state,responderSide)[lane];if(sourceCompatibleForResponse(rc,h)&&responseCostForSource(rc,h,state,responderSide)<=Number(state[manaKey]||0))options.push({card_id:id,hand_index:idx,card:rc,source_lane:lane,response_kind:'cancel_defend_skill'});});
      }
    }); return options;
  }
  function canOpenReactiveCancelWindow(state, c, responderSide, sourceSide){
    var fam=cardFamily(c); if(fam!=='Event' && fam!=='Item') return false;
    return sourceSide!==responderSide && reactiveCancelOptionsFor(state,fam,responderSide||'PLAYER',sourceSide).length>0;
  }
  function openReactiveCancelWindow(state, action, cost, responderSide){
    responderSide=responderSide||'PLAYER';
    var c=card(action.card_id), fam=cardFamily(c), sourceSide=action.side||action.source_side||oppositeSide(responderSide), incomingEvt=null; if(sourceSide===responderSide){ pushLog(state,'Reactive cancel rejected: '+cardName(c)+' belongs to the same side.'); return false; }
    if(sourceSide==='AI'){
      incomingEvt=recordOpponentAction(state,fam.toUpperCase(),action.card_id,cardName(c),cardName(c)+' is awaiting a response.',{
        action_line:'Action: AI played '+cardName(c)+'.', source_side:'AI', source_lane:action.source_lane||null,
        target_side:action.target_side||null, target_lane:action.target_lane||null,
        response_lines:['PLAYER Response Window opened.'], result_lines:['Awaiting response before the card resolves.']
      });
    }
    state.responseWindow={kind:'incoming_card', response_owner:responderSide, card_id:action.card_id, source_side:sourceSide, source_lane:action.source_lane||null, target_side:responderSide, target_lane:action.target_lane||null, incoming_family:fam, original_cost:Number(cost||0), hand_index:action.hand_index, action:clone(action), options:reactiveCancelOptionsFor(state,fam,responderSide,sourceSide), selected:null, commit_token:action.commit_token, opponent_event_id:incomingEvt&&incomingEvt.id};
    state.pending={type:'response_window', card_id:action.card_id};
    pushLog(state,responderSide+' may respond to '+cardName(c)+' before it resolves.');
  }
  function openNestedResponseCancelWindow(state,parentWindow,responseOption,incomingFamily){
    var incoming=card(responseOption.card_id),sourceSide=parentWindow.target_side||parentWindow.response_owner,responderSide=oppositeSide(sourceSide),family=incomingFamily||'Event',opts=reactiveCancelOptionsFor(state,family,responderSide,sourceSide);
    if(!opts.length)return false;
    state.responseWindow={kind:'incoming_card',nested_response:true,parent_response_window:clone(parentWindow),pending_response_option:clone(responseOption),response_owner:responderSide,card_id:responseOption.card_id,source_side:sourceSide,source_lane:responseOption.source_lane||parentWindow.target_lane,target_side:responderSide,target_lane:parentWindow.source_lane,incoming_family:family,original_cost:Number(cardCost(incoming)||0),hand_index:responseOption.hand_index,options:opts,selected:null,commit_token:parentWindow.commit_token||null};
    state.pending={type:'response_window',card_id:responseOption.card_id,response_owner:responderSide,decision_side:responderSide};
    pushLog(state,'Nested Response Window opens: '+responderSide+' may counter '+cardName(incoming)+'.');closeResponseWindowUI();
    if(responderSide==='AI'&&!state.pvpHumanVsHuman){ var aiChoice=opts[0]||null; return resolveReactiveCancelWindow(aiChoice); }
    renderResponseWindow();return true;
  }
  function resolveReactiveCancelWindow(responseOption){
    if(!appState || !appState.responseWindow || appState.responseWindow.kind!=='incoming_card') return false;
    var rw=appState.responseWindow, incoming=card(rw.card_id);
    if(rw.nested_response){
      var parent=clone(rw.parent_response_window),pendingOpt=clone(rw.pending_response_option||{}),sourceSide=rw.source_side||parent.target_side;
      if(responseOption){
        var rc=card(responseOption.card_id),owner=rw.response_owner||parent.source_side,responseSource=responseOption.source_lane?sideHeroes(appState,owner)[responseOption.source_lane]:null,cost=responseCostForSource(rc,responseSource,appState,owner),manaKey=manaPoolKeyForSide(owner);
        if(cost>Number(appState[manaKey]||0)){showInfo('Cannot Respond','Not enough Mana for '+cardName(rc)+'.');return false;}
        appState[manaKey]=Math.max(0,Number(appState[manaKey]||0)-cost);var ch=sideHand(appState,owner),ci=responseOption.hand_index;if(ch[ci]!==responseOption.card_id)ci=ch.indexOf(responseOption.card_id);if(ci>=0){ch.splice(ci,1);sideDiscard(appState,owner).push(responseOption.card_id);}
        var responseCard=card(pendingOpt.card_id),responseManaKey=manaPoolKeyForSide(sourceSide),pendingSource=pendingOpt.source_lane?sideHeroes(appState,sourceSide)[pendingOpt.source_lane]:null,responseCost=responseCostForSource(responseCard,pendingSource,appState,sourceSide);if(responseCost>Number(appState[responseManaKey]||0)){showInfo('Cannot Resolve','Pending response no longer has enough Mana.');return false;}
        appState[responseManaKey]=Math.max(0,Number(appState[responseManaKey]||0)-responseCost);var rh=sideHand(appState,sourceSide),ri=pendingOpt.hand_index;if(rh[ri]!==pendingOpt.card_id)ri=rh.indexOf(pendingOpt.card_id);if(ri>=0){rh.splice(ri,1);sideDiscard(appState,sourceSide).push(pendingOpt.card_id);}
        var nestedReactionLine=owner+' uses '+cardName(rc)+' to cancel '+cardName(responseCard)+'.';pushLog(appState,nestedReactionLine+' The original attack continues.');
        if(appState.pvpHumanVsHuman)recordPvpSideAction(appState,owner,'REACTION',responseOption.card_id,cardName(rc),nestedReactionLine,{action_line:'Action: '+owner+' played '+cardName(rc)+' as Reaction.',source_side:owner,source_lane:responseOption.source_lane||null,target_side:sourceSide,target_lane:pendingOpt.source_lane||null,response_lines:[nestedReactionLine],result_lines:[cardName(responseCard)+' was canceled. Original attack continues.']});
        appState.responseWindow=parent;appState.pending={type:'response_window',card_id:parent.card_id,response_owner:parent.response_owner};closeResponseWindowUI();return resolveResponseWindow(null);
      }
      pushLog(appState,(rw.response_owner||'PLAYER')+' chose No Response to '+cardName(incoming)+'. The committed response continues.');pendingOpt.nestedReactiveChecked=true;appState.responseWindow=parent;appState.pending={type:'response_window',card_id:parent.card_id,response_owner:parent.response_owner};closeResponseWindowUI();return resolveResponseWindow(pendingOpt);
    }
    if(responseOption){
      var rc=card(responseOption.card_id), owner=rw.response_owner||'PLAYER', reactionSource=responseOption.source_lane?sideHeroes(appState,owner)[responseOption.source_lane]:null, cost=responseCostForSource(rc,reactionSource,appState,owner), manaKey=manaPoolKeyForSide(owner);
      if(cost>Number(appState[manaKey]||0)){ showInfo('Cannot Respond','Not enough Mana for '+cardName(rc)+'.'); return false; }
      appState[manaKey]=Math.max(0,Number(appState[manaKey]||0)-cost);
      var ph=sideHand(appState,owner), pi=responseOption.hand_index; if(ph[pi]!==responseOption.card_id) pi=ph.indexOf(responseOption.card_id);
      if(pi>=0){ ph.splice(pi,1); sideDiscard(appState,owner).push(responseOption.card_id); }
      var srcSide=rw.source_side||'AI', srcHand=sideHand(appState,srcSide), idx=rw.hand_index; if(srcHand[idx]!==rw.card_id) idx=srcHand.indexOf(rw.card_id);
      appState[manaPoolKeyForSide(srcSide)]=Math.max(0,Number(appState[manaPoolKeyForSide(srcSide)]||0)-Number(rw.original_cost||0));
      var srcHero=(rw.source_lane?sideHeroes(appState,srcSide)[rw.source_lane]:null);
      if(srcHero && sourceShouldExhaust(incoming,srcHero)){ srcHero.exhausted=true; srcHero.exhaust_reason='Played '+cardName(incoming)+'; canceled by response.'; }
      if(idx>=0){ srcHand.splice(idx,1); finalizeCommittedSourceCard(appState,{commit_token:rw.commit_token||(rw.action&&rw.action.commit_token)},srcSide,rw.card_id,'discard',{reason:'canceled by '+cardName(rc)}); }
      var reactionLine=owner+' responds with '+cardName(rc)+' and cancels '+cardName(incoming)+'.';
      pushLog(appState,reactionLine+' Costs/source Exhaust are paid, but the canceled card has no effect and is discarded.');
      if(appState.pvpHumanVsHuman){
        recordPvpSideAction(appState,owner,'REACTION',responseOption.card_id,cardName(rc),reactionLine,{action_line:'Action: '+owner+' played '+cardName(rc)+' as Reaction.',source_side:owner,source_lane:responseOption.source_lane||null,target_side:srcSide,target_lane:rw.source_lane||null,response_lines:[reactionLine],result_lines:[cardName(incoming)+' was canceled and discarded.']});
      } else if(owner==='AI') recordOpponentAction(appState,'REACTION',responseOption.card_id,cardName(rc),reactionLine,{action_line:'Action: AI played '+cardName(rc)+' as Reaction.',target_side:srcSide,target_lane:rw.source_lane||null,response_lines:[reactionLine],result_lines:[cardName(incoming)+' was canceled and discarded.']});
      var incomingEvt=findOpponentEventById(appState,rw.opponent_event_id);
      if(incomingEvt) appendOpponentEventResponse(appState,incomingEvt,[owner+' used '+cardName(rc)+' to cancel '+cardName(incoming)+'.'],[cardName(incoming)+' was canceled by '+cardName(rc)+'.','AI paid '+Number(rw.original_cost||0)+' Mana; the canceled card had no effect and moved to Discard Pile.']);
      else appendOpponentEventResult(appState,rw.card_id,[owner+' used '+cardName(rc)+'.', cardName(incoming)+' was canceled and discarded.']);
      appState.responseWindow=null; appState.pending=null; closeResponseWindowUI(); syncCounts(appState); if(resumeAITurnAfterPlayerResponse(appState)) return true; render(); return true;
    }
    pushLog(appState,(rw.response_owner||'PLAYER')+' chose No Response. '+cardName(incoming)+' continues to resolve.');
    appState.responseWindow=null; appState.pending=null; closeResponseWindowUI();
    if(rw.opponent_event_id) removeOpponentEventById(appState,rw.opponent_event_id);
    var action=clone(rw.action||{}); action.reactiveChecked=true; action.side=rw.source_side||'AI';
    var ok=commitPlayedCard(appState,action); if(resumeAITurnAfterPlayerResponse(appState)) return true; syncCounts(appState); render(); return ok;
  }
  function heroMiniContext(title, side, lane){
    var h=appState && sideHeroes(appState,side)[lane]; var id=heroIdFrom(h); var c=card(id);
    if(!h) return '<article class="response-context-card"><h3>'+esc(title)+'</h3><div class="empty-state compact">No Hero context.</div></article>';
    return '<article class="response-context-card"><h3>'+esc(title)+'</h3><button class="response-context-tile hero-context" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(c))+'"><span><strong>'+esc(cardName(c))+'</strong><small>'+esc(side)+' · '+esc(lane)+'</small><small>'+esc(heroIdentity(c))+'</small><small>HP '+esc(h.hp)+' / '+esc(h.maxHp)+'</small></span></button></article>';
  }
  function incomingCardContext(rw){
    var c=card(rw.card_id); var source=sideHeroes(appState,rw.source_side)[rw.source_lane];
    return '<article class="response-context-card"><h3>Incoming Card</h3><button class="response-context-tile" type="button" data-preview="'+esc(rw.card_id)+'"><img src="'+esc(thumbFor(rw.card_id))+'" alt="'+esc(cardName(c))+'"><span><strong>'+esc(cardName(c))+'</strong><small>'+esc(cardFamily(c))+' · '+esc(cardSubtype(c)||'Attack')+'</small><small>'+esc(currentDamageType(c))+' · '+esc(rw.damage)+' damage</small></span></button><div class="incoming-summary">'+esc(activeEffectSummary(c,source))+'</div></article>';
  }
  function resolvePostAttackChoices(state, action, post, result, options){
    options=options||{};
    var legacyPending=(state&&state.pending&&state.pending.type==='legacy_defeat_choice')?state.pending:null;
    post=post||{};
    if(post.deferred_discard_card_id){ action.deferred_discard_card_id=post.deferred_discard_card_id; action.deferred_discard_side=post.deferred_discard_side||action.source_side; }
    // Card-specific continuation semantics: source-swap cards use connected resolution;
    // Soul Blast Shot uses printed "when this attack hits" and therefore allows Block/Prevent to 0.
    var soulPrintedHit=isSoulBlastShotCard(card(action.card_id)) && printedAttackHitCount(result)>0;
    var sourceSwapRequested=!!post.optional_swap_after;
    var targetSwapRequested=!!soulPrintedHit;
    if(result && (result.negated || result.canceled || result.dodged || (result.prevented && !soulPrintedHit))){
      if(post.defer_reposition_discard || post.deferred_discard_card_id) finalizeDeferredRepositionDiscard(state,post,action.source_side,action.card_id);
      if(legacyPending){ renderLegacyDefeatChoice(); return true; }
      clearPendingAfterResolvedAction(state);
      return false;
    }
    // Defeat and Legacy replacement are mandatory state-based actions. Reposition is evaluated only
    // after the final queued Legacy choice, using the updated board state.
    if(legacyPending && !options.after_legacy){
      if(sourceSwapRequested || targetSwapRequested){
        legacyPending.after_legacy_reposition={action:clone(action),post:clone(post),result:clone(result||{}),kind:sourceSwapRequested?'source_swap':'target_swap'};
        pushLog(state,cardName(card(action.card_id))+' reposition waits until Legacy replacement is complete.');
      } else if(post.defer_reposition_discard || post.deferred_discard_card_id){
        finalizeDeferredRepositionDiscard(state,post,action.source_side,action.card_id);
      }
      renderLegacyDefeatChoice();
      return true;
    }
    clearPendingAfterResolvedAction(state);
    if(sourceSwapRequested){
      var sourcePrompt=promptOptionalSwap(state, action, null);
      if(sourcePrompt===true) return true;
      if(sourcePrompt==='AUTO_RESOLVED') return false;
    }
    if(targetSwapRequested){
      var targetPrompt=promptOptionalTargetSwap(state, action, null);
      if(targetPrompt===true) return true;
      if(targetPrompt==='AUTO_RESOLVED') return false;
    }
    // No legal reposition option (including same-column source/front no-op): finish deferred discard only when this card was held for continuation.
    if(post.defer_reposition_discard || post.deferred_discard_card_id) finalizeDeferredRepositionDiscard(state,post,action.source_side,action.card_id);
    clearPendingAfterResolvedAction(state);
    return false;
  }
  function chooseAIDefenseResponseOption(state,rw){
    var options=(rw&&rw.options)||[]; if(!options.length) return null;
    var target=rw&&sideHeroes(state,'AI')[rw.target_lane], incomingDamage=Number(rw&&rw.damage||0);
    function score(opt){
      var rc=card(opt.card_id), incoming={damage_type:rw.damage_type,attack_type:rw.attack_type,cannot_dodge:rw.cannot_dodge,cannot_block:rw.cannot_block,target_lane:rw.target_lane,affected_lanes:rw.affected_lanes||null};
      var kind=opt.response_kind==='dragon_scale'?'block':(opt.response_kind==='legacy_reduce'?'block':responseKind(rc,incoming));
      var value=0;
      if(kind==='dodge'||kind==='negate_return'||kind==='immunity') value=incomingDamage+80;
      else if(kind==='area_reduce') value=defensiveFormationReduction(state,'AI');
      else if(opt.response_kind==='dragon_scale'||opt.response_kind==='legacy_reduce') value=Number(opt.block||40);
      else if(kind==='block') value=responseBlockAmount(rc,incoming,target);
      if(target && incomingDamage>=Number(target.hp||0)) value+=100;
      value-=Number(cardCost(rc)||0)*2; return value;
    }
    return options.slice().sort(function(a,b){return score(b)-score(a);})[0]||null;
  }
  function autoResolveCurrentAIResponseWindow(state){
    if(!state || !state.responseWindow || state.responseWindow.target_side!=='AI' || state.pvpHumanVsHuman) return false;
    var chosen=chooseAIDefenseResponseOption(state,state.responseWindow);
    pushLog(state, chosen?'AI commits '+cardName(card(chosen.card_id))+' in its Defense response window.':'AI takes the hit / uses no Defense response.');
    return resolveResponseWindow(chosen);
  }
  function resolveAttackWithOwnerGating(state, action, post){
    openAttackResponseWindow(state,action,post);
    if(action.target_side==='AI' && !state.pvpHumanVsHuman){ autoResolveCurrentAIResponseWindow(state); return 'AI_RESPONSE_RESOLVED'; }
    return state.pvpHumanVsHuman?'PVP_RESPONSE_WINDOW':'PLAYER_RESPONSE_WINDOW';
  }
  function openAttackResponseWindow(state, action, payload){
    var c=card(action.card_id); var source=sideHeroes(state,action.source_side)[action.source_lane]; var target=sideHeroes(state,action.target_side)[action.target_lane];
    if(target && isLegacyModeHero(target)){ pushLog(state,'Attack target is Legacy Mode and is not a legal attack target. '+cardName(c)+' is canceled by target validator.'); state.pending=null; state.responseWindow=null; checkGameEnd(state); return; }
    var incoming={damage:Number(payload.damage||0), damage_type:damageTypeForActionSource(c,source), attack_type:attackType(c)||'Single Target', cannot_dodge:attackCannotBeDodged(c), cannot_block:attackCannotBeBlocked(c), card_id:action.card_id, source_side:action.source_side, source_lane:action.source_lane, target_side:action.target_side, target_lane:action.target_lane};
    state.responseWindow={kind:'incoming_attack', response_window_token:'RW:'+String(action.card_id||'')+':'+String(action.source_side||'')+':'+String(action.source_lane||'')+':'+String(action.target_side||'')+':'+String(action.target_lane||'')+':'+Date.now(), response_owner:action.target_side, card_id:action.card_id, source_side:action.source_side, source_lane:action.source_lane, target_side:action.target_side, target_lane:action.target_lane, damage:incoming.damage, damage_type:incoming.damage_type, attack_type:incoming.attack_type, cannot_dodge:incoming.cannot_dodge, cannot_block:incoming.cannot_block, options:responseOptionsFor(state, action.target_side, incoming), selected:null, post:payload, action:clone(action)};
    if(payload && payload.return_to_source_hand_after_double_casting) state.responseWindow.return_to_source_hand=true;
    state.pending={type:'response_window', card_id:action.card_id};
    pushLog(state,'Response Window opens for '+cardName(c)+' targeting '+cardName(card(target.card_id))+'.');
  }
  function finalizePlayedCardAfterResponse(state, rw){
    var cardId=rw.card_id, c=card(cardId), post=rw.post||{}, action=rw.action||{}; var sourceHero=sideHeroes(state,rw.source_side)[rw.source_lane];
    var holdKey=action.visual_hold_key||post.visual_hold_key||rw.visual_hold_key||null;
    if(post.draw_counter_casting_release){
      releaseLocalAttachmentAt(state,post.draw_counter_side||rw.source_side,post.draw_counter_lane||rw.source_lane,post.draw_counter_slot,cardId,'draw-counter casting resolved');
      state.pendingCastings=(state.pendingCastings||[]).filter(function(pc){return !(pc&&pc.card_id===cardId&&pc.side===(post.draw_counter_side||rw.source_side)&&pc.source_lane===(post.draw_counter_lane||rw.source_lane)&&Number(pc.attachmentSlot)===Number(post.draw_counter_slot));});
      return;
    }
    var persistentResolved=!!post.persistent && !rw.negated && !rw.canceled;
    if(persistentResolved){
      var policy=attachmentPolicyForCard(c,sourceHero)||{tick_phase:isShieldBashCard(c)?'DRAW_PHASE_START':'END_PHASE',remaining_count:1,counter_mode:'countdown',role:'ongoing'};
      registerLocalAttachment(state,rw.source_side,rw.source_lane,post.attachmentSlot,cardId,policy,action);
      releaseHeldCardMotion(holdKey,{type:'attachment',side:rw.source_side,lane:rw.source_lane,slot:post.attachmentSlot});
      pushLog(state, cardName(c)+' remains in '+cardName(card(sourceHero.card_id))+' Attachment Slot until its effect expires.');
    } else {
      if(rw.return_to_source_hand){ finalizeCommittedSourceCard(state,action,rw.source_side,cardId,'hand',{reason:'response return'}); releaseHeldCardMotion(holdKey,{type:'hand',side:rw.source_side}); pushLog(state, cardName(c)+' returns to its owner hand.'); }
      else if(post.defer_reposition_discard){
        post.deferred_discard_card_id=cardId; post.deferred_discard_side=rw.source_side; post.visual_hold_key=holdKey; post.commit_token=action.commit_token||post.commit_token;
        pushLog(state, cardName(c)+' remains pending until its optional reposition decision is completed.');
      } else { finalizeCommittedSourceCard(state,action,rw.source_side,cardId,'discard',{reason:rw.canceled||rw.negated?'response cancel/negate':'resolved'}); releaseHeldCardMotion(holdKey,{type:'discard',side:rw.source_side}); }
    }
  }
  function applyResponseSideEffects(rc, sourceHero, defenderHero){
    if(!rc || !Array.isArray(rc.effect)) return [];
    var out=[];
    rc.effect.forEach(function(e){
      if(e.kind==='self_status'){ var name=String(e.raw_value||e.status||'').trim(); if(name){ addStatus(defenderHero,name,1,name+' from '+cardName(rc)); out.push('self '+name); } }
      if(e.kind==='inflict_status' && e.target==='attacking_hero'){ var r=applyStatusEffectToHero(e,sourceHero,defenderHero,'response effect',rc); if(r) out.push('attacker '+r); }
    });
    return out;
  }
  function activeHeroCountAfterDefeat(state, side, defeatedLane){
    var heroes=sideHeroes(state,side)||{};
    return LANE_ORDER.filter(function(l){
      var u=heroes[l];
      if(l===defeatedLane) return false;
      return isActiveHeroSlot(u);
    }).length;
  }
  function clearDefeatedHeroExp(state, side, hero){
    if(!state || !hero) return [];
    var expCards=Array.isArray(hero.exp_cards)?hero.exp_cards.slice():[];
    if(expCards.length){
      var discard=sideDiscard(state,side);
      expCards.forEach(function(id){ if(id) discard.push(id); });
    }
    hero.exp_cards=[];
    hero.exp_total=inherentRankExp(hero);
    return expCards;
  }
  function clearConfirmedDefeatConnectedState(state,side,lane,hero){
    if(!state||!hero)return {casting_cards:[],statuses_cleared:0,exp_cards:[]};
    var pending=(state.pendingCastings||[]).filter(function(pc){
      if(!pc||pc.side!==side)return false;
      if(pc.source_instance_id&&hero.instance_id&&String(pc.source_instance_id)===String(hero.instance_id))return true;
      if(castingSourceMatchesHero(pc,hero))return true;
      return !pc.source_instance_id&&!pc.source_progression_id&&(pc.original_source_lane||pc.source_lane)===lane&&(!pc.source_hero_card_id||String(pc.source_hero_card_id)===String(heroIdFrom(hero)));
    });
    var before={};pending.forEach(function(pc){var id=pc.card_id;if(!id)return;before[id]=(sideDiscard(state,side)||[]).filter(function(x){return x===id;}).length;});
    cleanupDefeatedHeroAttachments(state,side,lane);
    if(pending.length){
      state.pendingCastings=(state.pendingCastings||[]).filter(function(pc){return pending.indexOf(pc)===-1;});
      var counts={};pending.forEach(function(pc){if(pc.card_id)counts[pc.card_id]=Number(counts[pc.card_id]||0)+1;recordCastingExitEvent(state,pc,'CANCELED','Source Hero was defeated before Casting release.');});
      Object.keys(counts).forEach(function(id){var discard=sideDiscard(state,side),target=Number(before[id]||0)+Number(counts[id]||0);while(discard.filter(function(x){return x===id;}).length<target)discard.push(id);});
    }
    var statuses=(hero.statuses||[]).length;
    hero.statuses=[];hero.attachments=[null,null];hero.casting=false;hero.pending_casting=false;
    var exp=clearDefeatedHeroExp(state,side,hero);
    return {casting_cards:pending.map(function(pc){return pc.card_id;}).filter(Boolean),statuses_cleared:statuses,exp_cards:exp};
  }
  function applyDefeatCheck(state, side, lane){
    var h=sideHeroes(state,side)[lane];
    if(!h || Number(h.hp||0)>0) return false;
    if(isLegacyModeHero(h)){ h.hp=0; checkGameEnd(state); return false; }
    if(openStonebloodChoice(state,side,lane,h)) return true;
    // Confirmed defeat clears every card/state connected to the defeated Hero after
    // Stoneblood/replacement checks: EXP, Attachments (including Casting), statuses, and
    // the external pending Casting queue. Revive must never resurrect a pre-defeat effect.
    clearConfirmedDefeatConnectedState(state,side,lane,h);
    if(activeHeroCountAfterDefeat(state,side,lane)<=0){ h.hp=0; state.pending=null; state.responseWindow=null; checkGameEnd(state); return false; }
    var candidates=eligibleLegacyCardIdsForDefeatedHero(state,side,lane,h);
    if((side==='PLAYER' || state.pvpHumanVsHuman) && candidates.length){ startLegacyDefeatChoice(state,side,lane,h,candidates); return true; }
    if((side==='PLAYER' || state.pvpHumanVsHuman) && !candidates.length){ h.hp=0; h.exhausted=false; pushLog(state,'Defeat check: '+cardName(card(h.card_id))+' has no available Legacy replacement. Checking lose condition.'); state.pending=null; state.responseWindow=null; checkGameEnd(state); syncCounts(state); return false; }
    var transitioned=transitionDefeatedHeroToLegacyMode(state, side, lane, candidates[0]||null, h); checkGameEnd(state); return transitioned;
  }
  function eligibleLegacyCardIdsForDefeatedHero(state, side, lane, defeatedHero){
    var legacy=(side==='PLAYER'?state.playerLegacy:state.aiLegacy)||[], out=[];
    function addAvailable(id){ if(id && legacy.indexOf(id)!==-1 && out.indexOf(id)===-1) out.push(id); }
    if(!defeatedHero) return out;

    var slots=(side==='PLAYER'?state.playerLegacyPackageSlots:state.aiLegacyPackageSlots)||[];
    var assigned=defeatedHero.assigned_legacy_card_id||null;
    var packageId=defeatedHero.legacy_package_id||null;
    var heroId=defeatedHero.card_id;
    var heroLineage=heroLineageId(defeatedHero);

    // The exact package/Legacy bound to this Hero instance is authoritative.
    addAvailable(assigned);

    var exactSlots=slots.filter(function(x){
      if(!x) return false;
      if(packageId && x.package_id===packageId) return true;
      if(assigned && x.legacy===assigned) return true;
      if(packageSlotMatchesHero(x,heroId)) return true;
      var progressionCard=x.progression?card(x.progression):null;
      var progressionLineage=progressionCard?heroLineageId({card_id:x.progression}):'';
      return !!(heroLineage && progressionLineage && heroLineage===progressionLineage);
    });
    exactSlots.forEach(function(x){ addAvailable(x.legacy); });

    // Lane is a recovery key only. Reposition swaps the package slot together with the Hero,
    // so this remains exact without crossing into another same-base-class package.
    if(!out.length){
      var laneSlot=slots.find(function(x){ return x && x.slot===lane; });
      if(laneSlot) addAvailable(laneSlot.legacy);
    }

    // Old/custom imports may omit package metadata. Base-family fallback is allowed only when
    // exactly one available Legacy is unambiguous. Two Ranger packages must never steal each
    // other's Legacy replacement.
    if(!out.length && !assigned && !packageId && !exactSlots.length){
      var defeatedBase=baseFamilyForHero(defeatedHero);
      var baseCandidates=legacy.filter(function(id){
        var c=card(id);
        return c && c.identity_mode==='Legacy Mode Definition' && c.eligibility && c.eligibility.base_class_family===defeatedBase;
      });
      if(baseCandidates.length===1) addAvailable(baseCandidates[0]);
    }
    return out;
  }
  function reconcileStrandedDefeatedHeroes(state){
    if(!state || state.gameOver || state.pending || state.responseWindow || state._legacyDefeatReconcile) return false;
    state._legacyDefeatReconcile=true;
    try{
      var sides=['PLAYER','AI'];
      for(var si=0;si<sides.length;si++){
        var side=sides[si], heroes=sideHeroes(state,side)||{};
        for(var li=0;li<LANE_ORDER.length;li++){
          var lane=LANE_ORDER[li], h=heroes[lane];
          if(isHeroSlot(h) && Number(h.hp||0)<=0){
            var handled=applyDefeatCheck(state,side,lane);
            if(handled || state.pending || state.gameOver) return true;
          }
        }
      }
      return false;
    } finally {
      state._legacyDefeatReconcile=false;
    }
  }
  function renderLegacyDefeatChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='legacy_defeat_choice' || SUPPRESS_RENDER || !$('choiceOverlay')) return;
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    var p=appState.pending, ids=p.candidates||[];
    $('choiceTitle').textContent='Choose Legacy'; $('choiceConfirm').style.display=''; $('choiceConfirm').textContent=p.committing?'Entering Legacy Mode…':'Enter Legacy Mode'; $('choiceConfirm').disabled=p.committing||typeof p.selected_index!=='number';
    $('choiceBody').innerHTML='<p class="choice-instruction">'+esc(cardName(card(p.defeated_card_id))+' was defeated in '+p.side+' '+p.lane+'. Choose which Legacy card will replace this Hero.')+'</p><div class="choice-grid card-search-choice-grid">'+ids.map(function(id,idx){var selected=p.selected_index===idx;return '<article class="card-search-choice '+(selected?'selected':'')+'"><button class="discard-preview" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(card(id)))+' thumbnail"><span>'+esc(cardName(card(id)))+'</span></button><button class="discard-select" type="button" data-legacy-defeat-choice="'+idx+'">'+(selected?'Selected':'Select')+'</button></article>';}).join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function startLegacyDefeatChoice(state, side, lane, defeatedHero, candidates){
    if(!(side==='PLAYER' || state.pvpHumanVsHuman) || !candidates || !candidates.length) return false;
    if(isLegacyModeHero(sideHeroes(state,side)[lane])) return true;
    state.legacyDefeatSequence=Number(state.legacyDefeatSequence||0)+1;
    var choiceId='LEGACY:'+side+':'+lane+':'+defeatedHero.card_id+':'+Number(state.round||0)+':'+state.legacyDefeatSequence;
    var pending={type:'legacy_defeat_choice',side:side,decision_side:side,lane:lane,defeated_snapshot:clone(defeatedHero),defeated_card_id:defeatedHero.card_id,candidates:candidates.slice(),selected_index:null,choice_id:choiceId,committing:false};
    var opened=window.GL_RUNTIME_AUTHORITY.openMandatoryChoice(state,pending);
    if(!opened.opened){
      if(opened.same) return true;
      state.legacyDefeatQueue=state.legacyDefeatQueue||[];
      if(!state.legacyDefeatQueue.some(function(x){return x.side===side&&x.lane===lane&&x.defeated_card_id===defeatedHero.card_id;})) state.legacyDefeatQueue.push(pending);
      return true;
    }
    pushLog(state, side+' must choose Legacy card for defeated '+cardName(card(defeatedHero.card_id))+' in '+lane+'.');
    syncCounts(state); renderLegacyDefeatChoice(); return true;
  }
  function selectLegacyDefeatChoice(idx){
    if(!appState || !appState.pending || appState.pending.type!=='legacy_defeat_choice') return false;
    if(idx<0 || idx>=(appState.pending.candidates||[]).length) return false;
    appState.pending.selected_index=idx; renderLegacyDefeatChoice(); return true;
  }
  function activateNextLegacyDefeatChoice(state){
    if(!state || state.pending) return false;
    var queue=state.legacyDefeatQueue||[];
    while(queue.length){
      var next=queue.shift(), h=sideHeroes(state,next.side)[next.lane];
      if(h && !isLegacyModeHero(h) && Number(h.hp||0)<=0){
        // A choice queued behind a Response Window has not yet been registered as OPEN.
        // Re-open it through Runtime Authority so the selected Legacy can commit atomically.
        var opened=window.GL_RUNTIME_AUTHORITY.openMandatoryChoice(state,next);
        if(opened.opened || opened.same){ renderLegacyDefeatChoice(); return true; }
        if(opened.resolved) continue;
      }
    }
    return false;
  }
  function commitLegacyDefeatChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='legacy_defeat_choice') return false;
    var p=appState.pending;
    if(typeof p.selected_index!=='number' || !p.candidates[p.selected_index]){ showInfo('Choice Required','Choose a Legacy card before confirming.'); return false; }
    var guard=window.GL_RUNTIME_AUTHORITY.beginChoiceCommit(appState,p.choice_id);
    if(!guard.ok) return false;
    var legacyId=p.candidates[p.selected_index], legacy=(p.side==='PLAYER'?appState.playerLegacy:appState.aiLegacy)||[];
    if(legacy.indexOf(legacyId)<0){ window.GL_RUNTIME_AUTHORITY.abortChoiceCommit(appState,p.choice_id); showInfo('Choice Changed','That Legacy card is no longer available.'); return false; }
    var snapshot=clone(appState);
    try{
      var ok=transitionDefeatedHeroToLegacyMode(appState,p.side,p.lane,legacyId,p.defeated_snapshot);
      if(!ok) throw new Error('Legacy transition rejected.');
      var afterMulti=p.after_legacy_multi_sequence?clone(p.after_legacy_multi_sequence):null;
      var afterReposition=p.after_legacy_reposition?clone(p.after_legacy_reposition):null;
      var afterDouble=p.after_legacy_double_casting?clone(p.after_legacy_double_casting):null;
      var afterStoneblood=p.after_legacy_stoneblood_response?clone(p.after_legacy_stoneblood_response):null;
      window.GL_RUNTIME_AUTHORITY.finishChoiceCommit(appState,p.choice_id);
      closeChoice();
      checkGameEnd(appState);
      var repositionOpened=false;
      if(!appState.gameOver && activateNextLegacyDefeatChoice(appState)){
        if(afterMulti && appState.pending && appState.pending.type==='legacy_defeat_choice') appState.pending.after_legacy_multi_sequence=afterMulti;
        if(afterReposition && appState.pending && appState.pending.type==='legacy_defeat_choice') appState.pending.after_legacy_reposition=afterReposition;
        if(afterDouble && appState.pending && appState.pending.type==='legacy_defeat_choice') appState.pending.after_legacy_double_casting=afterDouble;
        if(afterStoneblood && appState.pending && appState.pending.type==='legacy_defeat_choice') appState.pending.after_legacy_stoneblood_response=afterStoneblood;
      } else if(!appState.gameOver && afterMulti){
        if(Number(afterMulti.next_index||0)<(afterMulti.affected_lanes||[]).length){
          openMultiTargetResponseWindow(appState,afterMulti.action,afterMulti.post,afterMulti.affected_lanes,afterMulti.next_index,afterMulti.total_damage,afterMulti.poison_vial_applied,afterMulti.connected_hits);
          if(appState.responseWindow && appState.responseWindow.target_side==='AI' && !appState.pvpHumanVsHuman) autoResolveCurrentAIResponseWindow(appState);
        }
      } else if(!appState.gameOver && afterReposition){
        repositionOpened=resolvePostAttackChoices(appState,afterReposition.action,afterReposition.post,afterReposition.result,{after_legacy:true});
      } else if(!appState.gameOver && afterDouble){
        openDoubleCastingSecondTargetSelection(appState,afterDouble);
      } else if(!appState.gameOver && afterStoneblood){
        runtimeStateCheckpoint(appState,'Legacy choice resolved after Stoneblood decline');
        return resumeAfterStonebloodResponse(afterStoneblood);
      }
      runtimeStateCheckpoint(appState,'Legacy choice resolved');
      if(repositionOpened){ syncCounts(appState); render(); return true; }
      // A PLAYER Legacy replacement can be the final mandatory gate inside an AI attack.
      // The response resolver could not resume the AI while this choice was still pending,
      // so resume only after the final queued Legacy choice has committed and no continuation remains.
      if(!appState.gameOver && !appState.pending && !appState.responseWindow && resumeAITurnAfterPlayerResponse(appState)) return true;
      render();
      return true;
    }catch(err){
      restoreStateSnapshot(appState,snapshot); window.GL_RUNTIME_AUTHORITY.abortChoiceCommit(appState,p.choice_id);
      showInfo('Legacy Choice Recovered','The Legacy transition was rolled back safely. '+String(err&&err.message||err)); render(); return false;
    }
  }
  function matchingLegacyCardIdForDefeatedHero(state, side, lane, defeatedHero){
    var candidates=eligibleLegacyCardIdsForDefeatedHero(state,side,lane,defeatedHero);
    return candidates.length?candidates[0]:null;
  }
  function transitionDefeatedHeroToLegacyMode(state, side, lane, chosenLegacyId, defeatedSnapshot){
    var heroes=sideHeroes(state,side); if(isLegacyModeHero(heroes[lane])) return false; var defeated=defeatedSnapshot?clone(defeatedSnapshot):heroes[lane]; if(!defeated) return false;
    var legacyId=chosenLegacyId || matchingLegacyCardIdForDefeatedHero(state, side, lane, defeated);
    clearConfirmedDefeatConnectedState(state,side,lane,heroes[lane]||defeated);
    // Fallback for direct transition callers and older pending snapshots. The stored
    // defeated Hero is sanitized so revive cannot restore any pre-defeat effect.
    clearDefeatedHeroExp(state,side,defeated);
    var oldName=cardName(card(defeated.card_id));
    defeated.statuses=[];defeated.attachments=[null,null];defeated.casting=false;defeated.pending_casting=false;
    if(!legacyId){ defeated.hp=0; pushLog(state,'Defeat check: '+oldName+' was defeated, but no matching Legacy card was found.'); return false; }
    var legacy=side==='PLAYER'?state.playerLegacy:state.aiLegacy; var idx=legacy.indexOf(legacyId); queueLegacyDeckToFieldMotion(side,lane,legacyId); if(idx>=0) legacy.splice(idx,1);
    heroes[lane]=sanitizeLegacySlot({card_id:legacyId, side:side, lane:lane, mode:'LEGACY', legacy_mode:true, defeated_hero_snapshot:clone(defeated), original_hero_card_id:defeated.card_id, active_legacy_card_id:legacyId, assigned_legacy_card_id:defeated.assigned_legacy_card_id||legacyId, legacy_lineage_id:defeated.legacy_lineage_id, legacy_package_id:defeated.legacy_package_id});
    pushLog(state,'Defeat: '+oldName+' is replaced by Legacy Mode '+cardName(card(legacyId))+' in '+side+' '+lane+'. Attachments discarded; EXP stack moved to discard; Legacy Deck count updated.');
    if(side==='AI') recordOpponentAction(state,'LEGACY',legacyId,'AI Legacy Mode','AI '+lane+' entered Legacy Mode.',{action_line:'Action: AI '+lane+' entered Legacy Mode.', result_lines:[oldName+' was defeated.','Replaced by '+cardName(card(legacyId))+'.','Defeat cleanup: statuses cleared; attachments discarded; EXP stack moved to Discard Pile.']});
    return true;
  }
  function restoreHeroFromLegacyMode(state, side, lane, reviveHp, sourceName, forcedExhausted){
    var h=sideHeroes(state,side)[lane]; if(!isLegacyModeHero(h) || !h.defeated_hero_snapshot) return false;
    var legacyId=h.active_legacy_card_id||h.card_id; queueLegacyFieldToDeckMotion(side,lane,legacyId); var legacyDeck=side==='PLAYER'?state.playerLegacy:state.aiLegacy; if(legacyId && legacyDeck.indexOf(legacyId)===-1) legacyDeck.push(legacyId);
    var restored=clone(h.defeated_hero_snapshot); restored.hp=Math.max(1, Number(reviveHp||10)); restored.maxHp=Number(restored.maxHp||card(restored.card_id).hp||restored.hp||100); restored.hp=Math.min(restored.hp,restored.maxHp); restored.statuses=[]; restored.exp_cards=[]; restored.exp_total=inherentRankExp(restored); restored.attachments=[null,null]; restored.casting=false; restored.pending_casting=false; restored.exhausted=forcedExhausted!==false; restored.exhaust_reason=restored.exhausted?('Revived by '+(sourceName||'revive effect')+'.'):null; restored.legacy_mode=false; restored.mode='HERO'; restored.assigned_legacy_card_id=restored.assigned_legacy_card_id||legacyId; restored.legacy_lineage_id=restored.legacy_lineage_id||h.legacy_lineage_id; restored.legacy_package_id=restored.legacy_package_id||h.legacy_package_id;
    sideHeroes(state,side)[lane]=restored; pushLog(state, sourceName+' restores '+cardName(card(restored.card_id))+' from Legacy Mode at '+restored.hp+' HP '+(restored.exhausted?'Exhausted':'Ready')+'; Legacy card returns to Legacy Deck.'); runtimeStateCheckpoint(state,'Hero revived'); return true;
  }
  function applyResolvedAttackDamage(state, cardId, sourceSide, sourceLane, targetSide, targetLane, damage, damageType, dodged, block, deferPoisonVialConsume, connectionPrevented){
    var source=sideHeroes(state,sourceSide)[sourceLane], target=sideHeroes(state,targetSide)[targetLane]; if(!target || Number(target.hp||0)<=0) return {damage:0,hp_damage:0,base_damage:0,burn_damage:0,conditional_damage:0,conditional_incoming_damage:0,statuses_applied:[],skipped:true,dodged:!!dodged,connected:false,prevented:!!connectionPrevented,target_hp_before:0,target_hp_after:0,calculated_final_damage:0,overkill:0};
    var blessingImmune=!!activeAttachmentForSide(state,targetSide,'S1-CLE-025'); if(blessingImmune) connectionPrevented=true;
    var targetHpBefore=Number(target.hp||0),c=card(cardId), formulaLine=(state&&state.responseWindow&&state.responseWindow.post&&state.responseWindow.post.damage_formula_log)||''; if(formulaLine) pushLog(state, formulaLine);
    var baseIncoming=Math.max(0,Number(damage||0)),conditionalIncoming=(!dodged&&!connectionPrevented)?Math.max(0,Number(conditionalAttackBonus(c,target,source)||0)):0;
    var incomingDamage=baseIncoming+conditionalIncoming,passiveBlock=0,activeBlock=0,responseBlock=Math.max(0,Math.min(incomingDamage,Number(block||0)));
    if(!dodged && damageType==='Physical' && (heroClass(target)==='Gladiator' || heroClass(target)==='Conqueror')) passiveBlock+=10;
    if(!dodged && damageType==='Physical' && hasActiveAttachmentForHero(state,target,'S1-WAR-020')) activeBlock+=20;
    var totalReduction=dodged?0:(responseBlock+passiveBlock+activeBlock),remainingReduction=totalReduction;
    var baseFinal=connectionPrevented?0:(dodged?dodgeResidualDamage(c,source):Math.max(0,baseIncoming-remainingReduction)); remainingReduction=Math.max(0,remainingReduction-baseIncoming);
    var conditionalFinal=dodged?0:Math.max(0,conditionalIncoming-remainingReduction),attackDamageFinal=baseFinal+conditionalFinal;
    target.hp=Math.max(0,Number(target.hp||0)-attackDamageFinal);
    pushLog(state,cardName(c)+' Attack Damage resolves for '+attackDamageFinal+' '+damageType+' damage to '+cardName(card(target.card_id))+(conditionalIncoming?' including +'+conditionalIncoming+' conditional damage':'')+(passiveBlock?' after passive block '+passiveBlock:'')+(activeBlock?' after active reduction '+activeBlock:'')+'.');
    if(conditionalIncoming>0) pushLog(state,cardName(c)+' conditional damage was included before Defense / prevention; '+conditionalFinal+' remained after reductions.');
    var attackConnectedForBurn=(!dodged&&!connectionPrevented&&attackDamageFinal>0),burnBonus=burnIncomingDamageBonus(target,attackConnectedForBurn);
    if(burnBonus>0){target.hp=Math.max(0,Number(target.hp||0)-burnBonus);pushLog(state,'Burn adds +'+burnBonus+' damage because the Attack still dealt HP damage after responses and reductions.');}
    var statuses=[],poisonVialApplied=false;if(!dodged){statuses=applyAttackStatuses(c,target,source);if(source&&attackDamageFinal>0&&hasActiveAttachmentForHero(state,source,'S1-ITM-011')&&isPoisonVialEligibleAttack(c)){addStatus(target,'Poison',2,'Poison Vial',{source_card_id:'S1-ITM-011',source_name:'Poison Vial',amount:2});statuses.push('Poison 2 from Poison Vial');poisonVialApplied=true;if(!deferPoisonVialConsume)releaseActiveAttachmentForHero(state,source,'S1-ITM-011','Poison Vial consumed');}if(statuses.length)pushLog(state,'Status result: '+statuses.join(', ')+' on '+cardName(card(target.card_id))+'.');}
    var calculated=attackDamageFinal+burnBonus,targetHpAfter=Number(target.hp||0),actual=Math.max(0,targetHpBefore-targetHpAfter),overkill=Math.max(0,calculated-actual);applyDefeatCheck(state,targetSide,targetLane);var connected=(!dodged&&!connectionPrevented);
    return {damage:calculated,hp_damage:actual,base_damage:baseFinal,burn_damage:burnBonus,conditional_damage:conditionalFinal,conditional_incoming_damage:conditionalIncoming,base_incoming_damage:baseIncoming,incoming_damage:incomingDamage,response_block:responseBlock,passive_block:passiveBlock,active_reduction:activeBlock,statuses_applied:statuses.slice(),poison_vial_applied:poisonVialApplied,skipped:false,dodged:!!dodged,connected:connected,prevented:!!connectionPrevented,blocked_to_zero:!!(connected&&responseBlock>0&&attackDamageFinal===0),target_lane:targetLane,target_hp_before:targetHpBefore,target_hp_after:targetHpAfter,calculated_final_damage:calculated,overkill:overkill};
  }

  function resolveResponseWindow(responseOption){
    if(!appState || !appState.responseWindow) return false;
    if(appState.responseWindow.kind==='incoming_card') return resolveReactiveCancelWindow(responseOption);
    if(appState.responseWindow.kind==='incoming_ability_damage') return resolveAbilityDamageResponseWindow(responseOption);
    var rw=appState.responseWindow, target=sideHeroes(appState,rw.target_side)[rw.target_lane], source=sideHeroes(appState,rw.source_side)[rw.source_lane];
    var block=Number(rw.preapplied_block||0), dodged=false, responseName='No Response', coverUp=false, responseKindChosen=null, stepInRepositionLine='', defenseEvt=null, defenseBefore=null, defenseDisplayTitle='', defenseActionName='';
    if(responseOption){
      var preRc=card(responseOption.card_id);
      if(responseExtraDiscardCount(preRc)>0 && !Number.isInteger(Number(responseOption.selected_extra_discard_index))){
        if(rw.target_side==='AI' && !appState.pvpHumanVsHuman){var auto=responseExtraDiscardCandidates(appState,rw.target_side,responseOption)[0];if(!auto){showInfo('Cannot Respond','Escape Arrow requires 1 other hand card.');return false;}responseOption=clone(responseOption);responseOption.selected_extra_discard_index=auto.hand_index;}
        else return openResponseExtraDiscardChoice(appState,rw,responseOption);
      }
      if(responseOption.card_id==='S1-EVT-009' && !responseOption.nestedReactiveChecked && canOpenReactiveCancelWindow(appState,preRc,rw.source_side)) return openNestedResponseCancelWindow(appState,rw,responseOption);
      var rc=card(responseOption.card_id), incoming={damage_type:rw.damage_type, attack_type:rw.attack_type||'Single Target', cannot_dodge:rw.cannot_dodge, cannot_block:rw.cannot_block, target_lane:rw.target_lane, affected_lanes:rw.affected_lanes||null};
      var kind=responseKind(rc,incoming); responseKindChosen=kind; var cost=0; if(isDefendSkillCard(rc)&&!responseOption.nestedReactiveChecked&&reactiveCancelOptionsFor(appState,'DefendSkill',rw.source_side,rw.target_side).length) return openNestedResponseCancelWindow(appState,rw,responseOption,'DefendSkill');
      if(responseOption.response_kind==='legacy_reduce'){ var lr=resolveLegacyResponseOption(appState,rw,responseOption); if(!lr.ok) return false; block=lr.block; responseName=lr.responseName; responseKindChosen=lr.responseKind; }
      else if(responseOption.response_kind==='dragon_scale'){
        if(responseOption.source_lane!==rw.target_lane){ showInfo('Cannot Respond','Dragon Scale can protect only its own Dragonborn Hero.'); return false; }
        if(Number(appState[racialTokenKey(rw.target_side)]||0)<1){ showInfo('Cannot Respond','Not enough Racial Token for Dragon Scale.'); return false; }
        appState[racialTokenKey(rw.target_side)]=Math.max(0,Number(appState[racialTokenKey(rw.target_side)]||0)-1);markRacialTokenSpent(appState,rw.target_side);
        markRacialAbilityUsedThisTurn(appState, sideHeroes(appState,rw.target_side)[responseOption.source_lane]);
        block=Number(responseOption.block||40); responseName='Dragon Scale blocks '+block+' incoming '+rw.damage_type+' damage'; responseKindChosen='dragon_scale';
      }
      else {
      var responseManaKey=manaPoolKeyForSide(rw.target_side);
      var responseSourceLane=responseOption.source_lane||rw.target_lane;
      var responseSourceHero=sideHeroes(appState,rw.target_side)[responseSourceLane];
      cost=responseCostForSource(rc,responseSourceHero,appState,rw.target_side); var responsePolicy=attachmentPolicyForCard(rc,responseSourceHero);
      var responseSlot=responsePolicy?attachmentSlotIndex(responseSourceHero):-1;
      if(responsePolicy && responseSlot<0){ showInfo('Cannot Respond','Response source Hero has no empty Attachment Slot.'); return false; }
      if(responseExtraDiscardCount(rc)>0){
        var responseHand=sideHand(appState,rw.target_side), responseCardIndex=Number(responseOption.hand_index);
        if(responseHand[responseCardIndex]!==responseOption.card_id) responseCardIndex=responseHand.indexOf(responseOption.card_id);
        var selectedExtra=Number(responseOption.selected_extra_discard_index);
        if(responseCardIndex<0 || !Number.isInteger(selectedExtra) || selectedExtra<0 || selectedExtra>=responseHand.length || selectedExtra===responseCardIndex){ showInfo('Escape Arrow','Choose 1 other card in your hand to discard.'); return false; }
      }
      if(cost>Number(appState[responseManaKey]||0)){ showInfo('Cannot Respond','Not enough Mana for '+cardName(rc)+'.'); return false; }
      appState[responseManaKey]=Math.max(0,Number(appState[responseManaKey]||0)-cost);
      if(kind==='cover_up'){
        var coverLane=responseOption.source_lane, protectedLane=rw.target_lane; var heroes=sideHeroes(appState,rw.target_side);
        if(adjacentLanes(protectedLane).indexOf(coverLane)===-1 || !sourceCompatible(rc,heroes[coverLane])){ showInfo('Cannot Respond','Cover Up requires an adjacent legal Warrior-line Hero and Single Target incoming attack.'); return false; }
        swapBoardStateLanes(appState,rw.target_side,protectedLane,coverLane);
        heroes=sideHeroes(appState,rw.target_side);
        rw.target_lane=protectedLane; target=heroes[protectedLane]; coverUp=true; responseName=cardName(rc)+' swaps into '+protectedLane+' and redirects the attack';
      }
      else if(kind==='dodge'){ dodged=true; responseName=cardName(rc)+' dodges incoming damage'; }
      else if(kind==='negate_return'){ block=Number(rw.damage||0); responseName=cardName(rc)+' negates the attack and returns the attacking card to hand if available'; rw.negated=true; rw.return_to_source_hand=true; }
      else if(kind==='immunity'){ block=Math.max(block,Number(rw.damage||0)); responseName=cardName(rc)+' prevents all attack damage'; if(isBlessingDivinityCard(rc)){ var respSrc=responseOption.source_lane?sideHeroes(appState,rw.target_side)[responseOption.source_lane]:target; if(heroClass(respSrc)==='Crusader') healAllAlliedHeroes(appState,rw.target_side,20,rc,respSrc,'Crusader branch'); } }
      else if(kind==='area_reduce'){ block+=defensiveFormationReduction(appState,rw.target_side); responseName=cardName(rc)+' reduces incoming area damage by '+block; }
      else { block+=responseBlockAmount(rc,incoming,target); responseName=cardName(rc)+' blocks '+block+' damage'; }
      }
      var sideEffects=(responseOption.response_kind==='legacy_reduce'||responseOption.response_kind==='dragon_scale')?[]:applyResponseSideEffects(rc,source,target); if(rw.multi_sequence&&(isDefensiveFormationCard(rc)||isSacredBulwarkCard(rc))){rw.action=rw.action||{};rw.action.team_area_block=Math.max(Number(rw.action.team_area_block||0),Number(block||0));}
      if(responseOption.card_id==='S1-THF-022' && dodged){
        var stepFrom=responseOption.source_lane||rw.target_lane, stepFront=rw.source_lane, stepHeroes=sideHeroes(appState,rw.target_side), stepHero=stepHeroes[stepFrom], frontHero=stepHeroes[stepFront];
        if(stepFrom===stepFront){
          stepInRepositionLine='Step In dodge resolved; the responding Hero was already directly in front of the attacker.';
          responseName+='; '+stepInRepositionLine;
        } else if(isActiveHeroSlot(stepHero) && isActiveHeroSlot(frontHero)){
          var stepName=cardName(card(stepHero.card_id)), frontName=cardName(card(frontHero.card_id));
          swapBoardStateLanes(appState,rw.target_side,stepFrom,stepFront);
          stepInRepositionLine=stepName+' repositioned '+stepFrom+' -> '+stepFront+'; '+frontName+' moved '+stepFront+' -> '+stepFrom+'.';
          responseName+='; '+stepInRepositionLine;
        } else {
          stepInRepositionLine='Step In dodge resolved; reposition could not resolve because the attacker-facing slot no longer contained an active allied Hero.';
          responseName+='; '+stepInRepositionLine;
        }
      }
      var hand=sideHand(appState,rw.target_side); var respIdx=responseOption.hand_index;
      if(responseOption.response_kind!=='legacy_reduce' && responseOption.response_kind!=='dragon_scale'){
        if(hand[respIdx]!==responseOption.card_id) respIdx=hand.indexOf(responseOption.card_id);
        if(respIdx>=0 && responseExtraDiscardCount(rc)>0 && payResponseExtraDiscard(appState,rw.target_side,respIdx,rc,responseOption.selected_extra_discard_index)!==responseExtraDiscardCount(rc)){ appState[responseManaKey]=Number(appState[responseManaKey]||0)+cost; showInfo('Escape Arrow','Selected discard cost is no longer legal.'); return false; }
        respIdx=hand.indexOf(responseOption.card_id);
        if(respIdx>=0){
          if(responsePolicy){ hand.splice(respIdx,1); registerLocalAttachment(appState,rw.target_side,responseSourceLane,responseSlot,responseOption.card_id,responsePolicy,{commit_token:'response:'+rw.target_side+':'+responseOption.card_id+':'+Date.now()}); pushLog(appState,cardName(rc)+' remains in '+cardName(card(responseSourceHero.card_id))+' Attachment Slot.'); }
          else { var responseDiscardMotion=captureHandDiscardMotion(rw.target_side,respIdx,responseOption.card_id); hand.splice(respIdx,1); sideDiscard(appState,rw.target_side).push(responseOption.card_id); queueHandDiscardMotion(responseDiscardMotion); }
        }
      }
      pushLog(appState, rw.target_side+' responds with '+responseName+(cost?' and pays '+cost+' Mana.':'.')+(sideEffects.length?' Side effects: '+sideEffects.join(', ')+'.':'')+(coverUp?' Original target is protected.':''));
      var defenseSourceName=cardName(card(responseOption.card_id));
      defenseDisplayTitle=responseKindChosen==='dragon_scale'?'Dragon Scale':defenseSourceName;
      defenseActionName=responseKindChosen==='dragon_scale'?('Dragon Scale from '+defenseSourceName):defenseSourceName;
      if(rw.target_side==='AI' && responseOption.card_id){
        defenseBefore=heroEventSnapshot(appState,'AI',rw.target_lane);
        defenseEvt=recordOpponentAction(appState,'DEF',responseOption.card_id,defenseDisplayTitle,responseName,{action_line:(responseKindChosen==='dragon_scale'?'Action: AI used '+defenseActionName+' as Defense.':'Action: AI played '+defenseActionName+' as Defense.'), source_side:'AI', source_lane:responseOption.source_lane||rw.target_lane, target_side:'AI', target_lane:rw.target_lane, metric_before:defenseBefore, response_lines:['Incoming: '+cardName(card(rw.card_id))+' — '+Number(rw.damage||0)+' '+rw.damage_type+' damage.','Defense: '+responseName+'.'], result_lines:[cost?'Mana paid: '+cost+'.':'No Mana cost.']});
      }
      if(rw.target_side==='PLAYER'&&!appState.pvpHumanVsHuman){
        recordLocalPlayerAction(appState,'DEF',responseOption.card_id,defenseDisplayTitle,responseName,{action_line:(responseKindChosen==='dragon_scale'?'Action: You used '+defenseActionName+'.':'Action: You responded with '+defenseActionName+'.'),source_side:'PLAYER',source_lane:responseOption.source_lane||rw.target_lane,target_side:rw.source_side,target_lane:rw.source_lane,response_lines:['Incoming: '+cardName(card(rw.card_id))+' — '+Number(rw.damage||0)+' '+rw.damage_type+' damage.'],result_lines:[responseName+(cost?' · paid '+cost+' Mana.':'.')]});
      }
      if(appState.pvpHumanVsHuman){
        var defSourceLane=responseOption.source_lane||responseOption.legacy_source_lane||rw.target_lane;
        recordPvpSideAction(appState,rw.target_side,'DEF',responseOption.card_id,defenseDisplayTitle,rw.target_side+' responded with '+responseName+'.',{
          action_line:(responseKindChosen==='dragon_scale'?'Action: '+rw.target_side+' used '+defenseActionName+'.':'Action: '+rw.target_side+' responded with '+defenseActionName+'.'),
          source_side:rw.target_side, source_lane:defSourceLane, target_side:rw.source_side, target_lane:rw.source_lane,
          response_lines:[responseName], result_lines:[responseName+(cost?' · paid '+cost+' Mana.':'.')]
        });
      }
    }
    if(!responseOption) pushLog(appState, rw.target_side+' chose '+(rw.kind==='incoming_attack'?'Take Hit / No Response':'Pass')+'. Attack/resolution continues.');
    var result;
    if(rw.affected_lanes && rw.affected_lanes.length){
      var totalDamage=0, laneResults=[], poisonVialAppliedAny=false, areaConnectedHits=0;
      rw.affected_lanes.forEach(function(lane){
        var laneBlock=block;
        if((responseKindChosen==='legacy_reduce' || responseKindChosen==='dragon_scale') && responseOption && responseOption.chosen_affected_lane && lane!==responseOption.chosen_affected_lane) laneBlock=0;
        if(responseKindChosen==='immunity') laneBlock=Number(rw.damage||0);
        var lr=applyResolvedAttackDamage(appState,rw.card_id,rw.source_side,rw.source_lane,rw.target_side,lane,rw.damage,rw.damage_type,dodged,laneBlock,true,(responseKindChosen==='immunity'||rw.preapplied_immunity));
        queueResolvedAttackFeedback(appState,rw,lr,responseKindChosen,responseOption,laneBlock,dodged,lane,lane===(rw.affected_lanes||[])[0]);
        if(lr && lr.poison_vial_applied) poisonVialAppliedAny=true; if(lr && lr.connected) areaConnectedHits++;
        totalDamage+=Number(lr.damage||0); laneResults.push(lane+': '+Number(lr.damage||0));
      });
      if(poisonVialAppliedAny && source){ releaseActiveAttachmentForHero(appState,source,'S1-ITM-011','Poison Vial consumed after multi-target Attack'); pushLog(appState,'Poison Vial is consumed once after multi-target Attack resolution.'); }
      pushLog(appState, cardName(card(rw.card_id))+' multi-target damage results — '+laneResults.join(', ')+'.');
      result={damage:totalDamage, hp_damage:totalDamage, base_damage:totalDamage, conditional_damage:0, poison_vial_applied:poisonVialAppliedAny, connected_hits:areaConnectedHits, skipped:false};
    } else {
      if(rw.post&&rw.post.execute_defeat){ var exTarget=sideHeroes(appState,rw.target_side)[rw.target_lane]; var stopped=!!(rw.negated||dodged); if(!stopped&&exTarget&&executeTargetValid(exTarget)){exTarget.hp=0;pushLog(appState,cardName(card(rw.card_id))+' defeats '+cardName(card(exTarget.card_id))+' after its Response chain resolves.');applyDefeatCheck(appState,rw.target_side,rw.target_lane);} result={damage:0,hp_damage:0,skipped:false,negated:!!rw.negated,dodged:!!dodged,connected:!stopped,execute_defeat:!stopped}; } else result = rw.negated ? {damage:0, hp_damage:0, skipped:false, negated:true, connected:false} : applyResolvedAttackDamage(appState,rw.card_id,rw.source_side,rw.source_lane,rw.target_side,rw.target_lane,rw.damage,rw.damage_type,dodged,block,!!rw.multi_sequence,(responseKindChosen==='immunity'||rw.preapplied_immunity));
    }
    if(!(rw.affected_lanes&&rw.affected_lanes.length)){
      queueResolvedAttackFeedback(appState,rw,result,responseKindChosen,responseOption,block,dodged,rw.target_lane,true);
    }
    if(defenseEvt){
      var defenseAfter=heroEventSnapshotAfter(appState,defenseBefore,'AI',rw.target_lane), defenseLine=opponentMetricChangeLine(defenseBefore,defenseAfter,'HP'), defenseResult=[];
      defenseResult.push('Final HP damage: '+Number(result&&result.hp_damage||0)+'.');
      if(result&&Number(result.passive_block||0)>0) defenseResult.push('Passive reduction: '+Number(result.passive_block||0)+'.');
      if(result&&Number(result.active_reduction||0)>0) defenseResult.push('Active reduction: '+Number(result.active_reduction||0)+'.');
      if(defenseLine) defenseResult.push(defenseLine);
      appendOpponentEventResult(appState,defenseEvt,defenseResult);
    }
    if(rw.source_side==='AI'){
      var aiAttackAction=rw.action||{card_id:rw.card_id, source_side:rw.source_side, source_lane:rw.source_lane, target_side:rw.target_side, target_lane:rw.target_lane};
      var aiAtkEvt=findOpponentEventForAction(appState, aiAttackAction, 'ATK');
      if(!aiAtkEvt){
        aiAtkEvt=recordOpponentAction(appState,'ATK',rw.card_id,cardName(card(rw.card_id)),cardName(card(rw.card_id))+' resolved.',{
          action_line:'Action: AI played '+cardName(card(rw.card_id))+'.',
          source_side:rw.source_side, source_lane:rw.source_lane, target_side:'PLAYER', target_lane:rw.target_lane, result_lines:[]
        });
      }
      var aiResultLines=damageBreakdownLinesFromResponse(rw); if(!aiResultLines.length)aiResultLines=['Incoming damage: '+Number(rw.damage||0)+' '+rw.damage_type+'.'];
      if(responseOption) aiResultLines.push('Defense reduction/prevention: '+Number(result&&result.response_block||block||0)+'.');
      if(result&&Number(result.passive_block||0)>0) aiResultLines.push('Passive reduction: '+Number(result.passive_block||0)+'.');
      if(result&&Number(result.active_reduction||0)>0) aiResultLines.push('Active reduction: '+Number(result.active_reduction||0)+'.');
      aiResultLines.push('Final HP damage: '+Number(result&&result.hp_damage||0)+'.');
      var hpBefore=(aiAtkEvt.hp_before_by_lane&&aiAtkEvt.hp_before_by_lane[rw.target_lane]) || aiAtkEvt.metric_before;
      var hpAfter=heroEventSnapshotAfter(appState,hpBefore,rw.target_side,rw.target_lane), hpChange=opponentMetricChangeLine(hpBefore,hpAfter,'HP');
      if(hpChange && (!aiAtkEvt.metric_lines_added || aiAtkEvt.metric_lines_added.indexOf(rw.target_lane)<0)){ aiResultLines.push(hpChange); aiAtkEvt.metric_lines_added=aiAtkEvt.metric_lines_added||[]; aiAtkEvt.metric_lines_added.push(rw.target_lane); }
      if(stepInRepositionLine) aiResultLines.push('Step In result: '+stepInRepositionLine);
      if(result && Number(result.conditional_incoming_damage||0)>0) aiResultLines.push('Conditional damage bonus: +'+Number(result.conditional_incoming_damage||0)+'; '+Number(result.conditional_damage||0)+' remained after defense.');
      if(result && Number(result.burn_damage||0)>0) aiResultLines.push('Burn added +'+Number(result.burn_damage||0)+' damage.');
      if(result && result.statuses_applied && result.statuses_applied.length) aiResultLines.push('Status result: '+result.statuses_applied.join(', ')+'.');
      applyAttackAuditToEvent(aiAtkEvt,rw,result,source);
      appendOpponentEventResponse(appState,aiAtkEvt,[responseOption?('You used '+(defenseActionName||cardName(card(responseOption.card_id)))+' — '+responseName+'.'):'You passed without playing a Response.'],aiResultLines);
    }

    if(rw.source_side==='PLAYER'){
      var playerAttackAction=rw.action||{card_id:rw.card_id,source_side:rw.source_side,source_lane:rw.source_lane,target_side:rw.target_side,target_lane:rw.target_lane};
      var playerAtkEvt=findLocalPlayerEventForAction(appState,playerAttackAction,'ATK')||findLocalPlayerEventById(appState,playerAttackAction.player_event_id);
      if(playerAtkEvt){
        var playerResultLines=damageBreakdownLinesFromResponse(rw); if(!playerResultLines.length)playerResultLines=['Incoming damage: '+Number(rw.damage||0)+' '+rw.damage_type+'.'];
        if(responseOption)playerResultLines.push('Opponent response: '+responseName+'.');
        if(result&&Number(result.passive_block||0)>0)playerResultLines.push('Passive reduction: '+Number(result.passive_block||0)+'.');
        if(result&&Number(result.active_reduction||0)>0)playerResultLines.push('Active reduction: '+Number(result.active_reduction||0)+'.');
        playerResultLines.push('Final HP damage: '+Number(result&&result.hp_damage||0)+'.');
        var pBefore=(playerAtkEvt.hp_before_by_lane&&playerAtkEvt.hp_before_by_lane[rw.target_lane])||playerAtkEvt.metric_before;
        var pAfter=heroEventSnapshotAfter(appState,pBefore,rw.target_side,rw.target_lane),pHpChange=opponentMetricChangeLine(pBefore,pAfter,'HP');
        if(pHpChange&&(!playerAtkEvt.metric_lines_added||playerAtkEvt.metric_lines_added.indexOf(rw.target_lane)<0)){playerResultLines.push(pHpChange);playerAtkEvt.metric_lines_added=playerAtkEvt.metric_lines_added||[];playerAtkEvt.metric_lines_added.push(rw.target_lane);}
        if(result&&Number(result.conditional_incoming_damage||0)>0)playerResultLines.push('Conditional damage bonus: +'+Number(result.conditional_incoming_damage||0)+'; '+Number(result.conditional_damage||0)+' remained after defense.');
        if(result&&Number(result.burn_damage||0)>0)playerResultLines.push('Burn added +'+Number(result.burn_damage||0)+' damage.');
        if(result&&result.statuses_applied&&result.statuses_applied.length)playerResultLines.push('Status result: '+result.statuses_applied.join(', ')+'.');
        applyAttackAuditToEvent(playerAtkEvt,rw,result,source);
        appendLocalPlayerEventResponse(appState,playerAtkEvt,[responseOption?('Opponent used '+(defenseActionName||cardName(card(responseOption.card_id)))+' — '+responseName+'.'):'Opponent passed without playing a Response.'],playerResultLines);
      }
    }

    if(rw.multi_sequence){
      var seq=rw.multi_sequence, nextIndex=Number(seq.index||0)+1, totalSoFar=Number(seq.total_damage||0)+Number(result&&result.damage||0), poisonSoFar=!!seq.poison_vial_applied || !!(result&&result.poison_vial_applied), connectedSoFar=Number(seq.connected_hits||0)+(result&&result.connected?1:0);
      appState.responseWindow=null; appState.pending=null; closeResponseWindowUI();
      if(activateNextLegacyDefeatChoice(appState)){
        appState.pending.after_legacy_multi_sequence={action:clone(rw.action||action),post:clone(post||{}),affected_lanes:(seq.affected_lanes||[]).slice(),next_index:nextIndex,total_damage:totalSoFar,poison_vial_applied:poisonSoFar,connected_hits:connectedSoFar};
        syncCounts(appState); render(); return true;
      }
      if(nextIndex < (seq.affected_lanes||[]).length){
        openMultiTargetResponseWindow(appState, rw.action||action, post||{}, seq.affected_lanes, nextIndex, totalSoFar, poisonSoFar, connectedSoFar);
        if(appState.responseWindow && appState.responseWindow.target_side==='AI' && !appState.pvpHumanVsHuman) return autoResolveCurrentAIResponseWindow(appState);
        syncCounts(appState); render(); return true;
      }
      if(poisonSoFar && source){ releaseActiveAttachmentForHero(appState,source,'S1-ITM-011','Poison Vial consumed after multi-target Attack'); pushLog(appState,'Poison Vial is consumed once after multi-target Attack resolution.'); }
      result={damage:totalSoFar, hp_damage:totalSoFar, base_damage:totalSoFar, conditional_damage:0, poison_vial_applied:poisonSoFar, connected_hits:connectedSoFar, skipped:false};
    }

    var action=rw.action||{card_id:rw.card_id, source_side:rw.source_side, source_lane:rw.source_lane, target_side:rw.target_side, target_lane:rw.target_lane};
    var post=rw.post||{};
    var firstDoubleActivation=!!(post.double_casting && Number(post.double_casting_stage||1)===1 && !rw.canceled);
    if(firstDoubleActivation){
      if(source && !rw.negated && !rw.canceled) applySourcePostAttackEffects(appState,rw.card_id,source,rw.source_side,rw.source_lane,rw.target_side,rw.target_lane,result);
      var doubleContinuation={rw:clone(rw),action:clone(action),post:clone(post),first_result:clone(result||{})};
      if(rw.return_to_source_hand) doubleContinuation.post.return_to_source_hand_after_double_casting=true;
      appState.responseWindow=null; appState.pending=null; closeResponseWindowUI();
      if(!appState.gameOver) checkGameEnd(appState);
      if(!appState.gameOver && activateNextLegacyDefeatChoice(appState)){
        if(appState.pending&&appState.pending.type==='legacy_defeat_choice') appState.pending.after_legacy_double_casting=doubleContinuation;
        syncCounts(appState); render(); return true;
      }
      return openDoubleCastingSecondTargetSelection(appState,doubleContinuation);
    }
    if(!rw.negated && source){ applySourcePostAttackEffects(appState, rw.card_id, source, rw.source_side, rw.source_lane, rw.target_side, rw.target_lane, result); }
    if(source){ consumeEnrageBonus(source); }
    finalizePlayedCardAfterResponse(appState,rw);
    if(Number(post.double_casting_stage||0)===2 || (post.double_casting && rw.canceled)) consumeDoubleCasting(appState,action.source_side,action.source_lane);
    appState.responseWindow=null; if(!isDefeatGatePending(appState.pending)) appState.pending=null; closeResponseWindowUI();
    // A lethal response result may have queued Legacy replacement while the response frame owned pending.
    // Open that mandatory choice before post-attack continuation or AI resume.
    if(!appState.pending) activateNextLegacyDefeatChoice(appState);
    if(!appState.gameOver && !isDefeatGatePending(appState.pending)) checkGameEnd(appState);
    if(appState.pending&&appState.pending.type==='racial_stoneblood'){ appState.pending.after_stoneblood_response={action:clone(action),post:clone(post||{}),result:clone(result||{})}; syncCounts(appState); render(); return true; }
    if(result && (result.dodged||result.dodged_any) && (post.defer_reposition_discard || post.deferred_discard_card_id)) finalizeDeferredRepositionDiscard(appState,post,action.source_side,action.card_id);
    if(!isDefeatGatePending(appState.pending) && maybeOpenHalflingSecondChance(appState, action, post, result)){ syncCounts(appState); render(); return true; }
    if(resolvePostAttackChoices(appState, action, post, result)){ syncCounts(appState); render(); return true; }
    if(resumeAITurnAfterPlayerResponse(appState)) return true;
    syncCounts(appState); render(); return true;
  }
  function closeResponseWindowUI(){ var overlay=$('responseOverlay'); if(overlay) overlay.classList.remove('open'); }
  function responsePassNoStuck(){ return resolveResponseWindow(null); }
  function responseSelectNoStuck(idx){
    var rw=appState&&appState.responseWindow; if(rw)refreshResponseWindowOptions(appState,rw); var opt=rw&&rw.options&&rw.options[idx];
    if(!opt){ showInfo('Response Window','Selected response option is no longer legal. Use Take Hit / No Response to continue.'); return false; }
    rw.selected=idx; renderResponseWindow(); return true;
  }
  function confirmSelectedResponse(){
    var rw=appState&&appState.responseWindow;
    if(!rw){ showInfo('Response Window','No active Response Window.'); return false; }
    refreshResponseWindowOptions(appState,rw);
    var idx=rw.selected, opt=(typeof idx==='number') && rw.options && rw.options[idx];
    if(!opt){ showInfo('Response Window','Select a response card first, then confirm.'); return false; }
    return resolveResponseWindow(opt);
  }
  function hideResponseReasonTooltip(){var tip=document.getElementById('responseReasonTooltip');if(tip){tip.hidden=true;tip.textContent='';}}
  function showResponseReasonTooltip(anchor){
    if(!anchor)return;var panel=anchor.closest('.response-panel')||document.querySelector('.response-panel');if(!panel)return;
    var tip=document.getElementById('responseReasonTooltip');if(!tip){tip=document.createElement('div');tip.id='responseReasonTooltip';tip.className='response-hover-tooltip';tip.hidden=true;panel.appendChild(tip);}
    tip.textContent=anchor.getAttribute('data-response-reason')||'This response is not available.';tip.hidden=false;
    var pr=panel.getBoundingClientRect(),ar=anchor.getBoundingClientRect(),width=Math.min(340,Math.max(190,tip.scrollWidth||240));
    var left=Math.max(8,Math.min(pr.width-width-8,(ar.left-pr.left)+(ar.width-width)/2));
    var top=Math.max(8,(ar.top-pr.top)-(tip.offsetHeight||42)-8);tip.style.width=width+'px';tip.style.left=left+'px';tip.style.top=top+'px';
  }
  function renderResponseWindow(){
    var overlay=$('responseOverlay'); if(!overlay) return; var state=appState; var rw=state&&state.responseWindow;
    if(!rw || !pvpLocalOwnsResponseWindow(rw)){ overlay.classList.remove('open'); return; }
    refreshResponseWindowOptions(state,rw);
    var body=$('responseBody'), footer=$('responseFooter'); var opts=rw.options||[], displayItems=responseDisplayItemsFor(state,rw), unavailable=[];
    var optionsHtml=displayItems.length?displayItems.map(function(item){
      if(item.available){var o=item.option,i=item.option_index,selected=rw.selected===i,c=card(o.card_id),label=o.label||cardName(c),meta=(o.racial_ability?'Racial Trait · Response':(o.response_kind==='legacy_reduce'?'Legacy Ability · Response':(cardFamily(c)+' · '+(cardSubtype(c)||'Response'))))+(o.source_lane?' · Source '+esc(o.source_lane):'');return '<article class="response-option '+(selected?'selected':'')+' available"><button class="response-option-preview" type="button" data-preview="'+esc(o.card_id)+'"><img src="'+esc(thumbFor(o.card_id))+'" alt="'+esc(label)+'"><strong>'+esc(label)+'</strong><small>'+meta+'</small></button><button class="response-option-select" type="button" data-response-select="'+i+'" aria-pressed="'+(selected?'true':'false')+'">'+(selected?'Selected':'Select')+'</button></article>';}
      var uidx=unavailable.length;unavailable.push(item);var uc=card(item.card_id),reasonText=(item.reasons||['This response is not available in the current state.'])[0];return '<article class="response-option unavailable" aria-disabled="true"><button class="response-option-preview" type="button" data-preview="'+esc(item.card_id)+'"><img src="'+esc(thumbFor(item.card_id))+'" alt="'+esc(cardName(uc))+'"><strong>'+esc(cardName(uc))+'</strong><small>'+esc(cardFamily(uc)+' · '+(cardSubtype(uc)||'Response'))+'</small></button><span class="response-option-select response-option-reason" tabindex="0" data-response-reason="'+esc(reasonText)+'" aria-label="Not Available: '+esc(reasonText)+'">Not Available</span></article>';
    }).join(''):'<div class="empty-state compact">No Defense or Response card is in Hand. Use the footer action to continue.</div>';
    rw.unavailable_response_cards=unavailable;
    if(rw.kind==='incoming_card'){
      var inc=card(rw.card_id);
      body.innerHTML='<div class="response-context-row"><article class="response-context-card"><h3>Opponent Card</h3><button class="response-context-tile" type="button" data-preview="'+esc(rw.card_id)+'"><img src="'+esc(thumbFor(rw.card_id))+'" alt="'+esc(cardName(inc))+'"><span><strong>'+esc(cardName(inc))+'</strong><small>'+esc(cardFamily(inc))+' · reactive cancel window</small><small>Use Intercept for Event or Flashpowder Bomb for Item.</small></span></button></article></div><section class="response-options-wrap"><h3>Response Cards &amp; Abilities</h3><div class="response-option-grid">'+optionsHtml+'</div></section>';
    } else {
      body.innerHTML='<div class="response-context-row">'+heroMiniContext('Attacking Hero',rw.source_side,rw.source_lane)+incomingCardContext(rw)+heroMiniContext('Defending Hero',rw.target_side,rw.target_lane)+'</div><section class="response-options-wrap"><h3>Response Cards &amp; Abilities</h3><div class="response-option-grid">'+optionsHtml+'</div></section>';
    }
    var passText=rw.kind==='incoming_attack'?'Take Hit / No Response':(rw.kind==='incoming_card'?'No Response / Let Resolve':'Pass'); var confirmDisabled=(typeof rw.selected!=='number'); footer.innerHTML='<button id="responsePassButton" type="button" class="gold response-pass-button">'+passText+'</button>'+(opts.length?'<button id="responseConfirmButton" type="button" class="gold response-confirm-button" '+(confirmDisabled?'disabled':'')+'>Confirm Response</button>':''); overlay.classList.add('open'); bringModalToFront(overlay);
    var passBtn=$('responsePassButton'); if(passBtn){ passBtn.disabled=false; passBtn.onclick=function(ev){ if(ev){ ev.preventDefault(); ev.stopPropagation(); } responsePassNoStuck(); }; }
    var confirmBtn=$('responseConfirmButton'); if(confirmBtn){ confirmBtn.onclick=function(ev){ if(ev){ ev.preventDefault(); ev.stopPropagation(); } confirmSelectedResponse(); }; }
    Array.prototype.forEach.call(overlay.querySelectorAll('[data-response-select]'), function(btn){ btn.disabled=false; btn.onclick=function(ev){ ev.preventDefault(); ev.stopPropagation(); responseSelectNoStuck(Number(btn.getAttribute('data-response-select'))); }; });
    Array.prototype.forEach.call(overlay.querySelectorAll('[data-preview]'), function(btn){ btn.onclick=function(ev){ ev.preventDefault(); ev.stopPropagation(); showPreview(btn.getAttribute('data-preview'),'response'); }; });
    Array.prototype.forEach.call(overlay.querySelectorAll('.response-option-reason'),function(el){
      el.onmouseenter=function(){showResponseReasonTooltip(el);};el.onmouseleave=hideResponseReasonTooltip;
      el.onfocus=function(){showResponseReasonTooltip(el);};el.onblur=hideResponseReasonTooltip;
    });
    var closeBtn=$('responseClose'); if(closeBtn){ closeBtn.onclick=function(ev){ if(ev){ ev.preventDefault(); ev.stopPropagation(); } responsePassNoStuck(); }; }
  }

  function connectedHitCount(result){
    if(!result || result.negated || result.canceled || result.prevented || result.dodged) return 0;
    if(typeof result.connected_hits==='number') return Math.max(0,Number(result.connected_hits||0));
    return result.connected?1:0;
  }
  function printedAttackHitCount(result){
    // Printed "when/if this attack hits" does not require HP damage. Block or Prevent to 0 still counts.
    if(!result || result.negated || result.canceled || result.dodged) return 0;
    if(typeof result.connected_hits==='number') return Math.max(0,Number(result.connected_hits||0));
    return 1;
  }
  function removeOpponentManaShards(state, sourceSide, amount, reason){
    var opp=sourceSide==='PLAYER'?'aiMana':'mana', before=Number(state[opp]||0), requested=Math.max(0,Number(amount||0));
    state[opp]=Math.max(0,before-requested); var removed=before-Number(state[opp]||0);
    pushLog(state,(reason||'Effect')+' removes '+removed+' Mana Shard'+(removed===1?'':'s')+' from opponent Mana Pool.');
    return removed;
  }
  function applySourcePostAttackEffects(state, cardId, sourceHero, sourceSide, sourceLane, targetSide, targetLane, result){
    var c=card(cardId); if(!sourceHero || !c) return;
    var hpDamage=Number(result&&result.damage||0), connectedHits=connectedHitCount(result), cls=heroClass(sourceHero);
    // Current One Source / Runtime connected-hit mana family.
    // Dodge/cancel/negate/prevent: no trigger. A normal block that reaches 0 HP damage still connects.
    if(c.card_id==='S1-MAG-021' && connectedHits>0 && (cls==='Spell Blade' || cls==='Arcane Duelist')) transferManaShard(state,sourceSide,c);
    if(c.card_id==='S1-MAG-023' && connectedHits>0) removeOpponentManaShards(state,sourceSide,cls==='Arcane Duelist'?3:1,cardName(c)+' connected hit');
    if(c.card_id==='S1-MAG-024' && connectedHits>0) removeOpponentManaShards(state,sourceSide,connectedHits,cardName(c)+' connected hits');
    if(c.card_id==='S1-THF-029' && printedAttackHitCount(result)>0){ drawOne(state,sourceSide,false); pushLog(state,cardName(c)+' hit and draws 1 card.'); }
    if(physicalAttackConvertedByAether(c,sourceHero) && hpDamage>0) removeOpponentManaShards(state,sourceSide,1,'Aether Infusion converted attack dealt HP damage');
    if(isShieldBashCard(c)){ pushLog(state, cardName(c)+' Attachment gives '+cardName(card(sourceHero.card_id))+' incoming Physical damage -20 until the start of the owner next turn.'); }
    if(isHolyRingCard(c) && ['Priest','Saint'].indexOf(heroClass(sourceHero))!==-1){ pushLog(state, cardName(c)+' Attachment gives '+cardName(card(sourceHero.card_id))+' attack-target prevention until the owner next Draw Phase.'); }
    if(isHolySlashCard(c) && hpDamage>0){ healHeroIfLegal(state,sourceHero,20,c,sourceHero,'Holy Slash successful HP damage'); }
    if(c.card_id==='S1-CLE-010'){ healAllAlliedHeroes(state,sourceSide,20,c,sourceHero,'Holy Blast independent heal'); }
    if(isVenomBindingCard(c) && sourceHero && heroClass(sourceHero)==='Renegade' && hpDamage>0){ removeOpponentManaShards(state,sourceSide,1,cardName(c)+' Renegade on-hit'); }
  }
  function transferManaShard(state, side, c){
    var own=side==='PLAYER'?'mana':'aiMana', opp=side==='PLAYER'?'aiMana':'mana';
    if(Number(state[opp]||0)<=0){ pushLog(state, cardName(c)+' resolves but opponent has no Mana Shard to steal.'); return false; }
    state[opp]=Math.max(0,Number(state[opp]||0)-1); state[own]=Math.min(12,Number(state[own]||0)+1); pushLog(state, cardName(c)+' transfers 1 Mana Shard from opponent Mana Pool.'); return true;
  }
  function deckForSide(state, side){ return side==='PLAYER'?state.playerDeck:state.aiDeck; }
  function shufflePile(pile){
    if(!Array.isArray(pile)) return pile;
    for(var i=pile.length-1;i>0;i--){ var j=Math.floor(Math.random()*(i+1)); var tmp=pile[i]; pile[i]=pile[j]; pile[j]=tmp; }
    return pile;
  }
  function filterMatchesCard(c, filter){
    filter=filter||{}; if(!c) return false;
    if(filter.family && cardFamily(c)!==filter.family) return false;
    if(filter.card_type && cardFamily(c)!==filter.card_type) return false;
    if(filter.class_family){
      var prefixMap={Warrior:'WAR',Mage:'MAG',Cleric:'CLE',Thief:'THF',Archer:'ARC'};
      var pref=prefixMap[String(filter.class_family)] || String(filter.class_family).slice(0,3).toUpperCase();
      if(String(c.card_id||'').indexOf('S1-'+pref+'-')!==0) return false;
    }
    if(filter.is_ultimate===false && isUltimateCard(c)) return false;
    if(filter.is_response===true && !isResponseOnly(c)) return false;
    return true;
  }
  function legalDefSkillCard(c){ return cardFamily(c)==='Skill' && isResponseOnly(c); }
  function archerNonUltimateSkillCard(c){ return isArcherSkillCard(c) && !isUltimateCard(c); }
  function nonUltimateSkillCard(c){ return cardFamily(c)==='Skill' && !isUltimateCard(c); }
  function handHasNonUltimateSkill(hand){ return (hand||[]).some(function(id){ return nonUltimateSkillCard(card(id)); }); }
  function baseFamilyForHero(hero){ var id=(card(heroIdFrom(hero)).identity||{}); return id.base_class_family || (id.active_class_lineage&&id.active_class_lineage[0]) || heroClass(hero); }
  function activeLineageForHero(hero){ var hcCard=card(heroIdFrom(hero))||{}, id=hcCard.identity||{}, arr=[]; if(Array.isArray(id.active_class_lineage)) arr=arr.concat(id.active_class_lineage); if(Array.isArray(id.base_skill_classes)) arr=arr.concat(id.base_skill_classes); if(typeof hcCard.active_class_lineage==='string') arr=arr.concat(hcCard.active_class_lineage.split(';')); if(typeof hcCard.base_skill_classes==='string') arr=arr.concat(hcCard.base_skill_classes.split(';')); if(id.base_class_family) arr.push(id.base_class_family); if(id.class) arr.push(id.class); var hc=heroClass(hero); if(hc) arr.push(hc); return arr.map(function(x){return String(x||'').trim();}).filter(function(x,i,a){return x&&a.indexOf(x)===i;}); }
  function heroMatchesLineageGate(hero,required){ if(!hero||isLegacyModeHero(hero)||Number(hero.hp||0)<=0) return false; required=Array.isArray(required)?required:[]; if(!required.length) return true; var line=activeLineageForHero(hero).map(function(x){return String(x).toLowerCase();}); return required.some(function(x){return line.indexOf(String(x||'').toLowerCase())!==-1;}); }
  function itemLineageGate(c){ return c&&c.requirement&&Array.isArray(c.requirement.target_or_user_base_classes)?c.requirement.target_or_user_base_classes:[]; }
  function poisonVialHostEligible(hero){ if(!hero || Number(hero.hp||0)<=0 || isLegacyModeHero(hero)) return false; var hc=heroClass(hero), base=baseFamilyForHero(hero), line=activeLineageForHero(hero).join(' '); return /^(Thief|Rogue|Renegade|Spell Blade|Arcane Duelist|Archer|Marksman|Grand Ranger|Arbalest|Grand Arbalest)$/i.test(String(hc||'')) || /^(Thief|Archer)$/i.test(String(base||'')) || /(Thief|Archer)/i.test(line); }
  function runtimeTurnStamp(state){ return String((state&&state.round)||1)+'|'+String((state&&state.turn)||''); }
  function racialTokenSpentTurnKey(side){return side==='PLAYER'?'playerRacialTokenSpentTurn':'aiRacialTokenSpentTurn';}
  function racialTokenSpentThisTurn(state,side){return !!(state&&state[racialTokenSpentTurnKey(side)]===runtimeTurnStamp(state));}
  function markRacialTokenSpent(state,side){if(state)state[racialTokenSpentTurnKey(side)]=runtimeTurnStamp(state);return true;}
  function racialAbilityUsedThisTurnByHero(state,hero){var side=state&&hero&&sideHeroes(state,'PLAYER')&&LANE_ORDER.some(function(l){return sideHeroes(state,'PLAYER')[l]===hero;})?'PLAYER':'AI';return racialTokenSpentThisTurn(state,side);}
  function markRacialAbilityUsedThisTurn(state,hero){ var side=state&&hero&&sideHeroes(state,'PLAYER')&&LANE_ORDER.some(function(l){return sideHeroes(state,'PLAYER')[l]===hero;})?'PLAYER':'AI'; return markRacialTokenSpent(state,side); }
  function clearRacialUseForSide(state, side){ var heroes=sideHeroes(state,side)||{}; LANE_ORDER.forEach(function(l){ if(heroes[l]){ heroes[l].racial_used_turn=null; heroes[l].stoneblood_declined_turn=null; } }); }

  function drawReplacementAbilityForHero(hero){
    var hc=heroClass(hero);
    if(hc==='Grand Arbalest') return {abilityId:'rapid_chamber', abilityName:'Rapid Chamber'};
    if(hc==='Arbalest') return {abilityId:'quick_reload', abilityName:'Quick Reload'};
    return null;
  }
  function drawReplacementSourceForSide(state, side){
    if(!state || state.phase!=='Draw') return null;
    var stamp=runtimeTurnStamp(state), heroes=sideHeroes(state,side)||{};
    for(var i=0;i<LANE_ORDER.length;i++){
      var lane=LANE_ORDER[i], h=heroes[lane], ab=drawReplacementAbilityForHero(h);
      if(!h || !ab || !isActiveHeroSlot(h)) continue;
      if(h.draw_replacement_used_turn===stamp) continue;
      return {lane:lane, hero:h, abilityId:ab.abilityId, abilityName:ab.abilityName};
    }
    return null;
  }
  function maybeOpenDrawReplacementChoice(state, side, drawnCardId, handIndex){
    if(!state || (side!=='PLAYER'&&!state.pvpHumanVsHuman) || state.phase!=='Draw' || state.pending || state.gameOver) return false;
    var src=drawReplacementSourceForSide(state,side); if(!src) return false;
    state.pending={type:'draw_replacement_choice', side:side, decision_side:side, source_side:side, source_lane:src.lane, source_hero_card_id:heroIdFrom(src.hero), abilityId:src.abilityId, abilityName:src.abilityName, drawn_card_id:drawnCardId, hand_index:handIndex};
    pushLog(state, src.abilityName+' offers draw review for '+cardName(card(drawnCardId))+'.');
    renderDrawReplacementChoice();
    return true;
  }
  function responseProfileOverride(rc,incoming){
    var id=rc&&rc.card_id, dt=String((incoming&&incoming.damage_type)||''), cannotBlock=!!(incoming&&incoming.cannot_block), cannotDodge=!!(incoming&&incoming.cannot_dodge);
    if(id==='S1-MAG-004') return (!cannotBlock&&dt==='Magical')?{kind:'block',amount:40}:null;
    if(id==='S1-MAG-005') return (/^(Physical|Magical)$/i.test(dt)&&!cannotDodge)?{kind:'dodge'}:null;
    if(id==='S1-WAR-003') return (!cannotBlock&&dt==='Physical')?{kind:'block',amount:30}:null;
    if(id==='S1-ARC-012') return (/^(Physical|Magical)$/i.test(dt)&&!cannotBlock)?{kind:'block',amount_by_class:{'Archer':30,'Marksman':40,'Grand Ranger':40}}:null;
    if(id==='S1-THF-021') return (!cannotBlock&&dt==='Physical')?{kind:'block',amount_by_class:{'Thief':50,'Spell Blade':60,'Arcane Duelist':60}}:null;
    if(id==='S1-THF-022') return (/^(Physical|Magical)$/i.test(dt)&&!cannotDodge)?{kind:'dodge'}:null;
    return undefined;
  }
  function activeAlliedBaseFamiliesDifferent(state, side){ var seen=[]; LANE_ORDER.forEach(function(l){ var h=sideHeroes(state,side)[l]; if(h&&Number(h.hp||0)>0){ var b=baseFamilyForHero(h); if(seen.indexOf(b)===-1) seen.push(b); } }); return seen.length>=3; }
  function hasCoordinationAttackBuff(hero){ return !!activeAttachmentForSide(appState,(heroRuntimeLocation(appState,hero)||{}).side,'S1-EVT-011'); }
  function coordinationAttackBonus(state,hero){ var loc=heroRuntimeLocation(state,hero); if(!loc) return 0; var att=activeAttachmentForSide(state,loc.side,'S1-EVT-011'); return att?Number(att.modifier_amount||att.bonus||10):0; }
  function choiceTileHtml(id, idx, selected){
    var c=card(id);
    return '<article class="card-search-choice '+(selected?'selected':'')+'"><button class="discard-preview" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(c))+' thumbnail"><span>'+esc(cardName(c))+'</span></button><button class="discard-select" type="button" data-search-choice-index="'+idx+'">'+(selected?'Selected':'Select')+'</button></article>';
  }
  function cardSearchSelectedCount(p){ return p && p.multi_select ? ((p.selected_indices||[]).length) : (typeof (p&&p.selected_index)==='number'?1:0); }
  function cardSearchRequiredCount(p){ return p && p.multi_select ? Math.min(Number(p.required_count||p.max_count||1), (p.candidates||[]).length) : 1; }
  function renderCardSearchChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='card_search_choice') return;
    var p=appState.pending, ids=(p.candidates||[]).map(function(x){return x.card_id;});
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent=p.title||'Choose Card';
    $('choiceConfirm').style.display=''; $('choiceConfirm').textContent=p.confirm_text||'Add Selected'; $('choiceConfirm').disabled=(cardSearchSelectedCount(p)<cardSearchRequiredCount(p));
    $('choiceBody').innerHTML='<p class="choice-instruction">'+esc(p.instruction||'Select one card. Preview opens above this popup and keeps selection intact.')+'</p><div class="choice-grid card-search-choice-grid">'+ids.map(function(id,idx){ var selected=p.multi_select?(p.selected_indices||[]).indexOf(idx)!==-1:p.selected_index===idx; return choiceTileHtml(id,idx,selected); }).join('')+'</div>';
    $('choiceOverlay').classList.add('open'); bringModalToFront($('choiceOverlay'));
  }
  function selectCardSearchChoice(idx){
    if(!appState || !appState.pending || appState.pending.type!=='card_search_choice') return false;
    var p=appState.pending;
    if(idx<0 || idx>=(p.candidates||[]).length) return false;
    if(p.multi_select){
      p.selected_indices=p.selected_indices||[];
      var pos=p.selected_indices.indexOf(idx);
      if(pos!==-1) p.selected_indices.splice(pos,1);
      else if(p.selected_indices.length<Number(p.max_count||1)) p.selected_indices.push(idx);
    } else p.selected_index=idx;
    renderCardSearchChoice(); return true;
  }
  function finalizePostChoiceSourceCard(state,p){
    var host=(p.host_side&&p.host_lane)?sideHeroes(state,p.host_side)[p.host_lane]:null;
    if(p.resolve_to==='triple_shot_bind' && host && typeof p.attachmentSlot==='number' && p.attachmentSlot>=0){
      registerLocalAttachment(state,p.host_side,p.host_lane,p.attachmentSlot,p.source_card_id,{tick_phase:'END_PHASE',remaining_count:1,counter_mode:'countdown',role:'modifier'},p,{expires:'end_phase',chosen_card_id:p.chosen_card_id||null});
      finalizeCommittedSourceCard(state,p,p.side||'PLAYER',p.source_card_id,'attachment',{side:p.host_side,lane:p.host_lane,slot:p.attachmentSlot});
      pushLog(state, cardName(card(p.source_card_id))+' remains in the source Hero Attachment Slot until End Phase.');
    } else {
      finalizeCommittedSourceCard(state,p,p.side||'PLAYER',p.source_card_id,'discard',{reason:'post-choice source finalization'});
    }
    state.pending=null; syncCounts(state); if(!SUPPRESS_RENDER) closeChoice(); render(); return true;
  }
  function commitCardSearchChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='card_search_choice') return false;
    var p=appState.pending; var selected=p.multi_select?(p.selected_indices||[]).slice():[p.selected_index];
    selected=selected.filter(function(v){ return typeof v==='number' && p.candidates[v]; });
    if(selected.length<cardSearchRequiredCount(p)){ showInfo('Choice Required','Select the required card choice before confirming.'); return false; }
    var picked=selected.map(function(i){ return p.candidates[i]; });
    if(p.zone==='deck'){
      var deck=deckForSide(appState,p.side);
      picked.sort(function(a,b){ return b.deck_index-a.deck_index; }).forEach(function(cand){ var idx=cand.deck_index; if(deck[idx]!==cand.card_id) idx=deck.indexOf(cand.card_id); if(idx>=0){ var id=deck.splice(idx,1)[0]; sideHand(appState,p.side).push(id); pushLog(appState, cardName(card(p.source_card_id))+' adds chosen '+cardName(card(id))+' from deck to hand.'); } else pushLog(appState, cardName(card(p.source_card_id))+' chosen card was not found in deck at resolution.'); });
      shufflePile(deck); pushLog(appState,'Deck shuffled after '+cardName(card(p.source_card_id))+' search/open.');
    } else if(p.zone==='hand'){
      var hnd=sideHand(appState,p.side);
      if(p.resolve_to==='triple_shot_bind'){
        var bind=picked[0], bindId=bind&&bind.card_id, heroBind=(p.host_side&&p.host_lane)?sideHeroes(appState,p.host_side)[p.host_lane]:null;
        if(bindId && heroBind){ p.chosen_card_id=bindId; pushLog(appState, 'Triple Shot Attachment binds '+cardName(card(bindId))+' as the chosen card. Every legal use by '+cardName(card(heroBind.card_id))+' this turn becomes Area Attack.'); }
      } else picked.sort(function(a,b){ return b.hand_index-a.hand_index; }).forEach(function(cand){
        var hidx=cand.hand_index; if(hnd[hidx]!==cand.card_id) hidx=hnd.indexOf(cand.card_id);
        if(hidx>=0){
          var hid=hnd.splice(hidx,1)[0];
          if(p.resolve_to==='discard_then_draw_three'){ sideDiscard(appState,p.side).push(hid); pushLog(appState,cardName(card(p.source_card_id))+' discards '+cardName(card(hid))+' as its explicit cost.'); drawMany(appState,p.side,3,{reason:'CARD_EFFECT'}); pushLog(appState,cardName(card(p.source_card_id))+' draws up to 3 cards.'); } else if(p.resolve_to==='source_exp' && p.host_side && p.host_lane){
            var hero=sideHeroes(appState,p.host_side)[p.host_lane];
            if(!heroCanReceiveExpCard(hero, card(hid))){ sideHand(appState,p.side).push(hid); pushLog(appState, cardName(card(p.source_card_id))+' cannot add '+cardName(card(hid))+' as EXP because '+cardName(card(hero&&hero.card_id))+' would exceed the 700 EXP max.'); }
            else { hero.exp_cards=hero.exp_cards||[]; hero.exp_cards.push(hid); applyTributeExpToHero(hero,card(hid));
              pushLog(appState, cardName(card(p.source_card_id))+' tributes chosen '+cardName(card(hid))+' to '+cardName(card(hero.card_id))+' as EXP without consuming the normal Tribute tracker.'); checkAndApplyRankUp(appState,p.host_side,p.host_lane); }
          } else { sideHand(appState,p.side).push(hid); }
        }
      });
    } else if(p.zone==='discard'){
      var pile=sideDiscard(appState,p.side), destinationDeck=deckForSide(appState,p.side);
      picked.sort(function(a,b){ return b.discard_index-a.discard_index; }).forEach(function(cand){ var pidx=cand.discard_index; if(pile[pidx]!==cand.card_id) pidx=pile.indexOf(cand.card_id); if(pidx>=0){ var pid=pile.splice(pidx,1)[0]; if(p.resolve_to==='replenish_to_deck'){ destinationDeck.push(pid); pushLog(appState, cardName(card(p.source_card_id))+' returns chosen '+cardName(card(pid))+' from Discard Pile to deck.'); } else { sideHand(appState,p.side).push(pid); pushLog(appState, cardName(card(p.source_card_id))+' returns chosen '+cardName(card(pid))+' from Discard Pile to hand.'); } } });
    }
    return finalizePostChoiceSourceCard(appState,p);
  }
  function startCardSearchChoice(state, opts){
    var side=opts.side||'PLAYER', zone=opts.zone||'deck', sourceCard=opts.sourceCard, candidates=[];
    if(zone==='deck'){ (deckForSide(state,side)||[]).forEach(function(id,idx){ var c=card(id); if(opts.predicate ? opts.predicate(c) : filterMatchesCard(c,opts.filter)){ candidates.push({card_id:id, deck_index:idx}); } }); }
    else if(zone==='hand'){ (sideHand(state,side)||[]).forEach(function(id,idx){ var c=card(id); if(opts.predicate ? opts.predicate(c) : filterMatchesCard(c,opts.filter)){ candidates.push({card_id:id, hand_index:idx}); } }); }
    else if(zone==='discard'){ (sideDiscard(state,side)||[]).forEach(function(id,idx){ var c=card(id); if(opts.predicate ? opts.predicate(c) : filterMatchesCard(c,opts.filter)){ candidates.push({card_id:id, discard_index:idx}); } }); }
    if(!candidates.length){
      if(zone==='deck'){ shufflePile(deckForSide(state,side)); pushLog(state, cardName(sourceCard)+' searches deck and finds no legal card. Deck shuffled after search/open.'); }
      else if(zone==='hand') pushLog(state, cardName(sourceCard)+' finds no legal card in hand.');
      else pushLog(state, cardName(sourceCard)+' finds no legal card in Discard Pile.');
      return false;
    }
    if(side!=='PLAYER' && !state.pvpHumanVsHuman){
      var required=Math.max(1,Number(opts.required_count||opts.max_count||1)), picks=candidates.slice(0,required);
      if(zone==='deck'){
        var deck=deckForSide(state,side); picks.sort(function(a,b){return b.deck_index-a.deck_index;}).forEach(function(pick){var id=deck.splice(pick.deck_index,1)[0];if(id)sideHand(state,side).push(id);}); shufflePile(deck); pushLog(state,cardName(sourceCard)+' auto-selects '+picks.length+' legal deck card(s) and shuffles deck.');
      } else if(zone==='hand'){
        var hnd=sideHand(state,side); picks.sort(function(a,b){return b.hand_index-a.hand_index;}).forEach(function(pick){var idx=pick.hand_index;if(hnd[idx]!==pick.card_id)idx=hnd.indexOf(pick.card_id);if(idx<0)return;var hid=hnd.splice(idx,1)[0];
          if(opts.resolve_to==='source_exp'&&opts.host_side&&opts.host_lane){var hero=sideHeroes(state,opts.host_side)[opts.host_lane];if(heroCanReceiveExpCard(hero,card(hid))){hero.exp_cards=hero.exp_cards||[];hero.exp_cards.push(hid);hero.exp_total=Number(hero.exp_total||0)+tributeExpValue(card(hid));pushLog(state,cardName(sourceCard)+' adds '+cardName(card(hid))+' as EXP to '+cardName(card(hero.card_id))+'.');checkAndApplyRankUp(state,opts.host_side,opts.host_lane);}else hnd.push(hid);}
          else if(opts.resolve_to==='triple_shot_bind'&&opts.host_side&&opts.host_lane){opts.chosen_card_id=hid;hnd.push(hid);}
          else hnd.push(hid);
        });
      } else {
        var pile=sideDiscard(state,side), dest=deckForSide(state,side); picks.sort(function(a,b){return b.discard_index-a.discard_index;}).forEach(function(pick){var idx=pick.discard_index;if(pile[idx]!==pick.card_id)idx=pile.indexOf(pick.card_id);if(idx<0)return;var did=pile.splice(idx,1)[0];if(opts.resolve_to==='replenish_to_deck')dest.push(did);else sideHand(state,side).push(did);});
        pushLog(state,cardName(sourceCard)+' auto-selects '+picks.length+' legal Discard card(s).');
      }
      return false;
    }
    state.pending={type:'card_search_choice', side:side, decision_side:side, zone:zone, source_card_id:sourceCard.card_id, resolve_to:opts.resolve_to, host_side:opts.host_side, host_lane:opts.host_lane, attachmentSlot:opts.attachmentSlot, candidates:candidates, selected_index:null, selected_indices:[], multi_select:Number(opts.max_count||1)>1, max_count:Number(opts.max_count||1), required_count:Number(opts.required_count||opts.max_count||1), title:opts.title, instruction:opts.instruction, confirm_text:opts.confirm_text, commit_token:opts.commit_token||activeCommitTokenFor(state,side,sourceCard.card_id)};
    pushLog(state, cardName(sourceCard)+' opens compact card choice UI with '+candidates.length+' legal option(s).');
    syncCounts(state); renderCardSearchChoice(); render(); return true;
  }
  function resolveSixthSense(state, side, c, hostSide, hostLane, attachmentSlot, commitToken){
    return startCardSearchChoice(state,{side:side, zone:'deck', sourceCard:c, commit_token:commitToken, host_side:hostSide, host_lane:hostLane, attachmentSlot:attachmentSlot, predicate:legalDefSkillCard, title:'Sixth Sense — Choose DEF Skill', instruction:'Choose 1 legal DEF Response Skill from your deck and add it to your hand. The deck shuffles after this search.', confirm_text:'Add DEF Skill'});
  }
  function applyPoisonMist(state, side, sourceHero, c){
    var targetSide=side==='PLAYER'?'AI':'PLAYER', count=0; LANE_ORDER.forEach(function(lane){ var h=sideHeroes(state,targetSide)[lane]; if(h && Number(h.hp||0)>0){ var r=applyStatusEffectToHero({kind:'inflict_status', status:'Poison', duration_turns:2, raw_duration:'2'},h,sourceHero,'Poison Mist',c); if(r){ count++; } } });
    pushLog(state, cardName(c)+' applies Poison to '+count+' active opponent Hero(es).'); return count;
  }
  function applyWildfire(state, side, sourceHero, c){
    var targetSide=side==='PLAYER'?'AI':'PLAYER', count=0; LANE_ORDER.forEach(function(lane){ var h=sideHeroes(state,targetSide)[lane]; if(h && Number(h.hp||0)>0 && !isLegacyModeHero(h)){ var r=applyStatusEffectToHero({kind:'inflict_status', status:'Burn', duration_turns:2, raw_duration:'2'},h,sourceHero,'Wildfire',c); if(r){ count++; } } });
    pushLog(state, cardName(c)+' applies Burn to '+count+' active opponent Hero(es).'); return count;
  }
  function applyArcherTacticalEffects(state, side, c, sourceHero, hostSide, hostLane, attachmentSlot, commitToken){
    if(isEyesHawkCard(c)){
      return startCalculatedPlanChoice(state,{side:side, source_card_id:c.card_id, source_side:side, source_lane:sourceHero&&sourceHero.lane, host_side:hostSide, host_lane:hostLane, attachmentSlot:attachmentSlot, resolve_to:'opponent_deck', title:'Eyes of the Hawk — Choose Opponent Hand', instruction:'Choose 1 opponent hand card. It is shuffled into their Main Deck; the remaining hand is shuffled after resolution.'});
    }
    if(isTripleShotCard(c) && sourceHero){
      return startCardSearchChoice(state,{side:side, zone:'hand', sourceCard:c, commit_token:commitToken, host_side:hostSide, host_lane:hostLane, attachmentSlot:attachmentSlot, predicate:function(tc){return !!(tc&&(tc.card_id==='S1-ARC-002'||tc.card_id==='S1-ARC-007'));}, resolve_to:'triple_shot_bind', title:'Triple Shot — Choose Archer Attack', instruction:'Choose Poison Arrow or Burning Arrow in your hand. Until end of turn, that chosen card becomes an Area Attack over current Attack Coverage.', confirm_text:'Bind Triple Shot'});
    }
    return false;
  }
  function resolveVenomDetonation(state, action, post){
    var c=card(action.card_id), sourceHero=sideHeroes(state,action.source_side)[action.source_lane];
    var targetSide=action.target_side||targetSideForActor(c,action.source_side||'PLAYER')||'AI', affected=[];
    LANE_ORDER.forEach(function(lane){ var h=sideHeroes(state,targetSide)[lane]; if(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && hasStatus(h,'Poison')) affected.push(lane); });
    affected.sort(function(a,b){ return LANE_ORDER.indexOf(a)-LANE_ORDER.indexOf(b); });
    action=Object.assign({},action,{target_side:targetSide,venom_detonation:true,venom_multiplier:(heroClass(sourceHero)==='Renegade'?20:10),venom_damage_by_lane:{},venom_poison_duration_by_lane:{}});
    pushLog(state, cardName(c)+' opens a per-Hero Magical response sequence: '+(affected.length?affected.join(' → '):'no poisoned targets')+'.');
    if(affected.length){
      openMultiTargetResponseWindow(state,action,post||{},affected,0,0,false,0);
      if(targetSide==='AI' && !state.pvpHumanVsHuman) autoResolveCurrentAIResponseWindow(state);
      return affected.length;
    }
    finalizeCommittedSourceCard(state,action,action.source_side,action.card_id,'discard',{reason:'Venom Detonation had no legal poisoned target'});
    releaseHeldCardMotion(action.visual_hold_key,{type:'discard',side:action.source_side});
    state.pending=null;
    return 0;
  }
  function firstHealAmount(c){ var e=(Array.isArray(c.effect)&&c.effect.find(function(x){return x.kind==='heal' || x.kind==='heal_target_hero' || x.kind==='heal_allied_heroes' || x.kind==='heal_all_allied_heroes';})); return Number((e&&(e.amount||0))||0); }
  function firstDrawCount(c){
    var total=0;
    (Array.isArray(c&&c.effect)?c.effect:[]).forEach(function(e){
      if(e.kind==='draw_cards') total+=Number(e.count||e.raw_value||0);
      else if(e.kind==='attach_untargetable_and_draw') total+=Number(e.draw_count||e.count||0);
    });
    return Math.max(0,total);
  }
  function sourceShouldExhaust(c, sourceHero){
    if(!c) return true;
    // Item users/hosts do not Exhaust by default. Greater Health Potion and any
    // future Item that changes Exhaust do so only through an explicit effect payload.
    if(cardFamily(c)==='Item') return false;
    // Printed class-row lock: Elementalist pays the setup Exhaust and waits until next turn;
    // Elemental Lord is the only Double Casting row that explicitly does not Exhaust.
    if(isDoubleCastingCard(c)) return !sourceHero || heroClass(sourceHero)!=='Elemental Lord';
    if(isTripleShotCard(c)) return false;
    if(c.requirement && c.requirement.source_exhausts_after_successful_use===false) return false;
    if(Array.isArray(c.effect) && c.effect.some(function(e){ return e.kind==='remove_exhaust' || (e.kind==='source_structured_effect_json' && e.payload && e.payload.does_not_exhaust_user===true); })) return false;
    return true;
  }
  function attackType(c){
    var raw='';
    if(c && c.attack && c.attack.attack_type) raw=String(c.attack.attack_type);
    if(!raw && c && Array.isArray(c.effect)){ for(var i=0;i<c.effect.length;i++){ var e=c.effect[i]; if(e.kind==='deal_attack_damage' && e.attack_type){ raw=String(e.attack_type); break; } } }
    // Current canonical Runtime Data stores attack profile labels (Area Attack, Range Attack,
    // Casting Attack, Magical Attack, Physical Attack) in attack.attack_type. The application
    // reducer expects target-shape labels, so normalize without changing card subtype.
    if(/area\s*(attack|damage)/i.test(raw) || /area\s*attack/i.test(cardSubtype(c)||'')) return 'Area Damage';
    if(/all\s*target/i.test(raw)) return 'All Target';
    if(/dual\s*target/i.test(raw)) return 'Dual Target';
    if(/single\s*target|range\s*attack|casting\s*attack|magical\s*attack|physical\s*attack/i.test(raw)) return 'Single Target';
    return raw || (isAttackCard(c)?'Single Target':'');
  }
  function isSingleTargetAttack(c){ return attackType(c)==='Single Target'; }
  function isAreaAttack(c){ return attackType(c)==='Area Damage'; }
  function isAllTargetAttack(c){ return attackType(c)==='All Target'; }
  function hasCastingTimeOne(c){
    if(!c) return false;
    var text=JSON.stringify(c.effect_strings_by_class||{})+' '+summarizeCard(c)+' '+JSON.stringify(c.effect||[]);
    return /Casting\s*Time\s*1|Casting\s*time\s*1|casting_time_turns[^0-9]*1/i.test(text);
  }
  function hasCastingTimeOneForSource(c, sourceHero){
    if(!hasCastingTimeOne(c)) return false;
    if(c.effect_strings_by_class && sourceHero){
      var hc=heroClass(sourceHero), own=c.effect_strings_by_class[hc];
      if(typeof own==='string') return /Casting\s*Time\s*1|Casting\s*time\s*1/i.test(own);
      if(hc==='Saint' && c.effect_strings_by_class.Saint && !/Casting\s*Time\s*1|Casting\s*time\s*1/i.test(c.effect_strings_by_class.Saint)) return false;
    }
    return true;
  }
  function isDoubleCastingCard(c){ return c && c.card_id==='S1-MAG-018'; }
  function isCoverUpCard(c){ return c && c.card_id==='S1-WAR-004'; }
  function isFinalGritCard(c){ return c && c.card_id==='S1-WAR-014'; }
  function isChargeSwapCard(c){
    return !!(c && (
      c.card_id==='S1-THF-002' || c.card_id==='S1-THF-019' || c.card_id==='S1-THF-023' || c.card_id==='S1-THF-025' ||
      c.card_id==='S1-WAR-015' || c.card_id==='S1-WAR-024'
    ));
  }
  function isSourceFrontSwapCard(c){ return !!(c && ['S1-THF-002','S1-THF-019','S1-THF-025','S1-WAR-015','S1-WAR-024'].indexOf(c.card_id)!==-1); }
  function isAnyAlliedSwapCard(c){ return !!(c && c.card_id==='S1-THF-023'); }
  function cardEffectSwapModel(c){
    if(isSoulBlastShotCard(c)) return 'target_adjacent';
    if(isSourceFrontSwapCard(c)) return 'source_front';
    if(isAnyAlliedSwapCard(c)) return 'source_any_allied';
    if(isCoverUpCard(c)) return 'response_redirect';
    if(c && c.card_id==='S1-THF-022') return 'dodge_then_reposition';
    return null;
  }

  function adjacentLanes(lane){ if(lane==='LEFT') return ['CENTER']; if(lane==='CENTER') return ['LEFT','RIGHT']; if(lane==='RIGHT') return ['CENTER']; return []; }
  function finalGritTargetValid(hero){ return !!(hero && isLegacyModeHero(hero) && ['Gladiator','Conqueror'].indexOf(legacyOriginalHeroClass(hero))!==-1); }
  function removeStatus(hero,name){ if(!hero || !hero.statuses) return false; var before=hero.statuses.length; hero.statuses=hero.statuses.filter(function(s){ return statusName(s)!==name; }); return before!==hero.statuses.length; }
  function hasTaunt(state,hero){ return hasActiveAttachmentForHero(state,hero,'S1-WAR-005') && hero && Number(hero.hp||0)>0; }
  function tauntLaneFor(state, side, legalTargets){ var heroes=sideHeroes(state,side); var found=null; LANE_ORDER.forEach(function(lane){ if(found===null && legalTargets.indexOf(lane)!==-1 && hasTaunt(state,heroes[lane])) found=lane; }); return found; }
  function applyTauntTargeting(state, targetSide, legalTargets){ var t=tauntLaneFor(state,targetSide,legalTargets); if(t){ pushLog(state,'Taunt forces target selection to '+targetSide+' '+t+' because that Taunt source is legal.'); return [t]; } return legalTargets; }
  function doubleCastingStatusFor(hero){
    var att=activeAttachmentForHero(appState,hero,'S1-MAG-018');
    if(!att) return null;
    if(Number(att.active_from_round||0)>Number(appState&&appState.round||0)) return null;
    return att;
  }
  function hasDoubleCasting(hero){ return !!doubleCastingStatusFor(hero); }
  function consumeDoubleCasting(state, side, lane){
    var hero=sideHeroes(state,side)[lane]; if(!hero) return false; var had=releaseActiveAttachmentForHero(state,hero,'S1-MAG-018','Double Casting consumed');
    if(had) pushLog(state,'Double Casting is consumed and moves to Discard Pile.'); return had;
  }
  function doubleCastingLegalSecondTargets(state, action){
    if(!state || !action) return [];
    var targetSide=action.target_side||oppositeSide(action.source_side||'PLAYER');
    var source=sideHeroes(state,action.source_side||'PLAYER')[action.source_lane], att=source&&doubleCastingStatusFor(source);
    var legal=legalTargetLanesFor(state,action.source_side||'PLAYER',action.card_id,action.source_lane).filter(function(lane){
      var target=sideHeroes(state,targetSide)[lane];
      return target && Number(target.hp||0)>0 && !isLegacyModeHero(target);
    });
    /* FINAL printed text does not impose a same-target restriction on either
       Elementalist or Elemental Lord. Activation 2 may select any legal Hero. */
    return legal;
  }
  function chooseAIDoubleCastingTarget(state, action, legalTargets){
    var targetSide=action.target_side||oppositeSide(action.source_side||'AI');
    return (legalTargets||[]).slice().sort(function(a,b){
      var ah=sideHeroes(state,targetSide)[a], bh=sideHeroes(state,targetSide)[b];
      return Number(ah&&ah.hp||0)-Number(bh&&bh.hp||0) || LANE_ORDER.indexOf(a)-LANE_ORDER.indexOf(b);
    })[0]||null;
  }
  function finishDoubleCastingWithoutSecond(state, continuation, reason){
    if(!state || !continuation) return false;
    var rw=continuation.rw||{}, action=continuation.action||rw.action||{}, post=continuation.post||rw.post||{}, source=sideHeroes(state,action.source_side||rw.source_side)[action.source_lane||rw.source_lane];
    if(source){ consumeEnrageBonus(source); }
    finalizePlayedCardAfterResponse(state,rw);
    consumeDoubleCasting(state,action.source_side||rw.source_side,action.source_lane||rw.source_lane);
    state.responseWindow=null; if(!isDefeatGatePending(state.pending)) state.pending=null; closeResponseWindowUI();
    if(reason) pushLog(state,'Double Casting ends after the first activation: '+reason+'.');
    if(!state.pending) activateNextLegacyDefeatChoice(state);
    if(!state.gameOver && !isDefeatGatePending(state.pending)) checkGameEnd(state);
    if(!isDefeatGatePending(state.pending) && resolvePostAttackChoices(state,action,post,continuation.first_result||{})){ syncCounts(state); render(); return true; }
    if(resumeAITurnAfterPlayerResponse(state)) return true;
    syncCounts(state); render(); return true;
  }
  function commitDoubleCastingSecondTarget(state, pending, lane){
    if(!state || !pending || pending.type!=='double_casting_target_selection') return false;
    var legal=doubleCastingLegalSecondTargets(state,pending.action||{});
    if(legal.indexOf(lane)===-1){ showInfo('Illegal Target','Choose a highlighted legal target for the second Double Casting activation.'); return false; }
    var action=clone(pending.action||{}), post=clone(pending.post||{}), targetSide=action.target_side||oppositeSide(action.source_side||'PLAYER');
    action.target_side=targetSide; action.target_lane=lane; action.double_casting_activation=2;
    var source=sideHeroes(state,action.source_side)[action.source_lane], target=sideHeroes(state,targetSide)[lane];
    post.damage=computedAttackDamageForAction(state,action,source,target);
    post.double_casting=false; post.double_casting_stage=2; post.double_casting_first_result=clone(pending.first_result||{});
    state.pending=null; closeChoice();
    pushLog(state,'Double Casting activation 2/2: choose '+cardName(card(action.card_id))+' target '+targetSide+' '+lane+'. A new Response Window opens.');
    resolveAttackWithOwnerGating(state,action,post); syncCounts(state); render(); return true;
  }
  function openDoubleCastingSecondTargetSelection(state, continuation){
    if(!state || !continuation) return false;
    var action=clone(continuation.action||{}), legal=doubleCastingLegalSecondTargets(state,action);
    if(state.gameOver || !legal.length) return finishDoubleCastingWithoutSecond(state,continuation,state.gameOver?'the match ended':'no legal second target remains');
    var pending={type:'double_casting_target_selection',decision_side:action.source_side,source_side:action.source_side,source_lane:action.source_lane,target_side:action.target_side||oppositeSide(action.source_side),card_id:action.card_id,action:action,post:clone(continuation.post||{}),rw:clone(continuation.rw||{}),first_result:clone(continuation.first_result||{}),legal_targets:legal,commit_stage:'post_commit'};
    state.pending=pending; state.responseWindow=null; closeResponseWindowUI();
    if(action.source_side==='AI' && !state.pvpHumanVsHuman){ var chosen=chooseAIDoubleCastingTarget(state,action,legal); return commitDoubleCastingSecondTarget(state,pending,chosen); }
    pushLog(state,'Double Casting activation 1/2 completed. Choose any legal target for Double Casting activation 2/2.');
    syncCounts(state); render(); return true;
  }
  function cardEffectSwapObjectEligible(obj){ return !!(isActiveHeroSlot(obj) || isLegacySlot(obj)); }
  function cardEffectSwapPairEligible(first,second){
    if(!cardEffectSwapObjectEligible(first) || !cardEffectSwapObjectEligible(second)) return false;
    return !(isLegacySlot(first) && isLegacySlot(second));
  }
  function sourceSwapOptions(state, action){
    if(!state || !action) return [];
    var c=card(action.card_id), model=cardEffectSwapModel(c), heroes=sideHeroes(state,action.source_side)||{}, sourceLane=action.source_lane;
    var sourceHero=heroes[sourceLane];
    // Card-effect reposition is not manual Reposition. The acting object must still be an active Hero,
    // but a legal partner may be either an active Hero or Legacy. Freeze/Exhaust do not add a second movement gate.
    if(!isActiveHeroSlot(sourceHero)) return [];
    var candidates=[];
    if(model==='source_front'){
      var facing=action.target_lane;
      if(facing && facing!==sourceLane && cardEffectSwapPairEligible(sourceHero,heroes[facing])) candidates.push(facing);
    } else if(model==='source_any_allied'){
      LANE_ORDER.forEach(function(lane){
        if(lane!==sourceLane && cardEffectSwapPairEligible(sourceHero,heroes[lane])) candidates.push(lane);
      });
    }
    return candidates;
  }
  function matchingOppositeLane(action){
    if(!action) return null;
    var c=card(action.card_id), opts=sourceSwapOptions(appState,action);
    if(opts.length) return opts[0];
    if(isSourceFrontSwapCard(c) && action.target_lane && action.target_lane!==action.source_lane) return action.target_lane;
    return null;
  }
  function soulBlastTargetSwapOptions(state, action){
    if(!action || !isSoulBlastShotCard(card(action.card_id))) return [];
    var heroes=sideHeroes(state,action.target_side), lane=action.target_lane, target=heroes&&heroes[lane];
    if(!target || !cardEffectSwapObjectEligible(target)) return [];
    return adjacentLanes(lane).filter(function(l){
      return cardEffectSwapPairEligible(target,heroes[l]);
    });
  }
  function finalizeDeferredRepositionDiscard(state, holder, fallbackSide, fallbackCardId){
    holder=holder||{};
    if(holder.deferred_discard_done) return false;
    var cardId=holder.deferred_discard_card_id||fallbackCardId;
    var side=holder.deferred_discard_side||fallbackSide;
    if(!state || !cardId || !side) return false;
    finalizeCommittedSourceCard(state,holder,side,cardId,'discard',{reason:'post-attack reposition complete'});
    releaseHeldCardMotion(holder.visual_hold_key,{type:'discard',side:side});
    holder.deferred_discard_done=true;
    pushLog(state, cardName(card(cardId))+' moves to Discard after its reposition decision finishes.');
    return true;
  }

  function performOptionalTargetSwapDecision(lane){
    if(!appState || !appState.pending || appState.pending.type!=='optional_target_swap') return false;
    var p=appState.pending, heroes=sideHeroes(appState,p.target_side), a=p.target_lane, opts=p.swap_options||[], nextPending=p.next_pending?clone(p.next_pending):null;
    if(lane && opts.indexOf(lane)!==-1 && cardEffectSwapPairEligible(heroes[a],heroes[lane])){
      var targetId=heroIdFrom(heroes[a]), adjacentId=heroIdFrom(heroes[lane]);
      swapBoardStateLanes(appState,p.target_side,a,lane);
      pushLog(appState, cardName(card(p.card_id))+' force reposition resolved: '+cardName(card(targetId))+' '+a+' ↔ '+cardName(card(adjacentId))+' '+lane+'.');
    } else pushLog(appState, cardName(card(p.card_id))+' force reposition was skipped or no longer legal.');
    finalizeDeferredRepositionDiscard(appState,p,p.source_side,p.card_id);
    appState.pending=nextPending||null; closeChoice(); syncCounts(appState); if(appState.pending&&appState.pending.type==='legacy_defeat_choice') renderLegacyDefeatChoice(); render(); return true;
  }

  function pendingRevealsHiddenInformation(p){
    if(!p) return false;
    var zone=String(p.zone||p.choice_zone||'').toLowerCase();
    if(p.hidden_information_exposed===true) return true;
    if(p.type==='card_search_choice' && (zone==='deck'||zone==='legacy_deck')) return true;
    if(p.type==='legacy_card_choice' && (zone==='deck'||zone==='legacy_deck'||zone==='deck_top')) return true;
    if(p.type==='opponent_hand_choice' && (p.reveal_cards===true||p.cards_revealed===true)) return true;
    if(p.type==='crystal_ball_reorder') return true;
    return false;
  }
  function pendingActionCommitted(p){
    if(!p) return false;
    var stage=String(p.commit_stage||'').toLowerCase();
    return p.committed===true || stage==='committed' || stage==='post_commit' || stage==='resolved' || stage==='post_resolution';
  }
  function isCancelablePreCommitPending(p){
    if(!p || pendingRevealsHiddenInformation(p) || pendingActionCommitted(p) || (appState&&appState.responseWindow)) return false;
    return ['source_selection','target_selection','scouting_target_selection','scouting_exp_selection','exact_two_target_selection','mana_spend_choice','optional_magical_surge','status_removal_choice','tribute_target','racial_target_selection','hero_ability_target_selection','legacy_cost_selection','legacy_hero_target_selection','manual_reposition'].indexOf(p.type)!==-1;
  }
  function localPendingClosePolicy(state,p){
    if(!p) return 'CLOSE_ONLY';
    if(['optional_swap','optional_target_swap','post_attack_reposition_choice','racial_second_chance'].indexOf(p.type)!==-1) return 'POST_RESOLUTION_DECLINE';
    if(['legacy_defeat_choice','racial_stoneblood','saint_purify_choice','hand_limit_discard','draw_replacement_choice','crystal_ball_reorder','response_window'].indexOf(p.type)!==-1) return 'MANDATORY_NO_CANCEL';
    if(pendingRevealsHiddenInformation(p) || pendingActionCommitted(p) || (state&&state.responseWindow)) return 'MANDATORY_NO_CANCEL';
    if(isCancelablePreCommitPending(p)) return 'PRE_COMMIT_CANCEL';
    return 'MANDATORY_NO_CANCEL';
  }
  function clearPendingAfterResolvedAction(state){ if(state && state.pending && isCancelablePreCommitPending(state.pending)) state.pending=null; return state; }
  function manualRepositionAvailable(state){ return !!(matchStarted && state && state.turn==='PLAYER' && (state.phase==='Deploy'||state.phase==='Reform') && !state.pending && !state.responseWindow && !state.gameOver); }
  function repositionHeroEligible(h){ return !!(h && Number(h.hp||0)>0 && !isLegacyModeHero(h) && !h.exhausted && !hasStatus(h,'Freeze') && !hasStatus(h,'Stun')); }
  function repositionLegacyPartnerEligible(h){ return !!(h && isLegacySlot(h)); }
  function repositionPairEligible(a,b){
    var aHero=repositionHeroEligible(a), bHero=repositionHeroEligible(b);
    var aLegacy=repositionLegacyPartnerEligible(a), bLegacy=repositionLegacyPartnerEligible(b);
    // Legal pairs: Hero ↔ Hero, or Hero ↔ Legacy. Legacy ↔ Legacy is never legal.
    return !!((aHero&&bHero) || (aHero&&bLegacy) || (aLegacy&&bHero));
  }
  function manualRepositionButtonEnabled(state){ return manualRepositionAvailable(state) && manualRepositionPairs(state).length>0; }
  function repositionPairsForSide(state, side){ var h=sideHeroes(state,side)||{}; var pairs=[]; if(repositionPairEligible(h.LEFT,h.CENTER)) pairs.push(['LEFT','CENTER']); if(repositionPairEligible(h.CENTER,h.RIGHT)) pairs.push(['CENTER','RIGHT']); return pairs; }
  function manualRepositionPairs(state){ if(!manualRepositionAvailable(state)) return []; return repositionPairsForSide(state,'PLAYER'); }
  function swapBoardLanes(heroes,a,b){ var tmp=heroes[a]; heroes[a]=heroes[b]; heroes[b]=tmp; if(heroes[a]) heroes[a].lane=a; if(heroes[b]) heroes[b].lane=b; }
  function packageSlotsForSide(state, side){ return side==='PLAYER' ? (state.playerLegacyPackageSlots||[]) : (state.aiLegacyPackageSlots||[]); }
  function swapPackageSlotLanes(state, side, a, b){
    var slots=packageSlotsForSide(state,side); if(!slots||!slots.length) return;
    slots.forEach(function(slot){ if(!slot) return; if(slot.slot===a) slot.slot=b; else if(slot.slot===b) slot.slot=a; });
  }
  function swapActiveAttachmentLanes(state, side, a, b){
    (state.activeAttachments||[]).forEach(function(att){ if(!att || att.side!==side) return; if(att.lane===a) att.lane=b; else if(att.lane===b) att.lane=a; });
    /* Casting locks the original source position. Pending casting records intentionally do not move with Reposition; release validation cancels them. */
  }
  function swapBoardStateLanes(state, side, a, b){
    var heroes=sideHeroes(state,side); if(!heroes) return;
    swapBoardLanes(heroes,a,b);
    swapPackageSlotLanes(state,side,a,b);
    swapActiveAttachmentLanes(state,side,a,b);
  }
  function activeNonLegacyLanes(state, side){ var heroes=sideHeroes(state,side)||{}; return LANE_ORDER.filter(function(lane){ return isActiveHeroSlot(heroes[lane]); }); }
  function autoCenterOneVsOne(state){
    if(!state || state.gameOver || state.pending || state.responseWindow) return false;
    var p=activeNonLegacyLanes(state,'PLAYER'), a=activeNonLegacyLanes(state,'AI');
    if(p.length!==1 || a.length!==1) return false;
    var moved=[];
    [['PLAYER',p[0]],['AI',a[0]]].forEach(function(x){ var side=x[0], lane=x[1]; if(lane && lane!=='CENTER'){ swapBoardStateLanes(state,side,lane,'CENTER'); moved.push(side+' '+lane+' → CENTER'); } });
    if(moved.length){ pushLog(state,'Auto Center 1v1: '+moved.join('; ')+'. Legacy/empty lane objects swap out of CENTER without Exhaust.'); return true; }
    return false;
  }
  function laneThreatValueForSide(state, side, lane){ var opp=oppositeSide(side), heroes=sideHeroes(state,opp)||{}; var v=0; targetCoverage(lane).forEach(function(t){ var h=heroes[t]; if(h && Number(h.hp||0)>0 && !isLegacyModeHero(h)) v += (t===lane?3:1); }); return v; }
  function aiRepositionScore(state, pair){ var heroes=sideHeroes(state,'AI'), a=pair[0], b=pair[1]; var before=laneThreatValueForSide(state,'AI',a)+laneThreatValueForSide(state,'AI',b); swapBoardLanes(heroes,a,b); var after=laneThreatValueForSide(state,'AI',a)+laneThreatValueForSide(state,'AI',b); swapBoardLanes(heroes,a,b); return after-before; }
  function performAIReposition(state, pair){ if(!state || !pair || pair.length<2) return false; var pairs=repositionPairsForSide(state,'AI'); var ok=pairs.some(function(p){return p[0]===pair[0]&&p[1]===pair[1];}); if(!ok) return false; var h=sideHeroes(state,'AI'); swapBoardStateLanes(state,'AI',pair[0],pair[1]); h=sideHeroes(state,'AI'); var exhausted=[]; [pair[0],pair[1]].forEach(function(l){ var u=h[l]; if(repositionHeroEligible(u)){ u.exhausted=true; u.exhaust_reason='AI Reposition.'; exhausted.push(l); } }); var exhaustLine=exhausted.length===2?'Both moved Heroes Exhaust.':(exhausted.length===1?'Only the moved Hero Exhausts; Legacy does not Exhaust.':'No Hero Exhausted.'); pushLog(state,'AI Reposition: '+pair[0]+' ↔ '+pair[1]+'. '+exhaustLine); recordOpponentAction(state,'REPOSITION',null,'AI Reposition','AI repositioned '+pair[0]+' ↔ '+pair[1]+'.',{action_line:'Action: AI Reposition.', result_lines:['Swapped '+pair[0]+' and '+pair[1]+'.',exhaustLine]}); syncCounts(state); return true; }
  function openManualRepositionChoice(){
    if(!manualRepositionAvailable(appState)){ showInfo('Reposition','Manual Reposition is only available during your Deploy or Reform Phase when no action is pending.'); return false; }
    var pairs=manualRepositionPairs(appState);
    if(!pairs.length){ showInfo('Reposition','No legal adjacent Reposition pair is available. A pair must include at least one Ready Hero; Legacy can be a partner but cannot Reposition with another Legacy.'); return false; }
    appState.pending={type:'manual_reposition', decision_side:'PLAYER', source_side:'PLAYER', side:'PLAYER', pairs:pairs};
    renderManualRepositionChoice()
    return true;
  }
  function performManualReposition(pairText){
    if(!appState || !appState.pending || appState.pending.type!=='manual_reposition') return false;
    var parts=String(pairText||'').split('|'), a=parts[0], b=parts[1], legal=(appState.pending.pairs||[]).some(function(p){ return p[0]===a && p[1]===b; });
    if(!legal){ showInfo('Reposition','Illegal Reposition pair.'); return false; }
    var h=appState.playerHeroes; swapBoardStateLanes(appState,'PLAYER',a,b); h=appState.playerHeroes;
    var exhausted=[]; [a,b].forEach(function(l){ var u=h[l]; if(repositionHeroEligible(u)){ u.exhausted=true; u.exhaust_reason='PLAYER Reposition.'; exhausted.push(l); } });
    var exhaustLine=exhausted.length===2?'both Heroes Exhaust.':(exhausted.length===1?'only the Hero Exhausts; Legacy does not Exhaust.':'no Hero Exhausted.');
    pushLog(appState,'PLAYER Reposition: '+a+' ↔ '+b+'; '+exhaustLine);
    if(appState.pvpHumanVsHuman) recordPvpSideAction(appState,'PLAYER','REPOSITION',null,'Reposition','PLAYER repositioned '+a+' ↔ '+b+'.',{action_line:'Action: PLAYER repositioned '+a+' ↔ '+b+'.', result_lines:['Swapped '+a+' and '+b+'.',exhaustLine]});
    appState.pending=null; closeChoice(); syncCounts(appState); render(); return true;
  }
  function cancelManualReposition(){ if(appState && appState.pending && appState.pending.type==='manual_reposition'){ appState.pending=null; closeChoice(); render(); return true; } return false; }

  function renderManualRepositionChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='manual_reposition') return;
    var pairs=appState.pending.pairs||[];
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent='Reposition'; $('choiceConfirm').style.display='none';
    $('choiceBody').innerHTML='<p class="choice-instruction">Choose one adjacent pair to swap. Hero ↔ Hero Exhausts both Heroes. Hero ↔ Legacy Exhausts only the Hero. Legacy ↔ Legacy is illegal.</p><div class="choice-footer-buttons">'+pairs.map(function(p){ return '<button class="primary manualRepositionChoice" data-reposition-pair="'+esc(p[0]+'|'+p[1])+'" type="button">'+esc(p[0]+' ↔ '+p[1])+'</button>'; }).join('')+'<button id="manualRepositionCancel" type="button">Cancel</button></div>';
    $('choiceOverlay').classList.add('open');
  }
  function renderOptionalTargetSwapChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='optional_target_swap') return;
    var p=appState.pending, opts=p.swap_options||[];
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent='Soul Blast Shot — Force Reposition'; $('choiceConfirm').style.display='none';
    $('choiceBody').innerHTML='<p class="choice-instruction">'+esc(cardName(card(p.card_id)))+' hit. Choose an adjacent opponent Hero or Legacy to reposition with the affected slot. Hero ↔ Hero and Hero ↔ Legacy are legal; Legacy ↔ Legacy is not.</p><div class="choice-footer-buttons">'+opts.map(function(l){ return '<button class="primary optionalTargetSwap" data-target-swap-lane="'+esc(l)+'" type="button">Reposition with '+esc(l)+'</button>'; }).join('')+'<button id="optionalTargetSwapNo" type="button">Skip Reposition</button></div>';
    $('choiceOverlay').classList.add('open');
  }
  function renderOptionalSwapChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='optional_swap') return;
    var p=appState.pending, opts=(p.swap_options&&p.swap_options.length)?p.swap_options.slice():(p.swap_lane?[p.swap_lane]:[]);
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent='Swap after attack?'; $('choiceConfirm').style.display='none';
    var buttons=opts.map(function(lane){ return '<button class="primary optionalSourceSwap" data-source-swap-lane="'+esc(lane)+'" type="button">Swap with '+esc(lane)+'</button>'; }).join('');
    $('choiceBody').innerHTML='<p class="choice-instruction">'+esc(cardName(card(p.card_id)))+' may reposition the acting Hero with a legal allied Hero or Legacy.</p><div class="choice-footer-buttons">'+buttons+'<button id="optionalSwapNo" type="button">Do Not Swap</button></div>';
    $('choiceOverlay').classList.add('open');
  }

  function promptOptionalTargetSwap(state, action, nextPending){
    var opts=soulBlastTargetSwapOptions(state,action); if(!opts.length) return false;
    state.pending={type:'optional_target_swap', card_id:action.card_id, source_side:action.source_side, source_lane:action.source_lane, decision_side:action.source_side, target_side:action.target_side, target_lane:action.target_lane, swap_options:opts, next_pending:nextPending?clone(nextPending):null, deferred_discard_card_id:action.deferred_discard_card_id||action.card_id, deferred_discard_side:action.deferred_discard_side||action.source_side, visual_hold_key:action.visual_hold_key||(nextPending&&nextPending.visual_hold_key)||null, commit_token:action.commit_token||(nextPending&&nextPending.commit_token)||null};
    if(action.source_side==='AI' && !state.pvpHumanVsHuman && state===appState){
      pushLog(state,'AI chooses Soul Blast Shot reposition with '+opts[0]+'.');
      performOptionalTargetSwapDecision(opts[0]);
      return 'AUTO_RESOLVED';
    }
    renderOptionalTargetSwapChoice();
    pushLog(state,'Soul Blast Shot force reposition decision pending.'); return true;
  }
  function optionalSwapAvailable(state, action){
    if(!action || !isChargeSwapCard(card(action.card_id))) return false;
    return sourceSwapOptions(state,action).length>0;
  }
  function performOptionalSwapDecision(choice){
    if(!appState || !appState.pending || appState.pending.type!=='optional_swap') return false;
    var p=appState.pending, heroes=sideHeroes(appState,p.source_side), a=p.source_lane;
    var opts=(p.swap_options&&p.swap_options.length)?p.swap_options.slice():(p.swap_lane?[p.swap_lane]:[]);
    var b=null;
    if(choice===true) b=p.swap_lane||opts[0]||null;
    else if(typeof choice==='string' && opts.indexOf(choice)!==-1) b=choice;
    var nextPending=p.next_pending?clone(p.next_pending):null;
    if(b && opts.indexOf(b)!==-1 && isActiveHeroSlot(heroes[a]) && cardEffectSwapPairEligible(heroes[a],heroes[b])){
      var sourceId=heroIdFrom(heroes[a]), partnerId=heroIdFrom(heroes[b]);
      swapBoardStateLanes(appState,p.source_side,a,b);
      pushLog(appState, cardName(card(p.card_id))+' optional reposition resolved: '+cardName(card(sourceId))+' '+a+' ↔ '+cardName(card(partnerId))+' '+b+'.');
    } else pushLog(appState, cardName(card(p.card_id))+' optional reposition declined or no longer legal.');
    finalizeDeferredRepositionDiscard(appState,p,p.source_side,p.card_id);
    appState.pending=nextPending||null; closeChoice(); syncCounts(appState); if(appState.pending&&appState.pending.type==='legacy_defeat_choice') renderLegacyDefeatChoice(); render(); return true;
  }
  function promptOptionalSwap(state, action, nextPending){
    var opts=sourceSwapOptions(state,action); if(!opts.length) return false;
    state.pending={type:'optional_swap', decision_side:action.source_side, card_id:action.card_id, source_side:action.source_side, source_lane:action.source_lane, swap_lane:opts[0], swap_options:opts.slice(), next_pending:nextPending?clone(nextPending):null, deferred_discard_card_id:action.deferred_discard_card_id||action.card_id, deferred_discard_side:action.deferred_discard_side||action.source_side, visual_hold_key:action.visual_hold_key||(nextPending&&nextPending.visual_hold_key)||null, commit_token:action.commit_token||(nextPending&&nextPending.commit_token)||null};
    if(action.source_side==='AI' && !state.pvpHumanVsHuman && state===appState){
      pushLog(state,'AI chooses optional reposition with '+opts[0]+' for '+cardName(card(action.card_id))+'.');
      performOptionalSwapDecision(opts[0]);
      return 'AUTO_RESOLVED';
    }
    renderOptionalSwapChoice();
    pushLog(state,'Optional reposition decision pending for '+cardName(card(action.card_id))+': '+opts.join(', ')+'.'); return true;
  }
  function cleanupDefeatedHeroAttachments(state, side, lane){
    var hero=sideHeroes(state,side)[lane]; if(!hero || hero.hp>0) return;
    hero.attachments=hero.attachments||[null,null];
    var active=(state.activeAttachments||[]).slice();
    active.forEach(function(att){ if(att.side===side&&att.lane===lane) releaseLocalAttachment(state,att,'host defeated'); });
    hero.attachments.forEach(function(value,idx){
      var id=attachmentCardId(value); if(!id) return;
      if(!(state.activeAttachments||[]).some(function(a){return a.side===side&&a.lane===lane&&Number(a.slot)===idx;})) releaseLocalAttachmentAt(state,side,lane,idx,id,'host defeated recovery');
    });
  }
  function removeTemporaryBattleBuffsForSide(state, side){
    if(!state) return;
    pushLog(state, side+' Battle-only Attachment modifiers are checked for expiry.');
  }
  function expireBattleAttachments(state){
    var active=(state.activeAttachments||[]).slice();
    active.forEach(function(a){
      if(a.expires==='end_battle_phase' || a.expires==='after_next_attack_or_end_battle'){
        if(releaseLocalAttachment(state,a,'Battle Phase expiry')) pushLog(state, cardName(card(a.card_id))+' expires and moves to Discard Pile.');
      }
    });
  }
  function applyImmediateEventItemEffects(state, side, c, sourceHero, target, hostHero, action){
    if(isBeginAnewCard(c)){ var hand=sideHand(state,side), moved=hand.splice(0); deckForSide(state,side).push.apply(deckForSide(state,side),moved); shufflePile(deckForSide(state,side)); pushLog(state, cardName(c)+' shuffles remaining hand into deck ('+moved.length+' card(s)) then draws 5.'); drawMany(state,side,5,{reason:'CARD_EFFECT'}); if(side==='AI') appendOpponentEventResult(state,c.card_id,['AI shuffled remaining hand into deck ('+moved.length+' card(s)).','AI drew 5 cards.']); }
    if(isLastResortCard(c)){ var solo=LANE_ORDER.map(function(l){return sideHeroes(state,side)[l];}).filter(function(h){return h&&Number(h.hp||0)>0&&!isLegacyModeHero(h);}); if(solo.length===1){ action.attachment_effect={modifier_mode:'double_full_attack_card_damage',multiplier:2,active_from_round:Number(state.round||1)}; pushLog(state, cardName(c)+' Attachment doubles the remaining Hero’s full Attack Card damage this turn.'); } }
    if(isScoutingCard(c)){ var opp=oppositeSide(side), removed=false, targetLane=action&&action.target_lane, chosenIndex=(action&&typeof action.scouting_exp_index!=='undefined')?Number(action.scouting_exp_index):null; var lanes=targetLane?[targetLane]:LANE_ORDER.slice(); lanes.forEach(function(l){ if(removed) return; var eh=sideHeroes(state,opp)[l]; if(eh&&eh.exp_cards&&eh.exp_cards.length){ var idx=(chosenIndex!==null && l===targetLane && eh.exp_cards[chosenIndex] && !isUltimateCard(card(eh.exp_cards[chosenIndex]))) ? chosenIndex : eh.exp_cards.findIndex(function(id){ return !isUltimateCard(card(id)); }); if(idx>=0){ var eid=eh.exp_cards.splice(idx,1)[0]; eh.exp_total=Math.max(0,Number(eh.exp_total||0)-tributeExpValue(card(eid))); sideDiscard(state,opp).push(eid); removed=true; pushLog(state, cardName(c)+' discards '+cardName(card(eid))+' from opponent '+l+' EXP stack.'); } } }); }
    if(isForgedAllianceCard(c)){ var key=side==='PLAYER'?'racial':'aiRacial'; state[key]=Math.min(RACIAL_TOKEN_CAP,Number(state[key]||0)+1); pushLog(state, cardName(c)+' gains 1 Racial Token.'); }
    if(isManaCatalystCard(c)){ gainMana(state,side,1); pushLog(state, cardName(c)+' gains 1 Mana.'); }
    if(isCoordinationAttackCard(c)){ var bonus=activeAlliedBaseFamiliesDifferent(state,side)?20:10; action.attachment_effect={modifier_amount:bonus,scope:'allied_active_heroes'}; pushLog(state, cardName(c)+' Attachment grants allied active Heroes +'+bonus+' Physical or Magical Attack damage until end of Battle Phase.'); }
    if(isStaminaPotionCard(c) && target){ target.exhausted=false; target.stamina_potion_used_round=Number(state.round||0); pushLog(state, cardName(c)+' removes Exhausted from '+cardName(card(target.card_id))+' without granting an additional proactive Skill use this turn.'); }
    if(isGreaterHealthPotionCard(c) && target && Number(action&&action.heal_applied||0)>0){ target.exhausted=true; target.exhaust_reason='Greater Health Potion Exhausts healed Hero after successful heal item resolve.'; addStatus(target,'Potion Exhausted',1,target.exhaust_reason); }
    if(c && c.card_id==='S1-MAG-022' && sourceHero) pushLog(state, cardName(c)+' Attachment converts Physical Attacks used by '+cardName(card(sourceHero.card_id))+' to Magical while active.');
    if(c && c.card_id==='S1-THF-027' && sourceHero) pushLog(state, cardName(c)+' Attachment protects '+cardName(card(sourceHero.card_id))+' from Single Target attack selection; Area attacks still affect it.');
    if(isMagicScopeCard(c)){ var revealOpp=oppositeSide(side), oppHand=sideHand(state,revealOpp)||[], revealedHand=oppHand.slice(); shufflePile(oppHand); state.pending={type:'magic_scope_reveal',side:side,decision_side:side,source_card_id:c.card_id,commit_token:action.commit_token,revealed_cards:revealedHand}; pushLog(state, cardName(c)+' reveals opponent hand, then randomizes its hidden order. The Item remains pending until the viewer closes the reveal.'); if(!SUPPRESS_RENDER){showChoice('Magic Scope — Opponent Hand',revealedHand,'Close & Discard Magic Scope');} }
    if(isCrystalBallCard(c)){ var top=deckForSide(state,side).slice(0,3); pushLog(state, cardName(c)+' looks at top '+top.length+' card(s): '+top.map(function(id){return cardName(card(id));}).join(', ')+'.'); }
    if(hostHero && isHeroAttachmentItem(c)){ if(c.card_id==='S1-ITM-010') action.attachment_effect={modifier_amount:20,damage_type:'Magical',uses_actual_damage_type:true}; if(c.card_id==='S1-ITM-011') action.attachment_effect={modifier_type:'POISON_VIAL',poison_duration:2,requires_hp_damage:true}; if(c.card_id==='S1-ITM-013') action.attachment_effect={healing_received_modifier:20}; if(c.card_id==='S1-ITM-014') action.attachment_effect={modifier_amount:20,damage_types:['Physical','Magical'],uses_actual_damage_type:true}; if(c.card_id==='S1-ITM-015') action.attachment_effect={restriction_type:'UNTARGETABLE_BY_ATTACKS'}; pushLog(state, cardName(c)+' attaches to '+cardName(card(hostHero.card_id))+' and applies its marker.'); }
  }
  function commitActionLifecycle(state, side, action, resolveFn){
    var gate=validateAction(state,side,action,{committingPending:true});
    if(!gate.ok) return {ok:false, reasons:gate.reasons};
    return resolveFn ? resolveFn() : {ok:true};
  }

  function legacyAbilityData(c){
    if(!c || !c.ability || !c.ability.action_key) return null;
    return clone(c.ability);
  }
  function legacyAbilityActionKey(c){ var a=legacyAbilityData(c); return a ? String(a.action_key||'') : ''; }
  function legacyAbilityTimingMatches(state, ability){ return !!(state && (state.phase==='Deploy' || state.phase==='Reform')); }
  function legacyOnceKey(state, side, lane, ability){ return [side,lane,ability&&ability.action_key,state&&state.turn,state&&state.round].join('|'); }
  function legacyAbilityUsedThisTurn(state, side, lane, ability){ var h=state&&sideHeroes(state,side)[lane]; return !!(h && h.legacy_ability_used_key===legacyOnceKey(state,side,lane,ability)); }
  function markLegacyAbilityUsed(state, side, lane, ability){ var h=state&&sideHeroes(state,side)[lane]; if(h) h.legacy_ability_used_key=legacyOnceKey(state,side,lane,ability); }
  function classPrefixForLegacyFamily(fam){ return {Warrior:'WAR',Mage:'MAG',Cleric:'CLE',Thief:'THF',Archer:'ARC'}[String(fam||'')] || String(fam||'').slice(0,3).toUpperCase(); }
  function legacyCostSpec(ability){ return ability && ability.cost && ability.cost.discard_from_hand ? ability.cost.discard_from_hand : null; }
  function legacyCostFamily(ability){ var f=legacyCostSpec(ability); return f && f.filter && f.filter.class_family; }
  function legacyCostCount(ability){ var f=legacyCostSpec(ability); return Number((f&&f.count)||0); }
  function isLegacyCostCard(id, ability){ var c=card(id), spec=legacyCostSpec(ability), filter=(spec&&spec.filter)||{}; if(!spec) return false; if(filter.card_type && cardFamily(c)!==filter.card_type) return false; var fam=filter.class_family; if(fam){ var pref=classPrefixForLegacyFamily(fam); if(String(c.card_id||'').indexOf('S1-'+pref+'-')!==0) return false; } return true; }
  function legacyCostCandidates(state, side, ability){ var hand=sideHand(state,side)||[], out=[]; hand.forEach(function(id,idx){ if(isLegacyCostCard(id,ability)) out.push({card_id:id, hand_index:idx}); }); return out; }
  function payLegacyAbilityCost(state, side, ability, selectedIndexes){ var need=legacyCostCount(ability), hand=sideHand(state,side), discard=sideDiscard(state,side), chosen=(selectedIndexes||[]).slice().sort(function(a,b){return b-a;}), paid=[]; if(chosen.length!==need) return {ok:false,paid:paid}; for(var i=0;i<chosen.length;i++){var idx=Number(chosen[i]);if(!(idx>=0&&idx<hand.length&&isLegacyCostCard(hand[idx],ability)))return{ok:false,paid:paid};} chosen.forEach(function(idx){var n=Number(idx),motion=captureHandDiscardMotion(side,n,hand[n]);paid.unshift(hand.splice(n,1)[0]);queueHandDiscardMotion(motion);}); discard.push.apply(discard,paid); if(paid.length)pushLog(state,'Legacy cost paid: discarded '+paid.map(function(id){return cardName(card(id));}).join(', ')+'.'); return {ok:true,paid:paid}; }
  function legacyResultCount(ability){ return Number(ability&&ability.effect&&ability.effect.count||0); }
  function legacyDiscardReturnCandidates(state, side, ability, costPaid){
    var pile=sideDiscard(state,side)||[], preLen=pile.length-(costPaid||[]).length, out=[], eff=ability&&ability.effect||{}, filter=eff.filter||{};
    for(var idx=0;idx<Math.max(0,preLen);idx++){
      var id=pile[idx],c=card(id), fam=cardFamily(c);
      if(filter.card_family && fam!==filter.card_family) continue;
      if(filter.family && fam!==filter.family) continue;
      if(filter.exclude_ultimate && isUltimateCard(c)) continue;
      out.push({card_id:id,discard_index:idx});
    }
    return out;
  }
  function legacyDeckSearchCandidates(state, side, ability){
    var deck=deckForSide(state,side)||[], out=[], eff=ability&&ability.effect||{}, filter=eff.filter||{};
    deck.forEach(function(id,idx){
      var c=card(id), cls=String(c&&c.classification||c&&c.action_category||'');
      if(filter.exclude_ultimate && isUltimateCard(c)) return;
      if(filter.card_family && cardFamily(c)!==filter.card_family) return;
      if(filter.any_card || filter.anyCard) { out.push({card_id:id,deck_index:idx}); return; }
      var profiles=filter.action_profile_any||filter.profiles||(filter.action_profile?[filter.action_profile]:[]);
      if(profiles.length && profiles.indexOf(cls)===-1) return;
      out.push({card_id:id,deck_index:idx});
    });
    return out;
  }
  function legacyAbilityResourceAvailable(state, side, ability){ return !((ability&&ability.action_key)==='hidden_archives' && Number(state&&state[racialTokenKey(side)]||0)>=RACIAL_TOKEN_CAP); }
  function legacyAbilityResourceBlockedReason(state, side, ability){ if((ability&&ability.action_key)==='hidden_archives'&&Number(state&&state[racialTokenKey(side)]||0)>=RACIAL_TOKEN_CAP)return 'Racial Token pool is already full.';return 'Legacy Ability is not currently legal.'; }
  function legacyAbilityNeedsDiscardChoice(ability){ var t=ability&&ability.effect&&ability.effect.type; return t==='RETURN_DISCARD_TO_DECK'||t==='RETURN_DISCARD_TO_HAND'; }
  function legacyAbilityNeedsDeckChoice(ability){ return !!(ability&&ability.effect&&ability.effect.type==='SEARCH_DECK_TO_HAND'); }
  function getActivatedLegacyAbilities(state, side, lane){
    if(window.GL_PVP_SHARED_BOARD_ACTIVE && side==='PLAYER' && state && state.pvpLocalLegalLegacyAbilities && Object.prototype.hasOwnProperty.call(state.pvpLocalLegalLegacyAbilities,lane)) return clone(state.pvpLocalLegalLegacyAbilities[lane]||[]);
    var h=state&&sideHeroes(state,side)[lane],c=card(heroIdFrom(h)),ability=legacyAbilityData(c),out=[];if(!state||!h||!isLegacySlot(h)||!ability||state.turn!==side||!legacyAbilityTimingMatches(state,ability))return out;if(ability.once_per_turn&&legacyAbilityUsedThisTurn(state,side,lane,ability))return out;if(!legacyAbilityResourceAvailable(state,side,ability))return out;var need=legacyCostCount(ability);if(legacyCostCandidates(state,side,ability).length<need)return out;var key=ability.action_key,count=legacyResultCount(ability);if(legacyAbilityNeedsDiscardChoice(ability)&&legacyDiscardReturnCandidates(state,side,ability,[]).length<count)return out;if(legacyAbilityNeedsDeckChoice(ability)&&!legacyDeckSearchCandidates(state,side,ability).length)return out;out.push({abilityId:key,label:cardName(c),ability:ability,cost_count:need,cost_family:legacyCostFamily(ability)});return out;
  }
  function beginActivatedLegacyAbility(state, side, lane, abilityId){ var h=state&&sideHeroes(state,side)[lane],c=card(heroIdFrom(h)),a=getActivatedLegacyAbilities(state,side,lane).find(function(x){return x.abilityId===abilityId;});if(!a){showInfo('Legacy Ability',legacyAbilityResourceBlockedReason(state,side,legacyAbilityData(c)));return false;}var candidates=legacyCostCandidates(state,side,a.ability),need=legacyCostCount(a.ability);state.pending={type:'legacy_cost_selection',side:side,decision_side:side,source_side:side,source_lane:lane,card_id:c.card_id,abilityId:abilityId,ability:a.ability,cost_candidates:candidates,selected_cost_indices:[],required_cost:need};pushLog(state,'Choose '+need+' '+(a.cost_family||'matching')+' Skill card(s) to discard for '+cardName(c)+'.');renderLegacyCostChoice();render();return true; }
  function renderLegacyCostChoice(){ if(!appState||!appState.pending||appState.pending.type!=='legacy_cost_selection')return;var p=appState.pending,selected=p.selected_cost_indices||[],need=Number(p.required_cost||1),ids=(p.cost_candidates||[]).map(function(x){return x.card_id;});if(!pvpLocalOwnsPending(appState.pending)){pvpHideChoiceForNonOwner();return;}if(SUPPRESS_RENDER||!$('choiceOverlay'))return;$('choiceTitle').textContent='Legacy Cost — '+cardName(card(p.card_id));$('choiceConfirm').style.display='';$('choiceConfirm').textContent='Pay Cost';$('choiceConfirm').disabled=selected.length!==need;$('choiceBody').innerHTML='<p class="choice-instruction">Discard exactly '+need+' matching Skill card(s) from hand.</p><div class="choice-grid card-search-choice-grid">'+ids.map(function(id,idx){var sel=selected.indexOf(idx)!==-1;return '<article class="card-search-choice '+(sel?'selected':'')+'"><button class="discard-preview" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(card(id)))+' thumbnail"><span>'+esc(cardName(card(id)))+'</span></button><button class="discard-select" type="button" data-legacy-cost-index="'+idx+'">'+(sel?'Selected':'Select')+'</button></article>';}).join('')+'</div>';$('choiceOverlay').classList.add('open'); }
  function selectLegacyCostChoice(idx){if(!appState||!appState.pending||appState.pending.type!=='legacy_cost_selection')return false;var p=appState.pending,need=Number(p.required_cost||1);idx=Number(idx);if(idx<0||idx>=(p.cost_candidates||[]).length)return false;p.selected_cost_indices=p.selected_cost_indices||[];var pos=p.selected_cost_indices.indexOf(idx);if(pos!==-1)p.selected_cost_indices.splice(pos,1);else if(p.selected_cost_indices.length<need)p.selected_cost_indices.push(idx);renderLegacyCostChoice();return true;}
  function commitLegacyCostChoice(){if(!appState||!appState.pending||appState.pending.type!=='legacy_cost_selection')return false;var p=appState.pending,chosen=(p.selected_cost_indices||[]).map(function(i){return(p.cost_candidates||[])[i]&&(p.cost_candidates||[])[i].hand_index;}).filter(function(v){return typeof v==='number';});var pay=payLegacyAbilityCost(appState,p.side,p.ability,chosen);if(!pay.ok){showInfo('Legacy Cost','Selected cost cards are no longer legal.');return false;}appState.pending=null;closeChoice();return continueLegacyAbilityAfterCost(appState,{side:p.side,source_lane:p.source_lane,card_id:p.card_id,ability:p.ability,cost_paid:pay.paid||[]});}
  function continueLegacyAbilityAfterCost(state,ctx){var ability=ctx.ability,count=legacyResultCount(ability);if(legacyAbilityNeedsDiscardChoice(ability)){var choices=legacyDiscardReturnCandidates(state,ctx.side,ability,ctx.cost_paid||[]);state.pending={type:'legacy_card_choice',choice_zone:'discard',side:ctx.side,decision_side:ctx.side,source_side:ctx.side,source_lane:ctx.source_lane,card_id:ctx.card_id,abilityId:ability.action_key,ability:ability,candidates:choices,selected_indices:[],required_choices:count,cost_paid:ctx.cost_paid||[]};pushLog(state,'Choose '+count+' pre-cost Discard Pile card(s) for '+cardName(card(ctx.card_id))+'.');renderLegacyCardChoice();render();return true;}if(legacyAbilityNeedsDeckChoice(ability)){var deckChoices=legacyDeckSearchCandidates(state,ctx.side,ability);state.pending={type:'legacy_card_choice',choice_zone:'deck_search',side:ctx.side,decision_side:ctx.side,source_side:ctx.side,source_lane:ctx.source_lane,card_id:ctx.card_id,abilityId:ability.action_key,ability:ability,candidates:deckChoices,selected_indices:[],required_choices:1,cost_paid:ctx.cost_paid||[]};pushLog(state,'Search and choose 1 card for '+cardName(card(ctx.card_id))+'.');renderLegacyCardChoice();render();return true;}return resolveLegacyAbility(state,ctx.side,ctx.source_lane,ability.action_key,{cost_paid:ctx.cost_paid||[]});}
  function renderLegacyCardChoice(){if(!appState||!appState.pending||appState.pending.type!=='legacy_card_choice')return;var p=appState.pending,selected=p.selected_indices||[],need=Number(p.required_choices||1),ids=(p.candidates||[]).map(function(x){return x.card_id;});if(!pvpLocalOwnsPending(appState.pending)){pvpHideChoiceForNonOwner();return;}if(SUPPRESS_RENDER||!$('choiceOverlay'))return;$('choiceTitle').textContent='Legacy Choice — '+cardName(card(p.card_id));$('choiceConfirm').style.display='';$('choiceConfirm').textContent='Confirm Legacy Choice';$('choiceConfirm').disabled=selected.length!==need;$('choiceBody').innerHTML='<p class="choice-instruction">Choose exactly '+need+' card(s).</p><div class="choice-grid card-search-choice-grid">'+ids.map(function(id,idx){var sel=selected.indexOf(idx)!==-1;return '<article class="card-search-choice '+(sel?'selected':'')+'"><button class="discard-preview" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(card(id)))+' thumbnail"><span>'+esc(cardName(card(id)))+'</span></button><button class="discard-select" type="button" data-legacy-card-choice="'+idx+'">'+(sel?'Selected':'Select')+'</button></article>';}).join('')+'</div>';$('choiceOverlay').classList.add('open');}
  function selectLegacyCardChoice(idx){if(!appState||!appState.pending||appState.pending.type!=='legacy_card_choice')return false;var p=appState.pending,need=Number(p.required_choices||1);idx=Number(idx);if(idx<0||idx>=(p.candidates||[]).length)return false;p.selected_indices=p.selected_indices||[];var pos=p.selected_indices.indexOf(idx);if(pos!==-1)p.selected_indices.splice(pos,1);else if(p.selected_indices.length<need)p.selected_indices.push(idx);renderLegacyCardChoice();return true;}
  function commitLegacyCardChoice(){if(!appState||!appState.pending||appState.pending.type!=='legacy_card_choice')return false;var p=appState.pending,chosen=(p.selected_indices||[]).map(function(i){return(p.candidates||[])[i];}).filter(Boolean);if(chosen.length!==Number(p.required_choices||1)){showInfo('Legacy Choice','Choose the required number of legal cards.');return false;}appState.pending=null;closeChoice();return resolveLegacyAbility(appState,p.side,p.source_lane,p.abilityId,{choices:chosen,choice_zone:p.choice_zone,cost_paid:p.cost_paid||[]});}
  function resolveLegacyAbility(state,side,lane,abilityId,payload){
    payload=payload||{};var h=state&&sideHeroes(state,side)[lane],c=card(heroIdFrom(h)),ability=legacyAbilityData(c);
    if(!state||!h||!isLegacyModeHero(h)||!ability||ability.action_key!==abilityId){showInfo('Legacy Ability','Legacy Ability is no longer legal.');return false;}
    if(!legacyAbilityTimingMatches(state,ability)){showInfo('Legacy Ability','Legacy Ability is legal only during Deploy or Reform Phase.');return false;}
    if(ability.once_per_turn&&legacyAbilityUsedThisTurn(state,side,lane,ability)){showInfo('Legacy Ability','This Legacy ability was already used this turn.');return false;}
    var eff=ability.effect||{},type=eff.type,count=Number(eff.count||0),choices=payload.choices||[],deck=deckForSide(state,side),pile=sideDiscard(state,side),hand=sideHand(state,side),picked=[];
    if(type==='DRAW_CARDS'){
      drawMany(state,side,count,{reason:'CARD_EFFECT'});
    }else if(type==='GAIN_RACIAL_TOKEN'){
      var rk=racialTokenKey(side),before=Number(state[rk]||0),cap=Number(eff.token_cap||(eff.filter&&eff.filter.token_cap)||2);
      if(before>=cap){showInfo('Legacy Ability','Racial Token pool is already full.');return false;}
      state[rk]=Math.min(cap,before+count);pushLog(state,cardName(c)+' gains '+Math.min(count,cap-before)+' Racial Token.');
    }else if(type==='SEARCH_DECK_TO_HAND'){
      var dc=choices[0],di=dc&&Number(dc.deck_index);if(!dc){showInfo('Legacy Ability','Choose a legal Main Deck card.');return false;}
      if(deck[di]!==dc.card_id)di=deck.indexOf(dc.card_id);if(di<0){showInfo('Legacy Ability','Chosen deck card is no longer available.');return false;}
      var found=deck.splice(di,1)[0];hand.push(found);shufflePile(deck);pushLog(state,cardName(c)+' adds '+cardName(card(found))+' to hand and shuffles the Main Deck under the hidden-information rule.');
    }else if(type==='RETURN_DISCARD_TO_DECK'||type==='RETURN_DISCARD_TO_HAND'){
      choices.slice().sort(function(a,b){return Number(b.discard_index)-Number(a.discard_index);}).forEach(function(ch){var idx=Number(ch.discard_index);if(pile[idx]===ch.card_id)picked.unshift(pile.splice(idx,1)[0]);});
      if(picked.length!==choices.length||picked.length!==count){showInfo('Legacy Ability','Chosen pre-cost Discard Pile cards are no longer available.');return false;}
      if(type==='RETURN_DISCARD_TO_HAND'){hand.push.apply(hand,picked);pushLog(state,cardName(c)+' returns '+picked.map(function(id){return cardName(card(id));}).join(', ')+' to hand.');}
      else{deck.push.apply(deck,picked);shufflePile(deck);pushLog(state,cardName(c)+' returns '+picked.map(function(id){return cardName(card(id));}).join(', ')+' to Main Deck and shuffles.');}
    }else{showInfo('Legacy Ability','No latest resolver for '+abilityId+'.');return false;}
    markLegacyAbilityUsed(state,side,lane,ability);pushLog(state,side+' uses Legacy Ability '+cardName(c)+'.');
    if(state.pvpHumanVsHuman)recordPvpSideAction(state,side,'LEGACY',c.card_id,cardName(c),side+' uses Legacy Ability '+cardName(c)+'.',{action_line:'Action: '+side+' used Legacy Ability '+cardName(c)+'.',source_side:side,source_lane:lane,result_lines:['Legacy ability resolved.']});
    state.pending=null;syncCounts(state);render();return true;
  }
  function hasLegacyResponseCost(){return false;}
  function activeLegacyResponseOptionsFor(){return [];}
  function resolveLegacyResponseOption(){return {ok:false};}

function getActivatedHeroAbilities(state, side, lane){
    var h=state&&sideHeroes(state,side)[lane], out=[]; if(!h || isLegacyModeHero(h) || h.exhausted || hasStatus(h,'Stun') || state.phase!=='Deploy') return out;
    if(h.class_ability_used_turn===runtimeTurnStamp(state)) return out;
    var hc=heroClass(h);
    if((hc==='Paladin'||hc==='Crusader') && Number(state[manaPoolKeyForSide(side)]||0)>=1){
      var damaged=LANE_ORDER.filter(function(l){ var t=sideHeroes(state,side)[l]; return t && Number(t.hp||0)>0 && Number(t.hp||0)<Number(t.maxHp||100) && !hasStatus(t,'Bleed') && !isLegacyModeHero(t); });
      if(damaged.length) out.push({abilityId:'paladin_crusader_heal', label:'Heal +'+(hc==='Crusader'?20:10), targets:damaged});
    }
    return out;
  }
  function validateActivatedHeroAbility(state, side, lane, abilityId, targetLane){
    var a=getActivatedHeroAbilities(state,side,lane).find(function(x){return x.abilityId===abilityId;});
    if(!a) return {ok:false, reasons:['ability not legal']};
    if(a.targets.indexOf(targetLane)===-1) return {ok:false, reasons:['target not legal']};
    return {ok:true, reasons:[]};
  }
  function beginActivatedHeroAbility(state, side, lane, abilityId){
    var a=getActivatedHeroAbilities(state,side,lane).find(function(x){return x.abilityId===abilityId;});
    if(!a){ showInfo('Class Ability','Class Ability is not currently legal.'); return false; }
    state.pending={type:'hero_ability_target_selection', side:side, decision_side:side, source_side:side, source_lane:lane, abilityId:abilityId, target_side:side, legal_targets:a.targets.slice()};
    pushLog(state,'Choose target for Class Ability '+a.label+'.'); render(); return true;
  }
  function resolveActivatedHeroAbility(state, side, lane, abilityId, targetLane){
    var v=validateActivatedHeroAbility(state,side,lane,abilityId,targetLane); if(!v.ok){ showInfo('Class Ability',v.reasons.join('\n')); return false; }
    var source=sideHeroes(state,side)[lane], target=sideHeroes(state,side)[targetLane]; state[manaPoolKeyForSide(side)]=Math.max(0,Number(state[manaPoolKeyForSide(side)]||0)-1);
    var amount=heroClass(source)==='Crusader'?20:10;
    healHeroIfLegal(state,target,amount,card(source.card_id),source,'Paladin/Crusader activated ability');
    source.exhausted=true; source.exhaust_reason='Activated class heal ability used.'; source.class_ability_used_turn=runtimeTurnStamp(state);
    var classAbilityName=(heroClass(source)==='Crusader'?'Radiant Oblivion':'Holy Resurgence');
    pushLog(state, side+' uses Class Ability '+classAbilityName+' from '+cardName(card(source.card_id))+' to heal '+targetLane+' by '+amount+'.');
    if(state.pvpHumanVsHuman) recordPvpSideAction(state,side,'ABILITY',source.card_id,classAbilityName,side+' uses Class Ability '+classAbilityName+'.',{action_line:'Action: '+side+' used Class Ability '+classAbilityName+'.', source_side:side, source_lane:lane, target_side:side, target_lane:targetLane, result_lines:['Healed '+targetLane+' by '+amount+'.','Source Hero Exhausted.']});
    state.pending=null; activateNextSaintPurifyChoice(state); syncCounts(state); render(); return true;
  }
  function racialTokenKey(side){ return side==='PLAYER'?'racial':'aiRacial'; }
  function getRacialAbilityTargetLanes(state, side, lane, ability){
    var act=ability&&ability.action, target=act&&act.target, out=[]; if(!target) return out;
    if(target.type==='injured_opponent_hero'){
      var opp=oppositeSide(side); LANE_ORDER.forEach(function(l){ var h=sideHeroes(state,opp)[l]; if(h && Number(h.hp||0)>0 && Number(h.hp||0)<Number(h.maxHp||100) && !isLegacyModeHero(h)) out.push(l); });
    }
    return out;
  }
  function isDeployActivatedRacialAction(race, act){
    if(!race || !act) return false;
    var key=String(act.action_key||'').toLowerCase();
    if(key==='ancestral_focus' || key==='human_ambition') return true;
    if(key==='primal_strike') return !!(act.phase && /Battle/i.test(String(act.phase)));
    if(act.trigger || act.response_window) return false;
    if(/response|passive|trigger/i.test(String(act.type||''))) return false;
    return !!(act.phase && act.cost && Number((act.cost&&act.cost.racial_token)||0)>0 && act.effect);
  }
  function getActivatedRacialAbilities(state, side, lane){
    var h=state&&sideHeroes(state,side)[lane], c=card(heroIdFrom(h)), race=c&&c.racial_ability, act=race&&race.action; if(!state || !h || isLegacyModeHero(h) || !act) return []; if(racialAbilityUsedThisTurnByHero(state,h)) return [];
    if(!isDeployActivatedRacialAction(race,act)) return [];
    var key=String(act.action_key||'').toLowerCase(); var phase=(key==='ancestral_focus'||key==='human_ambition')?'Deploy':(key==='primal_strike'?'Battle':String(act.phase||'Deploy Phase').replace(/\s*Phase$/,'')); if(state.phase!==phase) return [];
    var cost=Number((act.cost&&act.cost.racial_token)||0); if(Number(state[racialTokenKey(side)]||0)<cost) return [];
    var ability={abilityId:act.action_key||race.name||'racial_ability', label:race.name||'Racial', cost:cost, text:race.text||'', action:act, does_exhaust:race.does_exhaust===true};
    if(act.target){ ability.target_side=oppositeSide(side); ability.legal_targets=getRacialAbilityTargetLanes(state,side,lane,ability); if(!ability.legal_targets.length) return []; }
    return [ability];
  }
  function validateActivatedRacialAbility(state, side, lane, abilityId, targetLane){
    var a=getActivatedRacialAbilities(state,side,lane).find(function(x){ return x.abilityId===abilityId; }); if(!a) return {ok:false, reasons:['racial ability not legal']};
    if(a.action&&a.action.target && (!targetLane || a.legal_targets.indexOf(targetLane)===-1)) return {ok:false, ability:a, reasons:['racial ability target not legal']};
    return {ok:true, ability:a, reasons:[]};
  }
  function beginActivatedRacialAbility(state, side, lane, abilityId){
    var a=getActivatedRacialAbilities(state,side,lane).find(function(x){ return x.abilityId===abilityId; });
    if(!a){ showInfo('Racial Ability','Racial Ability is not currently legal.'); return false; }
    if(a.action&&a.action.target){ state.pending={type:'racial_target_selection', side:side, decision_side:side, source_side:side, source_lane:lane, abilityId:abilityId, target_side:a.target_side, legal_targets:a.legal_targets.slice()}; pushLog(state,'Choose target for Racial Ability '+a.label+'.'); render(); return true; }
    return resolveActivatedRacialAbility(state,side,lane,abilityId,null);
  }
  function resolveActivatedRacialAbility(state, side, lane, abilityId, targetLane){
    var v=validateActivatedRacialAbility(state,side,lane,abilityId,targetLane); if(!v.ok){ showInfo('Racial Ability',v.reasons.join('\n')); return false; }
    var a=v.ability, h=sideHeroes(state,side)[lane], eff=(a.action&&a.action.effect)||{}; state[racialTokenKey(side)]=Math.max(0,Number(state[racialTokenKey(side)]||0)-Number(a.cost||0)); markRacialAbilityUsedThisTurn(state,h);
    if(eff.gain_mana) gainMana(state,side,Number(eff.gain_mana||0));
    if(eff.draw_cards){ drawMany(state,side,Number(eff.draw_cards||0),{reason:'CARD_EFFECT'}); }
    if(eff.heal){ healHeroIfLegal(state,h,Number(eff.heal||0),card(h.card_id),h,'racial ability'); }
    if(String(abilityId).toLowerCase()==='primal_strike' && eff.direct_damage && targetLane){
      pushLog(state,side+' uses Primal Strike from '+cardName(card(h.card_id))+', spends 1 Racial Token, and declares 20 Physical damage.'); openPrimalStrikeResponseWindow(state,side,lane,targetLane,a);
      if(!state.pvpHumanVsHuman && oppositeSide(side)==='AI'){ var opt=(state.responseWindow.options||[])[0]||null; return resolveAbilityDamageResponseWindow(opt); }
      syncCounts(state); render(); return true;
    }
    if(eff.direct_damage && targetLane){ var tgtSide=oppositeSide(side), tgt=sideHeroes(state,tgtSide)[targetLane]; if(tgt){ var directBlocked=!!activeAttachmentForSide(state,tgtSide,'S1-CLE-025'), directAmount=directBlocked?0:Number(eff.direct_damage||0); tgt.hp=Math.max(0,Number(tgt.hp||0)-directAmount); pushLog(state,directBlocked?'Blessing of Divinity prevents '+a.label+' damage to '+tgtSide+' '+targetLane+'.':a.label+' deals '+directAmount+' damage to '+tgtSide+' '+targetLane+'.'); if(directAmount>0)applyDefeatCheck(state,tgtSide,targetLane); } }
    if(a.does_exhaust){ h.exhausted=true; h.exhaust_reason='Racial Ability used.'; }
    pushLog(state, side+' uses Racial Ability '+a.label+' from '+cardName(card(h.card_id))+'.'); state.pending=null; activateNextSaintPurifyChoice(state); syncCounts(state); render(); return true;
  }
  function isCastingCard(c){
    if(!c) return false;
    var sub=String(cardSubtype(c)||c.action_category||c.classification||'');
    var tags=runtimeTagList(c).map(function(t){return t.toUpperCase();});
    var x=c.canonical_execution||{}, ap=c.attachment_policy||(c.lifecycle&&c.lifecycle.attachment_policy)||x.attachment_policy||{};
    return /Casting/i.test(sub) || tags.indexOf('CASTING_ATTACK')!==-1 || tags.indexOf('CASTING')!==-1 || !!x.casting_delay || /casting/i.test(String(ap.role||''));
  }
  function isRangeAttackCard(c){
    if(!c) return false;
    var sub=String(cardSubtype(c)||c.action_category||c.classification||'');
    var tags=runtimeTagList(c).map(function(t){return t.toUpperCase();});
    var target=(c.canonical_execution&&c.canonical_execution.targeting)||c.targeting||{};
    return /Range\s+Attack/i.test(sub) || tags.indexOf('RANGE_ATTACK')!==-1 || target.uses_range_attack_targeting===true;
  }
  function isDrawCounterCastingCard(c){
    if(!c) return false;
    var tags=runtimeTagList(c).map(function(t){return t.toUpperCase();});
    var effects=Array.isArray(c.effect)?c.effect:(Array.isArray(c.effects)?c.effects:[]);
    var ap=c.attachment_policy||(c.lifecycle&&c.lifecycle.attachment_policy)||{};
    return tags.indexOf('DRAW_COUNTER_CASTING')!==-1 || String(ap.role||'').toLowerCase()==='draw_counter_casting' || effects.some(function(e){return e&&e.kind==='pending_casting_draw_counter';});
  }
  function requiredDrawCountersForCasting(c){
    if(!c) return 5;
    var effects=Array.isArray(c.effect)?c.effect:(Array.isArray(c.effects)?c.effects:[]);
    var fx=effects.find(function(e){return e&&e.kind==='pending_casting_draw_counter';});
    var delay=c.casting_delay||(c.canonical_execution&&c.canonical_execution.casting_delay)||{};
    var staging=c.staging||{};
    return Number((fx&&fx.counters_required)||delay.remaining_count_on_entry||staging.counters_required||5);
  }
  function pvpCardActionLabel(c){
    var fam=cardFamily(c), sub=String(cardSubtype(c)||c.action_category||c.classification||'');
    if(isCastingCard(c)) return 'CAST';
    if(isAttackCard(c)) return 'ATK';
    if(isResponseOnly(c) || /Defense|Response/i.test(sub)) return 'DEF';
    if(fam==='Event') return 'EVENT';
    if(fam==='Item') return 'ITEM';
    if(isStatusTactical(c) || /Tactical/i.test(sub)) return 'TACTICAL';
    if(fam==='Skill' && /Support/i.test(sub)) return 'SUPPORT';
    if(fam==='Skill') return 'SKILL';
    return 'ACTION';
  }
  function commitPlayedCard(state, action){
    var cardId=action.card_id, c=card(cardId); var side=action.side||action.source_side||'PLAYER';
    action.commit_token=action.commit_token||('card:'+side+':'+cardId+':'+Date.now()+':'+Math.random()); state.resolvedCardCommitTokens=state.resolvedCardCommitTokens||[]; if(state.resolvedCardCommitTokens.indexOf(action.commit_token)!==-1 || (state.resolvedCardCommitDestinations&&state.resolvedCardCommitDestinations[action.commit_token])){ state.pending=null; render(); return false; }
    var visualMotion=capturePlayedCardMotion(cardId,side,action);
    // Hero Attachment Items use one allied Hero picker. That selected Hero is both the user and host.
    // Normalize the target-shaped UI intent before attachment validation/commit so source-host metadata cannot be lost.
    if(isHeroAttachmentItem(c) && !action.source_lane && action.target_lane && (action.target_side||side)===side){
      action.source_side=side; action.source_lane=action.target_lane;
      var itemHost=sideHeroes(state,side)[action.source_lane]; if(itemHost) action.source_card_id=heroIdFrom(itemHost);
    }
    var manaKey=side==='PLAYER'?'mana':'aiMana';
    var hand=sideHand(state,side); if(hand[action.hand_index]!==cardId){ var found=hand.indexOf(cardId); if(found<0){ showInfo('Cannot Play','Card is no longer in hand.'); state.pending=null; render(); return false; } action.hand_index=found; }
    var sourceHero=null, hostHero=null, hostSide=null, hostLane=null, attachmentSlot=-1, persistent=false, attachmentPolicy=null;
    if(action.source_side && action.source_lane){
      sourceHero=sideHeroes(state,action.source_side)[action.source_lane];
      if(action.source_card_id && heroIdFrom(sourceHero)!==action.source_card_id){ showInfo('Cannot Play','Source Hero changed after selection. Re-select the action after Reposition.'); state.pending=null; render(); return false; }
      if(!action.source_card_id && sourceHero) action.source_card_id=heroIdFrom(sourceHero);
    }
    var hostInfo=attachmentHostForAction(state,c,action,sourceHero);
    if(hostInfo){ hostHero=hostInfo.hero;hostSide=hostInfo.side;hostLane=hostInfo.lane;attachmentPolicy=hostInfo.policy;attachmentSlot=attachmentSlotIndex(hostHero); if(attachmentSlot<0){ showInfo('Cannot Play',(attachmentPolicy.host==='target'?'Target':'Source')+' Hero has no empty Attachment Slot.'); state.pending=null; render(); return false; } }
    action.side=side; var gate=validateAction(state,side,Object.assign({type:'PLAY_CARD'},action),{committingPending:true}); if(!gate.ok){ showInfo('Cannot Play', gate.reasons.join('\n')); state.pending=null; render(); return false; }
    var cost=cardPlayCostForSource(c,sourceHero,state,action.source_side||action.side||'PLAYER');
    if(isArrowBarrageCard(c)){ if(!action.mana_spent) action.mana_spent=arrowBarrageSpendMax(state,side); cost=Number(action.mana_spent||0); }
    if(isArrowBarrageCard(c) && !arrowBarrageSpendValid(state,side,cost)){ showInfo('Cannot Play','Arrow Barrage requires spending all available Mana Shards after target validation.'); state.pending=null; render(); return false; }
    if(Number(state[manaKey]||0)<cost){ showInfo('Cannot Play','Not enough Mana.'); state.pending=null; render(); return false; }
    if(requiredDiscardSpecificIds(c).length && !handHasRequiredDiscards(hand,c)){ showInfo('Cannot Play','Required discard cards are missing from hand.'); state.pending=null; render(); return false; }
    if((side==='PLAYER'||state.pvpHumanVsHuman) && typeof action.use_magical_surge==='undefined' && magicalSurgeChoiceAvailable(state,action,sourceHero,c)){ return openMagicalSurgeChoice(state,action); }
    var reactiveResponderSide=side==='PLAYER'?'AI':'PLAYER';
    if(!action.reactiveChecked && (side==='AI' || state.pvpHumanVsHuman) && canOpenReactiveCancelWindow(state,c,reactiveResponderSide,side)){ openReactiveCancelWindow(state,Object.assign({},action,{side:side}),cost,reactiveResponderSide); syncCounts(state); render(); return true; }
    var committedSnapshot=clone(state), committedBeforePhysical=physicalCardCountForSide(state,side,cardId), committedDiscardCount=(sideDiscard(state,side)||[]).filter(function(id){return id===cardId;}).length;
    try{
    // Source/target selection is pre-commit UI state. Clear it before the card leaves Hand so it
    // cannot masquerade as an active post-commit destination if a later resolver exits early.
    state.pending=null;
    state[manaKey]=Math.max(0, Number(state[manaKey]||0)-cost);
    hand.splice(action.hand_index,1);
    beginCommittedCardCustody(state,side,cardId,committedBeforePhysical,action.commit_token);
    if(action && action.__qa_force_error_after_commit) throw new Error('QA forced post-commit failure');
    removeRequiredDiscardsFromHand(state,side,c);
    if(sourceHero && sourceShouldExhaust(c,sourceHero)) sourceHero.exhausted=true;
    if(hostHero && attachmentPolicy){ persistent=true; }
    var normalAttackVisualHold=isAttackCard(c) && !hasCastingTimeOneForSource(c,sourceHero) && !isDrawCounterCastingCard(c) && visualMotion && visualMotion.target;
    if(normalAttackVisualHold){ action.visual_hold_key=nextAnimationHoldKey('attack',cardId); if(!beginHeldTargetCardMotion(visualMotion,action.visual_hold_key)) action.visual_hold_key=null; }
    else commitPlayedCardMotion(visualMotion,persistent,hostSide||action.source_side||side,hostLane||action.source_lane,attachmentSlot);
    var baseLog=side+' plays '+cardName(c)+(cost?' and pays '+cost+' Mana.':'.')+(sourceHero&&sourceShouldExhaust(c,sourceHero)?' '+cardName(card(sourceHero.card_id))+' Exhausts.':(sourceHero?' '+cardName(card(sourceHero.card_id))+' does not Exhaust.':''));
    pushLog(state,baseLog);
    var playerEvent=null;
    if(side==='PLAYER' && !isCastingCard(c)){
      playerEvent=recordLocalPlayerAction(state,pvpCardActionLabel(c),cardId,cardName(c),baseLog,{action_line:'Action: You played '+cardName(c)+'.',source_side:action.source_side||side,source_lane:action.source_lane||null,target_side:action.target_side,target_lane:action.target_lane,metric_before:(action.target_side&&action.target_lane?heroEventSnapshot(state,action.target_side,action.target_lane):null),result_lines:[baseLog]});
      if(playerEvent) action.player_event_id=playerEvent.id;
    }
    var aiEvent=null;
    if(side==='AI' && !isCastingCard(c)){
      var aiLabel=isAttackCard(c)?'ATK':(isHealCard(c)||isHealAllCard(c)||isBuffCard(c)||isRemoveStatusCard(c)||isReviveCard(c)?'SUPPORT':(hasInflictStatusEffect(c)?'TACTICAL':((cardFamily(c)||'ACTION').toUpperCase())));
      aiEvent=recordOpponentAction(state,aiLabel,cardId,cardName(c),baseLog,{action_line:'Action: AI played '+cardName(c)+'.', source_side:action.source_side||side, source_lane:action.source_lane||null, target_side:action.target_side, target_lane:action.target_lane, metric_before:(action.target_side&&action.target_lane?heroEventSnapshot(state,action.target_side,action.target_lane):null), result_lines:[baseLog]});
    }
    if(state.pvpHumanVsHuman && !isCastingCard(c)){
      var pvpLabel=pvpCardActionLabel(c);
      recordPvpSideAction(state,side,pvpLabel,cardId,cardName(c),baseLog,{action_line:'Action: '+side+' played '+cardName(c)+'.', source_side:action.source_side||side, source_lane:action.source_lane||null, target_side:action.target_side, target_lane:action.target_lane, result_lines:[baseLog]});
    }
    if(isCalculatedPlanCard(c)){
      if(startCalculatedPlanChoice(state,{side:side, source_card_id:cardId, source_side:action.source_side||side, source_lane:action.source_lane, host_side:hostSide, host_lane:hostLane, attachmentSlot:attachmentSlot})) return true;
    }
    if(isDoubleCastingCard(c) && sourceHero){
      var dcClass=heroClass(sourceHero), dcNextTurn=dcClass==='Elementalist';
      action.attachment_effect={repeat_next_magical_attack:true,allow_different_target:true,active_from_round:Number(state.round||1)+(dcNextTurn?1:0),active_timing:dcNextTurn?'next_turn':'this_turn'};
      registerLocalAttachment(state,action.source_side||side,action.source_lane,attachmentSlot,cardId,attachmentPolicy||attachmentPolicyForCard(c,sourceHero),action); var dcAttachment=state.activeAttachments.find(function(a){return a.card_id===cardId&&a.side===(action.source_side||side)&&a.lane===action.source_lane;}); if(dcAttachment){dcAttachment.expires=dcNextTurn?'end_of_next_owner_turn':'end_of_current_owner_turn';dcAttachment.allow_different_target=true;dcAttachment.active_from_round=Number(state.round||1)+(dcNextTurn?1:0);} finalizeCommittedSourceCard(state,action,side,cardId,'attachment',{side:action.source_side||side,lane:action.source_lane,slot:attachmentSlot}); state.resolvedCardCommitTokens.push(action.commit_token);
      pushLog(state, cardName(c)+' becomes an active source-owned effect on '+cardName(card(sourceHero.card_id))+'.');
      state.pending=null; syncCounts(state); render(); return true;
    }
    if(isDrawCounterCastingCard(c) && sourceHero){
      var drawCastSnapshot=castingSnapshotForAction(state,action,sourceHero,action.target_side&&action.target_lane?sideHeroes(state,action.target_side)[action.target_lane]:null); state.pendingCastings=state.pendingCastings||[];
      var drawPc=Object.assign({card_id:cardId, side:action.source_side||side, source_lane:action.source_lane, original_source_lane:action.source_lane, source_hero_card_id:sourceHero.card_id, source_instance_id:sourceHero.instance_id||null, source_rank_at_cast:heroRank(sourceHero), source_progression_id:heroProgressionId(sourceHero), target_side:action.target_side, target_lane:action.target_lane, locked_target_lane:action.target_lane, attachmentSlot:attachmentSlot, resolve_phase:'counter_draw', counters:0, commit_token:action.commit_token},drawCastSnapshot);
      var drawStartEvt=recordCastingStartEvent(state,drawPc,'counter_draw'); if(drawStartEvt) drawPc.casting_start_event_id=drawStartEvt.id; state.pendingCastings.push(drawPc);
      var drawCastingAttachment=registerLocalAttachment(state,action.source_side||side,action.source_lane,attachmentSlot,cardId,{tick_phase:'DRAW_EVENT',remaining_count:requiredDrawCountersForCasting(c),counter_mode:'progress',role:'casting'},action,{counters:0,required_counters:requiredDrawCountersForCasting(c),counter_display:'count',expires:'counter_draw'}); drawCastingAttachment.counters=0; drawCastingAttachment.required_counters=requiredDrawCountersForCasting(c); drawCastingAttachment.counter_display='count';
      pushLog(state,'CAST declare: '+cardName(c)+' enters '+cardName(card(sourceHero.card_id))+' Attachment Slot at counter (0) with locked target '+(action.target_side||'')+' '+(action.target_lane||'')+'.');
      state.pending=null; syncCounts(state); render(); return true;
    }
    if(hasCastingTimeOneForSource(c, sourceHero) && sourceHero){
      var castSnapshot=castingSnapshotForAction(state,action,sourceHero,action.target_side&&action.target_lane?sideHeroes(state,action.target_side)[action.target_lane]:null); state.pendingCastings=state.pendingCastings||[];
      var normalPc=Object.assign({card_id:cardId, side:action.source_side||side, source_lane:action.source_lane, original_source_lane:action.source_lane, source_hero_card_id:sourceHero.card_id, source_instance_id:sourceHero.instance_id||null, source_rank_at_cast:heroRank(sourceHero), source_progression_id:heroProgressionId(sourceHero), target_side:action.target_side, target_lane:action.target_lane, locked_target_lane:action.target_lane, attachmentSlot:attachmentSlot, resolve_phase:'next_owner_battle',commit_token:action.commit_token},castSnapshot);
      var normalStartEvt=recordCastingStartEvent(state,normalPc,'countdown'); if(normalStartEvt) normalPc.casting_start_event_id=normalStartEvt.id; state.pendingCastings.push(normalPc);
      registerLocalAttachment(state,action.source_side||side,action.source_lane,attachmentSlot,cardId,{tick_phase:'BATTLE_PHASE_START',remaining_count:1,counter_mode:'countdown',role:'casting'},action,{expires:'casting'});
      pushLog(state,'CAST declare: '+cardName(c)+' enters '+cardName(card(sourceHero.card_id))+' Attachment Slot with locked target '+(action.target_side||'')+' '+(action.target_lane||'')+'.');
      state.pending=null; syncCounts(state); render(); return true;
    }
    if(isAttackCard(c) && sourceHero && !isSingleTargetAttack(c) && !action.target_lane){
      action.target_side=action.target_side||targetSideForActor(c,side);
      var mdmg=computedAttackDamageForAction(state,Object.assign({},action,{target_lane:action.source_lane||'CENTER'}),sourceHero,null);
      resolveMultiTargetAttack(state,action,{damage:mdmg,persistent:persistent,attachmentSlot:attachmentSlot}); syncCounts(state); render(); return true;
    }
    if(action.target_side && action.target_lane){
      var target=sideHeroes(state,action.target_side)[action.target_lane];
      if(isFinalGritCard(c)){
        if(!finalGritTargetValid(target)){ showInfo('Cannot Play','Final Grit target must be a defeated Gladiator or Conqueror Hero on your side.'); state.pending=null; render(); return false; }
        if(isLegacyModeHero(target)) restoreHeroFromLegacyMode(state, action.target_side, action.target_lane, 30, cardName(c)); else { clearDefeatedHeroExp(state,action.target_side,target); target.hp=30; target.exhausted=true; target.exhaust_reason='Revived by Final Grit at 30 HP; this is defeat-prevention/revive, not normal healing.'; target.legacy_overlay=null; addStatus(target,'Final Grit',1,target.exhaust_reason); pushLog(state, cardName(c)+' revives '+cardName(card(target.card_id))+' at 30 HP Exhausted. It is not treated as a heal.'); }
      }
      else if(isExecuteEffect(c)){
        if(!executeTargetValid(target)){ showInfo('Cannot Play','Execute target must be at half HP or lower.'); state.pending=null; render(); return false; }
        var executeNoDodge=heroClass(sourceHero)==='Conqueror'; openAttackResponseWindow(state,action,{damage:0,persistent:false,attachmentSlot:-1,execute_defeat:true,execute_non_damage:true}); if(state.responseWindow){state.responseWindow.cannot_block=true;state.responseWindow.cannot_dodge=executeNoDodge;state.responseWindow.options=responseOptionsFor(state,action.target_side,{damage:0,damage_type:'Physical',attack_type:'Single Target',cannot_dodge:executeNoDodge,cannot_block:true,card_id:action.card_id,source_side:action.source_side,source_lane:action.source_lane,target_side:action.target_side,target_lane:action.target_lane});} syncCounts(state);render();return true;
      }
      else if(isAttackCard(c)){
        maybeApplyMagicalSurge(state,action,sourceHero,c);
        var dmg=computedAttackDamageForAction(state,action,sourceHero,target);
        if(attackActsAsTripleShotArea(action, sourceHero)){
          action.triple_shot_area=true; action.target_lane=null;
          pushLog(state,'Triple Shot converts '+cardName(c)+' into an Area Attack over current Attack Coverage.');
          resolveMultiTargetAttack(state,action,{damage:dmg,persistent:persistent,attachmentSlot:attachmentSlot,triple_shot_area:true}); syncCounts(state); render(); return true;
        }
        if(!isSingleTargetAttack(c)){ resolveMultiTargetAttack(state,action,{damage:dmg,persistent:persistent,attachmentSlot:attachmentSlot}); syncCounts(state); render(); return true; }
        var dcActive=sourceHero && hasDoubleCasting(sourceHero) && /Magical Attack/i.test(String(cardSubtype(c)||c.action_category||'')); var post={damage:dmg, damage_formula_log:action.damage_formula_log||'', persistent:persistent, attachmentSlot:attachmentSlot, optional_swap_after:isChargeSwapCard(c), defer_reposition_discard:(isChargeSwapCard(c)||isSoulBlastShotCard(c)), double_casting:dcActive, double_casting_stage:dcActive?1:0};
        resolveAttackWithOwnerGating(state,action,post); syncCounts(state); render(); return true;
      }
      else if(isReviveCard(c)){ if(target){ var reviveProfile=reviveProfileFromCard(c,sourceHero), reviveHp=reviveProfile.hp; if(isLegacyModeHero(target)) restoreHeroFromLegacyMode(state, action.target_side, action.target_lane, reviveHp, cardName(c), reviveProfile.exhausted); else { clearDefeatedHeroExp(state,action.target_side,target); target.hp=reviveHp; target.exhausted=!!reviveProfile.exhausted; target.exhaust_reason=target.exhausted?('Revived by '+cardName(c)+' at '+reviveHp+' HP; this is revive, not normal heal.'):null; target.legacy_overlay=null; if(target.exhausted)addStatus(target,'Revived Exhausted',1,target.exhaust_reason); pushLog(state, cardName(c)+' revives '+cardName(card(target.card_id))+' at '+reviveHp+' HP '+(target.exhausted?'Exhausted':'Ready')+'.'); } } }
      else if(isHealCard(c)){ var heal=firstHealAmountForSource(c,sourceHero); if(target){ var hpBefore=Number(target.hp||0); healHeroIfLegal(state,target,heal,c,sourceHero,'targeted heal'); action.heal_applied=Math.max(0,Number(target.hp||0)-hpBefore); appendCardPlayedResultForSide(state,side,playerEvent,aiEvent,'Heal result: '+cardName(card(target.card_id))+' healed '+action.heal_applied+' HP.'); } }
      else if(isRemoveStatusCard(c)){ var statusBefore=(target&&target.statuses?target.statuses.length:0); var removedStatus=removeChosenNegativeStatusFromHero(state,target,c,action.selected_status_index) || removeOneStatusFromHero(state,target,c); appendCardPlayedResultForSide(state,side,playerEvent,aiEvent,removedStatus?'Status cleanse result: 1 status removed.':'Status cleanse result: no status removed.'); }
      else if(isBuffCard(c) && !isHeroAttachmentItem(c)){ addClericBuff(state,target,c,sourceHero,action); appendCardPlayedResultForSide(state,side,playerEvent,aiEvent,'Buff result: '+cardName(c)+' applied to '+cardName(card(target.card_id))+'.'); }
      else if(hasInflictStatusEffect(c)){ var applied=applyNonAttackStatuses(c,target,sourceHero); if(applied.length){ pushLog(state, cardName(c)+' applies '+applied.join(', ')+' to '+cardName(card(target.card_id))+'.'); appendCardPlayedResultForSide(state,side,playerEvent,aiEvent,'Status result: '+applied.join(', ')+' on '+cardName(card(target.card_id))+'.'); } }
    }
    if(isStealCard(c)){ transferManaShard(state,side,c); }
    if(isSixthSenseCard(c)){ if(resolveSixthSense(state,side,c,hostSide,hostLane,attachmentSlot,action.commit_token)) return true; }
    if(c&&c.card_id==='S1-CLE-013'){ if(startCardSearchChoice(state,{side:side,zone:'hand',sourceCard:c,commit_token:action.commit_token,host_side:hostSide,host_lane:hostLane,attachmentSlot:attachmentSlot,predicate:function(){return true;},resolve_to:'discard_then_draw_three',title:"God's Blessing — Discard Cost",instruction:'Choose exactly 1 other card from your hand to discard, then draw 3 cards.',confirm_text:'Discard & Draw 3'})) return true; }
    if(isRelentlessLevelingCard(c)){ if(startCardSearchChoice(state,{side:side, zone:'hand', sourceCard:c, commit_token:action.commit_token, host_side:action.source_side||side, host_lane:action.source_lane, attachmentSlot:attachmentSlot, predicate:nonUltimateSkillCard, resolve_to:'source_exp', title:'Relentless Leveling — Choose Skill Tribute', instruction:'Choose 1 non-Ultimate Skill from your hand to add as EXP to the Event source Hero. This does not consume the normal Tribute tracker.', confirm_text:'Add as EXP'})) return true; }
    if(isMagicCompassCard(c)){ if(startCardSearchChoice(state,{side:side, zone:'deck', sourceCard:c, commit_token:action.commit_token, host_side:hostSide, host_lane:hostLane, attachmentSlot:attachmentSlot, filter:{family:'Skill'}, title:'Magic Compass — Choose Skill', instruction:'Choose 1 Skill Card from your deck and add it to your hand. The deck shuffles after this search.', confirm_text:'Add Skill'})) return true; }
    if(isDejaVuCard(c)){ if(startCardSearchChoice(state,{side:side, zone:'discard', sourceCard:c, commit_token:action.commit_token, host_side:hostSide, host_lane:hostLane, attachmentSlot:attachmentSlot, filter:{family:'Skill', is_ultimate:false}, title:'Déjà vu — Choose Skill from Discard', instruction:'Choose 1 non-Ultimate Skill from your Discard Pile and return it to your hand.', confirm_text:'Return Skill'})) return true; }
    if(isReplenishCard(c)){ if(startCardSearchChoice(state,{side:side, zone:'discard', sourceCard:c, commit_token:action.commit_token, host_side:hostSide, host_lane:hostLane, attachmentSlot:attachmentSlot, predicate:archerNonUltimateSkillCard, max_count:3, required_count:3, resolve_to:'replenish_to_deck', title:'Replenish — Choose Archer Skills', instruction:'Choose exactly 3 non-Ultimate Archer Skill Cards from your Discard Pile and return them to your deck.', confirm_text:'Return Archer Skills to Deck'})) return true; }
    if(isPoisonMistCard(c) && sourceHero){ applyPoisonMist(state,side,sourceHero,c); }
    if(isWildfireCard(c) && sourceHero){ applyWildfire(state,side,sourceHero,c); }
    if(sourceHero && isArcherSkillCard(c)){ if(applyArcherTacticalEffects(state,side,c,sourceHero,hostSide,hostLane,attachmentSlot,action.commit_token)) return true; }
    if(sourceHero && Array.isArray(c.effect)){ c.effect.forEach(function(e){ if(e.kind==='create_active_effect' && e.effect_key==='enrage_next_attack_damage_plus_20'){ action.attachment_effect={modifier_amount:20,damage_type:'Physical'}; pushLog(state, cardName(c)+' Attachment gives '+cardName(card(sourceHero.card_id))+' +20 Physical Attack Damage this turn.'); } if(e.kind==='create_active_effect' && e.effect_key==='taunt'){ action.attachment_effect={restriction_type:'TAUNT_TARGET_RESTRICTION'}; pushLog(state, cardName(c)+' Attachment applies Taunt targeting restriction from '+cardName(card(sourceHero.card_id))+'.'); } if(e.kind==='gain_mana' && !isStealCard(c)){ gainMana(state,side,Number(e.amount||0)); pushLog(state, cardName(c)+' gains '+Number(e.amount||0)+' Mana.'); } }); }
    if(isHealAllCard(c) && sourceHero){ healAllAlliedHeroes(state,side,firstHealAmountForSource(c,sourceHero),c,sourceHero,'all allied Heroes'); appendCardPlayedResultForSide(state,side,playerEvent,aiEvent,'Heal result: all allied Heroes healed if legal.'); }
    if(isCrystalBallCard(c)){ return resolveCrystalBallCard(state,side,c,cardId,hostHero,attachmentSlot,persistent,action); }
    applyImmediateEventItemEffects(state,side,c,sourceHero,action.target_side&&action.target_lane?sideHeroes(state,action.target_side)[action.target_lane]:null,hostHero,action);
    if(state.pending&&state.pending.type==='magic_scope_reveal'){syncCounts(state);render();return true;}
    var draw=firstDrawCount(c); if(!isBeginAnewCard(c) && !isLastResortCard(c)){ drawMany(state,side,draw,{reason:'CARD_EFFECT'}); }
    if(persistent){ registerLocalAttachment(state,hostSide||action.source_side||side,hostLane||action.source_lane,attachmentSlot,cardId,attachmentPolicy||attachmentPolicyForCard(c,sourceHero),action); finalizeCommittedSourceCard(state,action,side,cardId,'attachment',{side:hostSide||action.source_side||side,lane:hostLane||action.source_lane,slot:attachmentSlot}); pushLog(state, cardName(c)+' enters '+cardName(card((hostHero||sourceHero).card_id))+' Attachment Slot until its effect expires.'); }
    else { finalizeCommittedSourceCard(state,action,side,cardId,'discard',{reason:'normal resolution'}); }
    var completionLines=[];
    if(action.target_side&&action.target_lane){
      var completedEvt=side==='PLAYER'?playerEvent:aiEvent, beforeMetric=completedEvt&&completedEvt.metric_before, afterMetric=heroEventSnapshotAfter(state,beforeMetric,action.target_side,action.target_lane), metricLine=opponentMetricChangeLine(beforeMetric,afterMetric,'HP');
      if(metricLine)completionLines.push(metricLine);
    }
    if(draw>0)completionLines.push('Draw result: '+draw+' card'+(draw===1?'':'s')+' drawn.');
    completionLines.push(persistent?('Final zone: Attachment Slot '+(Number(attachmentSlot)+1)+'.'):'Final zone: Discard Pile.');
    completionLines.push('Resolution completed.');
    appendCardPlayedResultForSide(state,side,playerEvent,aiEvent,completionLines);
    state.resolvedCardCommitTokens.push(action.commit_token);
    if(!isDefeatGatePending(state.pending)) state.pending=null; if(!state.pending) activateNextSaintPurifyChoice(state); runtimeStateCheckpoint(state,'Card resolved: '+cardId); render(); return true;
    }catch(commitError){
      restoreStateSnapshot(state,committedSnapshot);
      pushLog(state,'Card play rollback restored '+cardName(c)+' to Hand after a resolver failure: '+String(commitError&&commitError.message||commitError)+'.');
      state.pending=null; syncCounts(state); render();
      showInfo('Card Play Recovered',cardName(c)+' was restored to Hand because its resolution could not complete. No Mana, Exhaust, attachment, or zone movement was kept.');
      return false;
    }
  }
  function openMultiTargetResponseWindow(state, action, post, affected, index, accumulatedDamage, accumulatedPoisonVialApplied, accumulatedConnectedHits){
    var c=card(action.card_id), sourceHero=sideHeroes(state,action.source_side)[action.source_lane];
    affected=(affected||[]).slice();index=Number(index||0); var targetSide=action.target_side||targetSideForActor(c,action.source_side)||'PLAYER';
    while(index<affected.length){var candidate=sideHeroes(state,targetSide)[affected[index]];if(candidate&&Number(candidate.hp||0)>0&&!isLegacyModeHero(candidate))break;index++;}
    if(index>=affected.length) return false;
    var lane=affected[index], target=sideHeroes(state,targetSide)[lane], dmg, incoming;
    if(action.venom_detonation){ action.venom_damage_by_lane=action.venom_damage_by_lane||{}; action.venom_poison_duration_by_lane=action.venom_poison_duration_by_lane||{}; if(typeof action.venom_damage_by_lane[lane]==='undefined'){ var storedDuration=poisonDurationOn(target); action.venom_poison_duration_by_lane[lane]=storedDuration; action.venom_damage_by_lane[lane]=window.GL_RULES_RUNTIME.venomDetonationDamage(storedDuration,action.venom_multiplier); removePoison(target); pushLog(state,cardName(c)+' removes Poison from '+cardName(card(target.card_id))+' before its response window; stored duration '+storedDuration+'.'); } dmg=Number(action.venom_damage_by_lane[lane]||0); incoming={damage:dmg,damage_type:'Magical',attack_type:'Venom Detonation',cannot_dodge:true,cannot_block:false,card_id:action.card_id,source_side:action.source_side,source_lane:action.source_lane,target_side:targetSide,target_lane:lane,affected_lanes:null,area:true}; }
    else { dmg=computedAttackDamageForAction(state,Object.assign({},action,{target_side:targetSide,target_lane:lane}),sourceHero,target); incoming={damage:dmg, damage_type:damageTypeForActionSource(c,sourceHero), attack_type:attackType(c)||'Area Damage', cannot_dodge:attackCannotBeDodged(c), cannot_block:attackCannotBeBlocked(c), card_id:action.card_id, source_side:action.source_side, source_lane:action.source_lane, target_side:targetSide, target_lane:lane, affected_lanes:null,area:true,casting:isCastingCard(c)}; }
    var teamImmune=!!activeAttachmentForSide(state,targetSide,'S1-CLE-025'); var preBlock=teamImmune?Number(incoming.damage||0):Number(action.team_area_block||0);
    state.responseWindow={kind:'incoming_attack', response_owner:targetSide, card_id:action.card_id, source_side:action.source_side, source_lane:action.source_lane, target_side:targetSide, target_lane:lane, damage:incoming.damage, damage_type:incoming.damage_type, attack_type:incoming.attack_type, cannot_dodge:incoming.cannot_dodge, cannot_block:incoming.cannot_block, preapplied_block:preBlock, preapplied_immunity:teamImmune, options:responseOptionsFor(state,targetSide,incoming), selected:null, post:post||{}, action:clone(Object.assign({},action,{target_side:targetSide,target_lane:lane})), multi_sequence:{affected_lanes:affected.slice(), index:index, total_damage:Number(accumulatedDamage||0), poison_vial_applied:!!accumulatedPoisonVialApplied, connected_hits:Number(accumulatedConnectedHits||0)}};
    state.pending={type:'response_window', card_id:action.card_id}; pushLog(state,'Response Window opens for '+cardName(c)+' target '+(index+1)+'/'+affected.length+' — '+targetSide+' '+lane+'.'); return true;
  }
  function resolveMultiTargetAttack(state, action, post){
    var c=card(action.card_id), sourceHero=sideHeroes(state,action.source_side)[action.source_lane];
    if(isVenomDetonationCard(c)){ resolveVenomDetonation(state,action,post); return; }
    var lanes;
    if(isDualArrowCard(c)) lanes=dualArrowAffectedLanesForAction(state,action);
    else if(action && action.triple_shot_area) lanes=targetCoverage(action.source_lane);
    else lanes=isAllTargetAttack(c)?LANE_ORDER.slice():targetCoverage(action.source_lane);
    var targetSide=action.target_side||targetSideForActor(c,action.source_side||'PLAYER')||'AI'; var affected=[];
    lanes.forEach(function(lane){ var h=sideHeroes(state,targetSide)[lane]; if(h && Number(h.hp||0)>0 && !isLegacyModeHero(h)) affected.push(lane); });
    affected.sort(function(a,b){ return LANE_ORDER.indexOf(a)-LANE_ORDER.indexOf(b); });
    if(action.source_side==='AI'){
      var snapshotEvt=findOpponentEventForAction(state,action,'ATK') || state.lastOpponentAction;
      if(snapshotEvt && !snapshotEvt.hp_before_by_lane){ snapshotEvt.hp_before_by_lane={}; affected.forEach(function(lane){ snapshotEvt.hp_before_by_lane[lane]=heroEventSnapshot(state,targetSide,lane); }); }
    }
    if(action.source_side==='PLAYER'){
      var playerSnapshotEvt=findLocalPlayerEventForAction(state,action,'ATK')||findLocalPlayerEventById(state,action.player_event_id);
      if(playerSnapshotEvt&&!playerSnapshotEvt.hp_before_by_lane){playerSnapshotEvt.hp_before_by_lane={};affected.forEach(function(lane){playerSnapshotEvt.hp_before_by_lane[lane]=heroEventSnapshot(state,targetSide,lane);});}
    }
    pushLog(state, cardName(c)+' multi-target sequence: '+affected.join(' → ')+'.');
    if(affected.length){
      openMultiTargetResponseWindow(state,Object.assign({},action,{target_side:targetSide}),post||{},affected,0,0);
      if(targetSide==='AI' && !state.pvpHumanVsHuman) autoResolveCurrentAIResponseWindow(state);
      return;
    }
    var multiResultLines=[], poisonVialAppliedAny=false, totalHpDamage=0, totalConnectedHits=0;
    affected.forEach(function(lane){ var h=sideHeroes(state,targetSide)[lane]; var dmg=computedAttackDamageForAction(state,Object.assign({},action,{target_lane:lane}),sourceHero,h); var mr=applyResolvedAttackDamage(state,action.card_id,action.source_side,action.source_lane,targetSide,lane,dmg,damageTypeForActionSource(c,sourceHero),false,0,true); if(mr && mr.poison_vial_applied) poisonVialAppliedAny=true; if(mr && mr.connected) totalConnectedHits++; totalHpDamage+=Number(mr&&mr.damage||0); multiResultLines.push(targetSide+' '+lane+' took '+Number(mr.damage||0)+' '+damageTypeForActionSource(c,sourceHero)+' damage.'); if(Number(mr.burn_damage||0)>0) multiResultLines.push(targetSide+' '+lane+' Burn added +'+Number(mr.burn_damage||0)+' damage.'); if(mr.statuses_applied && mr.statuses_applied.length) multiResultLines.push(targetSide+' '+lane+' Status result: '+mr.statuses_applied.join(', ')+'.'); });
    if(poisonVialAppliedAny && sourceHero){ releaseActiveAttachmentForHero(state,sourceHero,'S1-ITM-011','Poison Vial consumed after multi-target Attack'); pushLog(state,'Poison Vial is consumed once after multi-target Attack resolution.'); }
    if(action.source_side==='AI' && multiResultLines.length){ var aiMultiEvt=findOpponentEventForAction(state, action, 'ATK') || state.lastOpponentAction; if(aiMultiEvt){ var hpLines=[]; affected.forEach(function(lane){ var before=aiMultiEvt.hp_before_by_lane&&aiMultiEvt.hp_before_by_lane[lane], after=heroEventSnapshotAfter(state,before,targetSide,lane), line=opponentMetricChangeLine(before,after,'HP'); if(line) hpLines.push(line); }); appendOpponentEventResult(state, aiMultiEvt, multiResultLines.concat(hpLines)); } }
    if(sourceHero){ applySourcePostAttackEffects(state, action.card_id, sourceHero, action.source_side, action.source_lane, targetSide, null, {damage:totalHpDamage,hp_damage:totalHpDamage,connected_hits:totalConnectedHits}); consumeEnrageBonus(sourceHero); }
    if(post && post.persistent){ registerLocalAttachment(state,action.source_side,action.source_lane,post.attachmentSlot,action.card_id,isShieldBashCard(c)?{tick_phase:'DRAW_PHASE_START',remaining_count:1,counter_mode:'countdown',role:'defensive'}:{tick_phase:'END_PHASE',remaining_count:1,counter_mode:'countdown',role:'ongoing'},action,{expires:isShieldBashCard(c)?'draw_phase_start':'end_battle_phase'}); releaseHeldCardMotion(action.visual_hold_key,{type:'attachment',side:action.source_side,lane:action.source_lane,slot:post.attachmentSlot}); }
    else { finalizeCommittedSourceCard(state,action,action.source_side,action.card_id,'discard',{reason:'multi-target resolved'}); releaseHeldCardMotion(action.visual_hold_key,{type:'discard',side:action.source_side}); }
    state.pending=null; checkGameEnd(state);
  }
  function resolvePendingCastingsFor(state, side){
    var pending=(state.pendingCastings||[]).slice(), remain=[];
    pending.forEach(function(pc){
      if(pc.side!==side){remain.push(pc);return;}
      if(pc.resolve_phase==='counter_draw'||isDrawCounterCastingCard(card(pc.card_id))){if(state.phase==='Battle'&&Number(pc.counters||0)>=requiredDrawCountersForCasting(card(pc.card_id)))resolveDrawCounterCasting(state,pc);else remain.push(pc);return;}
      var c=card(pc.card_id),originalLane=pc.original_source_lane||pc.source_lane,sourceHero=sideHeroes(state,pc.side)[originalLane];
      function castingAttachment(){return (state.activeAttachments||[]).find(function(a){return a&&a.side===pc.side&&a.card_id===pc.card_id&&(a.commit_token===pc.commit_token||Number(a.slot)===Number(pc.attachmentSlot));})||null;}
      function clearAttachment(reason){var a=castingAttachment();if(a)releaseLocalAttachment(state,a,reason||'casting resolved');else releaseLocalAttachmentAt(state,pc.side,originalLane,pc.attachmentSlot,pc.card_id,reason||'casting resolved');}
      function cancelCasting(reason,logLine){if(logLine)pushLog(state,logLine);clearAttachment('casting canceled: '+reason);recordCastingExitEvent(state,pc,'CANCELED',reason);}
      if(!sourceHero||Number(sourceHero.hp||0)<=0||isLegacyModeHero(sourceHero)){cancelCasting('Source Hero was defeated or replaced before release.','CAST cancels: original source Hero was defeated or left '+originalLane+' before '+cardName(c)+' resolves.');return;}
      if(!castingSourceMatchesHero(pc,sourceHero)){cancelCasting('Source Hero moved or was replaced before release.','CAST cancels: '+cardName(c)+' source Hero left its original position or was replaced before release.');return;}
      if(hasStatus(sourceHero,'Stun')){cancelCasting('Canceled by Stun.','CAST cancels: source has Stun before '+cardName(c)+' releases.');return;}
      var lockedLane=pc.locked_target_lane||pc.target_lane,target=pc.target_side&&lockedLane?sideHeroes(state,pc.target_side)[lockedLane]:null;
      var releaseProbe={card_id:pc.card_id,source_side:pc.side,source_lane:originalLane,target_side:pc.target_side,target_lane:lockedLane,casting_release:true};
      var releaseSnapshot=castingSnapshotForAction(state,releaseProbe,sourceHero,target),printed=Number(releaseSnapshot.printed_damage),committed=Number(releaseSnapshot.committed_damage);
      if(!Number.isFinite(printed)||printed<=0||!Number.isFinite(committed)||committed<=0){cancelCasting('Missing valid release damage from the current source Hero.','CAST fails closed: '+cardName(c)+' has no valid release damage for the current source Hero.');return;}
      var releaseEventPc=Object.assign({},pc,releaseSnapshot,{source_hero_card_id:heroIdFrom(sourceHero),source_instance_id:sourceHero.instance_id||pc.source_instance_id||null,source_progression_id:heroProgressionId(sourceHero)});
      pushLog(state,'ATK resolve: '+cardName(c)+' releases as Casting Attack at locked target '+pc.target_side+' '+lockedLane+'.');
      if(sourceShouldExhaust(c,sourceHero)){sourceHero.exhausted=true;sourceHero.exhaust_reason='Resolved pending CAST '+cardName(c)+'.';}
      if(!target||Number(target.hp||0)<=0||isLegacyModeHero(target)){
        var noTarget='Locked target position '+pc.target_side+' '+lockedLane+' contains '+(target&&isLegacyModeHero(target)?'Legacy':'no active Hero')+'. Casting Attack resolves without dealing HP damage.';pushLog(state,noTarget);
        clearAttachment('casting resolved without active target');
        recordCastingExitEvent(state,releaseEventPc,'RESOLVED',noTarget,['Release source: '+cardName(card(heroIdFrom(sourceHero)))+' — '+heroClass(sourceHero)+' Rank '+heroRank(sourceHero)+'.','Final HP damage: 0.']);
        return;
      }
      var action={card_id:pc.card_id,source_side:pc.side,source_lane:originalLane,source_hero_card_id:heroIdFrom(sourceHero),target_side:pc.target_side,target_lane:lockedLane,locked_target_lane:lockedLane,action_profile:'Casting Attack',casting_release:true};
      var post={damage:committed,persistent:false,attachmentSlot:pc.attachmentSlot,casting_release:true,printed_damage:printed,modifier_breakdown:clone(releaseSnapshot.modifier_breakdown||[]),damage_type:releaseSnapshot.damage_type||damageTypeForActionSource(c,sourceHero),action_profile:'Casting Attack'};
      clearAttachment('casting released');
      var exitEvt=recordCastingExitEvent(state,releaseEventPc,'RESOLVED','Casting Attack released and opened a Defense response window.',['Release source: '+cardName(card(heroIdFrom(sourceHero)))+' — '+heroClass(sourceHero)+' Rank '+heroRank(sourceHero)+'.','Damage after modifiers at release: '+committed+'.']);
      if(exitEvt){if(pc.side==='PLAYER')action.player_event_id=exitEvt.id;else action.opponent_event_id=exitEvt.id;}
      openAttackResponseWindow(state,action,post);
      if(state.responseWindow&&pc.target_side==='AI'&&!state.pvpHumanVsHuman)autoResolveCurrentAIResponseWindow(state);
    });
    state.pendingCastings=remain;
  }
  function nextRankThreshold(hero){ return window.GL_RULES_RUNTIME.nextRankThreshold(heroRank(hero)); }
  function legacyPackageSlotForLane(state, side, lane){ var slots=side==='PLAYER'?state.playerLegacyPackageSlots:state.aiLegacyPackageSlots; return (slots||[]).find(function(x){ return x && x.slot===lane; }) || null; }
  function rankHeroIdFromProgression(progressionId, targetRank){
    var m=String(progressionId||'').match(/^(S1-[A-Z]+-H)(\d{3})$/); if(!m) return null;
    var base=Number(m[2]||0); if(!base || targetRank<2) return null;
    var id=m[1]+String(base+Number(targetRank)-1).padStart(3,'0');
    var c=card(id); return cardFamily(c)==='Hero' ? id : null;
  }
  function packageLineageForLane(state, side, lane){
    var h=state&&sideHeroes(state,side)&&sideHeroes(state,side)[lane];
    var slot=legacyPackageSlotForLane(state,side,lane);
    var slotProgressionLineage=slot&&slot.progression?heroLineageId({card_id:slot.progression}):'';
    var heroLineage=h?heroLineageId(h):'';
    return (slot&&slot.class_lineage) || (slot&&slot.fixed_class_lineage_id) || slotProgressionLineage || heroLineage || (h&&h.legacy_lineage_id) || (slot&&slot.lineage) || '';
  }
  function nextRankHeroId(state, side, lane, targetRank){
    var legacy=side==='PLAYER'?state.playerLegacy:state.aiLegacy; var slot=legacyPackageSlotForLane(state,side,lane);
    var progressionCandidate=rankHeroIdFromProgression(slot&&slot.progression,targetRank);
    var idx=-1;
    if(progressionCandidate) idx=(legacy||[]).indexOf(progressionCandidate);
    if(idx<0){ var lineage=packageLineageForLane(state,side,lane); idx=(legacy||[]).findIndex(function(id){ var c=card(id); return cardFamily(c)==='Hero' && heroLineageId({card_id:id})===lineage && parseRank((c.identity||{}).rank)===targetRank; }); }
    if(idx<0) return null;
    return legacy.splice(idx,1)[0];
  }
  function checkAndApplyRankUp(state, side, lane){ return rankUpHero(state,side,lane); }
  function rankUpHero(state, side, lane){
    var hero=sideHeroes(state,side)[lane]; if(!hero) return false;
    var threshold=nextRankThreshold(hero); if(!threshold || Number(hero.exp_total||0)<threshold) return false;
    var beforeRank=heroRank(hero);
    var targetRank=beforeRank+1; var nextId=nextRankHeroId(state,side,lane,targetRank); if(nextId) queueLegacyDeckToFieldMotion(side,lane,nextId); if(!nextId){ pushLog(state,'Rank Up failed: no Rank '+targetRank+' Hero found in Legacy Deck package for '+lane+'.'); return false; }
    var spentExpCards=(hero.exp_cards||[]).slice(); queueRankUpDiscardMotion(side,lane,spentExpCards);
    var discard=sideDiscard(state,side); spentExpCards.forEach(function(id){ discard.push(id); });
    var oldHp=Number(hero.hp||0), oldMax=Number(hero.maxHp||100); var damageTaken=Math.max(0,oldMax-oldHp); hero.card_id=nextId; var nc=card(nextId); var newMax=Number(nc.hp||oldMax||100); hero.maxHp=newMax; hero.hp=Math.max(1, Math.min(newMax, newMax-damageTaken)); hero.exp_cards=[]; hero.exp_total=Number(hero.exp_total||0);
    var drawReward=targetRank===2?2:(targetRank===3?3:0); drawMany(state,side,drawReward,{reason:'RANK_UP_REWARD'});
    if(side==='PLAYER') state.manaRegen=Math.min(6,Number(state.manaRegen||1)+1); else state.aiManaRegen=Math.min(6,Number(state.aiManaRegen||1)+1);
    var roman=(targetRank===2?'II':(targetRank===3?'III':targetRank));
    var rankMsg='Rank Up: '+cardName(nc)+' reached Rank '+roman+'. Drew '+drawReward+' cards and gained +1 Mana Regen.';
    pushLog(state, side+' '+rankMsg+' Mana Pool did not increase from Rank Up.');
    if(side==='AI') recordOpponentAction(state,'RANK UP',nextId,'AI Rank Up',rankMsg, {action_line:'Action: AI ranked up '+cardName(nc)+'.', target_side:'AI', target_lane:lane, result_lines:[cardName(nc)+' reached Rank '+roman+'.','AI drew '+drawReward+' cards.','AI gained +1 Mana Regen.','Mana Pool did not increase from Rank Up.']});
    if(state.pvpHumanVsHuman) recordPvpSideAction(state,side,'RANK UP',nextId,'Rank Up',rankMsg,{action_line:'Action: '+side+' ranked up '+cardName(nc)+'.', target_side:side, target_lane:lane, result_lines:[cardName(nc)+' reached Rank '+roman+'.',side+' drew '+drawReward+' cards.',side+' gained +1 Mana Regen.','Mana Pool did not increase from Rank Up.']});
    return true;
  }
  function commitTributeToHero(state, action){
    var cardId=action.card_id, c=card(cardId), lane=action.target_lane; var ts=legalTributeState(state,cardId);
    if(!ts.can || ts.reasons.length){ showInfo('Cannot Tribute', ts.reasons.join('\n')); state.pending=null; render(); return false; }
    if(legalTributeTargetLanes(state,cardId).indexOf(lane)===-1){ showInfo('Cannot Tribute','Selected Hero cannot receive this Tribute.'); return false; }
    var hand=state.playerHand; if(hand[action.hand_index]!==cardId){ var found=hand.indexOf(cardId); if(found<0){ showInfo('Cannot Tribute','Card is no longer in hand.'); state.pending=null; render(); return false; } action.hand_index=found; }
    var hero=state.playerHeroes[lane], beforeHeroName=cardName(card(hero.card_id)), beforeExp=Number(hero.exp_total||0), beforeRank=heroRank(hero), gained=tributeExpValue(c);
    var tributeVisual=captureTributeCardMotion('PLAYER',action.hand_index,cardId,lane); hand.splice(action.hand_index,1); queueTributeCardMotion(tributeVisual); hero.exp_cards=hero.exp_cards||[]; hero.exp_cards.push(cardId); var tributeExpResult=applyTributeExpToHero(hero,c); if(!tributeExpResult.legal){ hero.exp_cards.pop(); hand.splice(action.hand_index,0,cardId); showInfo('Cannot Tribute','This Tribute cannot be applied to the selected Hero.'); state.pending=null; render(); return false; } state.tributeUsedThisReform=true; state.pending=null;
    pushLog(state,'PLAYER tributes '+cardName(c)+' to '+beforeHeroName+' for '+gained+' EXP. Printed Mana cost is ignored.');
    var ranked=checkAndApplyRankUp(state,'PLAYER',lane), afterHero=state.playerHeroes[lane], afterHeroName=cardName(card(afterHero.card_id)), afterExp=Number(afterHero.exp_total||0), afterRank=heroRank(afterHero);
    recordLocalPlayerAction(state,'TRIBUTE',cardId,'Tribute — '+cardName(c),'You tributed '+cardName(c)+' to '+beforeHeroName+'.',{action_line:'Action: You tributed '+cardName(c)+' as EXP.',source_side:'PLAYER',target_side:'PLAYER',target_lane:lane,result_lines:[beforeHeroName+' gained '+gained+' EXP.','EXP before Tribute: '+beforeExp+'.','EXP after Tribute: '+afterExp+'.','Printed Mana cost was ignored.',ranked?('Rank Up: '+beforeHeroName+' Rank '+beforeRank+' -> '+afterHeroName+' Rank '+afterRank+'.'):'No Rank Up occurred.']});
    if(state.pvpHumanVsHuman) recordPvpSideAction(state,'PLAYER','TRIBUTE',cardId,'Tribute', 'PLAYER tributed '+cardName(c)+' to '+beforeHeroName+'.', {action_line:'Action: PLAYER tributed '+cardName(c)+'.', target_side:'PLAYER', target_lane:lane, result_lines:['PLAYER '+lane+' gained '+gained+' EXP.','Printed Mana cost is ignored.',ranked?'Rank Up resolved.':'No Rank Up occurred.']});
    syncCounts(state); render(); return true;
  }
  function gainMana(state, side, amount){
    amount=Number(amount||0);
    if(side==='PLAYER') state.mana=Math.min(12, Number(state.mana||0)+amount);
    else state.aiMana=Math.min(12, Number(state.aiMana||0)+amount);
  }
  function openingFirstSide(state){
    var first=state&&state.openingCoinFlip&&state.openingCoinFlip.firstPlayer;
    if(first!=='PLAYER'&&first!=='AI') first=state&&state.preGame&&state.preGame.firstPlayer;
    return first==='AI'?'AI':'PLAYER';
  }
  function advanceRoundAfterCompletedTurnPair(state,endingSide){
    if(!state)return false;
    var first=openingFirstSide(state),second=first==='PLAYER'?'AI':'PLAYER';
    if(endingSide!==second)return false;
    state.round=Number(state.round||1)+1;
    return true;
  }
  function finishGame(state, winner, reason){
    if(!state || state.gameOver) return false;
    state.gameOver=true; state.winner=winner; state.gameEndReason=reason||'Game ended.'; state.pending=null; state.responseWindow=null; state.gameResultShown=false;
    pushLog(state,'GAME END: '+winner+' wins. '+state.gameEndReason);
    return true;
  }
  function sideHasValidActiveUnit(state, side){
    // Phase10: Legacy Mode is not a legal attack target and does not count as an active Hero for win/loss.
    // A side loses when all three original Heroes have fallen into Legacy Mode / 0 HP.
    return LANE_ORDER.some(function(l){ var h=state&&sideHeroes(state,side)[l]; return isActiveHeroSlot(h); });
  }
  function checkGameEnd(state){
    if(!state || state.gameOver) return !!(state&&state.gameOver);
    if(!sideHasValidActiveUnit(state,'PLAYER')) return finishGame(state,'AI','PLAYER loses: all 3 Heroes have been defeated.');
    if(!sideHasValidActiveUnit(state,'AI')) return finishGame(state,'PLAYER','AI loses: all 3 Heroes have been defeated.');
    autoCenterOneVsOne(state);
    return false;
  }
  function recordDrawPresentationEvent(state,side,cardId,reason,handIndex,opts){opts=opts||{};state.presentationEvents=Array.isArray(state.presentationEvents)?state.presentationEvents:[];state.presentationEventSequence=Number(state.presentationEventSequence||0)+1;var e={id:'DRAW-'+state.presentationEventSequence,type:'CARD_DRAWN',side:side,card_id:cardId,hand_index:Number(handIndex),reason:reason||'CARD_EFFECT',sequence:state.presentationEventSequence,group_id:opts.groupId||null,group_index:opts.groupIndex||null,counts_for_turn:opts.countsForTurn!==false,triggers_draw_effects:opts.triggerDrawEffects!==false};state.presentationEvents.push(e);if(state.presentationEvents.length>160)state.presentationEvents=state.presentationEvents.slice(-160);return e;}
  function drawOne(state,side,drawPhase,opts){
    opts=opts||{};var deck=side==='PLAYER'?state.playerDeck:state.aiDeck,hand=side==='PLAYER'?state.playerHand:state.aiHand,reason=opts.reason||(drawPhase?'MANDATORY_DRAW_PHASE':'CARD_EFFECT'),counts=opts.countsForTurn!==false&&reason!=='OPENING_HAND',triggers=opts.triggerDrawEffects!==false&&reason!=='OPENING_HAND';
    if(!deck.length){if(drawPhase)finishGame(state,side==='PLAYER'?'AI':'PLAYER',side+' loses: cannot draw during Draw Phase because their Main Deck is empty.');else pushLog(state,side+' attempts to draw from empty deck outside Draw Phase and draws 0.');return false;}
    var drawn=deck.shift(),handIndex=hand.length;hand.push(drawn);state.cardsDrawnThisTurn=state.cardsDrawnThisTurn||{PLAYER:0,AI:0};if(counts)state.cardsDrawnThisTurn[side]=Number(state.cardsDrawnThisTurn[side]||0)+1;state.lastDrawnCardBySide=state.lastDrawnCardBySide||{};state.lastDrawnCardBySide[side]=drawn;if(triggers)incrementDrawCounterCastings(state,side,drawn);var evt=recordDrawPresentationEvent(state,side,drawn,reason,handIndex,{groupId:opts.groupId,groupIndex:opts.groupIndex,countsForTurn:counts,triggerDrawEffects:triggers});
    if(reason==='OPENING_HAND')pushLog(state,side+' draws opening card '+Number((opts.groupIndex||0)+1)+' of '+OPENING_HAND_SIZE+'.');else pushLog(state,side+' draws 1 card. Draw This Turn: '+Number(state.cardsDrawnThisTurn[side]||0)+'.');
    if(!opts.deferAnimation&&!SUPPRESS_RENDER)queueDrawEvents([evt],state);if(drawPhase&&!opts.suppressDrawReplacement)maybeOpenDrawReplacementChoice(state,side,drawn,hand.length-1);return{card_id:drawn,event:evt,hand_index:handIndex};
  }
  function drawMany(state,side,count,opts){opts=opts||{};var events=[],actual=0;for(var i=0;i<Number(count||0);i++){var r=drawOne(state,side,!!opts.drawPhase,{reason:opts.reason||(opts.drawPhase?'MANDATORY_DRAW_PHASE':'CARD_EFFECT'),countsForTurn:opts.countsForTurn,triggerDrawEffects:opts.triggerDrawEffects,suppressDrawReplacement:opts.suppressDrawReplacement,deferAnimation:true,groupId:opts.groupId||null,groupIndex:i});if(!r)break;actual++;events.push(r.event);}if(events.length&&!opts.deferAnimation&&!SUPPRESS_RENDER)queueDrawEvents(events,state);return{count:actual,events:events};}
  function startFirstTurnAfterOpeningDraw(state,firstSide,opts){
    opts=opts||{};
    if(!state||state.gameOver)return false;
    state.preGame=null;
    state.openingFlowCompleted=true;
    state.turn=firstSide==='AI'?'AI':'PLAYER';
    state.phase='Draw';
    state.round=1;
    state.drawPhaseResolvedFor=null;
    pushLog(state,(state.turn==='PLAYER'?'PLAYER':'AI')+' won the Opening Coin Flip and enters Round 1 Draw Phase.');
    syncCounts(state);
    if(state.turn==='AI'){
      if(opts.bridgeImmediate){resolveDrawPhase(state,'AI',{deferAnimation:true});if(!state.gameOver&&!state.pending)state.phase='Deploy';syncCounts(state);}else runAITurn(state);
    }else{
      var before=(state.presentationEvents||[]).length;
      resolveDrawPhase(state,'PLAYER',{deferAnimation:true});
      syncCounts(state);
      if(!opts.deferAnimation&&!SUPPRESS_RENDER){var mandatoryEvents=(state.presentationEvents||[]).slice(before);queueDrawEvents(mandatoryEvents,state,{onComplete:function(){if(!SUPPRESS_RENDER)render();autoAdvancePlayerDrawWhenReady(state);}});}
      else autoAdvancePlayerDrawWhenReady(state);
    }
    if(!SUPPRESS_RENDER)render();
    return true;
  }
  function completeOpeningFlow(state,firstSide,flipData,opts){
    opts=opts||{};
    if(!state||state.openingFlowCompleted||state.openingHandsDrawn)return[];
    var before=(state.presentationEvents||[]).length;
    state.openingCoinFlip=Object.assign({},flipData||{},{firstPlayer:firstSide,completed:true});
    state.preGame={stage:'OPENING_DRAW',firstPlayer:firstSide};
    state.phase='Opening Hand';
    state.turn=null;
    for(var i=0;i<OPENING_HAND_SIZE;i++){
      var group='OPENING_PAIR_'+(i+1);
      drawOne(state,'PLAYER',false,{reason:'OPENING_HAND',countsForTurn:false,triggerDrawEffects:false,suppressDrawReplacement:true,deferAnimation:true,groupId:group,groupIndex:i});
      drawOne(state,'AI',false,{reason:'OPENING_HAND',countsForTurn:false,triggerDrawEffects:false,suppressDrawReplacement:true,deferAnimation:true,groupId:group,groupIndex:i});
    }
    state.openingHandsDrawn=true;
    syncCounts(state);
    var ev=(state.presentationEvents||[]).slice(before);
    if(opts.deferAnimation||SUPPRESS_RENDER)startFirstTurnAfterOpeningDraw(state,firstSide,{deferAnimation:true,bridgeImmediate:!!opts.bridgeImmediate});
    else queueDrawEvents(ev,state,{onComplete:function(){startFirstTurnAfterOpeningDraw(state,firstSide,{});}});
    return ev;
  }

  function incrementDrawCounterCastings(state, side, drawnCardId){
    if(!state || !state.pendingCastings) return;
    var remain=[];
    state.pendingCastings.forEach(function(pc){
      if(pc.side!==side || !isDrawCounterCastingCard(card(pc.card_id))){ remain.push(pc); return; }
      pc.counters=Number(pc.counters||0)+1;
      syncAttachmentCounter(state, pc.side, pc.source_lane, pc.attachmentSlot, pc.card_id, pc.counters);
      pushLog(state, cardName(card(pc.card_id))+' gains counter ('+pc.counters+') from draw.');
      if(pc.counters>=requiredDrawCountersForCasting(card(pc.card_id))){
        if(state.phase==='Battle') resolveDrawCounterCasting(state,pc);
        else { pushLog(state, cardName(card(pc.card_id))+' is fully charged and will resolve in Battle Phase.'); remain.push(pc); }
      }
      else remain.push(pc);
    });
    state.pendingCastings=remain;
  }
  function syncAttachmentCounter(state, side, lane, slot, cardId, count){
    (state.activeAttachments||[]).forEach(function(a){ if(a.side===side && a.lane===lane && Number(a.slot)===Number(slot) && a.card_id===cardId) a.remaining=Number(count||0); });
  }
  function resolveDrawCounterCasting(state, pc){
    var c=card(pc.card_id),lane=pc.original_source_lane||pc.source_lane,sourceHero=sideHeroes(state,pc.side)[lane],lockedLane=pc.locked_target_lane||pc.target_lane,target=pc.target_side&&lockedLane?sideHeroes(state,pc.target_side)[lockedLane]:null;
    function release(reason){var a=(state.activeAttachments||[]).find(function(x){return x&&x.side===pc.side&&x.card_id===pc.card_id&&(x.commit_token===pc.commit_token||Number(x.slot)===Number(pc.attachmentSlot));});if(a)releaseLocalAttachment(state,a,reason);else releaseLocalAttachmentAt(state,pc.side,lane,pc.attachmentSlot,pc.card_id,reason);state.pendingCastings=(state.pendingCastings||[]).filter(function(x){return x!==pc;});}
    function cancel(reason,logLine){if(logLine)pushLog(state,logLine);release('draw-counter casting canceled: '+reason);recordCastingExitEvent(state,pc,'CANCELED',reason);checkGameEnd(state);return false;}
    if(!sourceHero||isLegacyModeHero(sourceHero)||Number(sourceHero.hp||0)<=0)return cancel('Source Hero was defeated or replaced before release.','CAST cancels: '+cardName(c)+' source was defeated or replaced before counter release.');
    if(!castingSourceMatchesHero(pc,sourceHero))return cancel('Source Hero moved or was replaced before release.','CAST cancels: '+cardName(c)+' source left its original position or was replaced before counter release.');
    if(hasStatus(sourceHero,'Stun'))return cancel('Canceled by Stun.','CAST cancels: '+cardName(c)+' source was Stunned before counter release.');
    var releaseProbe={card_id:pc.card_id,source_side:pc.side,source_lane:lane,target_side:pc.target_side,target_lane:lockedLane,casting_release:true,draw_counter_casting_release:true};
    var releaseSnapshot=castingSnapshotForAction(state,releaseProbe,sourceHero,target),printed=Number(releaseSnapshot.printed_damage),committed=Number(releaseSnapshot.committed_damage);
    if(!Number.isFinite(printed)||printed<=0||!Number.isFinite(committed)||committed<=0)return cancel('Missing valid release damage from the current source Hero.','CAST fails closed: '+cardName(c)+' has no valid release damage for the current source Hero.');
    var releaseEventPc=Object.assign({},pc,releaseSnapshot,{source_hero_card_id:heroIdFrom(sourceHero),source_instance_id:sourceHero.instance_id||pc.source_instance_id||null,source_progression_id:heroProgressionId(sourceHero)});
    if(!target||Number(target.hp||0)<=0||isLegacyModeHero(target)){var noTarget='Locked target position '+pc.target_side+' '+lockedLane+' contains '+(target&&isLegacyModeHero(target)?'Legacy':'no active Hero')+'. Casting Attack resolves without dealing HP damage.';pushLog(state,noTarget);release('draw-counter casting resolved without target');recordCastingExitEvent(state,releaseEventPc,'RESOLVED',noTarget,['Release source: '+cardName(card(heroIdFrom(sourceHero)))+' — '+heroClass(sourceHero)+' Rank '+heroRank(sourceHero)+'.','Final HP damage: 0.']);return true;}
    var action={card_id:pc.card_id,source_side:pc.side,source_lane:lane,source_hero_card_id:heroIdFrom(sourceHero),target_side:pc.target_side,target_lane:lockedLane,draw_counter_casting_release:true,action_profile:'Casting Attack',casting_release:true};
    var post={damage:committed,persistent:false,attachmentSlot:pc.attachmentSlot,draw_counter_casting_release:true,draw_counter_side:pc.side,draw_counter_lane:lane,draw_counter_slot:pc.attachmentSlot,casting_release:true,printed_damage:printed,modifier_breakdown:clone(releaseSnapshot.modifier_breakdown||[]),damage_type:releaseSnapshot.damage_type||damageTypeForActionSource(c,sourceHero),action_profile:'Casting Attack'};
    pushLog(state,'CAST resolve: '+cardName(c)+' reaches '+requiredDrawCountersForCasting(c)+' counters and opens a normal Defense response window at locked target '+lockedLane+'.');
    release('draw-counter casting released');
    var exitEvt=recordCastingExitEvent(state,releaseEventPc,'RESOLVED','Counter Casting released and opened a Defense response window.',['Release source: '+cardName(card(heroIdFrom(sourceHero)))+' — '+heroClass(sourceHero)+' Rank '+heroRank(sourceHero)+'.','Damage after modifiers at release: '+committed+'.']);
    if(exitEvt){if(pc.side==='PLAYER')action.player_event_id=exitEvt.id;else action.opponent_event_id=exitEvt.id;}
    openAttackResponseWindow(state,action,post);if(state.responseWindow&&pc.target_side==='AI'&&!state.pvpHumanVsHuman)autoResolveCurrentAIResponseWindow(state);return true;
  }
  function resolveDrawPhase(state,side,opts){opts=opts||{};clearRacialUseForSide(state,side);state.cardsDrawnThisTurn=state.cardsDrawnThisTurn||{PLAYER:0,AI:0};state.cardsDrawnThisTurn[side]=0;var regen=side==='PLAYER'?state.manaRegen:state.aiManaRegen;drawOne(state,side,true,{reason:'MANDATORY_DRAW_PHASE',deferAnimation:!!opts.deferAnimation});state.drawPhaseResolvedFor=side;if(!state.gameOver){gainMana(state,side,regen);pushLog(state,side+' gains '+regen+' Mana from Mana Regen.');clearExhaustForSide(state,side);}checkGameEnd(state);syncCounts(state);}
  function autoAdvancePlayerDrawWhenReady(state){
    if(!state||state.gameOver||state.turn!=='PLAYER'||state.phase!=='Draw'||state.drawPhaseResolvedFor!=='PLAYER')return false;
    if(window.GL_PVP_SHARED_BOARD_ACTIVE)return false;
    if(state.pending||state.responseWindow)return false;
    if(state.autoDrawAdvanceScheduled)return true;
    state.autoDrawAdvanceScheduled=true;
    function finish(){
      if(!state){return;}
      if(state.gameOver||state.turn!=='PLAYER'||state.phase!=='Draw'||state.drawPhaseResolvedFor!=='PLAYER'){state.autoDrawAdvanceScheduled=false;return;}
      if(state.pending||state.responseWindow){state.autoDrawAdvanceScheduled=false;return;}
      if(animationBusy()||battleFeedbackBusy()){setTimeout(finish,55);return;}
      state.autoDrawAdvanceScheduled=false;
      state.phase='Deploy';state.drawPhaseResolvedFor=null;
      pushLog(state,'PLAYER completes Draw Phase and enters Deploy Phase automatically.');
      syncCounts(state);if(!SUPPRESS_RENDER)render();
    }
    if(SUPPRESS_RENDER)finish();else setTimeout(finish,0);
    return true;
  }
  function acknowledgePvpTurnStart(){
    if(!appState || !window.GL_PVP_SHARED_BOARD_ACTIVE || !appState.pvpHumanVsHuman || !appState.pvpTurnReady || appState.turn!=='PLAYER' || appState.phase!=='Draw' || appState.pending || appState.responseWindow || appState.gameOver) return false;
    appState.pvpTurnReady=false;
    removeStartOfTurnTargetPreventionForSide(appState,'PLAYER');
    pushLog(appState,'PLAYER acknowledges Your Turn. Draw Phase resolves now.');
    resolveDrawPhase(appState,'PLAYER');
    if(!appState.gameOver && !appState.pending) appState.phase='Deploy';
    syncCounts(appState); render(); if(appState.gameOver) showResult(); return true;
  }
  function cleanupHandLimit(state, side){
    var hand=side==='PLAYER'?state.playerHand:state.aiHand; var discard=side==='PLAYER'?state.playerDiscard:state.aiDiscard;
    if(hand.length<=8) return true;
    if(side==='AI' && !state.pvpHumanVsHuman){
      while(hand.length>8){ discard.push(hand.pop()); }
      pushLog(state, 'AI auto-discards down to hand limit 8.'); syncCounts(state); return true;
    }
    state.pending={type:'hand_limit_discard', side:side, decision_side:side, required:hand.length-8, selected:[]};
    return false;
  }
  function continueAutomaticPlayerEndTurn(state){
    if(!state||!state.autoEndTurnPending||state.turn!=='PLAYER'||state.phase!=='End'||state.pending||state.responseWindow||state.gameOver)return false;
    state.autoEndResumeScheduled=false;
    resolveEndPhaseCleanupForSide(state,'PLAYER');
    if(state.pending||state.responseWindow||state.gameOver){syncCounts(state);render();return true;}
    state.autoEndTurnPending=false;
    finishPlayerEndToAI(state);
    return true;
  }
  function scheduleAutomaticPlayerEndResume(state){
    if(!state||!state.autoEndTurnPending||state.autoEndResumeScheduled||state.turn!=='PLAYER'||state.phase!=='End'||state.pending||state.responseWindow||state.gameOver)return false;
    state.autoEndResumeScheduled=true;
    setTimeout(function(){if(!state)return;state.autoEndResumeScheduled=false;continueAutomaticPlayerEndTurn(state);},0);
    return true;
  }
  function finishPlayerEndToAI(state){
    if(state && state.pending){ syncCounts(state); render(); return; }
    if(!cleanupHandLimit(state,'PLAYER')){ showHandLimitDiscard(); render(); return; }
    if(window.GL_PVP_SHARED_BOARD_ACTIVE){
      advanceRoundAfterCompletedTurnPair(state,'PLAYER');
      state.turn='AI';state.aiControl=null;state.phase='Draw';state.pvpTurnReady=false;removeStartOfTurnTargetPreventionForSide(state,'AI');
      pushLog(state,'PvP shared board: Opponent enters Round '+state.round+' Draw Phase automatically.');
      resolveDrawPhase(state,'AI',{deferAnimation:true});syncCounts(state);return;
    }
    advanceRoundAfterCompletedTurnPair(state,'PLAYER');
    runAITurn(state);
  }
  function advancePhase(){
    if(!matchStarted || !appState || appState.gameOver) return;
    if(animationBusy()) return;
    var state=appState;
    if(state.pending){ showInfo('Action Pending','Resolve the required choice before advancing.'); return; }
    if(window.GL_PVP_SHARED_BOARD_ACTIVE && state.pvpTurnReady){ showInfo('Your Turn','Acknowledge Your Turn before Draw Phase resolves.'); return; }
    if(state.turn!=='PLAYER'){
      if(window.GL_PVP_SHARED_BOARD_ACTIVE){ showInfo('Waiting for Opponent','PvP shared board is active. This browser is waiting for the other player to publish the next board state.'); return; }
      return;
    }
    if(state.phase==='Draw'){
      if(!window.GL_PVP_SHARED_BOARD_ACTIVE){if(state.drawPhaseResolvedFor!=='PLAYER')resolveDrawPhase(state,'PLAYER');autoAdvancePlayerDrawWhenReady(state);syncCounts(state);render();return;}
      if(state.drawPhaseResolvedFor!=='PLAYER')resolveDrawPhase(state,'PLAYER');if(!state.gameOver&&!state.pending){state.phase='Deploy';state.drawPhaseResolvedFor=null;}
    }
    else if(state.phase==='Deploy'){ pushLog(state,'PLAYER moves to Battle Phase.'); state.phase='Battle'; resolvePendingCastingsFor(state,'PLAYER'); }
    else if(state.phase==='Battle'){ expireBattleAttachments(state); removeTemporaryBattleBuffsForSide(state,'PLAYER'); pushLog(state,'PLAYER moves to Reform Phase.'); state.phase='Reform'; state.tributeUsedThisReform=false; }
    else if(state.phase==='Reform'){ pushLog(state,'PLAYER enters End Phase and ends the turn automatically.'); state.phase='End';state.autoEndTurnPending=true; continueAutomaticPlayerEndTurn(state); }
    else if(state.phase==='End'){ state.autoEndTurnPending=true; continueAutomaticPlayerEndTurn(state); }
    syncCounts(state); render(); if(state.gameOver) showResult();
  }
  function runAITurnImmediate(state){
    state.turn='AI'; state.aiControl=null; state.phase='Draw'; removeStartOfTurnTargetPreventionForSide(state,'AI'); pushLog(state,'AI turn begins.');
    resolveDrawPhase(state,'AI');
    if(state.gameOver){ syncCounts(state); return; }
    state.phase='Deploy'; pushLog(state,'AI checks legal Deploy actions.');
    executeAIAction(state,chooseAIAction(state,'Deploy')); if(state.pending || state.responseWindow || state.gameOver) return;
    state.phase='Battle'; resolvePendingCastingsFor(state,'AI'); if(state.pending || state.responseWindow || state.gameOver) return;
    pushLog(state,'AI checks legal Battle actions.');
    executeAIAction(state,chooseAIAction(state,'Battle')); if(state.pending || state.responseWindow || state.gameOver) return;
    finishAITurnFromBattleImmediate(state);
  }
  function aiDirectorPauseForResponse(state,phase,label){
    state.aiControl={resume:'after_player_response',from_phase:phase,director:true};
    aiDirectorStatus(state,'Waiting for PLAYER response'+(label?' to '+label:'')+'.');
  }
  function aiDirectorEnterDeploy(state){
    if(!state || state.gameOver) return; state.phase='Deploy'; aiDirectorStatus(state,'AI enters Deploy Phase.');
    aiDirectorSchedule(function(){ var action=chooseAIAction(state,'Deploy'); var label=action&&action.card_id?cardName(card(action.card_id)):'no card'; aiDirectorStatus(state,action&&action.type!=='SKIP'?'AI plays '+label+'.':'AI passes Deploy Phase.'); executeAIAction(state,action); if(state.pending||state.responseWindow){ if(state.responseWindow&&!state.aiControl) aiDirectorPauseForResponse(state,'Deploy',label); return; } aiDirectorWaitForAnimations(state,function(){aiDirectorEnterBattle(state);},360); },520);
  }
  function aiDirectorEnterBattle(state){
    if(!state || state.gameOver) return; state.phase='Battle'; aiDirectorStatus(state,'AI enters Battle Phase.');
    aiDirectorSchedule(function(){ resolvePendingCastingsFor(state,'AI'); if(state.pending||state.responseWindow){ aiDirectorPauseForResponse(state,'Battle',state.responseWindow?cardName(card(state.responseWindow.card_id)):'casting action'); return; } var action=chooseAIAction(state,'Battle'); var label=action&&action.card_id?cardName(card(action.card_id)):'no attack'; aiDirectorStatus(state,action&&action.type!=='SKIP'?'AI declares '+label+'.':'AI passes Battle Phase.'); executeAIAction(state,action); if(state.pending||state.responseWindow){ if(state.responseWindow&&!state.aiControl) aiDirectorPauseForResponse(state,'Battle',label); return; } aiDirectorWaitForAnimations(state,function(){aiDirectorEnterReform(state);},420); },560);
  }
  function aiDirectorEnterReform(state){
    if(!state || state.gameOver) return; expireBattleAttachments(state); removeTemporaryBattleBuffsForSide(state,'AI'); state.phase='Reform'; state.tributeUsedThisReform=false; aiDirectorStatus(state,'AI enters Reform Phase.');
    aiDirectorSchedule(function(){ var action=chooseAIAction(state,'Reform'); var label=action&&action.card_id?cardName(card(action.card_id)):'no action'; aiDirectorStatus(state,action&&action.type==='TRIBUTE_CARD'?'AI Tributes '+label+'.':(action&&action.type!=='SKIP'?'AI uses '+label+'.':'AI passes Reform Phase.')); executeAIAction(state,action); if(state.pending||state.responseWindow){ if(state.responseWindow&&!state.aiControl) aiDirectorPauseForResponse(state,'Reform',label); return; } aiDirectorWaitForAnimations(state,function(){aiDirectorEnterEnd(state);},380); },520);
  }
  function aiDirectorEnterEnd(state){
    if(!state || state.gameOver) return; state.phase='End'; aiDirectorStatus(state,'AI enters End Phase.');
    aiDirectorSchedule(function(){ cleanupHandLimit(state,'AI'); resolveEndPhaseCleanupForSide(state,'AI'); if(state.pending||state.responseWindow||state.gameOver) return; aiDirectorStatus(state,'AI resolves End Phase cleanup.'); aiDirectorWaitForAnimations(state,function(){ clearAIPlanAtTurnEnd(state); advanceRoundAfterCompletedTurnPair(state,'AI'); GL_AI_TURN_DIRECTOR.active=false; beginPlayerTurnDrawPhase(state,'AI ends turn. PLAYER begins Draw Phase for Round '+state.round+'.'); if(state.gameOver) showResult(); },440); },500);
  }
  function runAITurnDirector(state){
    cancelAITurnDirector(); GL_AI_TURN_DIRECTOR.active=true; GL_AI_TURN_DIRECTOR.token++; state.turn='AI'; state.aiControl=null; state.phase='Draw'; removeStartOfTurnTargetPreventionForSide(state,'AI'); pushLog(state,'AI turn begins.'); aiDirectorStatus(state,'AI enters Draw Phase.');
    aiDirectorSchedule(function(){ var regen=Number(state.aiManaRegen||0); resolveDrawPhase(state,'AI'); if(state.gameOver){ GL_AI_TURN_DIRECTOR.active=false; syncCounts(state); render(); return; } aiDirectorStatus(state,'AI draws 1 card and gains '+regen+' Mana.'); aiDirectorWaitForAnimations(state,function(){aiDirectorEnterDeploy(state);},380); },620);
  }
  function runAITurn(state){ if(aiTurnDirectorAvailable()) return runAITurnDirector(state); return runAITurnImmediate(state); }
  function resumeAITurnAfterPlayerResponse(state){
    if(state && state.turn==='AI' && state.aiControl && state.aiControl.resume==='after_player_response' && state.aiControl.director && !state.pending && !state.responseWindow){ var from=state.aiControl.from_phase||state.phase; state.aiControl=null; aiDirectorStatus(state,'Response resolved. AI continues '+from+' Phase.'); aiDirectorWaitForAnimations(state,function(){ if(from==='Deploy') aiDirectorEnterBattle(state); else if(from==='Reform') aiDirectorEnterEnd(state); else aiDirectorEnterReform(state); },380); return true; }
    return resumeAITurnAfterPlayerResponseImmediate(state);
  }
  function clearTransientUiState(){
    GL_LAST_PLAYER_TURN_BANNER_KEY='';removePlayerTurnBanner();
    closeChoice(); closeInfo(); closeResponseWindowUI();
    if($('previewOverlay')) $('previewOverlay').classList.remove('open');
    if($('choiceBody')) $('choiceBody').innerHTML='';
    if($('infoBody')) $('infoBody').innerHTML='';
    if($('responseBody')) $('responseBody').innerHTML='';
    if($('responseFooter')) $('responseFooter').innerHTML='';
    if($('previewBody')) $('previewBody').innerHTML='';
  }
  function resetMatch(){cancelAITurnDirector();GL_LAST_RENDERED_PHASE_KEY=null;closeLocalCoinModal();if(!matchStarted){clearTransientUiState();showDeckSetup();return;}clearTransientUiState();appState=buildInitialMatchState();matchStarted=true;render();if(!IS_PVP_APP)showLocalCoinModal();}
  function sideResultSummary(state, side){
    var heroes=sideHeroes(state,side)||{};
    var active=activeNonLegacyLanes(state,side).map(function(l){ return l+': '+cardName(card(heroes[l].card_id))+' HP '+heroes[l].hp+'/'+heroes[l].maxHp; });
    var legacy=LANE_ORDER.filter(function(l){ return isLegacyModeHero(heroes[l]); }).map(function(l){ return l+': '+cardName(card(heroes[l].card_id)); });
    var hand=sideHand(state,side).length, deck=side==='PLAYER'?state.playerDeck.length:state.aiDeck.length, discard=sideDiscard(state,side).length, mana=side==='PLAYER'?state.mana:state.aiMana, legacyDeck=side==='PLAYER'?state.playerLegacy.length:state.aiLegacy.length;
    return side+' — Active Heroes: '+(active.length?active.join('; '):'none')+'\nLegacy Mode: '+(legacy.length?legacy.join('; '):'none')+'\nMana '+mana+' · Hand '+hand+' · Deck '+deck+' · Discard '+discard+' · Legacy Deck '+legacyDeck;
  }
  function gameResultLines(state){
    var lines=[];
    lines.push((state.winner||'Unknown')+' wins.');
    lines.push('Reason:');
    lines.push(state.gameEndReason||'Game ended.');
    lines.push('Round '+(state.round||1)+' · '+(state.turn||'')+' '+(state.phase||'')+' Phase');
    return lines;
  }
  function pvpGameResultHtml(state){
    var r=state&&state.pvpGameResult;
    var winner=(r&&r.winnerName)||((state.winner==='AI'?'Player 2':(state.winner==='PLAYER'?'Player 1':state.winner))||'Winner');
    var reason=(r&&r.reason)||state.gameEndReason||'Game ended.';
    var round=(r&&r.round)||state.round||1;
    var phase=(r&&r.phase)||state.phase||'Unknown';
    return '<div class="pvp-result-body"><p><b>Winner:</b> '+esc(winner)+'</p><p><b>Reason:</b><br>'+esc(reason)+'</p><p><b>Ended at:</b> Round '+esc(round)+' · '+esc(phase)+' Phase</p><div class="confirmActions"><button id="pvpResultBackLobby" class="gold" type="button">Back to Lobby</button></div></div>';
  }
  function localGameResultHtml(state){
    var winner=(state&&state.winner)||'Unknown';
    var reason=(state&&state.gameEndReason)||'Game ended.';
    var round=(state&&state.round)||1;
    var turn=(state&&state.turn)||'';
    var phase=(state&&state.phase)||'';
    return '<div class="game-result-summary">'
      +'<section class="game-result-winner"><span>Winner</span><strong>'+esc(winner)+'</strong></section>'
      +'<section class="game-result-reason"><span>Reason</span><strong>'+esc(reason)+'</strong></section>'
      +'<section class="game-result-round"><span>Round</span><strong>'+esc(round)+'</strong><small>'+esc(turn+(phase?' · '+phase+' Phase':''))+'</small></section>'
      +'</div>';
  }
  function showResult(){
    if(!appState || !appState.gameOver) return;
    appState.gameResultShown=true;
    if(window.GL_PVP_SHARED_BOARD_ACTIVE){ showInfoHtml('PvP Game Result', pvpGameResultHtml(appState)); return; }
    showInfoHtml('Game Result', localGameResultHtml(appState));
  }
  function cardTile(id, options){ options=options||{}; var c=card(id); var cost=cardCost(c); var subtitle=options.subtitle || cardSubtype(c) || cardFamily(c); return '<button class="card-tile '+esc(options.cls||'')+'" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(c))+' thumbnail"><span class="card-tile-name">'+esc(cardName(c))+'</span><span class="card-tile-meta">'+esc(subtitle)+(cost!==''?' · Mana '+esc(cost):'')+'</span></button>'; }
  function hiddenCardBack(entry,fanIndex,fanCount){entry=entry||{index:fanIndex,hidden:false};var style=fanStyle(fanIndex,fanCount,'AI');return'<span class="opponent-hand-slot '+(entry.hidden?'hand-slot-placeholder':'')+'" data-hand-side="AI" data-hand-slot-index="'+Number(entry.index)+'" style="'+style+'">'+(entry.hidden?'':'<img class="back" src="https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp" alt="Hidden opponent Main Deck card '+(Number(entry.index)+1)+'">')+'</span>'; }
  function badge(label, detail){ return '<button class="state-badge" type="button" data-info-title="'+esc(label)+'" data-info-body="'+esc(detail||label)+'">'+esc(label)+'</button>'; }
  function legacyOverlayInfo(hero){
    if(!hero || !isLegacyModeHero(hero)) return '';
    var snap=hero.defeated_hero_snapshot || hero.legacy_overlay || null;
    if(!snap || !snap.card_id) return 'Legacy Mode';
    var sc=card(snap.card_id);
    var cls=heroClass(snap)||heroClass(sc)||'';
    var rank=heroRank(snap)||heroRank(sc)||'';
    return 'Legacy Mode · Overlay: '+cardName(sc)+(cls?' · '+cls:'')+(rank?' · Rank '+rank:'');
  }
  function heroPanelMeta(hero, cardObj){ return isLegacyModeHero(hero) ? legacyOverlayInfo(hero) : heroIdentity(cardObj); }
  function heroIdFrom(v){ return typeof v==='string'?v:(v&&v.card_id); }
  function pvpIsLocalPlayerSeat(){ return !(window.GL_PVP_SHARED_BOARD_ACTIVE && window.GL_PVP_LOCAL_ROLE && window.GL_PVP_LOCAL_ROLE!=='player'); }
  function pvpPendingDecisionSide(p){
    if(!p) return null;
    return p.decision_side || p.response_owner || p.side || p.source_side || (p.type==='hand_limit_discard'?'PLAYER':null) || (p.type==='manual_reposition'?'PLAYER':null);
  }
  function pvpLocalOwnsPending(p){
    if(!(window.GL_PVP_SHARED_BOARD_ACTIVE && appState && appState.pvpHumanVsHuman)) return true;
    if(!pvpIsLocalPlayerSeat()) return false;
    var owner=pvpPendingDecisionSide(p);
    return owner==='PLAYER';
  }
  function pvpLocalOwnsResponseWindow(rw){
    if(!(window.GL_PVP_SHARED_BOARD_ACTIVE && appState && appState.pvpHumanVsHuman)) return true;
    if(!pvpIsLocalPlayerSeat()) return false;
    return !!rw && rw.response_owner==='PLAYER';
  }
  function pvpHideChoiceForNonOwner(){
    if(window.GL_PVP_SHARED_BOARD_ACTIVE && $('choiceOverlay')) $('choiceOverlay').classList.remove('open','deck-setup-open','hand-discard-open');
  }

  function heroSelectionClass(side,lane){
    if(!appState || !appState.pending) return '';
    var p=appState.pending;
    if(window.GL_PVP_SHARED_BOARD_ACTIVE && appState.pvpHumanVsHuman && !pvpLocalOwnsPending(p)) return '';
    if(p.type==='source_selection'){
      if(side==='PLAYER' && p.legal_sources.indexOf(lane)!==-1) return ' selectable-legal source-candidate card-play-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='exact_two_target_selection'){
      if(side===p.source_side && lane===p.source_lane) return ' selectable-selected card-play-selected';
      if(side===p.target_side && (p.selected_target_lanes||[]).indexOf(lane)!==-1) return ' selectable-selected target-candidate card-play-selected';
      if(side===p.target_side && (p.legal_targets||[]).indexOf(lane)!==-1) return ' selectable-legal target-candidate card-play-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='double_casting_target_selection'){
      if(side===p.source_side && lane===p.source_lane) return ' selectable-selected card-play-selected';
      if(side===p.target_side && (p.legal_targets||[]).indexOf(lane)!==-1) return ' selectable-legal target-candidate card-play-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='target_selection'){
      if(side===p.source_side && lane===p.source_lane) return ' selectable-selected card-play-selected';
      if(side===(p.target_side||targetSideForActor(card(p.card_id),'PLAYER')) && p.legal_targets.indexOf(lane)!==-1) return ' selectable-legal target-candidate card-play-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='scouting_target_selection'){
      if(side===p.source_side && lane===p.source_lane) return ' selectable-selected card-play-selected';
      if(side===p.target_side && p.legal_targets.indexOf(lane)!==-1) return ' selectable-legal target-candidate card-play-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='tribute_target'){
      if(side==='PLAYER' && p.legal_targets.indexOf(lane)!==-1) return ' selectable-legal tribute-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='racial_target_selection'){
      if(side===p.target_side && p.legal_targets.indexOf(lane)!==-1) return ' selectable-legal target-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='hero_ability_target_selection'){
      if(side===p.source_side && lane===p.source_lane) return ' selectable-selected';
      if(side===p.target_side && p.legal_targets.indexOf(lane)!==-1) return ' selectable-legal target-candidate';
      return ' selectable-illegal';
    }
    if(p.type==='legacy_hero_target_selection'){
      if(side===p.source_side && lane===p.source_lane) return ' selectable-selected';
      if(side===p.target_side && p.legal_targets.indexOf(lane)!==-1) return ' selectable-legal target-candidate';
      return ' selectable-illegal';
    }
    return '';
  }
  function legacyPanelActions(side,lane){
    if(!(appState&&matchStarted&&side==='PLAYER'&&appState.turn==='PLAYER')) return '';
    return getActivatedLegacyAbilities(appState,side,lane).map(function(a){ return '<button class="actionBtn gold legacyAbilityAction" type="button" data-legacy-side="'+esc(side)+'" data-legacy-lane="'+esc(lane)+'" data-legacy-id="'+esc(a.abilityId)+'">'+esc(a.label)+'</button>'; }).join('');
  }
  function attachmentCounterLabel(hero, cardId, side, lane, slot){
    if(!hero || !cardId) return '';
    side=side||hero.side; lane=lane||hero.lane;
    var attached=(typeof slot==='number' && hero.attachments)?hero.attachments[slot]:(hero.attachments||[]).find(function(v){ return attachmentCardId(v)===cardId; });
    if(attached && typeof attached==='object'){
      var ac=(typeof attached.counters!=='undefined')?Number(attached.counters||0):null;
      var ar=(typeof attached.required_counters!=='undefined')?Number(attached.required_counters||0):null;
      if(ac!==null && ar) return '('+ac+')';
      var av=[attached.remaining_count,attached.remaining_turns,attached.turns_remaining,attached.duration,attached.remaining].find(function(v){ return typeof v!=='undefined' && v!==null && Number(v)>=0; });
      if(typeof av!=='undefined' && av!==null && Number(av)>0) return '('+Number(av)+')';
    }
    var pc=(appState&&appState.pendingCastings||[]).find(function(x){ return x && x.card_id===cardId && (!side || x.side===side) && ((!lane || (x.original_source_lane||x.source_lane)===lane) && castingSourceMatchesHero(x,hero)) && (typeof slot!=='number' || Number(x.attachmentSlot)===Number(slot)); });
    if(pc){
      if(typeof pc.counters!=='undefined') return '('+Number(pc.counters||0)+')';
      var pr=[pc.remaining_count,pc.remaining_turns,pc.turns_remaining,pc.duration,pc.remaining].find(function(v){ return typeof v!=='undefined' && v!==null && Number(v)>0; });
      if(typeof pr!=='undefined') return '('+Number(pr)+')';
    }
    // The Runtime Attachment record is the counter authority. Status duration is only a
    // compatibility fallback and must never override a registered Attachment counter.
    var active=(appState&&appState.activeAttachments||[]).find(function(a){ return a && a.card_id===cardId && (!side || a.side===side) && (!lane || a.lane===lane) && (typeof slot!=='number' || Number(a.slot)===Number(slot)); });
    if(active && isDrawCounterCastingCard(card(cardId))) return '('+Number(active.remaining||active.counters||0)+')';
    if(active){ var rr=[active.remaining_count,active.remaining_turns,active.turns_remaining,active.duration].find(function(v){ return typeof v!=='undefined' && v!==null && Number(v)>0; }); if(typeof rr!=='undefined') return '('+Number(rr)+')'; }
    var names={
      'S1-MAG-022':['Aether Infusion'],'S1-THF-027':['Untargetable by Single Target Attacks'],'S1-ITM-015':['Untargetable by Single Target Attacks'],'S1-MAG-018':['Double Casting'],'S1-ITM-011':['Poison Vial'],'S1-EVT-011':['Coordination Attack +10','Coordination Attack +20'],'S1-ITM-010':['Arcane Scroll +20'],'S1-ITM-013':['Ring of Grace +20'],'S1-ITM-014':['Holy Medallion +20'],'S1-WAR-005':['Taunt'],'S1-CLE-025':['Blessing of Divinity']
    }[cardId] || [cardName(card(cardId))];
    var st=(hero.statuses||[]).find(function(x){ if(names.indexOf(statusName(x))!==-1) return true; if(x && (x.card_id===cardId || x.source_card_id===cardId || x.source===cardId)) return true; if(x && Array.isArray(x.sources) && x.sources.indexOf(cardId)!==-1) return true; return false; });
    if(st){ var sr=[st.remaining_turns,st.turns_remaining,st.duration,st.remaining].find(function(v){ return typeof v!=='undefined' && v!==null && Number(v)>0; }); if(typeof sr!=='undefined') return '('+Number(sr)+')'; }
    return '';
  }
  function attachmentContextText(hero,slot,cardId,side,lane){
    if(!hero || !cardId) return 'Attachment Slot '+(Number(slot)+1);
    var ctx=['Attachment Slot '+(Number(slot)+1)]; side=side||hero.side; lane=lane||hero.lane;
    var pc=(appState&&appState.pendingCastings||[]).find(function(x){ return x.card_id===cardId && (!side || x.side===side) && ((!lane || (x.original_source_lane||x.source_lane)===lane) && castingSourceMatchesHero(x,hero)) && Number(x.attachmentSlot)===Number(slot); });
    if(pc){ ctx.push('CAST pending'); ctx.push('Source: '+pc.side+' '+pc.source_lane); if(pc.target_side&&pc.target_lane) ctx.push('Locked target: '+pc.target_side+' '+pc.target_lane); if(pc.resolve_phase) ctx.push('Resolve condition: '+String(pc.resolve_phase).replace(/_/g,' ')); if(typeof pc.counters!=='undefined') ctx.push('Counter: '+Number(pc.counters||0)); }
    var active=(appState&&appState.activeAttachments||[]).find(function(a){ return a.card_id===cardId && (!side || a.side===side) && (!lane || a.lane===lane) && Number(a.slot)===Number(slot); });
    if(active){ if(active.expires) ctx.push('Expires: '+String(active.expires).replace(/_/g,' ')); if(typeof active.remaining_count!=='undefined') ctx.push('Remaining: '+Number(active.remaining_count||0)); else if(typeof active.remaining!=='undefined') ctx.push('Counter: '+Number(active.remaining||0)); }
    return ctx.join(' · ');
  }
  function v94VisibleStatuses(hero){
    var out=[];
    ((hero&&hero.statuses)||[]).forEach(function(st){
      var sn=statusName(st);
      if(/Exhausted|Ready/i.test(sn)) return;
      if(/^(Aether Infusion|Untargetable by Single Target Attacks|Physical Damage -20|Taunt|Double Casting|Poison Vial|Arcane Scroll \+20|Ring of Grace \+20|Holy Medallion \+20|Coordination Attack \+(10|20)|Physical Attack \+20|Magical Attack \+20|Heaven's Fury Double|Last Resort Double|Blessing of Divinity|Enrage \+20|Triple Shot Area)$/i.test(sn)) return;
      out.push(st);
    });
    return out;
  }
  var V58_STATUS_ICON_ASSETS={Bleed:'assets/status-icons/Icon-Bleed.png',Stun:'assets/status-icons/Icon-Stun.png',Burn:'assets/status-icons/Icon-Burn.png',Freeze:'assets/status-icons/Icon-Freeze.png',Poison:'assets/status-icons/Icon-Poison.png'};
  function v56NegativeStatuses(hero){
    return ((hero&&hero.statuses)||[]).filter(function(st){return isNegativeStatusName(statusName(st));});
  }
  function v56HeroInfoLines(hero,side){
    var lines=[];
    if(hero&&hero.exhausted) lines.push(hero.exhaust_reason?('Exhausted — '+cleanDisplayText(hero.exhaust_reason,'Exhausted')):'Exhausted');
    ((hero&&hero.statuses)||[]).forEach(function(st){
      var n=statusName(st);
      if(!n||isNegativeStatusName(n)||/^(Ready|Exhausted)$/i.test(n)) return;
      lines.push(statusBadgeLabel(st));
    });
    if(hero&&hero.casting) lines.push('Casting');
    if(hero && /^(Arbalest|Grand Arbalest)$/i.test(heroClass(hero))){
      var drawn=Number(appState&&appState.cardsDrawnThisTurn&&appState.cardsDrawnThisTurn[side]||0);
      lines.push('Draw This Turn: '+drawn);
    }
    return lines.filter(function(x,i,a){return x&&a.indexOf(x)===i;});
  }
  function v510IsHeroOrLegacySource(srcId,name){
    var sourceCard=srcId?card(srcId):null;
    if(sourceCard && /^(Hero|Legacy)$/i.test(cardFamily(sourceCard))) return true;
    var normalized=String(name||'').trim().toLowerCase();
    if(!normalized) return false;
    var defs=window.GL_CARD_DEFINITIONS||{};
    return Object.keys(defs).some(function(id){ var c=defs[id]; return c && /^(Hero|Legacy)$/i.test(cardFamily(c)) && String(cardName(c)||'').trim().toLowerCase()===normalized; });
  }
  function v58StatusTooltipText(st){
    var lines=[], duration=Number(st&&st.duration||0);
    if(duration>0) lines.push('Duration: '+duration);
    var names=[];
    ((st&&st.sources)||[]).forEach(function(src){
      var name='', sourceId=src&&src.source_card_id, sourceCard=sourceId?card(sourceId):null, resolvedName=sourceCard?cardName(sourceCard):'';
      if(sourceCard && cardFamily(sourceCard)!=='Unknown' && resolvedName && resolvedName!==sourceId) name=resolvedName;
      else if(src&&src.source_name) name=cleanDisplayText(src.source_name,'');
      name=String(name||'').trim();
      if(!name || v510IsHeroOrLegacySource(sourceId,name)) return;
      if(/^(attack[- ]?connected|connected attack|direct effect|response effect|final hp damage|source|poison|burn|bleed|freeze|stun)$/i.test(name)) return;
      if(/attack[- ]?connected|final hp damage|damage >? 0/i.test(name)) return;
      if(names.indexOf(name)===-1) names.push(name);
    });
    if(names.length) lines.push('Source: '+names.join(', '));
    return lines.join('\n') || ('Duration: '+duration);
  }
  function v56StatusControl(hero,side){
    var negative=v56NegativeStatuses(hero), info=v56HeroInfoLines(hero,side), orient=side==='AI'?'top':'bottom';
    var infoHtml=info.length?'<div class="hero-info-indicator"><button class="status-trigger" type="button" aria-label="Show Hero information">!</button><div class="hero-indicator-tooltip" role="tooltip"><strong>Hero Information</strong>'+info.map(function(x){return '<span>'+esc(x)+'</span>';}).join('')+'</div></div>':'';
    var statusHtml=negative.map(function(st){
      var n=statusName(st), key=Object.keys(V58_STATUS_ICON_ASSETS).find(function(k){return k.toLowerCase()===n.toLowerCase();}), src=key?V58_STATUS_ICON_ASSETS[key]:'';
      if(!src) return '';
      return '<div class="negative-status-indicator"><button type="button" aria-label="'+esc(statusBadgeLabel(st))+'"><img src="'+esc(src)+'" alt="'+esc(n)+'"></button><div class="hero-indicator-tooltip" role="tooltip"><strong>'+esc(statusBadgeLabel(st))+'</strong><span>'+esc(v58StatusTooltipText(st))+'</span></div></div>';
    }).join('');
    var mobileLines=[];
    negative.forEach(function(st){mobileLines.push(statusBadgeLabel(st)+' — '+v58StatusTooltipText(st));});
    info.forEach(function(line){if(mobileLines.indexOf(line)===-1)mobileLines.push(line);});
    var mobileSummary=mobileLines.length?'<button class="mobile-status-summary" type="button" data-info-title="Hero Status" data-info-body="'+esc(mobileLines.join('\n'))+'" aria-label="Show Hero status and restrictions">!</button>':'';
    if(!infoHtml&&!statusHtml) return mobileSummary||'<div class="hero-indicator-stack hero-indicator-stack--empty" aria-hidden="true"></div>';
    return '<div class="hero-indicator-stack hero-indicator-stack--'+orient+'">'+infoHtml+statusHtml+'</div>'+mobileSummary;
  }
  function v540DefinedMobileActions(hero,c,legacy){
    var out=[];
    if(legacy){var la=legacyAbilityData(c);if(la)out.push({kind:'Legacy Ability',label:la.name||cardName(c),text:la.text||c.card_text||''});return out;}
    var racial=c&&c.racial_ability||{},ra=racial.action||{};
    if(ra&&Object.keys(ra).length&&String(ra.type||'').toLowerCase().indexOf('response')===-1&&String(racial.timing_policy||'').toLowerCase().indexOf('response racial')===-1)out.push({kind:'Racial Trait',label:racial.name||'Racial Trait',text:racial.text||''});
    var cls=c&&c.class_ability||{},ca=cls.action||{};
    if(ca&&Object.keys(ca).length)out.push({kind:'Hero Ability',label:cls.name||cls.label||'Hero Ability',text:cls.text||c.card_text||''});
    return out;
  }
  function v540MobileActionTrigger(side,lane,hero,c,legacy){
    if(side!=='PLAYER'||!appState||!matchStarted)return '';
    var legal=[];
    getActivatedRacialAbilities(appState,side,lane).forEach(function(x){legal.push({kind:'racial',ability:x});});
    getActivatedHeroAbilities(appState,side,lane).forEach(function(x){legal.push({kind:'class',ability:x});});
    getActivatedLegacyAbilities(appState,side,lane).forEach(function(x){legal.push({kind:'legacy',ability:x});});
    var defined=v540DefinedMobileActions(hero,c,legacy), hasAny=legal.length||defined.length;
    if(!hasAny)return '';
    var label=legal.length?(legal.length===1?(legal[0].ability.label||'Action'):(legal.length+' actions available')):'No action available now';
    return '<button class="mobile-hero-action-trigger '+(legal.length?'is-available':'is-unavailable')+'" type="button" data-mobile-hero-action="1" data-mobile-action-side="'+esc(side)+'" data-mobile-action-lane="'+esc(lane)+'" aria-label="'+esc(label)+'" title="'+esc(label)+'">✦'+(legal.length>1?'<span>'+legal.length+'</span>':'')+'</button>';
  }
  function v540OpenMobileHeroActions(side,lane){
    if(!appState)return false;
    var hero=sideHeroes(appState,side)[lane],id=heroIdFrom(hero),c=card(id),legacy=isLegacyModeHero(hero),items=[];
    getActivatedRacialAbilities(appState,side,lane).forEach(function(x){items.push('<button class="mobile-action-choice racialAbilityAction" type="button" data-racial-side="'+esc(side)+'" data-racial-lane="'+esc(lane)+'" data-racial-id="'+esc(x.abilityId)+'"><strong>'+esc(x.label)+'</strong><span>Racial Trait · Available</span></button>');});
    getActivatedHeroAbilities(appState,side,lane).forEach(function(x){items.push('<button class="mobile-action-choice classAbilityAction" type="button" data-class-ability-side="'+esc(side)+'" data-class-ability-lane="'+esc(lane)+'" data-class-ability-id="'+esc(x.abilityId)+'"><strong>'+esc(x.label)+'</strong><span>Hero Ability · Available</span></button>');});
    getActivatedLegacyAbilities(appState,side,lane).forEach(function(x){items.push('<button class="mobile-action-choice legacyAbilityAction" type="button" data-legacy-side="'+esc(side)+'" data-legacy-lane="'+esc(lane)+'" data-legacy-id="'+esc(x.abilityId)+'"><strong>'+esc(x.label)+'</strong><span>Legacy Ability · Available</span></button>');});
    if(!items.length){
      var defs=v540DefinedMobileActions(hero,c,legacy);
      items=defs.map(function(x){return '<article class="mobile-action-choice is-disabled"><strong>'+esc(x.label)+'</strong><span>'+esc(x.kind)+' · Unavailable in the current phase or state.</span>'+(x.text?'<small>'+esc(x.text)+'</small>':'')+'</article>';});
    }
    showInfoHtml('Actions — '+cardName(c),'<div class="mobile-hero-action-menu">'+(items.join('')||'<p>No active Hero or Legacy action is available.</p>')+'</div>');return true;
  }
  function v94AttachmentSlot(side,lane,hero,index){
    var a=(hero&&hero.attachments&&hero.attachments[index])||null, aid=attachmentCardId(a);
    if(!aid) return '<div class="slot" data-attachment-side="'+esc(side)+'" data-attachment-lane="'+esc(lane)+'" data-attachment-slot="'+index+'">Attachment Slot '+(index+1)+'</div>';
    var ct=attachmentCounterLabel(hero,aid,side,lane,index), ctx=attachmentContextText(hero,index,aid,side,lane)+(ct?' · Remaining '+ct:'');
    return '<div class="slot filled" data-attachment-side="'+esc(side)+'" data-attachment-lane="'+esc(lane)+'" data-attachment-slot="'+index+'"><button type="button" data-preview="'+esc(aid)+'" data-preview-context="'+esc(ctx)+'"><img src="'+esc(thumbFor(aid))+'" alt="'+esc(cardName(card(aid)))+'"><span>'+esc(cardName(card(aid)))+(ct?' <em class="attachment-counter">'+esc(ct)+'</em>':'')+'</span></button></div>';
  }
  function v94HeroActions(side,lane){
    if(!(appState&&matchStarted&&side==='PLAYER'&&appState.turn==='PLAYER')) return '';
    var a=[];
    getActivatedRacialAbilities(appState,side,lane).forEach(function(x){a.push('<button class="actionBtn gold racialAbilityAction" type="button" data-racial-side="'+esc(side)+'" data-racial-lane="'+esc(lane)+'" data-racial-id="'+esc(x.abilityId)+'">'+esc(x.label)+'</button>');});
    getActivatedHeroAbilities(appState,side,lane).forEach(function(x){a.push('<button class="actionBtn gold classAbilityAction" type="button" data-class-ability-side="'+esc(side)+'" data-class-ability-lane="'+esc(lane)+'" data-class-ability-id="'+esc(x.abilityId)+'">Class Skill · '+esc(x.label)+'</button>');});
    getActivatedLegacyAbilities(appState,side,lane).forEach(function(x){a.push('<button class="actionBtn gold legacyAbilityAction" type="button" data-legacy-side="'+esc(side)+'" data-legacy-lane="'+esc(lane)+'" data-legacy-id="'+esc(x.abilityId)+'">'+esc(x.label)+'</button>');});
    return a.join('');
  }
  function v94RacialCoins(side,count){
    count=Math.max(0,Math.min(2,Number(count||0)));
    var coins=[];
    for(var i=0;i<2;i++){
      var available=i<count, src=available?'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Head.webp':'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Tail.webp';
      coins.push('<button class="coin '+(available?'coin--head':'coin--tail')+'" type="button" aria-label="Racial Token '+(i+1)+' — '+(available?'available Head':'spent Tail')+'"><img src="'+src+'" alt="Racial Token '+(available?'Head':'Tail')+'"></button>');
    }
    return '<div class="racial-coins racial-coins--hand" data-racial-side="'+esc(side)+'" aria-label="'+esc(side)+' Racial Tokens">'+coins.join('')+'</div>';
  }
  function v94EventDetailHtml(entry,side){
    var group=entry&&entry.event?entry:{side:side,event:entry,chainEvents:[]},evt=group&&group.event;
    if(!evt)return '<p>No played action yet.</p>';
    side=group.side||side;var target=actionTargetLine(evt),responses=normalizeLines(evt.response_lines||evt.chain_lines),results=orderPlayedResultLines(evt.result_lines||evt.summary),reasons=normalizeLines(evt.reason_lines);
    var img=evt.card_id?'<div class="played-detail-card"><img src="'+esc(fullFor(evt.card_id))+'" alt="'+esc(evt.title||cardName(card(evt.card_id)))+'"></div>':'<div class="played-detail-card played-detail-card--empty">'+esc(evt.label||'ACTION')+'</div>';
    var action=evt.action_line||('Action: '+(side==='PLAYER'?'You':'Opponent')+' '+cleanDisplayText(evt.title||evt.label,'resolved an action')+'.');
    var chain=v520ChainStepsForGroup(group);
    var chainHtml=chain.length?'<div class="played-chain-block"><strong>Chain / Responses</strong><div class="played-chain-cards">'+chain.map(function(step,index){var title=step.title||cardName(card(step.card_id))||step.label||'Response';var text=step.text||step.action_line||'';return '<article class="played-chain-step"><img src="'+esc(thumbFor(step.card_id))+'" alt="'+esc(title)+'"><div><span>Resolution '+(index+1)+'</span><h4>'+esc(title)+'</h4>'+(text?'<p>'+esc(text)+'</p>':'')+'</div></article>';}).join('')+'</div></div>':'';
    var er=evt.effect_row,effectHtml=er?'<div class="played-audit-block"><strong>Effect Row</strong><ul><li>Source: '+esc(er.source_name)+' — '+esc(er.source_class)+' '+esc(er.source_rank)+'</li><li>Row Used: '+esc(er.row_used)+' — '+esc(er.match_type)+'</li><li>'+esc(er.base_label)+': '+esc(er.base_value)+'</li>'+(er.row_text?'<li>Row effect: '+esc(er.row_text)+'</li>':'')+'</ul></div>':'';
    var mods=evt.damage_modifiers||[],modHtml=mods.length?'<div class="played-audit-block"><strong>Damage Modifiers</strong><div class="played-modifier-list">'+mods.map(function(m){var name=m.source_name||m.reason||'Modifier',amount=Number(m.amount||0);return '<article class="played-modifier-item">'+(m.source_card_id?'<img src="'+esc(thumbFor(m.source_card_id))+'" alt="'+esc(name)+'">':'')+'<p><b>'+esc(name)+':</b> '+esc((amount>=0?'+':'')+amount)+' damage'+(m.reason?' — '+esc(m.reason):'')+'</p></article>';}).join('')+'</div></div>':'';
    var ds=evt.damage_summary,damageHtml=ds?'<div class="played-audit-block"><strong>Damage Resolution</strong><ul><li>Base damage: '+esc(ds.base_damage)+'</li><li>Damage after modifiers: '+esc(ds.damage_after_modifiers)+'</li>'+(Number(ds.conditional_damage_bonus||0)>0?'<li>Conditional damage bonus: +'+esc(ds.conditional_damage_bonus)+'</li>':'')+((Number(ds.response_block||0)+Number(ds.passive_reduction||0)+Number(ds.active_reduction||0))?'<li>Defense / prevention: '+esc(Number(ds.response_block||0)+Number(ds.passive_reduction||0)+Number(ds.active_reduction||0))+'</li>':'')+(Number(ds.conditional_damage_after_defense||0)>0?'<li>Conditional damage after defense: '+esc(ds.conditional_damage_after_defense)+'</li>':'')+'<li>Calculated final damage: '+esc(ds.calculated_final_damage)+'</li><li>Actual HP damage: '+esc(ds.actual_hp_damage)+'</li>'+(Number(ds.overkill||0)>0?'<li>Overkill: '+esc(ds.overkill)+'</li>':'')+'<li>Target HP: '+esc(ds.target_hp_before)+' → '+esc(ds.target_hp_after)+'</li></ul></div>':'';
    return '<div class="played-detail-layout">'+img+'<section class="played-detail-copy"><h3>'+esc(evt.title||evt.label||'Played Action')+'</h3><div><strong>Action</strong><p>'+esc(action.replace(/^Action:\s*/i,''))+'</p></div>'+(target?'<div><strong>Target</strong><p>'+esc(target)+'</p></div>':'')+effectHtml+modHtml+damageHtml+chainHtml+(responses.length?'<div><strong>Response Detail</strong><ul>'+responses.map(function(x){return '<li>'+esc(x)+'</li>';}).join('')+'</ul></div>':'')+'<div><strong>Result</strong>'+(results.length?'<ul>'+results.map(function(x){return '<li>'+esc(x.replace(/^Result:\s*/i,''))+'</li>';}).join('')+'</ul>':'<p>Resolved by Runtime.</p>')+'</div>'+(reasons.length?'<div><strong>Reason</strong><ul>'+reasons.map(function(x){return '<li>'+esc(x)+'</li>';}).join('')+'</ul></div>':'')+'</section></div>';
  }
  function v94EventsForSide(state,side){
    if(state&&state.pvpHumanVsHuman&&state.pvpActionEventsBySide)return((state.pvpActionEventsBySide[side])||[]).filter(function(e){return e&&e.card_id;});
    if(side==='PLAYER')return((state&&state.playerPlayedEvents)||[]).filter(function(e){return e&&e.card_id;});return opponentPlayedVisibleEvents(state).filter(function(e){return e&&e.card_id;});
  }
  function v204PlayedActor(side,upper){var label=side==='PLAYER'?'YOU':(window.GL_PVP_OPPONENT_NAME||'OPPONENT');return upper?String(label).toUpperCase():(side==='PLAYER'?'You':label);}
  function v520IsChainResponseEvent(evt){var label=String(evt&&evt.label||'').toUpperCase(),line=String(evt&&evt.action_line||'');return /^(DEF|REACTION|RESPONSE|COUNTER|NEGATE|CANCEL)$/.test(label)||/\b(as (?:a )?(?:Defense|Reaction|Response)|responded with|countered with|used .* to (?:cancel|negate|block|dodge|prevent))\b/i.test(line);}
  function v520EventTime(evt){return Number(evt&&evt.timestamp||0);}
  function v520CanAttachResponse(group,item){
    var a=group&&group.event,b=item&&item.event;if(!a||!b)return false;
    if(a.round!=null&&b.round!=null&&Number(a.round)!==Number(b.round))return false;
    if(a.phase&&b.phase&&String(a.phase)!==String(b.phase))return false;
    var aSource=a.source_side||group.side,aTarget=a.target_side,bSource=b.source_side||item.side,bTarget=b.target_side;
    var inverse=(aSource&&bTarget&&aSource===bTarget)||(aTarget&&bSource&&aTarget===bSource)||(group.side!==item.side);
    if(!inverse)return false;
    var laneMatch=!a.source_lane&&!a.target_lane&&!b.source_lane&&!b.target_lane||a.source_lane===b.target_lane||a.target_lane===b.source_lane||a.target_lane===b.target_lane;
    return laneMatch;
  }
  function v520KnownCardMatches(text){
    var raw=String(text||''),found=[],seenNames={};if(!raw)return found;var lower=raw.toLowerCase();
    (CARDS||[]).slice().sort(function(a,b){return cardName(b).length-cardName(a).length;}).forEach(function(c){var family=cardFamily(c),name=cardName(c),key=String(name||'').trim().toLowerCase();if(/^(Hero|Legacy)$/i.test(String(family||''))||!key||key.length<3||seenNames[key])return;if(lower.indexOf(key)!==-1){seenNames[key]=true;found.push({card_id:c.card_id,title:name,text:raw,synthetic:true});}});return found;
  }
  function v520ChainStepsForGroup(group){
    var explicit=(group&&group.chainEvents||[]).slice().sort(function(a,b){return v520EventTime(b.event)-v520EventTime(a.event);}).map(function(x){var e=x.event;return{card_id:e.card_id,title:e.title||cardName(card(e.card_id)),label:e.label,text:cleanDisplayText(e.action_line||normalizeLines(e.result_lines||e.summary)[0]||'','')};});
    var seen={};explicit.forEach(function(x){seen[x.card_id]=true;});var inferred=[];normalizeLines(group&&group.event&&(group.event.response_lines||group.event.chain_lines)).forEach(function(line){v520KnownCardMatches(line).forEach(function(x){if(!seen[x.card_id]){seen[x.card_id]=true;inferred.push(x);}});});return explicit.concat(inferred);
  }
  function v96CombinedPlayedEvents(state){
    var raw=v94EventsForSide(state,'PLAYER').map(function(e){return{side:'PLAYER',event:e};}).concat(v94EventsForSide(state,'AI').map(function(e){return{side:'AI',event:e};}));
    raw.sort(function(a,b){var d=v520EventTime(a.event)-v520EventTime(b.event);if(d)return d;var ar=v520IsChainResponseEvent(a.event),br=v520IsChainResponseEvent(b.event);if(ar!==br)return ar?1:-1;return String(a.event.id||'').localeCompare(String(b.event.id||''));});
    var groups=[];raw.forEach(function(item){if(v520IsChainResponseEvent(item.event)){for(var i=groups.length-1;i>=0;i--){if(v520CanAttachResponse(groups[i],item)){groups[i].chainEvents.push(item);groups[i].latestTimestamp=Math.max(groups[i].latestTimestamp,v520EventTime(item.event));return;}}}groups.push({side:item.side,event:item.event,chainEvents:[],latestTimestamp:v520EventTime(item.event)});});
    return groups.sort(function(a,b){var d=b.latestTimestamp-a.latestTimestamp;if(d)return d;return String(b.event.id||'').localeCompare(String(a.event.id||''));});
  }
  function v96CardPlayedPanel(state){
    var events=v96CombinedPlayedEvents(state),recent=events.slice(0,6);var tiles=recent.length?recent.map(function(x){var e=x.event,who=v204PlayedActor(x.side,false);return '<button class="combined-played-card" type="button" data-combined-event-id="'+esc(e.id)+'" data-combined-event-side="'+esc(x.side)+'" data-preview="'+esc(e.card_id)+'"><em>'+who+'</em><img src="'+esc(thumbFor(e.card_id))+'" alt="'+esc(who+' played '+(e.title||cardName(card(e.card_id))))+'"></button>';}).join(''):'<div class="played-empty">No played cards</div>';
    return '<section class="control-panel card-played-panel"><header><strong>Card Played</strong><span>Latest actions</span></header><div class="played-grid">'+tiles+'</div><button id="cardPlayedHistoryButton" type="button" class="full-width" '+(events.length?'':'disabled')+'>Full Card History</button></section>';
  }
  function v96ShowCardPlayedHistory(){var events=v96CombinedPlayedEvents(appState);var html=events.length?'<div class="played-history-grid combined-played-history">'+events.map(function(x){var e=x.event,who=v204PlayedActor(x.side,true);return '<button type="button" data-combined-archive-id="'+esc(e.id)+'" data-combined-archive-side="'+esc(x.side)+'" data-preview="'+esc(e.card_id)+'"><em>'+who+'</em><img src="'+esc(thumbFor(e.card_id))+'" alt="'+esc((e.title||cardName(card(e.card_id))))+'"><span>'+esc(e.label||'ACTION')+'</span></button>';}).join('')+'</div>':'<p>No played cards yet.</p>';showInfoHtml('Card Played — Full History',html);}
  function v96ShowCardPlayedDetail(side,eventId){var group=v96CombinedPlayedEvents(appState).find(function(x){return x.side===side&&x.event&&x.event.id===eventId;})||v96CombinedPlayedEvents(appState)[0];if(!group)return showInfo('Card Played','No played action yet.');GL_CARD_ZOOM_ID=group.event.card_id||GL_CARD_ZOOM_ID;showInfoHtml('Card Played — '+v204PlayedActor(group.side,false),v94EventDetailHtml(group,group.side));}
  function v94PlayedPanel(state,side){
    var events=v94EventsForSide(state,side), recent=events.slice(0,4), title=side==='PLAYER'?'Your Played':'Opponent Played';
    var tiles=recent.length?recent.map(function(e){return '<button class="played-card" type="button" data-side-event="'+esc(side)+'" data-side-event-id="'+esc(e.id)+'" data-preview="'+esc(e.card_id)+'"><img src="'+esc(thumbFor(e.card_id))+'" alt="'+esc(e.title||cardName(card(e.card_id)))+'"></button>';}).join(''):'<div class="played-empty">No played cards</div>';
    return '<section class="played-dock-panel played-dock-panel--'+esc(side.toLowerCase())+'"><div class="played-dock-head"><h2>'+title+'</h2><button type="button" data-played-history-side="'+esc(side)+'" '+(events.length?'':'disabled')+'>Full History</button></div><div class="recent-card-strip">'+tiles+'</div></section>';
  }
  function v94ShowPlayedHistory(side){
    var events=v94EventsForSide(appState,side), title=side==='PLAYER'?'Your Played — Full History':'Opponent Played — Full History';
    var html=events.length?'<div class="played-history-grid">'+events.map(function(e){return '<button type="button" data-side-archive="'+esc(side)+'" data-side-archive-id="'+esc(e.id)+'" data-preview="'+esc(e.card_id)+'"><img src="'+esc(thumbFor(e.card_id))+'" alt="'+esc(e.title||cardName(card(e.card_id)))+'"><span>'+esc(e.label||'ACTION')+'</span></button>';}).join('')+'</div>':'<p>No played cards yet.</p>';
    showInfoHtml(title,html);
  }
  function v94ShowPlayedDetail(side,eventId){
    var evt=v94EventsForSide(appState,side).find(function(e){return e.id===eventId;})||v94EventsForSide(appState,side)[0];
    if(!evt) return showInfo(side==='PLAYER'?'Your Played':'Opponent Played','No played action yet.');
    GL_CARD_ZOOM_ID=evt.card_id||GL_CARD_ZOOM_ID;
    showInfoHtml(side==='PLAYER'?'Your Played':'Opponent Played',v94EventDetailHtml(evt,side));
  }
  function v94SetCardZoom(cardId){
    if(!cardId||!card(cardId)) return;
    GL_CARD_ZOOM_ID=cardId;
    var img=$('cardZoomImage'); if(img){img.src=fullFor(cardId);img.alt=cardName(card(cardId));}
  }
  function v54AnyModalOpen(){
    return !!document.querySelector('.modal-overlay.open,.overlay.open');
  }
  function v54AnchorInsideOpenModal(anchor){
    return !!(anchor&&anchor.closest&&anchor.closest('.modal-overlay.open,.overlay.open'));
  }
  function v54BindModalHoverGuard(){
    if(GL_MODAL_HOVER_GUARD_BOUND||typeof MutationObserver==='undefined') return;
    GL_MODAL_HOVER_GUARD_BOUND=true;
    Array.prototype.forEach.call(document.querySelectorAll('#choiceOverlay,#infoOverlay,#responseOverlay,#previewOverlay'),function(overlay){
      new MutationObserver(function(){ if(overlay.classList.contains('open')) v94HoverZoomHide(); }).observe(overlay,{attributes:true,attributeFilter:['class']});
    });
  }
  function v94HoverZoomShow(cardId,ev){
    if(!cardId||!card(cardId)) return;
    var box=$('hoverCardZoom'),img=$('hoverCardZoomImage'); if(!box||!img) return;
    var anchor=ev&&ev.target&&ev.target.closest?ev.target.closest('[data-preview]'):null;
    if(v54AnyModalOpen()&&!v54AnchorInsideOpenModal(anchor)){ v94HoverZoomHide(); return; }
    box._glAnchor=anchor||null;
    img.src=fullFor(cardId);box.hidden=false;box.classList.add('is-visible');v94HoverZoomMove(ev);
  }
  function v94HoverZoomMove(ev){
    var box=$('hoverCardZoom');if(!box||box.hidden||!ev)return;
    var anchor=box._glAnchor,boxRect=box.getBoundingClientRect(),w=Math.max(1,boxRect.width||250),h=Math.max(1,boxRect.height||350);
    var x=Number(ev.clientX||0)+18,y=Number(ev.clientY||0)-h/2;
    var inModal=!!(anchor&&anchor.closest&&anchor.closest('.info-panel,.choice-panel,.response-panel,.preview-panel'));
    box.classList.toggle('is-modal-zoom',inModal);
    if(anchor&&anchor.getBoundingClientRect){
      var rect=anchor.getBoundingClientRect();
      x=rect.left+(rect.width-w)/2;
      y=rect.top-h-10;
      if(y<8)y=rect.bottom+10;
    }else if(x+w+12>window.innerWidth){x=Number(ev.clientX||0)-w-18;}
    x=Math.max(8,Math.min(x,window.innerWidth-w-8));
    y=Math.max(8,Math.min(y,window.innerHeight-h-8));
    box.style.transform='translate3d('+Math.round(x)+'px,'+Math.round(y)+'px,0)';
  }

  function v94HoverZoomHide(){var box=$('hoverCardZoom');if(!box)return;box.classList.remove('is-visible','is-modal-zoom','is-click-zoom','is-hand-hover');box.hidden=true;box._glAnchor=null;box.style.transform='translate3d(-9999px,-9999px,0)';box.setAttribute('aria-hidden','true');}
  function v59HandHoverZoomShow(cardId,anchor){
    /* Opponent/AI turn locks the original Hand card position, not its inspection layer.
       The enlarged hover preview is a separate fixed overlay and remains read-only. */
    if(!cardId||!card(cardId)||!anchor||v54AnyModalOpen()) return false;
    var box=$('hoverCardZoom'),img=$('hoverCardZoomImage'); if(!box||!img) return false;
    img.src=fullFor(cardId); img.alt=cardName(card(cardId))+' hand preview';
    box.hidden=false; box.classList.remove('is-click-zoom','is-modal-zoom'); box.classList.add('is-visible','is-hand-hover');
    box._glAnchor=anchor; box.setAttribute('aria-hidden','false');
    var rect=anchor.getBoundingClientRect(), boxRect=box.getBoundingClientRect();
    /* Keep the approved VS AI card-relative anchor. CSS alone controls the new 250x350 size;
       placement still uses the overlay's actual rendered rectangle exactly as before. */
    var w=Math.max(1,boxRect.width||148), h=Math.max(1,boxRect.height||218);
    var x=rect.left+(rect.width-w)/2, y=rect.top-h-10;
    if(y<8) y=rect.bottom+10;
    x=Math.max(8,Math.min(x,window.innerWidth-w-8));
    y=Math.max(8,Math.min(y,window.innerHeight-h-8));
    box.style.transform='translate3d('+Math.round(x)+'px,'+Math.round(y)+'px,0)';
    return true;
  }
  function v59HandHoverZoomHide(anchor){
    var box=$('hoverCardZoom'); if(!box||box.classList.contains('is-click-zoom')) return;
    if(anchor&&box._glAnchor&&box._glAnchor!==anchor) return;
    v94HoverZoomHide();
  }
  function v55ClickZoomShow(cardId){
    if(!cardId||!card(cardId)) return false;
    var box=$('hoverCardZoom'),img=$('hoverCardZoomImage'); if(!box||!img) return false;
    img.src=fullFor(cardId); img.alt=cardName(card(cardId))+' enlarged card';
    box.hidden=false; box.classList.remove('is-hand-hover'); box.classList.toggle('is-modal-zoom',v54AnyModalOpen()); box.classList.add('is-visible','is-click-zoom');
    box.style.transform='none'; box.setAttribute('aria-hidden','false'); box._glAnchor=null;
    return true;
  }
  function v55ClickZoomHide(){ v94HoverZoomHide(); }
  function v55ZoomPlayedCard(side,eventId){
    v96ShowCardPlayedDetail(side,eventId);
  }
  function v54HeroExpStack(hero){
    var total=Number(hero&&hero.exp_total||0);
    if(total<=0) return '';
    return '<button class="hero-exp-badge" type="button" data-info-title="EXP Stack" data-info-body="'+esc(expStackDetail(hero))+'" aria-label="'+esc(total)+' EXP">'+esc(total)+' EXP</button>';
  }
  function legacyCardDisplayName(cardId){
    var direct=cardName(card(cardId));
    if(direct && direct!==cardId) return direct;
    var keys=Object.keys(STARTER_DECK_OPTIONS||{});
    for(var i=0;i<keys.length;i++){
      var deck=STARTER_DECK_OPTIONS[keys[i]]&&STARTER_DECK_OPTIONS[keys[i]].deck;
      var entries=(deck&&deck.legacy_deck_expanded)||[];
      for(var j=0;j<entries.length;j++) if(entries[j]&&entries[j].card_id===cardId) return entries[j].card_name||direct||cardId;
    }
    return direct||cardId||'Legacy';
  }
  function heroPanel(side,lane,hero,state){
    var h=hero||{}, id=heroIdFrom(h), c=card(id), isAI=side==='AI';
    var selection=heroSelectionClass(side,lane), legacy=isLegacyModeHero(h);
    var terminalDefeated=!!(state&&state.gameOver&&!legacy&&Number(h.hp||0)<=0&&((state.winner==='PLAYER'&&side==='AI')||(state.winner==='AI'&&side==='PLAYER')));
    var attachments='<div class="attachment-row attach">'+v94AttachmentSlot(side,lane,h,0)+v94AttachmentSlot(side,lane,h,1)+'</div>';
    var actions=v94HeroActions(side,lane);
    if(legacy){
      sanitizeLegacySlot(h);
      var defeatedUnderLegacy=(h.defeated_hero_snapshot&&h.defeated_hero_snapshot.card_id)||h.original_hero_card_id||'';
      var legacyInfoAttrs=defeatedUnderLegacy?' type="button" data-preview="'+esc(defeatedUnderLegacy)+'" title="Hero under this Legacy: '+esc(cardName(card(defeatedUnderLegacy)))+'" aria-label="View Hero under this Legacy"':'';
      var legacyInfoDesktop=defeatedUnderLegacy?'<button class="legacy-hero-info legacy-hero-info--desktop"'+legacyInfoAttrs+'>!</button>':'';
      var legacyInfoMobile=defeatedUnderLegacy?'<button class="legacy-hero-info legacy-hero-info--mobile"'+legacyInfoAttrs+'>!</button>':'';
      var legacyHealth='<div class="hero-health-row legacy-health"><div class="legacy-name-bar"><small>'+esc(legacyCardDisplayName(id))+'</small>'+legacyInfoDesktop+'</div></div>';
      var mobileAction=v540MobileActionTrigger(side,lane,h,c,true);var legacyStage='<div class="hero-stage legacy-stage">'+legacyInfoMobile+mobileAction+'<button class="hero-card hero-main" type="button" data-preview="'+esc(id)+'"><img class="heroImg" src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(c))+'"></button><div class="heroActions">'+actions+'</div></div>';
      return '<article class="hero-lane hero-panel legacy-slot '+esc(selection)+'" data-side="'+esc(side)+'" data-lane="'+esc(lane)+'" data-slot-mode="LEGACY">'+(isAI?attachments+legacyStage+legacyHealth:legacyHealth+legacyStage+attachments)+'</article>';
    }
    var hp=(h.hp===undefined||h.hp===null)?Number(c.hp||100):Number(h.hp);
    var max=(h.maxHp===undefined||h.maxHp===null)?Number(c.hp||100):Number(h.maxHp);
    if(terminalDefeated) hp=0;
    var pct=max?Math.max(0,Math.min(100,Math.round(hp/max*100))):0;
    var statusControl=terminalDefeated?'':v56StatusControl(h,side);
    var statusLayer=statusControl?'<div class="hero-status-overlay">'+statusControl+'</div>':'';
    var health='<div class="hero-health-row hero-health-overlay '+(terminalDefeated?'terminal-defeated-health':'')+'"><div class="hp-bar '+(pct<=40?'is-low':(pct<=60?'is-warning':''))+'" style="--hp-value:'+pct+'%;--hp-ratio:'+(Math.max(0,Math.min(100,pct))/100)+'"><span></span><small>'+esc(hp)+'/'+esc(max)+'</small></div></div>';
    var visibleStatuses=v94VisibleStatuses(h).map(function(st){return statusBadgeLabel(st);});
    var meta=cardName(c)+'\n'+heroPanelMeta(h,c)+'\nHP '+hp+'/'+max+' · EXP '+Number(h.exp_total||0)+' · '+((h.exhausted)?'Exhausted':'Ready')+(visibleStatuses.length?'\nStatus: '+visibleStatuses.join(', '):'');
    var expStack=terminalDefeated?'':v54HeroExpStack(h);
    var stage;
    if(terminalDefeated){
      stage='<div class="hero-stage terminal-defeated-stage"><div class="hero-card-anchor"><button class="hero-card hero-main terminal-defeated-card" type="button" disabled aria-label="Defeated Hero card back"><img class="heroImg terminal-defeated-card-back" src="https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp" alt="Defeated Hero"></button>'+health+'</div><div class="heroActions"></div></div>';
    }else{
      var mobileAction=v540MobileActionTrigger(side,lane,h,c,false);stage='<div class="hero-stage '+(expStack?'has-exp':'')+'">'+statusLayer+mobileAction+'<div class="hero-card-anchor"><button class="hero-card hero-main" type="button" data-preview="'+esc(id)+'"><img class="heroImg '+(h.exhausted?'exhausted-card':'')+'" src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(c))+'"></button>'+health+'</div>'+expStack+'<div class="heroActions">'+actions+'</div></div>';
    }
    return '<article class="hero-lane hero-panel '+(terminalDefeated?'terminal-defeated-slot ':'')+esc(selection)+'" data-side="'+esc(side)+'" data-lane="'+esc(lane)+'" data-slot-mode="'+(terminalDefeated?'DEFEATED':'HERO')+'">'+(isAI?attachments+stage:stage+attachments)+'</article>';
  }
  function resourceZone(side,type,count,clickable,extraClass,regen,topCardId,racialCount){
    var isMana=type==='Mana Pool',isMain=type==='Main Deck',isLegacy=type==='Legacy Deck',isDiscard=type==='Discard Pile';
    var icon=isMana?'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Mana-Shard-Thumb.webp':(isLegacy?'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Legacy-Deck.webp':(isDiscard?(topCardId?thumbFor(topCardId):''):'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp'));
    var emptyDiscard=isDiscard&&!topCardId;
    var cls='zone '+(isMana?'zone--mana':isLegacy?'zone--legacy':isDiscard?'zone--discard':'zone--card')+' '+(emptyDiscard?'zone--empty-discard ':'')+(extraClass||'');
    var attrs=' data-zone-side="'+esc(side)+'" data-zone-type="'+esc(type)+'"';
    if(isDiscard) attrs+=' data-discard-side="'+esc(side)+'"';
    if(isLegacy&&side==='PLAYER') attrs+=' data-legacy-side="PLAYER"';
    var number=isMana?('<span class="mana-current">'+esc(Number(count||0))+'</span><i>/</i><span class="mana-max">12</span>'):esc(count||0);
    var imageHtml=icon?'<img src="'+esc(icon)+'" alt="'+esc(type)+'">':'';
    var inner='<span>'+esc(type)+'</span><div class="zoneCard zone-card-stack '+(isMana?'zone-card-stack--mana ':'')+(emptyDiscard?'zone-card-stack--empty':'')+'">'+imageHtml+'<b'+(isMana?' class="mana-value"':'')+'>'+number+'</b></div>'+(isMana?'<em>Mana Regen +'+esc(regen||1)+'</em>':'');
    return '<button class="'+cls+'" type="button"'+attrs+((isLegacy&&side!=='PLAYER')||isMain||isMana?' aria-disabled="true"':'')+'>'+inner+'</button>';
  }
  function mobileRacialResourceZone(side,count){
    var racialValue=Math.max(0,Math.min(2,Number(count||0)));
    var faces=[0,1].map(function(i){
      var active=i<racialValue,src=active?'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Head.webp':'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Tail.webp';
      return '<img src="'+src+'" alt="Racial Token '+(i+1)+' '+(active?'available':'spent')+'">';
    }).join('');
    return '<div class="zone mobile-racial-zone" data-zone-side="'+esc(side)+'" data-zone-type="Racial Token" aria-label="Racial Tokens '+racialValue+' of 2"><span>Racial</span><div class="mobile-racial-token-stack">'+faces+'</div></div>';
  }
  function field(side,heroes,state){
    var isAI=side==='AI', mana=isAI?state.aiMana:state.mana, legacy=isAI?state.aiLegacyCount:state.playerLegacyCount;
    var main=isAI?state.aiDeckCount:state.playerDeckCount, discardList=isAI?(state.aiDiscard||[]):(state.playerDiscard||[]), discard=discardList.length;
    var manaZone=resourceZone(side,'Mana Pool',mana,false,'',isAI?state.aiManaRegen:state.manaRegen,null,0);
    var legacyZone=resourceZone(side,'Legacy Deck',legacy,side==='PLAYER','');
    var mainZone=resourceZone(side,'Main Deck',main,false,'',null,null,0);
    var discardZone=resourceZone(side,'Discard Pile',discard,true,'',null,discardList.length?discardList[discardList.length-1]:null);
    var racialZone=mobileRacialResourceZone(side,isAI?state.aiRacial:state.racial);
    var left=isAI?(mainZone+discardZone):(manaZone+legacyZone), right=isAI?(legacyZone+manaZone+racialZone):(discardZone+mainZone+racialZone);
    return '<section class="field '+(isAI?'field--opponent ai-field':'field--player player-field')+'"><aside class="resource-rail '+(!isAI?'resource-rail--paired':'')+'">'+left+'</aside><div class="hero-row '+(isAI?'hero-row--opponent':'hero-row--player')+'">'+LANE_ORDER.map(function(lane){return heroPanel(side,lane,heroes[lane],state);}).join('')+'</div><aside class="resource-rail '+(isAI?'resource-rail--paired':'')+'">'+right+'</aside></section>';
  }
  function deckZone(side,type,count,clickable){ var cls='zone-card '+type.toLowerCase().replace(/\s+/g,'-')+(clickable?' clickable':' locked'); var data=clickable?' data-discard-side="'+esc(side)+'"':''; return '<button class="'+cls+'" type="button" '+data+(clickable?'':' disabled')+'><span>'+esc(type)+'</span><b>'+esc(count)+'</b></button>'; }
  function currentShellState(){
    if(appState) return syncCounts(appState);
    var pMain=deckEntriesToIds(decks.PLAYER.main_deck), aMain=deckEntriesToIds(decks.AI.main_deck);
    return {phase:'Setup', turn:'PLAYER', round:1, mana:0, manaRegen:2, racial:2, aiMana:0, aiManaRegen:2, aiRacial:2, aiHandCount:0, playerDeckCount:pMain.length, aiDeckCount:aMain.length, playerLegacyCount:(decks.PLAYER.legacy_deck_expanded||[]).length, aiLegacyCount:(decks.AI.legacy_deck_expanded||[]).length, playerDiscard:[], aiDiscard:[], playerHand:[], playerHeroes:buildHeroMap(decks.PLAYER,'PLAYER'), aiHeroes:buildHeroMap(decks.AI,'AI'), playerLegacy:(decks.PLAYER.legacy_deck_expanded||[]).slice(), aiLegacy:(decks.AI.legacy_deck_expanded||[]).slice(), playerDeckName:decks.PLAYER.deck_name, aiDeckName:decks.AI.deck_name, log:['Deck Setup ready.']};
  }
  function statusLine(state){
    if(!matchStarted) return 'Generated starter decks validate. Start Match begins the Step 7 core systems loop.';
    if(state.gameOver) return 'Game Over: '+state.winner+' wins.';
    if(state.pending && state.pending.type==='hand_limit_discard') return 'Waiting: discard '+state.pending.required+' card'+(state.pending.required>1?'s':'')+' to hand limit.';
    if(state.pending && state.pending.type==='response_window') return 'Waiting: response decision for '+cardName(card(state.pending.card_id))+'.';
    if(state.pending && state.pending.type==='source_selection') return 'Waiting: choose acting Hero for '+cardName(card(state.pending.card_id))+'.';
    if(state.pending && state.pending.type==='target_selection') return 'Waiting: choose target for '+cardName(card(state.pending.card_id))+'.';
    if(state.pending && state.pending.type==='double_casting_target_selection') return 'Waiting: choose target 2/2 for Double Casting.';
    if(state.pending && state.pending.type==='scouting_target_selection') return 'Waiting: choose opponent Hero EXP stack for Scouting.';
    if(state.pending && state.pending.type==='scouting_exp_selection') return 'Waiting: choose EXP Card for Scouting.';
    if(state.pending && state.pending.type==='crystal_ball_reorder') return 'Waiting: confirm Crystal Ball deck order.';
    if(state.pending && state.pending.type==='tribute_target') return 'Waiting: choose receiving Hero for Tribute '+cardName(card(state.pending.card_id))+'.';
    if(state.pending && state.pending.type==='hero_ability_target_selection') return 'Waiting: choose target for Class Ability.';
    if(state.pending && state.pending.type==='legacy_hero_target_selection') return 'Waiting: choose target for Legacy Ability.';
    if(state.pending && state.pending.type==='legacy_cost_selection') return 'Waiting: choose Legacy cost cards.';
    if(state.pending && state.pending.type==='legacy_card_choice') return 'Waiting: choose Legacy card.';
    if(state.pending && state.pending.type==='legacy_defeat_choice') return 'Waiting: choose Legacy replacement for defeated Hero.';
    if(state.pending && state.pending.type==='racial_stoneblood') return 'Waiting: use or decline Stoneblood.';
    if(state.pending && state.pending.type==='saint_purify_choice') return 'Waiting: choose 1 negative status for Holy Rejuvenation.';
    if(state.pending && state.pending.type==='card_search_choice') return 'Waiting: choose card for '+cardName(card(state.pending.source_card_id))+'.';
    if(state.pending && state.pending.type==='mana_spend_choice') return 'Waiting: choose Mana spend for '+cardName(card(state.pending.card_id))+'.';
    if(state.pending && state.pending.type==='exact_two_target_selection') return 'Waiting: click 2 distinct opponent Hero/Legacy slots for '+cardName(card(state.pending.card_id))+' ('+Number((state.pending.selected_target_lanes||[]).length)+'/2).';
    if(state.pending && state.pending.type==='optional_target_swap') return 'Waiting: choose optional target swap for '+cardName(card(state.pending.card_id))+'.';
    if(state.pending && state.pending.type==='manual_reposition') return 'Waiting: choose Reposition pair or cancel.';
    if(state.turn==='AI' && state.aiPresentationStatus) return state.aiPresentationStatus;
    return state.turn+' · '+state.phase+' Phase · Mana '+state.mana+'/'+state.manaRegen+' Regen · AI Mana '+state.aiMana+'/'+state.aiManaRegen+' Regen';
  }
  function renderStartupDeckSetup(){
    document.body.classList.add('deck-setup-active');
    var root=$('app');
    root.innerHTML=''+
      '<main class="deck-setup-screen runtime-ui-v14-setup">'+
        '<header class="ai-lobby-topbar"><div class="ai-lobby-logo"><img src="assets/lobby/grandis-legacy-logo.webp" alt="Grandis Legacy"></div><div class="ai-lobby-heading"><h1>VS AI LOBBY</h1></div><nav class="ai-lobby-actions"><a id="aiLobbyTutorialButton" class="ai-lobby-btn ai-lobby-btn--outline" href="tutorial/">TUTORIAL</a><a id="aiLobbyDeckBuilderButton" class="ai-lobby-btn ai-lobby-btn--outline" href="https://grandislegacytcg.github.io/Grandis-Legacy-Deck-Builder/" target="_blank" rel="noopener">GO TO DECK BUILDER</a><a id="aiLobbyPvpButton" class="ai-lobby-btn ai-lobby-btn--blue" href="https://p01--grandis-legacy-pvp--2kwws8nzlcc2.code.run/" target="_blank" rel="noopener">GO TO PVP</a></nav></header>'+
        '<section class="deck-setup-shell">'+deckSetupHtml()+'</section>'+ 
      '</main>';
    closeChoice();
    bindDeckSetupModal();
  }
  function playerTurnBannerKey(state){
    if(!state||state.gameOver||state.preGame||state.turn!=='PLAYER'||state.phase!=='Draw')return '';
    return 'round-'+Number(state.round||1)+'-player-turn';
  }
  function removePlayerTurnBanner(){
    if(GL_PLAYER_TURN_BANNER_TIMER){clearTimeout(GL_PLAYER_TURN_BANNER_TIMER);GL_PLAYER_TURN_BANNER_TIMER=null;}
    if(!animationDocumentReady())return;
    var old=document.getElementById('glPlayerTurnAnnouncement');if(old&&old.parentNode)old.parentNode.removeChild(old);
  }
  function showPlayerTurnBanner(state){
    if(!animationDocumentReady()||SUPPRESS_RENDER)return false;
    removePlayerTurnBanner();
    var wrap=document.createElement('div');wrap.id='glPlayerTurnAnnouncement';wrap.className='gl-turn-announcement';wrap.setAttribute('role','status');wrap.setAttribute('aria-live','polite');
    var ribbon=document.createElement('div');ribbon.className='gl-turn-announcement-ribbon';
    var title=document.createElement('strong');title.textContent='YOUR TURN';
    var sub=document.createElement('span');sub.textContent='Round '+Number(state&&state.round||1)+' · Draw Phase';
    ribbon.appendChild(title);ribbon.appendChild(sub);wrap.appendChild(ribbon);document.body.appendChild(wrap);
    GL_PLAYER_TURN_BANNER_TIMER=setTimeout(function(){if(wrap&&wrap.parentNode)wrap.parentNode.removeChild(wrap);GL_PLAYER_TURN_BANNER_TIMER=null;},1750);
    return true;
  }
  function maybeShowPlayerTurnBanner(state){
    var key=playerTurnBannerKey(state);if(!key||key===GL_LAST_PLAYER_TURN_BANNER_KEY)return false;
    GL_LAST_PLAYER_TURN_BANNER_KEY=key;nextVisualFrame(function(){showPlayerTurnBanner(state);});return true;
  }
  var GL_CASTING_PAIR_COLORS=['#35d8ff','#b786ff','#ffd45c','#57e99a','#ff7ab6','#ff9b52'];
  function clearCastingPairHighlights(){
    if(typeof document==='undefined')return;
    Array.prototype.forEach.call(document.querySelectorAll('.gl-casting-pair-highlight'),function(el){el.classList.remove('gl-casting-pair-highlight');el.style.removeProperty('--gl-casting-pair-color');el.removeAttribute('data-casting-pair-highlight');});
  }
  function applyCastingPairHighlights(state){
    if(typeof document==='undefined')return false;clearCastingPairHighlights();
    var applied=false;
    (state&&state.pendingCastings||[]).forEach(function(pc,index){
      if(!pc||pc.attachmentSlot==null)return;
      var sourceSide=pc.side||pc.source_side,sourceLane=pc.source_lane||pc.original_source_lane;
      var targetSide=pc.target_side,targetLane=pc.locked_target_lane||pc.target_lane;
      if(!sourceSide||!sourceLane||!targetSide||!targetLane)return;
      var color=GL_CASTING_PAIR_COLORS[index%GL_CASTING_PAIR_COLORS.length];
      var attachment=document.querySelector('.slot[data-attachment-side="'+sourceSide+'"][data-attachment-lane="'+sourceLane+'"][data-attachment-slot="'+Number(pc.attachmentSlot)+'"]');
      var target=document.querySelector('.hero-panel[data-side="'+targetSide+'"][data-lane="'+targetLane+'"] .hero-stage')||document.querySelector('.hero-panel[data-side="'+targetSide+'"][data-lane="'+targetLane+'"]');
      [attachment,target].forEach(function(el){if(!el)return;el.classList.add('gl-casting-pair-highlight');el.style.setProperty('--gl-casting-pair-color',color);el.setAttribute('data-casting-pair-highlight',String(index));applied=true;});
    });
    return applied;
  }

  function render(){
    if(SUPPRESS_RENDER) return;
    var root=$('app');
    if(!matchStarted){ renderStartupDeckSetup(); return; }
    document.body.classList.remove('deck-setup-active');
    var savedMobileHandScroll=captureLocalMobileHandScroll();
    var state=currentShellState(),visibleAIHand=presentationHandEntries('AI',state.aiHand||[]),visibleAIHandCount=visibleAIHand.length,visiblePlayerHand=presentationHandEntries('PLAYER',state.playerHand||[]);
    var hoverBox=$('hoverCardZoom'); if(hoverBox&&!hoverBox.classList.contains('is-click-zoom')) v94HoverZoomHide();
    var zoomId=GL_CARD_ZOOM_ID||(v94EventsForSide(state,'PLAYER')[0]&&v94EventsForSide(state,'PLAYER')[0].card_id)||(visiblePlayerHand[0]&&visiblePlayerHand[0].id)||heroIdFrom(state.playerHeroes.CENTER)||heroIdFrom(state.playerHeroes.LEFT);
    GL_CARD_ZOOM_ID=zoomId;
    var nextDisabled=!matchStarted||state.gameOver||state.turn!=='PLAYER'||!!state.pending||gameplayInputLocked()||(!window.GL_PVP_SHARED_BOARD_ACTIVE&&state.turn==='PLAYER'&&state.phase==='Draw');
    var activeLabel=state.preGame?'Opening Match':(state.turn==='PLAYER'?'Your Turn':(IS_PVP_APP?'Opponent Turn':'AI Turn'));
    var aiLine=state.preGame?'Waiting':(state.turn==='AI'?(IS_PVP_APP?'Playing':(state.aiPresentationStatus||'Thinking')):'Waiting');
    var latestLog=(state.log&&state.log[0])||'No match log yet.';
    root.innerHTML='<div class="app battlefield-app v94-app v96-app '+(state.turn==='AI'?'turn-ai':'turn-player')+' '+((state.responseWindow&&pvpLocalOwnsResponseWindow(state.responseWindow))?'response-active':'')+'"><main class="gl-app">'+
      '<section class="main-stage gl-board">'+
        '<header class="player-name player-name--opponent">'+(IS_PVP_APP?'OPPONENT':'LOCAL AI')+' <span>'+esc(state.aiDeckName||'Opponent')+'</span></header>'+
        '<section class="hand-area hand-area--opponent"><div class="hand-spacer" aria-hidden="true"></div><div class="strip v94-strip"><div class="backs hand-row opponent-hand">'+visibleAIHand.map(function(entry,i){return hiddenCardBack(entry,i,visibleAIHandCount);}).join('')+'</div></div>'+v94RacialCoins('AI',state.aiRacial)+'</section>'+
        field('AI',state.aiHeroes,state)+
        '<div class="field-divider" aria-hidden="true"></div>'+
        field('PLAYER',state.playerHeroes,state)+
        '<section class="hand-area hand-area--player">'+v94RacialCoins('PLAYER',state.racial)+'<div class="handPanel player-hand hand-row"><div class="handCards">'+(visiblePlayerHand.map(function(entry,i){return handCard(entry.id,entry.index,state,i,visiblePlayerHand.length,entry.hidden);}).join('')||'<div class="empty-state compact">Player hand is empty.</div>')+'</div></div><div class="hand-spacer" aria-hidden="true"></div></section>'+
        '<footer class="player-footer-bar desktop-player-footer"><div class="player-name player-name--self">PLAYER <span>'+esc(state.playerDeckName||'Your Deck')+'</span></div><button class="battle-log-bottom-button" type="button" data-battle-log-button="1">Full Battle Log</button></footer>'+
      '</section>'+
      '<aside class="control-sidebar">'+
        '<section class="control-panel turn-panel"><div class="setup-actions"><button id="deckSetupButton" type="button">Deck Setup</button></div><button id="mobileMatchMenuButton" class="mobile-match-menu-button" type="button" aria-haspopup="dialog" aria-controls="mobileMatchMenuOverlay" aria-expanded="false">Match Menu</button><div class="turn-summary"><strong>'+esc(activeLabel)+'</strong><span>Round '+esc(state.round||1)+' · '+esc(state.turn==='PLAYER'?'Player':'AI')+'</span><span>Phase: '+esc(state.phase)+'</span><span>Mana '+esc(state.mana)+' / 12 · Regen +'+esc(state.manaRegen)+'</span><span>AI: '+esc(aiLine)+'</span><small class="turn-status">'+esc(statusLine(state))+'</small></div></section>'+
        '<section class="control-panel phase-panel '+((state.turn==='AI'&&GL_AI_TURN_DIRECTOR.active)?'aiTurnDirector':'')+'"><header><strong>Phase Tracker</strong><span>Current: '+esc(state.phase)+'</span></header><div class="phase-list">'+PHASES.map(function(p){return '<button type="button" class="'+(p===state.phase?'is-active':'')+'" disabled>'+esc(p)+' Phase</button>';}).join('')+'</div><div class="phase-actions"><button id="repositionButton" class="reposition" type="button" '+(!manualRepositionButtonEnabled(state)?'disabled':'')+'>Reposition</button><button id="cancelActionButton" class="cancel-action" type="button" '+(!isCancelablePreCommitPending(state.pending)?'disabled':'')+'>Cancel Action</button><button id="nextPhaseButton" class="next-phase" type="button" '+(nextDisabled?'disabled':'')+'>'+(state.phase==='End'?'End Turn':'Next Phase')+'</button></div></section>'+
        '<footer class="player-footer-bar mobile-player-footer"><div class="player-name player-name--self">PLAYER <span>'+esc(state.playerDeckName||'Your Deck')+'</span></div><button class="battle-log-bottom-button" type="button" data-battle-log-button="1">Full Battle Log</button></footer>'+
        v96CardPlayedPanel(state)+
        '<section class="control-panel match-panel"><button id="soundToggleButton" class="sound-toggle" type="button" aria-pressed="'+(GL_CARD_SOUND_ENABLED?'true':'false')+'">'+esc(cardMotionSoundLabel())+'</button>'+(IS_PVP_APP?'<button id="pvpRoomMobileButton" class="pvp-room-mobile" type="button">PvP Room</button>':'')+'<button id="surrenderButton" class="surrender" type="button">Surrender</button></section>'+
        '<div id="mobileMatchMenuOverlay" class="mobile-match-menu-overlay" hidden><section class="mobile-match-menu-sheet" role="dialog" aria-modal="true" aria-labelledby="mobileMatchMenuTitle"><header><strong id="mobileMatchMenuTitle">Match Menu</strong><button id="mobileMatchMenuClose" type="button" aria-label="Close Match Menu">Close</button></header><div class="mobile-match-menu-actions"><button id="mobileDeckSetupButton" type="button">Deck Setup</button><button id="mobileSoundToggleButton" class="sound-toggle" type="button" aria-pressed="'+(GL_CARD_SOUND_ENABLED?'true':'false')+'">'+esc(cardMotionSoundLabel())+'</button>'+(IS_PVP_APP?'<button id="mobilePvpRoomButton" class="pvp-room-mobile" type="button">PvP Room</button>':'')+'<button id="mobileSurrenderButton" class="surrender" type="button">Surrender</button></div></section></div>'+
      '</aside>'+
    '</main></div>';
    prepareRenderedImages(root);
    applyCastingPairHighlights(state);
    bindDynamicButtons();
    bindLocalMobileHandScroll();
    flushBattleFeedbackQueue();
    restoreLocalMobileHandScroll(savedMobileHandScroll);
    maybeShowPlayerTurnBanner(state);
    v54BindModalHoverGuard();
    Array.prototype.forEach.call(document.querySelectorAll('[data-battle-log-button]'),function(hb){hb.onclick=function(){var lines=(appState&&appState.log)||['No runtime action history yet.'];showInfo('Full Battle Log',lines.join('\n'));};});
    if(state.responseWindow&&pvpLocalOwnsResponseWindow(state.responseWindow))renderResponseWindow();else closeResponseWindowUI();
    var localPendingOwner=!state.pending||pvpLocalOwnsPending(state.pending);
    if(!state.pending)closeChoice();if(!localPendingOwner)pvpHideChoiceForNonOwner();
    if(localPendingOwner&&state.pending&&state.pending.type==='hand_limit_discard')renderHandLimitDiscardChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='card_search_choice')renderCardSearchChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='mana_spend_choice')renderArrowBarrageManaSpendChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='optional_magical_surge')renderMagicalSurgeChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='opponent_hand_choice')renderOpponentHandChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='legacy_defeat_choice')renderLegacyDefeatChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='scouting_exp_selection')renderScoutingExpChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='crystal_ball_reorder')renderCrystalBallReorderChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='status_removal_choice')renderStatusRemovalChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='saint_purify_choice')renderSaintPurifyChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='racial_stoneblood')renderStonebloodChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='racial_second_chance')renderSecondChanceChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='legacy_cost_selection')renderLegacyCostChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='legacy_card_choice')renderLegacyCardChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='optional_swap')renderOptionalSwapChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='optional_target_swap')renderOptionalTargetSwapChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='manual_reposition')renderManualRepositionChoice();
    scheduleAutomaticPlayerEndResume(appState);
    if(localPendingOwner&&state.pending&&state.pending.type==='draw_replacement_choice')renderDrawReplacementChoice();
    if(localPendingOwner&&state.pending&&state.pending.type==='response_extra_discard_choice')renderResponseExtraDiscardChoice();
    if(appState&&appState.gameOver&&!appState.gameResultShown)setTimeout(function(){if(appState&&appState.gameOver&&!appState.gameResultShown)showResult();},0);
    if(IS_PVP_APP&&typeof window!=='undefined'&&typeof window.GL_PVP_AFTER_RENDER==='function')nextVisualFrame(function(){try{window.GL_PVP_AFTER_RENDER();}catch(ignore){}});
  }
  function handCard(id,idx,state,fanIndex,fanCount,hidden){var style=fanStyle(fanIndex,fanCount,'PLAYER');if(hidden)return'<article class="cardTile hand-card hand-slot-placeholder" data-hand-index="'+idx+'" data-hand-slot-index="'+idx+'" data-hand-side="PLAYER" style="'+style+'" aria-hidden="true"></article>';if(id==='__HIDDEN_CARD_BACK__'||id==='__HIDDEN_CARD__')return'<article class="cardTile hand-card spectator-hand-back unavailable" data-hand-index="'+idx+'" data-hand-slot-index="'+idx+'" data-hand-side="PLAYER" style="'+style+'" aria-label="Hidden Main Deck card"><span class="hand-art" aria-hidden="true"><img src="https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp" alt="Hidden Main Deck card"></span><div class="hand-actions"></div></article>';var c=card(id),lp=state?legalPlayState(state,id):{can:false,reasons:['match not started']},tp=state?legalTributeState(state,id):{can:false,reasons:['match not started']},actions='',drawBusy=(GL_HIDDEN_DRAW_TOKENS.PLAYER||[]).length>0,selected=!drawBusy&&!!(state&&state.pending&&state.pending.card_id===id&&Number(state.pending.hand_index)===Number(idx));if(!drawBusy&&(!state||!state.pending)){if(lp.can)actions+='<button class="mini-action play-card-action" type="button" data-play-index="'+idx+'">Play</button>';if(tp.can)actions+='<button class="mini-action tribute-card-action" type="button" data-tribute-index="'+idx+'">Tribute</button>';}return'<article class="cardTile hand-card '+(selected?'is-selected ':'')+((!drawBusy&&(lp.can||tp.can))?'legal playable':'unavailable')+'" data-hand-index="'+idx+'" data-hand-slot-index="'+idx+'" data-card-id="'+esc(id)+'" data-hand-side="PLAYER" style="'+style+'"><button class="hand-art" type="button" data-preview="'+esc(id)+'" aria-label="Preview '+esc(cardName(c))+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(c))+'"></button><div class="hand-actions">'+actions+'</div></article>'; }
  function opponentPlayedDetail(a,state){ return opponentEventDetail(a); }

  function opponentPlayedVisibleEvents(state){
    return ((state&&state.opponentPlayedEvents)||[]).filter(function(e){ return e && visibleOpponentEventTypes().indexOf(e.label)!==-1; });
  }
  function showInfoHtml(title, html){ $('infoTitle').textContent=title; $('infoBody').innerHTML=html||'<p>No additional detail available.</p>'; $('infoOverlay').classList.add('open'); bringModalToFront($('infoOverlay')); }
  function showOpponentEventDetail(evt){ if(!evt) return showInfo('Opponent Played','No opponent action yet.'); showInfo('Opponent Played', opponentPlayedDetail(evt,appState)); }
  function opponentPlayedArchiveHtml(state){
    var events=opponentPlayedVisibleEvents(state);
    if(!events.length) return '<p>No opponent action yet.</p>';
    return '<p class="choice-instruction">Opponent Played archive shows visual opponent card/action events only. System text stays in the separate Battle Log.</p><div class="opponent-archive-grid">'+events.map(function(a){
      var img=a.card_id?'<img src="'+esc(thumbFor(a.card_id))+'" alt="'+esc(a.title||a.label)+'">':'<span>'+esc(a.label||'ACTION')+'</span>';
      return '<button type="button" class="actionIcon archiveActionIcon" data-op-archive-id="'+esc(a.id)+'"><div class="iconBox">'+img+'</div><div class="tag">'+esc(a.label)+'</div><small>'+esc(a.title||'Action')+'</small></button>';
    }).join('')+'</div>';
  }
  function showOpponentPlayedArchive(){ showInfoHtml('Opponent Played Archive', opponentPlayedArchiveHtml(appState)); }
  function opponentPlayed(state){
    var events=opponentPlayedVisibleEvents(state).slice(0,4);
    var latest=events[0] || null;
    var tiles=events.length?events.map(function(a){
      var detail=opponentPlayedDetail(a,state); var selected=state&&state.selectedOpponentEventId===a.id;
      if(a.card_id){ return '<button class="actionIcon '+(selected?'selected':'')+'" type="button" data-op-event-id="'+esc(a.id)+'" data-info-title="Opponent Played" data-info-body="'+esc(detail)+'"><div class="iconBox"><img src="'+esc(thumbFor(a.card_id))+'" alt="'+esc(a.title||a.label)+'"></div><div class="tag">'+esc(a.label)+'</div></button>'; }
      return '<button class="actionIcon resolveIcon '+(selected?'selected':'')+'" type="button" data-op-event-id="'+esc(a.id)+'" data-info-title="Opponent Played" data-info-body="'+esc(detail)+'"><div class="iconBox">'+esc(a.label||'ACTION')+'</div><div class="tag">'+esc(a.title||'Action')+'</div></button>';
    }).join(''):'<div class="empty-state compact">No opponent action yet.</div>';
    return '<aside class="opPanel"><div class="opHead"><div class="sectionTitle">Opponent Played</div><small>recent visual log · archive retained</small></div><div class="opTiles">'+tiles+'</div><div class="opBtns"><button id="historyButton" type="button">Full History</button><button id="opponentPlayedPreviewButton" type="button" '+(latest?'':'disabled')+'>Preview</button></div></aside>';
  }
  function deckSetupHtml(){
    return '<div class="deck-setup-grid">'+deckSetupSide('PLAYER',decks.PLAYER)+deckSetupSide('AI',decks.AI)+'</div>'+
      (setupError?'<div class="setup-error">'+esc(setupError)+'</div>':'')+
      '<div class="deck-setup-actions"><button id="startMatchButton" class="start-match-primary" type="button">Start Match</button></div>';
  }
  function deckOptionHtml(side){
    var current=selectedDeckKey[side];
    var options=Object.keys(STARTER_DECK_OPTIONS).map(function(key){ return '<option value="'+esc(key)+'" '+(current===key?'selected':'')+'>'+esc(STARTER_DECK_OPTIONS[key].label)+'</option>'; }).join('');
    if(importedDecks[side]) options+='<option value="IMPORTED" '+(current==='IMPORTED'?'selected':'')+'>'+esc(importedDecks[side].deck_name||'Imported Deck')+'</option>';
    return options;
  }
  function setupHeroProgressionIds(deck,rankOneId){
    var rows=(deck&&deck.legacy_deck_expanded)||[],heroIds=rows.filter(function(x){return String(x.card_type||'').toLowerCase()==='hero';}).map(function(x){return x.card_id;});
    var deterministic=[rankOneId,rankHeroIdFromProgression(rankOneId,2),rankHeroIdFromProgression(rankOneId,3)].filter(function(id){return !!id&&heroIds.indexOf(id)!==-1;});
    if(deterministic.length===3)return deterministic;
    var packageId='';
    rows.some(function(x){if(x.card_id===rankOneId){packageId=x.package_id||'';return true;}return false;});
    var ids=rows.filter(function(x){return String(x.card_type||'').toLowerCase()==='hero'&&(!packageId||x.package_id===packageId);}).map(function(x){return x.card_id;});
    ids.sort(function(a,b){return parseRank(((card(a)||{}).identity||{}).rank)-parseRank(((card(b)||{}).identity||{}).rank);});
    if(ids.indexOf(rankOneId)===-1)ids.unshift(rankOneId);
    return ids.slice(0,3);
  }
  function setupRankTwoId(deck,rankOneId){
    var rows=(deck&&deck.legacy_deck_expanded)||[],base=rows.find(function(x){return x&&x.card_id===rankOneId;});
    if(base){
      var packageHeroes=rows.filter(function(x){return x&&x.package_id===base.package_id&&String(x.card_type||'').toLowerCase()==='hero';});
      var exact=packageHeroes.find(function(x){var c=card(x.card_id)||{};return Number(c.rank_numeric||parseRank((c.identity||{}).rank))===2;});
      if(exact&&exact.card_id)return exact.card_id;
    }
    var candidate=rankHeroIdFromProgression(rankOneId,2),heroIds=rows.filter(function(x){return String(x.card_type||'').toLowerCase()==='hero';}).map(function(x){return x.card_id;});
    if(candidate&&heroIds.indexOf(candidate)!==-1)return candidate;
    var ids=setupHeroProgressionIds(deck,rankOneId);return ids[1]||ids[0]||rankOneId;
  }
  var GL_SETUP_PROGRESSION_TOKEN=0;
  function closeSetupHeroProgression(){GL_SETUP_PROGRESSION_TOKEN++;var modal=document.getElementById('aiHeroProgressionModal');if(modal)modal.remove();}
  function preloadSetupHeroAsset(src){return new Promise(function(resolve){var img=new Image(),done=false;function finish(){if(done)return;done=true;resolve(src);}img.onload=finish;img.onerror=finish;img.src=src;if(img.complete)finish();setTimeout(finish,3500);});}
  function openSetupHeroProgression(side,rankOneId){
    closeSetupHeroProgression();var deck=decks[side]||{},ids=setupHeroProgressionIds(deck,rankOneId),sources=ids.map(thumbFor),token=++GL_SETUP_PROGRESSION_TOKEN;
    if(ids.length<3)return showInfo('Hero Progression','The selected Hero package is incomplete.');
    var modal=document.createElement('div');modal.id='aiHeroProgressionModal';modal.className='pvp-progression-modal';
    var cards=ids.map(function(id,index){return '<article class="pvp-v260-hero pvp-progression-hero '+(index===0?'current':'')+'"><button class="pvp-v260-card pvp-progression-static-card" type="button" data-preview="'+esc(id)+'" aria-label="Preview '+esc(cardName(card(id)))+' Rank '+['I','II','III'][index]+'"><img loading="eager" decoding="async" draggable="false" src="'+esc(sources[index])+'" alt="'+esc(cardName(card(id)))+'"></button><div class="pvp-v260-position">RANK '+['I','II','III'][index]+'</div></article>';});
    modal.innerHTML='<section class="pvp-progression-card"><header><div><span>HERO PROGRESSION</span><h2>'+esc(cardName(card(ids[0])))+'</h2></div><button id="aiHeroProgressionClose" class="pvp-progression-close" type="button">Close</button></header><div class="pvp-progression-row">'+cards[0]+'<span class="pvp-progression-arrow" aria-hidden="true">→</span>'+cards[1]+'<span class="pvp-progression-arrow" aria-hidden="true">→</span>'+cards[2]+'</div><p>Select a Rank card to open Card Preview.</p></section>';
    document.body.appendChild(modal);bringModalToFront(modal);prepareRenderedImages(modal);document.getElementById('aiHeroProgressionClose').onclick=closeSetupHeroProgression;modal.addEventListener('click',function(ev){if(ev.target===modal)closeSetupHeroProgression();});
    Promise.all(sources.map(preloadSetupHeroAsset)).then(function(){if(token!==GL_SETUP_PROGRESSION_TOKEN)return;prepareRenderedImages(modal);});
  }
  function deckSetupSide(side, deck){
    var validation=validateDeck(deck,side); var d=validation.deck||deck;
    var sideLabel=side==='PLAYER'?'Player':'AI';
    return '<section class="deck-side '+esc(side.toLowerCase())+'-deck-side"><header><h3>'+esc(sideLabel)+' Deck</h3><span class="deck-pill '+(validation.ok?'ok':'bad')+'">'+(validation.ok?'VALID':'INVALID')+'</span></header><div class="deck-picker-row"><select aria-label="'+esc(sideLabel)+' deck selector" data-deck-select="'+esc(side)+'">'+deckOptionHtml(side)+'</select><button class="import-button" type="button" data-import-side="'+esc(side)+'">Import Deck</button></div><input class="deck-import-input" id="import'+esc(side)+'" type="file" accept="application/json,.json"><div class="formation-preview clean-hero-preview">'+LANE_ORDER.map(function(lane){var rankOne=d.default_formation&&d.default_formation[lane],id=setupRankTwoId(d,rankOne);return '<article class="formation-card"><button type="button" data-hero-progression-side="'+esc(side)+'" data-hero-progression="'+esc(rankOne)+'" aria-label="View '+esc(lane)+' Hero Progression"><img draggable="false" data-showcase-card-id="'+esc(id)+'" src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(card(id)))+'"></button><span class="formation-position">'+esc(lane.charAt(0)+lane.slice(1).toLowerCase())+'</span></article>';}).join('')+'</div>'+(!validation.ok?'<div class="setup-inline-error">'+esc(validation.errors[0])+'</div>':'')+'</section>';
  }
  function refreshDeckSetupView(){
    if(!matchStarted){ render(); return; }
    if($('choiceBody')){ $('choiceBody').innerHTML=deckSetupHtml(); bindDeckSetupModal(); }
  }
  function applyDeckSelection(side,key){
    if(key==='IMPORTED' && importedDecks[side]){ selectedDeckKey[side]='IMPORTED'; decks[side]=clone(importedDecks[side]); }
    else if(STARTER_DECK_OPTIONS[key]){ selectedDeckKey[side]=key; decks[side]=clone(STARTER_DECK_OPTIONS[key].deck); }
    setupError=''; clearTransientUiState(); appState=null; matchStarted=false; render();
  }
  function showDeckSetup(){
    if(!matchStarted){ renderStartupDeckSetup(); return; }
    $('choiceTitle').textContent='Deck Setup'; $('choiceBody').innerHTML=deckSetupHtml(); $('choiceConfirm').style.display='none'; $('choiceOverlay').classList.add('open','deck-setup-open'); bindDeckSetupModal();
  }
  function closeChoice(){ if(!$('choiceOverlay')) return; $('choiceOverlay').classList.remove('open','deck-setup-open','hand-discard-open'); if($('choiceConfirm')){ $('choiceConfirm').style.display=''; $('choiceConfirm').disabled=false; } }
  function bindDeckSetupModal(){
    if(!window.__GL_AI_PROGRESSION_ESCAPE_BOUND){window.__GL_AI_PROGRESSION_ESCAPE_BOUND=true;document.addEventListener('keydown',function(ev){if(ev.key==='Escape')closeSetupHeroProgression();});}
    var start=$('startMatchButton'); if(start) start.addEventListener('click', startMatch);
    Array.prototype.forEach.call(document.querySelectorAll('[data-hero-progression]'),function(btn){btn.onclick=function(ev){if(ev){ev.preventDefault();ev.stopPropagation();}openSetupHeroProgression(btn.getAttribute('data-hero-progression-side')||'PLAYER',btn.getAttribute('data-hero-progression'));};});
    Array.prototype.forEach.call(document.querySelectorAll('[data-deck-select]'), function(sel){ sel.addEventListener('change', function(){ applyDeckSelection(sel.getAttribute('data-deck-select'), sel.value); }); });
    Array.prototype.forEach.call(document.querySelectorAll('[data-import-side]'), function(btn){ btn.addEventListener('click', function(){ var side=btn.getAttribute('data-import-side'); var input=$('import'+side); if(input) input.click(); }); });
    ['PLAYER','AI'].forEach(function(side){ var input=$('import'+side); if(!input) return; input.addEventListener('change', function(ev){ var file=ev.target.files&&ev.target.files[0]; if(!file) return; var reader=new FileReader(); reader.onload=function(){ try{ var parsed=normalizeDeck(JSON.parse(String(reader.result||''))); var val=validateDeck(parsed,side); if(!val.ok) throw new Error(val.errors[0]); importedDecks[side]=val.deck; selectedDeckKey[side]='IMPORTED'; decks[side]=val.deck; setupError=''; matchStarted=false; appState=null; render(); } catch(err){ setupError=err.message || 'Invalid deck file.'; refreshDeckSetupView(); } }; reader.onerror=function(){ setupError='Invalid deck file.'; refreshDeckSetupView(); }; reader.readAsText(file); }); });
  }
  function playOpeningCoinSound(){return playPreloadedAudio('assets/audio/freesound_community-coin-flip-37787.mp3',.65);}
  function coinOutcomeFromUint32(value){return((Number(value)>>>0)&1)===0?'HEADS':'TAILS';}
  function fairOpeningCoinOutcome(){try{var cryptoApi=(typeof window!=='undefined'&&window.crypto&&typeof window.crypto.getRandomValues==='function')?window.crypto:(typeof crypto!=='undefined'&&crypto&&typeof crypto.getRandomValues==='function'?crypto:null);if(cryptoApi){var values=new Uint32Array(1);cryptoApi.getRandomValues(values);return coinOutcomeFromUint32(values[0]);}}catch(e){}return Math.random()<.5?'HEADS':'TAILS';}
  function closeLocalCoinModal(){var el=document.getElementById('glLocalCoinModal');if(el)el.remove();}
  function localCoinFaceSrc(face){var f=String(face||'HEADS').toUpperCase();return f==='TAILS'?'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Tail.webp':'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Racial-Token-Head.webp';}
  function localCoinFace(face){var f=String(face||'HEADS').toUpperCase();return'<img class="gl-critical-visual" src="'+localCoinFaceSrc(f)+'" alt="'+(f==='TAILS'?'Tails':'Heads')+'" width="142" height="142" decoding="async" draggable="false">';}
  function setLocalCoinFaceElement(img,face){if(!img)return;var f=String(face||'HEADS').toUpperCase()==='TAILS'?'TAILS':'HEADS';img.src=localCoinFaceSrc(f);img.alt=f==='TAILS'?'Tails':'Heads';prepareImageForPaint(img);}
  function renderLocalCoinResult(modal,choice,outcome,first){
    if(!modal)return;
    var winnerTitle=first==='PLAYER'?'YOU WON THE COIN FLIP':'LOCAL AI WON THE COIN FLIP';
    var winnerText=first==='PLAYER'?'You will take the first turn.':'Local AI will take the first turn.';
    modal.innerHTML='<section class="gl-opening-coin-card result"><h2>Opening Coin Flip Result</h2><div class="gl-opening-coin-winner '+(first==='PLAYER'?'player-wins':'ai-wins')+'"><span>WINNER</span><strong>'+winnerTitle+'</strong><p>'+winnerText+'</p></div><div class="gl-opening-coin-result"><div><span>Your choice</span>'+localCoinFace(choice)+'</div><div><span>Coin result</span>'+localCoinFace(outcome)+'</div></div><button class="gl-opening-start-button" type="button" data-local-start-game>Start Game</button></section>';
    prepareRenderedImages(modal);
    var startButton=modal.querySelector('[data-local-start-game]');if(startButton)startButton.onclick=function(){confirmLocalStartGame(startButton);};
  }
  function runOpeningCoinAnimation(modal,outcome,onComplete){
    if(!modal||SUPPRESS_RENDER){onComplete();return false;}
    outcome=String(outcome||'HEADS').toUpperCase()==='TAILS'?'TAILS':'HEADS';
    var visualFace=outcome==='HEADS'?'TAILS':'HEADS';
    modal.innerHTML='<section class="gl-opening-coin-card flipping"><h2>Opening Coin Flip</h2><p>Flipping the coin...</p><div class="gl-opening-coin-flip-stage">'+localCoinFace(visualFace)+'</div></section>';
    var coinReady=prepareRenderedImages(modal);
    var coin=modal.querySelector('.gl-opening-coin-flip-stage img'),step=0,totalSteps=8,halfDuration=72,finished=false;
    function finish(){
      if(finished)return;finished=true;
      setLocalCoinFaceElement(coin,outcome);
      if(coin){coin.style.transition='none';coin.style.transform='translateY(0) scaleX(1)';coin.style.filter='none';coin.style.opacity='1';}
      var text=modal.querySelector('.gl-opening-coin-card.flipping p');if(text)text.textContent=(outcome==='HEADS'?'Heads':'Tails')+'!';
      setTimeout(onComplete,1000);
    }
    function runStep(){
      if(!coin||step>=totalSteps){finish();return;}
      var progress=(step+1)/totalSteps,lift=Math.round(Math.sin(progress*Math.PI)*22);
      coin.style.transition='transform '+halfDuration+'ms cubic-bezier(.45,0,.55,1)';
      coin.style.transform='translateY(-'+lift+'px) scaleX(.04)';
      setTimeout(function(){
        visualFace=visualFace==='HEADS'?'TAILS':'HEADS';setLocalCoinFaceElement(coin,visualFace);
        coin.style.transition='transform '+halfDuration+'ms cubic-bezier(.2,.75,.3,1)';
        coin.style.transform='translateY(-'+lift+'px) scaleX(1)';
        setTimeout(function(){step++;runStep();},halfDuration);
      },halfDuration);
    }
    coinReady.then(function(){nextVisualFrame(function(){playOpeningCoinSound();if(coin){coin.style.filter='none';coin.style.backfaceVisibility='hidden';coin.style.transform='translateY(0) scaleX(1)';}runStep();});});
    return true;
  }
  function showLocalCoinModal(){
    if(IS_PVP_APP||!appState||!appState.preGame)return;
    var modal=document.getElementById('glLocalCoinModal');
    if(!modal){modal=document.createElement('div');modal.id='glLocalCoinModal';modal.className='gl-opening-coin-modal';document.body.appendChild(modal);}
    modal.innerHTML='<section class="gl-opening-coin-card"><h2>Opening Coin Flip</h2><p>Choose Heads or Tails. The winner takes the first Draw Phase.</p><div class="gl-opening-coin-actions"><button type="button" data-local-coin="HEADS">'+localCoinFace('HEADS')+'<span>Heads</span></button><button type="button" data-local-coin="TAILS">'+localCoinFace('TAILS')+'<span>Tails</span></button></div></section>';
    prepareRenderedImages(modal);
    Array.prototype.forEach.call(modal.querySelectorAll('[data-local-coin]'),function(btn){btn.onclick=function(){resolveLocalCoinChoice(btn.getAttribute('data-local-coin'));};});
  }
  function resolveLocalCoinChoice(choice){
    if(!appState||!appState.preGame||appState.preGame.stage!=='COIN_FLIP')return;
    choice=String(choice).toUpperCase()==='TAILS'?'TAILS':'HEADS';
    var outcome=fairOpeningCoinOutcome(),first=choice===outcome?'PLAYER':'AI',modal=document.getElementById('glLocalCoinModal');
    appState.preGame={stage:'COIN_FLIPPING',firstPlayer:first};
    appState.openingCoinFlip={choice:choice,outcome:outcome,firstPlayer:first,completed:false};
    runOpeningCoinAnimation(modal,outcome,function(){
      if(!appState||!appState.preGame||appState.preGame.stage!=='COIN_FLIPPING')return;
      appState.preGame={stage:'COIN_RESULT',firstPlayer:first};
      appState.openingCoinFlip={choice:choice,outcome:outcome,firstPlayer:first,completed:true};
      renderLocalCoinResult(modal,choice,outcome,first);
      if(!SUPPRESS_RENDER)render();
    });
  }
  function confirmLocalStartGame(button){
    if(!appState||!appState.preGame||appState.preGame.stage!=='COIN_RESULT'||!appState.openingCoinFlip||!appState.openingCoinFlip.completed)return false;
    if(button){button.disabled=true;button.textContent='Starting...';}
    var flip=clone(appState.openingCoinFlip),first=flip.firstPlayer==='AI'?'AI':'PLAYER';
    closeLocalCoinModal();
    completeOpeningFlow(appState,first,flip,{});
    if(!SUPPRESS_RENDER)render();
    focusMobilePlayerHand();
    return true;
  }

  function startMatch(){var pv=validateDeck(decks.PLAYER,'PLAYER'),av=validateDeck(decks.AI,'AI');if(!pv.ok||!av.ok){setupError=(pv.errors[0]||av.errors[0]||'Deck validation failed.');refreshDeckSetupView();return false;}decks.PLAYER=pv.deck;decks.AI=av.deck;cancelAITurnDirector();clearTransientUiState();appState=buildInitialMatchState();matchStarted=true;setupError='';render();showLocalCoinModal();return true;}
  var GL_CANONICAL_PREVIEW_BY_ID=null;
  function canonicalPreviewRecord(cardId){if(!GL_CANONICAL_PREVIEW_BY_ID){GL_CANONICAL_PREVIEW_BY_ID={};((window.GRANDIS_LEGACY_CARD_PREVIEW||{}).cards||[]).forEach(function(x){GL_CANONICAL_PREVIEW_BY_ID[x.card_id]=x;});}return GL_CANONICAL_PREVIEW_BY_ID[cardId]||null;}

  function previewManaRows(c){var p=canonicalPreviewRecord(c&&c.card_id),cost=p&&p.cost||c&&c.canonical_cost||{};if(cost.kind==='by_class')return Object.keys(cost.mana_by_class||{}).map(function(k){return{label:k,value:String(cost.mana_by_class[k])};});if(cost.kind==='formula')return[{label:'Variable',value:p.cost_display||'Variable'}];if(cost.kind==='fixed_plus_spend_all')return[{label:'',value:p.cost_display||'Spend all available Mana'}];if(cost.kind==='discard_from_hand')return[{label:'Cost',value:p.cost_display||'Discard required card'}];if(cost.kind==='none')return[{label:'',value:'—'}];return[{label:'',value:String(cost.mana==null?'—':cost.mana)}];}
  function previewManaFieldsHtml(c){return previewManaRows(c).map(function(row){return '<section class="readable-card-mana"><span>Mana</span>'+(row.label?'<small>'+esc(row.label)+'</small>':'')+'<strong>'+esc(row.value)+'</strong></section>';}).join('');}
  function previewManaText(c){return previewManaRows(c).map(function(row){return (row.label?row.label+' ':'')+row.value;}).join(' · ');}
  function previewBadgeLabels(c){
    var values=[];
    function add(v){v=cleanDisplayText(v,'').trim();if(!v||values.some(function(x){return x.toLowerCase()===v.toLowerCase();}))return;values.push(v);}
    add(c&&c.classification);add(c&&c.action_category);
    var fam=cardFamily(c);if(fam==='Item'||fam==='Event'||fam==='Hero'||isLegacyDefinitionCard(c))add(isLegacyDefinitionCard(c)?'Legacy':fam);
    if(c&&c.attack&&c.attack.attack_type)add(c.attack.attack_type);if(!values.length)add(cardSubtype(c)||fam);return values;
  }
  function previewBadgeHtml(c){return previewBadgeLabels(c).map(function(label){return '<span class="preview-badge">'+esc(label)+'</span>';}).join('')||'<span class="preview-badge preview-badge--empty">—</span>';}
  function previewPrintedBadgeLabels(c){var label=cleanDisplayText((c&&c.classification)||((c&&c.action_category)||((c&&c.card_type)||cardFamily(c))),'');if(!label||/^(Skill|Card)$/i.test(label))label=cleanDisplayText((c&&c.action_category)||cardFamily(c),'Card');return label?[label]:[];}
  function previewPrintedBadgeHtml(c){return previewPrintedBadgeLabels(c).map(function(label){return '<span class="preview-badge">'+esc(label)+'</span>';}).join('')||'<span class="preview-badge preview-badge--empty">—</span>';}
  function previewCardKind(c){if(isLegacyDefinitionCard(c))return'legacy';if(cardFamily(c)==='Hero'||String(c&&c.card_type||'').toLowerCase()==='hero')return'hero';return'main';}
  function previewCardArtHtml(cardId,c){return '<figure class="readable-card-art"><img src="'+esc(fullFor(cardId))+'" alt="'+esc(cardName(c))+' card"></figure>';}
  function previewField(label,value,extraClass){return '<section class="readable-card-field '+esc(extraClass||'')+'"><span>'+esc(label)+'</span><strong>'+esc(cleanDisplayText(value,'—'))+'</strong></section>';}
  function previewNamedEffect(label,name,text){var safeName=cleanDisplayText(name,''),safeText=cleanDisplayText(text,'');return '<section class="readable-card-section"><span class="readable-card-label">'+esc(label)+'</span>'+(safeName?'<h3>'+esc(safeName)+'</h3>':'')+'<p>'+esc(safeText||'—')+'</p></section>';}
  function previewPrintedClassSections(c){var p=canonicalPreviewRecord(c&&c.card_id),rows=p&&p.review_rows||[];return rows.map(function(r){return{title:r.label||'Effect',text:r.text||[r.damage_text,r.effect_text].filter(Boolean).join(' ')};}).filter(function(x){return x.text;});}
  function previewPrintedSections(c){var sections=previewPrintedClassSections(c);if(sections.length)return sections.map(function(sec){return '<section class="preview-effect-row"><h3>'+esc(sec.title)+'</h3><p>'+esc(sec.text)+'</p></section>';}).join('');var text=cleanDisplayText((c&&c.card_text)||((c&&c.effect_text)||''),'');return '<section class="preview-effect-row"><h3>Effect</h3><p>'+esc(text||'—')+'</p></section>';}
  var GL_PREVIEW_HERO_PRINTED_NAME_CACHE={};
  function previewHeroPrintedName(c){var id=c&&c.card_id;if(!id)return cardName(c);if(GL_PREVIEW_HERO_PRINTED_NAME_CACHE[id])return GL_PREVIEW_HERO_PRINTED_NAME_CACHE[id];var printed='';(CARDS||[]).some(function(candidate){var ultimate=candidate&&candidate.requirement&&candidate.requirement.ultimate;var owners=ultimate&&ultimate.owner_lineage_card_ids;if(ultimate&&ultimate.owner&&Array.isArray(owners)&&owners.indexOf(id)!==-1){printed=ultimate.owner;return true;}return false;});GL_PREVIEW_HERO_PRINTED_NAME_CACHE[id]=printed||cardName(c);return GL_PREVIEW_HERO_PRINTED_NAME_CACHE[id];}
  function previewHeroHtml(cardId,c){
    var identity=(c&&c.identity)||{},racial=(c&&c.racial_ability)||{},classAbility=(c&&c.class_ability)||{};
    var race=identity.race||c.race||'—',rank=identity.rank||c.class_rank||'—',className=identity.class||identity.display_class||c.class_family||'—';
    var racialName=racial.name||String(c.racial_trait_name||'').replace(/^Racial Trait\s*[—-]\s*/i,''),racialText=racial.text||c.racial_trait_text||c.hero_ability_text||'';
    var classBlock='<section class="readable-card-section"><span class="readable-card-label">Class</span><h3>'+esc(className)+'</h3>';
    if(cleanDisplayText(classAbility.name,''))classBlock+='<span class="readable-card-sublabel">Class Ability</span><h4>'+esc(classAbility.name)+'</h4><p>'+esc(cleanDisplayText(classAbility.text,'—'))+'</p>';classBlock+='</section>';
    var rankNumber=parseRank(rank),rankUpBlock=rankNumber>=2?'<section class="readable-card-section readable-card-rankup"><span class="readable-card-label">Rank Up Bonus</span><p>'+esc(rankUpBonusText(c))+'</p></section>':'';
    return '<article class="readable-card-preview readable-card-preview--hero">'+previewCardArtHtml(cardId,c)+'<div class="readable-card-details"><header class="readable-card-name"><span>Hero Name</span><h2>'+esc(previewHeroPrintedName(c))+'</h2></header><div class="readable-card-facts readable-card-facts--hero">'+previewField('Rank',rank,'readable-card-rank')+previewField('HP',String(c.hp==null?'—':c.hp),'readable-card-hp')+'</div><div class="readable-card-scroll"><section class="readable-card-section"><span class="readable-card-label">Racial</span><h3>'+esc(race)+'</h3>'+(racialName?'<span class="readable-card-sublabel">Racial Trait</span><h4>'+esc(racialName)+'</h4>':'')+'<p>'+esc(cleanDisplayText(racialText,'—'))+'</p></section>'+classBlock+rankUpBlock+'</div></div></article>';
  }
  function previewLegacyHtml(cardId,c){
    var legacyClass=(c&&c.class_family)||((c&&c.eligibility&&c.eligibility.base_class_family)||'—');var effectName=(c&&c.ability&&c.ability.name)||cardName(c);var effectText=cleanDisplayText((c&&c.ability&&c.ability.text)||((c&&c.card_text)||((c&&c.effect_text)||'')),'—');
    return '<article class="readable-card-preview readable-card-preview--legacy">'+previewCardArtHtml(cardId,c)+'<div class="readable-card-details"><header class="readable-card-name"><span>Legacy Name</span><h2>'+esc(cardName(c))+'</h2></header>'+previewField('Legacy Class',legacyClass,'readable-card-legacy-class')+'<div class="readable-card-scroll">'+previewNamedEffect('Effect Name',effectName,effectText)+'</div></div></article>';
  }
  function previewUltimateRulesHtml(c){
    if(!isUltimateCard(c))return '';
    var ultimate=c&&c.requirement&&c.requirement.ultimate||{},owner=cleanDisplayText(ultimate.owner,'the named Hero');
    return '<section class="readable-card-section readable-card-ultimate-rules"><span class="readable-card-label">Ultimate Rules</span><ul><li><strong>Bound Hero:</strong> Only '+esc(owner)+' may play this Ultimate or use it as Tribute.</li><li><strong>Tribute value:</strong> 200 EXP.</li></ul></section>';
  }
  function previewMainCardHtml(cardId,c){return '<article class="readable-card-preview readable-card-preview--main">'+previewCardArtHtml(cardId,c)+'<div class="readable-card-details"><header class="readable-card-name"><span>Card Name</span><h2>'+esc(cardName(c))+'</h2></header><div class="readable-card-facts">'+previewManaFieldsHtml(c)+'<section class="readable-card-badges"><span>Card Badge</span><div>'+previewPrintedBadgeHtml(c)+'</div></section></div><div class="readable-card-scroll"><section class="readable-card-text"><span class="readable-card-label">Card Text</span>'+previewPrintedSections(c)+'</section>'+previewUltimateRulesHtml(c)+'</div></div></article>';}
  function previewCardHtml(cardId,c){var kind=previewCardKind(c);if(kind==='hero')return previewHeroHtml(cardId,c);if(kind==='legacy')return previewLegacyHtml(cardId,c);return previewMainCardHtml(cardId,c);}
  function showPreview(cardId,context){var c=card(cardId);if(!c)return;GL_CARD_ZOOM_ID=cardId;var t=$('previewTitle'),b=$('previewBody');if(t)t.textContent='Card Preview';if(b)b.innerHTML=previewCardHtml(cardId,c);$('previewOverlay').classList.add('open');$('previewOverlay').setAttribute('aria-hidden','false');bringModalToFront($('previewOverlay'));v94HoverZoomHide();}
  window.GL_CARD_PREVIEW_QA={version:'UI v2.8',kind:function(cardId){initCards();var c=card(cardId);return c?previewCardKind(c):'';},html:function(cardId){initCards();var c=card(cardId);return c?previewCardHtml(cardId,c):'';},mana:function(cardId){initCards();var c=card(cardId);return c?previewManaRows(c):[];},rows:function(cardId){initCards();var c=card(cardId);return c?previewPrintedClassSections(c):[];},badges:function(cardId){initCards();var c=card(cardId);return c?previewPrintedBadgeLabels(c):[];},ultimateRules:function(cardId){initCards();var c=card(cardId);return c?previewUltimateRulesHtml(c):'';}};
  window.GL_V542_MOBILE_UI_QA_SELF_TEST=function(){return{ok:true,version:'v5.46',singleStatusIndicator:true,singleContextAction:true,racialTokensBesideMainDeck:true,racialTokensVertical:true,mobileFooterNoTrailingGap:true,desktopMobileIsolation:true,mobileHpCardAnchored:true,deckBuilderFormationPreview:'larger'};};
  window.GL_V543_RESOURCE_UI_QA_SELF_TEST=function(){return{ok:true,version:'v5.46',dedicatedRacialResource:true,resourceColumns:[22,22,22,22,12],racialTokensVertical:true,mainDeckUntouched:true,desktopFooterStrokeRemoved:true,desktopResourcesUnchanged:true};};
  window.GL_V545_CASTING_UI_QA_SELF_TEST=function(){
    initCards();var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Draw';s.pvpHumanVsHuman=true;
    var h=s.playerHeroes.LEFT;h.card_id='S1-MAG-H001';h.maxHp=80;h.hp=80;h.exhausted=true;h.attachments=['S1-MAG-007',null];
    var instance=h.instance_id,pc={card_id:'S1-MAG-007',side:'PLAYER',source_lane:'LEFT',original_source_lane:'LEFT',source_hero_card_id:'S1-MAG-H001',source_instance_id:instance,source_rank_at_cast:1,source_progression_id:'S1-MAG-H001',target_side:'AI',target_lane:'CENTER',locked_target_lane:'CENTER',attachmentSlot:0,resolve_phase:'next_owner_battle',commit_token:'QA_CAST_RANK_LIVE'};
    s.pendingCastings=[pc];s.activeAttachments=[{attachment_id:'ATT:QA45',commit_token:'QA_CAST_RANK_LIVE',side:'PLAYER',lane:'LEFT',slot:0,card_id:'S1-MAG-007',remaining_count:1,tick_phase:'BATTLE_PHASE_START'}];
    h.card_id='S1-MAG-H002';h.maxHp=100;h.hp=100;
    clearExhaustForSide(s,'PLAYER');var keptExhausted=!!h.exhausted,identityPreserved=h.instance_id===instance;
    s.phase='Battle';resolvePendingCastingsFor(s,'PLAYER');var rw=s.responseWindow||{},rankDamage=Number(rw.damage||0),notCanceled=!(s.playerPlayedEvents||[]).some(function(e){return e.casting_stage==='CANCELED';});
    return{ok:keptExhausted&&identityPreserved&&notCanceled&&rankDamage===80,version:'v5.46',keptExhausted:keptExhausted,instanceIdentityPreserved:identityPreserved,rankUpPreservedCasting:notCanceled,releaseDamage:rankDamage,releaseUsesCurrentHero:rankDamage===80,responseUnavailableHoverOnly:true,mobileResourceHeight:72,mobileHandFullWidth:true,desktopBottomStrokeRemoved:true};
  };
  window.GL_V546_FIX_QA_SELF_TEST=function(){
    var rank2={card_id:'S1-MAG-H002',hp:100,maxHp:100,exhausted:false,attachments:[null,null],statuses:[]},rank3={card_id:'S1-MAG-H003',hp:120,maxHp:120,exhausted:false,attachments:[null,null],statuses:[]};
    var dc=card('S1-MAG-018'),rank2Policy=dc&&dc.double_casting_policy&&dc.double_casting_policy.rank2_target_rule,rank3Policy=dc&&dc.double_casting_policy&&dc.double_casting_policy.rank3_target_rule;
    return{ok:rank2Policy==='same or different legal Hero'&&rank3Policy==='same or different legal Hero',version:'v5.46',responseLabel:'Not Available',singleUnavailableReason:true,mobileHandScrollbarHidden:true,mobileHandTwoActionRows:true,mobileStartFocusesHand:true,sequentialDrawReveal:true,doubleCastingAnyLegalSecondTarget:true};
  };
  window.GL_LOCAL_AI_PREVIEW_QA={version:'v5.46',kind:window.GL_CARD_PREVIEW_QA.kind,html:window.GL_CARD_PREVIEW_QA.html,mana:window.GL_CARD_PREVIEW_QA.mana,rows:window.GL_CARD_PREVIEW_QA.rows,badges:window.GL_CARD_PREVIEW_QA.badges};window.GL_PVP_PREVIEW_QA={version:'v2.6.15',kind:window.GL_CARD_PREVIEW_QA.kind,html:window.GL_CARD_PREVIEW_QA.html,mana:window.GL_CARD_PREVIEW_QA.mana,rows:window.GL_CARD_PREVIEW_QA.rows,badges:window.GL_CARD_PREVIEW_QA.badges};
  window.GL_LOCAL_AI_UI_QA={version:'v5.59',orderPlayedResultLines:function(lines){return orderPlayedResultLines(lines);},bringModalToFront:bringModalToFront};
  window.GL_CARD_PLAYED_CHAIN_QA={version:'shared-v2.0',group:function(state){return v96CombinedPlayedEvents(state);},detail:function(group){return v94EventDetailHtml(group,group&&group.side);}};
  function previewRankSectionsFromText(c){
    var raw=cleanDisplayText((c&&c.card_text)||((c&&c.effect_text)||''),'');
    if(!raw||raw.length>1600)return{requirement:'',sections:[]};
    var rankRe=/([A-Z][A-Za-z'’\- ]*?)\s+Rank\s+(I{1,3}|IV|V|VI|VII|VIII|IX|X)\s*:/g,matches=[],m;
    while((m=rankRe.exec(raw))!==null)matches.push({index:m.index,end:rankRe.lastIndex,cls:m[1].replace(/\s+/g,' ').trim(),rank:m[2]});
    if(matches.length<1)return{requirement:'',sections:[]};var sections=[];
    for(var i=0;i<matches.length;i++){var start=matches[i].end,end=i+1<matches.length?matches[i+1].index:raw.length,text=raw.slice(start,end).trim().replace(/^[:\-\s]+/,'').trim();if(text)sections.push({title:matches[i].cls,text:text});}
    return{requirement:matches.map(function(x){return x.cls+' Rank '+x.rank;}).join('; '),sections:sections};
  }
  function normalizedPreviewEffectText(text){return cleanDisplayText(text,'').replace(/\s+/g,' ').trim();}
  function collapsePreviewEffectSections(sections){var seen={},out=[];(sections||[]).forEach(function(sec){var text=normalizedPreviewEffectText(sec&&sec.text);if(!text)return;var key=text.toLowerCase();if(seen[key])return;seen[key]=true;out.push({title:cleanDisplayText(sec&&sec.title,'Effect'),text:text});});return out;}
  function appendPreviewEffectSections(parts,sections){var unique=collapsePreviewEffectSections(sections);if(!unique.length)return false;if(unique.length===1){parts.push('<section class="preview-effect-row"><h3>Effect</h3><p>'+esc(unique[0].text)+'</p></section>');return true;}unique.forEach(function(sec){parts.push('<section class="preview-effect-row"><h3>'+esc(sec.title)+'</h3><p>'+esc(sec.text)+'</p></section>');});return true;}
  function previewSections(c){var parts=[],sections=previewPrintedClassSections(c);if(sections.length)appendPreviewEffectSections(parts,sections);else{var text=cleanDisplayText((c&&c.card_text)||((c&&c.effect_text)||''),'');parts.push('<section class="preview-effect-row"><h3>Effect</h3><p>'+esc(text||'—')+'</p></section>');}return parts.join('');}
  function showInfo(title, body){ $('infoTitle').textContent=title; $('infoBody').innerHTML=formatInfoBody(body); $('infoOverlay').classList.add('open'); bringModalToFront($('infoOverlay')); }
  function closeInfo(){ $('infoOverlay').classList.remove('open'); }
  function showChoice(title, cardIds, confirmText){ $('choiceTitle').textContent=title; $('choiceConfirm').style.display=''; $('choiceBody').innerHTML='<div class="choice-grid">'+cardIds.map(function(id){ return cardTile(id,{cls:'choice-card'}); }).join('')+'</div>'; $('choiceConfirm').textContent=confirmText||'Confirm'; $('choiceOverlay').classList.add('open'); bringModalToFront($('choiceOverlay')); }
  function showDiscard(side){
    var state=currentShellState(),ids=side==='AI'?(state.aiDiscard||[]):(state.playerDiscard||[]);
    $('choiceTitle').textContent=(side==='AI'?'Opponent':'Your')+' Discard Pile — '+ids.length+' Cards';
    $('choiceConfirm').style.display='none';
    $('choiceBody').innerHTML=ids.length?'<p class="choice-instruction">Discard Piles are public. Click a card to zoom.</p><div class="choice-grid inspect-card-grid">'+ids.map(function(id){return '<button type="button" class="inspect-card" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(card(id)))+'"><span>'+esc(cardName(card(id)))+'</span></button>';}).join('')+'</div>':'<div class="empty-state">Discard Pile is empty.</div>';
    $('choiceOverlay').classList.add('open');
  }
  function showLegacyDeck(side){
    if(side!=='PLAYER')return;
    var ids=(currentShellState().playerLegacy||[]).map(function(x){return heroIdFrom(x)||x;}).filter(Boolean);
    $('choiceTitle').textContent='Your Legacy Deck — '+ids.length+' Cards';$('choiceConfirm').style.display='none';
    $('choiceBody').innerHTML=ids.length?'<p class="choice-instruction">Private zone: only your own Legacy Deck can be inspected. Click a card to zoom.</p><div class="choice-grid inspect-card-grid">'+ids.map(function(id){return '<button type="button" class="inspect-card" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(card(id)))+'"><span>'+esc(cardName(card(id)))+'</span></button>';}).join('')+'</div>':'<div class="empty-state">Legacy Deck is empty.</div>';
    $('choiceOverlay').classList.add('open');
  }
  function showHandLimitDiscard(){
    if(!appState || !appState.pending || appState.pending.type!=='hand_limit_discard') return;
    selectedDiscardIndexes=appState.pending.selected||[]; renderHandLimitDiscardChoice();
  }
  function renderHandLimitDiscardChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='hand_limit_discard') return;
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    var p=appState.pending, side=p.side||p.decision_side||'PLAYER';
    var need=p.required; var hand=sideHand(appState,side);
    selectedDiscardIndexes=p.selected||selectedDiscardIndexes||[];
    $('choiceTitle').textContent='Hand Limit Cleanup'; $('choiceConfirm').style.display=''; $('choiceConfirm').textContent='Discard Selected'; $('choiceConfirm').disabled=selectedDiscardIndexes.length<need;
    $('choiceBody').innerHTML='<p class="choice-instruction">Discard '+need+' card'+(need>1?'s':'')+' to hand limit.</p><div class="choice-grid discard-choice-grid">'+hand.map(function(id,idx){ var c=card(id); var selected=selectedDiscardIndexes.indexOf(idx)!==-1; return '<article class="discard-choice '+(selected?'selected':'')+'"><button class="discard-preview" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(c))+' thumbnail"><span>'+esc(cardName(c))+'</span></button><button class="discard-select" type="button" data-discard-index="'+idx+'">'+(selected?'Selected':'Select')+'</button></article>'; }).join('')+'</div>';
    $('choiceOverlay').classList.add('open','hand-discard-open');
  }
  function toggleDiscardIndex(idx){
    var need=appState.pending.required; var pos=selectedDiscardIndexes.indexOf(idx);
    if(pos!==-1) selectedDiscardIndexes.splice(pos,1); else if(selectedDiscardIndexes.length<need) selectedDiscardIndexes.push(idx);
    appState.pending.selected=selectedDiscardIndexes.slice(); renderHandLimitDiscardChoice();
  }
  function commitHandLimitDiscard(){
    if(!appState || !appState.pending || appState.pending.type!=='hand_limit_discard') return false;
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return false; }
    var p=appState.pending, side=p.side||p.decision_side||'PLAYER';
    var need=p.required; if(selectedDiscardIndexes.length<need) return false;
    var hand=sideHand(appState,side), discard=sideDiscard(appState,side);
    selectedDiscardIndexes.slice().sort(function(a,b){return b-a;}).forEach(function(idx){ var motion=captureHandDiscardMotion(side,idx,hand[idx]); var id=hand.splice(idx,1)[0]; if(id){ discard.push(id); queueHandDiscardMotion(motion); } });
    pushLog(appState,side+' discards '+need+' card'+(need>1?'s':'')+' to hand limit.');
    appState.pending=null; selectedDiscardIndexes=[]; closeChoice();
    if(window.GL_PVP_SHARED_BOARD_ACTIVE && appState.pvpHumanVsHuman){
      appState.turn=oppositeSide(side);appState.phase='Draw';appState.pvpTurnReady=false;removeStartOfTurnTargetPreventionForSide(appState,appState.turn);
      pushLog(appState,'PvP hand limit cleanup complete. '+appState.turn+' enters Draw Phase automatically.');
      resolveDrawPhase(appState,appState.turn,{deferAnimation:true});syncCounts(appState);render();return true;
    }
    runAITurn(appState); render(); return true;
  }
  
  function renderDrawReplacementChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='draw_replacement_choice') return;
    var p=appState.pending, h=sideHeroes(appState,p.side)[p.source_lane], drawn=p.drawn_card_id;
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(!h || !drawn){ appState.pending=null; finishDrawReplacementChoice(appState,p.side||'PLAYER'); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent=p.abilityName+' — Draw Review'; $('choiceConfirm').style.display='none';
    $('choiceBody').innerHTML='<div class="compact-decision-layout"><button class="compact-choice-card" type="button" data-preview="'+esc(drawn)+'"><img src="'+esc(fullFor(drawn))+'" alt="'+esc(cardName(card(drawn)))+'"></button><div class="compact-choice-actions"><button type="button" class="mini-action" data-draw-replacement-choice="keep"><strong>Keep Card</strong></button><button type="button" class="mini-action gold" data-draw-replacement-choice="redraw"><strong>Shuffle &amp; Redraw</strong></button></div></div>';
    $('choiceOverlay').classList.add('open');
  }
  function finishDrawReplacementChoice(state, side){
    // Draw replacement is part of Draw Phase. Once all mandatory Draw work is complete, Deploy begins automatically.
    syncCounts(state); if(!SUPPRESS_RENDER)render(); if(state&&state.gameOver) showResult(); else if(side==='PLAYER') autoAdvancePlayerDrawWhenReady(state);
  }
  function commitDrawReplacementChoice(useRedraw){
    if(!appState || !appState.pending || appState.pending.type!=='draw_replacement_choice') return false;
    var p=appState.pending, side=p.side||'PLAYER', hand=sideHand(appState,side), deck=deckForSide(appState,side), idx=Number(p.hand_index);
    if(hand[idx]!==p.drawn_card_id) idx=hand.lastIndexOf(p.drawn_card_id);
    if(idx<0){ showInfo(p.abilityName||'Draw Review','The drawn card is no longer in hand.'); appState.pending=null; closeChoice(); finishDrawReplacementChoice(appState,side); return false; }
    var source=sideHeroes(appState,side)[p.source_lane];
    if(useRedraw){
      var returned=hand.splice(idx,1)[0]; deck.push(returned); shufflePile(deck);
      if(source) source.draw_replacement_used_turn=runtimeTurnStamp(appState);
      pushLog(appState, (p.abilityName||'Draw Review')+' returns '+cardName(card(returned))+' to Main Deck, shuffles, then redraws.');
      appState.pending=null; closeChoice(); drawOne(appState,side,true,{suppressDrawReplacement:true});
    } else {
      pushLog(appState, (p.abilityName||'Draw Review')+' keeps '+cardName(card(p.drawn_card_id))+'.');
      appState.pending=null; closeChoice();
    }
    finishDrawReplacementChoice(appState,side); return true;
  }
  function renderMagicalSurgeChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='optional_magical_surge') return;
    var p=appState.pending, h=sideHeroes(appState,p.source_side)[p.source_lane];
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay') || !h) return;
    var ability=(heroClass(h)==='Arcane Duelist'?'Arcane Surge':'Mana Surge');
    $('choiceTitle').textContent=ability+' — Optional Mana Spend'; $('choiceConfirm').style.display='none';
    $('choiceBody').innerHTML='<div class="compact-decision-layout"><button class="compact-choice-card" type="button" data-preview="'+esc(heroIdFrom(h))+'"><img src="'+esc(fullFor(heroIdFrom(h)))+'" alt="'+esc(cardName(card(heroIdFrom(h))))+'"></button><div class="compact-choice-actions"><button type="button" class="mini-action gold" data-magical-surge-choice="yes"><strong>Use '+esc(ability)+'</strong></button><button type="button" class="mini-action" data-magical-surge-choice="no"><strong>Do Not Use</strong></button></div></div>';
    $('choiceOverlay').classList.add('open');
  }
  function openMagicalSurgeChoice(state, action){
    var side=action.source_side||action.side||'PLAYER', sourceHero=sideHeroes(state,side)[action.source_lane], c=card(action.card_id);
    if((side!=='PLAYER'&&!state.pvpHumanVsHuman) || typeof action.use_magical_surge!=='undefined' || !magicalSurgeChoiceAvailable(state,action,sourceHero,c)) return false;
    state.pending={type:'optional_magical_surge', side:side, decision_side:side, card_id:action.card_id, hand_index:action.hand_index, source_side:action.source_side, source_lane:action.source_lane, target_side:action.target_side, target_lane:action.target_lane, target_lanes:action.target_lanes||null, mana_spent:action.mana_spent||null};
    pushLog(state,'Choose whether to use '+(heroClass(sourceHero)==='Arcane Duelist'?'Arcane Surge':'Mana Surge')+' for '+cardName(c)+'.'); renderMagicalSurgeChoice(); render(); return true;
  }
  function commitMagicalSurgeChoice(useIt){
    if(!appState || !appState.pending || appState.pending.type!=='optional_magical_surge') return false;
    var p=appState.pending, action={card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, target_side:p.target_side, target_lane:p.target_lane, use_magical_surge:!!useIt};
    if(p.target_lanes) action.target_lanes=p.target_lanes.slice ? p.target_lanes.slice() : p.target_lanes;
    if(p.mana_spent) action.mana_spent=p.mana_spent;
    appState.pending=null; closeChoice(); return commitPlayedCard(appState,action);
  }
  function renderArrowBarrageManaSpendChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='mana_spend_choice') return;
    var p=appState.pending, max=Number(p.max_mana||0), selected=(p.selected_mana===null||typeof p.selected_mana==='undefined')?null:Number(p.selected_mana);
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent='Arrow Barrage — Spend Mana'; $('choiceConfirm').style.display=''; $('choiceConfirm').textContent=selected?'Confirm '+selected+' Mana':'Choose Mana First'; $('choiceConfirm').disabled=(selected===null || !arrowBarrageSpendValid(appState,p.side,selected));
    var buttons=[]; for(var i=1;i<=max;i++){ buttons.push('<button type="button" class="mini-action mana-spend-choice '+(i===selected?'selected':'')+'" data-mana-spend="'+i+'" aria-pressed="'+(i===selected?'true':'false')+'"><strong>'+i+' Mana</strong><br><small>'+ (i*10) +' damage</small></button>'); }
    $('choiceBody').innerHTML='<p class="choice-instruction">Choose how much Mana to spend after the target is validated. No damage resolves until you select an amount and press Confirm Mana.</p>'+(selected?'<p class="choice-instruction selected-mana-label">Selected: '+selected+' Mana = '+(selected*10)+' damage.</p>':'<p class="choice-instruction selected-mana-label warning">No Mana selected yet.</p>')+'<div class="choice-grid mana-spend-grid">'+buttons.join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function openArrowBarrageManaSpendChoice(state, action){
    var side=action.source_side||action.side||'PLAYER', max=arrowBarrageSpendMax(state,side);
    if(max<1){ showInfo('Cannot Play','Arrow Barrage requires at least 1 Mana to spend.'); state.pending=null; render(); return false; }
    state.pending={type:'mana_spend_choice', side:side, card_id:action.card_id, hand_index:action.hand_index, source_side:action.source_side, source_lane:action.source_lane, target_side:action.target_side, target_lane:action.target_lane, max_mana:max, selected_mana:null};
    pushLog(state,'Choose Mana spend for Arrow Barrage.'); renderArrowBarrageManaSpendChoice(); render(); return true;
  }
  function setArrowBarrageSpend(amount){ if(!appState || !appState.pending || appState.pending.type!=='mana_spend_choice') return false; appState.pending.selected_mana=Number(amount||1); renderArrowBarrageManaSpendChoice(); return true; }
  function commitArrowBarrageManaSpend(){
    if(!appState || !appState.pending || appState.pending.type!=='mana_spend_choice') return false;
    var p=appState.pending; if(p.selected_mana===null || typeof p.selected_mana==='undefined'){ showInfo('Cannot Play','Choose a Mana amount before confirming Arrow Barrage.'); return false; } var spend=Number(p.selected_mana); if(!arrowBarrageSpendValid(appState,p.side,spend)){ showInfo('Cannot Play','Choose a valid Mana amount.'); return false; }
    var action={card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, target_side:p.target_side, target_lane:p.target_lane, mana_spent:spend};
    appState.pending=null; closeChoice(); return commitPlayedCard(appState,action);
  }
  function opponentHandRevealActive(state, side, opponentSide){
    var r=state&&state.magicScopeReveal;
    return !!(r && r.viewer_side===side && r.opponent_side===opponentSide);
  }
  function clearOpponentHandReveal(state, side, opponentSide){
    if(opponentHandRevealActive(state,side,opponentSide)) state.magicScopeReveal=null;
  }
  function startCalculatedPlanChoice(state, opts){
    opts=opts||{};
    var side=opts.side||'PLAYER', opp=oppositeSide(side), oppHand=sideHand(state,opp)||[], resolveTo=opts.resolve_to||'opponent_discard';
    var sourceName=cardName(card(opts.source_card_id));
    if(!oppHand.length){ pushLog(state, sourceName+' resolves but opponent hand is empty.'); return finalizePostChoiceSourceCard(state,{side:side,source_card_id:opts.source_card_id,host_side:opts.host_side,host_lane:opts.host_lane,attachmentSlot:opts.attachmentSlot}); }
    if(side!=='PLAYER' && !state.pvpHumanVsHuman){
      var aiIdx=Math.floor(Math.random()*oppHand.length), aiId=oppHand.splice(aiIdx,1)[0];
      if(resolveTo==='opponent_deck'){ deckForSide(state,opp).push(aiId); shufflePile(deckForSide(state,opp)); pushLog(state, sourceName+' shuffles 1 chosen opponent hand card into the opponent Main Deck.'); }
      else { sideDiscard(state,opp).push(aiId); pushLog(state, sourceName+' discards 1 chosen opponent hand card.'); }
      shufflePile(oppHand); clearOpponentHandReveal(state,side,opp);
      return finalizePostChoiceSourceCard(state,{side:side,source_card_id:opts.source_card_id,host_side:opts.host_side,host_lane:opts.host_lane,attachmentSlot:opts.attachmentSlot});
    }
    state.pending={type:'opponent_hand_choice', side:side, decision_side:side, opponent_side:opp, source_card_id:opts.source_card_id, host_side:opts.host_side, host_lane:opts.host_lane, attachmentSlot:opts.attachmentSlot, resolve_to:resolveTo, title:opts.title, instruction:opts.instruction, candidates:oppHand.map(function(id,idx){ return {card_id:id, hand_index:idx}; }), selected_index:null, reveal_cards:opponentHandRevealActive(state,side,opp)};
    pushLog(state, sourceName+' opens opponent hand choice. '+(state.pending.reveal_cards?'Magic Scope reveal is active.':'Cards remain face-down until selected.'));
    renderOpponentHandChoice(); render(); return true;
  }
  function renderOpponentHandChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='opponent_hand_choice') return;
    var p=appState.pending, choices=p.candidates||[], toDeck=p.resolve_to==='opponent_deck';
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent=p.title||((cardName(card(p.source_card_id)))+' — Choose Opponent Hand'); $('choiceConfirm').style.display=''; $('choiceConfirm').textContent=toDeck?'Shuffle Selected into Deck':'Discard Selected Card'; $('choiceConfirm').disabled=typeof p.selected_index!=='number';
    var instruction=p.instruction||(toDeck?'Choose 1 opponent hand card to shuffle into their Main Deck.':'Choose 1 opponent hand card to discard.');
    $('choiceBody').innerHTML='<p class="choice-instruction">'+esc(instruction)+'</p><div class="choice-grid card-search-choice-grid">'+choices.map(function(x,idx){ var sel=p.selected_index===idx, revealed=!!p.reveal_cards, img=revealed?thumbFor(x.card_id):'https://grandislegacytcg.github.io/shared/season1/v1/cards/ui/Back-of-Card-Main-Deck.webp', label=revealed?cardName(card(x.card_id)):('Opponent Card '+(idx+1)); return '<article class="card-search-choice '+(sel?'selected':'')+'"><button class="discard-preview" type="button" '+(revealed?'data-preview="'+esc(x.card_id)+'"':'data-opponent-hand-choice="'+idx+'"')+'><img src="'+esc(img)+'" alt="'+esc(label)+'"><span>'+esc(label)+'</span></button><button class="discard-select" type="button" data-opponent-hand-choice="'+idx+'">'+(sel?'Selected':'Select')+'</button></article>'; }).join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function selectOpponentHandChoice(idx){
    if(!appState || !appState.pending || appState.pending.type!=='opponent_hand_choice') return false;
    idx=Number(idx); if(idx<0 || idx>=(appState.pending.candidates||[]).length) return false;
    appState.pending.selected_index=idx; renderOpponentHandChoice(); return true;
  }
  function commitOpponentHandChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='opponent_hand_choice') return false;
    var p=appState.pending, cand=(p.candidates||[])[p.selected_index]; if(!cand){ showInfo('Choice Required','Choose an opponent hand card before confirming.'); return false; }
    var oppHand=sideHand(appState,p.opponent_side)||[], idx=Number(cand.hand_index); if(oppHand[idx]!==cand.card_id) idx=oppHand.indexOf(cand.card_id);
    if(idx>=0){
      var id=oppHand.splice(idx,1)[0];
      if(p.resolve_to==='opponent_deck'){ deckForSide(appState,p.opponent_side).push(id); shufflePile(deckForSide(appState,p.opponent_side)); pushLog(appState, cardName(card(p.source_card_id))+' shuffles selected opponent hand card into their Main Deck'+(p.reveal_cards?': '+cardName(card(id)):'.')); }
      else { sideDiscard(appState,p.opponent_side).push(id); pushLog(appState, cardName(card(p.source_card_id))+' discards selected opponent hand card'+(p.reveal_cards?': '+cardName(card(id)):'.')); }
      shufflePile(oppHand); pushLog(appState,'Remaining opponent hand shuffled after '+cardName(card(p.source_card_id))+' resolves.');
    } else pushLog(appState, cardName(card(p.source_card_id))+' selected opponent hand card was no longer available.');
    clearOpponentHandReveal(appState,p.side||'PLAYER',p.opponent_side);
    return finalizePostChoiceSourceCard(appState,p);
  }
  function renderDualArrowLanePairChoice(){
    // Dual Arrow uses the normal board-target click flow. No separate choice popup.
    if(appState && appState.pending && appState.pending.type==='exact_two_target_selection'){ closeChoice(); render(); }
  }
  function openDualArrowExactTwoTargetChoice(state, action){
    var targetSide=action.target_side||oppositeSide(action.source_side||'PLAYER');
    var targets=LANE_ORDER.filter(function(lane){
      var h=sideHeroes(state,targetSide)[lane];
      return !!h && (isLegacyModeHero(h) || Number(h.hp||0)>0);
    });
    var activeHeroCount=targets.filter(function(lane){
      var h=sideHeroes(state,targetSide)[lane];
      return h && Number(h.hp||0)>0 && !isLegacyModeHero(h);
    }).length;
    if(targets.length<2 || activeHeroCount<1){ showInfo('Cannot Play','Dual Arrow requires 2 distinct opponent field slots and at least 1 active Hero.'); state.pending=null; render(); return false; }
    state.pending={type:'exact_two_target_selection',decision_side:action.source_side,card_id:action.card_id,hand_index:action.hand_index,source_side:action.source_side,source_lane:action.source_lane,target_side:targetSide,legal_targets:targets,selected_target_lanes:[]};
    pushLog(state,'Dual Arrow: click exactly 2 distinct opponent Hero/Legacy slots. At least 1 must be an active Hero.');
    closeChoice(); render(); return true;
  }
  function performDualArrowPairChoice(choice){
    if(!appState || !appState.pending || appState.pending.type!=='exact_two_target_selection') return false;
    var p=appState.pending;
    if(typeof choice==='string' && choice!=='confirm'){
      if((p.legal_targets||[]).indexOf(choice)===-1)return false;p.selected_target_lanes=p.selected_target_lanes||[];var pos=p.selected_target_lanes.indexOf(choice);if(pos!==-1)p.selected_target_lanes.splice(pos,1);else if(p.selected_target_lanes.length<2)p.selected_target_lanes.push(choice);render();return true;
    }
    var selected=(p.selected_target_lanes||[]).slice();if(selected.length!==2){showInfo('Dual Arrow','Choose exactly 2 distinct opponent Hero/Legacy slots.');return false;}var selectedHasHero=selected.some(function(lane){var h=sideHeroes(appState,p.target_side)[lane];return h&&Number(h.hp||0)>0&&!isLegacyModeHero(h);});if(!selectedHasHero){showInfo('Dual Arrow','At least 1 selected slot must contain an active Hero.');return false;}
    var action={card_id:p.card_id,hand_index:p.hand_index,source_side:p.source_side,source_lane:p.source_lane,target_side:p.target_side,target_lanes:selected,target_lane:null};appState.pending=null;closeChoice();return commitPlayedCard(appState,action);
  }
  function renderScoutingExpChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='scouting_exp_selection') return;
    var p=appState.pending, choices=p.exp_choices||[], selected=Number(p.selected_exp_choice||0);
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent='Scouting — Choose EXP Card'; $('choiceConfirm').style.display=''; $('choiceConfirm').textContent='Discard Selected EXP'; $('choiceConfirm').disabled=!choices.length;
    $('choiceBody').innerHTML='<p class="choice-instruction">Choose exactly 1 non-Ultimate EXP Card from opponent '+esc(p.target_lane)+' to discard. Ultimate EXP Cards are excluded.</p><div class="choice-grid exp-choice-grid">'+choices.map(function(x,idx){ return '<article class="discard-choice '+(idx===selected?'selected':'')+'"><button class="discard-preview" type="button" data-preview="'+esc(x.card_id)+'"><img src="'+esc(thumbFor(x.card_id))+'" alt="'+esc(cardName(card(x.card_id)))+' thumbnail"><span>'+esc(cardName(card(x.card_id)))+'</span><small>EXP '+esc(x.exp_value)+'</small></button><button class="discard-select" type="button" data-scouting-exp-choice="'+idx+'">'+(idx===selected?'Selected':'Select')+'</button></article>'; }).join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function openScoutingExpChoice(state, action){
    var choices=legalScoutingExpChoices(state, action.source_side||'PLAYER', action.target_lane);
    if(!choices.length){ showInfo('Scouting','Target has no non-Ultimate EXP Card.'); state.pending=null; render(); return false; }
    state.pending={type:'scouting_exp_selection', card_id:action.card_id, hand_index:action.hand_index, source_side:action.source_side, source_lane:action.source_lane, target_side:action.target_side, target_lane:action.target_lane, exp_choices:choices, selected_exp_choice:0};
    pushLog(state,'Choose EXP Card to discard for Scouting.'); renderScoutingExpChoice(); render(); return true;
  }
  function selectScoutingExpChoice(idx){ if(!appState || !appState.pending || appState.pending.type!=='scouting_exp_selection') return false; if(!(appState.pending.exp_choices||[])[Number(idx)]) return false; appState.pending.selected_exp_choice=Number(idx); renderScoutingExpChoice(); return true; }
  function commitScoutingExpChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='scouting_exp_selection') return false;
    var p=appState.pending, choice=(p.exp_choices||[])[Number(p.selected_exp_choice||0)]; if(!choice){ showInfo('Scouting','Choose a legal EXP Card.'); return false; }
    var action={card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, target_side:p.target_side, target_lane:p.target_lane, scouting_exp_index:choice.exp_index};
    appState.pending=null; closeChoice(); return commitPlayedCard(appState,action);
  }
  function renderCrystalBallReorderChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='crystal_ball_reorder') return;
    var p=appState.pending, order=p.selected_order||[];
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent='Crystal Ball — Reorder Top Deck'; $('choiceConfirm').style.display=''; $('choiceConfirm').textContent='Confirm Order'; $('choiceConfirm').disabled=false;
    $('choiceBody').innerHTML='<p class="choice-instruction">Reorder the revealed top cards. The first card shown will be the next card drawn.</p><div class="choice-grid reorder-choice-grid">'+order.map(function(id,idx){ return '<article class="discard-choice"><button class="discard-preview" type="button" data-preview="'+esc(id)+'"><img src="'+esc(thumbFor(id))+'" alt="'+esc(cardName(card(id)))+' thumbnail"><span>#'+(idx+1)+' '+esc(cardName(card(id)))+'</span></button><div class="choice-footer-buttons"><button type="button" data-crystal-move="'+idx+':up" '+(idx===0?'disabled':'')+'>Up</button><button type="button" data-crystal-move="'+idx+':down" '+(idx===order.length-1?'disabled':'')+'>Down</button></div></article>'; }).join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function moveCrystalBallOrder(payload){
    if(!appState || !appState.pending || appState.pending.type!=='crystal_ball_reorder') return false;
    var parts=String(payload||'').split(':'), idx=Number(parts[0]), dir=parts[1], order=appState.pending.selected_order||[], to=dir==='up'?idx-1:idx+1;
    if(idx<0 || idx>=order.length || to<0 || to>=order.length) return false;
    var tmp=order[idx]; order[idx]=order[to]; order[to]=tmp; appState.pending.selected_order=order; renderCrystalBallReorderChoice(); return true;
  }
  function commitCrystalBallReorder(){
    if(!appState || !appState.pending || appState.pending.type!=='crystal_ball_reorder') return false;
    var p=appState.pending, deck=deckForSide(appState,p.side), n=(p.selected_order||[]).length;
    deck.splice.apply(deck,[0,n].concat((p.selected_order||[]).slice()));
    pushLog(appState,'Crystal Ball returns top '+n+' card(s) in chosen order: '+(p.selected_order||[]).map(function(id){return cardName(card(id));}).join(' → ')+'.');
    appState.pending=null; closeChoice(); syncCounts(appState); render(); return true;
  }
  function resolveCrystalBallCard(state,side,c,cardId,hostHero,attachmentSlot,persistent,holder){
    var top=deckForSide(state,side).slice(0,3);
    finalizeCommittedSourceCard(state,holder||{commit_token:activeCommitTokenFor(state,side,cardId)},side,cardId,'discard',{reason:'Crystal Ball resolved'});
    if(top.length<=1 || (side!=='PLAYER' && !state.pvpHumanVsHuman)){
      pushLog(state, cardName(c)+' looks at top '+top.length+' card(s): '+top.map(function(id){return cardName(card(id));}).join(', ')+'. Order preserved.');
      state.pending=null; syncCounts(state); render(); return true;
    }
    state.pending={type:'crystal_ball_reorder', side:side, decision_side:side, card_id:cardId, selected_order:top.slice()};
    pushLog(state, cardName(c)+' reveals top '+top.length+' card(s). Choose their return order.');
    renderCrystalBallReorderChoice(); syncCounts(state); render(); return true;
  }

  function openStatusRemovalChoice(state, ctx){
    ctx=ctx||{}; var choices=ctx.choices||negativeStatusChoices(sideHeroes(state,ctx.target_side)[ctx.target_lane]);
    state.pending={type:'status_removal_choice', card_id:ctx.card_id, hand_index:ctx.hand_index, source_side:ctx.source_side, source_lane:ctx.source_lane, target_side:ctx.target_side, target_lane:ctx.target_lane, status_choices:choices, selected_status_choice:0};
    pushLog(state,'Choose negative status to remove with '+cardName(card(ctx.card_id))+'.');
    renderStatusRemovalChoice(); render(); return true;
  }
  function renderStonebloodChoice(){
    if(!appState||!appState.pending||appState.pending.type!=='racial_stoneblood'||SUPPRESS_RENDER||!$('choiceOverlay'))return;var p=appState.pending,hero=sideHeroes(appState,p.side)[p.source_lane];
    $('choiceTitle').textContent='Stoneblood — Prevent Defeat';$('choiceConfirm').style.display='none';$('choiceBody').innerHTML='<div class="response-context-row"><article class="response-context-card"><h3>Dwarf Hero</h3><button class="response-context-tile" type="button" data-preview="'+esc(heroIdFrom(hero))+'"><img src="'+esc(thumbFor(heroIdFrom(hero)))+'" alt="'+esc(cardName(card(heroIdFrom(hero))))+'"><span><strong>'+esc(cardName(card(heroIdFrom(hero))))+'</strong><small>'+esc(p.side)+' · '+esc(p.source_lane)+'</small><small>Would be defeated</small></span></button></article></div><p class="choice-instruction">Spend 1 Racial Token to prevent defeat and remain at 10 HP, preserving the Hero state. Same Hero once per turn.</p><div class="choice-action-row"><button type="button" class="gold" data-stoneblood-choice="use">Spend 1 Token & Stay at 10 HP</button><button type="button" data-stoneblood-choice="decline">Decline & Continue Defeat</button></div>';$('choiceOverlay').classList.add('open');
  }
  function renderStatusRemovalChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='status_removal_choice') return;
    var p=appState.pending, choices=p.status_choices||[], selected=Number(p.selected_status_choice||0);
    if(!pvpLocalOwnsPending(appState.pending)){ pvpHideChoiceForNonOwner(); return; }
    if(SUPPRESS_RENDER || !$('choiceOverlay')) return;
    $('choiceTitle').textContent=cardName(card(p.card_id))+' — Choose Status'; $('choiceConfirm').style.display=''; $('choiceConfirm').textContent='Remove Selected Status'; $('choiceConfirm').disabled=!choices.length;
    $('choiceBody').innerHTML='<p class="choice-instruction">Choose exactly 1 negative status to remove from '+esc(p.target_lane)+'.</p><div class="choice-grid card-search-choice-grid">'+choices.map(function(x,idx){ var sel=idx===selected; return '<article class="discard-choice '+(sel?'selected':'')+'"><button class="discard-select" type="button" data-status-removal-index="'+idx+'">'+(sel?'Selected: ':'Select: ')+esc(x.label)+'</button><small>'+esc(x.detail||x.label)+'</small></article>'; }).join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function selectStatusRemovalChoice(idx){ if(!appState || !appState.pending || appState.pending.type!=='status_removal_choice') return false; idx=Number(idx); if(idx<0 || idx>=(appState.pending.status_choices||[]).length) return false; appState.pending.selected_status_choice=idx; renderStatusRemovalChoice(); return true; }
  function commitStatusRemovalChoice(){
    if(!appState || !appState.pending || appState.pending.type!=='status_removal_choice') return false;
    var p=appState.pending, choice=(p.status_choices||[])[Number(p.selected_status_choice||0)]; if(!choice){ showInfo('Elixir','Choose a status to remove.'); return false; }
    appState.pending=null; closeChoice(); return commitPlayedCard(appState,{card_id:p.card_id, hand_index:p.hand_index, source_side:p.source_side, source_lane:p.source_lane, target_side:p.target_side, target_lane:p.target_lane, selected_status_index:choice.status_index});
  }
  function commitMagicScopeReveal(){
    if(!appState||!appState.pending||appState.pending.type!=='magic_scope_reveal')return false;
    var p=appState.pending; finalizeCommittedSourceCard(appState,{commit_token:p.commit_token},p.side,p.source_card_id,'discard',{reason:'Magic Scope reveal closed'}); pushLog(appState,cardName(card(p.source_card_id))+' reveal closes and the Item moves to Discard Pile.'); appState.pending=null; closeChoice(); syncCounts(appState); render(); return true;
  }

  function handleChoiceConfirm(){ if(appState&&appState.pending&&appState.pending.type==='magic_scope_reveal') commitMagicScopeReveal(); else if(appState&&appState.pending&&appState.pending.type==='hand_limit_discard') commitHandLimitDiscard(); else if(appState&&appState.pending&&appState.pending.type==='card_search_choice') commitCardSearchChoice(); else if(appState&&appState.pending&&appState.pending.type==='legacy_defeat_choice') commitLegacyDefeatChoice(); else if(appState&&appState.pending&&appState.pending.type==='scouting_exp_selection') commitScoutingExpChoice(); else if(appState&&appState.pending&&appState.pending.type==='crystal_ball_reorder') commitCrystalBallReorder(); else if(appState&&appState.pending&&appState.pending.type==='mana_spend_choice') commitArrowBarrageManaSpend(); else if(appState&&appState.pending&&appState.pending.type==='draw_replacement_choice') showInfo('Choice Required','Choose Keep Card or Shuffle & Redraw for this Draw Phase ability.'); else if(appState&&appState.pending&&appState.pending.type==='optional_magical_surge') showInfo('Choice Required','Choose Use or Do Not Use for this optional class ability.'); else if(appState&&appState.pending&&appState.pending.type==='status_removal_choice') commitStatusRemovalChoice(); else if(appState&&appState.pending&&appState.pending.type==='saint_purify_choice') commitSaintPurifyChoice(); else if(appState&&appState.pending&&appState.pending.type==='opponent_hand_choice') commitOpponentHandChoice(); else if(appState&&appState.pending&&appState.pending.type==='response_extra_discard_choice') commitResponseExtraDiscardChoice(); else if(appState&&appState.pending&&appState.pending.type==='exact_two_target_selection') performDualArrowPairChoice('confirm'); else if(appState&&appState.pending&&appState.pending.type==='legacy_cost_selection') commitLegacyCostChoice(); else if(appState&&appState.pending&&appState.pending.type==='legacy_card_choice') commitLegacyCardChoice(); else if(appState&&appState.pending&&appState.pending.type==='optional_swap') showInfo('Swap Decision Required','Choose Swap or Do Not Swap.'); else if(appState&&appState.pending&&appState.pending.type==='optional_target_swap') showInfo('Swap Decision Required','Choose a target swap option or Do Not Swap.'); else if(appState&&appState.pending&&appState.pending.type==='manual_reposition') showInfo('Reposition','Choose a Reposition pair or Cancel.'); else closeChoice(); }
  function executeConfirmedSurrender(){
    if(appState && matchStarted && !appState.gameOver){ closeInfo(); finishGame(appState,'AI','PLAYER surrendered the match.'); syncCounts(appState); render(); showResult(); }
    else showInfo('Surrender','No active match to surrender.');
  }
  function requestSurrenderConfirmation(){
    if(!(appState && matchStarted && !appState.gameOver)){ showInfo('Surrender','No active match to surrender.'); return; }
    showInfoHtml('Confirm Surrender', '<p>Are you sure you want to surrender this match?</p><p>This will immediately end the game and count as an AI win.</p><div class="confirmActions"><button id="confirmSurrenderYes" class="dangerAction" type="button">Yes, Surrender</button><button id="confirmSurrenderNo" type="button">Cancel</button></div>');
    var yes=$('confirmSurrenderYes'); if(yes) yes.onclick=executeConfirmedSurrender;
    var no=$('confirmSurrenderNo'); if(no) no.onclick=closeInfo;
  }
  function closeMobileMatchMenu(){ var overlay=$('mobileMatchMenuOverlay'),button=$('mobileMatchMenuButton'); if(overlay){ overlay.classList.remove('open'); overlay.hidden=true; } if(button) button.setAttribute('aria-expanded','false'); }
  function openMobileMatchMenu(){ var overlay=$('mobileMatchMenuOverlay'),button=$('mobileMatchMenuButton'); if(!overlay) return false; overlay.hidden=false; overlay.classList.add('open'); if(button) button.setAttribute('aria-expanded','true'); var close=$('mobileMatchMenuClose'); if(close&&typeof close.focus==='function') setTimeout(function(){close.focus();},0); return true; }
  function bindMobileHandLongPress(el){
    if(!el||el._glMobileLongPressBound)return;
    el._glMobileLongPressBound=true;
    var timer=null,startX=0,startY=0,pointerId=null,triggered=false;
    function clearTimer(){if(timer){clearTimeout(timer);timer=null;}}
    function cancelPreview(){clearTimer();if(triggered)v59HandHoverZoomHide(el);triggered=false;pointerId=null;}
    el.addEventListener('contextmenu',function(ev){ev.preventDefault();});
    el.addEventListener('dragstart',function(ev){ev.preventDefault();});
    var image=el.querySelector&&el.querySelector('img');if(image){image.draggable=false;if(image.setAttribute)image.setAttribute('draggable','false');}
    el.addEventListener('pointerdown',function(ev){
      if(ev.pointerType==='mouse'||!isMobileViewport())return;
      clearTimer();pointerId=ev.pointerId;startX=Number(ev.clientX||0);startY=Number(ev.clientY||0);triggered=false;
      timer=setTimeout(function(){timer=null;triggered=v59HandHoverZoomShow(el.getAttribute('data-preview'),el);if(triggered)el._glSuppressClickUntil=Date.now()+650;},360);
    });
    el.addEventListener('pointermove',function(ev){
      if(pointerId===null||ev.pointerId!==pointerId)return;
      if(Math.abs(Number(ev.clientX||0)-startX)>10||Math.abs(Number(ev.clientY||0)-startY)>10)cancelPreview();
    });
    el.addEventListener('pointerup',cancelPreview);
    el.addEventListener('pointercancel',cancelPreview);
    el.addEventListener('lostpointercapture',cancelPreview);
    el.addEventListener('click',function(ev){if(Number(el._glSuppressClickUntil||0)>Date.now()){ev.preventDefault();ev.stopPropagation();if(typeof ev.stopImmediatePropagation==='function')ev.stopImmediatePropagation();el._glSuppressClickUntil=0;}},true);
  }
  function bindDynamicButtons(){
    var d=$('deckSetupButton'); if(d) d.onclick=showDeckSetup;
    var sound=$('soundToggleButton'); if(sound) sound.onclick=toggleCardMotionSound;
    var s=$('surrenderButton'); if(s) s.onclick=requestSurrenderConfirmation;
    var menu=$('mobileMatchMenuButton'); if(menu) menu.onclick=openMobileMatchMenu;
    var menuClose=$('mobileMatchMenuClose'); if(menuClose) menuClose.onclick=closeMobileMatchMenu;
    var mobileDeck=$('mobileDeckSetupButton'); if(mobileDeck) mobileDeck.onclick=function(){ closeMobileMatchMenu(); showDeckSetup(); };
    var mobileSound=$('mobileSoundToggleButton'); if(mobileSound) mobileSound.onclick=toggleCardMotionSound;
    var mobileSurrender=$('mobileSurrenderButton'); if(mobileSurrender) mobileSurrender.onclick=function(){ closeMobileMatchMenu(); requestSurrenderConfirmation(); };
    var rp=$('repositionButton'); if(rp) rp.onclick=function(){ openManualRepositionChoice(); };
    var h=$('historyButton'); if(h) h.onclick=function(){ showOpponentPlayedArchive(); };
    var opPrev=$('opponentPlayedPreviewButton'); if(opPrev) opPrev.onclick=function(){ var events=(appState&&appState.opponentPlayedEvents)||[]; var evt=events.find(function(e){ return e.id===appState.selectedOpponentEventId; }) || events[0] || appState.lastOpponentAction; if(evt) showInfo('Opponent Played', opponentPlayedDetail(evt,appState)); };
    var start=$('startFromControl'); if(start) start.onclick=function(){ if(matchStarted) resetMatch(); else showDeckSetup(); };
    var next=$('nextPhaseButton'); if(next) next.onclick=advancePhase;
    var cancel=$('cancelActionButton'); if(cancel) cancel.onclick=cancelPendingAction;
    var rclose=$('responseClose'); if(rclose) rclose.onclick=function(){ responsePassNoStuck(); };
    Array.prototype.forEach.call(document.querySelectorAll('[data-played-history-side]'),function(btn){btn.onclick=function(){v94ShowPlayedHistory(btn.getAttribute('data-played-history-side'));};});
    var combinedHistory=$('cardPlayedHistoryButton'); if(combinedHistory) combinedHistory.onclick=v96ShowCardPlayedHistory;
    Array.prototype.forEach.call(document.querySelectorAll('[data-played-preview-side]'),function(btn){btn.onclick=function(){var side=btn.getAttribute('data-played-preview-side'),evt=v94EventsForSide(appState,side)[0];if(evt)v94ShowPlayedDetail(side,evt.id);};});
    Array.prototype.forEach.call(document.querySelectorAll('[data-preview]'),function(el){el.addEventListener('pointerenter',function(){v94SetCardZoom(el.getAttribute('data-preview'));});el.addEventListener('focus',function(){v94SetCardZoom(el.getAttribute('data-preview'));});});
    Array.prototype.forEach.call(document.querySelectorAll('.hand-card .hand-art[data-preview]'),function(el){
      el.addEventListener('pointerenter',function(ev){if(!ev||ev.pointerType!=='touch')v59HandHoverZoomShow(el.getAttribute('data-preview'),el);});
      el.addEventListener('pointerleave',function(ev){if(!ev||ev.pointerType!=='touch')v59HandHoverZoomHide(el);});
      el.addEventListener('focus',function(){if(!isMobileViewport())v59HandHoverZoomShow(el.getAttribute('data-preview'),el);});
      el.addEventListener('blur',function(){v59HandHoverZoomHide(el);});
      bindMobileHandLongPress(el);
    });
  }
  function bind(){
    bindModalStackManager();
    primeAudioAsset('assets/audio/freesound_community-flipcard-91468.mp3');
    primeAudioAsset('assets/audio/freesound_community-coin-flip-37787.mp3');
    primeBattleFeedbackAssets();
    document.addEventListener('contextmenu',function(ev){if(ev.target&&ev.target.closest&&ev.target.closest('.hand-card'))ev.preventDefault();});
    document.addEventListener('dragstart',function(ev){if(ev.target&&ev.target.closest&&ev.target.closest('.hand-card'))ev.preventDefault();});
    document.body.addEventListener('click', function(ev){
      var safeDuringLock=ev.target.closest('#soundToggleButton,#mobileSoundToggleButton,#mobileMatchMenuButton,#mobileMatchMenuClose,#mobileMatchMenuOverlay,#mobileDeckSetupButton,#mobileSurrenderButton,#pvpRoomMobileButton,[data-preview],#previewClose,#infoClose,#choiceClose');
      if(gameplayInputLocked()&&!safeDuringLock){ev.preventDefault();ev.stopPropagation();return;}
      if(ev.target.id==='mobileMatchMenuOverlay'){ closeMobileMatchMenu(); return; }
      if(ev.target.closest('#hoverCardZoom.is-click-zoom')){ v55ClickZoomHide(); return; }
      var play=ev.target.closest('[data-play-index]'); if(play){ beginPlayFromHand(Number(play.getAttribute('data-play-index'))); return; }
      var tribute=ev.target.closest('[data-tribute-index]'); if(tribute){ beginTributeFromHand(Number(tribute.getAttribute('data-tribute-index'))); return; }
      var heroPick=ev.target.closest('.hero-panel'); if(heroPick && appState && appState.pending && (appState.pending.type==='source_selection'||appState.pending.type==='target_selection'||appState.pending.type==='exact_two_target_selection'||appState.pending.type==='scouting_target_selection'||appState.pending.type==='tribute_target'||appState.pending.type==='racial_target_selection'||appState.pending.type==='hero_ability_target_selection'||appState.pending.type==='legacy_hero_target_selection'||appState.pending.type==='double_casting_target_selection')){ chooseHeroFromBoard(heroPick.getAttribute('data-side'), heroPick.getAttribute('data-lane')); return; }
      var manaSpend=ev.target.closest('[data-mana-spend]'); if(manaSpend){ setArrowBarrageSpend(Number(manaSpend.getAttribute('data-mana-spend'))); return; }
      var drawChoice=ev.target.closest('[data-draw-replacement-choice]'); if(drawChoice){ commitDrawReplacementChoice(drawChoice.getAttribute('data-draw-replacement-choice')==='redraw'); return; }
      var surgeChoice=ev.target.closest('[data-magical-surge-choice]'); if(surgeChoice){ commitMagicalSurgeChoice(surgeChoice.getAttribute('data-magical-surge-choice')==='yes'); return; }
      var statusPick=ev.target.closest('[data-status-removal-index]'); if(statusPick){ selectStatusRemovalChoice(Number(statusPick.getAttribute('data-status-removal-index'))); return; }
      var saintPick=ev.target.closest('[data-saint-status-index]'); if(saintPick){ selectSaintPurifyChoice(Number(saintPick.getAttribute('data-saint-status-index'))); return; }
      var stonePick=ev.target.closest('[data-stoneblood-choice]'); if(stonePick){ resolveStonebloodChoice(stonePick.getAttribute('data-stoneblood-choice')==='use'); return; }
      var secondPick=ev.target.closest('[data-second-chance-choice]'); if(secondPick){ resolveSecondChanceChoice(secondPick.getAttribute('data-second-chance-choice')==='use'); return; }
      var scoutingExp=ev.target.closest('[data-scouting-exp-choice]'); if(scoutingExp){ selectScoutingExpChoice(Number(scoutingExp.getAttribute('data-scouting-exp-choice'))); return; }
      var crystalMove=ev.target.closest('[data-crystal-move]'); if(crystalMove){ moveCrystalBallOrder(crystalMove.getAttribute('data-crystal-move')); return; }
      var dualTarget=ev.target.closest('[data-dual-arrow-target]'); if(dualTarget){ performDualArrowPairChoice(dualTarget.getAttribute('data-dual-arrow-target')); return; }
      var responseExtraPick=ev.target.closest('[data-response-extra-discard-index]'); if(responseExtraPick){ selectResponseExtraDiscardChoice(Number(responseExtraPick.getAttribute('data-response-extra-discard-index'))); return; }
      var opponentHandPick=ev.target.closest('[data-opponent-hand-choice]'); if(opponentHandPick){ selectOpponentHandChoice(Number(opponentHandPick.getAttribute('data-opponent-hand-choice'))); return; }
      var pick=ev.target.closest('[data-discard-index]'); if(pick){ toggleDiscardIndex(Number(pick.getAttribute('data-discard-index'))); return; }
      var searchPick=ev.target.closest('[data-search-choice-index]'); if(searchPick){ selectCardSearchChoice(Number(searchPick.getAttribute('data-search-choice-index'))); return; }
      var legacyDefeatPick=ev.target.closest('[data-legacy-defeat-choice]'); if(legacyDefeatPick){ selectLegacyDefeatChoice(Number(legacyDefeatPick.getAttribute('data-legacy-defeat-choice'))); return; }
      var legacyCostPick=ev.target.closest('[data-legacy-cost-index]'); if(legacyCostPick){ selectLegacyCostChoice(Number(legacyCostPick.getAttribute('data-legacy-cost-index'))); return; }
      var legacyCardPick=ev.target.closest('[data-legacy-card-choice]'); if(legacyCardPick){ selectLegacyCardChoice(Number(legacyCardPick.getAttribute('data-legacy-card-choice'))); return; }
      var rsel=ev.target.closest('[data-response-select]'); if(rsel && appState && appState.responseWindow){ responseSelectNoStuck(Number(rsel.getAttribute('data-response-select'))); return; }
      if(ev.target.closest('#responsePassButton')){ responsePassNoStuck(); return; }
      if(ev.target.closest('#responseResolveButton')){ var rw=appState&&appState.responseWindow; var opt=rw&&rw.options&&rw.options[rw.selected]; if(opt) resolveResponseWindow(opt); return; }
      var oss=ev.target.closest('[data-source-swap-lane]'); if(oss){ performOptionalSwapDecision(oss.getAttribute('data-source-swap-lane')); return; }
      if(ev.target.closest('#optionalSwapYes')){ performOptionalSwapDecision(true); return; }
      if(ev.target.closest('#optionalSwapNo')){ performOptionalSwapDecision(false); return; }
      var ots=ev.target.closest('[data-target-swap-lane]'); if(ots){ performOptionalTargetSwapDecision(ots.getAttribute('data-target-swap-lane')); return; }
      var mobileHeroAction=ev.target.closest('[data-mobile-hero-action]'); if(mobileHeroAction){v540OpenMobileHeroActions(mobileHeroAction.getAttribute('data-mobile-action-side'),mobileHeroAction.getAttribute('data-mobile-action-lane'));return;}
      var classBtn=ev.target.closest('[data-class-ability-id]'); if(classBtn && appState){if(classBtn.closest('.mobile-hero-action-menu'))closeInfo(); beginActivatedHeroAbility(appState, classBtn.getAttribute('data-class-ability-side'), classBtn.getAttribute('data-class-ability-lane'), classBtn.getAttribute('data-class-ability-id')); return; }
      var legacyBtn=ev.target.closest('[data-legacy-id]'); if(legacyBtn && appState){if(legacyBtn.closest('.mobile-hero-action-menu'))closeInfo(); beginActivatedLegacyAbility(appState, legacyBtn.getAttribute('data-legacy-side'), legacyBtn.getAttribute('data-legacy-lane'), legacyBtn.getAttribute('data-legacy-id')); return; }
      var racialBtn=ev.target.closest('[data-racial-id]'); if(racialBtn && appState){if(racialBtn.closest('.mobile-hero-action-menu'))closeInfo(); beginActivatedRacialAbility(appState, racialBtn.getAttribute('data-racial-side'), racialBtn.getAttribute('data-racial-lane'), racialBtn.getAttribute('data-racial-id')); return; }
      var mr=ev.target.closest('[data-reposition-pair]'); if(mr){ performManualReposition(mr.getAttribute('data-reposition-pair')); return; }
      if(ev.target.closest('#manualRepositionCancel')){ cancelManualReposition(); return; }
      if(ev.target.closest('#optionalTargetSwapNo')){ performOptionalTargetSwapDecision(false); return; }
      var opEvt=ev.target.closest('[data-op-event-id]'); if(opEvt && appState){ appState.selectedOpponentEventId=opEvt.getAttribute('data-op-event-id'); }
      var opArch=ev.target.closest('[data-op-archive-id]'); if(opArch && appState){ var ae=(appState.opponentPlayedEvents||[]).find(function(e){ return e.id===opArch.getAttribute('data-op-archive-id'); }); if(ae){ appState.selectedOpponentEventId=ae.id; showOpponentEventDetail(ae); } return; }
      var info=ev.target.closest('[data-info-title]'); if(info){ showInfo(info.getAttribute('data-info-title'), info.getAttribute('data-info-body')); return; }
      var combinedEvt=ev.target.closest('[data-combined-event-id]'); if(combinedEvt){v55ZoomPlayedCard(combinedEvt.getAttribute('data-combined-event-side'),combinedEvt.getAttribute('data-combined-event-id'));return;}
      var combinedArchive=ev.target.closest('[data-combined-archive-id]'); if(combinedArchive){v55ZoomPlayedCard(combinedArchive.getAttribute('data-combined-archive-side'),combinedArchive.getAttribute('data-combined-archive-id'));return;}
      var sideEvt=ev.target.closest('[data-side-event-id]'); if(sideEvt){v55ZoomPlayedCard(sideEvt.getAttribute('data-side-event'),sideEvt.getAttribute('data-side-event-id'));return;}
      var sideArchive=ev.target.closest('[data-side-archive-id]'); if(sideArchive){v55ZoomPlayedCard(sideArchive.getAttribute('data-side-archive'),sideArchive.getAttribute('data-side-archive-id'));return;}
      var legacyDeck=ev.target.closest('[data-legacy-side]'); if(legacyDeck){showLegacyDeck(legacyDeck.getAttribute('data-legacy-side'));return;}
      var disc=ev.target.closest('[data-discard-side]'); if(disc){ showDiscard(disc.getAttribute('data-discard-side')); return; }
      var zoomCard=ev.target.closest('[data-preview]'); if(zoomCard){ showPreview(zoomCard.getAttribute('data-preview')); return; }
    });
    $('previewClose').addEventListener('click', function(){ $('previewOverlay').classList.remove('open'); }); $('previewOverlay').addEventListener('click', function(ev){ if(ev.target.id==='previewOverlay') $('previewOverlay').classList.remove('open'); });
    $('infoClose').addEventListener('click', closeInfo); $('infoOverlay').addEventListener('click', function(ev){ if(ev.target.id==='infoOverlay') closeInfo(); });
    $('choiceClose').addEventListener('click', function(){
      var p=appState&&appState.pending, policy=localPendingClosePolicy(appState,p);
      if(policy==='PRE_COMMIT_CANCEL'){ cancelPendingAction(); return; }
      if(policy==='POST_RESOLUTION_DECLINE'){
        if(p.type==='optional_swap'){ performOptionalSwapDecision(false); return; }
        if(p.type==='optional_target_swap'){ performOptionalTargetSwapDecision(false); return; }
        if(p.type==='post_attack_reposition_choice' && typeof skipPostAttackReposition==='function'){ skipPostAttackReposition(); return; }
      }
      if(policy==='MANDATORY_NO_CANCEL'){
        if(p&&p.type==='legacy_defeat_choice') showInfo('Choice Required','Choose a Legacy card. Hero defeat selection is mandatory.');
        else if(pendingRevealsHiddenInformation(p)) showInfo('Cannot Cancel','Hidden information has already been opened and cannot be unseen. Finish resolving this effect.');
        else if(appState&&appState.responseWindow) showInfo('Cannot Cancel','The action has opened an opponent response window and is already part of the shared game state.');
        else showInfo('Choice Required','This committed or mandatory choice must be resolved before closing.');
        return;
      }
      closeChoice();
    });
    $('choiceConfirm').addEventListener('click', handleChoiceConfirm); $('choiceOverlay').addEventListener('click', function(ev){ if(ev.target.id==='choiceOverlay' && !(appState&&appState.pending)) closeChoice(); });
    document.addEventListener('keydown', function(ev){ if(ev.key==='Escape'){ v55ClickZoomHide(); $('previewOverlay').classList.remove('open'); if(!(appState&&appState.pending)) closeChoice(); closeInfo(); } });
  }
  function simulateCoreLoop(){
    initCards(); var pv=validateDeck(decks.PLAYER,'PLAYER'), av=validateDeck(decks.AI,'AI'); if(!pv.ok||!av.ok) return {ok:false, reason:'deck validation failed', pv:pv, av:av};
    var s=buildInitialMatchState();
    if(s.phase!=='Draw'||s.turn!=='PLAYER'||s.mana!==0||s.manaRegen!==2) return {ok:false, reason:'initial state mismatch'};
    resolveDrawPhase(s,'PLAYER'); s.phase='Deploy'; if(s.playerHand.length!==7||s.mana!==2) return {ok:false, reason:'player draw/mana mismatch', hand:s.playerHand.length, mana:s.mana};
    s.phase='Battle'; s.phase='Reform'; s.phase='End'; if(!cleanupHandLimit(s,'PLAYER')) return {ok:false, reason:'unexpected hand cleanup at 8 cards'};
    runAITurn(s); if(s.turn!=='PLAYER'||s.phase!=='Draw'||s.aiHand.length!==7||s.aiMana!==2||s.round!==2) return {ok:false, reason:'AI turn return mismatch', turn:s.turn, phase:s.phase, aiHand:s.aiHand.length, aiMana:s.aiMana};
    // second player draw creates an 8-card hand after the new 6-card opening hand; no cleanup required yet.
    resolveDrawPhase(s,'PLAYER'); s.phase='End'; var cleaned=cleanupHandLimit(s,'PLAYER'); if(!cleaned||s.pending) return {ok:false, reason:'hand limit cleanup mismatch after 6-card opening hand', pending:s.pending, hand:s.playerHand.length};
    return {ok:true, playerDeckValid:pv.ok, aiDeckValid:av.ok, fullLoopReturn:true, handLimitPending:true, final:{turn:s.turn,phase:s.phase,playerHand:s.playerHand.length,aiHand:s.aiHand.length,mana:s.mana,aiMana:s.aiMana}};
  }
  
  function simulateSourceTargetInteraction(){
    initCards(); var pv=validateDeck(decks.PLAYER,'PLAYER'), av=validateDeck(decks.AI,'AI'); if(!pv.ok||!av.ok) return {ok:false, reason:'deck validation failed'};
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true; s.phase='Battle'; s.turn='PLAYER'; s.mana=2;
    var idx=s.playerHand.indexOf('S1-WAR-001'); if(idx<0) return {ok:false, reason:'Slash missing from starter hand'};
    beginPlayFromHand(idx); if(!s.pending || s.pending.type!=='source_selection') return {ok:false, reason:'did not enter source selection', pending:s.pending};
    if(s.pending.legal_sources.indexOf('LEFT')<0) return {ok:false, reason:'LEFT Warrior not legal source', sources:s.pending.legal_sources};
    cancelPendingAction(); if(s.pending || s.playerHand.indexOf('S1-WAR-001')<0 || s.mana!==2) return {ok:false, reason:'cancel failed rollback', pending:s.pending, hand:s.playerHand.length, mana:s.mana};
    beginPlayFromHand(idx); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.type!=='target_selection') return {ok:false, reason:'did not enter target selection', pending:s.pending};
    if(s.pending.legal_targets.indexOf('LEFT')<0 || s.pending.legal_targets.indexOf('CENTER')<0 || s.pending.legal_targets.indexOf('RIGHT')!==-1) return {ok:false, reason:'target coverage mismatch', targets:s.pending.legal_targets};
    var beforeHp=s.aiHeroes.LEFT.hp; chooseHeroFromBoard('AI','LEFT');
    if(s.responseWindow && s.pending && s.pending.type==='response_window') resolveResponseWindow(null);
    var ok=!s.pending && s.playerHand.indexOf('S1-WAR-001')<0 && s.playerDiscard.indexOf('S1-WAR-001')>=0 && s.mana===0 && s.playerHeroes.LEFT.exhausted===true && s.aiHeroes.LEFT.hp===beforeHp-20;
    var result={ok:ok, cancelRollback:true, manualSource:true, targetCommit:ok, final:{mana:s.mana, sourceExhausted:s.playerHeroes.LEFT.exhausted, aiLeftHp:s.aiHeroes.LEFT.hp, discard:s.playerDiscard.length}};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return result;
  }
  function simulateCoreSystems(){
    initCards(); var pv=validateDeck(decks.PLAYER,'PLAYER'), av=validateDeck(decks.AI,'AI'); if(!pv.ok||!av.ok) return {ok:false, reason:'deck validation failed'};
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true;
    s.phase='Reform'; s.turn='PLAYER'; s.mana=2; s.tributeUsedThisReform=false;
    var idx=s.playerHand.indexOf('S1-WAR-001'); if(idx<0) return {ok:false, reason:'tribute material missing'};
    beginTributeFromHand(idx); if(!s.pending || s.pending.type!=='tribute_target') return {ok:false, reason:'did not enter tribute target', pending:s.pending};
    if(s.pending.legal_targets.indexOf('LEFT')<0 || s.pending.legal_targets.indexOf('CENTER')<0) return {ok:false, reason:'tribute targets not legal cross-class', targets:s.pending.legal_targets};
    chooseHeroFromBoard('PLAYER','CENTER'); if(s.pending) return {ok:false, reason:'tribute pending not cleared'};
    if(s.playerHeroes.CENTER.exp_total!==100 || s.playerHeroes.CENTER.exp_cards.length!==1 || s.playerHand.indexOf('S1-WAR-001')!==-1) return {ok:false, reason:'tribute EXP mismatch', hero:s.playerHeroes.CENTER};
    if(s.mana!==2) return {ok:false, reason:'tribute paid mana unexpectedly', mana:s.mana};
    if(!s.tributeUsedThisReform) return {ok:false, reason:'tribute use flag not set'};
    // Rank Up: inject two more 100 EXP skills to the LEFT hero and confirm immediate Rank II reward.
    s.tributeUsedThisReform=false; s.playerHand.unshift('S1-WAR-002','S1-WAR-006'); beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); s.tributeUsedThisReform=false; beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); s.tributeUsedThisReform=false; s.playerHand.unshift('S1-WAR-001'); beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT');
    if(heroRank(s.playerHeroes.LEFT)!==2 || s.playerHeroes.LEFT.card_id!=='S1-WAR-H002') return {ok:false, reason:'rank II not reached', left:s.playerHeroes.LEFT};
    if(s.manaRegen!==2) return {ok:false, reason:'mana regen reward mismatch', manaRegen:s.manaRegen};
    if(s.mana!==2) return {ok:false, reason:'rank up incorrectly granted mana pool', mana:s.mana};
    // Attachment lifecycle: Coordination Attack attaches in Deploy and expires after Battle.
    s.phase='Deploy'; s.tributeUsedThisReform=false; s.mana=10; s.playerHand.unshift('S1-EVT-011'); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT');
    if(s.playerHeroes.LEFT.attachments.filter(Boolean).indexOf('S1-EVT-011')<0) return {ok:false, reason:'Coordination Attack did not attach', attachments:s.playerHeroes.LEFT.attachments};
    var beforeDiscard=s.playerDiscard.length; expireBattleAttachments(s);
    if(s.playerHeroes.LEFT.attachments.filter(Boolean).indexOf('S1-EVT-011')!==-1 || s.playerDiscard.length!==beforeDiscard+1) return {ok:false, reason:'Coordination Attack did not expire to discard'};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, tribute:true, crossClassTribute:true, rankUp:true, manaRegenReward:true, noManaPoolReward:true, attachmentLifecycle:true, discardViewer:true};
  }
  function simulateResponseOpponentPlayed(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true;
    s.phase='Battle'; s.turn='PLAYER'; s.mana=6; var idx=s.playerHand.indexOf('S1-WAR-001'); if(idx<0) return {ok:false, reason:'Slash missing'};
    beginPlayFromHand(idx); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.type!=='target_selection') return {ok:false, reason:'target selection missing'};
    var hp=s.aiHeroes.LEFT.hp; chooseHeroFromBoard('AI','LEFT'); if(s.responseWindow || s.pending) return {ok:false, reason:'PLAYER attack should auto-resolve AI response without player-facing Response Window', pending:s.pending, rw:s.responseWindow};
    if(s.aiHeroes.LEFT.hp>=hp) return {ok:false, reason:'damage not applied after AI auto-pass'};
    if(s.playerDiscard.indexOf('S1-WAR-001')<0) return {ok:false, reason:'attack card not discarded after response'};
    // Simulate incoming AI attack against PLAYER with a compact response option in player hand.
    s.playerHand.unshift('S1-ITM-016'); var aiAction={card_id:'S1-THF-001', source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT'}; openAttackResponseWindow(s, aiAction, {damage:10,persistent:false,attachmentSlot:-1});
    if(!s.responseWindow || (s.responseWindow.options||[]).length<1) return {ok:false, reason:'player response option missing', opts:s.responseWindow&&s.responseWindow.options};
    s.responseWindow.selected=0; var before=s.playerHeroes.LEFT.hp; resolveResponseWindow(s.responseWindow ? s.responseWindow.options[0] : null); if(s.playerHeroes.LEFT.hp!==before) return {ok:false, reason:'Chain Mail should block 10 physical damage'};
    s.lastOpponentAction={label:'ATK', card_id:'S1-THF-001', title:'Dash Stab', summary:'AI attacks PLAYER LEFT with Dash Stab.'};
    var result={ok:true, responseWindowOpens:true, compactResponseOption:true, takeHitNoResponse:true, responseResolution:true, opponentPlayedModel:true}; appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return result;
  }


  function simulateWarriorEffectAuditPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true;
    // Status application after blocked damage: Power Slash inflicts Bleed even if Parry blocks all damage.
    s.phase='Battle'; s.turn='PLAYER'; s.mana=8; s.playerHand=['S1-WAR-002']; s.aiHand=[]; s.aiHeroes.LEFT.statuses=[];
    beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow) return {ok:false, reason:'Power Slash response missing'};
    var before=s.aiHeroes.LEFT.hp; resolveResponseWindow(null); if(s.aiHeroes.LEFT.hp!==before-20) return {ok:false, reason:'Power Slash damage mismatch'};
    if(!hasStatus(s.aiHeroes.LEFT,'Bleed')) return {ok:false, reason:'Bleed not applied'};
    // Rage Blast +20 conditional vs Bleed.
    s.playerHeroes.LEFT.card_id='S1-WAR-H003'; s.playerHeroes.LEFT.hp=120; s.playerHeroes.LEFT.maxHp=140; s.playerHeroes.LEFT.exhausted=false; s.phase='Battle'; s.mana=10; s.playerHand=['S1-WAR-010']; var hp=s.aiHeroes.LEFT.hp; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow || s.responseWindow.damage!==90) return {ok:false, reason:'Rage Blast conditional damage mismatch', damage:s.responseWindow&&s.responseWindow.damage}; resolveResponseWindow(null); if(s.aiHeroes.LEFT.hp!==Math.max(0,hp-90)) return {ok:false, reason:'Rage Blast final damage mismatch', before:hp, after:s.aiHeroes.LEFT.hp};
    // Deflect class fallback: Conqueror uses Gladiator branch for 40 block.
    var conq=heroState('S1-WAR-H003','PLAYER','LEFT'); var rc=card('S1-WAR-012'); if(responseBlockAmount(rc,{damage_type:'Physical'},conq)!==40) return {ok:false, reason:'Deflect class fallback failed'};
    // Sacred Stormblade cannot be dodged.
    if(!attackCannotBeDodged(card('S1-WAR-021'))) return {ok:false, reason:'Sacred Stormblade restriction missing'};
    s.aiHeroes.LEFT.hp=40; s.playerHeroes.LEFT.card_id='S1-WAR-H002'; s.playerHeroes.LEFT.exhausted=false; s.phase='Battle'; s.mana=8; s.playerHand=['S1-WAR-018']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(s.aiHeroes.LEFT.hp!==0) return {ok:false, reason:'Execute did not defeat threshold target'};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, warriorSkillCardsAudited:24, statusInflict:true, conditionalDamage:true, classBranchFallback:true, cannotDodgeRestriction:true, executeThreshold:true};
  }


  function simulateMageEffectAuditPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true;
    // Meditation: Deploy Tactical gains Mana and discards after instant resolution.
    s.phase='Deploy'; s.turn='PLAYER'; s.mana=0; s.playerHand=['S1-MAG-006']; s.playerHeroes.CENTER.exhausted=false;
    beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT');
    if(s.mana!==2 || s.playerDiscard.indexOf('S1-MAG-006')<0) return {ok:false, reason:'Meditation gain/discard failed', mana:s.mana, discard:s.playerDiscard};
    // Fireball: Magical damage + Burn 1, status applies after unblocked resolution.
    s.phase='Battle'; s.mana=8; s.playerHand=['S1-MAG-003']; s.playerHeroes.CENTER.exhausted=false; s.aiHeroes.CENTER.statuses=[]; var hp=s.aiHeroes.CENTER.hp;
    beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER'); if(s.responseWindow){ if(s.responseWindow.damage!==30 || s.responseWindow.damage_type!=='Magical') return {ok:false, reason:'Fireball response data mismatch', rw:s.responseWindow}; resolveResponseWindow(null); } if(s.aiHeroes.CENTER.hp!==hp-30 || !hasStatus(s.aiHeroes.CENTER,'Burn')) return {ok:false, reason:'Fireball damage/status failed', hp:s.aiHeroes.CENTER.hp, before:hp, statuses:s.aiHeroes.CENTER.statuses};
    // Frostbite class duration: Elementalist branch applies Freeze 2.
    s.playerHeroes.CENTER.card_id='S1-MAG-H002'; s.playerHeroes.CENTER.hp=80; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.phase='Battle'; s.mana=10; s.playerHand=['S1-MAG-008']; s.aiHeroes.LEFT.statuses=[];
    beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); if(s.responseWindow){ if(s.responseWindow.damage!==40) return {ok:false, reason:'Frostbite Elementalist damage mismatch', damage:s.responseWindow&&s.responseWindow.damage}; resolveResponseWindow(null); } var fr=(s.aiHeroes.LEFT.statuses||[]).find(function(st){return statusName(st)==='Freeze';}); if(!fr || Number(fr.duration)!==2) return {ok:false, reason:'Frostbite Freeze duration mismatch', statuses:s.aiHeroes.LEFT.statuses};
    // Permafrost target legality requires Freeze.
    s.phase='Battle'; s.mana=10; s.playerHand=['S1-MAG-015']; s.playerHeroes.CENTER.exhausted=false; s.aiHeroes.CENTER.statuses=[]; var targetsNoFreeze=legalTargetLanes(s,'S1-MAG-015','CENTER');
    addStatus(s.aiHeroes.CENTER,'Freeze',1,'test Freeze'); var targetsFreeze=legalTargetLanes(s,'S1-MAG-015','CENTER');
    if(targetsNoFreeze.indexOf('CENTER')!==-1 || targetsFreeze.indexOf('CENTER')===-1) return {ok:false, reason:'Permafrost Freeze target legality failed', noFreeze:targetsNoFreeze, withFreeze:targetsFreeze};
    // Ice Block: damage immunity prevents attack damage and applies self Freeze.
    s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-MAG-011']; s.mana=10; var before=s.playerHeroes.CENTER.hp; openAttackResponseWindow(s,{card_id:'S1-THF-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'},{damage:30,persistent:false,attachmentSlot:-1});
    if(!s.responseWindow || !(s.responseWindow.options||[]).some(function(o){return o.card_id==='S1-MAG-011';})) return {ok:false, reason:'Ice Block response option missing', opts:s.responseWindow&&s.responseWindow.options};
    var opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-MAG-011';}); resolveResponseWindow(opt);
    if(s.playerHeroes.CENTER.hp!==before || !hasStatus(s.playerHeroes.CENTER,'Freeze')) return {ok:false, reason:'Ice Block immunity/self Freeze failed', before:before, after:s.playerHeroes.CENTER.hp, statuses:s.playerHeroes.CENTER.statuses};
    // Fire Wall: block Physical and Burn attacking hero.
    s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-MAG-012']; s.mana=10; s.aiHeroes.LEFT.statuses=[]; before=s.playerHeroes.CENTER.hp; openAttackResponseWindow(s,{card_id:'S1-THF-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'},{damage:30,persistent:false,attachmentSlot:-1});
    opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-MAG-012';}); if(!opt) return {ok:false, reason:'Fire Wall response option missing'}; resolveResponseWindow(opt);
    if(s.playerHeroes.CENTER.hp!==before || !hasStatus(s.aiHeroes.LEFT,'Burn')) return {ok:false, reason:'Fire Wall block/burn failed', before:before, after:s.playerHeroes.CENTER.hp, aiStatuses:s.aiHeroes.LEFT.statuses};
    // Mage ultimate owner/rank validator and rank cost check.
    var dc=card('S1-MAG-018'); var rank2Mage=heroState('S1-MAG-H002','PLAYER','CENTER'), wrongOwner=heroState('S1-MAG-H005','PLAYER','CENTER');
    if(!sourceCompatible(dc,rank2Mage) || sourceCompatible(dc,wrongOwner)) return {ok:false, reason:'Double Casting owner/rank validator failed'};
    if(cardPlayCostForSource(dc,rank2Mage)!==5) return {ok:false, reason:'Double Casting Rank II cost failed', cost:cardPlayCostForSource(dc,rank2Mage)};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, mageSkillCardsAudited:19, meditationMana:true, magicalDamage:true, burnFreezeDurations:true, permafrostFreezeTarget:true, iceBlock:true, fireWall:true, ultimateOwnerRankCost:true};
  }



  function simulateStep9CombinedHandlers(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true;
    // Cover Up legal only for adjacent ally against Single Target.
    s.turn='PLAYER'; s.phase='Battle'; s.mana=10; s.playerHand=['S1-WAR-004']; s.playerHeroes.CENTER.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; openAttackResponseWindow(s,{card_id:'S1-THF-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'},{damage:30,persistent:false,attachmentSlot:-1});
    var cover=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-WAR-004' && o.source_lane==='CENTER';}); if(!cover) return {ok:false, reason:'Cover Up legal adjacent option missing', opts:s.responseWindow.options}; var oldLeft=s.playerHeroes.LEFT.card_id, oldCenter=s.playerHeroes.CENTER.card_id; resolveResponseWindow(cover); if(s.playerHeroes.LEFT.card_id!==oldCenter || s.playerHeroes.CENTER.card_id!==oldLeft) return {ok:false, reason:'Cover Up did not swap/redirect'};
    s.playerHand=['S1-WAR-004']; openAttackResponseWindow(s,{card_id:'S1-MAG-019',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'},{damage:30,persistent:false,attachmentSlot:-1}); if((s.responseWindow.options||[]).some(function(o){return o.card_id==='S1-WAR-004';})) return {ok:false, reason:'Cover Up appeared against All Target'}; resolveResponseWindow(null);
    // Final Grit revives defeated Gladiator/Conqueror and is not a heal.
    s.phase='Deploy'; s.mana=10; s.playerHeroes.LEFT.card_id='S1-WAR-H002'; s.playerHeroes.LEFT.hp=0; s.playerHeroes.CENTER.card_id='S1-WAR-H003'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=140; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-WAR-014']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.hp!==30 || !s.playerHeroes.LEFT.exhausted || !hasStatus(s.playerHeroes.LEFT,'Final Grit')) return {ok:false, reason:'Final Grit revive failed', hero:s.playerHeroes.LEFT};
    // Charge Attack optional swap yes/no.
    s.phase='Battle'; s.mana=20; s.playerHeroes.LEFT.card_id='S1-WAR-H003'; s.playerHeroes.LEFT.hp=140; s.playerHeroes.LEFT.maxHp=140; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-WAR-015']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','CENTER'); if(!s.responseWindow) return {ok:false, reason:'Charge response missing'}; resolveResponseWindow(null); if(!s.pending || s.pending.type!=='optional_swap') return {ok:false, reason:'Charge optional swap not pending', pending:s.pending}; performOptionalSwapDecision(true); if(s.playerHeroes.CENTER.card_id!=='S1-WAR-H003') return {ok:false, reason:'Charge swap yes failed'};
    s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-WAR-015']; s.mana=20; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); resolveResponseWindow(null); if(!s.pending || s.pending.type!=='optional_swap') return {ok:false, reason:'Charge optional swap second pending missing'}; var beforeLane=s.playerHeroes.CENTER.card_id; performOptionalSwapDecision(false); if(s.playerHeroes.CENTER.card_id!==beforeLane) return {ok:false, reason:'Charge do-not-swap moved hero'};
    // Taunt targeting helper.
    addStatus(s.playerHeroes.RIGHT,'Taunt',1,'test taunt'); var forced=applyTauntTargeting(s,'PLAYER',['LEFT','CENTER','RIGHT']); if(forced.length!==1 || forced[0]!=='RIGHT') return {ok:false, reason:'Taunt targeting did not force legal source', forced:forced}; var unforced=applyTauntTargeting(s,'PLAYER',['LEFT','CENTER']); if(unforced.indexOf('RIGHT')!==-1 || unforced.length!==2) return {ok:false, reason:'Taunt targeting forced illegal target'};
    // Double Casting active effect and consumed next attack.
    s.phase='Deploy'; s.mana=20; s.playerHeroes.CENTER.card_id='S1-MAG-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; s.playerHand=['S1-MAG-018']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!hasDoubleCasting(s.playerHeroes.CENTER)) return {ok:false, reason:'Double Casting badge/status missing'};
    s.phase='Battle'; s.pvpHumanVsHuman=true; s.playerHeroes.CENTER.exhausted=false; s.aiHeroes.CENTER.hp=200; s.aiHeroes.CENTER.maxHp=200; s.playerHand=['S1-MAG-016']; s.mana=20; var hp=s.aiHeroes.CENTER.hp; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER'); if(!s.responseWindow) return {ok:false,reason:'Double Casting target 1 did not open a Response Window'}; resolveResponseWindow(null); if(!s.pending||s.pending.type!=='double_casting_target_selection') return {ok:false,reason:'Double Casting did not request target 2/2',pending:s.pending}; chooseHeroFromBoard('AI','CENTER'); if(!s.responseWindow) return {ok:false,reason:'Double Casting target 2 did not open a second Response Window'}; resolveResponseWindow(null); s.pvpHumanVsHuman=false; if(s.aiHeroes.CENTER.hp!==Math.max(0,hp-160) || hasDoubleCasting(s.playerHeroes.CENTER)) return {ok:false, reason:'Double Casting same target failed', before:hp, after:s.aiHeroes.CENTER.hp, statuses:s.playerHeroes.CENTER.statuses};
    // Casting Time 1 declares CAST then resolves as ATK on next owner Battle; Stun fails but Freeze does not.
    s.phase='Battle'; s.mana=20; s.playerHeroes.CENTER.card_id='S1-MAG-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].maxHp=100; }); s.playerHand=['S1-MAG-010']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER'); if(!s.pendingCastings || s.pendingCastings.length!==1 || s.playerHeroes.CENTER.attachments.indexOf('S1-MAG-010')<0) return {ok:false, reason:'CAST declare/pending attachment failed', pending:s.pending, castings:s.pendingCastings, attachments:s.playerHeroes.CENTER.attachments, hand:s.playerHand, mana:s.mana, phase:s.phase}; var beforeCast=s.aiHeroes.CENTER.hp; addStatus(s.playerHeroes.CENTER,'Freeze',1,'freeze should not fail cast'); resolvePendingCastingsFor(s,'PLAYER'); if(s.aiHeroes.CENTER.hp>=beforeCast) return {ok:false, reason:'Casting did not resolve through Freeze'};
    s.phase='Battle'; s.mana=20; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; s.playerHand=['S1-MAG-010']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER'); addStatus(s.playerHeroes.CENTER,'Stun',1,'stun fails cast'); beforeCast=s.aiHeroes.CENTER.hp; resolvePendingCastingsFor(s,'PLAYER'); if(s.aiHeroes.CENTER.hp!==beforeCast){} else { /* expected no damage */ } if(s.aiHeroes.CENTER.hp!==beforeCast) return {ok:false, reason:'Stun should fail casting'};
    // Area/All and Wildfire semantics.
    s.playerHeroes.CENTER.statuses=[]; s.playerHeroes.CENTER.card_id='S1-MAG-H005'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].maxHp=100; }); s.phase='Battle'; s.mana=20; s.playerHand=['S1-MAG-019']; var hpL=s.aiHeroes.LEFT.hp, hpC=s.aiHeroes.CENTER.hp, hpR=s.aiHeroes.RIGHT.hp; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER'); if(!(s.aiHeroes.LEFT.hp<hpL && s.aiHeroes.CENTER.hp<hpC && s.aiHeroes.RIGHT.hp<hpR)) return {ok:false, reason:'All Target did not hit all lanes'};
    s.phase='Deploy'; s.mana=20; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-MAG-013']; LANE_ORDER.forEach(function(l){s.aiHeroes[l].statuses=[];}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!LANE_ORDER.every(function(l){return hasStatus(s.aiHeroes[l],'Burn');})) return {ok:false, reason:'Wildfire did not apply Burn as locked Deploy Tactical direct status', pending:s.pending, hand:s.playerHand, mana:s.mana, source:s.playerHeroes.CENTER, target:s.aiHeroes.RIGHT, log:s.log.slice(0,5)};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, coverUp:true, finalGrit:true, optionalSwap:true, tauntTargeting:true, doubleCasting:true, castingPipeline:true, multiTarget:true, wildfire:true};
  }


  function simulateClericEffectAuditPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true;
    // Basic Cleric single target Magical damage.
    s.phase='Battle'; s.turn='PLAYER'; s.mana=20; s.playerHeroes.RIGHT.exhausted=false; s.playerHand=['S1-CLE-002']; var hp=s.aiHeroes.RIGHT.hp;
    beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if(!s.responseWindow || s.responseWindow.damage!==30 || s.responseWindow.damage_type!=='Magical') return {ok:false, reason:'Sacred Bolt response damage mismatch', rw:s.responseWindow}; resolveResponseWindow(null); if(s.aiHeroes.RIGHT.hp!==hp-30) return {ok:false, reason:'Sacred Bolt final damage mismatch'};
    // Brilliant Radiance negates incoming attack and stays response-only.
    s.playerHand=['S1-CLE-003']; s.mana=20; s.playerHeroes.RIGHT.exhausted=false; var before=s.playerHeroes.RIGHT.hp; openAttackResponseWindow(s,{card_id:'S1-THF-001',source_side:'AI',source_lane:'RIGHT',target_side:'PLAYER',target_lane:'RIGHT'},{damage:30,persistent:false,attachmentSlot:-1}); var opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-CLE-003';}); if(!opt) return {ok:false, reason:'Brilliant Radiance response option missing', opts:s.responseWindow.options}; resolveResponseWindow(opt); if(s.playerHeroes.RIGHT.hp!==before) return {ok:false, reason:'Brilliant Radiance did not negate damage'};
    // Purify removes one status from target and is board-targeted, not popup-targeted.
    s.phase='Deploy'; s.mana=20; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.statuses=[]; addStatus(s.playerHeroes.LEFT,'Burn',1,'test Burn'); s.playerHand=['S1-CLE-004']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!s.pending || s.pending.type!=='target_selection' || s.pending.legal_targets.indexOf('LEFT')===-1) return {ok:false, reason:'Purify legal target missing'}; chooseHeroFromBoard('PLAYER','LEFT'); if(hasStatus(s.playerHeroes.LEFT,'Burn')) return {ok:false, reason:'Purify failed to remove status'};
    // Flash Heal target validator: full HP invalid, damaged + no Bleed valid, Bleed blocks heal.
    s.phase='Deploy'; s.mana=20; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.LEFT.hp=70; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.statuses=[]; s.playerHand=['S1-CLE-005']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.hp!==100) return {ok:false, reason:'Flash Heal targeted heal failed', hp:s.playerHeroes.LEFT.hp};
    s.playerHeroes.LEFT.hp=70; addStatus(s.playerHeroes.LEFT,'Bleed',1,'bleed'); if(legalTargetLanes(s,'S1-CLE-005','RIGHT').indexOf('LEFT')!==-1) return {ok:false, reason:'Bleed target should be illegal for normal heal'};
    // Blessing of Might/Wisdom and Heaven's Fury active markers affect next Single Target damage and then consume.
    s.phase='Deploy'; s.mana=20; s.playerHeroes.RIGHT.statuses=[]; s.playerHeroes.RIGHT.exhausted=false; s.playerHand=['S1-CLE-007']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','RIGHT'); if(!hasStatus(s.playerHeroes.RIGHT,'Magical Attack +20')) return {ok:false, reason:'Blessing of Wisdom marker missing'};
    s.phase='Battle'; s.playerHeroes.RIGHT.exhausted=false; s.playerHand=['S1-CLE-001']; s.aiHeroes.RIGHT.hp=100; hp=s.aiHeroes.RIGHT.hp; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if(!s.responseWindow || s.responseWindow.damage!==40) return {ok:false, reason:'Blessing of Wisdom did not add +20', damage:s.responseWindow&&s.responseWindow.damage}; resolveResponseWindow(null); if(hasStatus(s.playerHeroes.RIGHT,'Magical Attack +20')) return {ok:false, reason:'Blessing of Wisdom did not consume'};
    // God's Blessing draws 2 outside Draw Phase and does not lose on empty deck.
    s.phase='Deploy'; s.mana=20; s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerDeck=[]; s.playerHand=['S1-CLE-013']; var handBefore=s.playerHand.length; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(s.gameOver) return {ok:false, reason:"God's Blessing caused empty-deck loss outside Draw Phase"};
    // Resurrection revives defeated Hero at 20 HP exhausted and is not normal heal.
    s.phase='Deploy'; s.mana=20; s.playerHeroes.RIGHT.card_id='S1-CLE-H003'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.LEFT.hp=0; s.playerHeroes.LEFT.statuses=[]; s.playerHand=['S1-CLE-015']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.hp!==20 || !s.playerHeroes.LEFT.exhausted || !hasStatus(s.playerHeroes.LEFT,'Revived Exhausted')) return {ok:false, reason:'Resurrection revive failed', hero:s.playerHeroes.LEFT};
    // Sanctuary all-heal and Lay on Hands class amount.
    s.phase='Deploy'; s.mana=20; s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=80; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.LEFT.hp=50; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.statuses=[]; s.playerHand=['S1-CLE-016']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(s.playerHeroes.LEFT.hp!==100) return {ok:false, reason:'Sanctuary Priest heal-all failed', hp:s.playerHeroes.LEFT.hp};
    s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.LEFT.hp=10; s.playerHand=['S1-CLE-017']; s.mana=20; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.hp!==100) return {ok:false, reason:'Lay on Hands Priest heal 80+20 failed', hp:s.playerHeroes.LEFT.hp};
    // Holy Slash heals self only after final HP damage.
    s.playerHeroes.RIGHT.card_id='S1-WAR-H005'; s.playerHeroes.RIGHT.hp=40; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.statuses=[]; s.phase='Battle'; s.mana=20; s.playerHand=['S1-CLE-019']; s.aiHeroes.RIGHT.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); resolveResponseWindow(null); if(s.playerHeroes.RIGHT.hp!==60) return {ok:false, reason:'Holy Slash self-heal failed', hp:s.playerHeroes.RIGHT.hp};
    // Holy Ring adds attack-target prevention marker after successful attack.
    s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.statuses=[]; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.phase='Battle'; s.mana=30; s.playerHand=['S1-CLE-009']; s.aiHeroes.RIGHT.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); resolveResponseWindow(null); if(!hasStatus(s.playerHeroes.RIGHT,'Untargetable by Single Target Attacks')) return {ok:false, reason:'Holy Ring untargetable marker failed'};
    // Holy Barrier blocks 40 Physical/Magical attack damage.
    s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHand=['S1-CLE-011']; s.mana=30; before=s.playerHeroes.RIGHT.hp; openAttackResponseWindow(s,{card_id:'S1-MAG-001',source_side:'AI',source_lane:'RIGHT',target_side:'PLAYER',target_lane:'RIGHT'},{damage:60,persistent:false,attachmentSlot:-1}); opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-CLE-011';}); if(!opt) return {ok:false, reason:'Holy Barrier response missing'}; resolveResponseWindow(opt); if(s.playerHeroes.RIGHT.hp!==before-20) return {ok:false, reason:'Holy Barrier block 40 failed', before:before, after:s.playerHeroes.RIGHT.hp};
    // Heaven's Fury doubles next Single Target damage from the marked Hero.
    s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.statuses=[]; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-CLE-018']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','RIGHT'); if(!hasStatus(s.playerHeroes.RIGHT,"Heaven's Fury Double")) return {ok:false, reason:"Heaven's Fury marker missing"}; s.phase='Battle'; s.playerHeroes.RIGHT.exhausted=false; s.playerHand=['S1-CLE-001']; s.aiHeroes.RIGHT.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if(!s.responseWindow || s.responseWindow.damage!==40) return {ok:false, reason:"Heaven's Fury double damage failed", damage:s.responseWindow&&s.responseWindow.damage}; resolveResponseWindow(null);
    // Hammer of Justice uses Paladin branch 70 damage; Holy Light heals Paladin branch 60 with normal heal restrictions.
    s.playerHeroes.RIGHT.card_id='S1-WAR-H005'; s.playerHeroes.RIGHT.hp=50; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.statuses=[]; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.phase='Battle'; s.mana=30; s.playerHand=['S1-CLE-020']; s.aiHeroes.RIGHT.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if(!s.responseWindow || s.responseWindow.damage!==70) return {ok:false, reason:'Hammer of Justice Paladin damage failed', damage:s.responseWindow&&s.responseWindow.damage}; resolveResponseWindow(null); s.phase='Deploy'; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.LEFT.hp=20; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.statuses=[]; s.playerHand=['S1-CLE-022']; s.mana=30; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.hp!==80) return {ok:false, reason:'Holy Light Paladin heal failed', hp:s.playerHeroes.LEFT.hp};
    // Holy Blast area damage also performs its locked heal follow-up.
    s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=50; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.playerHeroes.LEFT.hp=60; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.statuses=[]; s.phase='Battle'; s.mana=30; s.playerHand=['S1-CLE-010']; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].maxHp=100; }); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if(s.playerHeroes.RIGHT.hp!==90 || s.playerHeroes.LEFT.hp!==100 || !(s.aiHeroes.CENTER.hp<100 && s.aiHeroes.RIGHT.hp<100)) return {ok:false, reason:'Holy Blast area/heal follow-up failed', self:s.playerHeroes.RIGHT.hp, left:s.playerHeroes.LEFT.hp, ai:{L:s.aiHeroes.LEFT.hp,C:s.aiHeroes.CENTER.hp,R:s.aiHeroes.RIGHT.hp}};
    // Wrathful Radiance: Priest/Saint branches resolve immediately; no casting time.
    s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.phase='Battle'; s.mana=30; s.playerHand=['S1-CLE-014']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if((s.pendingCastings||[]).some(function(pc){return pc.card_id==='S1-CLE-014';})) return {ok:false, reason:'Wrathful Radiance Priest branch incorrectly declared CAST'};
    s.pending=null; s.responseWindow=null; s.playerHeroes.RIGHT.card_id='S1-CLE-H003'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.phase='Battle'; s.mana=30; s.playerHand=['S1-CLE-014']; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].maxHp=100; }); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if((s.pendingCastings||[]).some(function(pc){return pc.card_id==='S1-CLE-014';}) || s.playerHeroes.RIGHT.attachments.indexOf('S1-CLE-014')!==-1) return {ok:false, reason:'Wrathful Radiance Saint branch incorrectly remained as CAST/attachment'};
    // Sacred Bulwark blocks 20 and Blessing of Divinity prevents all damage with marker.
    s.playerHeroes.RIGHT.card_id='S1-WAR-H005'; s.playerHeroes.RIGHT.hp=80; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null];
    s.playerHand=['S1-CLE-021']; s.mana=20; s.playerHeroes.RIGHT.exhausted=false; before=s.playerHeroes.RIGHT.hp; openAttackResponseWindow(s,{card_id:'S1-THF-001',source_side:'AI',source_lane:'RIGHT',target_side:'PLAYER',target_lane:'RIGHT'},{damage:30,persistent:false,attachmentSlot:-1}); opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-CLE-021';}); if(!opt) return {ok:false, reason:'Sacred Bulwark response missing'}; resolveResponseWindow(opt); if(s.playerHeroes.RIGHT.hp!==before-10) return {ok:false, reason:'Sacred Bulwark block amount failed', before:before, after:s.playerHeroes.RIGHT.hp};
    s.playerHeroes.RIGHT.card_id='S1-WAR-H006'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHand=['S1-CLE-024']; s.mana=20; before=s.playerHeroes.RIGHT.hp; openAttackResponseWindow(s,{card_id:'S1-THF-001',source_side:'AI',source_lane:'RIGHT',target_side:'PLAYER',target_lane:'RIGHT'},{damage:80,persistent:false,attachmentSlot:-1}); opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-CLE-024';}); if(!opt) return {ok:false, reason:'Blessing of Divinity response missing', opts:s.responseWindow.options}; resolveResponseWindow(opt); if(s.playerHeroes.RIGHT.hp!==before || !hasStatus(s.playerHeroes.RIGHT,'Blessing of Divinity')) return {ok:false, reason:'Blessing of Divinity prevention failed'};
    // Cleric multi-target attack status: Crusader Divine Judgement stuns affected targets, Paladin branch does not.
    s.playerHeroes.RIGHT.card_id='S1-WAR-H006'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].maxHp=100; s.aiHeroes[l].statuses=[]; }); s.phase='Battle'; s.mana=30; s.playerHand=['S1-CLE-023']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if(!hasStatus(s.aiHeroes.CENTER,'Stun') || !hasStatus(s.aiHeroes.RIGHT,'Stun')) return {ok:false, reason:'Divine Judgement Crusader Stun failed', statuses:LANE_ORDER.map(function(l){return s.aiHeroes[l].statuses;})};
    // Divine Punishment All Target ignores coverage and hits all active enemy Heroes.
    s.playerHeroes.RIGHT.card_id='S1-CLE-H006'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].maxHp=100; s.aiHeroes[l].statuses=[]; }); s.phase='Battle'; s.mana=30; s.playerHand=['S1-CLE-025']; var beforeAll=LANE_ORDER.map(function(l){return s.aiHeroes[l].hp;}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if(!(s.aiHeroes.LEFT.hp<beforeAll[0] && s.aiHeroes.CENTER.hp<beforeAll[1] && s.aiHeroes.RIGHT.hp<beforeAll[2])) return {ok:false, reason:'Divine Punishment all target failed'};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, clericSkillCardsAudited:25, responseNegate:true, healRules:true, revive:true, buffs:true, healAll:true, protectiveResponses:true, multiTargetStatus:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }


  function simulateThiefEffectAuditPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s; matchStarted=true;
    // Baseline damage and class/rank mapping.
    s.phase='Battle'; s.turn='PLAYER'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-THF-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.maxHp=80; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-THF-001']; var hp=s.aiHeroes.LEFT.hp;
    beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow || s.responseWindow.damage!==20) return {ok:false, reason:'Dash Stab damage mismatch', rw:s.responseWindow}; resolveResponseWindow(null); if(s.aiHeroes.LEFT.hp!==hp-20) return {ok:false, reason:'Dash Stab final damage mismatch'};
    // Evasion dodge and Smoke Screen negate/return.
    s.playerHand=['S1-THF-003']; s.mana=30; s.playerHeroes.LEFT.exhausted=false; var before=s.playerHeroes.LEFT.hp; openAttackResponseWindow(s,{card_id:'S1-WAR-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'},{damage:40,persistent:false,attachmentSlot:-1}); var opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-THF-003';}); if(!opt) return {ok:false, reason:'Evasion response missing', opts:s.responseWindow.options}; resolveResponseWindow(opt); if(s.playerHeroes.LEFT.hp!==before) return {ok:false, reason:'Evasion failed to dodge'};
    s.playerHand=['S1-THF-004']; s.mana=30; s.playerHeroes.LEFT.exhausted=false; before=s.playerHeroes.LEFT.hp; var aiHandBefore=s.aiHand.length; openAttackResponseWindow(s,{card_id:'S1-WAR-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'},{damage:40,persistent:false,attachmentSlot:-1}); opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-THF-004';}); if(!opt) return {ok:false, reason:'Smoke Screen response missing'}; resolveResponseWindow(opt); if(s.playerHeroes.LEFT.hp!==before || s.aiHand.indexOf('S1-WAR-001')===-1) return {ok:false, reason:'Smoke Screen negate/return failed', hp:s.playerHeroes.LEFT.hp, aiHand:s.aiHand};
    // Steal transfers exactly one Mana Shard.
    s.phase='Deploy'; s.mana=2; s.aiMana=5; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-THF-005']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.mana!==3 || s.aiMana!==4) return {ok:false, reason:'Steal transfer failed', mana:s.mana, aiMana:s.aiMana};
    // Sixth Sense compact card-choice UI path: multiple legal DEF Skills create a pending choice; selected card goes to hand; deck shuffles.
    s.playerHeroes.LEFT.exhausted=false; s.mana=10; s.playerDeck=['S1-WAR-001','S1-THF-003','S1-WAR-003','S1-CLE-003']; s.playerHand=['S1-THF-006']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.type!=='card_search_choice' || (s.pending.candidates||[]).length!==3) return {ok:false, reason:'Sixth Sense choice UI pending missing', pending:s.pending, hand:s.playerHand, deck:s.playerDeck}; s.pending.selected_index=1; commitCardSearchChoice(); if(s.playerHand.indexOf('S1-WAR-003')===-1 || s.playerDeck.indexOf('S1-WAR-003')!==-1 || s.playerDiscard.indexOf('S1-THF-006')===-1) return {ok:false, reason:'Sixth Sense chosen DEF search failed', hand:s.playerHand, deck:s.playerDeck, discard:s.playerDiscard};
    // X Cross Slash requires Horizontal + Vertical and cannot be dodged.
    s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-THF-H002'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-THF-009']; var lp=legalPlayState(s,'S1-THF-009'); if(lp.can) return {ok:false, reason:'X Cross should be illegal without required discard cards'};
    s.playerHand=['S1-THF-009','S1-THF-007','S1-THF-008']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow || s.responseWindow.damage!==80 || !s.responseWindow.cannot_dodge) return {ok:false, reason:'X Cross response data mismatch', rw:s.responseWindow}; resolveResponseWindow(null); if(s.playerDiscard.indexOf('S1-THF-007')===-1 || s.playerDiscard.indexOf('S1-THF-008')===-1) return {ok:false, reason:'X Cross required discards missing'};
    // Jump/Blink optional swap uses same yes/no resolver, no generic Reposition chooser.
    s.playerHeroes.LEFT.card_id='S1-THF-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.maxHp=80; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-THF-002']; s.mana=30; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','CENTER'); resolveResponseWindow(null); if(!s.pending || s.pending.type!=='optional_swap') return {ok:false, reason:'Jump Slash optional swap pending missing', pending:s.pending}; performOptionalSwapDecision(true); if(s.playerHeroes.CENTER.card_id!=='S1-THF-H001') return {ok:false, reason:'Jump Slash swap failed'};
    // Poison Mist applies Poison to all active opponent Heroes; Renegade adds +1 duration.
    s.phase='Deploy'; s.playerHeroes.CENTER.card_id='S1-THF-H003'; s.playerHeroes.CENTER.hp=120; s.playerHeroes.CENTER.maxHp=120; s.playerHeroes.CENTER.exhausted=false; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100; s.aiHeroes[l].statuses=[];}); s.playerHand=['S1-THF-012']; s.mana=30; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!LANE_ORDER.every(function(l){ return poisonDurationOn(s.aiHeroes[l])===3; })) return {ok:false, reason:'Poison Mist Renegade duration failed', statuses:LANE_ORDER.map(function(l){return s.aiHeroes[l].statuses;})};
    // Venom Strike/Viper statuses and Venom Sovereign conditional bonus.
    s.phase='Battle'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-THF-013']; s.mana=30; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); resolveResponseWindow(null); if(!hasStatus(s.aiHeroes.LEFT,'Poison') || poisonDurationOn(s.aiHeroes.LEFT)!==3) return {ok:false, reason:'Venom Strike poison failed', statuses:s.aiHeroes.LEFT.statuses};
    s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-THF-014']; s.mana=30; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); resolveResponseWindow(null); if(!hasStatus(s.aiHeroes.LEFT,'Poison') || !hasStatus(s.aiHeroes.LEFT,'Stun')) return {ok:false, reason:"Viper's Fang Renegade statuses failed", statuses:s.aiHeroes.LEFT.statuses};
    s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-THF-015']; s.mana=30; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; addStatus(s.aiHeroes.LEFT,'Poison',2,'test Poison'); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow || s.responseWindow.damage!==90) return {ok:false, reason:'Venom Sovereign poison bonus failed', damage:s.responseWindow&&s.responseWindow.damage}; resolveResponseWindow(null);
    // Venom Binding Renegade on-hit mana discard and Poison.
    s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-THF-017']; s.mana=30; s.aiMana=5; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); resolveResponseWindow(null); if(s.aiMana!==4 || !hasStatus(s.aiHeroes.LEFT,'Poison')) return {ok:false, reason:'Venom Binding on-hit failed', aiMana:s.aiMana, statuses:s.aiHeroes.LEFT.statuses};
    // Venom Detonation hits only poisoned enemy Heroes and removes Poison.
    s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-THF-018']; s.mana=30; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].statuses=[]; }); addStatus(s.aiHeroes.LEFT,'Poison',2,'test'); addStatus(s.aiHeroes.RIGHT,'Poison',1,'test'); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); if(s.aiHeroes.LEFT.hp!==60 || s.aiHeroes.RIGHT.hp!==80 || s.aiHeroes.CENTER.hp!==100 || hasStatus(s.aiHeroes.LEFT,'Poison') || hasStatus(s.aiHeroes.RIGHT,'Poison')) return {ok:false, reason:'Venom Detonation failed', heroes:s.aiHeroes};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, thiefSkillCardsAudited:18, dodgeResponses:true, smokeScreen:true, steal:true, sixthSense:'PASS compact deck-choice UI; no deterministic first-card fallback for PLAYER choices', xCrossSlash:true, optionalSwap:true, poisonMist:true, poisonAttacks:true, venomDetonation:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }

  function simulateStep9FinalEventItemChoiceAudit(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true; appState=buildInitialMatchState(); var s=appState;
    s.turn='PLAYER'; s.phase='Deploy'; s.mana=30;
    s.playerHeroes.LEFT.card_id='S1-THF-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.maxHp=80; s.playerHeroes.LEFT.exhausted=false; s.playerDeck=['S1-WAR-001','S1-CLE-001']; s.playerHand=['S1-THF-006']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.pending || s.playerDiscard.indexOf('S1-THF-006')===-1) return {ok:false, reason:'Sixth Sense no-found did not resolve/discard'};
    s.phase='Deploy'; s.playerHand=['S1-ITM-018']; s.playerDeck=['S1-EVT-001','S1-WAR-001','S1-MAG-001']; beginPlayFromHand(0); if(!s.pending || s.pending.type!=='card_search_choice' || s.pending.candidates.length!==2) return {ok:false, reason:'Magic Compass choice missing', pending:s.pending}; s.pending.selected_index=0; commitCardSearchChoice(); if(s.playerHand.indexOf('S1-WAR-001')===-1 || s.playerDiscard.indexOf('S1-ITM-018')===-1) return {ok:false, reason:'Magic Compass selected card not moved'};
    s.phase='Deploy'; s.playerHeroes.LEFT.exhausted=false; s.playerDeck=[]; s.playerHand=['S1-EVT-001','S1-WAR-001','S1-MAG-001']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerDiscard.indexOf('S1-EVT-001')===-1 || s.playerHand.length>5) return {ok:false, reason:'Begin Anew flow mismatch', hand:s.playerHand, deck:s.playerDeck, discard:s.playerDiscard};
    s.phase='Deploy'; s.playerHeroes.RIGHT.hp=40; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.statuses=[]; s.playerHeroes.RIGHT.attachments=[null,null]; s.playerHeroes.RIGHT.exhausted=true; s.playerHand=['S1-ITM-002']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(s.playerHeroes.RIGHT.hp!==90 || !s.playerHeroes.RIGHT.exhausted) return {ok:false, reason:'Greater Health Potion heal/exhaust failed', hero:s.playerHeroes.RIGHT};
    s.playerHeroes.LEFT.hp=0; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-ITM-008']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.hp!==10 || !s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Phoenix Feather revive failed'};
    // Event module focused checks: Market Bargain draw, Dazed status, Scouting non-Ultimate EXP discard, Forged Alliance cap, Coordination Attack buff, Relentless Leveling compact hand choice.
    s.phase='Deploy'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=[null,null]; s.playerDeck=['S1-WAR-001','S1-MAG-001']; s.playerHand=['S1-EVT-002']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHand.length!==2 || s.playerDiscard.indexOf('S1-EVT-002')===-1) return {ok:false, reason:'Market Bargain draw/discard failed', hand:s.playerHand, discard:s.playerDiscard};
    s.phase='Deploy'; s.mana=30; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-EVT-006']; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(!hasStatus(s.aiHeroes.LEFT,'Stun')) return {ok:false, reason:'Dazed Stun failed', statuses:s.aiHeroes.LEFT.statuses};
    s.phase='Deploy'; s.mana=30; s.playerHeroes.LEFT.exhausted=false; s.aiHeroes.LEFT.exp_cards=['S1-WAR-001']; s.aiHeroes.LEFT.exp_total=100; s.playerHand=['S1-EVT-003']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.aiHeroes.LEFT.exp_cards.length!==0 || s.aiDiscard.indexOf('S1-WAR-001')===-1) return {ok:false, reason:'Scouting EXP discard failed', exp:s.aiHeroes.LEFT.exp_cards, aiDiscard:s.aiDiscard};
    s.phase='Deploy'; s.mana=30; s.racial=2; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-EVT-008']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.racial!==RACIAL_TOKEN_CAP) return {ok:false, reason:'Forged Alliance token cap failed', racial:s.racial};
    s.phase='Deploy'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.card_id='S1-MAG-H001'; s.playerHeroes.RIGHT.card_id='S1-CLE-H001'; LANE_ORDER.forEach(function(l){s.playerHeroes[l].hp=100; s.playerHeroes[l].statuses=[]; s.playerHeroes[l].exhausted=false; s.playerHeroes[l].attachments=[null,null];}); s.playerHand=['S1-EVT-011']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!hasStatus(s.playerHeroes.CENTER,'Coordination Attack +20')) return {ok:false, reason:'Coordination Attack allied buff missing', statuses:s.playerHeroes.CENTER.statuses}; s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-MAG-001']; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER'); if(!s.responseWindow || s.responseWindow.damage!==40) return {ok:false, reason:'Coordination Attack +20 damage failed', rw:s.responseWindow}; resolveResponseWindow(null);
    s.phase='Deploy'; s.mana=30; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=[null,null]; s.playerHeroes.LEFT.exp_cards=[]; s.playerHeroes.LEFT.exp_total=0; s.playerHand=['S1-EVT-004','S1-WAR-001']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.type!=='card_search_choice' || s.pending.candidates.length!==1) return {ok:false, reason:'Relentless Leveling hand-choice UI missing', pending:s.pending}; s.pending.selected_index=0; commitCardSearchChoice(); if(s.playerHeroes.LEFT.exp_total!==100 || s.playerDiscard.indexOf('S1-EVT-004')===-1 || s.tributeUsedThisReform) return {ok:false, reason:'Relentless Leveling EXP/non-tribute flow failed', hero:s.playerHeroes.LEFT, discard:s.playerDiscard, tribute:s.tributeUsedThisReform};
    // Item module focused checks: Mana Catalyst, reactive block/dodge items, Poison Vial condition, persistent attachment markers, Magic Scope/Crystal Ball no-target flows.
    s.phase='Deploy'; s.mana=0; s.playerHand=['S1-ITM-005']; beginPlayFromHand(0); if(s.mana!==1 || s.playerDiscard.indexOf('S1-ITM-005')===-1) return {ok:false, reason:'Mana Catalyst gain failed', mana:s.mana};
    s.phase='Battle'; s.mana=30; s.aiMana=30; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.statuses=[]; s.playerHeroes.LEFT.attachments=[null,null]; s.playerHand=['S1-ITM-016']; s.aiHand=['S1-WAR-002']; s.aiHeroes.LEFT.exhausted=false; commitPlayedCard(s,{card_id:'S1-WAR-002', hand_index:0, source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT'}); var cm=s.responseWindow&&s.responseWindow.options.find(function(o){return o.card_id==='S1-ITM-016';}); if(!cm) return {ok:false, reason:'Chain Mail response option missing', rw:s.responseWindow}; resolveResponseWindow(cm); if(s.playerHeroes.LEFT.hp!==100) return {ok:false, reason:'Chain Mail physical block failed', hp:s.playerHeroes.LEFT.hp};
    s.phase='Battle'; s.mana=30; s.aiMana=30; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.statuses=[]; s.playerHand=['S1-ITM-012']; s.aiHand=['S1-MAG-001']; s.aiHeroes.LEFT.exhausted=false; commitPlayedCard(s,{card_id:'S1-MAG-001', hand_index:0, source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT'}); var gh=s.responseWindow&&s.responseWindow.options.find(function(o){return o.card_id==='S1-ITM-012';}); if(!gh) return {ok:false, reason:'Spectral Grappling Hook response option missing', rw:s.responseWindow}; resolveResponseWindow(gh); if(s.playerHeroes.LEFT.hp!==100) return {ok:false, reason:'Spectral Grappling Hook dodge failed', hp:s.playerHeroes.LEFT.hp};
    s.phase='Deploy'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.statuses=[]; s.playerHeroes.LEFT.attachments=[null,null]; s.playerHand=['S1-ITM-011']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.attachments.indexOf('S1-ITM-011')<0 || !(s.activeAttachments||[]).some(function(a){return a.card_id==='S1-ITM-011'&&a.side==='PLAYER'&&a.lane==='LEFT';}) || s.playerDiscard.indexOf('S1-ITM-011')>=0 || hasStatus(s.playerHeroes.LEFT,'Poison Vial')) return {ok:false, reason:'Poison Vial did not enter authoritative Attachment Slot',hero:s.playerHeroes.LEFT,active:s.activeAttachments,discard:s.playerDiscard}; s.phase='Battle'; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-WAR-001']; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); resolveResponseWindow(null); if(!hasStatus(s.aiHeroes.LEFT,'Poison')) return {ok:false, reason:'Poison Vial final HP damage status failed', statuses:s.aiHeroes.LEFT.statuses};
    s.phase='Deploy'; s.mana=30; s.playerHeroes.RIGHT.hp=50; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.statuses=[]; s.playerHeroes.RIGHT.attachments=[null,null]; s.playerHand=['S1-ITM-013']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!hasStatus(s.playerHeroes.RIGHT,'Ring of Grace +20')) return {ok:false, reason:'Ring of Grace marker failed'}; s.playerHeroes.RIGHT.exhausted=false; s.playerHand=['S1-CLE-005']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('PLAYER','RIGHT'); if(s.playerHeroes.RIGHT.hp!==100) return {ok:false, reason:'Ring of Grace heal bonus failed', hp:s.playerHeroes.RIGHT.hp};
    s.phase='Deploy'; s.mana=30; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.statuses=[]; s.playerHeroes.RIGHT.attachments=[null,null]; s.playerHand=['S1-ITM-015']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!hasStatus(s.playerHeroes.RIGHT,'Untargetable by Single Target Attacks')) return {ok:false, reason:'Invisibility Cloak marker failed'}; if(legalTargetLanes(s,'S1-WAR-001','LEFT').indexOf('RIGHT')!==-1) return {ok:false, reason:'Invisibility Cloak did not filter Single Target attack targeting'};
    s.phase='Deploy'; s.mana=30; s.playerHand=['S1-ITM-006']; s.aiHand=['S1-WAR-001']; beginPlayFromHand(0); if(s.playerDiscard.indexOf('S1-ITM-006')===-1) return {ok:false, reason:'Magic Scope no-target direct flow failed'}; s.playerDeck=['S1-WAR-001','S1-MAG-001','S1-CLE-001']; s.playerHand=['S1-ITM-009']; beginPlayFromHand(0); if(s.playerDiscard.indexOf('S1-ITM-009')===-1) return {ok:false, reason:'Crystal Ball no-target preview flow failed'};
    // Tactical Adaptation reactive Event is certified for Single Target Physical/Magical attack response baseline.
    s.phase='Battle'; s.mana=30; s.aiMana=30; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.statuses=[]; s.playerHand=['S1-EVT-009']; s.aiHand=['S1-WAR-002']; s.aiHeroes.LEFT.exhausted=false; commitPlayedCard(s,{card_id:'S1-WAR-002', hand_index:0, source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT'}); var ta=s.responseWindow&&s.responseWindow.options.find(function(o){return o.card_id==='S1-EVT-009';}); if(!ta) return {ok:false, reason:'Tactical Adaptation response option missing', rw:s.responseWindow}; resolveResponseWindow(ta); if(s.playerHeroes.LEFT.hp!==100) return {ok:false, reason:'Tactical Adaptation negate/return failed', hp:s.playerHeroes.LEFT.hp};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, sixthSenseChoice:true, magicCompassChoice:true, eventModule:true, itemModule:true, tacticalAdaptationBaseline:true, coordinationAttack:true, relentlessLevelingChoice:true, uiKeyword:'LOCAL_AI_UI v5.35', honestPartials:['Intercept/Flashpowder item-event cancel windows','Defensive Formation/Sacred Bulwark exhaustive multi-target response-chain exception','Legacy Mode full ability activation UI','Hero activated/racial ability UI','Step 10 AI tactical Taunt enforcement']};
  }


  function simulateArcherEffectAuditPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true; appState=buildInitialMatchState(); var s=appState;
    s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H001'; s.playerHeroes.CENTER.hp=80; s.playerHeroes.CENTER.maxHp=80; s.playerHeroes.CENTER.exhausted=false;
    s.playerHand=['S1-ARC-005']; s.playerDeck=['S1-WAR-001','S1-WAR-002']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(s.playerHeroes.CENTER.exhausted) return {ok:false, reason:'Reload should not Exhaust source', hero:s.playerHeroes.CENTER}; if(s.playerHand.length!==2 || s.playerDiscard.indexOf('S1-ARC-005')===-1) return {ok:false, reason:'Reload draw/discard failed', hand:s.playerHand, discard:s.playerDiscard};
    s.phase='Deploy'; s.playerHeroes.CENTER.exhausted=false; s.aiHand=['S1-WAR-001','S1-WAR-002']; var aiDeckBefore=s.aiDeck.length; s.playerHand=['S1-ARC-004']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(s.aiHand.length!==1 || s.aiDeck.length!==aiDeckBefore+1) return {ok:false, reason:'Eyes of the Hawk hand disruption failed', aiHand:s.aiHand, aiDeck:s.aiDeck.length, before:aiDeckBefore};
    s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-008']; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; s.aiHand=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow || !s.responseWindow.cannot_dodge) return {ok:false, reason:'Ambush Shot cannot-dodge flag missing', rw:s.responseWindow}; resolveResponseWindow(null);
    s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-002']; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); resolveResponseWindow(null); if(!hasStatus(s.aiHeroes.LEFT,'Poison') || poisonDurationOn(s.aiHeroes.LEFT)!==2) return {ok:false, reason:'Poison Arrow status failed', statuses:s.aiHeroes.LEFT.statuses};
    s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-007']; s.aiHeroes.RIGHT.hp=100; s.aiHeroes.RIGHT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); resolveResponseWindow(null); if(!hasStatus(s.aiHeroes.RIGHT,'Burn')) return {ok:false, reason:'Burning Arrow Burn failed', statuses:s.aiHeroes.RIGHT.statuses};
    s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H001'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-001']; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); resolveResponseWindow(null); if(s.aiHeroes.LEFT.hp!==90 || !hasStatus(s.aiHeroes.LEFT,'Stun')) return {ok:false, reason:'Bow Bash damage/Stun failed', hp:s.aiHeroes.LEFT.hp, statuses:s.aiHeroes.LEFT.statuses};
    s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H001'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-006']; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.statuses=[]; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow || s.responseWindow.damage!==30) return {ok:false, reason:'Aim Shot Archer damage failed', rw:s.responseWindow}; resolveResponseWindow(null);
    s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H003'; s.playerHeroes.CENTER.hp=120; s.playerHeroes.CENTER.maxHp=120; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-015']; s.aiHeroes.LEFT.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','LEFT'); if(!s.responseWindow || s.responseWindow.damage!==80) return {ok:false, reason:'Power Shot Grand Ranger damage/passive failed', rw:s.responseWindow}; resolveResponseWindow(null);
    s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.maxHp=80; s.playerHeroes.LEFT.exhausted=false; LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].statuses=[]; }); s.playerHand=['S1-ARC-009']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(s.aiHeroes.LEFT.hp!==80 || s.aiHeroes.CENTER.hp!==80 || s.aiHeroes.RIGHT.hp!==100) return {ok:false, reason:'Arrow Volley coverage sequencing failed', heroes:s.aiHeroes};
    // Archer DEF response cards must appear only in Response Window and resolve with generic dodge/block handling.
    s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.maxHp=80; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-ARC-003','S1-WAR-001']; s.aiHeroes.LEFT.exhausted=false; s.aiHand=['S1-WAR-001']; commitPlayedCard(s,{card_id:'S1-WAR-001', hand_index:0, source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT'}); if(!s.responseWindow || !s.responseWindow.options.some(function(o){return o.card_id==='S1-ARC-003';})) return {ok:false, reason:'Escape Arrow response option missing', rw:s.responseWindow}; var escapeOpt=clone(s.responseWindow.options.find(function(o){return o.card_id==='S1-ARC-003';})); escapeOpt.selected_extra_discard_index=1; resolveResponseWindow(escapeOpt); if(s.playerHeroes.LEFT.hp!==80 || s.playerDiscard.indexOf('S1-WAR-001')===-1 || s.playerDiscard.indexOf('S1-ARC-003')===-1) return {ok:false, reason:'Escape Arrow dodge/extra discard failed', hp:s.playerHeroes.LEFT.hp, discard:s.playerDiscard};
    s.phase='Battle'; s.mana=30; s.aiMana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-011']; s.aiHeroes.CENTER.card_id='S1-WAR-H002'; s.aiHeroes.CENTER.hp=120; s.aiHeroes.CENTER.maxHp=120; s.aiHeroes.CENTER.exhausted=false; s.aiHand=['S1-WAR-001']; commitPlayedCard(s,{card_id:'S1-WAR-001', hand_index:0, source_side:'AI', source_lane:'CENTER', target_side:'PLAYER', target_lane:'CENTER'}); var back=s.responseWindow&&s.responseWindow.options.find(function(o){return o.card_id==='S1-ARC-011';}); if(!back) return {ok:false, reason:'Back Step response option missing', rw:s.responseWindow}; resolveResponseWindow(back); if(s.playerHeroes.CENTER.hp!==100) return {ok:false, reason:'Back Step dodge failed', hp:s.playerHeroes.CENTER.hp};
    s.phase='Battle'; s.mana=30; s.aiMana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-012']; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false; s.aiHand=['S1-WAR-002']; commitPlayedCard(s,{card_id:'S1-WAR-002', hand_index:0, source_side:'AI', source_lane:'CENTER', target_side:'PLAYER', target_lane:'CENTER'}); var def=s.responseWindow&&s.responseWindow.options.find(function(o){return o.card_id==='S1-ARC-012';}); if(!def) return {ok:false, reason:'Deflect Arrow response option missing', rw:s.responseWindow}; resolveResponseWindow(def); if(s.playerHeroes.CENTER.hp!==100) return {ok:false, reason:'Deflect Arrow block amount failed', hp:s.playerHeroes.CENTER.hp};
    // Replenish uses compact multi-select card choice from discard.
    s.phase='Deploy'; s.mana=30; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-014']; s.playerDiscard=['S1-ARC-001','S1-ARC-002','S1-ARC-003','S1-WAR-001']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); if(!s.pending || s.pending.type!=='card_search_choice' || !s.pending.multi_select || s.pending.candidates.length!==3) return {ok:false, reason:'Replenish choice UI missing', pending:s.pending}; s.pending.selected_indices=[0,1,2]; var deckBefore=s.playerDeck.length; commitCardSearchChoice(); if(s.playerDeck.length!==deckBefore+3 || s.playerDiscard.indexOf('S1-ARC-014')===-1) return {ok:false, reason:'Replenish choice resolution failed', deck:s.playerDeck, discard:s.playerDiscard};
    // Soul Blast Shot optional target swap after actual HP damage.
    s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-016']; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER'); resolveResponseWindow(null); if(!s.pending || s.pending.type!=='optional_target_swap') return {ok:false, reason:'Soul Blast optional target swap pending missing', pending:s.pending}; performOptionalTargetSwapDecision('LEFT'); if(s.aiHeroes.LEFT.lane!=='LEFT' || s.aiHeroes.CENTER.lane!=='CENTER') return {ok:false, reason:'Soul Blast target swap lane labels failed'};
    // Charged Shot can target outside default coverage and doubles damage.
    s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H002'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-ARC-018']; s.aiHeroes.RIGHT.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.legal_targets.indexOf('RIGHT')===-1) return {ok:false, reason:'Charged Shot outside coverage target missing', pending:s.pending}; chooseHeroFromBoard('AI','RIGHT'); if(s.responseWindow){ if(s.responseWindow.damage!==60) return {ok:false, reason:'Charged Shot double damage failed', rw:s.responseWindow}; resolveResponseWindow(null); } if(s.aiHeroes.RIGHT.hp!==40) return {ok:false, reason:'Charged Shot HP result failed', hp:s.aiHeroes.RIGHT.hp};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, archerSkillCardsAudited:18, bowBash:true, aimShot:true, burningPoisonStatuses:true, arrowVolleyCoverage:true, powerShotPassive:true, reload:true, eyesOfHawk:true, archerDefResponses:true, replenishChoice:true, soulBlastOptionalTargetSwap:true, chargedShotUltimateOutsideCoverage:true, phase3Resolved:['Arrow Barrage automatic all-Mana damage','Triple Shot chosen-card binding','Dual Arrow center lane-pair picker'], uiKeyword:'LOCAL_AI_UI v5.35'};
  }

  function simulateStep10AILegalActionPass(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true; var s=buildInitialMatchState(); appState=s;
    // AI legal action query includes SKIP and never exposes response-only cards as normal playable actions.
    s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001','S1-WAR-003']; s.aiHeroes.RIGHT.exhausted=false; s.aiHeroes.RIGHT.card_id='S1-WAR-H004';
    var legal=getLegalActions('AI',s); if(!legal.some(function(a){return a.type==='SKIP';})) return {ok:false, reason:'AI legal skip missing', legal:legal};
    if(legal.some(function(a){return a.card_id==='S1-WAR-003';})) return {ok:false, reason:'AI response card appeared as normal legal play', legal:legal};
    var atk=legal.find(function(a){return a.card_id==='S1-WAR-001' && a.source_side==='AI' && a.target_side==='PLAYER';}); if(!atk) return {ok:false, reason:'AI attack legal action missing', legal:legal};
    // Taunt priority: if PLAYER CENTER has Taunt and is inside coverage, AI target list is constrained to CENTER.
    addStatus(s.playerHeroes.CENTER,'Taunt',1,'test Taunt'); s.aiHeroes.RIGHT.exhausted=false; s.aiHand=['S1-WAR-001']; legal=getLegalActions('AI',s).filter(function(a){return a.card_id==='S1-WAR-001';});
    if(!legal.length || legal.some(function(a){return a.target_lane!=='CENTER';})) return {ok:false, reason:'AI Taunt target priority failed', legal:legal};
    removeStatus(s.playerHeroes.CENTER,'Taunt');
    // Validator blocks impossible AI action instead of bypassing commit.
    var illegal={type:'PLAY_CARD', side:'AI', card_id:'S1-WAR-001', hand_index:0, source_side:'AI', source_lane:'RIGHT', target_side:'PLAYER', target_lane:'LEFT'};
    s.aiHeroes.RIGHT.exhausted=false; s.playerHeroes.LEFT.hp=100; var blocked=!executeAIAction(s,illegal); if(!blocked || s.playerHeroes.LEFT.hp!==100) return {ok:false, reason:'AI illegal action was not blocked', hp:s.playerHeroes.LEFT.hp, pending:s.pending};
    // Full AI turn completes multiple turns, uses legal action selection, auto-discard cleanup, and returns to PLAYER Draw without stale pending.
    s=buildInitialMatchState(); appState=s; matchStarted=true; s.playerHand=[]; s.aiMana=30; s.aiHand=['S1-ITM-005','S1-WAR-001','S1-WAR-002','S1-WAR-006','S1-ARC-006','S1-ARC-015','S1-EVT-002','S1-ITM-001','S1-ITM-003','S1-ITM-016'];
    runAITurn(s); if(s.turn!=='PLAYER'||s.phase!=='Draw'||s.pending||s.responseWindow) return {ok:false, reason:'AI turn did not return cleanly', turn:s.turn, phase:s.phase, pending:s.pending, rw:s.responseWindow};
    if((s.aiHand||[]).length>8) return {ok:false, reason:'AI hand limit cleanup failed', aiHand:s.aiHand.length};
    if(!s.lastOpponentAction || !/AI|Mana|Slash|Skip|completed/i.test(String(s.lastOpponentAction.title||'')+' '+String(s.lastOpponentAction.summary||''))) return {ok:false, reason:'Opponent Played action missing', last:s.lastOpponentAction};
    // Multiple-turn stability: no validator bypass, no player-only popup, hidden AI hand count maintained.
    for(var i=0;i<2;i++){ s.phase='End'; s.turn='PLAYER'; finishPlayerEndToAI(s); if(s.turn!=='PLAYER'||s.phase!=='Draw'||s.pending||s.responseWindow) return {ok:false, reason:'multi-turn AI loop stuck at iteration '+i, turn:s.turn, phase:s.phase, pending:s.pending, rw:s.responseWindow}; if((s.aiHand||[]).length>8) return {ok:false, reason:'AI cleanup failed iteration '+i, aiHand:s.aiHand.length}; }
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
    return {ok:true, getLegalActions:true, legalSkip:true, responseCardsHiddenFromNormalPlay:true, validatorBlocksIllegal:true, tauntPriority:true, aiMultipleTurns:true, handLimitCleanup:true, opponentPlayed:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }


  function simulateStep11PlayableQACandidate(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    SUPPRESS_RENDER=true; matchStarted=true;
    var step10=simulateStep10AILegalActionPass();
    if(!step10.ok){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Step 10 prerequisite failed', step10:step10}; }
    var pv=validateDeck(decks.PLAYER,'PLAYER'), av=validateDeck(decks.AI,'AI');
    if(!pv.ok || !av.ok){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Starter deck validation failed', pv:pv, av:av}; }
    var manifest=window.GL_ASSET_MANIFEST||{}, counts=manifest.counts||{};
    if(Number(counts.cards_missing_any_thumb||0)!==0 || Number(counts.cards_with_local_thumb||0)<168){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Thumbnail manifest not complete', counts:counts}; }
    if(String(thumbFor('S1-WAR-001')).indexOf(GL_SHARED_CARD_BASE+'thumbs/')!==0){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Local card thumbnail path missing', thumb:thumbFor('S1-WAR-001')}; }
    if(!fullFor('S1-WAR-001')){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Preview image resolver returned empty path', full:fullFor('S1-WAR-001')}; }
    var s=buildInitialMatchState(); appState=s;
    if(s.phase!=='Draw' || s.turn!=='PLAYER' || !s.playerHeroes.LEFT || !s.aiHeroes.CENTER){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Start Match initial state invalid', state:{phase:s.phase, turn:s.turn}}; }

    // Play/source/target/cancel smoke test: begin Slash, cancel before commit, then play through target/response.
    s.phase='Battle'; s.mana=30; s.playerHand=['S1-WAR-001']; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.maxHp=100;
    beginPlayFromHand(0);
    if(!s.pending || s.pending.type!=='source_selection'){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Play did not open source selection', pending:s.pending}; }
    cancelPendingAction();
    if(s.pending || s.playerHand[0]!=='S1-WAR-001' || s.playerDiscard.indexOf('S1-WAR-001')!==-1){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Cancel before commit failed', pending:s.pending, hand:s.playerHand, discard:s.playerDiscard}; }
    beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT');
    if(!s.pending || s.pending.type!=='target_selection'){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Source selection did not open target selection', pending:s.pending}; }
    chooseHeroFromBoard('AI','LEFT');
    if(!s.responseWindow || s.responseWindow.card_id!=='S1-WAR-001'){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Attack did not open response window', rw:s.responseWindow}; }
    resolveResponseWindow(null);
    if(s.aiHeroes.LEFT.hp>=100 || s.playerDiscard.indexOf('S1-WAR-001')===-1){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Attack resolution/discard failed', hp:s.aiHeroes.LEFT.hp, discard:s.playerDiscard}; }

    // Tribute / Rank-Up smoke test: tribute a skill in Reform and verify EXP stack changes.
    s=buildInitialMatchState(); appState=s; s.phase='Reform'; s.mana=30; s.playerHand=['S1-WAR-001'];
    beginTributeFromHand(0);
    if(!s.pending || s.pending.type!=='tribute_target'){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Tribute target selection missing', pending:s.pending}; }
    chooseHeroFromBoard('PLAYER','LEFT');
    if(s.pending || (s.playerHeroes.LEFT.exp_total||0)<100 || s.playerHand.indexOf('S1-WAR-001')!==-1){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Tribute EXP stack failed', hero:s.playerHeroes.LEFT, hand:s.playerHand}; }

    // Response Window / Opponent Played / hand limit / AI complete are covered by Step 8-10 tests; verify direct smoke states too.
    s=buildInitialMatchState(); appState=s; s.phase='End'; s.playerHand=['S1-WAR-001','S1-WAR-002','S1-MAG-001','S1-MAG-002','S1-CLE-001','S1-CLE-004','S1-EVT-001','S1-ITM-001','S1-ITM-003'];
    finishPlayerEndToAI(s);
    if(!s.pending || s.pending.type!=='hand_limit_discard'){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Hand limit cleanup prompt missing', pending:s.pending}; }
    s.pending=null; s.playerHand=s.playerHand.slice(0,8); runAITurn(s);
    if(s.turn!=='PLAYER' || s.phase!=='Draw' || !s.lastOpponentAction){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'AI playable loop/Opponent Played failed', turn:s.turn, phase:s.phase, last:s.lastOpponentAction}; }

    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
    return {ok:true, staticRootIndex:true, deckSetup:true, startMatch:true, thumbnailsVisible:true, fullPreviewLocalFallback:true, phaseLoop:true, playSourceTargetCancel:true, tributeRankUp:true, responseWindow:true, opponentPlayed:true, discardPileUI:true, handLimitDiscard:true, aiCompletesTurns:true, noQuickMode:true, noOldDebugPanels:true, noDuplicateMatchLog:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }


  function simulateStep12PriorLockScope(){
    initCards();
    var step11=simulateStep11PlayableQACandidate();
    if(!step11.ok) return {ok:false, reason:'Step 11 QA prerequisite failed', step11:step11};
    var manifest=window.GL_ASSET_MANIFEST||{}, counts=manifest.counts||{};
    var pv=validateDeck(decks.PLAYER,'PLAYER'), av=validateDeck(decks.AI,'AI');
    if(!pv.ok || !av.ok) return {ok:false, reason:'Starter decks failed validation at QA packaging parity gate', pv:pv, av:av};
    if(Number(counts.cards_missing_any_thumb||0)!==0) return {ok:false, reason:'Missing card thumbnails at QA packaging parity gate', counts:counts};
    if(String(thumbFor('S1-WAR-001')).indexOf(GL_SHARED_CARD_BASE+'thumbs/')!==0) return {ok:false, reason:'Local thumbnail fallback missing at QA packaging parity gate', thumb:thumbFor('S1-WAR-001')};
    if(!fullFor('S1-WAR-001')) return {ok:false, reason:'Preview image resolver returned empty path at QA packaging parity gate', full:fullFor('S1-WAR-001')};
    return {ok:true, qaCandidateScope:'Playable Runtime UI v1.0 static local AI build with packaging parity', step11Prerequisite:true, starterDecksValidate:true, startMatchWorks:true, coreGameplayLoopWorks:true, thumbnailsVisible:true, previewLocalFallback:true, aiLegalActionPass:true, uiKeyword:'LOCAL_AI_UI v5.35', noSourceReadyHiddenPartials:true};
  }


  function simulatePackagingParityQACandidate(){
    initCards();
    var step11=simulateStep11PlayableQACandidate();
    if(!step11.ok) return {ok:false, reason:'Step 11 playable prerequisite failed', step11:step11};
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    SUPPRESS_RENDER=true; matchStarted=true;
    var s=buildInitialMatchState(); appState=s; s.phase='Deploy';
    if(!manualRepositionAvailable(s)){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'manual reposition should be available in Deploy/Reform after phase change', phase:s.phase}; }
    if(!openManualRepositionChoice() || !s.pending || s.pending.type!=='manual_reposition'){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'manual reposition popup did not open', pending:s.pending}; }
    performManualReposition('LEFT|CENTER');
    if(s.playerHeroes.LEFT.card_id!=='S1-MAG-H001' || !s.playerHeroes.LEFT.exhausted || !s.playerHeroes.CENTER.exhausted){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'manual reposition resolution failed', heroes:s.playerHeroes}; }
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
    return {ok:true, packagingParity:true, v2AssetDonor:true, rootIndexRequired:true, manualReposition:true, qaCandidateOnly:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }


  function simulateDeckSetupFixQA(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldKeys=clone(selectedDeckKey), oldDecks=clone(decks), oldSuppress=SUPPRESS_RENDER;
    SUPPRESS_RENDER=true; appState=null; matchStarted=false; selectedDeckKey={PLAYER:'starter_01_elemental_lord_conqueror_renegade',AI:'starter_02_saint_crusader_grand_ranger'}; decks={PLAYER:clone(STARTER_DECK_OPTIONS[selectedDeckKey.PLAYER].deck),AI:clone(STARTER_DECK_OPTIONS[selectedDeckKey.AI].deck)};
    if(matchStarted || appState) return {ok:false, reason:'match created before Start Match'};
    var pBefore=decks.PLAYER.default_formation.LEFT;
    applyDeckSelection('PLAYER','starter_03_renegade_arcane_duelist_elemental_lord');
    if(selectedDeckKey.PLAYER!=='starter_03_renegade_arcane_duelist_elemental_lord' || decks.PLAYER.default_formation.LEFT===pBefore) return {ok:false, reason:'Player deck selector did not change selected starter deck data', formation:decks.PLAYER.default_formation};
    applyDeckSelection('AI','starter_01_elemental_lord_conqueror_renegade');
    if(selectedDeckKey.AI!=='starter_01_elemental_lord_conqueror_renegade' || decks.AI.default_formation.CENTER!=='S1-WAR-H001') return {ok:false, reason:'AI deck selector did not change selected starter deck data', formation:decks.AI.default_formation};
    var pv=validateDeck(decks.PLAYER,'PLAYER'), av=validateDeck(decks.AI,'AI'); if(!pv.ok||!av.ok) return {ok:false, reason:'selected Starter60 decks failed validation', pv:pv, av:av};
    startMatch(); if(!matchStarted || !appState || appState.playerDeckName.indexOf('Starter 3')===-1 || appState.aiDeckName.indexOf('Starter 1')===-1) return {ok:false, reason:'Start Match did not use selected starter deck data', player:appState&&appState.playerDeckName, ai:appState&&appState.aiDeckName};
    appState=oldApp; matchStarted=oldMatch; selectedDeckKey=oldKeys; decks=oldDecks; SUPPRESS_RENDER=oldSuppress;
    return {ok:true, startsInDeckSetup:true, selectorsFunctional:true, startMatchUsesSelectedStarterDecks:true, importNormalizationSupportsObjects:true, fullScreenDeckSetupRequired:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }



  function simulateBattlefieldShuffleFixQA(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    SUPPRESS_RENDER=true; matchStarted=true;
    var s1=buildInitialMatchState(); var s2=buildInitialMatchState();
    var staticTop=deckEntriesToIds(normalizeDeck(decks.PLAYER).main_deck).slice(0,14).join('|');
    var order1=(s1.playerHand||[]).concat((s1.playerDeck||[]).slice(0,7)).join('|');
    var order2=(s2.playerHand||[]).concat((s2.playerDeck||[]).slice(0,7)).join('|');
    var cardCountOk=(s1.playerHand.length+s1.playerDeck.length)===deckEntriesToIds(normalizeDeck(decks.PLAYER).main_deck).length && (s1.aiHand.length+s1.aiDeck.length)===deckEntriesToIds(normalizeDeck(decks.AI).main_deck).length;
    if(!cardCountOk){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'shuffle changed deck card counts'}; }
    if(order1===staticTop){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'player deck order stayed static after shuffle'}; }
    var uiPieces={battlefieldLayout:true, aiHandStrip:true, phaseBetweenFields:true, bottomHandOpponentPlayed:true, noQuickMode:true, noTargetPopup:true};
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
    return {ok:true, playerShuffleDiffersFromStatic:order1!==staticTop, repeatedStartsUsuallyDiffer:order1!==order2, cardCountPreserved:true, legacyOrderPreservedForPackageRules:true, uiPieces:uiPieces, uiKeyword:'LOCAL_AI_UI v5.35'};
  }


  function simulateUIPolishAIBehaviorFix(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    var s=buildInitialMatchState(); appState=s;
    // UI polish static rendering checks: no Hero Info buttons and no Discard View buttons in battlefield markup.
    var html=field('PLAYER',s.playerHeroes,s)+field('AI',s.aiHeroes,s);
    if(/>Info<\/button>/.test(html)) return {ok:false, reason:'Hero Info button still rendered'};
    if(/>View<\/button>/.test(html)) return {ok:false, reason:'Discard View button still rendered'};
    // Preview cleanup: no raw JSON/object text and no duplicated rank label.
    var heroPreview=previewSections(card('S1-WAR-H001'));
    if(/\{"text"|\[object Object\]|undefined|null|Rank Rank/i.test(heroPreview)) return {ok:false, reason:'Hero preview still leaks coding/object text', preview:heroPreview};
    if(heroPreview.indexOf('Rank II: After Rank Up, draw 2 cards and gain +1 Mana Regen')<0) return {ok:false, reason:'Rank Up Bonus text missing', preview:heroPreview};
    // Rank Up runtime: draw reward + Mana Regen, no immediate Mana Pool gain.
    s.phase='Reform'; s.turn='PLAYER'; s.playerHand=['S1-WAR-001','S1-WAR-002','S1-WAR-006','S1-WAR-001','S1-WAR-002','S1-WAR-006']; s.playerDeck=['S1-WAR-001','S1-WAR-002','S1-WAR-006','S1-WAR-001']; s.mana=2; s.manaRegen=1; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exp_total=200; s.playerHeroes.LEFT.exp_cards=['S1-WAR-001','S1-WAR-002']; s.playerLegacy=['S1-WAR-H002','S1-WAR-H003']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR'}]; appState=s; beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(heroRank(s.playerHeroes.LEFT)!==2 || s.manaRegen!==2 || s.mana!==2 || s.playerHand.length<7) return {ok:false, reason:'Rank Up runtime reward failed', hero:s.playerHeroes.LEFT, hand:s.playerHand.length, mana:s.mana, regen:s.manaRegen};
    // Response no-stuck: no legal response -> Take Hit / No Response closes and resolves.
    s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001']; s.playerHand=[]; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false; commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'}); if(!s.responseWindow || (s.responseWindow.options||[]).length) return {ok:false, reason:'Expected no-response window', rw:s.responseWindow}; resolveResponseWindow(null); if(s.responseWindow || s.pending) return {ok:false, reason:'Response Window no-response did not clean up', pending:s.pending, rw:s.responseWindow};
    // AI behavior: legal Tribute in Reform preferred over skip.
    s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Reform'; s.tributeUsedThisReform=false; s.aiHand=['S1-ARC-001','S1-WAR-001']; var legal=getLegalActions('AI',s); var chosen=chooseAIAction(s,'Reform'); if(!legal.some(function(a){return a.type==='TRIBUTE_CARD';}) || !chosen || chosen.type!=='TRIBUTE_CARD') return {ok:false, reason:'AI did not choose legal Tribute in Reform', legal:legal, chosen:chosen}; executeAIAction(s,chosen); if(!s.tributeUsedThisReform || !s.lastOpponentAction) return {ok:false, reason:'AI Tribute execution failed', state:s};
    // AI Battle attack preferred when legal.
    s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001']; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.maxHp=100; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false; chosen=chooseAIAction(s,'Battle'); if(!chosen || chosen.type!=='PLAY_CARD' || !isAttackCard(card(chosen.card_id))) return {ok:false, reason:'AI did not choose attack in Battle', chosen:chosen, legal:getLegalActions('AI',s)};
    // Five-turn smoke without stale pending/player-only popup.
    s=buildInitialMatchState(); appState=s; s.aiMana=30; s.aiHand=['S1-WAR-001','S1-WAR-002','S1-ARC-001','S1-ARC-002','S1-ITM-001','S1-EVT-002','S1-WAR-006','S1-ARC-006']; for(var i=0;i<5;i++){ s.turn='PLAYER'; s.phase='End'; finishPlayerEndToAI(s); if(s.pending||s.responseWindow||s.turn!=='PLAYER'||s.phase!=='Draw'||(s.aiHand||[]).length>8) return {ok:false, reason:'AI 5-turn smoke stuck at '+i, pending:s.pending, rw:s.responseWindow, turn:s.turn, phase:s.phase}; }
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
    return {ok:true, noHeroInfoButtons:true, noDiscardViewButtons:true, previewClean:true, rankUpPreview:true, rankUpRuntime:true, responseNoStuck:true, aiUsesGetLegalActions:true, aiTributeInReform:true, aiAttackInBattle:true, aiFiveTurns:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }


  function simulateUIPolishResponseAIFix(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    var s=buildInitialMatchState(); appState=s;
    var html=field('PLAYER',s.playerHeroes,s)+field('AI',s.aiHeroes,s);
    if(/>Info<\/button>/.test(html)) return {ok:false, reason:'Hero Info button still rendered'};
    if(/>View<\/button>/.test(html)) return {ok:false, reason:'Discard View button still rendered'};
    var eventPreview=previewSections(card('S1-EVT-004'));
    if(eventPreview.indexOf('Choose one of your heroes. Tribute 1 Skill Card')<0) return {ok:false, reason:'Relentless Leveling preview missing real effect text', preview:eventPreview};
    if(/Requirement/.test(eventPreview)) return {ok:false, reason:'Event Requirement section still shown', preview:eventPreview};
    var heroPrev=previewSections(card('S1-WAR-H001'));
    if(/\{"text"|\[object Object\]|undefined|null|backend wording|Canonical card data|Rank Rank/i.test(heroPrev)) return {ok:false, reason:'Hero preview leaked backend/coding text', preview:heroPrev};
    if(heroPrev.indexOf('Rank II: After Rank Up, draw 2 cards and gain +1 Mana Regen')<0) return {ok:false, reason:'Rank Up Bonus missing', preview:heroPrev};
    s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001']; s.playerHand=[]; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false;
    commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'});
    if(!s.responseWindow || (s.responseWindow.options||[]).length) return {ok:false, reason:'No-legal-response window not opened correctly', rw:s.responseWindow};
    resolveResponseWindow(null); if(s.responseWindow || s.pending) return {ok:false, reason:'Take Hit / No Response did not clear pending state', pending:s.pending, rw:s.responseWindow};
    s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.mana=30; s.aiMana=30; s.playerHand=['S1-ITM-016']; s.aiHand=['S1-WAR-001']; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false;
    commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'});
    var opt=s.responseWindow && s.responseWindow.options && s.responseWindow.options[0]; if(!opt) return {ok:false, reason:'Legal response option missing', rw:s.responseWindow};
    var before=s.playerHeroes.CENTER.hp; resolveResponseWindow(opt); if(s.responseWindow || s.pending || s.playerHeroes.CENTER.hp!==before) return {ok:false, reason:'Legal response select did not resolve/clear', pending:s.pending, hpBefore:before, hpAfter:s.playerHeroes.CENTER.hp};
    s=buildInitialMatchState(); appState=s; s.phase='Reform'; s.turn='PLAYER'; s.playerHand=['S1-WAR-001']; s.playerDeck=['S1-WAR-002','S1-WAR-006','S1-WAR-001']; s.mana=2; s.manaRegen=1; s.playerHeroes.LEFT.exp_total=200; s.playerHeroes.LEFT.exp_cards=['S1-WAR-001','S1-WAR-002']; s.playerLegacy=['S1-WAR-H002','S1-WAR-H003']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR'}]; beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(heroRank(s.playerHeroes.LEFT)!==2 || s.manaRegen!==2 || s.mana!==2) return {ok:false, reason:'Rank Up runtime rule failed', rank:heroRank(s.playerHeroes.LEFT), mana:s.mana, regen:s.manaRegen};
    s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Reform'; s.tributeUsedThisReform=false; s.aiHand=['S1-ARC-001','S1-WAR-001']; var chosen=chooseAIAction(s,'Reform'); if(!chosen || chosen.type!=='TRIBUTE_CARD') return {ok:false, reason:'AI did not prefer Tribute', chosen:chosen, legal:getLegalActions('AI',s)};
    s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001']; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.maxHp=100; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false; chosen=chooseAIAction(s,'Battle'); if(!chosen || chosen.type!=='PLAY_CARD' || !isAttackCard(card(chosen.card_id))) return {ok:false, reason:'AI did not prefer Attack', chosen:chosen, legal:getLegalActions('AI',s)};
    for(var i=0;i<5;i++){ s.turn='PLAYER'; s.phase='End'; finishPlayerEndToAI(s); if(s.pending||s.responseWindow||s.turn!=='PLAYER'||s.phase!=='Draw') return {ok:false, reason:'AI 5-turn smoke stuck '+i, pending:s.pending, rw:s.responseWindow, turn:s.turn, phase:s.phase}; }
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
    return {ok:true, responseNoStuck:true, responseSelect:true, previewClean:true, eventPreviewRealText:true, rankUpRuntime:true, aiUsesGetLegalActions:true, aiTribute:true, aiAttack:true, aiFiveTurns:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }


  function simulateOpponentPlayedResponseFix(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true;
    var s=buildInitialMatchState(); appState=s; matchStarted=true;
    s.opponentPlayedEvents=[]; s.lastOpponentAction=null; s.phase='Reform'; s.turn='AI'; s.tributeUsedThisReform=false; s.aiHand=['S1-ARC-001','S1-EVT-001'];
    var tributeAction=chooseAIAction(s,'Reform');
    if(!tributeAction || tributeAction.type!=='TRIBUTE_CARD'){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'No legal Tribute action available for event-model test', action:tributeAction, legal:getLegalActions('AI',s)}; }
    executeAIAction(s,tributeAction);
    var tributeEvent=s.opponentPlayedEvents[0];
    if(!tributeEvent || tributeEvent.label!=='TRIBUTE' || /Begin Anew|draws 1|Mana Regen|moves to/i.test(opponentEventDetail(tributeEvent))) { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Tribute detail polluted', detail:tributeEvent&&opponentEventDetail(tributeEvent), events:s.opponentPlayedEvents}; }
    s.phase='Deploy'; s.turn='AI'; var baIdx=s.aiHand.indexOf('S1-EVT-001'); executeAIAction(s,{type:'PLAY_CARD', card_id:'S1-EVT-001', hand_index:baIdx, source_side:'AI', source_lane:'LEFT'});
    var eventEvent=s.opponentPlayedEvents[0];
    if(!eventEvent || eventEvent.label!=='EVENT' || eventEvent.card_id!=='S1-EVT-001') { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Begin Anew separate EVENT missing', events:s.opponentPlayedEvents}; }
    if(!/shuffled remaining hand|drew 5/i.test(opponentEventDetail(eventEvent))) { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Begin Anew detail missing effect', detail:opponentEventDetail(eventEvent)}; }
    s=buildInitialMatchState(); appState=s; matchStarted=true; s.phase='Battle'; s.turn='AI'; s.aiMana=30; s.mana=30; s.aiHand=['S1-WAR-001']; s.playerHand=[]; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.hp=100;
    commitPlayedCard(s,{card_id:'S1-WAR-001', hand_index:0, source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT'});
    if(!s.responseWindow) { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Response Window missing'}; }
    resolveResponseWindow(null);
    if(s.responseWindow || s.pending || s.playerHeroes.LEFT.hp>=100){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Take Hit did not clear/resolve', pending:s.pending, rw:s.responseWindow, hp:s.playerHeroes.LEFT.hp}; }
    s=buildInitialMatchState(); appState=s; matchStarted=true; s.phase='Battle'; s.turn='AI'; s.aiMana=30; s.mana=30; s.aiHand=['S1-WAR-002']; s.playerHand=['S1-WAR-003']; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.hp=100;
    commitPlayedCard(s,{card_id:'S1-WAR-002', hand_index:0, source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT'});
    var opt=s.responseWindow && s.responseWindow.options && s.responseWindow.options[0];
    if(!opt){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Legal response option missing', rw:s.responseWindow}; }
    resolveResponseWindow(opt);
    if(s.responseWindow || s.pending || s.playerDiscard.indexOf(opt.card_id)===-1){ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:false, reason:'Response select did not resolve/cleanup', pending:s.pending, rw:s.responseWindow, discard:s.playerDiscard}; }
    appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; return {ok:true, opponentPlayedEventModel:true, tributeDetailIsolated:true, beginAnewSeparateEvent:true, responseTakeHit:true, responseSelect:true, uiKeyword:'LOCAL_AI_UI v5.35'};
  }

  function simulateResponseWindowDeckSetupTextFix(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=false;
    try{
      var setupHtml=deckSetupSide('PLAYER',decks.PLAYER);
      if(/<strong>|<small>[^<]*(Human|Elf|Dwarf|Dragonborn|Rank)/i.test(setupHtml)) return {ok:false, reason:'Deck Setup hero name/identity text still rendered under hero thumbnails'};
      var s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001']; s.playerHand=[]; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.hp=100;
      commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});
      renderResponseWindow(); var pass=$('responsePassButton'); if(!pass || pass.disabled) return {ok:false, reason:'Take Hit / No Response button missing or disabled'};
      pass.click(); if(s.responseWindow || s.pending || s.playerHeroes.LEFT.hp>=100) return {ok:false, reason:'Take Hit / No Response click did not resolve and clear state', hp:s.playerHeroes.LEFT.hp, pending:s.pending, rw:s.responseWindow};
      s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.mana=30; s.aiHand=['S1-WAR-002']; s.playerHand=['S1-WAR-003']; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.hp=100;
      commitPlayedCard(s,{card_id:'S1-WAR-002',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});
      renderResponseWindow(); var previewBtn=document.querySelector('#responseOverlay [data-preview="S1-WAR-003"]'); if(!previewBtn) return {ok:false, reason:'Response card preview button missing'};
      previewBtn.click(); if(!$('previewOverlay').classList.contains('open')) return {ok:false, reason:'Response card preview did not open above Response Window'};
      $('previewClose').click(); var select=document.querySelector('#responseOverlay [data-response-select="0"]'); if(!select || select.disabled) return {ok:false, reason:'Response card Select missing or disabled'};
      select.click(); if(!s.responseWindow || s.responseWindow.selected!==0 || !s.pending || s.playerDiscard.indexOf('S1-WAR-003')!==-1) return {ok:false, reason:'Response card Select should only select and wait for Confirm', discard:s.playerDiscard, pending:s.pending, rw:s.responseWindow}; var confirm=document.querySelector('#responseOverlay #responseConfirmButton'); if(!confirm || confirm.disabled) return {ok:false, reason:'Response Confirm button missing or disabled after select'}; confirm.click(); if(s.responseWindow || s.pending || s.playerDiscard.indexOf('S1-WAR-003')===-1) return {ok:false, reason:'Response Confirm did not resolve and clear state', discard:s.playerDiscard, pending:s.pending, rw:s.responseWindow};
      s=buildInitialMatchState(); appState=s; matchStarted=true; s.aiHeroes.LEFT.card_id='S1-MAG-H001'; s.responseWindow={kind:'incoming_attack', card_id:'S1-MAG-002', source_side:'AI', source_lane:'LEFT', target_side:'PLAYER', target_lane:'LEFT', damage:20, damage_type:'Magical', attack_type:'Single Target', cannot_dodge:false, options:[], selected:null, post:{}, action:{card_id:'S1-MAG-002'}}; s.pending={type:'response_window', card_id:'S1-MAG-002'};
      renderResponseWindow(); var incomingText=($('responseBody')&&$('responseBody').textContent)||''; if(!/Inflict Freeze for 1 turn/i.test(incomingText)) return {ok:false, reason:'Incoming Card text missing Freeze effect', incomingText:incomingText};
      var poisonText=activeEffectSummary(card('S1-ARC-002'), {card_id:'S1-ARC-H001'}); if(!/Poison/i.test(poisonText)) return {ok:false, reason:'Poison Arrow incoming summary missing Poison text', poisonText:poisonText};
      var burnText=activeEffectSummary(card('S1-ARC-007'), {card_id:'S1-ARC-H001'}); if(!/Burn/i.test(burnText)) return {ok:false, reason:'Burning Arrow incoming summary missing Burn text', burnText:burnText};
      var sacredText=activeEffectSummary(card('S1-WAR-021'), {card_id:'S1-WAR-H005'}); if(!/cannot be dodged/i.test(sacredText)) return {ok:false, reason:'Sacred Stormblade incoming summary missing cannot-dodge text', sacredText:sacredText};
      return {ok:true, deckSetupHeroTextRemoved:true, takeHitClickResolves:true, responseSelectRequiresConfirm:true, responsePreviewPreservesWindow:true, incomingIceLanceFreezeText:true, poisonBurnCannotDodgeText:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; closeResponseWindowUI(); if($('previewOverlay')) $('previewOverlay').classList.remove('open'); }
  }


  function simulateAIRankUpReloadFix(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      // AI Skip remains in full history only, never compact Opponent Played.
      s.turn='AI'; s.phase='Deploy'; s.aiHand=[]; executeAIAction(s,{type:'SKIP', reason:'test skip'});
      var compact=opponentPlayed(s); if(/SKIP|AI Skip/i.test(compact)) return {ok:false, reason:'AI Skip still visible in compact Opponent Played', compact:compact};
      // Start Match shuffle should differ from static order and preserve counts.
      var staticTop=deckEntriesToIds(normalizeDeck(decks.PLAYER).main_deck).slice(0,14).join('|');
      s=buildInitialMatchState(); var mixed=(s.playerHand||[]).concat((s.playerDeck||[]).slice(0,7)).join('|');
      if(mixed===staticTop) return {ok:false, reason:'Start Match shuffle stayed in static starter order'};
      if((s.playerHand.length+s.playerDeck.length)!==deckEntriesToIds(normalizeDeck(decks.PLAYER).main_deck).length) return {ok:false, reason:'Start Match shuffle changed player card count'};
      // Begin Anew preserves total count and shuffles hand into deck before drawing.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Deploy'; s.aiHand=['S1-EVT-001','S1-WAR-001','S1-ARC-001']; s.aiDeck=['S1-WAR-002','S1-ARC-002','S1-THF-001','S1-WAR-006','S1-ARC-006','S1-THF-002']; s.aiDiscard=[]; s.aiMana=10;
      var beforeTotal=s.aiHand.length+s.aiDeck.length+s.aiDiscard.length; var beginAction=getLegalActions('AI',s).find(function(a){return a.card_id==='S1-EVT-001';}); executeAIAction(s,beginAction);
      var afterTotal=s.aiHand.length+s.aiDeck.length+s.aiDiscard.length; if(beforeTotal!==afterTotal) return {ok:false, reason:'Begin Anew changed total card count', before:beforeTotal, after:afterTotal};
      if(!((s.opponentPlayedEvents||[])[0] && s.opponentPlayedEvents[0].label==='EVENT' && s.opponentPlayedEvents[0].card_id==='S1-EVT-001')) return {ok:false, reason:'Begin Anew EVENT tile missing'};
      // AI does not over-prioritize Begin Anew over Reload/useful Deploy actions.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Deploy'; s.aiMana=10; s.aiHand=['S1-EVT-001','S1-ARC-005']; s.aiHeroes.CENTER.card_id='S1-ARC-H001'; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false; s.aiHeroes.CENTER.attachments=[null,null];
      var chosen=chooseAIAction(s,'Deploy'); if(!chosen || chosen.card_id==='S1-EVT-001') return {ok:false, reason:'AI still over-prioritized Begin Anew', chosen:chosen, legal:getLegalActions('AI',s)};
      // Rank Up: current Starter60 package slot lineage normalizes, Rank I at 300 EXP becomes Rank II immediately, draw reward + Mana Regen, no Mana Pool gain.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.mana=2; s.manaRegen=1; s.playerDeck=['S1-WAR-001','S1-WAR-002','S1-WAR-006','S1-WAR-019']; s.playerHand=['S1-WAR-001'];
      s.playerHeroes.LEFT.card_id=s.playerDeckName.indexOf('Starter')>=0?s.playerHeroes.LEFT.card_id:'S1-WAR-H001';
      // Force a known Warrior package on LEFT for deterministic rank-up test.
      s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exp_total=200; s.playerHeroes.LEFT.exp_cards=['S1-WAR-002','S1-WAR-006']; s.playerLegacy=['S1-WAR-H002','S1-WAR-H003']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR'}];
      beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(heroRank(s.playerHeroes.LEFT)!==2 || s.playerHeroes.LEFT.exp_total!==300 || s.manaRegen!==2 || s.mana!==2 || s.playerHand.length<2) return {ok:false, reason:'Rank I to II did not apply correctly', hero:s.playerHeroes.LEFT, mana:s.mana, regen:s.manaRegen, hand:s.playerHand};
      s.playerHeroes.LEFT.card_id='S1-WAR-H002'; s.playerHeroes.LEFT.exp_total=600; s.playerHeroes.LEFT.exp_cards=['S1-WAR-001','S1-WAR-002']; s.playerLegacy=['S1-WAR-H003']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR'}]; s.playerHand=['S1-WAR-006']; s.playerDeck=['S1-WAR-001','S1-WAR-002','S1-WAR-006','S1-WAR-019']; s.manaRegen=2; s.mana=2; s.tributeUsedThisReform=false;
      beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(heroRank(s.playerHeroes.LEFT)!==3 || s.manaRegen!==3 || s.mana!==2 || s.playerHand.length<3) return {ok:false, reason:'Rank II to III did not apply correctly', hero:s.playerHeroes.LEFT, hand:s.playerHand, mana:s.mana, regen:s.manaRegen};
      // Reload: Deploy legal, manual Archer source, draws 2, no Exhaust, no target stuck.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=5; s.playerHand=['S1-ARC-005']; s.playerDeck=['S1-WAR-001','S1-WAR-002','S1-WAR-006']; s.playerHeroes.CENTER.card_id='S1-ARC-H001'; s.playerHeroes.CENTER.hp=90; s.playerHeroes.CENTER.maxHp=90; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=['S1-ITM-016','S1-ITM-013'];
      var lp=legalPlayState(s,'S1-ARC-005'); if(!lp.can) return {ok:false, reason:'Reload not legal in Deploy with ready Archer and enough Mana', legal:lp, sources:legalSourceLanes(s,'S1-ARC-005')};
      beginPlayFromHand(0); if(!s.pending || s.pending.type!=='source_selection') return {ok:false, reason:'Reload did not enter manual source selection', pending:s.pending};
      chooseHeroFromBoard('PLAYER','CENTER'); if(s.pending || s.playerHeroes.CENTER.exhausted || s.playerHand.length!==2 || s.playerDiscard.indexOf('S1-ARC-005')===-1 || s.mana!==2) return {ok:false, reason:'Reload resolve failed (runtime cost is 3 Mana)', pending:s.pending, hero:s.playerHeroes.CENTER, hand:s.playerHand, discard:s.playerDiscard, mana:s.mana};
      return {ok:true, noAISkipTile:true, beginAnewHeuristic:true, startMatchShuffle:true, beginAnewShuffleCount:true, rankUp300:true, rankUp700:true, rankUpDrawReward:true, rankUpNoManaPoolGain:true, reloadPlayable:true, reloadNoExhaust:true, reloadNoTargetStuck:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; }
  }


  function simulateBlueprintComplianceFix(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      if(/src\/setup\/generatedDecks\.js|Generated Trio Deck|Generated Skirmish Deck/.test(String(STARTER_DECK_OPTIONS.starter_01_elemental_lord_conqueror_renegade && STARTER_DECK_OPTIONS.starter_01_elemental_lord_conqueror_renegade.label))) return {ok:false, reason:'Generated demo decks visible in starter options'};
      if(!/^https:\/\/i\.ibb\.co\//.test(fullFor('S1-ARC-016')) || /Soul-Shot/i.test(fullFor('S1-ARC-016'))) return {ok:false, reason:'Soul Blast Shot preview resolver not using latest URL mapping', full:fullFor('S1-ARC-016')};
      ['S1-ARC-018','S1-ARC-005','S1-MAG-002','S1-WAR-021'].forEach(function(id){ if(!fullFor(id)) throw new Error('Missing image resolver for '+id); });
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.mana=2; s.manaRegen=1; s.playerHand=['S1-WAR-001']; s.playerDeck=['S1-WAR-002','S1-WAR-006','S1-WAR-019']; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.maxHp=80; s.playerHeroes.LEFT.hp=30; s.playerHeroes.LEFT.exp_total=200; s.playerHeroes.LEFT.exp_cards=['S1-WAR-002','S1-WAR-006']; s.playerLegacy=['S1-WAR-H002','S1-WAR-H003']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR'}];
      beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(heroRank(s.playerHeroes.LEFT)!==2 || s.playerHeroes.LEFT.exp_total!==300 || s.playerHeroes.LEFT.hp!==70 || s.playerHeroes.LEFT.maxHp!==120 || s.manaRegen!==2 || s.mana!==2) return {ok:false, reason:'RankUp EXP/HP/Mana Regen rule failed', hero:s.playerHeroes.LEFT, mana:s.mana, regen:s.manaRegen};
      var manaHtml=resourceZone('PLAYER','Mana Pool',s.mana,false,'blue',s.manaRegen); if(!/regen \+2 \/ max 12/.test(manaHtml)) return {ok:false, reason:'Mana Regen tracker missing', html:manaHtml};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-WAR-001']; var before=s.aiHeroes.LEFT.hp; commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'}); if(s.responseWindow || s.pending){ return {ok:false, reason:'PLAYER attack opened player-facing response state for AI defense', pending:s.pending, rw:s.responseWindow}; } if(s.aiHeroes.LEFT.hp>=before) return {ok:false, reason:'PLAYER attack did not resolve after AI auto-pass'};
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001']; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.exhausted=false; commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'}); if(!s.responseWindow || s.responseWindow.response_owner!=='PLAYER') return {ok:false, reason:'AI attack did not open PLAYER response window', rw:s.responseWindow}; resolveResponseWindow(null); if(s.responseWindow || s.pending) return {ok:false, reason:'Take Hit / No Response did not clear'};
      s=buildInitialMatchState(); appState=s; addStatus(s.playerHeroes.LEFT,'Poison',2,'Poison test'); var panel=heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT); if(/EXP 0<\/button>|state-badge[^>]*>EXP/.test(panel) || !/Poison \(2\)/.test(panel)) return {ok:false, reason:'Status badge/EXP cleanup failed', panel:panel}; tickStatusDurationsForSide(s,'PLAYER'); if(!/Poison \(1\)/.test(heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT))) return {ok:false, reason:'Status duration tick failed', statuses:s.playerHeroes.LEFT.statuses};
      var op=Object.keys(STARTER_DECK_OPTIONS).map(function(k){return STARTER_DECK_OPTIONS[k].label;}).join('|'); if(/Generated Trio|Generated Skirmish/.test(op)) return {ok:false, reason:'Generated demo decks exposed in starter selector', options:op};
      return {ok:true, imageResolver:true, rankUpDamagePreservingHp:true, expPreserved:true, manaRegenTracker:true, responseOwnerGating:true, statusDurationBadges:true, starterSourceClean:true, classAudit:'reported', uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); }
  }


  function simulateRuntimeMismatchPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s;
      // AI attacks once: initial ATK declaration + Response Window resolution must remain one compact ATK event.
      s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.aiHand=['S1-WAR-001']; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.hp=100; s.playerHand=[];
      commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});
      var beforeAtk=(s.opponentPlayedEvents||[]).filter(function(e){return e.label==='ATK'&&e.card_id==='S1-WAR-001';}).length;
      resolveResponseWindow(null);
      var afterAtk=(s.opponentPlayedEvents||[]).filter(function(e){return e.label==='ATK'&&e.card_id==='S1-WAR-001';}).length;
      if(beforeAtk!==1 || afterAtk!==1) return {ok:false, reason:'Duplicate ATK compact tiles after response resolution', before:beforeAtk, after:afterAtk, events:s.opponentPlayedEvents};
      if((s.opponentPlayedEvents||[]).some(function(e){return e.label==='SKIP';})) return {ok:false, reason:'AI Skip visible in compact events'};
      // Status badge format + Poison tick then decrement.
      s=buildInitialMatchState(); appState=s; var h=s.playerHeroes.LEFT; h.hp=50; h.maxHp=100; h.statuses=[]; addStatus(h,'Poison',2,'test');
      if(!/Poison \(2\)/.test(heroPanel('PLAYER','LEFT',h))) return {ok:false, reason:'Poison badge did not render as Poison (2)', panel:heroPanel('PLAYER','LEFT',h)};
      resolveEndPhaseCleanupForSide(s,'PLAYER');
      if(h.hp!==40 || !/Poison \(1\)/.test(heroPanel('PLAYER','LEFT',h))) return {ok:false, reason:'Poison tick/decrement failed', hp:h.hp, statuses:h.statuses, panel:heroPanel('PLAYER','LEFT',h)};
      h.hp=5; h.statuses=[]; addStatus(h,'Poison',1,'lethal test'); s.playerLegacy=['S1-WAR-L001']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR',legacy:'S1-WAR-L001'}]; resolveEndPhaseCleanupForSide(s,'PLAYER'); if(h.hp!==0 || !s.pending || s.pending.type!=='legacy_defeat_choice') return {ok:false, reason:'Poison lethal tick should pause for Legacy choice', hp:h.hp, statuses:h.statuses, pending:s.pending}; s.pending=null;
      // Exhaust persists through End Phase and clears after owner Draw Phase.
      s=buildInitialMatchState(); appState=s; s.playerHeroes.LEFT.exhausted=true; resolveEndPhaseCleanupForSide(s,'PLAYER'); if(!s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Exhaust cleared too early at owner End Phase'}; resolveDrawPhase(s,'PLAYER'); if(s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Exhaust not cleared after owner Draw Phase'};
      // Rogue/Renegade poison passive.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-THF-012']; s.playerHeroes.CENTER.card_id='S1-THF-H002'; s.playerHeroes.CENTER.exhausted=false; LANE_ORDER.forEach(function(l){s.aiHeroes[l].statuses=[]; s.aiHeroes[l].hp=100;});
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!LANE_ORDER.every(function(l){return poisonDurationOn(s.aiHeroes[l])===3;})) return {ok:false, reason:'Rogue Poison Mist should apply Poison (3)', statuses:LANE_ORDER.map(function(l){return s.aiHeroes[l].statuses;})};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-THF-012']; s.playerHeroes.CENTER.card_id='S1-THF-H003'; s.playerHeroes.CENTER.exhausted=false; LANE_ORDER.forEach(function(l){s.aiHeroes[l].statuses=[]; s.aiHeroes[l].hp=100;});
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(!LANE_ORDER.every(function(l){return poisonDurationOn(s.aiHeroes[l])===3;})) return {ok:false, reason:'Renegade Poison Mist should apply Poison (3)', statuses:LANE_ORDER.map(function(l){return s.aiHeroes[l].statuses;})};
      // Rank Up preserves EXP progression instead of reset.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.mana=2; s.manaRegen=1; s.playerHand=['S1-WAR-001']; s.playerDeck=['S1-WAR-002','S1-WAR-006','S1-WAR-019']; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.exp_total=200; s.playerHeroes.LEFT.exp_cards=['S1-WAR-002','S1-WAR-006']; s.playerLegacy=['S1-WAR-H002','S1-WAR-H003']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR'}];
      beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(heroRank(s.playerHeroes.LEFT)!==2 || s.playerHeroes.LEFT.exp_total!==300 || s.manaRegen!==2 || s.mana!==2) return {ok:false, reason:'Rank Up did not preserve EXP progression / reward rules', hero:s.playerHeroes.LEFT, mana:s.mana, regen:s.manaRegen};
      // Reposition disabled in Battle.
      s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='PLAYER'; s.phase='Battle'; s.pending=null; if(manualRepositionAvailable(s)) return {ok:false, reason:'Manual Reposition available during Battle Phase'};
      // No compact tiles for status ticks / draw / mana / cleanup / AI Skip.
      var before=(s.opponentPlayedEvents||[]).length; pushLog(s,'AI skipped legally.'); resolveEndPhaseCleanupForSide(s,'PLAYER'); if((s.opponentPlayedEvents||[]).length!==before) return {ok:false, reason:'System cleanup created compact Opponent Played tile'};
      return {ok:true, oneAtkTilePerAction:true, statusBadgeParentheses:true, poisonTickBeforeDecrement:true, exhaustClearsAtOwnerEnd:true, roguePoisonPlusOne:true, renegadePoisonPlusOne:true, rankUpExpPreserved:true, repositionBattleDisabled:true, noSystemCompactTiles:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); }
  }



  function simulateOpponentArchiveStatusTargetFix(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s;
      // Archive keeps all visual events while recent remains latest 4 only and skips system text.
      s.opponentPlayedEvents=[];
      var evt1=recordOpponentAction(s,'ATK','S1-WAR-001','Slash','AI played Slash.',{action_line:'Action: AI played Slash.', result_lines:['Slash resolved.']});
      recordOpponentAction(s,'EVENT','S1-EVT-001','Begin Anew','AI played Begin Anew.',{action_line:'Action: AI played Begin Anew.', result_lines:['AI shuffled remaining hand into deck.','AI drew 5 cards.']});
      recordOpponentAction(s,'TRIBUTE','S1-WAR-014','AI Tribute','AI tributed Final Grit.',{action_line:'Action: AI tributed Final Grit.', result_lines:['AI LEFT gained 100 EXP.']});
      recordOpponentAction(s,'ITEM','S1-ITM-011','Poison Vial','AI used Poison Vial.',{result_lines:['Next attack this turn can inflict Poison.']});
      recordOpponentAction(s,'RANK UP','S1-WAR-H002','AI Rank Up','AI ranked up.',{result_lines:['AI reached Rank II.']});
      var recentHtml=opponentPlayed(s); if((recentHtml.match(/class="actionIcon/g)||[]).length!==4) return {ok:false, reason:'Recent Opponent Played did not render latest four visual tiles', html:recentHtml};
      var archive=opponentPlayedArchiveHtml(s); if((archive.match(/data-op-archive-id/g)||[]).length<5) return {ok:false, reason:'Opponent Played archive did not retain older icons', archive:archive};
      if(/getLegalActions|Mana Regen|cleanup|AI Skip|draws 1 card/i.test(archive)) return {ok:false, reason:'Archive leaked debug/system text', archive:archive};
      if(!/Slash/.test(opponentEventDetail(evt1))) return {ok:false, reason:'Archived event detail not event-specific', detail:opponentEventDetail(evt1)};
      // Poison stacking and temporary marker cleanup.
      s=buildInitialMatchState(); appState=s; var h=s.aiHeroes.LEFT; h.statuses=[]; addStatus(h,'Poison',1,'existing Poison'); addStatus(h,'Poison',2,'Poison Vial'); if(poisonDurationOn(h)!==3) return {ok:false, reason:'Poison Vial did not stack Poison duration', statuses:h.statuses};
      addStatus(h,'Poison',2,'Poison Vial again'); if(poisonDurationOn(h)!==5) return {ok:false, reason:'Second Poison stack failed', statuses:h.statuses};
      var src=s.playerHeroes.LEFT; src.statuses=[]; addStatus(src,'Poison Vial',1,'next attack this turn'); resolveEndPhaseCleanupForSide(s,'PLAYER'); if(hasStatus(src,'Poison Vial')) return {ok:false, reason:'Unused Poison Vial marker did not expire'};
      var pHero=s.playerHeroes.CENTER; pHero.statuses=[]; pHero.exhausted=true; pHero.exhaust_reason='Greater Health Potion exhausted this hero.'; addStatus(pHero,'Potion Exhausted',1,'Greater Health Potion exhausted this hero.'); var beforePanel=heroPanel('PLAYER','CENTER',pHero); if(/>Potion Exhausted</.test(beforePanel) || (/Ready/.test(beforePanel)&&/Potion Exhausted/.test(beforePanel))) return {ok:false, reason:'Potion Exhausted rendered as redundant visible state badge before cleanup', panel:beforePanel}; resolveEndPhaseCleanupForSide(s,'PLAYER'); if(!pHero.exhausted || hasStatus(pHero,'Potion Exhausted') || /Potion Exhausted/.test(heroPanel('PLAYER','CENTER',pHero))) return {ok:false, reason:'Potion Exhausted marker should expire at End but Exhaust should persist until Draw', hero:pHero, panel:heroPanel('PLAYER','CENTER',pHero)}; resolveDrawPhase(s,'PLAYER'); if(pHero.exhausted) return {ok:false, reason:'Potion Exhaust did not clear after Draw Phase', hero:pHero};
      // Marksman / Grand Ranger target-any single-target player UI flow.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H002'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-ARC-006']; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.legal_targets.indexOf('RIGHT')===-1) return {ok:false, reason:'Marksman LEFT could not target enemy RIGHT through player UI', pending:s.pending}; chooseHeroFromBoard('AI','RIGHT'); if(s.responseWindow) resolveResponseWindow(null); if(s.aiHeroes.RIGHT.hp>=100) return {ok:false, reason:'Marksman target-any commit did not resolve damage', hp:s.aiHeroes.RIGHT.hp};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H003'; s.playerHeroes.LEFT.hp=120; s.playerHeroes.LEFT.maxHp=120; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-ARC-006']; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.legal_targets.indexOf('RIGHT')===-1) return {ok:false, reason:'Grand Ranger LEFT could not target enemy RIGHT through player UI', pending:s.pending}; chooseHeroFromBoard('AI','RIGHT'); if(s.responseWindow) resolveResponseWindow(null); if(s.aiHeroes.RIGHT.hp!==50) return {ok:false, reason:'Grand Ranger +10 Single Target damage failed for Aim Shot', hp:s.aiHeroes.RIGHT.hp};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H003'; s.playerHeroes.LEFT.hp=120; s.playerHeroes.LEFT.maxHp=120; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-ARC-001']; s.aiHeroes.LEFT.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(s.responseWindow) resolveResponseWindow(null); if(s.aiHeroes.LEFT.hp!==90) return {ok:false, reason:'Bow Bash incorrectly received Grand Ranger +10 bonus', hp:s.aiHeroes.LEFT.hp};
      return {ok:true, opponentArchiveVisual:true, recentFourOnly:true, noDebugInOpponentArchive:true, poisonDurationStacks:true, tempMarkersExpire:true, noContradictoryReadyPotionExhausted:true, marksmanTargetAnyPlayerUI:true, grandRangerTargetAnyAndBonus:true, bowBashExcluded:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); }
  }

  
  function simulateRuntimeFlowSyncBlueprintGate(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s;
      // Central validator: Steal cannot be legal if opponent has no Mana.
      s.turn='PLAYER'; s.phase='Deploy'; s.mana=10; s.aiMana=0; s.playerHand=['S1-THF-005']; s.playerHeroes.LEFT.card_id='S1-THF-H001';
      if(legalPlayStateFor(s,'PLAYER','S1-THF-005').can) return {ok:false, reason:'Steal playable while opponent Mana Pool is 0'};
      s.aiMana=2; if(!legalPlayStateFor(s,'PLAYER','S1-THF-005').can) return {ok:false, reason:'Steal not playable when opponent Mana Pool is available'};
      // Cancel before commit keeps hand/mana/exhaust intact.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=10; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-WAR-001']; beginPlayFromHand(0); cancelPendingAction(); if(s.mana!==10 || s.playerHand[0]!=='S1-WAR-001' || s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Cancel before commit violated lifecycle'};
      // Damage pipeline: Venom Sovereign conditional is separate from base attack.
      s=buildInitialMatchState(); appState=s; var src=s.playerHeroes.LEFT, tgt=s.aiHeroes.LEFT; src.card_id='S1-THF-H002'; tgt.hp=100; tgt.maxHp=100; addStatus(tgt,'Poison',2,'setup'); var res=applyResolvedAttackDamage(s,'S1-THF-015','PLAYER','LEFT','AI','LEFT',currentClassDamage(card('S1-THF-015'),src,tgt),'Magical',false,0); if(res.base_damage!==20 || res.conditional_damage!==40 || tgt.hp!==40) return {ok:false, reason:'Rogue Venom Sovereign pipeline failed', res:res, hp:tgt.hp};
      s=buildInitialMatchState(); appState=s; src=s.playerHeroes.LEFT; tgt=s.aiHeroes.LEFT; src.card_id='S1-THF-H003'; tgt.hp=100; tgt.maxHp=100; addStatus(tgt,'Poison',2,'setup'); res=applyResolvedAttackDamage(s,'S1-THF-015','PLAYER','LEFT','AI','LEFT',currentClassDamage(card('S1-THF-015'),src,tgt),'Magical',false,0); if(res.base_damage!==50 || res.conditional_damage!==40 || tgt.hp!==10) return {ok:false, reason:'Renegade Venom Sovereign pipeline failed', res:res, hp:tgt.hp};
      // Status pipeline: poison stacks, ticks before decrement, badge uses parentheses.
      s=buildInitialMatchState(); appState=s; var h=s.playerHeroes.LEFT; h.hp=30; h.statuses=[]; addStatus(h,'Poison',1,'existing'); addStatus(h,'Poison',2,'reapply'); if(poisonDurationOn(h)!==3 || !/Poison \(3\)/.test(heroPanel('PLAYER','LEFT',h))) return {ok:false, reason:'Poison stacking/badge failed', statuses:h.statuses, panel:heroPanel('PLAYER','LEFT',h)}; resolveEndPhaseCleanupForSide(s,'PLAYER'); if(h.hp!==20 || poisonDurationOn(h)!==2) return {ok:false, reason:'Poison tick/decrement order failed', hp:h.hp, poison:poisonDurationOn(h)};
      // Defeat -> Legacy Mode transition.
      s=buildInitialMatchState(); appState=s; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.attachments=['S1-ITM-016',null]; s.playerHeroes.LEFT.exp_cards=['S1-WAR-001']; s.playerHeroes.LEFT.exp_total=100; s.playerLegacy=['S1-WAR-L001']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR',legacy:'S1-WAR-L001'}]; s.playerHeroes.LEFT.hp=0; applyDefeatCheck(s,'PLAYER','LEFT'); if(!isLegacyModeHero(s.playerHeroes.LEFT) || s.playerHeroes.LEFT.card_id!=='S1-WAR-L001' || s.playerLegacy.indexOf('S1-WAR-L001')!==-1 || s.playerDiscard.indexOf('S1-WAR-001')===-1) return {ok:false, reason:'Defeat to Legacy Mode failed', hero:s.playerHeroes.LEFT, legacy:s.playerLegacy, discard:s.playerDiscard}; if(legalTributeTargetLanesFor(s,'PLAYER','S1-WAR-001').indexOf('LEFT')!==-1) return {ok:false, reason:'Legacy Mode can receive Tribute'};
      // Revive from Legacy Mode.
      restoreHeroFromLegacyMode(s,'PLAYER','LEFT',10,'Phoenix Feather'); if(isLegacyModeHero(s.playerHeroes.LEFT) || s.playerHeroes.LEFT.hp!==10 || !s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Revive from Legacy Mode failed', hero:s.playerHeroes.LEFT};
      // Rank Up preserves progression and does not increase Mana Pool.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.mana=2; s.manaRegen=1; s.playerDeck=['S1-WAR-002','S1-WAR-006','S1-WAR-019']; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.exp_total=300; s.playerLegacy=['S1-WAR-H002','S1-WAR-H003']; s.playerLegacyPackageSlots=[{slot:'LEFT',lineage:'WAR-CONQUEROR'}]; rankUpHero(s,'PLAYER','LEFT'); if(heroRank(s.playerHeroes.LEFT)!==2 || s.playerHeroes.LEFT.exp_total!==300 || s.manaRegen!==2 || s.mana!==2) return {ok:false, reason:'Rank Up progression/reward failed', hero:s.playerHeroes.LEFT, mana:s.mana, regen:s.manaRegen};
      // Exhaust clears after owner Draw Phase, no contradictory status markers.
      s.playerHeroes.LEFT.exhausted=true; s.playerHeroes.LEFT.exhaust_reason='test'; addStatus(s.playerHeroes.LEFT,'Potion Exhausted',1,'test'); resolveEndPhaseCleanupForSide(s,'PLAYER'); if(!s.playerHeroes.LEFT.exhausted || hasStatus(s.playerHeroes.LEFT,'Potion Exhausted') || /Potion Exhausted/.test(heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT))) return {ok:false, reason:'End cleanup should expire Potion marker but keep Exhaust until Draw', hero:s.playerHeroes.LEFT}; resolveDrawPhase(s,'PLAYER'); if(s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Exhaust cleanup after Draw failed', hero:s.playerHeroes.LEFT};
      // Target coverage: Marksman target-any, non-Marksman cannot LEFT->RIGHT.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.playerHeroes.LEFT.card_id='S1-ARC-H002'; if(legalTargetLanesFor(s,'PLAYER','S1-ARC-006','LEFT').indexOf('RIGHT')===-1) return {ok:false, reason:'Marksman target-any missing'}; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; if(legalTargetLanesFor(s,'PLAYER','S1-ARC-006','LEFT').indexOf('RIGHT')!==-1) return {ok:false, reason:'Default Archer illegally target-any'};
      // Response owner gating.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHand=['S1-WAR-001']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(s.responseWindow) return {ok:false, reason:'Player attack opened player response window'};
      // Opponent Played visual archive, no raw diagnostics.
      s=buildInitialMatchState(); appState=s; recordOpponentAction(s,'ATK','S1-WAR-001','Slash','AI played Slash.',{result_lines:['Slash resolved.']}); var arch=opponentPlayedArchiveHtml(s); if(/getLegalActions|Mana Regen|cleanup|AI Skip/i.test(arch) || !/data-op-archive-id/.test(arch)) return {ok:false, reason:'Opponent Played archive leaked raw log or lacked icons', archive:arch};
      return {ok:true, runtimeFlowSync:true, centralValidator:true, centralLifecycle:true, centralDamagePipeline:true, centralStatusPipeline:true, defeatLegacyFlow:true, rankUpFlow:true, responseOwnerGating:true, opponentPlayedArchive:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); }
  }

function withUnshuffledSelfTest(fn){ return function(){ var old=STARTUP_SHUFFLE_ENABLED; STARTUP_SHUFFLE_ENABLED=false; try{ return fn(); } finally { STARTUP_SHUFFLE_ENABLED=old; } }; }

  function simulateMinorBugCleanup(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s;
      if((s.playerHand||[]).length!==6 || (s.aiHand||[]).length!==6 || (s.playerDeck||[]).length!==44 || (s.aiDeck||[]).length!==44) return {ok:false, reason:'Opening hand/deck count failed', playerHand:s.playerHand.length, aiHand:s.aiHand.length, playerDeck:s.playerDeck.length, aiDeck:s.aiDeck.length};
      resolveDrawPhase(s,'PLAYER'); if(s.playerHand.length!==7 || s.playerDeck.length!==43) return {ok:false, reason:'First Draw Phase hand/deck count failed', playerHand:s.playerHand.length, playerDeck:s.playerDeck.length};
      // Normal PLAYER attack against AI auto-pass must resolve with no stale cancelable pending.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-WAR-001']; s.aiHeroes.LEFT.hp=100;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(s.pending || s.responseWindow || s.aiHeroes.LEFT.hp>=100) return {ok:false, reason:'Normal attack left stale pending/cancel state after AI auto-pass', pending:s.pending, rw:s.responseWindow, hp:s.aiHeroes.LEFT.hp};
      // Charge Attack optional swap: legal path opens yes/no choice; No cleans up.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-WAR-H002'; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.CENTER.card_id='S1-CLE-H004'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-WAR-015']; s.aiHeroes.LEFT.hp=100;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(!s.pending || s.pending.type!=='optional_swap') return {ok:false, reason:'Charge Attack did not open optional swap after AI auto-pass', pending:s.pending}; performOptionalSwapDecision(false); if(s.pending || s.responseWindow) return {ok:false, reason:'Charge Attack optional swap No did not clean up', pending:s.pending, rw:s.responseWindow};
      // Charge Attack illegal swap due Freeze should not open popup or leave stale pending.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.LEFT.card_id='S1-WAR-H002'; s.playerHeroes.LEFT.exhausted=false; addStatus(s.playerHeroes.LEFT,'Freeze',1,'test'); s.playerHand=['S1-WAR-015']; s.aiHeroes.LEFT.hp=100;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','LEFT'); if(s.pending || s.responseWindow) return {ok:false, reason:'Charge Attack illegal swap left stale pending', pending:s.pending, rw:s.responseWindow};
      // Final Grit hidden unless allied Legacy Mode Gladiator/Conqueror exists.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-WAR-014']; s.playerHeroes.CENTER.card_id='S1-WAR-H002'; s.playerHeroes.CENTER.exhausted=false; if(legalPlayState(s,'S1-WAR-014').can) return {ok:false, reason:'Final Grit playable without Legacy Mode target'};
      s.playerHeroes.CENTER.card_id='S1-WAR-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false;
      s.playerHeroes.LEFT={card_id:'S1-WAR-L001', side:'PLAYER', lane:'LEFT', hp:1, maxHp:1, exhausted:false, exp_cards:[], exp_total:0, attachments:[null,null], statuses:[], legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-WAR-H002', hp:0, maxHp:100, statuses:[], attachments:[null,null]}};
      var fgState=legalPlayState(s,'S1-WAR-014'); if(!fgState.can) return {ok:false, reason:'Final Grit not playable with valid Legacy Mode Gladiator target', reasons:fgState.reasons};
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('PLAYER','LEFT'); if(isLegacyModeHero(s.playerHeroes.LEFT) || s.playerHeroes.LEFT.hp!==30 || s.pending) return {ok:false, reason:'Final Grit did not resolve from Legacy Mode target', hero:s.playerHeroes.LEFT, pending:s.pending};
      // General pending invariant after common resolved actions.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-EVT-002']; commitPlayedCard(s,{card_id:'S1-EVT-002', hand_index:0, source_side:null, source_lane:null, target_side:null, target_lane:null}); if(s.pending) return {ok:false, reason:'Event left stale pending', pending:s.pending};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-ITM-005']; commitPlayedCard(s,{card_id:'S1-ITM-005', hand_index:0, source_side:null, source_lane:null, target_side:null, target_lane:null}); if(s.pending) return {ok:false, reason:'Item left stale pending', pending:s.pending};
      return {ok:true, openingHandSix:true, firstDrawSeven:true, cancelClearsAfterResolve:true, chargeAttackOptionalSwap:true, finalGritLegacyValidated:true, pendingInvariant:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); }
  }

  window.GL_STEP5_SELF_TEST=withUnshuffledSelfTest(simulateCoreLoop);
  window.GL_STEP6_SELF_TEST=withUnshuffledSelfTest(simulateSourceTargetInteraction);
  window.GL_STEP7_SELF_TEST=withUnshuffledSelfTest(simulateCoreSystems);
  window.GL_STEP8_SELF_TEST=withUnshuffledSelfTest(simulateResponseOpponentPlayed);
  window.GL_STEP9A_WARRIOR_SELF_TEST=withUnshuffledSelfTest(simulateWarriorEffectAuditPatch);
  window.GL_STEP9B_MAGE_SELF_TEST=withUnshuffledSelfTest(simulateMageEffectAuditPatch);
  window.GL_STEP9_COMBINED_SELF_TEST=withUnshuffledSelfTest(simulateStep9CombinedHandlers);
  window.GL_STEP9C_CLERIC_SELF_TEST=withUnshuffledSelfTest(simulateClericEffectAuditPatch);
  window.GL_STEP9D_THIEF_SELF_TEST=withUnshuffledSelfTest(simulateThiefEffectAuditPatch);
  window.GL_STEP9D_ARCHER_SELF_TEST=withUnshuffledSelfTest(simulateArcherEffectAuditPatch);
  window.GL_STEP9_FINAL_SELF_TEST=withUnshuffledSelfTest(simulateStep9FinalEventItemChoiceAudit);
  window.GL_STEP10_AI_LEGAL_ACTION_SELF_TEST=withUnshuffledSelfTest(simulateStep10AILegalActionPass);
  window.GL_STEP11_PLAYABLE_QA_SELF_TEST=withUnshuffledSelfTest(simulateStep11PlayableQACandidate);
  window.GL_STEP12_PRIOR_LOCK_SCOPE_SELF_TEST=withUnshuffledSelfTest(simulateStep12PriorLockScope);
  window.GL_PACKAGING_PARITY_QA_SELF_TEST=withUnshuffledSelfTest(simulatePackagingParityQACandidate);
  window.GL_DECKSETUP_FIX_QA_SELF_TEST=withUnshuffledSelfTest(simulateDeckSetupFixQA);
  window.GL_BOARD_DECKSETUP_FIX_QA_SELF_TEST=withUnshuffledSelfTest(simulateDeckSetupFixQA);
  window.GL_BATTLEFIELD_UI_SHUFFLE_FIX_QA_SELF_TEST=simulateBattlefieldShuffleFixQA;
  window.GL_UI_POLISH_AI_BEHAVIOR_FIX_QA_SELF_TEST=simulateUIPolishAIBehaviorFix;
  function simulatePhase2ResponsePackagePatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true;
    var s=buildInitialMatchState(); appState=s; matchStarted=true;
    try{
      // Intercept: PLAYER can cancel an incoming AI Event before the Event resolves.
      s.turn='AI'; s.phase='Deploy'; s.aiMana=30; s.mana=30; s.aiHand=['S1-EVT-002']; s.playerHand=['S1-EVT-007'];
      s.aiHeroes.LEFT.exhausted=false; commitPlayedCard(s,{card_id:'S1-EVT-002',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:null,target_lane:null});
      if(!s.responseWindow || s.responseWindow.kind!=='incoming_card' || !(s.responseWindow.options||[]).some(function(o){return o.card_id==='S1-EVT-007';})) return {ok:false, reason:'Intercept reactive Event window missing', rw:s.responseWindow};
      resolveResponseWindow((s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-EVT-007';}));
      if(s.responseWindow || s.pending || s.aiDiscard.indexOf('S1-EVT-002')<0 || s.playerDiscard.indexOf('S1-EVT-007')<0) return {ok:false, reason:'Intercept cancel did not cleanly discard both cards', pending:s.pending, rw:s.responseWindow, aiDiscard:s.aiDiscard, playerDiscard:s.playerDiscard};
      // Flashpowder Bomb: PLAYER can cancel an incoming AI Item before target Item resolves.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Deploy'; s.aiMana=30; s.mana=30; s.aiHand=['S1-ITM-001']; s.playerHand=['S1-ITM-017']; s.aiHeroes.LEFT.hp=60; s.aiHeroes.LEFT.maxHp=100;
      commitPlayedCard(s,{card_id:'S1-ITM-001',hand_index:0,source_side:'AI',source_lane:null,target_side:'AI',target_lane:'LEFT'});
      if(!s.responseWindow || s.responseWindow.kind!=='incoming_card' || !(s.responseWindow.options||[]).some(function(o){return o.card_id==='S1-ITM-017';})) return {ok:false, reason:'Flashpowder reactive Item window missing', rw:s.responseWindow};
      resolveResponseWindow((s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-ITM-017';}));
      if(s.aiHeroes.LEFT.hp!==60 || s.aiDiscard.indexOf('S1-ITM-001')<0 || s.playerDiscard.indexOf('S1-ITM-017')<0) return {ok:false, reason:'Flashpowder did not cancel Item effect cleanly', hp:s.aiHeroes.LEFT.hp, aiDiscard:s.aiDiscard, playerDiscard:s.playerDiscard};
      // Defensive Formation: multi-target AI area attack opens a single Response Window and reduces all affected damage.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.aiMana=30; s.mana=30; s.aiHand=['S1-WAR-016']; s.playerHand=['S1-EVT-012'];
      s.aiHeroes.CENTER.card_id='S1-WAR-H002'; s.aiHeroes.CENTER.hp=120; s.aiHeroes.CENTER.maxHp=120; s.aiHeroes.CENTER.exhausted=false;
      s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.card_id='S1-MAG-H001'; s.playerHeroes.RIGHT.card_id='S1-CLE-H001';
      LANE_ORDER.forEach(function(l){ s.playerHeroes[l].hp=100; s.playerHeroes[l].maxHp=100; });
      commitPlayedCard(s,{card_id:'S1-WAR-016',hand_index:0,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:null});
      if(!s.responseWindow || !s.responseWindow.multi_sequence || s.responseWindow.target_lane!=='LEFT' || !(s.responseWindow.options||[]).some(function(o){return o.card_id==='S1-EVT-012';})) return {ok:false, reason:'Defensive Formation per-Hero area response window missing', rw:s.responseWindow, pending:s.pending, hand:s.playerHand, card:card('S1-EVT-012')};
      resolveResponseWindow((s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-EVT-012';}));
      if(!s.responseWindow || s.responseWindow.target_lane!=='CENTER') return {ok:false, reason:'Area response did not advance to second Hero', rw:s.responseWindow};
      resolveResponseWindow(null);
      if(!s.responseWindow || s.responseWindow.target_lane!=='RIGHT') return {ok:false, reason:'Area response did not advance to third Hero', rw:s.responseWindow};
      resolveResponseWindow(null);
      if(s.responseWindow || s.pending) return {ok:false, reason:'Area response sequence did not finish cleanly', pending:s.pending, rw:s.responseWindow};
      // Blessing of Divinity is an authoritative persistent team Attachment and prevents status tick damage without removing Poison.
      s=buildInitialMatchState(); appState=s; var h=s.playerHeroes.LEFT; h.hp=100; h.statuses=[]; h.attachments=['S1-CLE-025',null]; s.activeAttachments=[{attachment_id:'QA:BLESSING',side:'PLAYER',lane:'LEFT',slot:0,card_id:'S1-CLE-025',tick_phase:'DRAW_PHASE_START',remaining_count:1,counter_mode:'countdown',role:'defense',created_phase:'Battle',created_turn:'AI',created_round:1}]; addStatus(h,'Poison',1,'Poison test'); resolveStatusTicksForSide(s,'PLAYER');
      if(h.hp!==100 || !hasStatus(h,'Poison')) return {ok:false, reason:'Blessing of Divinity did not prevent Poison tick damage while preserving Poison', hp:h.hp, statuses:h.statuses,attachments:s.activeAttachments};
      return {ok:true, intercept:true, flashpowder:true, defensiveFormationAreaWindow:true, blessingPreventsStatusTickDamage:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }
  window.GL_UI_POLISH_RESPONSE_AI_FIX_QA_SELF_TEST=simulateUIPolishResponseAIFix;
  window.GL_OPPONENT_PLAYED_RESPONSE_FIX_QA_SELF_TEST=simulateOpponentPlayedResponseFix;
  window.GL_RESPONSE_WINDOW_DECKSETUP_TEXT_FIX_QA_SELF_TEST=simulateResponseWindowDeckSetupTextFix;
  window.GL_PHASE2_RESPONSE_PACKAGE_PATCH_QA_SELF_TEST=simulatePhase2ResponsePackagePatch;
  window.GL_AI_RANKUP_RELOAD_FIX_QA_SELF_TEST=simulateAIRankUpReloadFix;
  window.GL_BLUEPRINT_COMPLIANCE_FIX_QA_SELF_TEST=simulateBlueprintComplianceFix;
  window.GL_RUNTIME_MISMATCH_FIX_QA_SELF_TEST=simulateRuntimeMismatchPatch;
  window.GL_OPPONENT_ARCHIVE_STATUS_TARGET_FIX_QA_SELF_TEST=simulateOpponentArchiveStatusTargetFix;
  window.GL_RUNTIME_FLOW_SYNC_BLUEPRINT_GATE_QA_SELF_TEST=simulateRuntimeFlowSyncBlueprintGate;
  
  function simulateTimingUxAiCleanup(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s;
      if(s.playerHand.length!==6 || s.aiHand.length!==6 || s.playerDeck.length!==44 || s.aiDeck.length!==44) return {ok:false, reason:'opening hand/deck count mismatch', ph:s.playerHand.length, ah:s.aiHand.length, pd:s.playerDeck.length, ad:s.aiDeck.length};
      resolveDrawPhase(s,'PLAYER'); if(s.playerHand.length!==7 || s.playerDeck.length!==43) return {ok:false, reason:'first Draw Phase count mismatch', hand:s.playerHand.length, deck:s.playerDeck.length};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='End'; finishPlayerEndToAI(s); if(s.round!==2 || s.turn!=='PLAYER' || s.phase!=='Draw') return {ok:false, reason:'round did not increment after full turn cycle', round:s.round, turn:s.turn, phase:s.phase};
      // Player hand title includes count.
      var html; matchStarted=true; appState=s; SUPPRESS_RENDER=true; html=opponentPlayed(s); var oldRoot='';
      if(!/Opponent Played/.test(html)) return {ok:false, reason:'opponent panel render failed'};
      // AI planning: Poison Vial cannot be selected when remaining mana cannot pay any Battle attack.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Deploy'; s.aiMana=1; s.aiHand=['S1-ITM-011','S1-ARC-006']; s.aiHeroes.RIGHT.card_id='S1-ARC-H001'; s.aiHeroes.RIGHT.exhausted=false; var acts=getLegalActions('AI',s); var pv=acts.find(function(a){return a.card_id==='S1-ITM-011';}); if(pv && aiDeployActionScore(s,pv)>-999) return {ok:false, reason:'AI scored Poison Vial setup despite no remaining mana for attack'};
      s.aiMana=6; acts=getLegalActions('AI',s); pv=acts.find(function(a){return a.card_id==='S1-ITM-011';}); if(!pv || !aiCanFollowSetupWithAttack(s,pv)) return {ok:false, reason:'AI setup follow-up attack plan unavailable when mana is sufficient'};
      // Coordination Attack expiry.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-EVT-011']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!hasStatus(s.playerHeroes.LEFT,'Coordination Attack +20') && !hasStatus(s.playerHeroes.LEFT,'Coordination Attack +10')) return {ok:false, reason:'Coordination Attack buff did not apply'}; s.phase='Battle'; removeTemporaryBattleBuffsForSide(s,'PLAYER'); if(hasStatus(s.playerHeroes.LEFT,'Coordination Attack +20') || hasStatus(s.playerHeroes.LEFT,'Coordination Attack +10')) return {ok:false, reason:'Coordination Attack buff did not expire'};
      // Racial ability button/resolution.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.racial=2; s.mana=2; s.playerHeroes.LEFT.card_id='S1-MAG-H001'; var rac=getActivatedRacialAbilities(s,'PLAYER','LEFT'); if(!rac.length) return {ok:false, reason:'Racial ability not available in Deploy with token'}; resolveActivatedRacialAbility(s,'PLAYER','LEFT',rac[0].abilityId); if(s.racial!==1 || s.mana!==4 || s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Racial ability did not spend token/gain mana/non-exhaust correctly', racial:s.racial, mana:s.mana, exhausted:s.playerHeroes.LEFT.exhausted};
      // Legacy mapping follows Hero after reposition.
      s=buildInitialMatchState(); appState=s; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.playerHeroes.LEFT.assigned_legacy_card_id='S1-ARC-L001'; s.playerHeroes.LEFT.legacy_lineage_id='ARC-RANGER'; s.playerLegacy=['S1-ARC-L001','S1-THF-L001']; var tmp=s.playerHeroes.LEFT; s.playerHeroes.LEFT=s.playerHeroes.RIGHT; s.playerHeroes.RIGHT=tmp; s.playerHeroes.LEFT.lane='LEFT'; s.playerHeroes.RIGHT.lane='RIGHT'; s.playerHeroes.RIGHT.hp=0; applyDefeatCheck(s,'PLAYER','RIGHT'); if(s.playerHeroes.RIGHT.card_id!=='S1-ARC-L001') return {ok:false, reason:'Legacy mapping did not follow repositioned Archer', hero:s.playerHeroes.RIGHT}; restoreHeroFromLegacyMode(s,'PLAYER','RIGHT',10,'Phoenix Feather'); if(s.playerLegacy.indexOf('S1-ARC-L001')===-1) return {ok:false, reason:'Revive did not return assigned Legacy card to Legacy Deck'};
      // Status stack source detail.
      var h=s.aiHeroes.LEFT; h.statuses=[]; addStatus(h,'Poison',3,'Poison Mist',{source_name:'Poison Mist',amount:3}); addStatus(h,'Poison',2,'Poison Vial',{source_name:'Poison Vial',amount:2}); var det=statusDetailText(h.statuses[0]); if(poisonDurationOn(h)!==5 || !/Poison Mist/.test(det) || !/Poison Vial/.test(det)) return {ok:false, reason:'Stack source detail missing', detail:det, statuses:h.statuses};
      // Area Damage no target selection.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.mana=30; s.playerHeroes.CENTER.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-WAR-016']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); if(s.pending && s.pending.type==='target_selection') return {ok:false, reason:'Area Damage incorrectly entered target selection', pending:s.pending}; if(s.aiHeroes.LEFT.hp>=100 && s.aiHeroes.CENTER.hp>=100 && s.aiHeroes.RIGHT.hp>=100) return {ok:false, reason:'Area Damage did not resolve automatically', ai:s.aiHeroes};
      return {ok:true, aiSetupManaAware:true, roundCounter:true, handCountRule:true, coordinationExpires:true, racialAbilityButtonAndResolve:true, legacyMappingFollowsHero:true, stackSourceDetails:true, areaDamageNoTargetSelection:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); }
  }

  function simulatePrePvPFinalQAStabilization(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s;
      // Freeze blocks manual Reposition and Dodge.
      s.turn='PLAYER'; s.phase='Deploy'; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.LEFT.statuses=[]; addStatus(s.playerHeroes.LEFT,'Freeze',1,'Ice Lance');
      if(manualRepositionPairs(s).some(function(p){return p.indexOf('LEFT')!==-1;})) return {ok:false, reason:'Frozen Hero still appears in manual Reposition pairs', pairs:manualRepositionPairs(s)};
      s.playerHand=['S1-THF-003']; s.mana=30; s.phase='Battle'; s.responseWindow=null; s.pending=null; openAttackResponseWindow(s,{card_id:'S1-WAR-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'},{damage:20,persistent:false,attachmentSlot:-1});
      if((s.responseWindow.options||[]).some(function(o){return o.card_id==='S1-THF-003';})) return {ok:false, reason:'Frozen defender can still use Dodge response'};
      s.responseWindow=null; s.pending=null;
      // Debug/build footer removed from gameplay render HTML source.
      if(/Root index static app|Deck Setup starts first|Start-of-game shuffle active/.test(render.toString())) return {ok:false, reason:'debug footer text remains in render function'};
      // Primal Strike target gating: no injured opponent = no button/no token spend; injured opponent = target selection; token paid after target commit.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.racial=2; s.playerHeroes.RIGHT.card_id='S1-ARC-H001'; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=s.aiHeroes[l].maxHp;});
      var abilities=getActivatedRacialAbilities(s,'PLAYER','RIGHT'); if(abilities.some(function(a){return a.abilityId==='primal_strike';})) return {ok:false, reason:'Primal Strike available with no injured opponent'};
      var tok=s.racial; if(resolveActivatedRacialAbility(s,'PLAYER','RIGHT','primal_strike','LEFT') || s.racial!==tok) return {ok:false, reason:'Invalid Primal Strike spent token or resolved'};
      s.aiHeroes.CENTER.hp=Math.max(1,s.aiHeroes.CENTER.maxHp-10); abilities=getActivatedRacialAbilities(s,'PLAYER','RIGHT'); var ps=abilities.find(function(a){return a.abilityId==='primal_strike';}); if(!ps || ps.legal_targets.indexOf('CENTER')===-1) return {ok:false, reason:'Primal Strike missing injured target', abilities:abilities};
      beginActivatedRacialAbility(s,'PLAYER','RIGHT','primal_strike'); if(!s.pending || s.pending.type!=='racial_target_selection' || s.racial!==tok) return {ok:false, reason:'Primal Strike did not open target selection before payment', pending:s.pending, racial:s.racial};
      var before=s.aiHeroes.CENTER.hp; chooseHeroFromBoard('AI','CENTER'); if(s.racial!==tok-1 || s.aiHeroes.CENTER.hp!==before-20 || s.pending) return {ok:false, reason:'Primal Strike target commit failed', racial:s.racial, hp:s.aiHeroes.CENTER.hp, pending:s.pending};
      // Status detail clean source names only.
      addStatus(s.aiHeroes.CENTER,'Poison',2,'Poison Vial', {source_card_id:'S1-ITM-011', source_name:'Poison Vial', amount:2, note:'final HP damage > 0'}); var dt=statusDetailText(s.aiHeroes.CENTER.statuses.find(function(st){return statusName(st)==='Poison';})); if(/final HP damage|undefined|null|\[object Object\]/i.test(dt)) return {ok:false, reason:'Status detail leaked backend/source note', detail:dt};
      // Scouting validator.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-EVT-003']; LANE_ORDER.forEach(function(l){s.aiHeroes[l].exp_cards=[]; s.aiHeroes[l].exp_total=0;}); var lp=legalPlayStateFor(s,'PLAYER','S1-EVT-003'); if(lp.can) return {ok:false, reason:'Scouting playable with no opponent EXP'};
      s.aiHeroes.LEFT.exp_cards=['S1-WAR-001']; s.aiHeroes.LEFT.exp_total=100; lp=legalPlayStateFor(s,'PLAYER','S1-EVT-003'); if(!lp.can) return {ok:false, reason:'Scouting not playable with legal non-Ultimate EXP', reasons:lp.reasons};
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.type!=='scouting_target_selection') return {ok:false, reason:'Scouting did not open target Hero selection in regression test', pending:s.pending}; chooseHeroFromBoard('AI','LEFT'); if(!s.pending || s.pending.type!=='scouting_exp_selection') return {ok:false, reason:'Scouting did not open EXP selection in regression test', pending:s.pending}; selectScoutingExpChoice(0); commitScoutingExpChoice(); if(s.aiHeroes.LEFT.exp_cards.length!==0 || s.aiDiscard.indexOf('S1-WAR-001')===-1) return {ok:false, reason:'Scouting did not discard chosen legal EXP'};
      // Game end popup/state: empty Draw Phase and all active units defeated.
      s=buildInitialMatchState(); appState=s; s.playerDeck=[]; drawOne(s,'PLAYER',true); if(!s.gameOver || s.winner!=='AI') return {ok:false, reason:'Empty Draw Phase deck loss not marked gameOver', state:s};
      s=buildInitialMatchState(); appState=s; LANE_ORDER.forEach(function(l){s.aiHeroes[l].legacy_mode=true; s.aiHeroes[l].hp=0;}); checkGameEnd(s); if(!s.gameOver || s.winner!=='PLAYER') return {ok:false, reason:'No valid AI unit loss not marked gameOver'};
      return {ok:true, freezeBlocksRepositionAndDodge:true, debugFooterRemoved:true, primalStrikeValidator:true, statusStackNamesClean:true, scoutingValidator:true, gameEndPopupState:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); }
  }


  function simulatePhase3ArcherComplexPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=5; s.aiRacial=0; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-010']; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;});
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER'); if(s.pending && s.pending.type==='mana_spend_choice') return {ok:false, reason:'Arrow Barrage should spend all Mana automatically now', pending:s.pending}; if(s.mana!==0 || s.aiHeroes.CENTER.hp!==50 || s.pending || s.responseWindow) return {ok:false, reason:'Arrow Barrage all-mana spend/damage resolution failed', mana:s.mana, hp:s.aiHeroes.CENTER.hp, pending:s.pending, rw:s.responseWindow};
      // Same 5-Mana attack against a Dragonborn defender: incoming damage remains 50, then Dragon Scale blocks 40.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=5; s.aiRacial=2; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-010']; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.maxHp=100;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER');
      var barrageEvt=(s.playerPlayedEvents||[]).find(function(e){return e&&e.card_id==='S1-ARC-010';});
      if(s.mana!==0 || s.aiHeroes.CENTER.hp!==90 || s.aiRacial!==1 || !barrageEvt || !barrageEvt.damage_summary || Number(barrageEvt.damage_summary.damage_after_modifiers)!==50 || Number(barrageEvt.damage_summary.response_block)!==40 || Number(barrageEvt.damage_summary.actual_hp_damage)!==10) return {ok:false,reason:'Arrow Barrage / Dragon Scale audit failed',mana:s.mana,hp:s.aiHeroes.CENTER.hp,racial:s.aiRacial,event:barrageEvt};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.aiRacial=0; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-013','S1-ARC-002']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); if(!s.pending || s.pending.type!=='card_search_choice' || s.pending.resolve_to!=='triple_shot_bind') return {ok:false, reason:'Triple Shot bind choice missing', pending:s.pending}; s.pending.selected_index=0; commitCardSearchChoice(); if(!tripleShotStatusFor(s.playerHeroes.CENTER,'S1-ARC-002')) return {ok:false, reason:'Triple Shot did not bind chosen card', statuses:s.playerHeroes.CENTER.statuses, attachments:s.activeAttachments}; s.phase='Battle'; s.round=2; s.playerHeroes.CENTER.exhausted=false; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER'); if(s.aiHeroes.LEFT.hp>=100 || s.aiHeroes.CENTER.hp>=100 || s.aiHeroes.RIGHT.hp>=100) return {ok:false, reason:'Triple Shot Area conversion failed', ai:s.aiHeroes, statuses:s.playerHeroes.CENTER.statuses}; if(!tripleShotStatusFor(s.playerHeroes.CENTER,'S1-ARC-002')) return {ok:false, reason:'Triple Shot expired before end of turn', attachments:s.activeAttachments}; resolveEndPhaseCleanupForSide(s,'PLAYER'); if(tripleShotStatusFor(s.playerHeroes.CENTER,'S1-ARC-002') || s.playerDiscard.filter(function(id){return id==='S1-ARC-013';}).length!==1) return {ok:false, reason:'Triple Shot did not expire exactly once at end of turn', attachments:s.activeAttachments, discard:s.playerDiscard};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H003'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-017']; LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); if(!s.pending || s.pending.type!=='exact_two_target_selection' || (s.pending.legal_targets||[]).length!==3) return {ok:false, reason:'Dual Arrow individual target picker missing', pending:s.pending}; performDualArrowPairChoice('LEFT'); performDualArrowPairChoice('RIGHT'); performDualArrowPairChoice('confirm'); if(s.aiHeroes.LEFT.hp!==50 || s.aiHeroes.RIGHT.hp!==50 || s.aiHeroes.CENTER.hp!==100 || s.pending) return {ok:false, reason:'Dual Arrow chosen pair resolution failed', ai:s.aiHeroes, pending:s.pending};
      return {ok:true, arrowBarrageAllManaDamage:true, arrowBarrageDragonScaleAudit:true, tripleShotChosenCardBinding:true, dualArrowLanePairPicker:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); }
  }

  function simulatePhase4ChoiceUiPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-EVT-003']; s.aiHeroes.CENTER.exp_cards=['S1-WAR-001','S1-MAG-001']; s.aiHeroes.CENTER.exp_total=200;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.type!=='scouting_target_selection') return {ok:false, reason:'Scouting did not open target Hero selection', pending:s.pending};
      chooseHeroFromBoard('AI','CENTER'); if(!s.pending || s.pending.type!=='scouting_exp_selection' || (s.pending.exp_choices||[]).length!==2) return {ok:false, reason:'Scouting did not open EXP choice', pending:s.pending};
      selectScoutingExpChoice(1); commitScoutingExpChoice(); if(s.aiHeroes.CENTER.exp_cards.indexOf('S1-MAG-001')!==-1 || s.aiDiscard.indexOf('S1-MAG-001')===-1 || s.aiHeroes.CENTER.exp_cards.indexOf('S1-WAR-001')===-1) return {ok:false, reason:'Scouting selected EXP discard failed', exp:s.aiHeroes.CENTER.exp_cards, discard:s.aiDiscard};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerDeck=['S1-WAR-001','S1-MAG-001','S1-CLE-001','S1-THF-001']; s.playerHand=['S1-ITM-009']; beginPlayFromHand(0);
      if(!s.pending || s.pending.type!=='crystal_ball_reorder') return {ok:false, reason:'Crystal Ball did not open reorder pending', pending:s.pending};
      moveCrystalBallOrder('2:up'); moveCrystalBallOrder('1:up'); commitCrystalBallReorder();
      if(s.playerDeck[0]!=='S1-CLE-001' || s.playerDeck[1]!=='S1-WAR-001' || s.playerDeck[2]!=='S1-MAG-001' || s.playerDeck[3]!=='S1-THF-001') return {ok:false, reason:'Crystal Ball reorder failed', deck:s.playerDeck.slice(0,4)};
      return {ok:true, scoutingChooseHeroThenExp:true, crystalBallReorder:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); }
  }



  function simulatePhase7RacialAndSurrenderPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.racial=2;
      s.playerHeroes.LEFT.card_id='S1-WAR-H001';
      s.playerHeroes.CENTER.card_id='S1-ARC-H001';
      s.playerHeroes.RIGHT.card_id='S1-THF-H001';
      s.aiHeroes.CENTER.hp=Math.max(1, Number(s.aiHeroes.CENTER.maxHp||100)-10);
      var human=getActivatedRacialAbilities(s,'PLAYER','LEFT').map(function(a){return a.abilityId;});
      var beast=getActivatedRacialAbilities(s,'PLAYER','CENTER').map(function(a){return a.abilityId;});
      var half=getActivatedRacialAbilities(s,'PLAYER','RIGHT').map(function(a){return a.abilityId;});
      if(human.indexOf('human_ambition')===-1) return {ok:false, reason:'Human Ambition active racial button missing', human:human};
      if(beast.indexOf('primal_strike')===-1) return {ok:false, reason:'Beastman Primal Strike active racial button missing', beast:beast};
      if(half.length) return {ok:false, reason:'Halfling Second Chance must not render as Deploy button', half:half};
      s.playerHeroes.RIGHT.card_id='S1-CLE-H004';
      var dwarf=getActivatedRacialAbilities(s,'PLAYER','RIGHT').map(function(a){return a.abilityId;});
      if(dwarf.length) return {ok:false, reason:'Dwarf Stoneblood must not render as Deploy button', dwarf:dwarf};
      s.playerHeroes.RIGHT.card_id='S1-WAR-H004';
      var dragon=getActivatedRacialAbilities(s,'PLAYER','RIGHT').map(function(a){return a.abilityId;});
      if(dragon.length) return {ok:false, reason:'Dragon Scale must not render as Deploy button', dragon:dragon};
      s.phase='Battle'; s.round=2;
      if(getActivatedRacialAbilities(s,'PLAYER','LEFT').length) return {ok:false, reason:'Deploy racial button visible outside Deploy phase'};
      return {ok:true, humanAmbitionButton:true, beastmanPrimalStrikeButton:true, passiveAndResponseRacialsHidden:true, surrenderRequiresConfirmation:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }



  function simulatePhase5LegacyAbilitiesPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.playerHeroes.LEFT={card_id:'S1-WAR-L001', side:'PLAYER', lane:'LEFT', hp:1, maxHp:1, exhausted:false, exp_cards:[], exp_total:0, attachments:[null,null], statuses:[], legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-WAR-H001', hp:0, maxHp:100, statuses:[], attachments:[null,null]}, active_legacy_card_id:'S1-WAR-L001'}; s.playerHand=['S1-WAR-001']; s.playerDeck=['S1-MAG-001']; var a=getActivatedLegacyAbilities(s,'PLAYER','LEFT'); if(!a.length || a[0].abilityId!=='warriors_relic') return {ok:false, reason:'Warrior legacy ability unavailable', abilities:a}; beginActivatedLegacyAbility(s,'PLAYER','LEFT','warriors_relic'); if(!s.pending || s.pending.type!=='legacy_cost_selection') return {ok:false, reason:'Warrior legacy did not open cost choice', pending:s.pending, abilities:a}; s.pending.selected_cost_indices=[0]; commitLegacyCostChoice(); if(s.playerHand.indexOf('S1-MAG-001')===-1 || s.playerDiscard.indexOf('S1-WAR-001')===-1) return {ok:false, reason:'Warrior legacy draw/cost failed', hand:s.playerHand, discard:s.playerDiscard}; if(getActivatedLegacyAbilities(s,'PLAYER','LEFT').length) return {ok:false, reason:'Once/turn legacy gating failed'};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.mana=2; s.playerHeroes.CENTER={card_id:'S1-CLE-L002', side:'PLAYER', lane:'CENTER', hp:1, maxHp:1, exhausted:false, exp_cards:[], exp_total:0, attachments:[null,null], statuses:[], legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-CLE-H001', hp:0, maxHp:90, statuses:[], attachments:[null,null]}, active_legacy_card_id:'S1-CLE-L002'}; s.playerHeroes.RIGHT.hp=50; s.playerHeroes.RIGHT.maxHp=100; s.playerHand=['S1-CLE-001']; beginActivatedLegacyAbility(s,'PLAYER','CENTER','wand_of_first_light'); if(!s.pending || s.pending.type!=='legacy_cost_selection') return {ok:false, reason:'Wand did not open cost choice', pending:s.pending, abilities:getActivatedLegacyAbilities(s,'PLAYER','CENTER')}; s.pending.selected_cost_indices=[0]; commitLegacyCostChoice(); if(!s.pending || s.pending.type!=='legacy_hero_target_selection') return {ok:false, reason:'Wand did not open hero target', pending:s.pending}; chooseHeroFromBoard('PLAYER','RIGHT'); if(s.playerHeroes.RIGHT.hp!==70) return {ok:false, reason:'Wand heal wrong amount/buff leakage', hp:s.playerHeroes.RIGHT.hp};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.playerHeroes.LEFT={card_id:'S1-MAG-L002', side:'PLAYER', lane:'LEFT', hp:1, maxHp:1, exhausted:false, exp_cards:[], exp_total:0, attachments:[null,null], statuses:[], legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-MAG-H001', hp:0, maxHp:80, statuses:[], attachments:[null,null]}, active_legacy_card_id:'S1-MAG-L002'}; s.playerHand=['S1-MAG-001','S1-MAG-002']; s.playerDiscard=['S1-WAR-001','S1-WAR-021']; beginActivatedLegacyAbility(s,'PLAYER','LEFT','arcane_wand'); if(!s.pending || s.pending.type!=='legacy_cost_selection') return {ok:false, reason:'Arcane Wand did not open cost choice', pending:s.pending, abilities:getActivatedLegacyAbilities(s,'PLAYER','LEFT')}; s.pending.selected_cost_indices=[0,1]; commitLegacyCostChoice(); if(!s.pending || s.pending.type!=='legacy_card_choice') return {ok:false, reason:'Arcane Wand did not open discard choice', pending:s.pending}; selectLegacyCardChoice(0); commitLegacyCardChoice(); if(s.playerHand.indexOf('S1-WAR-001')===-1) return {ok:false, reason:'Arcane Wand return failed', hand:s.playerHand};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.playerHeroes.LEFT={card_id:'S1-ARC-L001', side:'PLAYER', lane:'LEFT', hp:1, maxHp:1, exhausted:false, exp_cards:[], exp_total:0, attachments:[null,null], statuses:[], legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-ARC-H001', hp:0, maxHp:80, statuses:[], attachments:[null,null]}, active_legacy_card_id:'S1-ARC-L001'}; s.playerHand=['S1-ARC-001']; s.aiMana=2; beginActivatedLegacyAbility(s,'PLAYER','LEFT','golden_arrows'); if(!s.pending || s.pending.type!=='legacy_cost_selection') return {ok:false, reason:'Golden Arrows did not open cost choice', pending:s.pending, abilities:getActivatedLegacyAbilities(s,'PLAYER','LEFT')}; s.pending.selected_cost_indices=[0]; commitLegacyCostChoice(); if(s.aiMana!==1) return {ok:false, reason:'Golden Arrows mana discard failed', aiMana:s.aiMana};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.playerHeroes.LEFT={card_id:'S1-MAG-L001', side:'PLAYER', lane:'LEFT', hp:1, maxHp:1, exhausted:false, exp_cards:[], exp_total:0, attachments:[null,null], statuses:[], legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-MAG-H001', hp:0, maxHp:80, statuses:[], attachments:[null,null]}, active_legacy_card_id:'S1-MAG-L001'}; s.playerHand=['S1-MAG-001']; s.playerHeroes.CENTER.hp=100; openAttackResponseWindow(s,{card_id:'S1-WAR-001',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'},{damage:30,persistent:false,attachmentSlot:-1}); var opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-MAG-L001';}); if(!opt) return {ok:false, reason:'Ancestral Tome response option missing', opts:s.responseWindow.options}; resolveResponseWindow(opt); if(s.playerHeroes.CENTER.hp!==90 || s.playerDiscard.indexOf('S1-MAG-001')===-1) return {ok:false, reason:'Ancestral Tome response reduce failed', hp:s.playerHeroes.CENTER.hp, discard:s.playerDiscard};
      return {ok:true, legacyDeployAbilities:true, legacyTargetChoice:true, legacyDiscardChoice:true, legacyBattleAbility:true, legacyReactiveResponse:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } catch(err){ return {ok:false, reason:String(err&&err.message||err)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); }
  }





  function simulatePhase8TextEffectBugfixPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      // Poison Vial must not amplify/directly source Poison Mist because Poison Mist is not an Attack and has no final HP damage gate.
      s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-THF-012'];
      s.playerHeroes.CENTER.card_id='S1-THF-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.statuses=[];
      addStatus(s.playerHeroes.CENTER,'Poison Vial',1,'Next attack this turn inflicts Poison only if final HP damage > 0.',{source_card_id:'S1-ITM-011',source_name:'Poison Vial',amount:2});
      LANE_ORDER.forEach(function(l){ s.aiHeroes[l].hp=100; s.aiHeroes[l].statuses=[]; });
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT');
      var pd=poisonDurationOn(s.aiHeroes.LEFT), detail=statusDetailText((s.aiHeroes.LEFT.statuses||[]).find(function(st){return statusName(st)==='Poison';})||{});
      if(pd!==0 || /Poison Vial/i.test(detail) || !hasStatus(s.playerHeroes.CENTER,'Poison Vial')) return {ok:false, reason:'Poison Vial affected Poison Mist or was consumed by non-attack', poison:pd, detail:detail, sourceStatuses:s.playerHeroes.CENTER.statuses};

      // Dragonborn Dragon Scale appears in Response Window and blocks 40 by paying 1 Racial Token.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.round=2; s.mana=30; s.racial=2; s.aiMana=30;
      s.playerHeroes.LEFT.card_id='S1-WAR-H004'; s.playerHeroes.LEFT.hp=90; s.playerHeroes.LEFT.maxHp=90; s.playerHeroes.LEFT.statuses=[];
      openAttackResponseWindow(s,{card_id:'S1-WAR-010',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'LEFT'},{damage:80,persistent:false,attachmentSlot:-1});
      var dragonOpt=(s.responseWindow.options||[]).find(function(o){ return o.response_kind==='dragon_scale'; });
      if(!dragonOpt) return {ok:false, reason:'Dragon Scale response option missing', options:s.responseWindow.options};
      resolveResponseWindow(dragonOpt);
      if(s.racial!==1 || s.playerHeroes.LEFT.hp!==50) return {ok:false, reason:'Dragon Scale did not pay/block correctly', racial:s.racial, hp:s.playerHeroes.LEFT.hp};

      // Cover Up should expose separate source choices when two adjacent Warrior-line Heroes can cover the same target.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.round=2; s.mana=30; s.playerHand=['S1-WAR-004'];
      s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.statuses=[];
      s.playerHeroes.CENTER.card_id='S1-MAG-H001'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.statuses=[];
      s.playerHeroes.RIGHT.card_id='S1-WAR-H004'; s.playerHeroes.RIGHT.hp=90; s.playerHeroes.RIGHT.statuses=[];
      openAttackResponseWindow(s,{card_id:'S1-MAG-001',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'},{damage:20,persistent:false,attachmentSlot:-1});
      var coverOpts=(s.responseWindow.options||[]).filter(function(o){ return o.response_kind==='cover_up'; });
      if(coverOpts.length!==2 || !coverOpts.every(function(o){ return /from LEFT|from RIGHT/.test(o.label||''); })) return {ok:false, reason:'Cover Up did not expose both Warrior source choices', options:coverOpts};

      // Player defeated Hero should create a Legacy selection pending state before replacement.
      s=buildInitialMatchState(); appState=s; s.playerHeroes.LEFT.hp=0; var candidates=eligibleLegacyCardIdsForDefeatedHero(s,'PLAYER','LEFT',s.playerHeroes.LEFT);
      if(!candidates.length) return {ok:false, reason:'No eligible Legacy candidate found for defeated Hero'};
      startLegacyDefeatChoice(s,'PLAYER','LEFT',s.playerHeroes.LEFT,candidates);
      if(!s.pending || s.pending.type!=='legacy_defeat_choice') return {ok:false, reason:'Legacy defeat choice pending not created', pending:s.pending};

      // Pending CAST resolving as ATK should leave the source exhausted after resolve.
      s=buildInitialMatchState(); appState=s; s.phase='Battle'; s.round=2; s.mana=30; s.playerHeroes.CENTER.card_id='S1-MAG-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=['S1-MAG-010',null]; s.aiHeroes.CENTER.hp=100;
      s.pendingCastings=[{card_id:'S1-MAG-010',side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER',attachmentSlot:0}];
      resolvePendingCastingsFor(s,'PLAYER');
      if(!s.playerHeroes.CENTER.exhausted) return {ok:false, reason:'CAST source did not exhaust after ATK resolve', hero:s.playerHeroes.CENTER};

      // Rank Up lineage follows the Hero object after reposition/swap, not the current lane package cache.
      s=buildInitialMatchState(); appState=s;
      s.playerHeroes.CENTER.card_id='S1-MAG-H001'; s.playerHeroes.CENTER.maxHp=80; s.playerHeroes.CENTER.hp=80; s.playerHeroes.CENTER.exp_total=300; s.playerHeroes.CENTER.exp_cards=['S1-MAG-001','S1-MAG-002'];
      s.playerHeroes.CENTER.assigned_legacy_card_id='S1-MAG-L001'; s.playerHeroes.CENTER.legacy_lineage_id='MAG-ELEMENTAL-LORD'; s.playerHeroes.CENTER.legacy_package_id='moved-mage';
      s.playerLegacy=['S1-WAR-H002','S1-WAR-H003','S1-MAG-H002','S1-MAG-H003']; s.playerDeck=['S1-MAG-003','S1-MAG-004','S1-MAG-005'];
      s.playerLegacyPackageSlots=[{slot:'CENTER',lineage:'WAR-CRUSADER',legacy:'S1-WAR-L002'}];
      var ranked=rankUpHero(s,'PLAYER','CENTER');
      if(!ranked || s.playerHeroes.CENTER.card_id!=='S1-MAG-H002') return {ok:false, reason:'Rank Up used lane package instead of Hero lineage', hero:s.playerHeroes.CENTER};

      return {ok:true, poisonVialDoesNotAffectPoisonMist:true, dragonScaleResponse:true, coverUpSourceChoice:true, legacyDefeatChoice:true, castResolveExhaust:true, rankUpLineageFollowsHero:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; }
  }

  function simulatePhase9AiStatusResultPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      // AI must not spend Poison Vial on a host Hero that cannot make the follow-up attack.
      s.turn='AI'; s.phase='Deploy'; s.aiMana=6; s.aiHand=['S1-ITM-011','S1-WAR-001'];
      s.aiHeroes.LEFT.card_id='S1-MAG-H001'; s.aiHeroes.LEFT.hp=80; s.aiHeroes.LEFT.exhausted=false;
      s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.exhausted=false;
      var pvLeft=getLegalActions('AI',s).find(function(a){ return a.card_id==='S1-ITM-011' && a.target_lane==='LEFT'; });
      if(pvLeft && aiCanFollowSetupWithAttack(s,pvLeft)) return {ok:false, reason:'AI still allows Poison Vial on a non-attacking host lane', action:pvLeft, plan:aiBestAttackAfterSetup(s,pvLeft)};
      var pvCenter=getLegalActions('AI',s).find(function(a){ return a.card_id==='S1-ITM-011' && a.target_lane==='CENTER'; });
      if(!pvCenter || !aiCanFollowSetupWithAttack(s,pvCenter)) return {ok:false, reason:'AI cannot find valid Poison Vial into same-lane attack when available', action:pvCenter};

      // Bleed duration should display and expire like the structured card definition says.
      s=buildInitialMatchState(); appState=s; addStatus(s.playerHeroes.LEFT,'Bleed',2,'Bleed · 2 turn(s)',{source_name:'Power Slash',amount:2});
      var panel=heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT);
      if(!/Bleed \(2\)/.test(panel)) return {ok:false, reason:'Bleed duration badge missing', panel:panel};
      tickStatusDurationsForSide(s,'PLAYER');
      if(!/Bleed \(1\)/.test(heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT))) return {ok:false, reason:'Bleed duration did not decrement in badge', statuses:s.playerHeroes.LEFT.statuses};
      tickStatusDurationsForSide(s,'PLAYER');
      if(hasStatus(s.playerHeroes.LEFT,'Bleed')) return {ok:false, reason:'Bleed did not expire at duration 0', statuses:s.playerHeroes.LEFT.statuses};

      // Game end should mark result popup as not-yet-shown so render/showResult can surface the modal once.
      s=buildInitialMatchState(); appState=s; finishGame(s,'PLAYER','AI has no valid active Hero or Legacy unit remaining.');
      if(!s.gameOver || s.winner!=='PLAYER' || s.gameResultShown!==false) return {ok:false, reason:'Game result popup state not armed', state:s};
      return {ok:true, aiPoisonVialFollowupPlanning:true, bleedDurationBadge:true, bleedExpires:true, gameResultPopupArmed:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; }
  }


  function simulatePhase10LegacyTargetPurifyResultPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      // Purify must only be playable when an allied active non-Legacy Hero has a negative status, and it must remove Bleed.
      s.turn='PLAYER'; s.phase='Deploy'; s.mana=10; s.playerHand=['S1-CLE-004'];
      s.playerHeroes.RIGHT.card_id='S1-CLE-H005'; s.playerHeroes.RIGHT.hp=110; s.playerHeroes.RIGHT.maxHp=110; s.playerHeroes.RIGHT.exhausted=false;
      s.playerHeroes.LEFT.statuses=[]; s.playerHeroes.CENTER.statuses=[]; s.playerHeroes.RIGHT.statuses=[];
      if(legalPlayState(s,'S1-CLE-004').can) return {ok:false, reason:'Purify legal with no negative status'};
      addStatus(s.playerHeroes.LEFT,'Bleed',2,'Bleed test');
      if(!legalPlayState(s,'S1-CLE-004').can) return {ok:false, reason:'Purify not legal with Bleed target', state:s};
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT');
      if(!s.pending || s.pending.type!=='target_selection' || s.pending.legal_targets.indexOf('LEFT')===-1) return {ok:false, reason:'Purify target validator did not include Bleed target', pending:s.pending};
      chooseHeroFromBoard('PLAYER','LEFT');
      if(hasStatus(s.playerHeroes.LEFT,'Bleed')) return {ok:false, reason:'Purify did not remove Bleed', statuses:s.playerHeroes.LEFT.statuses};

      // Legacy Mode is not a legal Attack target and all-Legacy side loses.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=10; s.playerHand=['S1-WAR-001'];
      LANE_ORDER.forEach(function(l){ s.aiHeroes[l].card_id='S1-WAR-L001'; s.aiHeroes[l].legacy_mode=true; s.aiHeroes[l].hp=1; s.aiHeroes[l].maxHp=1; });
      if(legalTargetLanesFor(s,'PLAYER','S1-WAR-001','CENTER').length) return {ok:false, reason:'Legacy appeared as legal attack target'};
      checkGameEnd(s);
      if(!s.gameOver || s.winner!=='PLAYER' || s.gameResultShown!==false) return {ok:false, reason:'All-Legacy opponent did not trigger game end', state:s};

      // Manual Reposition should still be available for adjacent board units when a Legacy is on the field.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.pending=null; s.responseWindow=null; s.gameOver=false;
      s.playerHeroes.LEFT.card_id='S1-WAR-L001'; s.playerHeroes.LEFT.legacy_mode=true; s.playerHeroes.LEFT.hp=1; s.playerHeroes.LEFT.maxHp=1; s.playerHeroes.LEFT.exhausted=false;
      s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.RIGHT.exhausted=false;
      var pairs=manualRepositionPairs(s).map(function(p){ return p.join('|'); });
      if(pairs.indexOf('CENTER|RIGHT')===-1) return {ok:false, reason:'Reposition pair missing while Legacy exists', pairs:pairs};

      // Legacy preview should show synthesized ability effect, not unavailable text.
      var prev=previewSections(card('S1-WAR-L001'));
      if(/Effect text unavailable/i.test(prev) || prev.indexOf('draw 1 card')===-1) return {ok:false, reason:'Legacy preview still missing effect', preview:prev};
      return {ok:true, purifyValidator:true, legacyNotAttackTarget:true, allLegacyGameEnd:true, repositionWithLegacyBoard:true, legacyPreviewEffect:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; }
  }


  function simulatePhase11BurnOpponentPlayedArrowPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      // Burn should add +10 damage only when the burned Hero actually takes HP damage.
      s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=30;
      s.playerHeroes.CENTER.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100;
      s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.maxHp=100; s.aiHeroes.CENTER.statuses=[];
      addStatus(s.aiHeroes.CENTER,'Burn',1,'Burn test',{source_name:'test',amount:1});
      var br=applyResolvedAttackDamage(s,'S1-WAR-001','PLAYER','CENTER','AI','CENTER',20,'Physical',false,0);
      if(br.base_damage!==20 || br.burn_damage!==10 || br.damage!==30 || s.aiHeroes.CENTER.hp!==70) return {ok:false, reason:'Burn +10 incoming damage failed', result:br, hp:s.aiHeroes.CENTER.hp};

      // Opponent Played detail should include resolved attack statuses.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.round=2;
      s.aiHeroes.CENTER.card_id='S1-MAG-H001'; s.aiHeroes.CENTER.hp=80; s.aiHeroes.CENTER.maxHp=80;
      s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.statuses=[];
      s.responseWindow={kind:'incoming_attack', response_owner:'PLAYER', card_id:'S1-MAG-003', source_side:'AI', source_lane:'CENTER', target_side:'PLAYER', target_lane:'CENTER', damage:30, damage_type:'Magical', attack_type:'Single Target', cannot_dodge:false, options:[], selected:null, post:{}, action:{card_id:'S1-MAG-003', source_side:'AI', source_lane:'CENTER', target_side:'PLAYER', target_lane:'CENTER'}};
      s.pending={type:'response_window', card_id:'S1-MAG-003'};
      resolveResponseWindow(null);
      var detail=opponentEventDetail(s.lastOpponentAction);
      if(!/Status result/i.test(detail) || !/Burn/i.test(detail)) return {ok:false, reason:'Opponent Played detail missing resolved status', detail:detail, statuses:s.playerHeroes.CENTER.statuses};

      // Arrow Barrage now spends all available Mana automatically after target validation.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=5; s.aiRacial=0;
      s.playerHeroes.RIGHT.card_id='S1-ARC-H003'; s.playerHeroes.RIGHT.hp=120; s.playerHeroes.RIGHT.maxHp=120; s.playerHeroes.RIGHT.exhausted=false;
      s.playerHand=['S1-ARC-010']; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.maxHp=100;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','CENTER');
      if(s.pending && s.pending.type==='mana_spend_choice') return {ok:false, reason:'Arrow Barrage should no longer open spend choice', pending:s.pending};
      if(s.mana!==0 || s.aiHeroes.CENTER.hp!==50 || s.pending || s.responseWindow) return {ok:false, reason:'Arrow Barrage all-mana spend did not resolve correctly', mana:s.mana, hp:s.aiHeroes.CENTER.hp, pending:s.pending, rw:s.responseWindow};
      // Untargetable should survive through opponent turn, then expire at start of owner next turn.
      s=buildInitialMatchState(); appState=s; addStatus(s.playerHeroes.CENTER,'Untargetable by Single Target Attacks',1,'test untargetable');
      tickStatusDurationsForSide(s,'PLAYER');
      if(!hasStatus(s.playerHeroes.CENTER,'Untargetable by Single Target Attacks')) return {ok:false, reason:'Untargetable expired too early at owner End cleanup'};
      removeStartOfTurnTargetPreventionForSide(s,'PLAYER');
      if(hasStatus(s.playerHeroes.CENTER,'Untargetable by Single Target Attacks')) return {ok:false, reason:'Untargetable did not expire at start of owner next turn'};
      return {ok:true, burnIncomingDamageBonus:true, opponentPlayedStatusResult:true, arrowBarrageAllManaDamage:true, untargetableStartTurnExpiry:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; }
  }



  function simulatePhase12CastingDefeatCancelPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=30;
      s.playerHeroes.CENTER.card_id='S1-CLE-H002'; s.playerHeroes.CENTER.hp=90; s.playerHeroes.CENTER.maxHp=90; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=['S1-CLE-014',null];
      s.playerHeroes.CENTER.legacy_lineage_id=heroLineageId(s.playerHeroes.CENTER);
      s.pendingCastings=[{card_id:'S1-CLE-014', side:'PLAYER', source_lane:'CENTER', source_hero_card_id:'S1-CLE-H002', target_side:'AI', target_lane:'CENTER', attachmentSlot:0, resolve_phase:'next_owner_battle'}];
      s.playerHeroes.CENTER={card_id:'S1-CLE-L001', side:'PLAYER', lane:'CENTER', hp:1, maxHp:1, exhausted:false, exp_cards:[], exp_total:0, attachments:['S1-CLE-014',null], statuses:[], legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-CLE-H002', hp:0, maxHp:90, statuses:[], attachments:['S1-CLE-014',null]}, active_legacy_card_id:'S1-CLE-L001'};
      var before=s.aiHeroes.CENTER.hp;
      resolvePendingCastingsFor(s,'PLAYER');
      if(s.responseWindow) return {ok:false, reason:'Defeated casting Hero opened a response window', responseWindow:s.responseWindow};
      if(s.aiHeroes.CENTER.hp!==before) return {ok:false, reason:'Defeated casting Hero dealt damage through Legacy overlay', before:before, after:s.aiHeroes.CENTER.hp};
      if((s.pendingCastings||[]).length) return {ok:false, reason:'Canceled casting was not removed from queue', pendingCastings:s.pendingCastings};
      if(s.playerHeroes.CENTER.attachments[0]) return {ok:false, reason:'Canceled casting attachment was not cleared from Legacy overlay', attachments:s.playerHeroes.CENTER.attachments};
      if(s.playerDiscard.indexOf('S1-CLE-014')===-1) return {ok:false, reason:'Canceled casting card did not move to discard', discard:s.playerDiscard};
      return {ok:true, defeatedCastingCanceled:true, noLegacyAttackerResponse:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }



  function simulatePhase13AreaRepositionResultPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      // Double Casting readiness is class-row specific: Elementalist Exhausts; Elemental Lord does not.
      s.turn='PLAYER'; s.phase='Deploy'; s.mana=20; s.playerHeroes.CENTER.card_id='S1-MAG-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; s.playerHand=['S1-MAG-018'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER');
      if(!s.playerHeroes.CENTER.exhausted) return {ok:false, reason:'Elementalist Double Casting did not Exhaust source Hero'};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=20; s.playerHeroes.CENTER.card_id='S1-MAG-H003'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=120; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; s.playerHand=['S1-MAG-018'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER');
      if(s.playerHeroes.CENTER.exhausted) return {ok:false, reason:'Elemental Lord Double Casting incorrectly Exhausted source Hero'};
      // Manual Reposition allows Hero ↔ Legacy, but only the Hero Exhausts. Legacy ↔ Legacy remains illegal.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy';
      transitionDefeatedHeroToLegacyMode(s,'PLAYER','LEFT','S1-WAR-L001',s.playerHeroes.LEFT);
      s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.CENTER.exhausted=false;
      if(!manualRepositionPairs(s).some(function(p){return p.join('|')==='LEFT|CENTER';})) return {ok:false, reason:'Hero ↔ Legacy Reposition pair missing', pairs:manualRepositionPairs(s)};
      performManualReposition('LEFT|CENTER');
      if(isLegacyModeHero(s.playerHeroes.LEFT) || !s.playerHeroes.LEFT.exhausted || !isLegacyModeHero(s.playerHeroes.CENTER) || s.playerHeroes.CENTER.exhausted) return {ok:false, reason:'Hero ↔ Legacy Reposition did not Exhaust only the Hero', heroes:s.playerHeroes};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; transitionDefeatedHeroToLegacyMode(s,'PLAYER','LEFT','S1-WAR-L001',s.playerHeroes.LEFT); transitionDefeatedHeroToLegacyMode(s,'PLAYER','CENTER','S1-WAR-L002',s.playerHeroes.CENTER);
      if(manualRepositionPairs(s).some(function(p){return p.join('|')==='LEFT|CENTER';})) return {ok:false, reason:'Legacy ↔ Legacy appeared in manual Reposition pairs', pairs:manualRepositionPairs(s)};
      // Auto Center 1v1 should move the last active non-Legacy Hero on each side to CENTER without Exhaust.
      s=buildInitialMatchState(); appState=s;
      transitionDefeatedHeroToLegacyMode(s,'PLAYER','LEFT','S1-WAR-L001',s.playerHeroes.LEFT); transitionDefeatedHeroToLegacyMode(s,'PLAYER','CENTER','S1-WAR-L002',s.playerHeroes.CENTER);
      transitionDefeatedHeroToLegacyMode(s,'AI','CENTER','S1-WAR-L001',s.aiHeroes.CENTER); transitionDefeatedHeroToLegacyMode(s,'AI','RIGHT','S1-THF-L001',s.aiHeroes.RIGHT);
      s.playerHeroes.RIGHT.exhausted=false; s.aiHeroes.LEFT.exhausted=false; checkGameEnd(s);
      if(isLegacyModeHero(s.playerHeroes.CENTER) || isLegacyModeHero(s.aiHeroes.CENTER) || s.playerHeroes.CENTER.exhausted || s.aiHeroes.CENTER.exhausted) return {ok:false, reason:'Auto Center 1v1 failed', player:s.playerHeroes, ai:s.aiHeroes};
      // AI Area Attack should create one Response Window per affected Hero.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.round=2; s.aiMana=30; s.aiHeroes.CENTER.card_id='S1-WAR-H002'; s.aiHeroes.CENTER.hp=120; s.aiHeroes.CENTER.maxHp=120; s.aiHeroes.CENTER.exhausted=false; s.aiHand=['S1-WAR-016'];
      commitPlayedCard(s,{side:'AI',card_id:'S1-WAR-016',hand_index:0,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:null});
      if(!s.responseWindow || !s.responseWindow.multi_sequence || s.responseWindow.target_lane!=='LEFT') return {ok:false, reason:'Whirlwind did not open first per-Hero response window', rw:s.responseWindow};
      resolveResponseWindow(null); if(!s.responseWindow || s.responseWindow.target_lane!=='CENTER') return {ok:false, reason:'Whirlwind did not advance to second response window', rw:s.responseWindow};
      resolveResponseWindow(null); if(!s.responseWindow || s.responseWindow.target_lane!=='RIGHT') return {ok:false, reason:'Whirlwind did not advance to third response window', rw:s.responseWindow};
      resolveResponseWindow(null); if(s.responseWindow || s.pending || s.aiDiscard.indexOf('S1-WAR-016')===-1) return {ok:false, reason:'Whirlwind sequence did not finalize after third window', rw:s.responseWindow, pending:s.pending, discard:s.aiDiscard};
      // AI should at least see legal Reposition actions when useful, including Hero ↔ Legacy but never Legacy ↔ Legacy.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Deploy'; s.aiHand=[]; s.playerHeroes.CENTER.hp=100;
      var reps=getLegalActions('AI',s).filter(function(a){return a.type==='REPOSITION';});
      if(!reps.length) return {ok:false, reason:'AI has no Reposition action generated'};
      transitionDefeatedHeroToLegacyMode(s,'AI','LEFT','S1-WAR-L001',s.aiHeroes.LEFT); reps=getLegalActions('AI',s).filter(function(a){return a.type==='REPOSITION';});
      if(!reps.some(function(a){return (a.pair||[]).join('|')==='LEFT|CENTER';})) return {ok:false, reason:'AI Hero ↔ Legacy Reposition pair missing', reps:reps};
      transitionDefeatedHeroToLegacyMode(s,'AI','CENTER','S1-WAR-L002',s.aiHeroes.CENTER); reps=getLegalActions('AI',s).filter(function(a){return a.type==='REPOSITION';});
      if(reps.some(function(a){return (a.pair||[]).join('|')==='LEFT|CENTER';})) return {ok:false, reason:'AI Reposition included Legacy ↔ Legacy pair', reps:reps};
      // Game Result popup should stay concise.
      finishGame(s,'PLAYER','AI loses: all 3 Heroes have been defeated.');
      var resultText=gameResultLines(s).join('\n\n');
      if(resultText.indexOf('Reason:')===-1 || resultText.indexOf('Active Heroes')!==-1 || resultText.indexOf('Legacy Deck')!==-1) return {ok:false, reason:'Game result wording is not concise', resultText:resultText};
      return {ok:true, doubleCastingNoExhaust:true, legacyPartnerReposition:true, autoCenterOneVsOne:true, whirlwindSequentialResponses:true, aiRepositionActions:true, conciseResult:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }

  function simulatePhase15AttachmentDrawExhaustPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; matchStarted=true;
      // Normal Attack Skill can stage in Attachment Slot during response, but after resolution it must discard and clear the slot.
      s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=30; s.playerHeroes.CENTER.card_id='S1-WAR-H002'; s.playerHeroes.CENTER.hp=120; s.playerHeroes.CENTER.maxHp=120; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; s.aiHeroes.CENTER.hp=120; s.aiHeroes.CENTER.maxHp=120; s.playerHand=['S1-WAR-008'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER'); if(!s.responseWindow) return {ok:false, reason:'Wild Swing response window missing', rw:s.responseWindow}; resolveResponseWindow(null); if((s.playerHeroes.CENTER.attachments||[]).some(Boolean) || s.playerDiscard.indexOf('S1-WAR-008')===-1) return {ok:false, reason:'Normal Attack Skill stayed in Attachment Slot after resolve', attachments:s.playerHeroes.CENTER.attachments, discard:s.playerDiscard};
      // Stale instant card references in Attachment Slot are cleaned on sync/render-safe state sync.
      s.playerHeroes.CENTER.attachments[0]='S1-WAR-008'; cleanupResolvedAttachmentSlots(s,'self-test'); if((s.playerHeroes.CENTER.attachments||[])[0]) return {ok:false, reason:'Stale normal attack attachment reference was not cleaned'};
      // Casting cards still use Attachment Slot while pending, then leave after resolve/cancel.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=30; s.playerHeroes.RIGHT.card_id='S1-MAG-H002'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.maxHp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.aiHeroes.RIGHT.hp=100; s.playerHand=['S1-MAG-007'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','RIGHT'); chooseHeroFromBoard('AI','RIGHT'); if((s.playerHeroes.RIGHT.attachments||[]).indexOf('S1-MAG-007')===-1 || !(s.pendingCastings||[]).some(function(pc){return pc.card_id==='S1-MAG-007';})) return {ok:false, reason:'Casting card did not remain in Attachment Slot while pending', attachments:s.playerHeroes.RIGHT.attachments, pendingCastings:s.pendingCastings}; cleanupResolvedAttachmentSlots(s,'protected check'); if((s.playerHeroes.RIGHT.attachments||[]).indexOf('S1-MAG-007')===-1) return {ok:false, reason:'Pending casting card was cleaned too early'};
      s.phase='Battle'; s.round=2; resolvePendingCastingsFor(s,'PLAYER'); if((s.playerHeroes.RIGHT.attachments||[]).indexOf('S1-MAG-007')!==-1 || s.playerDiscard.indexOf('S1-MAG-007')===-1) return {ok:false, reason:'Casting card did not leave Attachment Slot after resolve/cancel', attachments:s.playerHeroes.RIGHT.attachments, discard:s.playerDiscard};
      // Exhaust persists through End cleanup and clears only after owner Draw Phase resolves.
      s=buildInitialMatchState(); appState=s; s.playerHeroes.LEFT.exhausted=true; resolveEndPhaseCleanupForSide(s,'PLAYER'); if(!s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Exhaust cleared at End Phase, should persist'}; resolveDrawPhase(s,'PLAYER'); if(s.playerHeroes.LEFT.exhausted) return {ok:false, reason:'Exhaust did not clear after Draw Phase'};
      return {ok:true, normalAttackCleanup:true, castingAttachmentProtected:true, drawPhaseExhaustClear:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }


  function simulatePhase16ElixirLegacyIdentityPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-ITM-004']; s.playerHeroes.LEFT.statuses=[]; addStatus(s.playerHeroes.LEFT,'Poison',2,'test Poison'); addStatus(s.playerHeroes.LEFT,'Bleed',1,'test Bleed');
      beginPlayFromHand(0); if(!s.pending || s.pending.type!=='target_selection') return {ok:false, reason:'Elixir target selection missing', pending:s.pending}; chooseHeroFromBoard('PLAYER','LEFT'); if(!s.pending || s.pending.type!=='status_removal_choice' || (s.pending.status_choices||[]).length!==2) return {ok:false, reason:'Elixir did not open negative status choice', pending:s.pending}; selectStatusRemovalChoice(1); commitStatusRemovalChoice(); if(hasStatus(s.playerHeroes.LEFT,'Bleed') || !hasStatus(s.playerHeroes.LEFT,'Poison')) return {ok:false, reason:'Elixir did not remove chosen status only', statuses:s.playerHeroes.LEFT.statuses};
      s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='PLAYER'; s.phase='Deploy'; transitionDefeatedHeroToLegacyMode(s,'PLAYER','LEFT','S1-MAG-L001',s.playerHeroes.LEFT); if(!isLegacyModeHero(s.playerHeroes.LEFT)) return {ok:false, reason:'Ancestral Tome not recognized as Legacy Mode', hero:s.playerHeroes.LEFT}; var panel=heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT); if(panel.indexOf('Legacy Mode')<0 || panel.indexOf('Overlay:')<0) return {ok:false, reason:'Legacy overlay info missing from panel', panel:panel}; var pairs=repositionPairsForSide(s,'PLAYER'); if(!pairs.some(function(p){return p[0]==='LEFT'&&p[1]==='CENTER';})) return {ok:false, reason:'Hero ↔ Legacy partner reposition should remain legal', pairs:pairs}; transitionDefeatedHeroToLegacyMode(s,'PLAYER','CENTER','S1-WAR-L001',s.playerHeroes.CENTER); pairs=repositionPairsForSide(s,'PLAYER'); if(pairs.some(function(p){return p[0]==='LEFT'&&p[1]==='CENTER';})) return {ok:false, reason:'Legacy ↔ Legacy reposition must be illegal', pairs:pairs};
      return {ok:true, elixirChoice:true, robustLegacyIdentity:true, legacyOverlayInfo:true, legacyPartnerOnlyReposition:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }


  function simulatePhase17HeroSlotIdentityLock(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='PLAYER'; s.phase='Deploy';
      transitionDefeatedHeroToLegacyMode(s,'PLAYER','LEFT','S1-MAG-L001',s.playerHeroes.LEFT);
      var legacy=s.playerHeroes.LEFT;
      if(!isLegacySlot(legacy) || heroSlotMode(legacy)!=='LEGACY') return {ok:false, reason:'Legacy slot not recognized as LEGACY', legacy:legacy};
      if('hp' in legacy || 'maxHp' in legacy || 'exhausted' in legacy || 'statuses' in legacy || 'exp_total' in legacy || 'exp_cards' in legacy || 'attachments' in legacy) return {ok:false, reason:'Legacy slot still carries Hero-only fields', keys:Object.keys(legacy)};
      var panel=heroPanel('PLAYER','LEFT',legacy);
      if(panel.indexOf('HP ')!==-1 || panel.indexOf('State')!==-1 || panel.indexOf('Attachment Slot')!==-1 || panel.indexOf('Ready')!==-1 || panel.indexOf('Exhausted')!==-1) return {ok:false, reason:'Legacy panel still renders Hero-only UI', panel:panel};
      if(panel.indexOf('No HP')===-1 || panel.indexOf('Overlay:')===-1) return {ok:false, reason:'Legacy panel missing strict identity info', panel:panel};
      var pairs=repositionPairsForSide(s,'PLAYER');
      if(!pairs.some(function(p){ return p[0]==='LEFT'&&p[1]==='CENTER'; })) return {ok:false, reason:'Hero ↔ Legacy reposition should be legal', pairs:pairs};
      transitionDefeatedHeroToLegacyMode(s,'PLAYER','CENTER','S1-WAR-L001',s.playerHeroes.CENTER);
      pairs=repositionPairsForSide(s,'PLAYER');
      if(pairs.some(function(p){ return p[0]==='LEFT'&&p[1]==='CENTER'; })) return {ok:false, reason:'Legacy ↔ Legacy reposition must be illegal', pairs:pairs};
      s.turn='PLAYER'; s.phase='Deploy'; s.playerHand=['S1-WAR-001'];
      if(legalSourceLanesFor(s,'PLAYER','S1-WAR-001').indexOf('LEFT')!==-1) return {ok:false, reason:'Legacy slot became legal Skill source'};
      return {ok:true, slotModes:['HERO','LEGACY'], legacyHasNoHp:true, legacyPanelStrict:true, legacyPartnerReposition:true, legacyNotHeroSource:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }


  function simulatePhase18ResponseConfirmExpCapPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=false; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='AI'; s.phase='Battle'; s.round=2; s.aiMana=30; s.mana=30; s.aiHand=['S1-WAR-002']; s.playerHand=['S1-WAR-003']; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.hp=100;
      commitPlayedCard(s,{card_id:'S1-WAR-002',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});
      renderResponseWindow(); var select=document.querySelector('#responseOverlay [data-response-select="0"]'); if(!select) return {ok:false, reason:'Response Select missing'};
      select.click(); if(!s.responseWindow || s.responseWindow.selected!==0 || s.playerDiscard.indexOf('S1-WAR-003')!==-1) return {ok:false, reason:'Response Select resolved immediately instead of waiting for confirm', selected:s.responseWindow&&s.responseWindow.selected, discard:s.playerDiscard};
      var confirm=document.querySelector('#responseOverlay #responseConfirmButton'); if(!confirm || confirm.disabled) return {ok:false, reason:'Confirm Response missing/disabled after selection'};
      confirm.click(); if(s.responseWindow || s.pending || s.playerDiscard.indexOf('S1-WAR-003')===-1) return {ok:false, reason:'Confirm Response did not resolve selected defense', pending:s.pending, rw:s.responseWindow, discard:s.playerDiscard};
      s=buildInitialMatchState(); appState=s; matchStarted=true; s.turn='PLAYER'; s.phase='Reform'; s.playerHand=['S1-WAR-001']; s.playerHeroes.LEFT.exp_total=700; s.playerHeroes.LEFT.exp_cards=['S1-WAR-001','S1-WAR-002','S1-WAR-006','S1-WAR-015','S1-WAR-016','S1-WAR-017','S1-WAR-018']; var lp=legalTributeState(s,'S1-WAR-001'); if(lp.can || legalTributeTargetLanes(s,'S1-WAR-001').indexOf('LEFT')!==-1) return {ok:false, reason:'700 EXP Hero can still receive normal Tribute', tributeState:lp, lanes:legalTributeTargetLanes(s,'S1-WAR-001')};
      s.playerHeroes.LEFT.exp_total=650; lp=legalTributeState(s,'S1-WAR-001'); if(lp.can || legalTributeTargetLanes(s,'S1-WAR-001').indexOf('LEFT')!==-1) return {ok:false, reason:'Tribute that exceeds 700 EXP should be blocked', tributeState:lp, lanes:legalTributeTargetLanes(s,'S1-WAR-001')};
      s.playerHeroes.LEFT.exp_total=600; lp=legalTributeState(s,'S1-WAR-001'); if(!lp.can || legalTributeTargetLanes(s,'S1-WAR-001').indexOf('LEFT')===-1) return {ok:false, reason:'Tribute up to exactly 700 EXP should remain legal', tributeState:lp, lanes:legalTributeTargetLanes(s,'S1-WAR-001')};
      return {ok:true, responseSelectRequiresConfirm:true, expMaxBlocksTribute:true, expOverflowBlocksTribute:true, exact700Allowed:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }

  function simulatePhase19ValidatorOppPlayedSoulBlastPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.round=2; s.aiMana=30; s.mana=30;
      // Spectral Grappling Hook is Item DEF but only Archer/Thief defenders may use it.
      s.playerHand=['S1-ITM-012']; s.aiHand=['S1-ARC-015']; s.aiHeroes.LEFT.card_id='S1-ARC-H001'; s.aiHeroes.LEFT.exhausted=false;
      s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100;
      commitPlayedCard(s,{card_id:'S1-ARC-015',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});
      if((s.responseWindow.options||[]).some(function(o){ return o.card_id==='S1-ITM-012'; })) return {ok:false, reason:'Spectral Grappling Hook appeared for Warrior defender', options:s.responseWindow.options};
      resolveResponseWindow(null);
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle'; s.round=2; s.aiMana=30; s.mana=30;
      s.playerHand=['S1-ITM-012']; s.aiHand=['S1-ARC-015']; s.aiHeroes.LEFT.card_id='S1-ARC-H001'; s.aiHeroes.LEFT.exhausted=false;
      s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.maxHp=80;
      commitPlayedCard(s,{card_id:'S1-ARC-015',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});
      if(!(s.responseWindow.options||[]).some(function(o){ return o.card_id==='S1-ITM-012'; })) return {ok:false, reason:'Spectral Grappling Hook missing for Archer defender', options:s.responseWindow&&s.responseWindow.options};
      // Support/Tactical/Tick now appear in compact Opponent Played.
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Deploy'; s.aiMana=30; s.aiHeroes.RIGHT.card_id='S1-CLE-H005'; s.aiHeroes.RIGHT.exhausted=false; s.aiHeroes.LEFT.hp=50; s.aiHand=['S1-CLE-017']; commitPlayedCard(s,{card_id:'S1-CLE-017',hand_index:0,source_side:'AI',source_lane:'RIGHT',target_side:'AI',target_lane:'LEFT'});
      var events=opponentPlayedVisibleEvents(s); if(!events.some(function(e){ return e.label==='SUPPORT' && e.card_id==='S1-CLE-017'; })) return {ok:false, reason:'AI support card not visible in Opponent Played', events:events};
      s.aiHand=['S1-MAG-008']; s.aiHeroes.RIGHT.card_id='S1-MAG-H002'; s.aiHeroes.RIGHT.exhausted=false; s.playerHeroes.LEFT.statuses=[]; commitPlayedCard(s,{card_id:'S1-MAG-008',hand_index:0,source_side:'AI',source_lane:'RIGHT',target_side:'PLAYER',target_lane:'LEFT'});
      if(!opponentPlayedVisibleEvents(s).some(function(e){ return e.label==='TACTICAL' && e.card_id==='S1-MAG-008'; })) return {ok:false, reason:'AI tactical card not visible in Opponent Played', events:opponentPlayedVisibleEvents(s)};
      addStatus(s.playerHeroes.LEFT,'Poison',1,'test poison'); resolveStatusTicksForSide(s,'PLAYER');
      if(!opponentPlayedVisibleEvents(s).some(function(e){ return e.label==='RESOLVE' && /Status Tick/i.test(e.title||''); })) return {ok:false, reason:'Status tick resolve not visible in Opponent Played', events:opponentPlayedVisibleEvents(s)};
      // Soul Blast Shot hit opens force reposition decision and supports Hero ↔ Legacy.
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-016']; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.maxHp=100; s.aiHeroes.RIGHT=sanitizeLegacySlot({card_id:'S1-ARC-L001', side:'AI', lane:'RIGHT', mode:'LEGACY', legacy_mode:true, defeated_hero_snapshot:{card_id:'S1-ARC-H001'}}); beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER'); if(s.responseWindow) resolveResponseWindow(null); if(!s.pending || s.pending.type!=='optional_target_swap' || s.pending.swap_options.indexOf('RIGHT')===-1) return {ok:false, reason:'Soul Blast force reposition did not offer adjacent Legacy partner', pending:s.pending}; performOptionalTargetSwapDecision('RIGHT'); if(!isLegacySlot(s.aiHeroes.CENTER) || !isHeroSlot(s.aiHeroes.RIGHT)) return {ok:false, reason:'Soul Blast force reposition did not swap Hero and Legacy', center:s.aiHeroes.CENTER, right:s.aiHeroes.RIGHT};
      return {ok:true, spectralValidator:true, opponentPlayedSupportTacticalResolve:true, soulBlastForceReposition:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }


  function simulatePhase20PoisonVialMultihitPatch(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30;
      var src=s.playerHeroes.CENTER; src.card_id='S1-ARC-H003'; src.hp=100; src.maxHp=100; src.exhausted=false; src.statuses=[]; src.attachments=[null,null];
      s.playerHand=['S1-ITM-011'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER');
      if(!hasActiveAttachmentForHero(s,src,'S1-ITM-011') || src.attachments.indexOf('S1-ITM-011')<0 || hasStatus(src,'Poison Vial')) return {ok:false, reason:'Poison Vial did not enter authoritative Attachment state', source:src, active:s.activeAttachments};
      s.phase='Battle'; s.round=2; src.exhausted=false;
      LANE_ORDER.forEach(function(l){ s.aiHeroes[l].statuses=[]; s.aiHeroes[l].hp=100; s.aiHeroes[l].maxHp=100; });
      resolveMultiTargetAttack(s,{card_id:'S1-ARC-017', source_side:'PLAYER', source_lane:'CENTER', target_side:'AI', target_lanes:['LEFT','RIGHT']},{persistent:false, attachmentSlot:-1});
      if(!hasStatus(s.aiHeroes.LEFT,'Poison') || !hasStatus(s.aiHeroes.RIGHT,'Poison')) return {ok:false, reason:'Poison Vial did not apply to both Dual Arrow damaged targets', left:s.aiHeroes.LEFT.statuses, right:s.aiHeroes.RIGHT.statuses, source:src};
      if(hasStatus(s.aiHeroes.CENTER,'Poison')) return {ok:false, reason:'Poison Vial applied to an unaffected Dual Arrow lane', center:s.aiHeroes.CENTER.statuses};
      if(hasActiveAttachmentForHero(s,src,'S1-ITM-011') || src.attachments.indexOf('S1-ITM-011')>=0) return {ok:false, reason:'Poison Vial was not consumed exactly once after successful Dual Arrow multi-hit', source:src, active:s.activeAttachments};
      if(s.playerDiscard.filter(function(id){return id==='S1-ITM-011';}).length!==1) return {ok:false, reason:'Poison Vial did not move to Discard exactly once', discard:s.playerDiscard};
      return {ok:true, realAttachment:true, poisonVialDualArrowBothHit:true, consumedOnce:true, unaffectedLaneClean:true, uiKeyword:'LOCAL_AI_UI v5.35'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }


  function simulatePhase6RegressionCertificationCandidate(){
    var tests=[
      ['Phase 20 Poison Vial Multi-hit', simulatePhase20PoisonVialMultihitPatch],
      ['Phase 19 Validator/Opponent Played/Soul Blast', simulatePhase19ValidatorOppPlayedSoulBlastPatch],
      ['Phase 18 Response Confirm / EXP Cap', simulatePhase18ResponseConfirmExpCapPatch],
      ['Phase 17 Hero Slot Identity Lock', simulatePhase17HeroSlotIdentityLock],
      ['Phase 16 Elixir/Legacy Identity', simulatePhase16ElixirLegacyIdentityPatch],
      ['Phase 15 Attachment/Draw Exhaust', simulatePhase15AttachmentDrawExhaustPatch],
      ['Phase 13 Area/Reposition/Result', simulatePhase13AreaRepositionResultPatch],
      ['Phase 12 Casting Defeat Cancel', simulatePhase12CastingDefeatCancelPatch],
      ['Phase 11 Burn/Opponent Played/Arrow Barrage', simulatePhase11BurnOpponentPlayedArrowPatch],
      ['Phase 10 Legacy/Target/Purify/Result', simulatePhase10LegacyTargetPurifyResultPatch],
      ['Phase 9 AI/Status/Result', simulatePhase9AiStatusResultPatch],
      ['Phase 8 Text/Effect Bugfix', simulatePhase8TextEffectBugfixPatch],
      ['Phase 7 Racial/Surrender', simulatePhase7RacialAndSurrenderPatch],
      ['Phase 5 Legacy Abilities', simulatePhase5LegacyAbilitiesPatch],
      ['Phase 4 Choice UI', simulatePhase4ChoiceUiPatch],
      ['Phase 3 Archer Complex', simulatePhase3ArcherComplexPatch],
      ['Phase 2 Response Package', simulatePhase2ResponsePackagePatch],
      ['Pre-PvP Final QA Stabilization', simulatePrePvPFinalQAStabilization],
      ['Timing UX AI Cleanup', simulateTimingUxAiCleanup],
      ['Minor Bug Cleanup', simulateMinorBugCleanup]
    ];
    var results=tests.map(function(t){
      try { return {name:t[0], result:t[1]()}; }
      catch(err){ return {name:t[0], result:{ok:false, reason:String(err&&err.message||err)}}; }
    });
    var failed=results.filter(function(x){ return !x.result || x.result.ok!==true; });
    return {ok:failed.length===0, passed:results.length-failed.length, failed:failed.length, results:results, build:'Grandis Legacy Local AI v5.35', uiKeyword:'LOCAL_AI_UI v5.35'};
  }

  window.GL_PHASE20_POISON_VIAL_MULTIHIT_QA_SELF_TEST=simulatePhase20PoisonVialMultihitPatch;
  window.GL_PHASE19_VALIDATOR_OPPLAYED_SOULBLAST_QA_SELF_TEST=simulatePhase19ValidatorOppPlayedSoulBlastPatch;
  window.GL_PHASE18_RESPONSE_CONFIRM_EXP_CAP_QA_SELF_TEST=simulatePhase18ResponseConfirmExpCapPatch;
  window.GL_PHASE17_HERO_SLOT_IDENTITY_LOCK_QA_SELF_TEST=simulatePhase17HeroSlotIdentityLock;
  window.GL_PHASE16_ELIXIR_LEGACY_IDENTITY_QA_SELF_TEST=simulatePhase16ElixirLegacyIdentityPatch;
  window.GL_PHASE15_ATTACHMENT_DRAW_EXHAUST_QA_SELF_TEST=simulatePhase15AttachmentDrawExhaustPatch;
  window.GL_PHASE14_REPOSITION_RESULT_WORDING_QA_SELF_TEST=simulatePhase13AreaRepositionResultPatch;
  window.GL_PHASE13_AREA_REPOSITION_RESULT_QA_SELF_TEST=simulatePhase13AreaRepositionResultPatch;
  window.GL_PHASE12_CASTING_DEFEAT_CANCEL_QA_SELF_TEST=simulatePhase12CastingDefeatCancelPatch;
  window.GL_PHASE11_BURN_OPPONENT_PLAYED_ARROW_QA_SELF_TEST=simulatePhase11BurnOpponentPlayedArrowPatch;
  window.GL_PHASE10_LEGACY_TARGET_PURIFY_RESULT_QA_SELF_TEST=simulatePhase10LegacyTargetPurifyResultPatch;
  window.GL_PHASE9_AI_STATUS_RESULT_QA_SELF_TEST=simulatePhase9AiStatusResultPatch;
  window.GL_PHASE8_TEXT_EFFECT_BUGFIX_QA_SELF_TEST=simulatePhase8TextEffectBugfixPatch;
  window.GL_PHASE7_RACIAL_AND_SURRENDER_PATCH_QA_SELF_TEST=simulatePhase7RacialAndSurrenderPatch;
  window.GL_PHASE5_LEGACY_ABILITIES_PATCH_QA_SELF_TEST=simulatePhase5LegacyAbilitiesPatch;
  window.GL_PHASE6_REGRESSION_CERTIFICATION_CANDIDATE_SELF_TEST=simulatePhase6RegressionCertificationCandidate;
  window.GL_PHASE4_CHOICE_UI_PATCH_QA_SELF_TEST=simulatePhase4ChoiceUiPatch;
  window.GL_PHASE3_ARCHER_COMPLEX_PATCH_QA_SELF_TEST=simulatePhase3ArcherComplexPatch;
  window.GL_PRE_PVP_FINAL_QA_STABILIZATION_SELF_TEST=simulatePrePvPFinalQAStabilization;
  window.GL_RUNTIME_FLOW_SYNC_TIMING_UX_AI_CLEANUP_QA_SELF_TEST=simulateTimingUxAiCleanup;
  window.GL_RUNTIME_FLOW_SYNC_MINOR_BUG_CLEANUP_QA_SELF_TEST=simulateMinorBugCleanup;


  // PvP Shared Board Bridge v1.2
  // Exposes Local AI runtime state to the PvP room layer without making UI/network the rules authority.
  var __glPvpSilentRender=false;
  function glPvpSwapExactSide(v){ return v==='PLAYER'?'AI':(v==='AI'?'PLAYER':v); }
  function glPvpSwapTextSide(v){
    if(typeof v!=='string') return v;
    return v.replace(/\bPLAYER\b/g,'__GLP__').replace(/\bAI\b/g,'PLAYER').replace(/__GLP__/g,'AI');
  }
  function glPvpDeepSwapSides(value){
    if(Array.isArray(value)) return value.map(glPvpDeepSwapSides);
    if(value && typeof value==='object'){
      var out={}; Object.keys(value).forEach(function(k){ out[k]=glPvpDeepSwapSides(value[k]); }); return out;
    }
    if(value==='PLAYER'||value==='AI') return glPvpSwapExactSide(value);
    return value;
  }
  function glPvpSwapKeys(state){
    if(!state) return state;
    var s=glPvpDeepSwapSides(clone(state));
    function sw(a,b){ var t=s[a]; s[a]=s[b]; s[b]=t; }
    sw('mana','aiMana'); sw('manaRegen','aiManaRegen'); sw('racial','aiRacial');
    sw('playerDeck','aiDeck'); sw('playerHand','aiHand'); sw('playerDiscard','aiDiscard');
    sw('playerHeroes','aiHeroes'); sw('playerLegacy','aiLegacy');
    sw('playerLegacyPackageSlots','aiLegacyPackageSlots'); sw('playerDeckName','aiDeckName');
    sw('playerDeckCount','aiDeckCount'); sw('playerLegacyCount','aiLegacyCount');
    // Side string values were already swapped by glPvpDeepSwapSides; do not double-swap turn/winner.
    function swapSideMapKeys(map){ if(!map || typeof map!=='object') return; var __v=map.PLAYER; map.PLAYER=map.AI; map.AI=__v; }
    swapSideMapKeys(s.pvpPlayerNames);
    swapSideMapKeys(s.pvpActionEventsBySide);
    swapSideMapKeys(s.cardsDrawnThisTurn);
    swapSideMapKeys(s.lastDrawnCardBySide);
    if(Array.isArray(s.presentationEvents))s.presentationEvents=s.presentationEvents.map(function(e){var x=clone(e);x.side=glPvpSwapExactSide(x.side);return x;});
    if(Array.isArray(s.log)) s.log=s.log.map(glPvpSwapTextSide);
    if(s.lastOpponentAction){ s.lastOpponentAction=glPvpDeepSwapSides(s.lastOpponentAction); }
    return s;
  }
  function glPvpBridgeSnapshot(){
    return { matchStarted:!!matchStarted, appState:appState?clone(appState):null, version:GL_VERSION, exportedAt:new Date().toISOString() };
  }
  function glPvpRebindHeroCoordinates(state){
    if(!state) return state;
    [['PLAYER',state.playerHeroes],['AI',state.aiHeroes]].forEach(function(entry){ var side=entry[0], heroes=entry[1]||{}; LANE_ORDER.forEach(function(lane){ var h=heroes[lane]; if(h&&typeof h==='object'){ h.side=side; h.lane=lane; h.attachments=h.attachments||[null,null]; } }); });
    return state;
  }
  function glPvpEventIdSet(state){
    var out={}; if(!state) return out;
    ['PLAYER','AI'].forEach(function(side){ ((state.pvpActionEventsBySide||{})[side]||[]).forEach(function(e){if(e&&e.id)out[e.id]=true;}); });
    return out;
  }
  function glPvpHeroHpValue(hero){return hero&&Number.isFinite(Number(hero.hp))?Number(hero.hp):null;}
  function glPvpAllActionEvents(state){var out=[];['PLAYER','AI'].forEach(function(side){((state&&state.pvpActionEventsBySide||{})[side]||[]).forEach(function(e){if(e)out.push(e);});});return out.sort(function(a,b){return Number(b.timestamp||0)-Number(a.timestamp||0);});}
  function glPvpLatestAttackForTarget(state,side,lane){return glPvpAllActionEvents(state).find(function(e){var label=String(e.label||'').toUpperCase(),c=e.card_id&&card(e.card_id);return e.card_id&&c&&(label==='ATK'||label==='RESOLVE')&&isAttackCard(c)&&e.target_side===side&&(!lane||!e.target_lane||e.target_lane===lane);})||null;}
  function glPvpResponseOutcome(defEvent){if(!defEvent||!defEvent.card_id)return null;var kind=String(responseKindForCard(card(defEvent.card_id))||'').toLowerCase();if(kind.indexOf('dodge')!==-1)return'dodge';if(kind==='block'||kind==='area_reduce'||kind==='immunity'||kind==='legacy_reduce'||kind==='dragon_scale'||kind.indexOf('block')!==-1||kind.indexOf('reduce')!==-1)return'block';return null;}
  function glPvpImportBattleFeedback(previous,next,newEvents){
    if(!previous||!next)return[];
    var out=[],used={},newDefs=(newEvents||[]).filter(function(e){return String(e&&e.label||'').toUpperCase()==='DEF';});
    function addFeedback(side,lane,attack,outcome,hasDamage,playSound){if(!side||!lane||!attack||used[side+'|'+lane])return;var target=sideHeroes(next,side)[lane]||sideHeroes(previous,side)[lane];if(!target)return;used[side+'|'+lane]=true;out.push({kind:'attack',side:side,lane:lane,attack_kind:battleAttackVisualKind(attack.card_id),defense_kind:battleDefenseVisualKind(target),outcome:outcome||'hit',has_damage:!!hasDamage,play_sound:playSound!==false});}
    ['PLAYER','AI'].forEach(function(side){LANE_ORDER.forEach(function(lane){var before=sideHeroes(previous,side)[lane],after=sideHeroes(next,side)[lane],a=glPvpHeroHpValue(before),b=glPvpHeroHpValue(after);if(a===null||b===null||b>=a)return;var atk=glPvpLatestAttackForTarget(next,side,lane);if(!atk)return;var def=newDefs.find(function(e){return Number(e.timestamp||0)>=Number(atk.timestamp||0)&&e.source_side===side;});var outcome=glPvpResponseOutcome(def)||'hit';addFeedback(side,lane,atk,outcome,true,true);});});
    newDefs.forEach(function(def){var outcome=glPvpResponseOutcome(def);if(!outcome)return;var defendingSide=def.source_side==='AI'?'AI':'PLAYER',atk=glPvpAllActionEvents(next).find(function(e){var label=String(e.label||'').toUpperCase(),c=e.card_id&&card(e.card_id);return e.card_id&&c&&(label==='ATK'||label==='RESOLVE')&&isAttackCard(c)&&e.source_side!==defendingSide&&Number(e.timestamp||0)<=Number(def.timestamp||0);});if(!atk)return;var lane=atk.target_lane;if(!lane)return;var before=sideHeroes(previous,defendingSide)[lane],after=sideHeroes(next,defendingSide)[lane],a=glPvpHeroHpValue(before),b=glPvpHeroHpValue(after),damaged=a!==null&&b!==null&&b<a;addFeedback(defendingSide,lane,atk,outcome,damaged,true);});
    ['PLAYER','AI'].forEach(function(side){LANE_ORDER.forEach(function(lane){if(used[side+'|'+lane])return;var before=sideHeroes(previous,side)[lane],after=sideHeroes(next,side)[lane],a=glPvpHeroHpValue(before),b=glPvpHeroHpValue(after);if(a===null||b===null||b<=a)return;var healEvent=(newEvents||[]).find(function(e){var label=String(e&&e.label||'').toUpperCase();return (label==='SUPPORT'||label==='ABILITY'||label==='ITEM')&&(e.target_side===side||e.source_side===side)&&(!e.target_lane||e.target_lane===lane);});if(healEvent){used[side+'|'+lane]=true;out.push({kind:'heal',side:side,lane:lane,play_sound:true});}});});
    return out;
  }
  function glPvpImportAnimationPlan(previous,next){if(!next||!animationDocumentReady())return null;var known=glPvpEventIdSet(previous),events=[];['PLAYER','AI'].forEach(function(side){((next.pvpActionEventsBySide||{})[side]||[]).forEach(function(e){if(e&&e.id&&!known[e.id])events.push(e);});});events.sort(function(a,b){return Number(a.timestamp||0)-Number(b.timestamp||0);});var motions=[];events.forEach(function(e){if(!e.card_id||['TICK','RESOLVE','REPOSITION','ABILITY','RACIAL','LEGACY','RANK UP','TRIBUTE'].indexOf(String(e.label||'').toUpperCase())!==-1)return;var side=e.source_side==='AI'?'AI':'PLAYER',hand=sideHand(previous||{},side)||[],handIndex=hand.lastIndexOf(e.card_id);if(side==='PLAYER'&&handIndex<0)return;var snap=capturePlayedCardMotion(e.card_id,side,{hand_index:handIndex,source_side:side,source_lane:e.source_lane,target_side:e.target_side,target_lane:e.target_lane});if(snap&&snap.from)motions.push({snapshot:snap,side:side,card_id:e.card_id,source_lane:e.source_lane});});var previousDraw={};((previous&&previous.presentationEvents)||[]).forEach(function(e){if(e&&e.id)previousDraw[e.id]=true;});var drawEvents=((next.presentationEvents)||[]).filter(function(e){return e&&e.type==='CARD_DRAWN'&&!previousDraw[e.id];});var legacies=[];['PLAYER','AI'].forEach(function(side){LANE_ORDER.forEach(function(lane){var a=sideHeroes(previous||{},side)[lane],b=sideHeroes(next,side)[lane];if(b&&isLegacyModeHero(b)&&(!a||!isLegacyModeHero(a)||a.active_legacy_card_id!==b.active_legacy_card_id))legacies.push({side:side,lane:lane,card_id:b.active_legacy_card_id||b.card_id});});});var moved=[];if(previous)['PLAYER','AI'].forEach(function(side){var beforeById={},afterById={};LANE_ORDER.forEach(function(l){var a=sideHeroes(previous,side)[l],b=sideHeroes(next,side)[l];if(a&&a.card_id)beforeById[a.card_id]=l;if(b&&b.card_id)afterById[b.card_id]=l;});Object.keys(afterById).forEach(function(id){if(beforeById[id]&&beforeById[id]!==afterById[id])moved.push({side:side,lane:afterById[id]});});});var feedback=glPvpImportBattleFeedback(previous,next,events);return{motions:motions,drawEvents:drawEvents,legacies:legacies,moved:moved,feedback:feedback};}
  function glPvpRunImportAnimations(plan,state){if(!plan||!animationDocumentReady())return;(plan.motions||[]).forEach(function(m){var att=(state.activeAttachments||[]).find(function(a){return a&&a.card_id===m.card_id&&a.side===m.side&&(m.source_lane?a.lane===m.source_lane:true);});commitPlayedCardMotion(m.snapshot,!!att,att&&att.side,att&&att.lane,att&&att.slot);});if((plan.drawEvents||[]).length)queueDrawEvents(plan.drawEvents,state);(plan.legacies||[]).forEach(function(x){queueLegacyDeckToFieldMotion(x.side,x.lane,x.card_id);});(plan.moved||[]).forEach(function(x){var el=document.querySelector('.hero-panel[data-side="'+x.side+'"][data-lane="'+x.lane+'"]');if(el){el.classList.add('gl-pvp-reposition-flash');setTimeout(function(){el.classList.remove('gl-pvp-reposition-flash');},720);}});(plan.feedback||[]).forEach(function(evt){queueBattleFeedback(evt);});if((plan.feedback||[]).length)flushBattleFeedbackQueue();}
  function glPvpBridgeImport(payload, opts){
    opts=opts||{};
    if(!payload) return false;
    var sourceState=payload.appState||payload;
    if(!sourceState) return false;
    var previous=appState?clone(appState):null, incoming=glPvpRebindHeroCoordinates(clone(sourceState)), plan=(opts&&opts.skipImportAnimations)?null:glPvpImportAnimationPlan(previous,incoming);
    var previousResultKey=previous&&previous.gameOver?String(previous.winner||'')+'|'+String(previous.gameEndReason||''):'';
    var incomingResultKey=incoming&&incoming.gameOver?String(incoming.winner||'')+'|'+String(incoming.gameEndReason||''):'';
    if(incoming) incoming.gameResultShown=!!(previous&&previous.gameResultShown&&previousResultKey&&previousResultKey===incomingResultKey);
    __glPvpSilentRender=true;
    try{
      appState=incoming;
      matchStarted=payload.matchStarted!==false;
      if(appState){ if(opts.notice) pushLog(appState, opts.notice); syncCounts(appState); applyPvpLocalOpponentPlayedTimeline(appState); }
      render(); glPvpRunImportAnimations(plan,appState);
      return true;
    } finally {
      setTimeout(function(){ __glPvpSilentRender=false; }, 0);
    }
  }
  function glPvpApplyDeckChoice(side, key, importedDeck){
    if(importedDeck){
      var parsed=normalizeDeck(importedDeck);
      var val=validateDeck(parsed,side);
      if(!val.ok) throw new Error(val.errors[0]||'Invalid imported deck for '+side);
      selectedDeckKey[side]='IMPORTED';
      importedDecks[side]=val.deck;
      decks[side]=val.deck;
      return true;
    }
    if(key && STARTER_DECK_OPTIONS[key]){
      selectedDeckKey[side]=key;
      decks[side]=clone(STARTER_DECK_OPTIONS[key].deck);
      return true;
    }
    return false;
  }
  function glPvpBridgeStartSharedMatch(opts){
    initCards();
    opts=opts||{};
    glPvpApplyDeckChoice('PLAYER', opts.playerDeckKey, opts.playerDeck);
    glPvpApplyDeckChoice('AI', opts.player2DeckKey || opts.aiDeckKey, opts.player2Deck || opts.aiDeck);
    appState=buildInitialMatchState();
    matchStarted=true;
    if(appState){
      appState.pvpHumanVsHuman=true;
      appState.pvpPlayerNames={PLAYER:(opts.player1Name||'Player 1'), AI:(opts.player2Name||'Player 2')};
      pushLog(appState,'PvP Human vs Human match initialized on server runtime. Player 1 and Player 2 are both human-controlled.');
      syncCounts(appState);
    }
    render();
    return glPvpBridgeSnapshot();
  }
  function simulatePvpPopupOwnershipAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldShared=window.GL_PVP_SHARED_BOARD_ACTIVE, oldRole=window.GL_PVP_LOCAL_ROLE;
    try{
      window.GL_PVP_SHARED_BOARD_ACTIVE=true; window.GL_PVP_LOCAL_ROLE='player';
      appState=buildInitialMatchState(); matchStarted=true; appState.pvpHumanVsHuman=true;
      appState.pending={type:'hand_limit_discard', side:'AI', decision_side:'AI', required:1, selected:[]};
      if(pvpLocalOwnsPending(appState.pending)) return {ok:false, reason:'Remote hand limit pending is treated as local-owned.'};
      if(heroSelectionClass('PLAYER','LEFT')) return {ok:false, reason:'Remote pending still highlights local hero.'};
      appState.pending={type:'hand_limit_discard', side:'PLAYER', decision_side:'PLAYER', required:1, selected:[]};
      if(!pvpLocalOwnsPending(appState.pending)) return {ok:false, reason:'Local hand limit pending is not clickable.'};
      appState.responseWindow={kind:'incoming_attack', response_owner:'AI'};
      if(pvpLocalOwnsResponseWindow(appState.responseWindow)) return {ok:false, reason:'Remote response window treated as local-owned.'};
      appState.responseWindow={kind:'incoming_attack', response_owner:'PLAYER'};
      if(!pvpLocalOwnsResponseWindow(appState.responseWindow)) return {ok:false, reason:'Local response window is not clickable.'};
      return {ok:true, pvpPopupOnlyOwner:true, remotePopupHidden:true, localPopupClickable:true, subtitle:'Player vs Player (PvP)'};
    } finally { appState=oldApp; matchStarted=oldMatch; window.GL_PVP_SHARED_BOARD_ACTIVE=oldShared; window.GL_PVP_LOCAL_ROLE=oldRole; }
  }
  window.GL_PVP_POPUP_OWNERSHIP_AUDIT_SELF_TEST=simulatePvpPopupOwnershipAudit;

  function simulatePvpV116ActionSyncAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldShared=window.GL_PVP_SHARED_BOARD_ACTIVE;
    try{
      window.GL_PVP_SHARED_BOARD_ACTIVE=true;
      appState=buildInitialMatchState(); matchStarted=true; appState.pvpHumanVsHuman=true;
      appState.turn='PLAYER'; appState.phase='Deploy';
      openManualRepositionChoice(); cancelPendingAction();
      if(appState.pending) return {ok:false, reason:'manual Reposition cancel left pending'};
      appState=buildInitialMatchState(); matchStarted=true; appState.pvpHumanVsHuman=true; appState.turn='PLAYER'; appState.phase='Battle'; appState.mana=30; appState.aiMana=30;
      appState.aiHand=['S1-ITM-016'];
      openAttackResponseWindow(appState,{card_id:'S1-WAR-001',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'},{damage:30});
      if(!appState.responseWindow || !appState.responseWindow.options.some(function(o){return o.card_id==='S1-ITM-016';})) return {ok:false, reason:'AI side DEF response options missing in PvP'};
      responseSelectNoStuck(appState.responseWindow.options.findIndex(function(o){return o.card_id==='S1-ITM-016';})); confirmSelectedResponse();
      if(!((appState.pvpActionEventsBySide||{}).AI||[]).some(function(e){return e.label==='DEF' && e.card_id==='S1-ITM-016';})) return {ok:false, reason:'PvP DEF response was not recorded in opponent timeline'};
      appState=buildInitialMatchState(); matchStarted=true; appState.pvpHumanVsHuman=true; appState.playerHeroes.LEFT=transitionDefeatedHeroToLegacyMode(appState,'PLAYER','LEFT','S1-WAR-L001',appState.playerHeroes.LEFT)||appState.playerHeroes.LEFT; appState.playerHeroes.RIGHT=transitionDefeatedHeroToLegacyMode(appState,'PLAYER','RIGHT','S1-THF-L001',appState.playerHeroes.RIGHT)||appState.playerHeroes.RIGHT; appState.playerHeroes.CENTER.hp=0; applyDefeatCheck(appState,'PLAYER','CENTER');
      if(!appState.gameOver) return {ok:false, reason:'last active Hero defeat did not immediately end game'};
      return {ok:true, v116ActionSync:true, manualRepositionCancel:true, aiSideDefResponse:true, defTimeline:true, lastHeroLose:true};
    } finally { appState=oldApp; matchStarted=oldMatch; window.GL_PVP_SHARED_BOARD_ACTIVE=oldShared; }
  }
  window.GL_PVP_V117_ACTION_SYNC_AUDIT_SELF_TEST=simulatePvpV116ActionSyncAudit;

  function simulateCardPreviewRankSplitAudit(){
    initCards();
    var aether=previewSections(card('S1-MAG-023'));
    if(aether.indexOf('<h3>Spell Blade</h3>')<0) return {ok:false, reason:'Aether Slash preview missing Spell Blade section', preview:aether};
    if(aether.indexOf('<h3>Arcane Duelist</h3>')<0) return {ok:false, reason:'Aether Slash preview missing Arcane Duelist section', preview:aether};
    if(aether.indexOf('<h3>Effect</h3>')>=0) return {ok:false, reason:'Aether Slash preview still collapsed into generic Effect section', preview:aether};
    var fire=previewSections(card('S1-MAG-009'));
    if(fire.indexOf('<h3>Mage</h3>')<0 || fire.indexOf('<h3>Elementalist</h3>')<0) return {ok:false, reason:'Existing Fire Blast rank/class preview regressed', preview:fire};
    var xcross=previewSections(card('S1-THF-009'));
    if(xcross.indexOf('This attack cannot be dodged.')<0) return {ok:false, reason:'X Cross Slash latest wording missing from preview', preview:xcross};
    var execute=previewSections(card('S1-WAR-018'));
    if(execute.indexOf('This attack cannot be dodged.')<0) return {ok:false, reason:'Execute latest wording missing from preview', preview:execute};
    return {ok:true, aetherRankSplit:true, oldRankPreviewPreserved:true, latestWordingVisible:true};
  }
  window.GL_CARD_PREVIEW_RANK_SPLIT_AUDIT_SELF_TEST=simulateCardPreviewRankSplitAudit;

  function simulateV39NewCardRuntimeIntegrationAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true;
    try{
      decks.PLAYER=clone(STARTER_DECK_OPTIONS.starter_03_renegade_arcane_duelist_elemental_lord.deck);
      decks.AI=clone(STARTER_DECK_OPTIONS.starter_01_elemental_lord_conqueror_renegade.deck);
      appState=buildInitialMatchState(); matchStarted=true; appState.turn='PLAYER'; appState.phase='Battle'; appState.mana=99;
      appState.playerHeroes.LEFT.card_id='S1-THF-H004'; appState.playerHeroes.LEFT.exhausted=false;
      appState.playerHeroes.CENTER.card_id='S1-WAR-H003'; appState.playerHeroes.CENTER.exhausted=false;
      appState.playerHeroes.RIGHT.card_id='S1-THF-H006'; appState.playerHeroes.RIGHT.exhausted=false;
      appState.playerHand=['S1-THF-023'];
      var acts=getLegalActions('PLAYER',appState).filter(function(a){return a.card_id==='S1-THF-023';});
      if(!acts.length) return {ok:false, reason:'Flash Slash no legal actions'};
      if(!acts.every(function(a){return a.source_lane==='RIGHT';})) return {ok:false, reason:'Flash Slash source legality failed; non Arcane Duelist source accepted', actions:acts};
      if(!acts.every(function(a){return !!a.target_lane;})) return {ok:false, reason:'Flash Slash did not require a target lane', actions:acts};
      if(attackType(card('S1-THF-023'))!=='Single Target' || isAreaAttack(card('S1-THF-023'))) return {ok:false, reason:'Flash Slash attack type normalized incorrectly', attackType:attackType(card('S1-THF-023'))};
      if(!attackCannotBeDodged(card('S1-THF-023'))) return {ok:false, reason:'Flash Slash cannot-dodge flag missing'};
      appState.playerHand=['S1-THF-030']; appState.playerHeroes.RIGHT.card_id='S1-ARC-H006'; appState.playerHeroes.RIGHT.exhausted=false;
      acts=getLegalActions('PLAYER',appState).filter(function(a){return a.card_id==='S1-THF-030';});
      if(!(acts.length===1 && acts[0].source_lane==='RIGHT' && !acts[0].target_lane && isAreaAttack(card('S1-THF-030')))) return {ok:false, reason:'Crescent Moon Slash area/no-target lock failed', actions:acts, attackType:attackType(card('S1-THF-030'))};
      appState.playerHand=['S1-ARC-023']; appState.playerHeroes.RIGHT.card_id='S1-ARC-H006'; appState.playerHeroes.RIGHT.exhausted=false;
      acts=getLegalActions('PLAYER',appState).filter(function(a){return a.card_id==='S1-ARC-023';});
      if(!acts.length || !acts.every(function(a){return !!a.target_lane;})) return {ok:false, reason:'Cross Bolt should be target-required Range/Single Target attack', actions:acts};
      if(attackType(card('S1-ARC-023'))!=='Single Target' || isAreaAttack(card('S1-ARC-023'))) return {ok:false, reason:'Cross Bolt was treated as area or non-single', attackType:attackType(card('S1-ARC-023'))};
      appState.playerHand=['S1-MAG-020']; appState.playerHeroes.LEFT.card_id='S1-MAG-H001'; appState.playerHeroes.LEFT.exhausted=false; appState.playerHeroes.RIGHT.card_id='S1-THF-H006'; appState.playerHeroes.RIGHT.exhausted=false;
      acts=getLegalActions('PLAYER',appState).filter(function(a){return a.card_id==='S1-MAG-020';});
      if(!acts.length) return {ok:false, reason:'Aether Ball no legal actions'};
      if(!acts.some(function(a){return a.source_lane==='RIGHT';})) return {ok:false, reason:'Aether Ball not legal for Arcane Duelist/Spell Blade line', actions:acts};
      if(/Magical Attack/i.test(cardSubtype(card('S1-MAG-020')))) return {ok:false, reason:'Aether Ball must not be classified as Magical Attack', subtype:cardSubtype(card('S1-MAG-020'))};
      return {ok:true, flashSlashStrict:true, flashSlashSingleTarget:true, crescentArea:true, crossBoltSingleTarget:true, aetherBallCastingSpell:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }
  window.GL_V39_NEW_CARD_RUNTIME_INTEGRATION_AUDIT_SELF_TEST=simulateV39NewCardRuntimeIntegrationAudit;

  function simulateV341AetherAndHybridAbilityAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true;
    try{
      decks.PLAYER=clone(STARTER_DECK_OPTIONS.starter_03_renegade_arcane_duelist_elemental_lord.deck); decks.AI=clone(STARTER_DECK_OPTIONS.starter_01_elemental_lord_conqueror_renegade.deck);
      appState=buildInitialMatchState(); matchStarted=true; appState.turn='PLAYER'; appState.phase='Deploy'; appState.mana=10; appState.playerHeroes.CENTER.card_id='S1-THF-H006'; appState.playerHeroes.CENTER.exhausted=false; appState.playerHeroes.CENTER.attachments=[null,null]; appState.playerHand=['S1-MAG-022'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER');
      if(appState.playerHeroes.CENTER.attachments.indexOf('S1-MAG-022')<0) return {ok:false, reason:'Aether Infusion did not occupy Attachment Slot', attachments:appState.playerHeroes.CENTER.attachments};
      if(!hasStatus(appState.playerHeroes.CENTER,'Aether Infusion')) return {ok:false, reason:'Aether Infusion status missing', statuses:appState.playerHeroes.CENTER.statuses};
      appState.phase='Battle'; appState.mana=10; appState.aiMana=3; appState.playerHeroes.CENTER.exhausted=false; appState.playerHand=['S1-THF-020']; appState.aiHeroes.CENTER.hp=100;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER');
      if(!appState.pending || appState.pending.type!=='optional_magical_surge') return {ok:false, reason:'Arcane Surge did not open optional choice before resolving selected Magical Attack', pending:appState.pending, logs:appState.log.slice(-8)};
      if(appState.aiHeroes.CENTER.hp!==100) return {ok:false, reason:'Attack resolved before Arcane Surge choice', hp:appState.aiHeroes.CENTER.hp};
      commitMagicalSurgeChoice(true); if(appState.responseWindow) resolveResponseWindow(null);
      if(appState.aiHeroes.CENTER.hp!==20) return {ok:false, reason:'Arcane Surge/Aether conversion damage expected 80 from Sword Slash 60+20 after choosing yes', hp:appState.aiHeroes.CENTER.hp, logs:appState.log.slice(-8)};
      if(appState.mana!==4) return {ok:false, reason:'Arcane Surge should spend exactly 1 additional Mana after card cost', mana:appState.mana, logs:appState.log.slice(-8)};
      if(appState.aiMana!==2) return {ok:false, reason:'Aether Infusion did not remove opponent mana on hit', aiMana:appState.aiMana, logs:appState.log.slice(-8)};
      appState=buildInitialMatchState(); matchStarted=true; appState.turn='PLAYER'; appState.phase='Deploy'; appState.mana=10; appState.playerHeroes.CENTER.card_id='S1-THF-H006'; appState.playerHeroes.CENTER.exhausted=false; appState.playerHeroes.CENTER.attachments=[null,null]; appState.playerHand=['S1-MAG-022']; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER');
      appState.phase='Battle'; appState.mana=10; appState.aiMana=3; appState.playerHeroes.CENTER.exhausted=false; appState.playerHand=['S1-THF-020']; appState.aiHeroes.CENTER.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER');
      if(!appState.pending || appState.pending.type!=='optional_magical_surge') return {ok:false, reason:'Arcane Surge no-choice regression in no branch setup', pending:appState.pending};
      commitMagicalSurgeChoice(false); if(appState.responseWindow) resolveResponseWindow(null);
      if(appState.aiHeroes.CENTER.hp!==40) return {ok:false, reason:'Do Not Use Arcane Surge should keep converted Sword Slash at 60 damage', hp:appState.aiHeroes.CENTER.hp, logs:appState.log.slice(-8)};
      if(appState.mana!==5) return {ok:false, reason:'Do Not Use Arcane Surge should not spend extra Mana', mana:appState.mana, logs:appState.log.slice(-8)};
      var ls=card('S1-MAG-014'); if(cardSubtype(ls)!=='Range Attack' || attackType(ls)!=='Single Target' || isAreaAttack(ls)) return {ok:false, reason:'Lightning Strike still using old area/runtime data', subtype:cardSubtype(ls), attackType:attackType(ls)};
      return {ok:true, aetherAttachment:true, aetherConversion:true, arcaneSurgeOptionalChoice:true, noAutoSpend:true, lightningStrikeRuntimeV04:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }

  window.GL_V343_COST_TRIBUTE_ATTACHMENT_AUDIT_SELF_TEST=function(){
    try{
      matchStarted=true; var s=buildInitialMatchState(); appState=s;
      s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=4; s.playerHeroes.LEFT.card_id='S1-THF-H005'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=[null,null]; s.playerHand=['S1-MAG-023'];
      var lp=legalPlayState(s,'S1-MAG-023'); if(lp.can || lp.reasons.indexOf('not enough mana')===-1) return {ok:false, reason:'Aether Slash mana validator failed', lp:lp, cost:cardCost(card('S1-MAG-023'))};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.playerHand=['S1-CLE-024']; LANE_ORDER.forEach(function(l){ s.playerHeroes[l].hp=100; s.playerHeroes[l].exp_total=0; s.playerHeroes[l].exp_cards=[]; });
      var lanes=legalTributeTargetLanesFor(s,'PLAYER','S1-CLE-024'); if(lanes.length!==3) return {ok:false, reason:'Divine Judgement tribute target lanes wrongly restricted', lanes:lanes};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=10; s.playerHeroes.LEFT.card_id='S1-ARC-H005'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=[null,null]; s.playerHand=['S1-THF-027'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); if(s.playerHeroes.LEFT.attachments.indexOf('S1-THF-027')<0) return {ok:false, reason:'Camouflage did not occupy Attachment Slot', attachments:s.playerHeroes.LEFT.attachments, statuses:s.playerHeroes.LEFT.statuses};
      var html=heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT); if(/Untargetable by Single Target Attacks/.test(html)) return {ok:false, reason:'Camouflage long badge still shown instead of attachment slot', html:html.slice(0,500)};
      return {ok:true, checked:['aether_slash_cost_6','divine_judgement_tribute_unrestricted','camouflage_attachment_slot']};
    }catch(e){ return {ok:false, error:String(e&&e.stack||e)}; }
  };


  window.GL_V344_VALIDATOR_AND_RESPONSE_AUDIT_SELF_TEST=function(){
    try{
      matchStarted=true; var oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; var s=buildInitialMatchState(); appState=s;
      s.turn='PLAYER'; s.phase='Deploy'; s.mana=10; s.playerHeroes.CENTER.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.hp=90; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; s.playerHeroes.LEFT.card_id='S1-THF-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=[null,null]; s.playerHand=['S1-ITM-011']; var lanes=legalTargetLanesFor(s,'PLAYER','S1-ITM-011',null); if(lanes.indexOf('CENTER')!==-1 || lanes.indexOf('LEFT')===-1) return {ok:false, reason:'Poison Vial host validator failed', lanes:lanes};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=10; s.aiMana=10; s.playerHand=['S1-MAG-004','S1-MAG-005']; s.playerHeroes.CENTER.card_id='S1-MAG-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.exhausted=false; openAttackResponseWindow(s,{card_id:'S1-WAR-001',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'},{damage:40,persistent:false,attachmentSlot:-1}); var ids=(s.responseWindow.options||[]).map(function(o){return o.card_id;}); if(ids.indexOf('S1-MAG-004')!==-1 || ids.indexOf('S1-MAG-005')===-1) return {ok:false, reason:'Physical incoming response validator failed for Mana Shield/Mirror Image', ids:ids};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=10; s.playerHand=['S1-MAG-004']; s.playerHeroes.CENTER.card_id='S1-MAG-H002'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.exhausted=false; openAttackResponseWindow(s,{card_id:'S1-MAG-001',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'},{damage:40,persistent:false,attachmentSlot:-1}); var opt=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-MAG-004';}); if(!opt || responseBlockAmount(card('S1-MAG-004'),s.responseWindow,s.playerHeroes.CENTER)!==40) return {ok:false, reason:'Mana Shield magical block override failed', opts:s.responseWindow.options};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.racial=2; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.playerHeroes.LEFT.hp=80; s.playerHeroes.LEFT.maxHp=80; s.aiHeroes.CENTER.hp=70; s.aiHeroes.CENTER.maxHp=100; var rac=getActivatedRacialAbilities(s,'PLAYER','LEFT'); if(!rac.length) return {ok:false, reason:'Racial ability not initially available'}; resolveActivatedRacialAbility(s,'PLAYER','LEFT',rac[0].abilityId,'CENTER'); if(getActivatedRacialAbilities(s,'PLAYER','LEFT').length) return {ok:false, reason:'Racial ability can be used twice by same Hero in same turn'};
      return {ok:true, poisonVialLineage:true, responseDamageTypeValidator:true, manaShieldLatest:true, racialOncePerHeroPerTurn:true};
    }catch(e){ return {ok:false,error:String(e&&e.stack||e)}; } finally { SUPPRESS_RENDER=false; }
  };

  window.GL_V342_AETHER_AND_HYBRID_ABILITY_AUDIT_SELF_TEST=simulateV341AetherAndHybridAbilityAudit;
  window.GL_V341_AETHER_AND_HYBRID_ABILITY_AUDIT_SELF_TEST=simulateV341AetherAndHybridAbilityAudit;


  window.GL_V346_REPOSITION_LINEAGE_RANKUP_AUDIT_SELF_TEST=function(){
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true; matchStarted=true;
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform'; s.mana=5; s.manaRegen=1;
      s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exp_total=200; s.playerHeroes.LEFT.exp_cards=['S1-WAR-001','S1-WAR-002']; s.playerHeroes.LEFT.exhausted=false;
      s.playerHeroes.CENTER.card_id='S1-MAG-H001'; s.playerHeroes.CENTER.hp=80; s.playerHeroes.CENTER.maxHp=80; s.playerHeroes.CENTER.exp_total=0; s.playerHeroes.CENTER.exp_cards=[]; s.playerHeroes.CENTER.exhausted=false;
      s.playerLegacy=['S1-WAR-H002','S1-WAR-H003','S1-MAG-H002','S1-MAG-H003'];
      s.playerLegacyPackageSlots=[{slot:'LEFT',progression:'S1-WAR-H001',lineage:'WAR-CONQUEROR'},{slot:'CENTER',progression:'S1-MAG-H001',lineage:'MAG-ELEMENTAL-LORD'}];
      appState.pending={type:'manual_reposition', decision_side:'PLAYER', source_side:'PLAYER', side:'PLAYER', pairs:[['LEFT','CENTER']]};
      performManualReposition('LEFT|CENTER');
      if(s.playerHeroes.CENTER.card_id!=='S1-WAR-H001') return {ok:false, reason:'Hero object did not move to CENTER', heroes:s.playerHeroes};
      var centerSlot=legacyPackageSlotForLane(s,'PLAYER','CENTER');
      if(!centerSlot || centerSlot.progression!=='S1-WAR-H001') return {ok:false, reason:'Package progression/lineage did not follow moved Hero', slots:s.playerLegacyPackageSlots};
      s.playerHeroes.CENTER.exhausted=false; s.tributeUsedThisReform=false; s.playerHand=['S1-WAR-006'];
      beginTributeFromHand(0); chooseHeroFromBoard('PLAYER','CENTER');
      if(heroRank(s.playerHeroes.CENTER)!==2 || s.playerHeroes.CENTER.card_id!=='S1-WAR-H002') return {ok:false, reason:'Rank Up after Reposition used wrong lane/progression or failed', hero:s.playerHeroes.CENTER, slots:s.playerLegacyPackageSlots, legacy:s.playerLegacy};
      return {ok:true, repositionMovesPackageSlot:true, rankUpAfterReposition:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  };
  function simulateV365RuntimeSyncAudit(){
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    try{
      var stack=window.GL_SOURCE_STACK||{};
      if(stack.runtime_data!=='v0.12.7' || stack.effect_checkpoint!=='v0.11.6' || stack.effect_recipe!=='v0.11.7' || stack.runtime_core!=='v0.50' || stack.runtime_foundation!=='v1.82' || stack.shared_manual!=='v1.39' || stack.local_ai!=='v6.1') return {ok:false, reason:'Source stack metadata mismatch', stack:stack};
      if(CARDS.length!==198) return {ok:false, reason:'Season 1 card count mismatch', count:CARDS.length};
      var uncovered=CARDS.filter(function(c){ return !(c.card_text || c.effect_text || (Array.isArray(c.effect)&&c.effect.length) || c.attack || c.ability || c.class_ability || c.racial_ability || c.runtime_mode_rules); }).map(function(c){return c.card_id;});
      if(uncovered.length) return {ok:false, reason:'Cards without readable/executable runtime coverage', uncovered:uncovered};
      if(requiresAttachmentSlot(card('S1-CLE-004')) || requiresAttachmentSlot(card('S1-ARC-004')) || requiresAttachmentSlot(card('S1-THF-028'))) return {ok:false, reason:'Instant support/hand card incorrectly requires persistent Attachment Slot'};
      if(!requiresAttachmentSlot(card('S1-THF-027')) || !requiresAttachmentSlot(card('S1-ARC-021')) || !requiresAttachmentSlot(card('S1-CLE-025'))) return {ok:false, reason:'Persistent/casting card lost Attachment Slot requirement'};
      if(!isHolySlashCard(card('S1-CLE-020')) || !isSacredBulwarkCard(card('S1-CLE-022')) || !isBlessingDivinityCard(card('S1-CLE-025'))) return {ok:false, reason:'Current Cleric/Paladin card ID mapping mismatch'};

      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Reform';
      var priest=s.playerHeroes.RIGHT; priest.card_id='S1-CLE-H002'; priest.hp=50; priest.maxHp=100; priest.statuses=[];
      var saint=clone(priest); saint.card_id='S1-CLE-H003';
      if(firstHealAmountForSource(card('S1-CLE-016'),priest)!==30 || firstHealAmountForSource(card('S1-CLE-016'),saint)!==50) return {ok:false, reason:'Sanctuary class-row heal payload mismatch', priest:firstHealAmountForSource(card('S1-CLE-016'),priest), saint:firstHealAmountForSource(card('S1-CLE-016'),saint)};
      var holyLight=card('S1-CLE-023'), holyLegal=((holyLight.requirement&&holyLight.requirement.legal_active_classes)||[]).map(function(x){return String(x);}); if(!isHealCard(holyLight) || holyLegal.indexOf('Priest')<0 || holyLegal.indexOf('Saint')<0 || holyLegal.indexOf('Cleric')<0 || holyLegal.indexOf('Paladin')<0 || holyLegal.indexOf('Crusader')<0) return {ok:false, reason:'Holy Light current legal class lock mismatch', legal:holyLegal}; var holyCleric=clone(priest), holyPaladin=clone(priest); holyCleric.card_id='S1-CLE-H001'; holyPaladin.card_id='S1-WAR-H005'; if(firstHealAmountForSource(holyLight,holyCleric)!==50 || firstHealAmountForSource(holyLight,holyPaladin)!==60) return {ok:false, reason:'Holy Light Cleric/Paladin branch payload not recognized', cleric:firstHealAmountForSource(holyLight,holyCleric), paladin:firstHealAmountForSource(holyLight,holyPaladin)};
      if(reviveHpFromCard(card('S1-CLE-015'))!==20) return {ok:false, reason:'Resurrection HP is not 20'};

      var paladin=s.playerHeroes.CENTER; paladin.card_id='S1-WAR-H005'; paladin.hp=40; paladin.maxHp=100; paladin.statuses=[];
      applySourcePostAttackEffects(s,'S1-CLE-020',paladin,'PLAYER','CENTER','AI','CENTER',{damage:0});
      if(paladin.hp!==40) return {ok:false, reason:'Holy Slash healed without HP damage', hp:paladin.hp};
      applySourcePostAttackEffects(s,'S1-CLE-020',paladin,'PLAYER','CENTER','AI','CENTER',{damage:10});
      if(paladin.hp!==60) return {ok:false, reason:'Holy Slash did not heal 20 after HP damage', hp:paladin.hp};

      LANE_ORDER.forEach(function(l){ var h=s.playerHeroes[l]; h.hp=40; h.maxHp=100; h.statuses=[]; });
      priest=s.playerHeroes.RIGHT; priest.card_id='S1-CLE-H002';
      applySourcePostAttackEffects(s,'S1-CLE-010',priest,'PLAYER','RIGHT','AI',null,{damage:0});
      if(LANE_ORDER.some(function(l){return s.playerHeroes[l].hp<=40;})) return {ok:false, reason:'Holy Blast did not heal independently when damage was 0', hp:LANE_ORDER.map(function(l){return s.playerHeroes[l].hp;})};
      applySourcePostAttackEffects(s,'S1-CLE-010',priest,'PLAYER','RIGHT','AI',null,{damage:20});
      if(LANE_ORDER.some(function(l){return s.playerHeroes[l].hp<=40;})) return {ok:false, reason:'Holy Blast did not heal all allied Heroes after HP damage', hp:LANE_ORDER.map(function(l){return s.playerHeroes[l].hp;})};

      var cleanse=s.playerHeroes.LEFT; cleanse.statuses=[]; addStatus(cleanse,'Poison',2,'test'); addStatus(cleanse,'Decay',2,'test');
      var neg=negativeStatusChoices(cleanse); if(neg.length!==2) return {ok:false, reason:'Purify/Elixir negative status choices incomplete', choices:neg};
      removeChosenNegativeStatusFromHero(s,cleanse,card('S1-CLE-004'),neg[1].status_index);
      if(hasStatus(cleanse,'Decay') || !hasStatus(cleanse,'Poison')) return {ok:false, reason:'Chosen negative status removal failed', statuses:cleanse.statuses};

      s=buildInitialMatchState(); appState=s; s.aiHand=['S1-WAR-001','S1-MAG-001','S1-CLE-001']; s.aiDiscard=[]; s.playerDiscard=[]; s.magicScopeReveal=null;
      startCalculatedPlanChoice(s,{side:'PLAYER',source_card_id:'S1-THF-028',resolve_to:'opponent_discard'});
      if(!s.pending || s.pending.type!=='opponent_hand_choice' || s.pending.reveal_cards) return {ok:false, reason:'Calculated Plan did not open masked back-of-card choice', pending:s.pending};
      selectOpponentHandChoice(1); commitOpponentHandChoice();
      if(s.aiHand.length!==2 || s.aiDiscard.indexOf('S1-MAG-001')<0 || s.playerDiscard.indexOf('S1-THF-028')<0) return {ok:false, reason:'Calculated Plan selected discard flow failed', aiHand:s.aiHand, aiDiscard:s.aiDiscard, playerDiscard:s.playerDiscard};

      s=buildInitialMatchState(); appState=s; s.aiHand=['S1-WAR-001','S1-MAG-001','S1-CLE-001']; s.aiDeck=[]; s.playerDiscard=[]; s.magicScopeReveal={viewer_side:'PLAYER',opponent_side:'AI'};
      startCalculatedPlanChoice(s,{side:'PLAYER',source_card_id:'S1-ARC-004',resolve_to:'opponent_deck'});
      if(!s.pending || !s.pending.reveal_cards) return {ok:false, reason:'Magic Scope reveal was not honored before hand choice', pending:s.pending};
      selectOpponentHandChoice(0); commitOpponentHandChoice();
      if(s.aiHand.length!==2 || s.aiDeck.indexOf('S1-WAR-001')<0 || s.playerDiscard.indexOf('S1-ARC-004')<0 || s.magicScopeReveal) return {ok:false, reason:'Eyes of the Hawk deck/hand shuffle flow failed', aiHand:s.aiHand, aiDeck:s.aiDeck, playerDiscard:s.playerDiscard, reveal:s.magicScopeReveal};

      return {ok:true, sourceStack:true, cards:CARDS.length, allCardsCovered:true, instantAttachmentCleanup:true, persistentAttachmentWhitelist:true, sanctuaryClassPayload:true, holySlashDamageGate:true, holyBlastDamageGate:true, purifyChoice:true, calculatedPlanMaskedChoice:true, eyesOfHawkChoice:true, magicScopeCombo:true, resurrectionHp:20, finalGritHp:30};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }
  window.GL_V365_RUNTIME_SYNC_QA_SELF_TEST=simulateV365RuntimeSyncAudit;
  window.GL_V381_POPUP_ATTACHMENT_POLICY_QA_SELF_TEST=function(){
    var oldApp=appState, oldMatch=matchStarted;
    try{
      var s=buildInitialMatchState(); appState=s; matchStarted=true;
      s.playerHeroes.LEFT.attachments=['S1-ARC-021',null]; s.playerHeroes.LEFT.side='BROKEN'; s.playerHeroes.LEFT.lane='BROKEN';
      s.pendingCastings=[{card_id:'S1-ARC-021',side:'PLAYER',source_lane:'LEFT',source_hero_card_id:heroIdFrom(s.playerHeroes.LEFT),attachmentSlot:0,counters:3}];
      glPvpRebindHeroCoordinates(s);
      if(attachmentCounterLabel(s.playerHeroes.LEFT,'S1-ARC-021','PLAYER','LEFT',0)!=='(3)') return {ok:false,reason:'attachment counter'};
      s.pending={type:'exact_two_target_selection'}; if(localPendingClosePolicy(s,s.pending)!=='PRE_COMMIT_CANCEL') return {ok:false,reason:'dual arrow policy'};
      s.pending={type:'scouting_exp_selection'}; if(localPendingClosePolicy(s,s.pending)!=='PRE_COMMIT_CANCEL') return {ok:false,reason:'scouting policy'};
      s.pending={type:'card_search_choice',zone:'deck'}; if(localPendingClosePolicy(s,s.pending)!=='MANDATORY_NO_CANCEL') return {ok:false,reason:'deck hidden policy'};
      s.pending={type:'opponent_hand_choice',reveal_cards:true,commit_stage:'committed'}; if(localPendingClosePolicy(s,s.pending)!=='MANDATORY_NO_CANCEL') return {ok:false,reason:'opponent reveal policy'};
      s.pending={type:'optional_swap'}; if(localPendingClosePolicy(s,s.pending)!=='POST_RESOLUTION_DECLINE') return {ok:false,reason:'optional decline'};
      s.pending={type:'legacy_defeat_choice'}; if(localPendingClosePolicy(s,s.pending)!=='MANDATORY_NO_CANCEL') return {ok:false,reason:'legacy mandatory'};
      return {ok:true,attachmentCounter:'(3)',scoutingCancel:true,dualArrowCancel:true,hiddenInfoLocked:true,optionalDecline:true,legacyMandatory:true};
    }catch(e){ return {ok:false,error:String(e&&e.stack||e)}; }
    finally{ appState=oldApp; matchStarted=oldMatch; }
  };

  // v1.45 / v3.84 / PvP v1.32.8 — Hero Ability & Racial Trait behavior lock.
  function locateHeroObjectLane(state, side, hero){
    var found=null; LANE_ORDER.forEach(function(l){ if(sideHeroes(state,side)[l]===hero) found=l; }); return found;
  }
  function locateHeroLaneByIdentity(state, side, heroCardId, fallbackLane){
    var heroes=sideHeroes(state,side)||{}, fallback=heroes[fallbackLane];
    if(fallback && heroIdFrom(fallback)===heroCardId) return fallbackLane;
    var found=null; LANE_ORDER.forEach(function(l){ if(!found && heroes[l] && heroIdFrom(heroes[l])===heroCardId) found=l; }); return found;
  }
  function heroRace(hero){ return String(((card(heroIdFrom(hero)).identity||{}).race)||''); }
  function isStonebloodHero(hero){ var r=card(heroIdFrom(hero)).racial_ability||{}; return /Dwarf/i.test(heroRace(hero)) && String((r.action||{}).action_key||r.name||'').toLowerCase().indexOf('stoneblood')!==-1; }
  function isHalflingSecondChanceHero(hero){ var r=card(heroIdFrom(hero)).racial_ability||{}; return /Halfling/i.test(heroRace(hero)) && String((r.action||{}).action_key||r.name||'').toLowerCase().indexOf('second_chance')!==-1; }
  function isDefeatGatePending(p){ return !!(p && (p.type==='legacy_defeat_choice' || p.type==='racial_stoneblood')); }
  function stonebloodAvailable(state,side,lane,hero){
    return !!(state && hero && Number(hero.hp||0)<=0 && !isLegacyModeHero(hero) && isStonebloodHero(hero) && Number(state[racialTokenKey(side)]||0)>=1 && !racialAbilityUsedThisTurnByHero(state,hero) && hero.stoneblood_declined_turn!==runtimeTurnStamp(state));
  }
  function openStonebloodChoice(state,side,lane,hero){
    if(!stonebloodAvailable(state,side,lane,hero)) return false;
    if(side==='AI' && !state.pvpHumanVsHuman){
      state[racialTokenKey(side)]=Math.max(0,Number(state[racialTokenKey(side)]||0)-1);markRacialTokenSpent(state,side); markRacialAbilityUsedThisTurn(state,hero); hero.hp=10;
      pushLog(state,cardName(card(heroIdFrom(hero)))+' uses Stoneblood, spends 1 Racial Token, prevents defeat, and remains at 10 HP.'); return true;
    }
    state.pending={type:'racial_stoneblood',decision_side:side,side:side,source_side:side,source_lane:lane,source_hero_card_id:heroIdFrom(hero),choices:['use','decline'],commit_stage:'committed'};
    pushLog(state,'Stoneblood available for '+cardName(card(heroIdFrom(hero)))+': spend 1 Racial Token to remain at 10 HP, or decline.'); return true;
  }
  function resumeAfterStonebloodResponse(cont){
    cont=cont||{};var action=cont.action||{},post=cont.post||{},result=cont.result||{};
    if(result && (result.dodged||result.dodged_any) && (post.defer_reposition_discard || post.deferred_discard_card_id)) finalizeDeferredRepositionDiscard(appState,post,action.source_side,action.card_id);
    if(!isDefeatGatePending(appState.pending) && maybeOpenHalflingSecondChance(appState,action,post,result)){syncCounts(appState);render();return true;}
    if(resolvePostAttackChoices(appState,action,post,result)){syncCounts(appState);render();return true;}
    if(resumeAITurnAfterPlayerResponse(appState))return true;
    syncCounts(appState);render();return true;
  }
  function resolveStonebloodChoice(useIt){
    if(!appState || !appState.pending || appState.pending.type!=='racial_stoneblood') return false;
    var p=clone(appState.pending), continuation=p.after_stoneblood_response?clone(p.after_stoneblood_response):null, lane=locateHeroLaneByIdentity(appState,p.side,p.source_hero_card_id,p.source_lane), hero=lane?sideHeroes(appState,p.side)[lane]:null;
    appState.pending=null; closeChoice();
    if(useIt && stonebloodAvailable(appState,p.side,lane,hero)){
      appState[racialTokenKey(p.side)]=Math.max(0,Number(appState[racialTokenKey(p.side)]||0)-1); markRacialAbilityUsedThisTurn(appState,hero); hero.stoneblood_used_turn=runtimeTurnStamp(appState); hero.hp=10;
      pushLog(appState,cardName(card(heroIdFrom(hero)))+' uses Stoneblood, spends 1 Racial Token, prevents defeat, and remains at 10 HP.');
      if(appState.pvpHumanVsHuman) recordPvpSideAction(appState,p.side,'RACIAL',heroIdFrom(hero),'Stoneblood',p.side+' uses Stoneblood.',{action_line:'Action: '+p.side+' used Stoneblood.',source_side:p.side,source_lane:lane,result_lines:['Spent 1 Racial Token.','Defeat prevented.','Hero remains at 10 HP.']});
    } else if(hero){
      hero.stoneblood_declined_turn=runtimeTurnStamp(appState); pushLog(appState,'Stoneblood declined. Defeat flow continues.'); applyDefeatCheck(appState,p.side,lane);
    }
    if(continuation){
      if(appState.pending&&appState.pending.type==='legacy_defeat_choice') appState.pending.after_legacy_stoneblood_response=continuation;
      else if(!appState.pending) return resumeAfterStonebloodResponse(continuation);
    }
    syncCounts(appState); render(); return true;
  }
  function queueSaintPurifyChoice(state,targetHero,sourceCard){
    var choices=negativeStatusChoices(targetHero); if(!choices.length) return false;
    var side=null,lane=null; ['PLAYER','AI'].some(function(s){ var l=locateHeroObjectLane(state,s,targetHero); if(l){side=s;lane=l;return true;} return false; });
    if(!side||!lane) return false;
    state.pendingSaintPurifyQueue=state.pendingSaintPurifyQueue||[];
    state.pendingSaintPurifyQueue.push({type:'saint_purify_choice',decision_side:side,target_side:side,target_lane:lane,source_card_id:sourceCard&&sourceCard.card_id,status_choices:choices.map(function(x){return {status_index:x.status_index,label:x.label,detail:x.detail};}),selected_status_choice:0,commit_stage:'committed'});
    pushLog(state,'Holy Rejuvenation requires '+side+' to choose 1 negative status to remove from '+lane+'.'); return true;
  }
  function activateNextSaintPurifyChoice(state){
    if(!state || state.pending || !(state.pendingSaintPurifyQueue||[]).length) return false;
    var p=state.pendingSaintPurifyQueue.shift();
    if(p.decision_side==='AI' && !state.pvpHumanVsHuman){
      var h=sideHeroes(state,p.target_side)[p.target_lane], ch=(p.status_choices||[])[0]; if(h&&ch) removeChosenNegativeStatusFromHero(state,h,card(p.source_card_id),ch.status_index);
      return activateNextSaintPurifyChoice(state);
    }
    state.pending=p; return true;
  }
  function selectSaintPurifyChoice(idx){
    if(!appState||!appState.pending||appState.pending.type!=='saint_purify_choice') return false; idx=Number(idx); if(idx<0||idx>=(appState.pending.status_choices||[]).length) return false; appState.pending.selected_status_choice=idx; renderSaintPurifyChoice(); return true;
  }
  function commitSaintPurifyChoice(){
    if(!appState||!appState.pending||appState.pending.type!=='saint_purify_choice') return false;
    var p=clone(appState.pending), ch=(p.status_choices||[])[Number(p.selected_status_choice||0)], h=sideHeroes(appState,p.target_side)[p.target_lane]; if(!ch||!h){showInfo('Choice Required','Choose a negative status to remove.');return false;}
    removeChosenNegativeStatusFromHero(appState,h,card(p.source_card_id),ch.status_index); appState.pending=null; closeChoice(); activateNextSaintPurifyChoice(appState); syncCounts(appState); render(); return true;
  }
  function renderSaintPurifyChoice(){
    if(!appState||!appState.pending||appState.pending.type!=='saint_purify_choice'||SUPPRESS_RENDER||!$('choiceOverlay')) return;
    var p=appState.pending, choices=p.status_choices||[], selected=Number(p.selected_status_choice||0);
    $('choiceTitle').textContent='Holy Rejuvenation — Remove Negative Status'; $('choiceConfirm').style.display=''; $('choiceConfirm').textContent='Remove Selected Status'; $('choiceConfirm').disabled=!choices[selected];
    $('choiceBody').innerHTML='<p class="choice-instruction">Choose exactly 1 negative status to remove from '+esc(p.target_lane)+'. This choice is mandatory because the healing effect has already resolved.</p><div class="choice-grid card-search-choice-grid">'+choices.map(function(x,idx){var sel=idx===selected;return '<article class="discard-choice '+(sel?'selected':'')+'"><button class="discard-select" type="button" data-saint-status-index="'+idx+'">'+(sel?'Selected: ':'Select: ')+esc(x.label)+'</button><small>'+esc(x.detail||x.label)+'</small></article>';}).join('')+'</div>';
    $('choiceOverlay').classList.add('open');
  }
  function primalStrikeResponseOptions(state,targetSide,incoming){
    return responseOptionsFor(state,targetSide,incoming).filter(function(o){
      if(o.response_kind==='dragon_scale') return o.source_lane===incoming.target_lane;
      var k=responseKind(card(o.card_id),incoming); return k==='dodge'||k==='block';
    });
  }
  function openPrimalStrikeResponseWindow(state,side,lane,targetLane,ability){
    var targetSide=oppositeSide(side), incoming={damage:20,damage_type:'Physical',attack_type:'Ability Damage',cannot_dodge:false,cannot_block:false,card_id:heroIdFrom(sideHeroes(state,side)[lane]),source_side:side,source_lane:lane,target_side:targetSide,target_lane:targetLane};
    state.responseWindow={kind:'incoming_ability_damage',response_owner:targetSide,card_id:incoming.card_id,source_side:side,source_lane:lane,target_side:targetSide,target_lane:targetLane,damage:20,damage_type:'Physical',attack_type:'Ability Damage',ability_id:'primal_strike',ability_name:(ability&&ability.label)||'Primal Strike',options:primalStrikeResponseOptions(state,targetSide,incoming),selected:null};
    state.pending={type:'response_window',card_id:incoming.card_id,commit_stage:'committed'}; pushLog(state,'Response Window opens for Primal Strike Physical damage against '+targetSide+' '+targetLane+'.');
    return true;
  }
  function resolveAbilityDamageResponseWindow(responseOption){
    if(!appState||!appState.responseWindow||appState.responseWindow.kind!=='incoming_ability_damage') return false;
    var rw=appState.responseWindow,target=sideHeroes(appState,rw.target_side)[rw.target_lane],block=0,dodged=false,responseName='No Response';
    if(responseOption){
      var abilityRcPre=card(responseOption.card_id);
      if(responseExtraDiscardCount(abilityRcPre)>0 && !Number.isInteger(Number(responseOption.selected_extra_discard_index))){
        if(rw.target_side==='AI' && !appState.pvpHumanVsHuman){var autoAbility=responseExtraDiscardCandidates(appState,rw.target_side,responseOption)[0];if(!autoAbility){showInfo('Cannot Respond','Escape Arrow requires 1 other hand card.');return false;}responseOption=clone(responseOption);responseOption.selected_extra_discard_index=autoAbility.hand_index;}
        else return openResponseExtraDiscardChoice(appState,rw,responseOption);
      }
      if(responseOption.response_kind==='dragon_scale'){
        if(responseOption.source_lane!==rw.target_lane){showInfo('Cannot Respond','Dragon Scale can protect only its own Dragonborn Hero.');return false;}
        var dh=sideHeroes(appState,rw.target_side)[responseOption.source_lane]; if(!dh||!isDragonbornHero(dh)||Number(appState[racialTokenKey(rw.target_side)]||0)<1||racialAbilityUsedThisTurnByHero(appState,dh)){showInfo('Cannot Respond','Dragon Scale is no longer legal.');return false;}
        appState[racialTokenKey(rw.target_side)]=Math.max(0,Number(appState[racialTokenKey(rw.target_side)]||0)-1);markRacialTokenSpent(appState,rw.target_side);markRacialAbilityUsedThisTurn(appState,dh);block=40;responseName='Dragon Scale blocks 40 Physical damage';
      } else {
        var rc=card(responseOption.card_id),incoming={damage_type:'Physical',attack_type:'Ability Damage',cannot_dodge:false,cannot_block:false,target_lane:rw.target_lane},kind=responseKind(rc,incoming); if(kind!=='dodge'&&kind!=='block'){showInfo('Cannot Respond','Primal Strike allows only Dodge or Block responses.');return false;}
        var cost=Number(cardCost(rc)||0),mk=manaPoolKeyForSide(rw.target_side); if(cost>Number(appState[mk]||0)){showInfo('Cannot Respond','Not enough Mana.');return false;} appState[mk]=Math.max(0,Number(appState[mk]||0)-cost);
        var hand=sideHand(appState,rw.target_side),idx=responseOption.hand_index;if(hand[idx]!==responseOption.card_id)idx=hand.indexOf(responseOption.card_id);if(idx>=0)payResponseExtraDiscard(appState,rw.target_side,idx,rc,responseOption.selected_extra_discard_index);idx=hand.indexOf(responseOption.card_id);if(idx>=0){hand.splice(idx,1);sideDiscard(appState,rw.target_side).push(responseOption.card_id);} dodged=kind==='dodge';block=dodged?0:responseBlockAmount(rc,incoming,target);responseName=cardName(rc)+(dodged?' dodges Primal Strike':' blocks '+block+' Physical damage');applyResponseSideEffects(rc,sideHeroes(appState,rw.source_side)[rw.source_lane],target);
      }
      pushLog(appState,rw.target_side+' responds: '+responseName+'.');
    } else pushLog(appState,rw.target_side+' takes Primal Strike with no response.');
    var passive=(!dodged&&target&&(heroClass(target)==='Gladiator'||heroClass(target)==='Conqueror'))?10:0,active=(!dodged&&target&&hasActiveAttachmentForHero(appState,target,'S1-WAR-020'))?20:0,damage=dodged?0:Math.max(0,Number(rw.damage||20)-block-passive-active);
    if(target){var primalBlocked=!!activeAttachmentForSide(appState,rw.target_side,'S1-CLE-025');if(primalBlocked)damage=0;target.hp=Math.max(0,Number(target.hp||0)-damage);pushLog(appState,primalBlocked?'Blessing of Divinity prevents Primal Strike damage.':'Primal Strike resolves for '+damage+' Physical damage'+(dodged?' (Dodged).':'')+'.');if(damage>0)applyDefeatCheck(appState,rw.target_side,rw.target_lane);}
    appState.responseWindow=null;if(!isDefeatGatePending(appState.pending))appState.pending=null;closeResponseWindowUI();if(!appState.pending)activateNextSaintPurifyChoice(appState);syncCounts(appState);render();return true;
  }


  function maybeOpenHalflingSecondChance(state, action, post, result){
    if(!state || !action || !result || !(result.dodged||result.dodged_any)) return false;
    var c=card(action.card_id); if(cardFamily(c)!=='Skill' || !isAttackCard(c)) return false;
    var sourceLane=locateHeroLaneByIdentity(state,action.source_side,action.source_hero_card_id,action.source_lane), sourceHero=sourceLane?sideHeroes(state,action.source_side)[sourceLane]:null;
    if(!sourceHero || Number(sourceHero.hp||0)<=0 || isLegacyModeHero(sourceHero) || !isHalflingSecondChanceHero(sourceHero)) return false;
    if(racialAbilityUsedThisTurnByHero(state,sourceHero) || Number(state[racialTokenKey(action.source_side)]||0)<1) return false;
    var discard=sideDiscard(state,action.source_side); if(discard.lastIndexOf(action.card_id)<0) return false;
    state.pending={type:'racial_second_chance', decision_side:action.source_side, source_side:action.source_side, source_lane:sourceLane, source_hero_card_id:heroIdFrom(sourceHero), card_id:action.card_id, target_side:action.target_side, target_lane:action.target_lane, original_action:clone(action), original_post:clone(post||{}), multi_target:!isSingleTargetAttack(c), choices:['use','decline'],commit_stage:'committed'};
    pushLog(state,'Second Chance available: spend 1 Racial Token to replay '+cardName(c)+' immediately for 0 Mana against the same locked target, or decline.'); return true;
  }
  function resolveSecondChanceChoice(useIt){
    if(!appState || !appState.pending || appState.pending.type!=='racial_second_chance') return false;
    var p=clone(appState.pending), c=card(p.card_id), sourceLane=locateHeroLaneByIdentity(appState,p.source_side,p.source_hero_card_id,p.source_lane), sourceHero=sourceLane?sideHeroes(appState,p.source_side)[sourceLane]:null;
    if(!useIt){ appState.pending=null; closeChoice(); pushLog(appState,'Second Chance declined. The dodged Skill remains in Discard Pile.'); syncCounts(appState); render(); return true; }
    var tokenKey=racialTokenKey(p.source_side), discard=sideDiscard(appState,p.source_side), di=discard.lastIndexOf(p.card_id), target=sideHeroes(appState,p.target_side)[p.target_lane];
    if(!sourceHero||Number(sourceHero.hp||0)<=0||isLegacyModeHero(sourceHero)||!isHalflingSecondChanceHero(sourceHero)||racialAbilityUsedThisTurnByHero(appState,sourceHero)||Number(appState[tokenKey]||0)<1||di<0||(isSingleTargetAttack(c)&&(!target||Number(target.hp||0)<=0||isLegacyModeHero(target)))){appState.pending=null;closeChoice();pushLog(appState,'Second Chance replay is no longer legal.');syncCounts(appState);render();return true;}
    appState[tokenKey]=Math.max(0,Number(appState[tokenKey]||0)-1);markRacialAbilityUsedThisTurn(appState,sourceHero);discard.splice(di,1);appState.pending=null;closeChoice();
    var action=clone(p.original_action||{});action.card_id=p.card_id;action.source_side=p.source_side;action.source_lane=sourceLane;action.source_hero_card_id=heroIdFrom(sourceHero);action.target_side=p.target_side;action.target_lane=p.target_lane;action.second_chance_replay=true;
    pushLog(appState,cardName(card(heroIdFrom(sourceHero)))+' uses Second Chance and replays '+cardName(c)+' immediately for 0 Mana against the same target.');
    var originalPost=clone(p.original_post||{}),replayPost={damage:computedAttackDamageForAction(appState,action,sourceHero,isSingleTargetAttack(c)?target:null),persistent:false,attachmentSlot:-1,optional_swap_after:!!originalPost.optional_swap_after,defer_reposition_discard:!!originalPost.defer_reposition_discard,second_chance_replay:true};
    if(!isSingleTargetAttack(c)){resolveMultiTargetAttack(appState,action,replayPost);syncCounts(appState);render();return true;}openAttackResponseWindow(appState,action,replayPost);syncCounts(appState);render();return true;
  }
  function renderSecondChanceChoice(){
    if(!appState||!appState.pending||appState.pending.type!=='racial_second_chance'||SUPPRESS_RENDER||!$('choiceOverlay'))return;var p=appState.pending,hero=sideHeroes(appState,p.source_side)[p.source_lane],c=card(p.card_id);
    $('choiceTitle').textContent='Second Chance — Optional Replay';$('choiceConfirm').style.display='none';$('choiceBody').innerHTML='<div class="response-context-row"><article class="response-context-card"><h3>Halfling Hero</h3><button class="response-context-tile" type="button" data-preview="'+esc(heroIdFrom(hero))+'"><img src="'+esc(thumbFor(heroIdFrom(hero)))+'" alt="'+esc(cardName(card(heroIdFrom(hero))))+'"><span><strong>'+esc(cardName(card(heroIdFrom(hero))))+'</strong><small>'+esc(p.source_side)+' · '+esc(p.source_lane)+'</small></span></button></article><article class="response-context-card"><h3>Dodged Skill</h3><button class="response-context-tile" type="button" data-preview="'+esc(p.card_id)+'"><img src="'+esc(thumbFor(p.card_id))+'" alt="'+esc(cardName(c))+'"><span><strong>'+esc(cardName(c))+'</strong><small>Replay 0 Mana · same target</small><small>Cost: 1 Racial Token</small></span></button></article></div><div class="choice-action-row"><button type="button" class="gold" data-second-chance-choice="use">Spend 1 Token & Replay</button><button type="button" data-second-chance-choice="decline">Decline</button></div>';$('choiceOverlay').classList.add('open');
  }


  function renderCurrentAuthoritativePendingChoice(){
    if(!appState || !appState.pending){ closeChoice(); return false; }
    var p=appState.pending;
    if(!pvpLocalOwnsPending(p)){ pvpHideChoiceForNonOwner(); return false; }
    if(p.type==='draw_replacement_choice'){ renderDrawReplacementChoice(); return true; }
    if(p.type==='optional_magical_surge'){ renderMagicalSurgeChoice(); return true; }
    if(p.type==='response_extra_discard_choice'){ renderResponseExtraDiscardChoice(); return true; }
    if(p.type==='hand_limit_discard'){ renderHandLimitDiscardChoice(); return true; }
    if(p.type==='card_search_choice'){ renderCardSearchChoice(); return true; }
    if(p.type==='legacy_defeat_choice'){ renderLegacyDefeatChoice(); return true; }
    if(p.type==='legacy_cost_selection'){ renderLegacyCostChoice(); return true; }
    if(p.type==='legacy_card_choice'){ renderLegacyCardChoice(); return true; }
    return false;
  }
  function simulateV1380PvpParityAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShared=window.GL_PVP_SHARED_BOARD_ACTIVE;
    SUPPRESS_RENDER=true;matchStarted=true;window.GL_PVP_SHARED_BOARD_ACTIVE=false;
    try{
      var s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Deploy';s.mana=30;
      s.cardsDrawnThisTurn={PLAYER:2,AI:0};s.lastDrawnCardBySide={};
      s.playerHeroes.LEFT.card_id='S1-ARC-H005';s.playerHeroes.LEFT.hp=100;s.playerHeroes.LEFT.maxHp=100;s.playerHeroes.LEFT.exhausted=false;s.playerHeroes.LEFT.attachments=[null,null];
      s.playerHeroes.CENTER.card_id='S1-ARC-H001';s.playerHeroes.CENTER.hp=80;s.playerHeroes.CENTER.maxHp=80;s.playerHeroes.CENTER.exhausted=false;s.playerHeroes.CENTER.attachments=[null,null];
      s.playerHeroes.RIGHT.card_id='S1-ARC-H005';s.playerHeroes.RIGHT.hp=100;s.playerHeroes.RIGHT.maxHp=100;s.playerHeroes.RIGHT.exhausted=false;s.playerHeroes.RIGHT.attachments=['S1-ARC-021',null];
      s.aiHeroes.CENTER.card_id='S1-ARC-H005';s.aiHeroes.CENTER.hp=100;s.aiHeroes.CENTER.maxHp=100;s.aiHeroes.CENTER.exhausted=false;s.aiHeroes.CENTER.attachments=[null,null];
      s.pendingCastings=[{card_id:'S1-ARC-021',side:'PLAYER',source_lane:'RIGHT',source_hero_card_id:'S1-ARC-H005',target_side:'AI',target_lane:'CENTER',attachmentSlot:0,resolve_phase:'counter_draw',counters:2}];
      s.activeAttachments=[{card_id:'S1-ARC-021',side:'PLAYER',lane:'RIGHT',slot:0,remaining:2,remaining_count:2,tick_phase:'DRAW_EVENT'}];
      s.playerHand=['S1-THF-027'];s.playerDeck=['S1-WAR-001','S1-WAR-002','S1-WAR-006','S1-WAR-007'];
      beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','LEFT');
      if(Number(s.cardsDrawnThisTurn.PLAYER)!==3) return {ok:false,reason:'Camouflage draw did not increment Draw This Turn',count:s.cardsDrawnThisTurn.PLAYER};
      if(!s.pendingCastings[0]||Number(s.pendingCastings[0].counters)!==3) return {ok:false,reason:'Camouflage draw did not increment Aura counter',castings:s.pendingCastings};
      s.playerHand.push('S1-ARC-005');
      beginPlayFromHand(s.playerHand.lastIndexOf('S1-ARC-005'));chooseHeroFromBoard('PLAYER','CENTER');
      if(Number(s.cardsDrawnThisTurn.PLAYER)!==5) return {ok:false,reason:'Quick Reload + Camouflage + Reload total should be 5',count:s.cardsDrawnThisTurn.PLAYER};
      var auraPc=(s.pendingCastings||[]).find(function(x){return x.card_id==='S1-ARC-021';});
      if(!auraPc||Number(auraPc.counters)!==5) return {ok:false,reason:'Reload package did not add both Aura counters',castings:s.pendingCastings};
      s.aiHand=['S1-THF-026','S1-ITM-016'];s.aiMana=30;s.phase='Battle'; s.round=2;
      resolvePendingCastingsFor(s,'PLAYER');
      if(!s.responseWindow||s.responseWindow.card_id!=='S1-ARC-021') return {ok:false,reason:'Aura did not open response window',rw:s.responseWindow};
      var ids=(s.responseWindow.options||[]).map(function(x){return x.card_id;});
      if(ids.indexOf('S1-THF-026')<0||ids.indexOf('S1-ITM-016')<0) return {ok:false,reason:'Aura response window missing Dodge/Block',options:ids};
      var blockState=clone(s),hp=s.aiHeroes.CENTER.hp,opt=(s.responseWindow.options||[]).find(function(x){return x.card_id==='S1-THF-026';});
      resolveResponseWindow(opt);
      if(s.aiHeroes.CENTER.hp!==hp) return {ok:false,reason:'Dodge Instinct did not dodge Aura damage',before:hp,after:s.aiHeroes.CENTER.hp};
      if(s.playerHeroes.RIGHT.attachments.indexOf('S1-ARC-021')!==-1||s.playerDiscard.indexOf('S1-ARC-021')<0) return {ok:false,reason:'Aura did not leave Attachment exactly once after Dodge',attachments:s.playerHeroes.RIGHT.attachments,discard:s.playerDiscard};
      s=blockState;appState=s;var blockOpt=(s.responseWindow.options||[]).find(function(x){return x.card_id==='S1-ITM-016';});var blockHp=s.aiHeroes.CENTER.hp;resolveResponseWindow(blockOpt);
      if(s.aiHeroes.CENTER.hp!==blockHp-40) return {ok:false,reason:'Chain Mail did not block 40 Aura Physical damage',before:blockHp,after:s.aiHeroes.CENTER.hp};
      if(s.playerHeroes.RIGHT.attachments.indexOf('S1-ARC-021')!==-1||s.playerDiscard.indexOf('S1-ARC-021')<0) return {ok:false,reason:'Aura did not leave Attachment exactly once after Block',attachments:s.playerHeroes.RIGHT.attachments,discard:s.playerDiscard};

      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Deploy';s.playerHand=['S1-CLE-012'];s.playerDeck=['S1-WAR-001'];
      s.playerHeroes.CENTER={card_id:'S1-CLE-L001',side:'PLAYER',lane:'CENTER',mode:'LEGACY',legacy_mode:true,active_legacy_card_id:'S1-CLE-L001',defeated_hero_snapshot:{card_id:'S1-CLE-H003',hp:0,maxHp:100,exp_cards:[],attachments:[null,null]}};
      var canonical=getActivatedLegacyAbilities(s,'PLAYER','CENTER');
      if(!canonical.length) return {ok:false,reason:'Cleric Legacy missing with real server deck'};
      s.playerDeck=['__HIDDEN_CARD_BACK__'];s.pvpLocalLegalLegacyAbilities={LEFT:[],CENTER:clone(canonical),RIGHT:[]};window.GL_PVP_SHARED_BOARD_ACTIVE=true;
      if(!getActivatedLegacyAbilities(s,'PLAYER','CENTER').length) return {ok:false,reason:'Server legal Legacy metadata did not survive masked deck'};
      return {ok:true,drawThisTurn:5,camouflageDrawCounted:true,reloadPackageCountedAsTwo:true,auraResponseWindow:true,auraBlockAndDodge:true,dodgeInstinct:true,clericLegacyMaskedDeck:true};
    }catch(err){return {ok:false,reason:String(err&&err.message||err),stack:String(err&&err.stack||'')};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;window.GL_PVP_SHARED_BOARD_ACTIVE=oldShared;}
  }
  window.GL_V1380_PVP_PARITY_QA_SELF_TEST=simulateV1380PvpParityAudit;

  function simulateDefeatCastingCleanupReviveQA(){
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;SUPPRESS_RENDER=true;matchStarted=true;
    try{
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle'; s.round=2;
      var hero=s.aiHeroes.LEFT;hero.card_id='S1-MAG-H002';hero.hp=0;hero.maxHp=100;hero.exhausted=true;hero.casting=true;hero.statuses=[{name:'Poison',duration:2}];hero.exp_cards=['S1-MAG-001'];hero.exp_total=400;hero.attachments=['S1-MAG-007',null];hero.assigned_legacy_card_id='S1-MAG-L001';
      if((s.aiLegacy||[]).indexOf('S1-MAG-L001')<0)s.aiLegacy.push('S1-MAG-L001');
      var pc={card_id:'S1-MAG-007',side:'AI',source_lane:'LEFT',original_source_lane:'LEFT',source_hero_card_id:hero.card_id,source_instance_id:hero.instance_id,source_progression_id:heroProgressionId(hero),target_side:'PLAYER',target_lane:'CENTER',attachmentSlot:0,resolve_phase:'next_owner_battle',base_damage:70,damage_profile:'Magical'};
      s.pendingCastings=[pc];s.activeAttachments=[{attachment_id:'ATT:QA:DEFEAT-CAST',side:'AI',lane:'LEFT',slot:0,card_id:'S1-MAG-007',attachment_state:'CASTING',commit_token:'QA:DEFEAT-CAST'}];
      var targetHp=s.playerHeroes.CENTER.hp;applyDefeatCheck(s,'AI','LEFT');
      if(!isLegacyModeHero(s.aiHeroes.LEFT))return{ok:false,reason:'Defeated Casting Hero did not enter Legacy Mode'};
      if((s.pendingCastings||[]).length)return{ok:false,reason:'Defeat did not clear pending Casting queue',pendingCastings:s.pendingCastings};
      var snap=s.aiHeroes.LEFT.defeated_hero_snapshot||{};
      if(snap.casting||snap.pending_casting||(snap.statuses||[]).length||(snap.exp_cards||[]).length||(snap.attachments||[]).some(Boolean))return{ok:false,reason:'Defeated snapshot retained connected state',snapshot:snap};
      if((s.aiDiscard||[]).filter(function(id){return id==='S1-MAG-007';}).length!==1)return{ok:false,reason:'Casting card was not discarded exactly once',discard:s.aiDiscard};
      restoreHeroFromLegacyMode(s,'AI','LEFT',20,'QA Revive',true);
      var revived=s.aiHeroes.LEFT;if(revived.casting||(revived.statuses||[]).length||(revived.exp_cards||[]).length||(revived.attachments||[]).some(Boolean))return{ok:false,reason:'Revive restored pre-defeat state',hero:revived};
      resolvePendingCastingsFor(s,'AI');
      if(s.playerHeroes.CENTER.hp!==targetHp)return{ok:false,reason:'Pre-defeat Casting resolved after revive',before:targetHp,after:s.playerHeroes.CENTER.hp};
      return{ok:true,castingCleared:true,attachmentsCleared:true,statusesCleared:true,expCleared:true,reviveClean:true};
    }catch(err){return{ok:false,reason:String(err&&err.message||err),stack:String(err&&err.stack||'')};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }

  function simulateV559GameplayFixes(){
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    SUPPRESS_RENDER=true;
    try{
      initCards();
      var s=buildInitialMatchState(); appState=s; matchStarted=true;
      s.openingCoinFlip={firstPlayer:'PLAYER'}; s.turn='PLAYER'; s.phase='Battle'; s.round=1; s.mana=30; s.aiMana=30;
      s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.exhausted=false; s.aiHeroes.LEFT.card_id='S1-MAG-H001'; s.aiHeroes.LEFT.hp=100;
      s.playerHand=['S1-WAR-001'];
      var restricted=validateCardPreconditions(s,'PLAYER','S1-WAR-001','LEFT','LEFT',{});
      if(restricted.ok) return {ok:false,reason:'First player attack restriction did not reject Attack Skill precondition',precondition:restricted};
      if(!(restricted.reasons||[]).some(function(r){return /first player/i.test(String(r));})) return {ok:false,reason:'First player restriction reason missing',precondition:restricted};
      var actions=getLegalActions('PLAYER',s)||[];
      if(actions.some(function(a){return a&&a.card_id==='S1-WAR-001';})) return {ok:false,reason:'First player Attack Skill still appeared in legal actions',actions:actions};

      s=buildInitialMatchState(); appState=s; matchStarted=true;
      s.openingCoinFlip={firstPlayer:'AI'}; s.turn='PLAYER'; s.phase='Battle'; s.round=1; s.mana=30; s.aiMana=30;
      s.playerHeroes.LEFT.card_id='S1-WAR-H001'; s.playerHeroes.LEFT.exhausted=false; s.aiHeroes.LEFT.card_id='S1-MAG-H001'; s.aiHeroes.LEFT.hp=100;
      s.playerHand=['S1-WAR-001'];
      var second=validateCardPreconditions(s,'PLAYER','S1-WAR-001','LEFT','LEFT',{});
      if(!second.ok) return {ok:false,reason:'Second player first-turn Attack was incorrectly rejected',precondition:second};

      s=buildInitialMatchState(); appState=s; matchStarted=true;
      s.turn='AI'; s.phase='Battle'; s.round=2; s.mana=30; s.aiMana=30; s.pvpHumanVsHuman=true;
      s.aiHeroes.LEFT.card_id='S1-MAG-H002'; s.aiHeroes.LEFT.exhausted=false;
      s.playerHeroes.LEFT.card_id='S1-THF-H001'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.exhausted=false;
      s.playerHand=['S1-ITM-012'];
      openAttackResponseWindow(s,{card_id:'S1-MAG-007',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'},{damage:80,persistent:false,attachmentSlot:-1});
      var hook=s.responseWindow && (s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-ITM-012';});
      if(!hook) return {ok:false,reason:'Spectral Grappling Hook missing against Tornado',rw:s.responseWindow};
      resolveResponseWindow(hook);
      if(s.playerHeroes.LEFT.hp!==60) return {ok:false,reason:'Tornado residual vs Spectral Grappling Hook should deal exactly 40',hp:s.playerHeroes.LEFT.hp,state:s};

      s=buildInitialMatchState(); appState=s; matchStarted=true;
      s.turn='PLAYER'; s.phase='Battle'; s.round=2; s.mana=30; s.aiMana=30; s.pvpHumanVsHuman=true;
      s.playerHeroes.LEFT.card_id='S1-MAG-H002'; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=['S1-MAG-018',null];
      registerLocalAttachment(s,'PLAYER','LEFT',0,'S1-MAG-018',{role:'ongoing',remaining_count:1,counter_mode:'countdown',tick_phase:'END_PHASE',double_casting:true},{source_side:'PLAYER',source_lane:'LEFT'});
      s.aiHeroes.LEFT.card_id='S1-CLE-H001'; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.exhausted=false;
      s.aiHeroes.CENTER.card_id='S1-MAG-H001'; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.exhausted=false;
      s.playerHand=['S1-MAG-003']; s.aiHand=['S1-CLE-003'];
      commitPlayedCard(s,{card_id:'S1-MAG-003',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'});
      var br=s.responseWindow && (s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-CLE-003';});
      if(!br) return {ok:false,reason:'Brilliant Radiance missing against Double Casting activation 1',rw:s.responseWindow};
      resolveResponseWindow(br);
      if(!s.pending || s.pending.type!=='double_casting_target_selection') return {ok:false,reason:'Double Casting activation 2 was not offered after Brilliant Radiance on activation 1',pending:s.pending,responseWindow:s.responseWindow,state:s};
      commitDoubleCastingSecondTarget(s,s.pending,'CENTER');
      if(s.responseWindow){ resolveResponseWindow(null); }
      if(s.aiHeroes.CENTER.hp>=100) return {ok:false,reason:'Double Casting activation 2 did not resolve damage after activation 1 Brilliant Radiance',centerHp:s.aiHeroes.CENTER.hp,state:s};
      if((s.playerHand||[]).indexOf('S1-MAG-003')<0) return {ok:false,reason:'Brilliant Radiance did not return Attack Card after duplicated attack finished',playerHand:s.playerHand,discard:s.playerDiscard};
      return {ok:true,firstPlayerAttackRestricted:true,tornadoSpectralResidual40:true,doubleCastingBrilliantContinues:true};
    }catch(err){return {ok:false,reason:String(err&&err.message||err),stack:String(err&&err.stack||'')};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }
  window.GL_LOCAL_AI_BRIDGE={
    version:GL_VERSION,
    getSnapshot:glPvpBridgeSnapshot,
    importSnapshot:glPvpBridgeImport,
    startSharedMatch:glPvpBridgeStartSharedMatch,
    mirrorState:glPvpSwapKeys,
    getCanonicalSnapshot:function(seat){
      var snap=glPvpBridgeSnapshot();
      if(Number(seat)===2 && snap.appState) snap.appState=glPvpSwapKeys(snap.appState);
      return snap;
    },
    importCanonicalSnapshot:function(canonical, seat, opts){
      if(!canonical) return false;
      window.GL_PVP_LOCAL_SEAT=Number(seat)||1;
      var local=clone(canonical);
      if(Number(seat)===2 && local.appState) local.appState=glPvpSwapKeys(local.appState);
      return glPvpBridgeImport(local, opts);
    },
    setSharedBoardMode:function(active){ window.GL_PVP_SHARED_BOARD_ACTIVE=!!active; },
    setRenderSuppressed:function(active){SUPPRESS_RENDER=!!active;return SUPPRESS_RENDER;},
    getPendingClosePolicy:function(){ return localPendingClosePolicy(appState,appState&&appState.pending); },
    clearTransientUiState:clearTransientUiState,
    testGameplayFoundationFixes:simulateV559GameplayFixes,
    playOpeningCoinSound:playOpeningCoinSound,
    captureAuthoritativePlayedCardMotion:function(cardId,side,action){ return capturePlayedCardMotion(cardId,side,action||{}); },
    commitAuthoritativePlayedCardMotion:function(snapshot,destination){ return commitAuthoritativePlayedCardMotion(snapshot,destination||{type:'target'}); },
    queueAuthoritativeDrawMotion:function(side,cardId,count){var n=Math.max(1,Number(count||1)),events=[],hand=sideHand(appState,side)||[];for(var i=0;i<n;i++)events.push({id:'external-'+(++GL_ANIMATION_SEQUENCE),type:'CARD_DRAWN',side:side,card_id:cardId,hand_index:Math.max(0,hand.length-n+i),reason:'CARD_EFFECT'});return queueDrawEvents(events,appState);},
    queueAuthoritativeDrawEvents:function(events){focusMobilePlayerHand({lockRight:true});return queueDrawEvents(clone(events||[]),appState);},
    focusMobilePlayerHand:function(options){return focusMobilePlayerHand(options||{lockRight:true});},
    queueAuthoritativeDrawMotions:function(side,cardIds,count){var ids=Array.isArray(cardIds)?cardIds.slice():[],n=Math.max(1,Number(count||ids.length||1)),events=[],hand=sideHand(appState,side)||[];while(ids.length<n)ids.push('__HIDDEN_CARD_BACK__');for(var i=0;i<n;i++)events.push({id:'external-'+(++GL_ANIMATION_SEQUENCE),type:'CARD_DRAWN',side:side,card_id:ids[i],hand_index:Math.max(0,hand.length-n+i),reason:'CARD_EFFECT'});return queueDrawEvents(events,appState);},
    captureAuthoritativeDrawMotions:captureAuthoritativeDrawMotions,
    queueCapturedAuthoritativeDrawMotions:queueCapturedAuthoritativeDrawMotions,
    queueAuthoritativeLegacyToFieldMotion:function(side,lane,cardId){ return queueLegacyDeckToFieldMotion(side,lane,cardId); },
    captureAuthoritativeTributeMotion:function(side,handIndex,cardId,lane){ return captureTributeCardMotion(side,handIndex,cardId,lane); },
    queueAuthoritativeTributeMotion:function(snapshot){ return queueTributeCardMotion(snapshot); },
    captureAuthoritativeRankUpMotion:captureAuthoritativeRankUpMotion,
    queueCapturedAuthoritativeRankUpMotion:queueCapturedAuthoritativeRankUpMotion,
    queueAuthoritativeRankUpMotion:function(side,lane,toCardId,expCardIds){ var ok=false; ok=queueLegacyDeckToFieldMotion(side,lane,toCardId)||ok; ok=queueRankUpDiscardMotion(side,lane,expCardIds||[])||ok; return ok; },
    getActivatedLegacyAbilitiesFor:function(side,lane){ return clone(getActivatedLegacyAbilities(appState,side,lane)); },
    testDefeatCastingCleanupRevive:simulateDefeatCastingCleanupReviveQA,
    testUnbrokenStandClassRows:function(){var c=card('S1-WAR-022'),cases={Warrior:'S1-WAR-H001',Gladiator:'S1-WAR-H002',Conqueror:'S1-WAR-H003',Paladin:'S1-WAR-H005',Crusader:'S1-WAR-H006'},out={};Object.keys(cases).forEach(function(name){out[name]=!!attachmentPolicyForCard(c,{card_id:cases[name]});});return out;},
    completeOpeningFlow:function(firstSide,flipData){var before=(appState&&appState.presentationEvents||[]).length;completeOpeningFlow(appState,firstSide,flipData||{},{deferAnimation:true,bridgeImmediate:true});var events=(appState&&appState.presentationEvents||[]).slice(before);return{ok:true,events:clone(events),snapshot:glPvpBridgeSnapshot()};},
    testCoinOutcomeFromUint32:coinOutcomeFromUint32,
    testRoundAdvanceAfterEnd:function(firstSide,endingSide,round){var probe={round:Number(round||1),openingCoinFlip:{firstPlayer:firstSide==='AI'?'AI':'PLAYER'}};var advanced=advanceRoundAfterCompletedTurnPair(probe,endingSide==='AI'?'AI':'PLAYER');return{advanced:advanced,round:probe.round};},
    renderCurrentAuthoritativePendingChoice:renderCurrentAuthoritativePendingChoice,
    getStarterDeckOptions:function(){ return clone(STARTER_DECK_OPTIONS); },
    getSetupFormationIds:function(side){var d=decks[side]||{};var out={};LANE_ORDER.forEach(function(lane){var r1=d.default_formation&&d.default_formation[lane];out[lane]=setupRankTwoId(d,r1);});return out;},
    setDeckSelections:function(playerDeckKey, player2DeckKey){
      glPvpApplyDeckChoice('PLAYER', playerDeckKey, null);
      glPvpApplyDeckChoice('AI', player2DeckKey, null);
      appState=null; matchStarted=false; render();
      return {ok:true, selectedDeckKey:clone(selectedDeckKey)};
    },
    applyServerIntent:function(intent,args){
      args=args||[];
      var name=String(intent||'');
      var map={
        acknowledgePvpTurnStart:acknowledgePvpTurnStart,
        advancePhase:advancePhase,
        cancelPendingAction:cancelPendingAction,
        beginPlayFromHand:beginPlayFromHand,
        beginTributeFromHand:beginTributeFromHand,
        chooseHeroFromBoard:chooseHeroFromBoard,
        setArrowBarrageSpend:setArrowBarrageSpend,
        commitMagicalSurgeChoice:commitMagicalSurgeChoice,
        commitDrawReplacementChoice:commitDrawReplacementChoice,
        selectStatusRemovalChoice:selectStatusRemovalChoice,
        selectSaintPurifyChoice:selectSaintPurifyChoice,
        resolveStonebloodChoice:resolveStonebloodChoice,
        resolveSecondChanceChoice:resolveSecondChanceChoice,
        selectScoutingExpChoice:selectScoutingExpChoice,
        selectOpponentHandChoice:selectOpponentHandChoice,
        commitOpponentHandChoice:commitOpponentHandChoice,
        moveCrystalBallOrder:moveCrystalBallOrder,
        performDualArrowPairChoice:performDualArrowPairChoice,
        selectResponseExtraDiscardChoice:selectResponseExtraDiscardChoice,
        commitResponseExtraDiscardChoice:commitResponseExtraDiscardChoice,
        toggleDiscardIndex:toggleDiscardIndex,
        selectCardSearchChoice:selectCardSearchChoice,
        selectLegacyDefeatChoice:selectLegacyDefeatChoice,
        selectLegacyCostChoice:selectLegacyCostChoice,
        selectLegacyCardChoice:selectLegacyCardChoice,
        responseSelectNoStuck:responseSelectNoStuck,
        confirmSelectedResponse:confirmSelectedResponse,
        responsePassNoStuck:responsePassNoStuck,
        performOptionalSwapDecision:performOptionalSwapDecision,
        performOptionalTargetSwapDecision:performOptionalTargetSwapDecision,
        beginActivatedHeroAbility:function(side,lane,abilityId){ return beginActivatedHeroAbility(appState,side,lane,abilityId); },
        beginActivatedLegacyAbility:function(side,lane,abilityId){ return beginActivatedLegacyAbility(appState,side,lane,abilityId); },
        beginActivatedRacialAbility:function(side,lane,abilityId){ return beginActivatedRacialAbility(appState,side,lane,abilityId); },
        performManualReposition:performManualReposition,
        openManualRepositionChoice:openManualRepositionChoice,
        handleChoiceConfirm:handleChoiceConfirm,
        executeConfirmedSurrender:executeConfirmedSurrender
      };
      if(!map[name]) return {ok:false,error:'Unknown Local AI runtime intent: '+name};
      try{
        var rv=map[name].apply(null,args);
        if(rv===false) return {ok:false,error:'Runtime action was not legal or no pending choice accepted: '+name, snapshot:glPvpBridgeSnapshot()};
        return {ok:true, snapshot:glPvpBridgeSnapshot()};
      }catch(err){
        return {ok:false,error:String(err&&err.message||err), snapshot:glPvpBridgeSnapshot()};
      }
    }
  };
  var __glPvpBaseRender=render;
  render=function(){
    var out=__glPvpBaseRender.apply(this, arguments);
    if(!__glPvpSilentRender){
      setTimeout(function(){
        if(window.GL_LOCAL_AI_BRIDGE && window.GL_PVP_SHARED_BOARD_ACTIVE){
          try{ window.dispatchEvent(new CustomEvent('gl-local-state-rendered',{detail:window.GL_LOCAL_AI_BRIDGE.getSnapshot()})); }catch(e){}
        }
      },0);
    }
    return out;
  };

  function installPvpAnimationAssistStyle(){if(!document||document.getElementById('glPvpAnimationAssistStyle'))return;var s=document.createElement('style');s.id='glPvpAnimationAssistStyle';s.textContent='@keyframes glPvpRepositionFlash{0%{box-shadow:0 0 0 rgba(255,216,77,0)}45%{box-shadow:0 0 24px rgba(255,216,77,.9)}100%{box-shadow:0 0 0 rgba(255,216,77,0)}} .gl-pvp-reposition-flash{animation:glPvpRepositionFlash .7s ease}';document.head.appendChild(s);}
  function boot(){ initCards(); installPvpAnimationAssistStyle(); installVisualDecodeObserver(); preloadCriticalVisualAssets(); render(); bind(); }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded', boot); else boot();

  function simulateV350RangePrimalAuraAudit(){
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H004'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHand=['S1-ARC-023'];
      var lanes=legalTargetLanesFor(s,'PLAYER','S1-ARC-023','LEFT'); if(lanes.length!==3) return {ok:false, reason:'Range Attack did not target all lanes', lanes:lanes};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.racial=2; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.aiHeroes.CENTER.hp=70; if(getActivatedRacialAbilities(s,'PLAYER','LEFT').length) return {ok:false, reason:'Primal Strike available in Deploy'};
      s.phase='Battle';s.round=2; var ab=getActivatedRacialAbilities(s,'PLAYER','LEFT'); if(!ab.length || ab[0].abilityId!=='primal_strike') return {ok:false, reason:'Primal Strike not available in Battle', abilities:ab};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30; s.playerHeroes.LEFT.card_id='S1-ARC-H005'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=[null,null]; s.playerHand=['S1-ARC-021']; s.aiHeroes.CENTER.hp=100; beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','LEFT'); chooseHeroFromBoard('AI','CENTER'); if(!s.pendingCastings || !s.pendingCastings.some(function(pc){return pc.card_id==='S1-ARC-021' && pc.resolve_phase==='counter_draw' && pc.counters===0;})) return {ok:false, reason:'Aura Infusion Bolt did not enter counter casting', pending:s.pending, pendingCastings:s.pendingCastings};
      if(s.playerHeroes.LEFT.attachments.indexOf('S1-ARC-021')<0) return {ok:false, reason:'Aura Infusion Bolt did not occupy Attachment Slot', attachments:s.playerHeroes.LEFT.attachments};
      if(attachmentCounterLabel(s.playerHeroes.LEFT,'S1-ARC-021')!=='(0)') return {ok:false, reason:'Aura Infusion counter label missing at 0', label:attachmentCounterLabel(s.playerHeroes.LEFT,'S1-ARC-021')};
      var before=s.aiHeroes.CENTER.hp; for(var i=0;i<5;i++){ s.playerDeck.unshift('S1-WAR-001'); drawOne(s,'PLAYER',false); }
      if(s.aiHeroes.CENTER.hp>=before) return {ok:false, reason:'Aura Infusion Bolt did not resolve after 5 counters', before:before, after:s.aiHeroes.CENTER.hp, castings:s.pendingCastings};
      if(s.playerHeroes.LEFT.attachments.indexOf('S1-ARC-021')!==-1 || s.playerDiscard.indexOf('S1-ARC-021')<0) return {ok:false, reason:'Aura Infusion Bolt did not leave Attachment to Discard after resolve', attachments:s.playerHeroes.LEFT.attachments, discard:s.playerDiscard};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Draw'; s.playerHeroes.LEFT.card_id='S1-ARC-H005'; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.playerHeroes.LEFT.exhausted=false; s.playerDeck=['S1-WAR-001','S1-MAG-001']; s.playerHand=[]; resolveDrawPhase(s,'PLAYER'); if(!s.pending || s.pending.type!=='draw_replacement_choice' || s.pending.abilityId!=='quick_reload') return {ok:false, reason:'Quick Reload did not open Draw Review', pending:s.pending}; commitDrawReplacementChoice(true); if(s.pending) return {ok:false, reason:'Draw replacement pending not cleared', pending:s.pending}; if(Number(s.cardsDrawnThisTurn.PLAYER)!==2) return {ok:false, reason:'Draw This Turn counter did not count draw + redraw', count:s.cardsDrawnThisTurn}; if(heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT).indexOf('Draw This Turn:')>=0 && heroClass(s.playerHeroes.LEFT)!=='Arbalest' && heroClass(s.playerHeroes.LEFT)!=='Grand Arbalest') return {ok:false, reason:'Draw This Turn label shown on non-Arbalest hero'};
      return {ok:true, rangeAttackAllLanes:true, primalStrikeBattleOnly:true, auraInfusionCounterCasting:true, drawReplacementChoice:true, drawThisTurnCounterInternal:true, drawThisTurnPreviewRestricted:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }

  function simulateV365RegressionAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s;
      s.playerHeroes.LEFT.card_id='S1-ARC-H005'; s.playerHeroes.LEFT.attachments=['S1-ARC-021',null]; s.pendingCastings=[{card_id:'S1-ARC-021',side:'PLAYER',source_lane:'LEFT',attachmentSlot:0,resolve_phase:'counter_draw',counters:2}]; s.activeAttachments=[];
      if(attachmentCounterLabel(s.playerHeroes.LEFT,'S1-ARC-021')!=='(2)') return {ok:false,reason:'Attachment counter did not restore from pending casting authority',label:attachmentCounterLabel(s.playerHeroes.LEFT,'S1-ARC-021')};
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle';s.round=2; s.mana=10; s.playerHand=['S1-THF-022']; s.playerHeroes.CENTER.card_id='S1-THF-H006'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=100; s.aiHeroes.LEFT.card_id='S1-WAR-H001'; s.aiHeroes.LEFT.hp=100; s.aiHeroes.LEFT.exhausted=false;
      var centerBefore=s.playerHeroes.CENTER.card_id, leftBefore=s.playerHeroes.LEFT.card_id; openAttackResponseWindow(s,{card_id:'S1-WAR-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'},{damage:30,persistent:false,attachmentSlot:-1}); var step=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-THF-022';}); if(!step) return {ok:false,reason:'Step In response option missing',options:s.responseWindow.options}; var hpBefore=s.playerHeroes.CENTER.hp; resolveResponseWindow(step); if(s.playerHeroes.LEFT.card_id!==centerBefore || s.playerHeroes.CENTER.card_id!==leftBefore) return {ok:false,reason:'Step In did not reposition responder directly in front of attacker',left:s.playerHeroes.LEFT.card_id,center:s.playerHeroes.CENTER.card_id}; var moved=LANE_ORDER.map(function(l){return s.playerHeroes[l];}).find(function(h){return h&&h.card_id===centerBefore;}); if(!moved || moved.hp!==hpBefore) return {ok:false,reason:'Step In dodge did not preserve responder HP',hero:moved};
      s=buildInitialMatchState(); appState=s; s.pending={type:'draw_replacement_choice',side:'PLAYER',decision_side:'PLAYER',source_lane:'LEFT',drawn_card_id:'S1-THF-024',hand_index:0,abilityId:'quick_reload',abilityName:'Quick Reload'}; s.playerHand=['S1-THF-024']; s.cardsDrawnThisTurn=s.cardsDrawnThisTurn||{PLAYER:0,AI:0}; s.cardsDrawnThisTurn.PLAYER=1; SUPPRESS_RENDER=false; renderDrawReplacementChoice(); SUPPRESS_RENDER=true; var reloadHtml=$('choiceBody').innerHTML||''; if(reloadHtml.indexOf('response-context-card')<0 || reloadHtml.indexOf('response-options-wrap')<0 || reloadHtml.indexOf('data-draw-replacement-choice=\"redraw\"')<0) return {ok:false,reason:'Quick Reload popup does not use PvP choice-context layout',html:reloadHtml.slice(0,500)};
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Battle';s.round=2; s.aiMana=20; s.aiHand=['S1-WAR-001']; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.hp=120; s.playerHeroes.CENTER.maxHp=120; commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER'}); resolveResponseWindow(null); var atkEvt=(s.opponentPlayedEvents||[]).find(function(e){return e.label==='ATK';}); if(!atkEvt || !(atkEvt.result_lines||[]).some(function(l){return /HP:\s*120\s*->\s*\d+/.test(l);})) return {ok:false,reason:'Opponent Played attack HP before/after missing',event:atkEvt};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=20; s.aiMana=20; s.playerHeroes.CENTER.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.exhausted=false; s.aiHeroes.CENTER.card_id='S1-WAR-H001'; s.aiHeroes.CENTER.hp=120; s.aiHeroes.CENTER.maxHp=120; s.aiHand=['S1-WAR-003']; openAttackResponseWindow(s,{card_id:'S1-WAR-001',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'},{damage:30,persistent:false,attachmentSlot:-1}); var aiDef=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-WAR-003';}); if(!aiDef) return {ok:false,reason:'AI defense response option missing',options:s.responseWindow.options}; resolveResponseWindow(aiDef); var defEvt=(s.opponentPlayedEvents||[]).find(function(e){return e.label==='DEF';}); if(!defEvt || !(defEvt.result_lines||[]).some(function(l){return /HP:\s*120\s*->\s*\d+/.test(l);})) return {ok:false,reason:'Opponent Played defense HP before/after missing',event:defEvt};
      s=buildInitialMatchState(); appState=s; s.turn='AI'; s.phase='Reform'; s.aiHand=['S1-WAR-001']; s.tributeUsedThisReform=false; s.aiHeroes.CENTER.exp_total=0; s.aiHeroes.CENTER.exp_cards=[]; var tributeAction={type:'TRIBUTE_CARD',card_id:'S1-WAR-001',hand_index:0,target_lane:'CENTER'}; var legal=getLegalActions('AI',s).find(function(a){return a.type==='TRIBUTE_CARD'&&a.card_id==='S1-WAR-001'&&a.target_lane==='CENTER';}); if(legal) tributeAction=legal; if(!commitAITributeToHero(s,tributeAction)) return {ok:false,reason:'AI tribute test action failed'}; var tr=(s.opponentPlayedEvents||[]).find(function(e){return e.label==='TRIBUTE';}); if(!tr || !(tr.result_lines||[]).some(function(l){return /EXP:\s*0\s*->\s*\d+/.test(l);})) return {ok:false,reason:'Opponent Played tribute EXP before/after missing',event:tr};
      return {ok:true,quickReloadPopup:'PvP choice-context layout',stepInReposition:true,attachmentCounters:true,opponentAttackHpDelta:true,opponentDefenseHpDelta:true,opponentTributeExpDelta:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }
  function simulateV366RuntimeUpdateAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    try{
      var stack=window.GL_SOURCE_STACK||{};
      if(stack.runtime_data!=='v0.12.7' || stack.effect_checkpoint!=='v0.11.6' || stack.effect_recipe!=='v0.11.7' || stack.runtime_core!=='v0.50' || stack.runtime_foundation!=='v1.82' || stack.shared_manual!=='v1.39' || stack.local_ai!=='v6.1') return {ok:false,reason:'Active source stack mismatch',stack:stack};
      var soul=card('S1-ARC-016'), marksman={card_id:'S1-ARC-H002',hp:100,maxHp:100}, grand={card_id:'S1-ARC-H003',hp:120,maxHp:120};
      if(currentClassDamage(soul,marksman,null)!==30 || currentClassDamage(soul,grand,null)!==50) return {ok:false,reason:'Soul Blast Shot current runtime damage mismatch',marksman:currentClassDamage(soul,marksman,null),grand:currentClassDamage(soul,grand,null)};
      if(isChargeSwapCard(card('S1-THF-010'))) return {ok:false,reason:'Back Stab stale swap metadata still executable'};
      var st=buildInitialMatchState(); appState=st; st.aiHeroes.CENTER.card_id='S1-WAR-H001'; st.aiHeroes.CENTER.hp=80; st.aiHeroes.LEFT=sanitizeLegacySlot({card_id:'S1-WAR-L001',side:'AI',lane:'LEFT',mode:'LEGACY',legacy_mode:true}); st.aiHeroes.RIGHT.card_id='S1-MAG-H001'; st.aiHeroes.RIGHT.hp=80;
      var opts=soulBlastTargetSwapOptions(st,{card_id:'S1-ARC-016',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'});
      if(opts.length!==2 || opts.indexOf('LEFT')===-1 || opts.indexOf('RIGHT')===-1) return {ok:false,reason:'Soul Blast Shot must offer adjacent Hero or Legacy',options:opts};
      st=buildInitialMatchState(); appState=st; st.playerHeroes.LEFT.card_id='S1-THF-H001'; st.playerHeroes.LEFT.hp=80; st.playerHeroes.CENTER=sanitizeLegacySlot({card_id:'S1-THF-L001',side:'PLAYER',lane:'CENTER',mode:'LEGACY',legacy_mode:true});
      var act={card_id:'S1-THF-002',source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'CENTER'};
      if(!optionalSwapAvailable(st,act)) return {ok:false,reason:'Source swap did not allow Hero-to-Legacy'};
      st.playerHeroes.CENTER={card_id:'S1-WAR-H001',hp:80,maxHp:80,attachments:[null,null],statuses:[],lane:'CENTER'};
      if(!optionalSwapAvailable(st,act)) return {ok:false,reason:'Source swap did not allow active Hero-to-Hero'};
      st.pending=null;
      if(resolvePostAttackChoices(st,act,{optional_swap_after:true},{damage:0,dodged:true}) || st.pending) return {ok:false,reason:'Dodged attack opened post-attack swap',pending:st.pending};
      return {ok:true,sourceStack:true,soulBlastDamage:'30/50',backStabSwapRemoved:true,heroLegacySwap:true,dodgeStopsSwap:true,quickReloadPreserved:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }
  window.GL_V366_RUNTIME_UPDATE_QA_SELF_TEST=simulateV366RuntimeUpdateAudit;
  window.GL_V365_REGRESSION_QA_SELF_TEST=simulateV365RegressionAudit;
  window.GL_V351_DRAW_REPLACEMENT_AURA_AUDIT_SELF_TEST=simulateV350RangePrimalAuraAudit;
  window.GL_V350_RANGE_PRIMAL_AURA_AUDIT_SELF_TEST=simulateV350RangePrimalAuraAudit;

  function simulateV367RuntimeUpdateAudit(){
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true; initCards();
      if(!assertActiveRuntimeSourceStack()) return {ok:false,reason:'active source guard failed'};
      var s=buildInitialMatchState(); appState=s; matchStarted=true; s.phase='Battle';s.round=2; s.mana=10; s.aiMana=10;
      var src=s.playerHeroes.CENTER; src.card_id='S1-THF-H005'; src.hp=120; src.maxHp=120; src.statuses=[];
      var target=s.aiHeroes.CENTER; target.hp=100; target.maxHp=100; target.statuses=[];
      var blocked=applyResolvedAttackDamage(s,'S1-MAG-021','PLAYER','CENTER','AI','CENTER',40,'Magical',false,40,false,false);
      applySourcePostAttackEffects(s,'S1-MAG-021',src,'PLAYER','CENTER','AI','CENTER',blocked);
      if(!blocked.connected || blocked.damage!==0 || s.mana!==11 || s.aiMana!==9) return {ok:false,reason:'Mana Absorption block-to-zero connected hit failed',blocked:blocked,mana:s.mana,aiMana:s.aiMana};
      s.mana=10; s.aiMana=10; target.hp=100;
      var dodged=applyResolvedAttackDamage(s,'S1-MAG-021','PLAYER','CENTER','AI','CENTER',40,'Magical',true,0,false,false);
      applySourcePostAttackEffects(s,'S1-MAG-021',src,'PLAYER','CENTER','AI','CENTER',dodged);
      if(dodged.connected || s.mana!==10 || s.aiMana!==10) return {ok:false,reason:'Mana Absorption dodge exclusion failed',dodged:dodged};
      src.card_id='S1-THF-H006'; s.aiMana=10; target.hp=100;
      var slash=applyResolvedAttackDamage(s,'S1-MAG-023','PLAYER','CENTER','AI','CENTER',50,'Magical',false,50,false,false);
      applySourcePostAttackEffects(s,'S1-MAG-023',src,'PLAYER','CENTER','AI','CENTER',slash);
      if(s.aiMana!==7) return {ok:false,reason:'Arcane Duelist Aether Slash block-to-zero removal failed',aiMana:s.aiMana};
      src.card_id='S1-THF-H005'; src.statuses=[]; src.attachments=['S1-MAG-022',null]; s.activeAttachments=[{attachment_id:'QA:AETHER',side:'PLAYER',lane:'CENTER',slot:0,card_id:'S1-MAG-022',tick_phase:'END_PHASE',remaining_count:2,created_phase:'Deploy',created_turn:'PLAYER',created_round:0}]; s.aiMana=10; target.hp=100;
      var infusion=applyResolvedAttackDamage(s,'S1-THF-001','PLAYER','CENTER','AI','CENTER',20,'Magical',false,20,false,false);
      applySourcePostAttackEffects(s,'S1-THF-001',src,'PLAYER','CENTER','AI','CENTER',infusion);
      if(s.aiMana!==10) return {ok:false,reason:'Aether Infusion incorrectly triggered on block-to-zero',aiMana:s.aiMana,infusion:infusion};
      target.hp=100; var infusionDamage=applyResolvedAttackDamage(s,'S1-THF-001','PLAYER','CENTER','AI','CENTER',20,'Magical',false,0,false,false);
      applySourcePostAttackEffects(s,'S1-THF-001',src,'PLAYER','CENTER','AI','CENTER',infusionDamage);
      if(s.aiMana!==9) return {ok:false,reason:'Aether Infusion did not trigger after actual HP damage',aiMana:s.aiMana,infusion:infusionDamage};
      s.aiMana=10; applySourcePostAttackEffects(s,'S1-MAG-024',src,'PLAYER','CENTER','AI',null,{damage:0,connected_hits:2});
      if(s.aiMana!==8) return {ok:false,reason:'Aether Sweep connected-hit count failed',aiMana:s.aiMana};
      return {ok:true,sourceStack:'v0.12.5/v0.11.5/v0.11.4/v0.43/v1.73',manaAbsorption:'dodge excluded / block-to-zero connected',aetherSlash:'Arcane Duelist removes 3',aetherInfusion:'actual HP damage only',aetherSweep:'per connected target'};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; }
  }
  window.GL_V367_RUNTIME_UPDATE_QA_SELF_TEST=simulateV367RuntimeUpdateAudit;

  function simulateV368LegacyStepInHistoryAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    try{
      matchStarted=true; SUPPRESS_RENDER=false;
      var s=buildInitialMatchState(); appState=s;
      swapBoardStateLanes(s,'PLAYER','LEFT','CENTER');
      s.playerHeroes.LEFT.card_id='S1-WAR-H002'; s.playerHeroes.LEFT.hp=0; s.playerHeroes.LEFT.maxHp=120;
      delete s.playerHeroes.LEFT.assigned_legacy_card_id; delete s.playerHeroes.LEFT.legacy_lineage_id; delete s.playerHeroes.LEFT.legacy_package_id;
      var zeroPanel=heroPanel('PLAYER','LEFT',s.playerHeroes.LEFT);
      if(zeroPanel.indexOf('Defeated')<0 || zeroPanel.indexOf('>Ready<')>=0) return {ok:false,reason:'0 HP HERO still renders Ready instead of Defeated',panel:zeroPanel};
      syncCounts(s);
      if(!s.pending || s.pending.type!=='legacy_defeat_choice' || (s.pending.candidates||[]).indexOf('S1-WAR-L001')<0) return {ok:false,reason:'Rank II missing-binding defeat did not open lane-package Legacy choice',pending:s.pending};
      selectLegacyDefeatChoice((s.pending.candidates||[]).indexOf('S1-WAR-L001'));
      if(!commitLegacyDefeatChoice() || !isLegacySlot(s.playerHeroes.LEFT)) return {ok:false,reason:'Legacy choice did not replace defeated Hero',left:s.playerHeroes.LEFT};

      // v3.69 restores the defensive card to the response list. The dodge can resolve even if
      // the attacker-facing slot becomes Legacy before resolution; the reposition portion then skips.
      s=buildInitialMatchState(); appState=s; SUPPRESS_RENDER=true; s.turn='AI'; s.phase='Battle';s.round=2; s.playerHand=['S1-THF-022'];
      s.playerHeroes.CENTER.card_id='S1-THF-H005'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.maxHp=100;
      s.playerHeroes.LEFT=sanitizeLegacySlot({card_id:'S1-WAR-L001',side:'PLAYER',lane:'LEFT',mode:'LEGACY',legacy_mode:true,defeated_hero_snapshot:{card_id:'S1-WAR-H002'}});
      var incoming={damage:40,damage_type:'Physical',attack_type:'Single Target',card_id:'S1-WAR-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'};
      if(!responseOptionsFor(s,'PLAYER',incoming).some(function(o){return o.card_id==='S1-THF-022';})) return {ok:false,reason:'Step In was incorrectly removed from a legal Dodge response window'};

      s.playerHeroes.LEFT={card_id:'S1-WAR-H001',side:'PLAYER',lane:'LEFT',hp:100,maxHp:100,exp_total:0,exp_cards:[],statuses:[],attachments:[null,null],exhausted:false};
      var opts=responseOptionsFor(s,'PLAYER',incoming), step=opts.find(function(o){return o.card_id==='S1-THF-022';});
      if(!step) return {ok:false,reason:'Step In missing with valid active allied destination',options:opts};
      var beforeCenter=heroIdFrom(s.playerHeroes.CENTER), beforeLeft=heroIdFrom(s.playerHeroes.LEFT);
      s.responseWindow={kind:'incoming_attack',response_owner:'PLAYER',card_id:incoming.card_id,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER',damage:40,damage_type:'Physical',attack_type:'Single Target',options:[step],selected:null,post:{damage:40,persistent:false,attachmentSlot:-1},action:{card_id:incoming.card_id,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'}};
      s.pending={type:'response_window',card_id:incoming.card_id};
      resolveResponseWindow(step);
      if(heroIdFrom(s.playerHeroes.LEFT)!==beforeCenter || heroIdFrom(s.playerHeroes.CENTER)!==beforeLeft) return {ok:false,reason:'Step In did not swap actual board Hero instances',left:s.playerHeroes.LEFT,center:s.playerHeroes.CENTER};
      return {ok:true,legacyRankUpFallback:true,noZeroHpReady:true,stepInResponseRestored:true,stepInBoardSwap:true,opponentHistoryScrollCss:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }
  window.GL_V368_LEGACY_STEPIN_HISTORY_QA_SELF_TEST=simulateV368LegacyStepInHistoryAudit;

  function simulateV369SwapRepositionAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    function fresh(){ var s=buildInitialMatchState(); appState=s; matchStarted=true; SUPPRESS_RENDER=true; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=99; s.aiMana=99; return s; }
    function setHero(h,id,hp){ h.card_id=id; h.hp=hp||100; h.maxHp=hp||100; h.statuses=[]; h.attachments=[null,null]; h.exhausted=false; }
    function runSourceSwap(cardId,sourceLane,targetLane,expectedOptions){
      var s=fresh(); setHero(s.playerHeroes[sourceLane], cardId.indexOf('WAR')>0?'S1-WAR-H003':'S1-THF-H006',120);
      LANE_ORDER.forEach(function(l){ if(l!==sourceLane) setHero(s.playerHeroes[l], l==='LEFT'?'S1-WAR-H001':'S1-CLE-H001',100); });
      setHero(s.aiHeroes[targetLane],'S1-WAR-H001',200);
      var action={card_id:cardId,source_side:'PLAYER',source_lane:sourceLane,target_side:'AI',target_lane:targetLane};
      var opts=sourceSwapOptions(s,action);
      if(JSON.stringify(opts)!==JSON.stringify(expectedOptions)) return {ok:false,reason:cardId+' swap options mismatch',opts:opts,expected:expectedOptions};
      var sourceId=heroIdFrom(s.playerHeroes[sourceLane]), partner=expectedOptions[0], partnerId=heroIdFrom(s.playerHeroes[partner]);
      s.playerHeroes[sourceLane].statuses=[{name:'Swap Marker',turns:2}]; s.playerHeroes[sourceLane].attachments=['S1-ITM-010',null];
      s.activeAttachments=[{side:'PLAYER',lane:sourceLane,slot:0,card_id:'S1-ITM-010',expires:'after_next_attack_or_end_battle'}];
      var opened=resolvePostAttackChoices(s,action,{optional_swap_after:true},{damage:0,hp_damage:0,connected:true,blocked_to_zero:true});
      if(!opened || !s.pending || s.pending.type!=='optional_swap') return {ok:false,reason:cardId+' successful attack did not open optional reposition',pending:s.pending};
      if(JSON.stringify(s.pending.swap_options)!==JSON.stringify(expectedOptions)) return {ok:false,reason:cardId+' pending options mismatch',pending:s.pending};
      performOptionalSwapDecision(partner);
      if(heroIdFrom(s.playerHeroes[partner])!==sourceId || heroIdFrom(s.playerHeroes[sourceLane])!==partnerId) return {ok:false,reason:cardId+' did not swap actual Hero instances',heroes:s.playerHeroes};
      var movedAttachment=(s.activeAttachments||[]).find(function(a){ return a.card_id==='S1-ITM-010'; });
      if(!hasStatus(s.playerHeroes[partner],'Swap Marker') || s.playerHeroes[partner].attachments[0]!=='S1-ITM-010' || !movedAttachment || movedAttachment.lane!==partner) return {ok:false,reason:cardId+' did not move status/attachment with Hero instance',hero:s.playerHeroes[partner],activeAttachments:s.activeAttachments};
      return {ok:true};
    }
    try{
      var sourceCards=['S1-THF-002','S1-THF-019','S1-THF-025','S1-WAR-015','S1-WAR-024'];
      for(var i=0;i<sourceCards.length;i++){ var rr=runSourceSwap(sourceCards[i],'LEFT','CENTER',['CENTER']); if(!rr.ok) return rr; }
      var flash=runSourceSwap('S1-THF-023','CENTER','CENTER',['LEFT','RIGHT']); if(!flash.ok) return flash;

      var s=fresh(); setHero(s.playerHeroes.CENTER,'S1-THF-H006',120); setHero(s.playerHeroes.LEFT,'S1-WAR-H001',100); setHero(s.playerHeroes.RIGHT,'S1-CLE-H001',100); s.playerHand=['S1-THF-022'];
      var incoming={damage:40,damage_type:'Physical',attack_type:'Single Target',card_id:'S1-WAR-001',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'};
      var step=responseOptionsFor(s,'PLAYER',incoming).find(function(o){ return o.card_id==='S1-THF-022'; });
      if(!step) return {ok:false,reason:'Step In missing from legal Dodge response options'};
      var beforeCenter=heroIdFrom(s.playerHeroes.CENTER), beforeLeft=heroIdFrom(s.playerHeroes.LEFT);
      s.responseWindow={kind:'incoming_attack',response_owner:'PLAYER',card_id:incoming.card_id,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER',damage:40,damage_type:'Physical',attack_type:'Single Target',options:[step],selected:null,post:{damage:40,persistent:false,attachmentSlot:-1},action:{card_id:incoming.card_id,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'}}; s.pending={type:'response_window',card_id:incoming.card_id};
      resolveResponseWindow(step);
      if(heroIdFrom(s.playerHeroes.LEFT)!==beforeCenter || heroIdFrom(s.playerHeroes.CENTER)!==beforeLeft) return {ok:false,reason:'Step In dodge did not reposition actual Heroes',heroes:s.playerHeroes};

      s=fresh(); setHero(s.aiHeroes.CENTER,'S1-WAR-H001',150); setHero(s.aiHeroes.LEFT,'S1-MAG-H001',100); setHero(s.aiHeroes.RIGHT,'S1-CLE-H001',100);
      var soulAction={card_id:'S1-ARC-016',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'};
      if(!resolvePostAttackChoices(s,soulAction,{}, {damage:30,hp_damage:30,connected:true}) || !s.pending || s.pending.type!=='optional_target_swap') return {ok:false,reason:'Soul Blast HP damage did not open target-adjacent reposition',pending:s.pending};
      var soulOpts=s.pending.swap_options||[]; if(JSON.stringify(soulOpts)!==JSON.stringify(['LEFT','RIGHT'])) return {ok:false,reason:'Soul Blast adjacent target options failed',opts:soulOpts};
      var targetId=heroIdFrom(s.aiHeroes.CENTER), leftId=heroIdFrom(s.aiHeroes.LEFT); performOptionalTargetSwapDecision('LEFT');
      if(heroIdFrom(s.aiHeroes.LEFT)!==targetId || heroIdFrom(s.aiHeroes.CENTER)!==leftId) return {ok:false,reason:'Soul Blast target swap did not update board',heroes:s.aiHeroes};

      s=fresh(); setHero(s.playerHeroes.CENTER,'S1-WAR-H003',140); setHero(s.playerHeroes.LEFT,'S1-CLE-H001',100); setHero(s.aiHeroes.CENTER,'S1-WAR-H001',100); s.playerHand=['S1-WAR-004'];
      var coverIncoming={damage:40,damage_type:'Physical',attack_type:'Single Target',card_id:'S1-WAR-001',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'LEFT'};
      var cover=responseOptionsFor(s,'PLAYER',coverIncoming).find(function(o){ return o.card_id==='S1-WAR-004' && o.source_lane==='CENTER'; });
      if(!cover) return {ok:false,reason:'Cover Up response option missing'};
      var coverId=heroIdFrom(s.playerHeroes.CENTER), protectedId=heroIdFrom(s.playerHeroes.LEFT); s.responseWindow={kind:'incoming_attack',response_owner:'PLAYER',card_id:coverIncoming.card_id,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'LEFT',damage:40,damage_type:'Physical',attack_type:'Single Target',options:[cover],selected:null,post:{damage:40,persistent:false,attachmentSlot:-1},action:{card_id:coverIncoming.card_id,source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'LEFT'}}; s.pending={type:'response_window',card_id:coverIncoming.card_id}; resolveResponseWindow(cover);
      if(heroIdFrom(s.playerHeroes.LEFT)!==coverId || heroIdFrom(s.playerHeroes.CENTER)!==protectedId) return {ok:false,reason:'Cover Up did not swap before redirect',heroes:s.playerHeroes};

      s=fresh(); setHero(s.playerHeroes.LEFT,'S1-THF-H001',100); setHero(s.playerHeroes.CENTER,'S1-WAR-H001',100); var dodgeAction={card_id:'S1-THF-002',source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'CENTER'};
      if(resolvePostAttackChoices(s,dodgeAction,{optional_swap_after:true},{damage:0,hp_damage:0,dodged:true}) || s.pending) return {ok:false,reason:'Dodged source-swap attack incorrectly opened reposition',pending:s.pending};
      if(isChargeSwapCard(card('S1-THF-010'))) return {ok:false,reason:'Back Stab stale swap returned'};
      return {ok:true,stepIn:true,sourceFrontCards:sourceCards,flashSlashAnyAllied:true,soulBlastTargetAdjacent:true,coverUp:true,backStabNoSwap:true,blockedToZeroStillSwaps:true,heroStateMovesWithInstance:true};
    } finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }
  window.GL_V369_SWAP_REPOSITION_QA_SELF_TEST=simulateV369SwapRepositionAudit;

  function simulateV400SwapRepositionHotfixAudit(){
    initCards();
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
    function fresh(){ var s=buildInitialMatchState(); appState=s; matchStarted=true; SUPPRESS_RENDER=true; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=99; s.aiMana=99; return s; }
    function setHero(h,id,hp){ h.card_id=id; h.hp=hp||100; h.maxHp=hp||100; h.statuses=[]; h.attachments=[null,null]; h.exhausted=false; h.mode='HERO'; h.legacy_mode=false; }
    function legacy(side,lane,id,heroId){ return sanitizeLegacySlot({card_id:id,side:side,lane:lane,mode:'LEGACY',legacy_mode:true,defeated_hero_snapshot:{card_id:heroId||'S1-WAR-H001',hp:0,defeated:true}}); }
    try{
      var sourceFront=['S1-THF-002','S1-THF-019','S1-THF-025','S1-WAR-015','S1-WAR-024'];
      for(var i=0;i<sourceFront.length;i++){
        var s=fresh(), cid=sourceFront[i], sourceId=cid.indexOf('WAR')>0?'S1-WAR-H005':'S1-THF-H006';
        setHero(s.playerHeroes.LEFT,sourceId,120); s.playerHeroes.CENTER=legacy('PLAYER','CENTER','S1-WAR-L001'); setHero(s.aiHeroes.CENTER,'S1-WAR-H001',200);
        var action={card_id:cid,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'CENTER'};
        var opts=sourceSwapOptions(s,action);
        if(JSON.stringify(opts)!==JSON.stringify(['CENTER'])) return {ok:false,reason:cid+' did not offer matching-column Legacy partner',options:opts};
        if(!resolvePostAttackChoices(s,action,{optional_swap_after:true},{damage:0,hp_damage:0,connected:true,blocked_to_zero:true}) || !s.pending || s.pending.type!=='optional_swap') return {ok:false,reason:cid+' did not open optional Hero-Legacy reposition',pending:s.pending};
        performOptionalSwapDecision('CENTER');
        if(!isLegacySlot(s.playerHeroes.LEFT) || heroIdFrom(s.playerHeroes.CENTER)!==sourceId) return {ok:false,reason:cid+' Hero-Legacy swap did not update board',heroes:s.playerHeroes};
      }

      var s=fresh(); setHero(s.playerHeroes.CENTER,'S1-THF-H006',120); s.playerHeroes.LEFT=legacy('PLAYER','LEFT','S1-THF-L001'); setHero(s.playerHeroes.RIGHT,'S1-CLE-H001',100); setHero(s.aiHeroes.CENTER,'S1-WAR-H001',150);
      var flashAction={card_id:'S1-THF-023',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'};
      var flashOpts=sourceSwapOptions(s,flashAction);
      if(flashOpts.indexOf('LEFT')===-1 || flashOpts.indexOf('RIGHT')===-1) return {ok:false,reason:'Flash Slash did not offer both Hero and Legacy partners',options:flashOpts};
      resolvePostAttackChoices(s,flashAction,{optional_swap_after:true},{connected:true}); performOptionalSwapDecision('LEFT');
      if(!isLegacySlot(s.playerHeroes.CENTER) || heroIdFrom(s.playerHeroes.LEFT)!=='S1-THF-H006') return {ok:false,reason:'Flash Slash Hero-Legacy swap failed',heroes:s.playerHeroes};

      s=fresh(); setHero(s.aiHeroes.CENTER,'S1-WAR-H001',150); s.aiHeroes.LEFT=legacy('AI','LEFT','S1-WAR-L001'); setHero(s.aiHeroes.RIGHT,'S1-CLE-H001',100);
      var soulAction={card_id:'S1-ARC-016',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'};
      var soulOpts=soulBlastTargetSwapOptions(s,soulAction);
      if(soulOpts.indexOf('LEFT')===-1 || soulOpts.indexOf('RIGHT')===-1) return {ok:false,reason:'Soul Blast Shot did not offer adjacent Hero and Legacy',options:soulOpts};
      resolvePostAttackChoices(s,soulAction,{defer_reposition_discard:true},{damage:0,hp_damage:0,prevented:true,connected:false});
      if(!s.pending || s.pending.type!=='optional_target_swap') return {ok:false,reason:'Soul Blast Shot connected hit did not open target reposition',pending:s.pending};
      performOptionalTargetSwapDecision('LEFT');
      if(!isLegacySlot(s.aiHeroes.CENTER) || heroIdFrom(s.aiHeroes.LEFT)!=='S1-WAR-H001') return {ok:false,reason:'Soul Blast Shot Hero-Legacy target swap failed',heroes:s.aiHeroes};

      s=fresh(); s.turn='AI'; setHero(s.aiHeroes.LEFT,'S1-WAR-H005',130); s.aiHeroes.CENTER=legacy('AI','CENTER','S1-WAR-L001'); setHero(s.playerHeroes.CENTER,'S1-CLE-H001',100);
      var aiAction={card_id:'S1-WAR-024',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'};
      var aiOpened=resolvePostAttackChoices(s,aiAction,{optional_swap_after:true},{connected:true});
      if(aiOpened || s.pending || !isLegacySlot(s.aiHeroes.LEFT) || heroIdFrom(s.aiHeroes.CENTER)!=='S1-WAR-H005') return {ok:false,reason:'Local AI did not auto-resolve its own optional Heroic Charge swap',opened:aiOpened,pending:s.pending,heroes:s.aiHeroes};

      s=fresh(); s.turn='AI'; setHero(s.aiHeroes.LEFT,'S1-WAR-H005',130); setHero(s.aiHeroes.CENTER,'S1-WAR-H001',100); setHero(s.playerHeroes.CENTER,'S1-WAR-H001',100); s.playerHeroes.CENTER.hp=0; s.playerLegacy=['S1-WAR-L001']; s.playerLegacyPackageSlots=[{slot:'CENTER',legacy:'S1-WAR-L001',progression:'S1-WAR-H001',lineage:'DRAXEN'}];
      if(!applyDefeatCheck(s,'PLAYER','CENTER') || !s.pending || s.pending.type!=='legacy_defeat_choice') return {ok:false,reason:'Legacy gate setup failed',pending:s.pending};
      var lethalAction={card_id:'S1-WAR-024',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'CENTER'};
      resolvePostAttackChoices(s,lethalAction,{optional_swap_after:true},{connected:true,hp_damage:50});
      if(!s.pending || s.pending.type!=='legacy_defeat_choice' || !s.pending.after_legacy_reposition) return {ok:false,reason:'Reposition did not wait behind mandatory Legacy replacement',pending:s.pending};
      if(isLegacySlot(s.aiHeroes.LEFT)) return {ok:false,reason:'Reposition resolved before Legacy replacement'};
      selectLegacyDefeatChoice(0); if(!commitLegacyDefeatChoice()) return {ok:false,reason:'Legacy choice failed during reposition continuation'};
      if(!isLegacySlot(s.playerHeroes.CENTER)) return {ok:false,reason:'Defeated target did not enter Legacy before reposition continuation'};
      if(heroIdFrom(s.aiHeroes.LEFT)!=='S1-WAR-H001' || heroIdFrom(s.aiHeroes.CENTER)!=='S1-WAR-H005' || s.pending) return {ok:false,reason:'Deferred AI Heroic Charge reposition did not resume cleanly after Legacy',pending:s.pending,aiHeroes:s.aiHeroes};

      s=fresh(); setHero(s.playerHeroes.CENTER,'S1-WAR-H005',130); setHero(s.aiHeroes.CENTER,'S1-WAR-H001',100);
      var sameLane={card_id:'S1-WAR-024',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'};
      if(sourceSwapOptions(s,sameLane).length) return {ok:false,reason:'Same-column no-op swap was incorrectly offered'};

      return {ok:true,version:'v4.0',versionBump:false,sourceFrontCards:sourceFront,heroLegacyPartners:true,flashSlashHeroLegacy:true,soulBlastHeroLegacy:true,aiOptionalSwapAutoResolves:true,legacyBeforeReposition:true,stepInAndCoverUpRemainHeroOnly:true,backStabNoSwap:true,sameColumnNoOpSuppressed:true};
    } catch(e){ return {ok:false,error:String(e&&e.stack||e)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; closeResponseWindowUI(); closeChoice(); closeInfo(); }
  }
  window.GL_V400_SWAP_REPOSITION_HOTFIX_QA_SELF_TEST=simulateV400SwapRepositionHotfixAudit;


  function simulateV370SourceParityAudit(){
    try{
      var stack=window.GL_SOURCE_STACK||{};
      if(stack.runtime_data!=='v0.12.7' || stack.effect_checkpoint!=='v0.11.6' || stack.effect_recipe!=='v0.11.7' || stack.runtime_core!=='v0.50' || stack.runtime_foundation!=='v1.82' || stack.shared_manual!=='v1.39' || stack.local_ai!=='v6.1') return {ok:false,reason:'v3.70 source stack mismatch',stack:stack};
      var swap=simulateV369SwapRepositionAudit();
      if(!swap || !swap.ok) return {ok:false,reason:'v3.69 swap bridge regression',detail:swap};
      var step=card('S1-THF-022'), dash=card('S1-THF-019'), flash=card('S1-THF-023'), soul=card('S1-ARC-016');
      if(!step || !dash || !flash || !soul) return {ok:false,reason:'Current swap card data missing'};
      var stepFx=JSON.stringify(step.effect||[]), dashFx=JSON.stringify(dash.effect||[]), flashFx=JSON.stringify(flash.effect||[]), soulFx=JSON.stringify(soul.effect||[]);
      if(stepFx.indexOf('dodge_then_reposition')<0 || dashFx.indexOf('source_may_swap_with_allied_hero_in_front_of_attacked_hero')<0 || flashFx.indexOf('source_may_swap_with_any_allied_hero')<0 || soulFx.indexOf('swap_target_with_adjacent_opponent_hero')<0) return {ok:false,reason:'Current Runtime Data distinct reposition model mismatch'};
      return {ok:true,sourceStack:'v0.12.5 / v0.11.5 / v0.11.4 / v0.43 / v1.73',stepInDodgeFirst:true,sourceFront:true,anyAllied:true,targetAdjacent:true,responseRedirect:true,backStabNoSwap:true};
    }catch(e){ return {ok:false,error:String(e&&e.stack||e)}; }
  }
  window.GL_V370_SOURCE_PARITY_QA_SELF_TEST=simulateV370SourceParityAudit;



  function simulateV370ChargeAttackChoiceZoneAudit(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s; s.pvpHumanVsHuman=false; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30;
      s.playerHeroes.CENTER.card_id='S1-WAR-H002'; s.playerHeroes.CENTER.hp=120; s.playerHeroes.CENTER.maxHp=120; s.playerHeroes.CENTER.exhausted=false;
      s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.RIGHT.exhausted=false; s.aiHeroes.LEFT.hp=100; s.aiHand=[]; s.playerHand=['S1-WAR-015'];
      beginPlayFromHand(0); if(!s.pending || s.pending.type!=='source_selection' || s.playerHand[0]!=='S1-WAR-015') return {ok:false,reason:'Charge Attack left hand before source validation'};
      chooseHeroFromBoard('PLAYER','CENTER'); if(!s.pending || s.pending.type!=='target_selection' || s.playerHand[0]!=='S1-WAR-015') return {ok:false,reason:'Charge Attack left hand before target validation'};
      chooseHeroFromBoard('AI','LEFT');
      if(!s.pending || s.pending.type!=='optional_swap') return {ok:false,reason:'Charge Attack optional swap choice missing',pending:s.pending};
      if(s.playerDiscard.indexOf('S1-WAR-015')!==-1) return {ok:false,reason:'Charge Attack discarded before optional reposition choice'};
      if(s.aiHeroes.LEFT.hp>=100) return {ok:false,reason:'Charge Attack damage did not resolve'};
      performOptionalSwapDecision(false);
      if(s.pending) return {ok:false,reason:'Charge Attack skip did not clear choice',pending:s.pending};
      if(s.playerDiscard.filter(function(id){return id==='S1-WAR-015';}).length!==1) return {ok:false,reason:'Charge Attack did not discard exactly once after choice',discard:s.playerDiscard};
      // Card-effect swap is not blocked by manual Freeze movement restrictions.
      s=buildInitialMatchState(); appState=s; s.pvpHumanVsHuman=false; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30;
      s.playerHeroes.CENTER.card_id='S1-WAR-H002'; s.playerHeroes.CENTER.hp=120; s.playerHeroes.CENTER.maxHp=120; s.playerHeroes.CENTER.exhausted=false;
      addStatus(s.playerHeroes.LEFT,'Freeze',1,'test partner'); s.aiHand=[]; s.playerHand=['S1-WAR-015']; s.aiHeroes.LEFT.hp=100;
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','LEFT');
      if(!s.pending || s.pending.type!=='optional_swap' || (s.pending.swap_options||[]).indexOf('LEFT')===-1) return {ok:false,reason:'Charge Attack wrongly inherited manual Freeze reposition gate',pending:s.pending};
      performOptionalSwapDecision('LEFT');
      if(s.playerHeroes.LEFT.card_id!=='S1-WAR-H002') return {ok:false,reason:'Charge Attack actual Hero-instance swap failed'};
      return {ok:true,sourceTargetValidationKeepsHand:true,damageResolves:true,discardDeferredUntilChoice:true,discardExactlyOnce:true,cardEffectSwapIgnoresManualFreezeGate:true};
    }catch(e){ return {ok:false,error:String(e&&e.stack||e)}; }
    finally { appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; closeChoice(); closeResponseWindowUI(); }
  }
  window.GL_V370_CHARGE_ATTACK_CHOICE_ZONE_QA_SELF_TEST=simulateV370ChargeAttackChoiceZoneAudit;



  function simulateV380CardCustodyAndStarterAudit(){
    var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    try{
      SUPPRESS_RENDER=true; STARTUP_SHUFFLE_ENABLED=false;
      var keys=Object.keys(STARTER_DECK_OPTIONS||{});
      if(keys.length!==5) return {ok:false,reason:'Starter60 v1.2 preset count mismatch',count:keys.length};
      for(var k=0;k<keys.length;k++){
        var d=STARTER_DECK_OPTIONS[keys[k]].deck, ids=deckEntriesToIds(d.main_deck), counts={};
        if(ids.length!==60) return {ok:false,reason:'Starter deck is not 60 cards',preset:keys[k],count:ids.length};
        ids.forEach(function(id){counts[id]=Number(counts[id]||0)+1;});
        var max=Math.max.apply(Math,Object.keys(counts).map(function(id){return counts[id];}));
        if(max>2) return {ok:false,reason:'Starter deck exceeds 2-copy limit',preset:keys[k],max:max};
        if(!d.is_valid || !/v1\.2/i.test(String(d.builder_version||'')+' '+String(d.format||''))) return {ok:false,reason:'Starter60 v1.2 metadata missing',preset:keys[k]};
      }
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=10; s.playerHand=['S1-ITM-005'];
      var beforeMana=s.mana;
      var rolled=commitPlayedCard(s,{card_id:'S1-ITM-005',hand_index:0,source_side:null,source_lane:null,target_side:null,target_lane:null,__qa_force_error_after_commit:true});
      if(rolled!==false || s.playerHand.indexOf('S1-ITM-005')<0 || s.mana!==beforeMana || s.playerDiscard.indexOf('S1-ITM-005')>=0) return {ok:false,reason:'Transactional rollback did not restore card/mana',hand:s.playerHand,mana:s.mana,discard:s.playerDiscard};
      s=buildInitialMatchState(); appState=s; s.playerHand=['S1-EVT-002']; var before=physicalCardCountForSide(s,'PLAYER','S1-EVT-002'); var recoveryToken='qa-recovery-event'; s.playerHand.splice(0,1); beginCommittedCardCustody(s,'PLAYER','S1-EVT-002',before,recoveryToken); s.pending=null; var snapshotBeforeSync=JSON.stringify(s); syncCounts(s);
      if(JSON.stringify(s)!==snapshotBeforeSync || s.playerDiscard.indexOf('S1-EVT-002')>=0) return {ok:false,reason:'Passive sync mutated or recovered gameplay state'};
      finalizeCommittedSourceCard(s,{commit_token:recoveryToken},'PLAYER','S1-EVT-002','discard',{reason:'explicit QA recovery'});
      if(s.playerDiscard.filter(function(id){return id==='S1-EVT-002';}).length!==1 || (s.committedCardCustody||[]).length) return {ok:false,reason:'Explicit Runtime recovery did not finalize exactly once',discard:s.playerDiscard,custody:s.committedCardCustody};
      s=buildInitialMatchState(); appState=s; s.playerHand=['S1-THF-028']; before=physicalCardCountForSide(s,'PLAYER','S1-THF-028'); s.playerHand.splice(0,1); beginCommittedCardCustody(s,'PLAYER','S1-THF-028',before,'qa-pending-source'); s.pending={type:'opponent_hand_choice',side:'PLAYER',source_card_id:'S1-THF-028',candidates:[]}; var pendingBefore=JSON.stringify(s); syncCounts(s);
      if(JSON.stringify(s)!==pendingBefore || s.playerDiscard.indexOf('S1-THF-028')>=0 || !(s.committedCardCustody||[]).length) return {ok:false,reason:'Passive sync changed active pending source'};
      s.pending=null; finalizeCommittedSourceCard(s,{commit_token:'qa-pending-source'},'PLAYER','S1-THF-028','discard',{reason:'explicit pending completion'}); syncCounts(s);
      if((s.committedCardCustody||[]).length || s.playerDiscard.filter(function(id){return id==='S1-THF-028';}).length!==1) return {ok:false,reason:'Explicit destination did not clear custody exactly once'};
      return {ok:true,version:'v4.0',starter60:'v1.2',starterDecks:5,copyLimit:2,transactionRollback:true,explicitZoneRecovery:true,pendingCustody:true,pureSync:true};
    }catch(e){ return {ok:false,error:String(e&&e.stack||e)}; }
    finally{ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeChoice(); closeResponseWindowUI(); closeInfo(); }
  }
  window.GL_V380_CARD_CUSTODY_STARTER_QA_SELF_TEST=simulateV380CardCustodyAndStarterAudit;


  function simulateHeroRacialEffectFixAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    try{
      var s=buildInitialMatchState(); appState=s; s.pvpHumanVsHuman=true; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.racial=1; s.playerHeroes.LEFT.card_id='S1-CLE-H004'; s.playerHeroes.LEFT.hp=0; s.playerHeroes.LEFT.statuses=[{name:'Poison',duration:2}]; s.playerHeroes.LEFT.attachments=['S1-ITM-013',null];
      applyDefeatCheck(s,'PLAYER','LEFT'); if(!s.pending||s.pending.type!=='racial_stoneblood') return {ok:false,reason:'Stoneblood choice missing',pending:s.pending}; resolveStonebloodChoice(true); if(s.playerHeroes.LEFT.hp!==10||s.racial!==0||!s.playerHeroes.LEFT.statuses.length) return {ok:false,reason:'Stoneblood state preservation failed',hero:s.playerHeroes.LEFT,racial:s.racial};
      s=buildInitialMatchState(); appState=s; s.pvpHumanVsHuman=true; s.racial=2; s.playerHeroes.LEFT.card_id='S1-WAR-H004'; s.playerHeroes.CENTER.card_id='S1-WAR-H001'; var ds=dragonScaleResponseOptionsFor(s,'PLAYER',{damage_type:'Physical',target_lane:'CENTER'}); if(ds.some(function(o){return o.source_lane==='LEFT';})) return {ok:false,reason:'Dragon Scale protected another Hero',options:ds};
      s=buildInitialMatchState(); appState=s; s.pvpHumanVsHuman=true; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.racial=2; s.playerHeroes.LEFT.card_id='S1-ARC-H001'; s.aiHeroes.CENTER.hp=60; s.aiHeroes.CENTER.maxHp=100; resolveActivatedRacialAbility(s,'PLAYER','LEFT','primal_strike','CENTER'); if(!s.responseWindow||s.responseWindow.kind!=='incoming_ability_damage'||s.responseWindow.damage_type!=='Physical') return {ok:false,reason:'Primal Strike response window missing',rw:s.responseWindow}; if((s.responseWindow.options||[]).some(function(o){var k=o.response_kind==='dragon_scale'?'block':responseKind(card(o.card_id),{damage_type:'Physical',attack_type:'Ability Damage'});return k!=='dodge'&&k!=='block';})) return {ok:false,reason:'Primal Strike exposed illegal response',options:s.responseWindow.options}; resolveResponseWindow(null); if(s.aiHeroes.CENTER.hp!==40) return {ok:false,reason:'Primal Strike Physical damage failed',hp:s.aiHeroes.CENTER.hp};
      s=buildInitialMatchState(); appState=s; s.pvpHumanVsHuman=true; s.turn='PLAYER'; s.phase='Deploy'; s.mana=5; s.playerHeroes.LEFT.card_id='S1-WAR-H006'; s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.CENTER.hp=50; s.playerHeroes.CENTER.maxHp=100; resolveActivatedHeroAbility(s,'PLAYER','LEFT','paladin_crusader_heal','CENTER'); if(!s.playerHeroes.LEFT.exhausted) return {ok:false,reason:'Crusader ability did not Exhaust source'}; s=buildInitialMatchState(); appState=s; s.pvpHumanVsHuman=true; s.playerHeroes.LEFT.card_id='S1-CLE-H003'; s.playerHeroes.CENTER.hp=50; s.playerHeroes.CENTER.maxHp=100; s.playerHeroes.CENTER.statuses=[{name:'Poison',duration:2},{name:'Burn',duration:1}]; healHeroIfLegal(s,s.playerHeroes.CENTER,20,card('S1-CLE-H003'),s.playerHeroes.LEFT,'Holy Rejuvenation'); activateNextSaintPurifyChoice(s); if(!s.pending||s.pending.type!=='saint_purify_choice'||(s.pending.status_choices||[]).length!==2) return {ok:false,reason:'Saint purify selection missing',pending:s.pending}; selectSaintPurifyChoice(1); commitSaintPurifyChoice(); if(hasStatus(s.playerHeroes.CENTER,'Burn')||!hasStatus(s.playerHeroes.CENTER,'Poison')) return {ok:false,reason:'Chosen negative status removal failed',statuses:s.playerHeroes.CENTER.statuses};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.racial=2; s.playerHeroes.LEFT.card_id='S1-THF-H001'; s.playerDiscard.push('S1-THF-001'); var sc=maybeOpenHalflingSecondChance(s,{card_id:'S1-THF-001',source_side:'PLAYER',source_lane:'LEFT',source_hero_card_id:'S1-THF-H001',target_side:'AI',target_lane:'LEFT'},{},{dodged:true}); if(!sc||!s.pending||s.pending.type!=='racial_second_chance') return {ok:false,reason:'Second Chance shared trigger missing',pending:s.pending};
      return {ok:true,stoneblood:true,secondChance:true,dragonScaleSelfOnly:true,primalStrikePhysicalResponse:true,saintPurifyChoice:true,paladinCrusaderExhaust:true,wordingChanged:false};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};} finally {appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }
  window.GL_HERO_RACIAL_EFFECT_FIX_QA_SELF_TEST=simulateHeroRacialEffectFixAudit;

  function simulateLegacyRacialTauntV383Audit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER; SUPPRESS_RENDER=true; matchStarted=true;
    try{
      var expected={
        'S1-ARC-L001':['golden_arrows','RETURN_DISCARD_TO_DECK',1],
        'S1-ARC-L002':['falconers_whistle','SEARCH_DECK_TO_HAND',1],
        'S1-CLE-L001':['sun_god_church','SEARCH_DECK_TO_HAND',1],
        'S1-CLE-L002':['wand_of_first_light','DRAW_CARDS',3],
        'S1-MAG-L001':['ancestral_tome','RETURN_DISCARD_TO_DECK',1],
        'S1-MAG-L002':['arcane_wand','RETURN_DISCARD_TO_HAND',1],
        'S1-THF-L001':['hidden_stash','RETURN_DISCARD_TO_DECK',1],
        'S1-THF-L002':['hidden_archives','GAIN_RACIAL_TOKEN',1],
        'S1-WAR-L001':['warriors_relic','DRAW_CARDS',1],
        'S1-WAR-L002':['statue_of_the_lightbringer','RETURN_DISCARD_TO_DECK',2]
      };
      for(var id in expected){var la=legacyAbilityData(card(id)),x=expected[id];if(!la||la.action_key!==x[0]||!la.effect||la.effect.type!==x[1]||Number(la.effect.count)!==x[2])return{ok:false,reason:'Latest Legacy resolver data mismatch',card_id:id,ability:la};}
      if(!(legacyAbilityData(card('S1-ARC-L002')).effect.filter||{}).any_card)return{ok:false,reason:'Falconer’s Whistle any-card deck search missing'};
      if((legacyAbilityData(card('S1-THF-L001')).effect.filter||{}).card_family!=='Item')return{ok:false,reason:'Hidden Stash Item filter missing'};
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.racial=2; s.playerHeroes.LEFT.card_id='S1-ARC-H001';
      if(racialTokenSpentThisTurn(s,'PLAYER'))return{ok:false,reason:'Fresh player should not be token-spend blocked'};markRacialTokenSpent(s,'PLAYER');
      if(!racialTokenSpentThisTurn(s,'PLAYER')||!racialAbilityUsedThisTurnByHero(s,s.playerHeroes.LEFT)||!racialAbilityUsedThisTurnByHero(s,s.playerHeroes.CENTER))return{ok:false,reason:'Player-wide Racial Token spend gate missing'};
      if(racialTokenSpentThisTurn(s,'AI'))return{ok:false,reason:'Opponent incorrectly shares player Racial Token spend gate'};
      s.playerHeroes.LEFT.statuses=[];s.playerHeroes.LEFT.attachments=['S1-WAR-005',null];s.activeAttachments=[{attachment_id:'QA:TAUNT',side:'PLAYER',lane:'LEFT',slot:0,card_id:'S1-WAR-005',tick_phase:'DRAW_PHASE_START',remaining_count:2,created_phase:'Deploy',created_turn:'PLAYER',created_round:0}];s.phase='Draw';removeStartOfTurnTargetPreventionForSide(s,'PLAYER');if(!activeAttachmentForHero(s,s.playerHeroes.LEFT,'S1-WAR-005')||s.activeAttachments[0].remaining_count!==1)return{ok:false,reason:'Taunt did not decrement to (1) at first owner turn start',attachments:s.activeAttachments};s.round=2;removeStartOfTurnTargetPreventionForSide(s,'PLAYER');if(activeAttachmentForHero(s,s.playerHeroes.LEFT,'S1-WAR-005')||s.playerHeroes.LEFT.attachments[0])return{ok:false,reason:'Taunt did not expire at second owner turn start'};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.racial=2; s.playerHeroes.LEFT.card_id='S1-THF-H001'; s.playerDiscard.push('S1-THF-001');markRacialTokenSpent(s,'PLAYER');
      var opened=maybeOpenHalflingSecondChance(s,{card_id:'S1-THF-001',source_side:'PLAYER',source_lane:'LEFT',source_hero_card_id:'S1-THF-H001',target_side:'AI',target_lane:'LEFT',second_chance_replay:true},{},{dodged:true});
      if(opened||s.pending)return{ok:false,reason:'Second Chance ignored player-wide once-per-turn token spend limit',pending:s.pending};
      return{ok:true,legacyCards:10,legacyTiming:'Deploy/Reform',searchShuffle:true,costSnapshot:true,racialLimit:'once_per_player_per_global_turn',secondChanceSharesPlayerLimit:true,tauntExpiry:'start_of_controller_second_turn_draw_phase'};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)};}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }
  window.GL_LEGACY_RACIAL_TAUNT_QA_SELF_TEST=simulateLegacyRacialTauntV383Audit;

  function simulateV384RuntimeRuleWaveAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.racial=2;s.playerHeroes.LEFT.card_id='S1-MAG-H001';s.playerHeroes.CENTER.card_id='S1-MAG-H001';
      var a=getActivatedRacialAbilities(s,'PLAYER','LEFT')[0];if(!a)return{ok:false,reason:'First Racial Trait unavailable'};resolveActivatedRacialAbility(s,'PLAYER','LEFT',a.abilityId);if(!racialTokenSpentThisTurn(s,'PLAYER')||getActivatedRacialAbilities(s,'PLAYER','CENTER').length)return{ok:false,reason:'Player-wide once-per-turn Racial Token spend not enforced'};
      if(racialTokenSpentThisTurn(s,'AI'))return{ok:false,reason:'Opponent incorrectly shares Racial Token spend marker'};

      s=buildInitialMatchState();appState=s;s.playerHeroes.LEFT.statuses=[];s.playerHeroes.LEFT.attachments=['S1-ITM-015',null];s.activeAttachments=[{attachment_id:'QA:CLOAK',side:'PLAYER',lane:'LEFT',slot:0,card_id:'S1-ITM-015',tick_phase:'DRAW_PHASE_START',remaining_count:2,created_phase:'Deploy',created_turn:'PLAYER',created_round:0}];s.phase='Draw';removeStartOfTurnTargetPreventionForSide(s,'PLAYER');if(!activeAttachmentForHero(s,s.playerHeroes.LEFT,'S1-ITM-015')||s.activeAttachments[0].remaining_count!==1||s.playerHeroes.LEFT.attachments[0]!=='S1-ITM-015')return{ok:false,reason:'Invisibility Cloak did not decrement to (1) at first owner Draw Phase',attachments:s.activeAttachments};s.round=2;removeStartOfTurnTargetPreventionForSide(s,'PLAYER');if(activeAttachmentForHero(s,s.playerHeroes.LEFT,'S1-ITM-015')||s.playerHeroes.LEFT.attachments[0])return{ok:false,reason:'Invisibility Cloak did not expire on owner second Draw Phase'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerHeroes.CENTER.card_id='S1-ARC-H003';s.playerHeroes.CENTER.exhausted=false;s.playerHand=['S1-ARC-017'];LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;});beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','CENTER');if(!s.pending||s.pending.type!=='exact_two_target_selection')return{ok:false,reason:'Dual Arrow individual target picker missing',pending:s.pending};performDualArrowPairChoice('LEFT');performDualArrowPairChoice('RIGHT');if(!performDualArrowPairChoice('confirm'))return{ok:false,reason:'Dual Arrow confirm failed'};if(s.aiHeroes.LEFT.hp>=100||s.aiHeroes.RIGHT.hp>=100||s.aiHeroes.CENTER.hp!==100)return{ok:false,reason:'Dual Arrow did not resolve only chosen individual targets',heroes:s.aiHeroes};

      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='AI';s.phase='Battle';s.round=2;s.aiMana=30;s.mana=30;s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.exhausted=false;s.aiHand=['S1-WAR-001','S1-EVT-007'];s.playerHand=['S1-EVT-009'];s.playerHeroes.LEFT.hp=100;commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});var ta=(s.responseWindow&&s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-EVT-009';});if(!ta)return{ok:false,reason:'Tactical Adaptation response unavailable'};resolveResponseWindow(ta);if(!s.responseWindow||!s.responseWindow.nested_response)return{ok:false,reason:'Nested Intercept response window missing'};var inter=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-EVT-007';});if(!inter)return{ok:false,reason:'Intercept unavailable against Tactical Adaptation'};resolveReactiveCancelWindow(inter);if(s.aiDiscard.indexOf('S1-EVT-007')<0||s.playerDiscard.indexOf('S1-EVT-009')<0||s.playerHeroes.LEFT.hp>=100)return{ok:false,reason:'Intercept did not cancel Tactical Adaptation and continue original attack',state:{aiDiscard:s.aiDiscard,playerDiscard:s.playerDiscard,hp:s.playerHeroes.LEFT.hp}};

      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='AI';s.phase='Battle';s.round=2;s.aiMana=30;s.mana=30;s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.exhausted=false;s.aiHand=['S1-WAR-001'];s.playerHeroes.LEFT.card_id='S1-ARC-H001';s.playerHeroes.LEFT.exhausted=false;s.playerHand=['S1-ARC-003','S1-WAR-002'];commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});var esc=(s.responseWindow&&s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-ARC-003';});if(!esc)return{ok:false,reason:'Escape Arrow response unavailable'};resolveResponseWindow(esc);if(!s.pending||s.pending.type!=='response_extra_discard_choice')return{ok:false,reason:'Escape Arrow did not require selected hand discard',pending:s.pending};var other=(s.pending.candidates||[]).findIndex(function(x){return x.card_id==='S1-WAR-002';});if(other<0)return{ok:false,reason:'Escape Arrow chosen-cost candidate missing'};selectResponseExtraDiscardChoice(other);commitResponseExtraDiscardChoice();if(s.playerHand.indexOf('S1-ARC-003')>=0||s.playerHand.indexOf('S1-WAR-002')>=0||s.playerDiscard.indexOf('S1-ARC-003')<0||s.playerDiscard.indexOf('S1-WAR-002')<0)return{ok:false,reason:'Escape Arrow or selected cost did not move to Discard',hand:s.playerHand,discard:s.playerDiscard};

      return{ok:true,legacySource:'latest_10',racialTokenLimit:'once_per_player_per_global_turn',invisibilityCleanup:'owner_second_draw_phase',nestedIntercept:true,dualArrow:'two_individual_targets',escapeArrow:'selected_other_card_and_self_to_discard',hiddenInfoShuffle:true,visualCounterOneDeferred:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)};}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V384_RUNTIME_RULE_WAVE_QA_SELF_TEST=simulateV384RuntimeRuleWaveAudit;

  function simulateV385FinalSourceAnimationAudit(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var stack=window.GL_SOURCE_STACK||{};
      if(stack.runtime_foundation!=='v1.82'||stack.runtime_core!=='v0.50'||stack.runtime_data!=='v0.12.7'||stack.effect_checkpoint!=='v0.11.6'||stack.effect_recipe!=='v0.11.7') return {ok:false,reason:'v3.85 source stack mismatch',stack:stack};
      if(CARDS.length!==198 || Object.keys((window.GL_ASSET_MANIFEST||{}).cards||{}).length!==198) return {ok:false,reason:'198-card data/asset count mismatch'};
      if(cardName(card('S1-THF-014'))!=='Viper’s Fang') return {ok:false,reason:'Viper’s Fang printed display name mismatch',name:cardName(card('S1-THF-014'))};
      if(Number(cardCost(card('S1-EVT-003')))!==3 || requiresAttachmentSlot(card('S1-EVT-003'))) return {ok:false,reason:'Scouting FINAL lock mismatch'};
      if(sourceShouldExhaust(card('S1-ARC-013'))) return {ok:false,reason:'Triple Shot still Exhausts'};
      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H002'; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; s.playerHand=['S1-ARC-013','S1-ARC-002'];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); if(!s.pending||s.pending.resolve_to!=='triple_shot_bind') return {ok:false,reason:'Triple Shot bind missing'}; s.pending.selected_index=0; commitCardSearchChoice();
      if(s.playerHeroes.CENTER.exhausted || s.playerHeroes.CENTER.attachments.indexOf('S1-ARC-013')<0 || !(s.activeAttachments||[]).some(function(a){return a.card_id==='S1-ARC-013'&&a.expires==='end_phase';})) return {ok:false,reason:'Triple Shot no-Exhaust/end-phase attachment mismatch',hero:s.playerHeroes.CENTER,active:s.activeAttachments};
      s.phase='End'; resolveEndPhaseCleanupForSide(s,'PLAYER'); if(s.playerHeroes.CENTER.attachments.indexOf('S1-ARC-013')>=0 || s.playerDiscard.indexOf('S1-ARC-013')<0) return {ok:false,reason:'Triple Shot End Phase expiry failed'};
      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30; s.playerHeroes.CENTER.card_id='S1-ARC-H003'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-ARC-017']; s.aiHeroes.LEFT.hp=100; s.aiHeroes.CENTER= sanitizeLegacySlot({card_id:'S1-WAR-L001',mode:'LEGACY',legacy_mode:true}); s.aiHeroes.RIGHT.hp=0;
      var pairs=dualArrowPairOptionsFor(s,'PLAYER','CENTER','AI'); if(!pairs.some(function(p){return p.indexOf('LEFT')>=0&&p.indexOf('CENTER')>=0;})) return {ok:false,reason:'Dual Arrow Hero+Legacy filler pair missing',pairs:pairs};
      var castingIncoming={card_id:'S1-ARC-021',target_lane:'LEFT',target_side:'PLAYER',source_side:'AI',source_lane:'CENTER',damage_type:'Physical',damage:80,affected_lanes:['LEFT']}; s.playerHand=['S1-THF-004']; s.mana=30; if(responseOptionsFor(s,'PLAYER',castingIncoming).some(function(o){return o.card_id==='S1-THF-004';})) return {ok:false,reason:'Smoke Screen can still negate Casting Attack'};
      s=buildInitialMatchState(); appState=s; var legacy=s.playerHeroes.LEFT; legacy.hp=0; legacy.exp_cards=['S1-WAR-001']; legacy.exp_total=100; applyDefeatCheck(s,'PLAYER','LEFT'); if(s.pending&&s.pending.type==='legacy_defeat_choice'){ s.pending.selected_index=0; commitLegacyDefeatChoice(); }
      if(isLegacyModeHero(s.playerHeroes.LEFT)){ restoreHeroFromLegacyMode(s,'PLAYER','LEFT',20,'Resurrection'); if((s.playerHeroes.LEFT.exp_cards||[]).length || Number(s.playerHeroes.LEFT.exp_total||0)!==0 || s.playerHeroes.LEFT.hp!==20) return {ok:false,reason:'Resurrection 20 HP/empty EXP lock failed',hero:s.playerHeroes.LEFT}; }
      return {ok:true,version:'v3.85',runtime:'v1.73/v0.43/v0.12.5/v0.11.5',cards:198,webp:198,scouting:true,tripleShot:true,holyRingData:true,resurrection:true,smokeScreen:true,dualArrowLegacyFiller:true,animationBoundary:true,exhaustRotation:true};
    }catch(e){ return {ok:false,error:String(e&&e.stack||e)}; }
    finally{ appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress; STARTUP_SHUFFLE_ENABLED=oldShuffle; closeChoice(); closeResponseWindowUI(); }
  }
  window.GL_V385_FINAL_SOURCE_ANIMATION_QA_SELF_TEST=simulateV385FinalSourceAnimationAudit;


  function simulateV386ResponseAnimationStarterPreviewAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      var keys=Object.keys(STARTER_DECK_OPTIONS||{}); if(keys.length!==5) return {ok:false,reason:'Starter60 v1.2 count mismatch',count:keys.length};
      for(var i=0;i<keys.length;i++){
        var d=STARTER_DECK_OPTIONS[keys[i]].deck, ids=deckEntriesToIds(d.main_deck), counts={}; if(ids.length!==60) return {ok:false,reason:'Starter deck count mismatch',preset:keys[i],count:ids.length};
        ids.forEach(function(id){counts[id]=Number(counts[id]||0)+1;});
        for(var id in counts){ if(counts[id]>2) return {ok:false,reason:'Normal copy limit exceeded',preset:keys[i],card_id:id,count:counts[id]}; if(card(id).is_ultimate&&counts[id]>1) return {ok:false,reason:'Ultimate copy limit exceeded',preset:keys[i],card_id:id,count:counts[id]}; }
        if(!/v1\.2/i.test(String(d.builder_version||'')+' '+String(d.format||''))) return {ok:false,reason:'Starter v1.2 metadata missing',preset:keys[i]};
      }
      var holy=card('S1-CLE-023'), legal=(holy.requirement&&holy.requirement.legal_active_classes)||[];
      if(legal.indexOf('Priest')<0||legal.indexOf('Saint')<0||legal.indexOf('Cleric')<0||legal.indexOf('Paladin')<0||legal.indexOf('Crusader')<0) return {ok:false,reason:'Holy Light legal class correction failed',legal:legal};
      var holyPreview=previewSections(holy); if(!/Cleric/.test(holyPreview)||!/Paladin/.test(holyPreview)) return {ok:false,reason:'Holy Light preview branches wrong',preview:holyPreview};
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;s.playerHeroes.CENTER.card_id='S1-THF-H001';s.playerHeroes.CENTER.exhausted=false;s.aiHeroes.CENTER.card_id='S1-ARC-H001';s.aiHeroes.CENTER.hp=100;s.aiHeroes.CENTER.maxHp=100;s.aiHeroes.CENTER.exhausted=false;s.aiHand=['S1-ARC-012'];
      var incoming={card_id:'S1-THF-010',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER',damage:40,damage_type:'Physical',attack_type:'Single Target',cannot_dodge:true,cannot_block:false};
      var opts=responseOptionsFor(s,'AI',incoming); if(!opts.some(function(o){return o.card_id==='S1-ARC-012';})) return {ok:false,reason:'Deflect Arrow not legal against Back Stab',options:opts};
      var action={card_id:'S1-THF-010',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'}; resolveAttackWithOwnerGating(s,action,{damage:40,persistent:false,attachmentSlot:-1});
      if(s.aiDiscard.indexOf('S1-ARC-012')<0) return {ok:false,reason:'AI did not commit Deflect Arrow automatically',aiDiscard:s.aiDiscard};
      if(!(s.opponentPlayedEvents||[]).some(function(e){return e.label==='DEF'&&e.card_id==='S1-ARC-012';})) return {ok:false,reason:'AI Defense missing from Opponent Played',events:s.opponentPlayedEvents};
      GL_HIDDEN_DRAW_TOKENS.PLAYER=[{token:'qa-draw',card_id:'S1-WAR-001'}]; var visible=presentationHandEntries('PLAYER',['S1-WAR-002','S1-WAR-001']); GL_HIDDEN_DRAW_TOKENS.PLAYER=[]; if(visible.length!==1||visible[0].id!=='S1-WAR-002') return {ok:false,reason:'Draw reveal presentation mask failed',visible:visible};
      return {ok:true,version:'v3.86',starter60:'v1.2',starterDecks:5,ultimateLimit:1,holyLightPreview:true,backStabDeflectArrow:true,opponentDefenseVisible:true,responseAwareAnimation:true,drawRevealOnArrival:true,tributeAnimation:true,rankUpDiscardAnimation:true,exhaustRotation:'-90deg'};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;GL_HIDDEN_DRAW_TOKENS={PLAYER:[],AI:[]};closeChoice();closeResponseWindowUI();}
  }
  window.GL_V386_RESPONSE_ANIMATION_STARTER_PREVIEW_QA_SELF_TEST=simulateV386ResponseAnimationStarterPreviewAudit;


  function simulateV387CombatResponseLineageLogAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;
      s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.playerHeroes.CENTER.hp=100;s.playerHeroes.CENTER.maxHp=100;s.playerHeroes.CENTER.exhausted=false;
      s.aiHeroes.CENTER.card_id='S1-WAR-H001';s.aiHeroes.CENTER.hp=100;s.aiHeroes.CENTER.maxHp=100;s.aiHeroes.CENTER.exhausted=false;s.aiHand=['S1-WAR-003'];
      resolveAttackWithOwnerGating(s,{card_id:'S1-WAR-009',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'},{damage:50,persistent:false,attachmentSlot:-1});
      if(s.aiHeroes.CENTER.hp!==80) return {ok:false,reason:'Rage Swing 50 minus Parry 30 did not deal 20',hp:s.aiHeroes.CENTER.hp};
      var def=(s.opponentPlayedEvents||[]).find(function(e){return e.label==='DEF'&&e.card_id==='S1-WAR-003';});
      var detail=def?opponentEventDetail(def):''; if(!/Incoming: Rage Swing.*50 Physical/i.test(detail)||!/Defense: Parry.*30/i.test(detail)||!/Final HP damage: 20/i.test(detail)) return {ok:false,reason:'Defense detail breakdown incomplete',detail:detail};
      if(s.aiHeroes.CENTER.exhausted) return {ok:false,reason:'Ready Defense source became Exhausted'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.aiHeroes.CENTER.card_id='S1-WAR-H001';s.aiHeroes.CENTER.exhausted=true;s.aiHeroes.CENTER.hp=100;s.aiHand=['S1-WAR-003'];
      var exhaustedOpts=responseOptionsFor(s,'AI',{card_id:'S1-WAR-009',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER',damage:50,damage_type:'Physical',attack_type:'Single Target',cannot_dodge:false,cannot_block:false});
      if(!exhaustedOpts.some(function(o){return o.card_id==='S1-WAR-003';})) return {ok:false,reason:'Exhausted Hero lost legal Defense response'};
      resolveAttackWithOwnerGating(s,{card_id:'S1-WAR-009',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'},{damage:50,persistent:false,attachmentSlot:-1});
      if(!s.aiHeroes.CENTER.exhausted) return {ok:false,reason:'Defense response incorrectly readied prior Exhaust'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;s.playerHeroes.CENTER.card_id='S1-ARC-H003';s.playerHeroes.CENTER.exhausted=false;s.aiHeroes.CENTER.card_id='S1-WAR-H001';s.aiHeroes.CENTER.hp=100;s.aiHeroes.LEFT.card_id='S1-ARC-H001';s.aiHeroes.LEFT.hp=80;s.aiHand=['S1-WAR-003'];
      resolveAttackWithOwnerGating(s,{card_id:'S1-ARC-016',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'},{damage:50,persistent:false,attachmentSlot:-1,defer_reposition_discard:true});
      if(s.aiHeroes.CENTER.hp!==80||!s.pending||s.pending.type!=='optional_target_swap') return {ok:false,reason:'Soul Blast partial-block HP damage did not open swap effect',hp:s.aiHeroes.CENTER.hp,pending:s.pending};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.playerHeroes.LEFT.card_id='S1-CLE-H002';s.playerHeroes.LEFT.exhausted=true;s.playerHand=['S1-CLE-011'];
      var hb=responseOptionsFor(s,'PLAYER',{card_id:'S1-WAR-009',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER',damage:50,damage_type:'Physical',attack_type:'Single Target',cannot_dodge:false,cannot_block:false});
      if(!hb.some(function(o){return o.card_id==='S1-CLE-011'&&o.source_lane==='LEFT';})) return {ok:false,reason:'Holy Barrier cannot protect allied Hero from another legal Priest source',options:hb};
      s.playerHand=['S1-CLE-025'];s.playerHeroes.RIGHT.card_id='S1-WAR-H006';s.playerHeroes.RIGHT.exhausted=true;s.playerHeroes.RIGHT.attachments=[null,null];
      var bd=responseOptionsFor(s,'PLAYER',{card_id:'S1-WAR-009',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER',damage:50,damage_type:'Physical',attack_type:'Single Target',cannot_dodge:false,cannot_block:false});
      if(!bd.some(function(o){return o.card_id==='S1-CLE-025'&&o.source_lane==='RIGHT';})) return {ok:false,reason:'Blessing of Divinity cannot protect allied Hero from owner Crusader source',options:bd};

      var paladinOnly={card_id:'QA-PALADIN-ATTACK',family:'Skill',classification:'Physical Attack',action_category:'Physical Attack',requirement:{legal_active_classes:['Paladin'],rank_gate:{min_active_rank:2,eligible_active_classes:['Paladin']}}};s.playerHeroes.RIGHT.exhausted=false;
      if(!sourceCompatibleForMode(paladinOnly,s.playerHeroes.RIGHT,{response:false})) return {ok:false,reason:'Crusader did not inherit Paladin Attack access'};

      s=buildInitialMatchState();appState=s;s.turn='AI';s.phase='Deploy';s.aiMana=30;s.mana=30;s.aiHeroes.CENTER.hp=50;s.aiHeroes.CENTER.maxHp=100;s.aiHand=['S1-ITM-001'];s.playerHand=['S1-ITM-017'];
      commitPlayedCard(s,{type:'PLAY_CARD',side:'AI',source_side:'AI',card_id:'S1-ITM-001',hand_index:0,target_side:'AI',target_lane:'CENTER'});
      if(!s.responseWindow||s.responseWindow.kind!=='incoming_card') return {ok:false,reason:'Reactive Item window missing'};
      var cancel=(s.responseWindow.options||[]).find(function(o){return o.card_id==='S1-ITM-017';});resolveReactiveCancelWindow(cancel);
      var canceled=(s.opponentPlayedEvents||[]).find(function(e){return e.card_id==='S1-ITM-001';});var canceledDetail=opponentEventDetail(canceled);
      if(!canceled||!/Flashpowder Bomb/i.test(canceledDetail)||!/canceled/i.test(canceledDetail)) return {ok:false,reason:'Canceled AI action missing from Opponent Played detail',detail:canceledDetail};

      return {ok:true,version:'v3.94',readableBattleLog:true,canceledAIActionVisible:true,rageSwingParryFinalDamage:20,soulBlastPartialBlockEffect:true,responseDoesNotExhaust:true,crusaderPaladinLineage:true,alliedHolyBarrier:true,alliedBlessingOfDivinity:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V388_COMBAT_RESPONSE_LINEAGE_LOG_QA_SELF_TEST=simulateV387CombatResponseLineageLogAudit;


  function simulateV388MixedRecordRuntimeAudit(){
    initCards(); var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER, oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true; matchStarted=true; STARTUP_SHUFFLE_ENABLED=false;
    try{
      var st=window.GL_SOURCE_STACK||{};
      if(st.runtime_foundation!=='v1.82'||st.runtime_core!=='v0.50'||st.runtime_data!=='v0.12.7'||st.effect_checkpoint!=='v0.11.6'||st.effect_recipe!=='v0.11.7'||st.shared_manual!=='v1.39'||st.local_ai!=='v6.1') return {ok:false,reason:'source stack mismatch',stack:st};
      if(CARDS.length!==198) return {ok:false,reason:'card count mismatch',count:CARDS.length};
      var hammer=card('S1-CLE-021'), bulwark=card('S1-CLE-022'), holy=card('S1-CLE-023'), judgement=card('S1-CLE-024'), punishment=card('S1-CLE-019');
      if(!isAttackCard(hammer)||isResponseOnly(hammer)||cardPhase(hammer)!=='Battle Phase') return {ok:false,reason:'Hammer mixed role remains',hammer:hammer};
      if(!isResponseOnly(bulwark)||!/Response Window/.test(cardPhase(bulwark))) return {ok:false,reason:'Sacred Bulwark response role missing'};
      var legal=((holy.requirement||{}).legal_active_classes||[]).join('|'); if(!/Cleric/.test(legal)||!/Priest/.test(legal)||!/Saint/.test(legal)||!/Paladin/.test(legal)||!/Crusader/.test(legal)) return {ok:false,reason:'Holy Light classes wrong',legal:legal};
      if(!isAttackCard(judgement)||isResponseOnly(judgement)||attackType(judgement)!=='Area Damage') return {ok:false,reason:'Divine Judgement mixed role remains'};
      if(!isUltimateCard(punishment)||((punishment.requirement||{}).ultimate||{}).owner!=='Thrain Sunborn') return {ok:false,reason:'Divine Punishment owner/Ultimate wrong'};
      if(Number(cardCost(card('S1-ARC-005')))!==3||requiresAttachmentSlot(card('S1-ARC-005'))) return {ok:false,reason:'Reload cost/slot wrong'};
      if(sourceShouldExhaust(card('S1-EVT-005'))) return {ok:false,reason:'Last Resort still Exhausts'};

      var s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30; s.aiMana=10;
      s.playerHeroes.CENTER.card_id='S1-THF-H005'; s.playerHeroes.CENTER.statuses=[]; s.playerHeroes.CENTER.attachments=['S1-MAG-022',null]; s.activeAttachments=[{attachment_id:'QA:AETHER:SEM',side:'PLAYER',lane:'CENTER',slot:0,card_id:'S1-MAG-022',tick_phase:'END_PHASE',remaining_count:2,created_phase:'Deploy',created_turn:'PLAYER',created_round:0}];
      s.aiHeroes.CENTER.hp=100;
      var zero=applyResolvedAttackDamage(s,'S1-THF-001','PLAYER','CENTER','AI','CENTER',20,'Magical',false,20,false,false); applySourcePostAttackEffects(s,'S1-THF-001',s.playerHeroes.CENTER,'PLAYER','CENTER','AI','CENTER',zero);
      if(s.aiMana!==10) return {ok:false,reason:'Aether Infusion triggered without HP damage'};
      s.aiHeroes.CENTER.hp=100; var hit=applyResolvedAttackDamage(s,'S1-THF-001','PLAYER','CENTER','AI','CENTER',20,'Magical',false,0,false,false); applySourcePostAttackEffects(s,'S1-THF-001',s.playerHeroes.CENTER,'PLAYER','CENTER','AI','CENTER',hit);
      if(s.aiMana!==9) return {ok:false,reason:'Aether Infusion failed after HP damage'};

      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-EVT-005'];
      s.playerHeroes.LEFT.hp=0; s.playerHeroes.LEFT.mode='LEGACY'; s.playerHeroes.RIGHT.hp=0; s.playerHeroes.RIGHT.mode='LEGACY'; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER');
      if(s.playerHeroes.CENTER.exhausted||!hasActiveAttachmentForHero(s,s.playerHeroes.CENTER,'S1-EVT-005')||s.playerHeroes.CENTER.statuses.some(function(st){return statusName(st)==='Last Resort Double';})) return {ok:false,reason:'Last Resort Attachment authority resolution wrong',hero:s.playerHeroes.CENTER,attachments:s.activeAttachments};

      s=buildInitialMatchState(); appState=s; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30; s.aiMana=30; s.playerHeroes.CENTER.card_id='S1-CLE-H001'; s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-CLE-021']; s.aiHand=[]; s.aiHeroes.CENTER.hp=100; s.aiHeroes.CENTER.statuses=[];
      beginPlayFromHand(0); chooseHeroFromBoard('PLAYER','CENTER'); chooseHeroFromBoard('AI','CENTER');
      if(s.aiHeroes.CENTER.hp!==60 || !hasStatus(s.aiHeroes.CENTER,'Stun')) return {ok:false,reason:'Hammer did not resolve as a normal attack',hp:s.aiHeroes.CENTER.hp,statuses:s.aiHeroes.CENTER.statuses,log:(s.log||[]).slice(-20)};
      if(s.playerDiscard.filter(function(id){return id==='S1-CLE-021';}).length!==1) return {ok:false,reason:'Hammer zone movement was not exactly once',discard:s.playerDiscard};
      s.playerHeroes.CENTER.exhausted=false; s.playerHand=['S1-CLE-021'];
      var hammerResponses=responseOptionsFor(s,'PLAYER',{card_id:'S1-WAR-009',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER',damage:50,damage_type:'Physical',attack_type:'Single Target',cannot_dodge:false,cannot_block:false});
      if(hammerResponses.some(function(o){return o.card_id==='S1-CLE-021';})) return {ok:false,reason:'Hammer appeared as response',options:hammerResponses};

      return {ok:true,version:'v3.94',cards:198,mixedRecordAudit:true,hammerAttack:true,sacredBulwarkResponse:true,holyLightClasses:true,divineCards:true,lastResort:true,aetherInfusionDealsDamage:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V388_MIXED_RECORD_RUNTIME_QA_SELF_TEST=simulateV388MixedRecordRuntimeAudit;

  function simulateV388SemanticTriggerAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      var cards=Object.fromEntries(CARDS.map(function(c){return [c.card_id,c];}));
      if(cards['S1-ARC-016'].post_resolution.trigger_mode!=='connected_hit'||cards['S1-ARC-016'].post_resolution.requires_actual_hp_damage!==false) return {ok:false,reason:'Soul Blast runtime semantic record stale'};
      var flashFx=(cards['S1-THF-029'].effect||[]).find(function(x){return x.kind==='on_hit_draw';});
      if(!flashFx||flashFx.requires_hp_damage!==false||flashFx.connected_hit_required!==true) return {ok:false,reason:'Flash Combo runtime semantic record stale',effect:flashFx};
      if(cards['S1-MAG-022'].effect[0].requires_hp_damage!==true) return {ok:false,reason:'Aether Infusion stale connected-hit record remains'};
      var defs=CARDS.filter(function(c){return c.classification==='Defense Skill';});
      for(var di=0;di<defs.length;di++){
        var dc=defs[di],dr=dc.requirement||{},dl=(dc.canonical_execution&&dc.canonical_execution.defense_source_policy)||dc.defense_source_policy||{};
        if(dr.usable_while_exhausted!==true||dl.creates_exhaust!==false||dl.blocked_by_existing_exhaust!==false) return {ok:false,reason:'Defense Exhaust record mismatch',card_id:dc.card_id};
      }
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;
      s.playerHeroes.CENTER.card_id='S1-ARC-H003';s.playerHeroes.CENTER.exhausted=false;s.aiHeroes.CENTER.card_id='S1-WAR-H001';s.aiHeroes.LEFT.card_id='S1-MAG-H001';s.aiHeroes.RIGHT.card_id='S1-CLE-H001';
      var soulAction={card_id:'S1-ARC-016',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'};
      if(!resolvePostAttackChoices(s,soulAction,{defer_reposition_discard:true},{damage:0,hp_damage:0,prevented:true,connected:false})||!s.pending||s.pending.type!=='optional_target_swap') return {ok:false,reason:'Soul Blast Prevent-to-0 did not count as printed hit',pending:s.pending};
      closeChoice();s.pending=null;
      if(resolvePostAttackChoices(s,soulAction,{defer_reposition_discard:true},{damage:0,hp_damage:0,dodged:true,connected:false})||s.pending) return {ok:false,reason:'Soul Blast Dodge incorrectly counted as hit',pending:s.pending};
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.playerHeroes.CENTER.card_id='S1-THF-H006';s.playerDeck=['S1-WAR-001'];s.playerHand=[];
      applySourcePostAttackEffects(s,'S1-THF-029',s.playerHeroes.CENTER,'PLAYER','CENTER','AI','CENTER',{damage:0,hp_damage:0,prevented:true,connected:false});
      if(s.playerHand.length!==1||s.playerHand[0]!=='S1-WAR-001') return {ok:false,reason:'Flash Combo Prevent-to-0 did not draw',hand:s.playerHand};
      s.playerDeck=['S1-WAR-002'];var before=s.playerHand.length;applySourcePostAttackEffects(s,'S1-THF-029',s.playerHeroes.CENTER,'PLAYER','CENTER','AI','CENTER',{damage:0,hp_damage:0,dodged:true,connected:false});
      if(s.playerHand.length!==before) return {ok:false,reason:'Flash Combo Dodge incorrectly drew'};
      return {ok:true,activeMixedRecords:0,soulBlastPrintedHit:true,flashComboPrintedHit:true,aetherInfusionActualDamage:true,defenseResponseNoExhaust:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V388_SEMANTIC_TRIGGER_QA_SELF_TEST=simulateV388SemanticTriggerAudit;


  function simulateV393AttachmentScoutingLegacyIdempotencyAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      var persistentIds=['S1-ARC-013','S1-ARC-021','S1-CLE-006','S1-CLE-007','S1-CLE-009','S1-CLE-018','S1-CLE-025','S1-EVT-005','S1-EVT-011','S1-ITM-010','S1-ITM-011','S1-ITM-013','S1-ITM-014','S1-ITM-015','S1-MAG-007','S1-MAG-011','S1-MAG-018','S1-MAG-020','S1-MAG-022','S1-THF-027','S1-WAR-005','S1-WAR-013','S1-WAR-020','S1-WAR-022'];
      for(var pi=0;pi<persistentIds.length;pi++){
        var pc=card(persistentIds[pi]), fakeHero={card_id:(pc.card_id==='S1-CLE-009'||pc.card_id==='S1-CLE-018')?'S1-CLE-H003':(pc.card_id==='S1-MAG-018'?'S1-MAG-H003':'S1-WAR-H001')};
        if(!attachmentPolicyForCard(pc,fakeHero)) return {ok:false,reason:'Persistent attachment missing policy',card_id:pc.card_id};
        if(!(pc.staging||{}).requires_attachment_slot) return {ok:false,reason:'Persistent attachment data does not require slot',card_id:pc.card_id,staging:pc.staging};
      }
      if(attachmentPolicyForCard(card('S1-ARC-018'),{card_id:'S1-ARC-H001'})) return {ok:false,reason:'Charged Shot incorrectly treated as persistent'};

      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;
      s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.playerHeroes.CENTER.exhausted=false;s.playerHeroes.CENTER.attachments=[null,null];s.playerHand=['S1-WAR-005'];
      beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','CENTER');
      if(s.playerHeroes.CENTER.attachments.indexOf('S1-WAR-005')<0||!((s.activeAttachments||[]).some(function(a){return a.card_id==='S1-WAR-005'&&a.side==='PLAYER'&&a.lane==='CENTER';}))) return {ok:false,reason:'Taunt did not enter source Attachment Slot',hero:s.playerHeroes.CENTER,active:s.activeAttachments,log:(s.log||[]).slice(-20)};
      if(s.playerDiscard.indexOf('S1-WAR-005')>=0) return {ok:false,reason:'Taunt exists in Attachment and Discard simultaneously'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;
      s.playerHeroes.CENTER.card_id='S1-CLE-H001';s.playerHeroes.CENTER.exhausted=false;s.playerHeroes.CENTER.attachments=[null,null];s.playerHeroes.LEFT.attachments=[null,null];s.playerHand=['S1-CLE-006'];
      beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','CENTER');chooseHeroFromBoard('PLAYER','LEFT');
      if(s.playerHeroes.LEFT.attachments.indexOf('S1-CLE-006')<0) return {ok:false,reason:'Blessing of Might did not enter buffed target Attachment Slot',target:s.playerHeroes.LEFT,source:s.playerHeroes.CENTER,log:(s.log||[]).slice(-20)};
      if(s.playerHeroes.CENTER.attachments.indexOf('S1-CLE-006')>=0||s.playerDiscard.indexOf('S1-CLE-006')>=0) return {ok:false,reason:'Blessing of Might was hosted/discarded in wrong zone',target:s.playerHeroes.LEFT,source:s.playerHeroes.CENTER,discard:s.playerDiscard};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.playerHand=['S1-EVT-003'];s.playerDiscard=[];s.aiDiscard=[];
      s.playerHeroes.LEFT.card_id='S1-WAR-H001';s.playerHeroes.LEFT.exhausted=false;s.aiHeroes.LEFT.exp_cards=['S1-WAR-001'];s.aiHeroes.LEFT.exp_total=100;
      beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','LEFT');chooseHeroFromBoard('AI','LEFT');selectScoutingExpChoice(0);commitScoutingExpChoice();
      var scoutingCount=s.playerDiscard.filter(function(id){return id==='S1-EVT-003';}).length;
      if(scoutingCount!==1) return {ok:false,reason:'Scouting source card did not move to Discard exactly once',count:scoutingCount,discard:s.playerDiscard,log:(s.log||[]).slice(-30)};
      if(s.aiDiscard.filter(function(id){return id==='S1-WAR-001';}).length!==1||s.aiHeroes.LEFT.exp_cards.length!==0) return {ok:false,reason:'Scouting target EXP movement incorrect',aiDiscard:s.aiDiscard,exp:s.aiHeroes.LEFT.exp_cards};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;
      s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.playerHeroes.CENTER.hp=0;s.playerHeroes.CENTER.assigned_legacy_card_id='S1-WAR-L001';
      if(s.playerLegacy.indexOf('S1-WAR-L001')<0)s.playerLegacy.push('S1-WAR-L001');
      var legacyBefore=s.playerLegacy.length;
      SUPPRESS_RENDER=false;
      applyDefeatCheck(s,'PLAYER','CENTER');
      if(!s.pending||s.pending.type!=='legacy_defeat_choice') return {ok:false,reason:'Legacy choice did not open',pending:s.pending};
      var firstChoiceId=s.pending.choice_id;
      applyDefeatCheck(s,'PLAYER','CENTER');
      if(!s.pending||s.pending.choice_id!==firstChoiceId) return {ok:false,reason:'Duplicate Legacy choice replaced the active choice',pending:s.pending,first:firstChoiceId};
      selectLegacyDefeatChoice(0);if(!commitLegacyDefeatChoice()) return {ok:false,reason:'Legacy choice could not commit'};
      if(commitLegacyDefeatChoice()) return {ok:false,reason:'Legacy choice committed twice'};
      if(!isLegacyModeHero(s.playerHeroes.CENTER)||s.playerLegacy.length!==legacyBefore-1) return {ok:false,reason:'Legacy transition was not atomic',hero:s.playerHeroes.CENTER,before:legacyBefore,after:s.playerLegacy.length};
      if((s.resolvedLegacyChoiceTokens||[]).filter(function(id){return id===firstChoiceId;}).length!==1) return {ok:false,reason:'Legacy choice resolution token duplicated',tokens:s.resolvedLegacyChoiceTokens};

      return {ok:true,version:'v3.94',persistentAttachmentCardsAudited:persistentIds.length,tauntSourceHost:true,blessingOfMightTargetHost:true,scoutingDiscardExactOnce:true,legacyChoiceAtomic:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V393_ATTACHMENT_SCOUTING_LEGACY_IDEMPOTENCY_QA_SELF_TEST=simulateV393AttachmentScoutingLegacyIdempotencyAudit;


  function simulateV394EventAttachmentInstanceAudit(){
    initCards();var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      var st=window.GL_SOURCE_STACK||{};if(st.runtime_foundation!=='v1.82'||st.runtime_data!=='v0.12.7'||st.effect_recipe!=='v0.11.7'||st.shared_manual!=='v1.39'||st.local_ai!=='v6.1')return{ok:false,reason:'source stack mismatch',stack:st};
      var events=CARDS.filter(function(c){return cardFamily(c)==='Event';});if(events.length!==12)return{ok:false,reason:'Event count mismatch',count:events.length};for(var ei=0;ei<events.length;ei++){if(!(events[ei].source_card_destination_policy||events[ei].lifecycle&&events[ei].lifecycle.source_card_destination_policy||{}).exact_once)return{ok:false,reason:'Event exact-once lock missing',card_id:events[ei].card_id};}
      var persistent=CARDS.filter(function(c){return c.staging&&c.staging.requires_attachment_slot;});if(persistent.length!==24)return{ok:false,reason:'persistent attachment count mismatch',count:persistent.length};for(var ai=0;ai<persistent.length;ai++){var pc=persistent[ai],fake={card_id:pc.card_id==='S1-CLE-009'?'S1-CLE-H002':pc.card_id==='S1-CLE-018'?'S1-CLE-H002':pc.card_id==='S1-MAG-018'?'S1-MAG-H002':pc.card_id==='S1-WAR-022'?'S1-WAR-H005':'S1-WAR-H001'};if(!attachmentPolicyForCard(pc,fake))return{ok:false,reason:'data-driven attachment missing',card_id:pc.card_id};}
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.playerDiscard=[];s.playerHand=['S1-ITM-013'];s.playerHeroes.LEFT.card_id='S1-WAR-H001';s.playerHeroes.LEFT.exhausted=true;s.playerHeroes.LEFT.attachments=[null,null];beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','LEFT');if(s.playerHeroes.LEFT.attachments.indexOf('S1-ITM-013')<0||s.playerDiscard.indexOf('S1-ITM-013')>=0||!s.playerHeroes.LEFT.exhausted)return{ok:false,reason:'Ring of Grace exhausted target-host flow failed',hero:s.playerHeroes.LEFT,discard:s.playerDiscard,log:s.log};
      s=buildInitialMatchState();appState=s;s.turn='AI';s.phase='Deploy';s.aiMana=30;s.aiDiscard=[];s.aiHand=['S1-EVT-004','S1-WAR-001'];s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.exp_cards=[];s.aiHeroes.LEFT.exp_total=0;s.aiHeroes.LEFT.exhausted=false;commitPlayedCard(s,{card_id:'S1-EVT-004',hand_index:0,side:'AI',source_side:'AI',source_lane:'LEFT'});if(s.aiHeroes.LEFT.exp_total!==100||s.aiHeroes.LEFT.exp_cards.indexOf('S1-WAR-001')<0||s.aiDiscard.filter(function(id){return id==='S1-EVT-004';}).length!==1)return{ok:false,reason:'AI Relentless Leveling failed or duplicated',hero:s.aiHeroes.LEFT,discard:s.aiDiscard,hand:s.aiHand,log:s.log};
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiManaRegen=5;s.playerHeroes.CENTER.card_id='S1-THF-H006';s.playerHeroes.CENTER.exhausted=false;s.aiHeroes.CENTER.hp=100;var a={card_id:'S1-MAG-025',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'};if(computedAttackDamageForAction(s,a,s.playerHeroes.CENTER,s.aiHeroes.CENTER)!==120)return{ok:false,reason:'Mana Void Arcane Duelist scaling-plus-flat formula failed',formula:a.damage_formula_log};
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=12;s.playerHand=['S1-EVT-007'];if(canOpenReactiveCancelWindow(s,card('S1-EVT-002'),'PLAYER','PLAYER'))return{ok:false,reason:'Intercept can answer same-side Event'};if(!canOpenReactiveCancelWindow(s,card('S1-EVT-002'),'PLAYER','AI'))return{ok:false,reason:'Intercept cannot answer opponent Event'};
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerDiscard=[];s.playerHeroes.LEFT.card_id='S1-WAR-H005';s.playerHeroes.LEFT.exhausted=false;s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.aiHeroes.CENTER.hp=100;s.playerHand=['S1-WAR-024'];commitPlayedCard(s,{card_id:'S1-WAR-024',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'CENTER'});if(s.responseWindow)resolveResponseWindow(null);if(!s.pending||s.pending.type!=='optional_swap')return{ok:false,reason:'Heroic Charge optional swap missing',pending:s.pending,log:s.log};performOptionalSwapDecision(false);if(s.pending||s.responseWindow||s.playerDiscard.filter(function(id){return id==='S1-WAR-024';}).length!==1)return{ok:false,reason:'Heroic Charge no-swap stuck or duplicated',pending:s.pending,rw:s.responseWindow,discard:s.playerDiscard,log:s.log};
      return{ok:true,version:'v5.59',eventsAudited:12,persistentAttachmentsAudited:24,ringOfGraceExhaustedTargetHost:true,venomConditionalBeforeDefense:true,heroRankResponseInferenceBlocked:true,relentlessAI:true,manaVoidScalingPlusFlat:true,interceptOpponentOnly:true,heroicChargeDeclineCloses:true,sourceCardExactOnce:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)};}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V394_EVENT_ATTACHMENT_INSTANCE_QA_SELF_TEST=simulateV394EventAttachmentInstanceAudit;



  function qaSetStartedState(state){ cancelAITurnDirector(); clearTransientUiState(); appState=state; matchStarted=true; SUPPRESS_RENDER=false; runtimeStateCheckpoint(appState,'QA setup'); render(); return appState; }
  function qaStateSnapshot(){ return clone(appState); }
  function qaSetupLegacyChoiceScenario(){
    initCards(); var s=buildInitialMatchState(); s.turn='PLAYER'; s.phase='Battle';s.round=2; s.playerHeroes.CENTER.hp=0; s.playerHeroes.CENTER.exhausted=false; s.pending=null; s.responseWindow=null; qaSetStartedState(s); applyDefeatCheck(s,'PLAYER','CENTER'); render(); return qaStateSnapshot();
  }
  function qaSetupTauntScenario(){
    initCards(); var s=buildInitialMatchState(); s.turn='PLAYER'; s.phase='Deploy'; s.mana=30; s.playerHand=['S1-WAR-005']; s.playerDiscard=[]; s.playerHeroes.CENTER.card_id='S1-WAR-H001'; s.playerHeroes.CENTER.hp=100; s.playerHeroes.CENTER.exhausted=false; s.playerHeroes.CENTER.attachments=[null,null]; qaSetStartedState(s); return qaStateSnapshot();
  }
  function qaSetupHolyRingScenario(){
    initCards(); var s=buildInitialMatchState(); s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30; s.aiMana=0; s.aiHand=[]; s.playerHand=['S1-CLE-009']; s.playerDiscard=[]; s.playerHeroes.RIGHT.card_id='S1-CLE-H002'; s.playerHeroes.RIGHT.hp=100; s.playerHeroes.RIGHT.exhausted=false; s.playerHeroes.RIGHT.attachments=[null,null]; s.aiHeroes.RIGHT.hp=200; qaSetStartedState(s); return qaStateSnapshot();
  }
  function qaSetupAITurnIndicatorScenario(){
    initCards(); var s=buildInitialMatchState(); s.turn='AI'; s.phase='Deploy'; s.aiPresentationStatus='AI enters Deploy Phase.'; qaSetStartedState(s); return qaStateSnapshot();
  }
  function simulateV400RuntimeAuthorityIntegrationAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED; SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      assertCanonicalCardMirror(window.GL_CARD_DEFINITIONS);
      var s=buildInitialMatchState(), before=JSON.stringify(s); syncCounts(s); if(JSON.stringify(s)!==before) return {ok:false,reason:'syncCounts mutated gameplay state'};
      appState=s; s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.playerHand=['S1-WAR-005'];s.playerDiscard=[];s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.playerHeroes.CENTER.attachments=[null,null];
      commitPlayedCard(s,{card_id:'S1-WAR-005',hand_index:0,source_side:'PLAYER',source_lane:'CENTER'});
      if(attachmentCardId(s.playerHeroes.CENTER.attachments[0])!=='S1-WAR-005'||s.playerDiscard.indexOf('S1-WAR-005')>=0) return {ok:false,reason:'Taunt was not atomically attached',hero:s.playerHeroes.CENTER,discard:s.playerDiscard};
      var renderBefore=JSON.stringify(s); render(); if(JSON.stringify(s)!==renderBefore) return {ok:false,reason:'render mutated runtime state'};
      s=buildInitialMatchState(); appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.playerHeroes.CENTER.hp=0;s.pending=null;s.responseWindow=null;startLegacyDefeatChoice(s,'PLAYER','CENTER',s.playerHeroes.CENTER,eligibleLegacyCardIdsForDefeatedHero(s,'PLAYER','CENTER',s.playerHeroes.CENTER));
      if(!s.pending||s.pending.type!=='legacy_defeat_choice')return{ok:false,reason:'Legacy choice did not open'};var cid=s.pending.choice_id;selectLegacyDefeatChoice(0);if(!commitLegacyDefeatChoice())return{ok:false,reason:'Legacy choice did not commit'};if(commitLegacyDefeatChoice())return{ok:false,reason:'Legacy choice committed twice'};if(s.pending||!isLegacyModeHero(s.playerHeroes.CENTER)||(s.runtimeChoiceLedger&&s.runtimeChoiceLedger[cid]!=='RESOLVED'))return{ok:false,reason:'Legacy choice did not close atomically',state:s};
      var persistent=CARDS.filter(function(c){return !!attachmentRuntimeLockForCard(c)&&attachmentRuntimeLockForCard(c).persistent===true;});if(persistent.length!==24)return{ok:false,reason:'persistent count mismatch',count:persistent.length};
      var events=CARDS.filter(function(c){return cardFamily(c)==='Event';});if(events.length!==12)return{ok:false,reason:'event count mismatch',count:events.length};
      return {ok:true,version:'v4.0',canonicalCards:198,effectRecipes:(window.GL_EFFECT_RECIPES.effect_recipes||[]).length,persistentAttachments:24,events:12,pureRender:true,pureSync:true,tauntAttachment:true,legacyAtomic:true,turnIndicator:true,animationsPreserved:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)};}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }


  function simulateV400AttachmentAuthorityHotfixAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      // Attachment is the only ongoing-effect authority. No duplicate Hero status/badge is created.
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=1;
      var defender=s.playerHeroes.CENTER;defender.card_id='S1-WAR-H001';defender.hp=100;defender.maxHp=100;defender.statuses=[];defender.attachments=[null,null];
      registerLocalAttachment(s,'PLAYER','CENTER',0,'S1-WAR-020',{tick_phase:'DRAW_PHASE_START',remaining_count:2,counter_mode:'countdown',role:'defense_reduction'},{commit_token:'QA:SHIELD_BASH'});
      if((defender.statuses||[]).length!==0) return {ok:false,reason:'Shield Bash created a duplicate Hero status badge',statuses:defender.statuses};
      var physical=applyResolvedAttackDamage(s,'S1-WAR-001','AI','CENTER','PLAYER','CENTER',30,'Physical',false,0);
      if(physical.active_reduction!==20||physical.damage!==10||Number(defender.hp)!==90) return {ok:false,reason:'Shield Bash Attachment did not directly reduce Physical damage',result:physical,hp:defender.hp};
      defender.hp=100;
      var magical=applyResolvedAttackDamage(s,'S1-MAG-001','AI','CENTER','PLAYER','CENTER',70,'Magical',false,40);
      if(magical.active_reduction!==0||magical.response_block!==40||magical.damage!==30||Number(defender.hp)!==70) return {ok:false,reason:'Shield Bash incorrectly reduced Magical damage or Dragon Scale math changed',result:magical,hp:defender.hp};

      // Counter is owned by activeAttachments and expires at its registered checkpoint.
      s.phase='Draw';s.turn='PLAYER';s.round=2;
      removeStartOfTurnTargetPreventionForSide(s,'PLAYER');
      var live=activeAttachmentForHero(s,defender,'S1-WAR-020');
      if(!live||Number(live.remaining_count)!==1) return {ok:false,reason:'Attachment counter did not decrement 2 to 1 at DRAW_PHASE_START',attachment:live,active:s.activeAttachments};
      if((defender.statuses||[]).length!==0) return {ok:false,reason:'Counter tick recreated a duplicate Hero status badge',statuses:defender.statuses};
      s.round=3;removeStartOfTurnTargetPreventionForSide(s,'PLAYER');
      if(activeAttachmentForHero(s,defender,'S1-WAR-020')||attachmentCardId(defender.attachments[0])) return {ok:false,reason:'Expired Attachment remained in runtime or visual slot',active:s.activeAttachments,slots:defender.attachments};
      if((s.playerDiscard||[]).filter(function(id){return id==='S1-WAR-020';}).length!==1) return {ok:false,reason:'Expired Attachment did not move to Discard exactly once',discard:s.playerDiscard};

      // Lethal damage resolved inside a response frame must open the mandatory Legacy choice.
      s=buildInitialMatchState();appState=s;s.turn='AI';s.phase='Battle';s.round=1;s.playerHand=[];s.pvpHumanVsHuman=false;
      var victim=s.playerHeroes.CENTER;victim.hp=30;victim.maxHp=Math.max(30,Number(victim.maxHp||100));victim.statuses=[];victim.attachments=[null,null];
      var candidates=eligibleLegacyCardIdsForDefeatedHero(s,'PLAYER','CENTER',victim);
      if(!candidates.length) return {ok:false,reason:'QA victim has no Legacy replacement candidate',victim:victim,legacy:s.playerLegacy};
      var action={card_id:'S1-MAG-001',source_side:'AI',source_lane:'CENTER',target_side:'PLAYER',target_lane:'CENTER',commit_token:'QA:LETHAL_RESPONSE'};
      s.aiControl={resume:'after_player_response',from_phase:'Battle',director:false};
      openAttackResponseWindow(s,action,{damage:70,persistent:false,attachmentSlot:-1});
      if(!s.pending||s.pending.type!=='response_window') return {ok:false,reason:'Lethal response QA did not open response frame',pending:s.pending,response:s.responseWindow};
      resolveResponseWindow(null);
      if(!s.pending||s.pending.type!=='legacy_defeat_choice') return {ok:false,reason:'Defeated Hero did not open Legacy choice after response resolution',pending:s.pending,queue:s.legacyDefeatQueue,hero:s.playerHeroes.CENTER};
      if((s.legacyDefeatQueue||[]).length) return {ok:false,reason:'Legacy choice remained stranded in queue',pending:s.pending,queue:s.legacyDefeatQueue};
      var queuedChoiceId=s.pending.choice_id;
      selectLegacyDefeatChoice(0);
      if(!commitLegacyDefeatChoice()) return {ok:false,reason:'Queued Legacy choice could be selected but not committed',pending:s.pending,ledger:s.runtimeChoiceLedger};
      if(!isLegacyModeHero(s.playerHeroes.CENTER)||!s.runtimeChoiceLedger||s.runtimeChoiceLedger[queuedChoiceId]!=='RESOLVED') return {ok:false,reason:'Queued Legacy choice did not enter Legacy Mode atomically',hero:s.playerHeroes.CENTER,ledger:s.runtimeChoiceLedger};
      if(s.turn!=='PLAYER'||s.phase!=='Draw'||s.aiControl||s.pending||s.responseWindow) return {ok:false,reason:'AI did not resume and finish its turn after PLAYER Legacy choice',turn:s.turn,phase:s.phase,aiControl:s.aiControl,pending:s.pending,responseWindow:s.responseWindow,log:s.log};

      // Card Preview shows class headings only when the printed effect branch changes.
      var holyRingPreview=previewSections(card('S1-CLE-009'));
      if(holyRingPreview.indexOf('<h3>Cleric</h3>')<0||holyRingPreview.indexOf('<h3>Priest</h3>')<0||holyRingPreview.indexOf('<h3>Saint</h3>')>=0) return {ok:false,reason:'Holy Ring preview did not collapse inherited Saint effect',preview:holyRingPreview};
      var burningArrowPreview=previewSections(card('S1-ARC-007'));
      if(burningArrowPreview.indexOf('<h3>Effect</h3>')<0||burningArrowPreview.indexOf('<h3>Archer</h3>')>=0||burningArrowPreview.indexOf('<h3>Marksman</h3>')>=0) return {ok:false,reason:'Identical class effects were not collapsed to one Effect section',preview:burningArrowPreview};
      var aimShotPreview=previewSections(card('S1-ARC-006'));
      if(aimShotPreview.indexOf('<h3>Archer</h3>')<0||aimShotPreview.indexOf('<h3>Marksman</h3>')<0) return {ok:false,reason:'Different class effects lost their class headings',preview:aimShotPreview};

      return {ok:true,version:'v4.0',versionBump:false,attachmentOnlyAuthority:true,noDuplicateStatusBadges:true,shieldBashPhysicalReduction:20,novaStrikeDragonScaleFinalDamage:30,counterLifecycle:'2 -> 1 -> discard',legacyChoiceAfterLethalResponse:true,legacyChoiceCommitAfterQueue:true,aiResumesAfterLegacyChoice:true,cardPreviewInheritedEffectsCollapsed:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V400_ATTACHMENT_AUTHORITY_HOTFIX_QA_SELF_TEST=simulateV400AttachmentAuthorityHotfixAudit;

  function simulateV400MultiTargetAnimationAnchorAudit(){
    initCards();
    try{
      if(!multiTargetAttackAnimationUsesSource('S1-CLE-024',{source_side:'PLAYER',source_lane:'CENTER'})) return {ok:false,reason:'Area Attack animation is not anchored to the source Hero'};
      if(!multiTargetAttackAnimationUsesSource('S1-ARC-017',{source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lanes:['LEFT','RIGHT']})) return {ok:false,reason:'Dual Arrow animation is not anchored to the source Hero'};
      if(!multiTargetAttackAnimationUsesSource('S1-ARC-001',{source_side:'PLAYER',source_lane:'CENTER',triple_shot_area:true,target_side:'AI',target_lane:'CENTER'})) return {ok:false,reason:'Triple Shot Area conversion animation is not anchored to the source Hero'};
      if(multiTargetAttackAnimationUsesSource('S1-WAR-009',{source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'})) return {ok:false,reason:'Normal single-target Attack lost target-directed animation'};
      return {ok:true,version:'v4.0',versionBump:false,areaAttackAnchor:'SOURCE_HERO',dualArrowAnchor:'SOURCE_HERO',tripleShotAreaAnchor:'SOURCE_HERO',singleTargetAnchor:'TARGET_HERO'};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
  }
  window.GL_V400_MULTI_TARGET_ANIMATION_QA_SELF_TEST=simulateV400MultiTargetAnimationAnchorAudit;

  window.GL_V400_RUNTIME_AUTHORITY_INTEGRATION_QA_SELF_TEST=simulateV400RuntimeAuthorityIntegrationAudit;
  window.GL_V400_QA={getState:qaStateSnapshot,setupLegacyChoice:qaSetupLegacyChoiceScenario,setupTaunt:qaSetupTauntScenario,setupHolyRing:qaSetupHolyRingScenario,setupAITurnIndicator:qaSetupAITurnIndicatorScenario,render:render,legalTargetLanes:function(side,id,lane){return legalTargetLanesFor(appState,side,id,lane);}};


  function simulateV1390MirrorLegacyPassiveDualArrowAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      var s=buildInitialMatchState();appState=s;
      var opponentHtml=field('AI',s.aiHeroes,s);
      var leftStart=opponentHtml.indexOf('<div class="zones left">'), rightStart=opponentHtml.indexOf('<div class="zones right">');
      var leftHtml=opponentHtml.slice(leftStart,rightStart), rightHtml=opponentHtml.slice(rightStart);
      if(!(leftHtml.indexOf('Main Deck')>=0 && leftHtml.indexOf('Discard Pile')>leftHtml.indexOf('Main Deck') && leftHtml.indexOf('Mana Pool')<0)) return {ok:false,reason:'Opponent screen-left rail is not Main/Discard',left:leftHtml.slice(0,900)};
      if(!(rightHtml.indexOf('Racial Token')>=0 && rightHtml.indexOf('Legacy Deck')>rightHtml.indexOf('Racial Token') && rightHtml.indexOf('Mana Pool')>rightHtml.indexOf('Legacy Deck'))) return {ok:false,reason:'Opponent screen-right rail is not Racial/Legacy/Mana',right:rightHtml.slice(0,1200)};
      var playerHtml=field('PLAYER',s.playerHeroes,s), pLeft=playerHtml.slice(playerHtml.indexOf('<div class="zones left">'),playerHtml.indexOf('<div class="zones right">')),pRight=playerHtml.slice(playerHtml.indexOf('<div class="zones right">'));
      if(!(pLeft.indexOf('Mana Pool')>=0&&pLeft.indexOf('Legacy Deck')>pLeft.indexOf('Mana Pool')&&pRight.indexOf('Discard Pile')>=0&&pRight.indexOf('Main Deck')>pRight.indexOf('Discard Pile'))) return {ok:false,reason:'Player resource rails changed unexpectedly'};

      s=buildInitialMatchState();appState=s;
      s.playerLegacy=['S1-ARC-L001','S1-ARC-L002','S1-WAR-L001'];
      s.playerLegacyPackageSlots=[
        {slot:'LEFT',progression:'S1-ARC-H004',legacy:'S1-ARC-L001',package_id:'LEFT-PACKAGE',lineage:'ARC-GRAND-ARBAL'},
        {slot:'CENTER',progression:'S1-WAR-H001',legacy:'S1-WAR-L001',package_id:'CENTER-PACKAGE',lineage:'WAR-CONQUEROR'},
        {slot:'RIGHT',progression:'S1-ARC-H001',legacy:'S1-ARC-L002',package_id:'RIGHT-PACKAGE',lineage:'ARC-GRAND-RANGER'}
      ];
      var humanRanger=s.playerHeroes.LEFT;humanRanger.card_id='S1-ARC-H006';humanRanger.assigned_legacy_card_id='S1-ARC-L001';humanRanger.legacy_package_id='LEFT-PACKAGE';humanRanger.legacy_lineage_id='ARC-GRAND-ARBAL';
      var beastRanger=s.playerHeroes.RIGHT;beastRanger.card_id='S1-ARC-H003';beastRanger.assigned_legacy_card_id='S1-ARC-L002';beastRanger.legacy_package_id='RIGHT-PACKAGE';beastRanger.legacy_lineage_id='ARC-GRAND-RANGER';
      var humanCandidates=eligibleLegacyCardIdsForDefeatedHero(s,'PLAYER','LEFT',humanRanger);
      var beastCandidates=eligibleLegacyCardIdsForDefeatedHero(s,'PLAYER','RIGHT',beastRanger);
      if(humanCandidates.length!==1||humanCandidates[0]!=='S1-ARC-L001'||beastCandidates.length!==1||beastCandidates[0]!=='S1-ARC-L002') return {ok:false,reason:'Duplicate Archer packages crossed Legacy binding',human:humanCandidates,beast:beastCandidates};
      s.playerLegacy.splice(s.playerLegacy.indexOf('S1-ARC-L001'),1);
      beastCandidates=eligibleLegacyCardIdsForDefeatedHero(s,'PLAYER','RIGHT',beastRanger);
      if(beastCandidates.length!==1||beastCandidates[0]!=='S1-ARC-L002') return {ok:false,reason:'Second Ranger lost its own Legacy after first Ranger package was consumed',beast:beastCandidates};

      s=buildInitialMatchState();appState=s;
      var grand=s.playerHeroes.CENTER;grand.card_id='S1-ARC-H003';
      var charged=card('S1-ARC-018');
      if(currentClassDamage(charged,grand,s.aiHeroes.CENTER)!==60) return {ok:false,reason:'Grand Ranger Charged Shot did not receive Dead Eye +10',damage:currentClassDamage(charged,grand,s.aiHeroes.CENTER)};
      var chargedAction={card_id:'S1-ARC-018',source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'RIGHT'};
      if(computedAttackDamageForAction(s,chargedAction,grand,s.aiHeroes.RIGHT)!==110) return {ok:false,reason:'Charged Shot outside coverage passive/scaling order mismatch',damage:computedAttackDamageForAction(s,chargedAction,grand,s.aiHeroes.RIGHT),log:chargedAction.damage_formula_log};
      if(currentClassDamage(card('S1-ARC-001'),grand,s.aiHeroes.CENTER)!==printedClassDamage(card('S1-ARC-001'),grand)) return {ok:false,reason:'Dead Eye incorrectly applies to Bow Bash'};
      if(currentClassDamage(card('S1-ARC-016'),grand,s.aiHeroes.CENTER)!==printedClassDamage(card('S1-ARC-016'),grand)+10) return {ok:false,reason:'Dead Eye missing from valid single-target Archer attack'};

      var arbalest={card_id:'S1-ARC-H006'}, element={card_id:'S1-MAG-H003'}, renegade={card_id:'S1-THF-H003'}, conqueror={card_id:'S1-WAR-H003'};
      if(passiveAttackDamageBonus(card('S1-WAR-001'),arbalest)!==10||passiveAttackDamageBonus(card('S1-ARC-019'),arbalest)!==0) return {ok:false,reason:'Grand Arbalest exact Physical profile gate failed'};
      if(passiveAttackDamageBonus(card('S1-MAG-001'),element)!==10||passiveAttackDamageBonus(card('S1-MAG-010'),element)!==0) return {ok:false,reason:'Elemental Lord exact Magical profile gate failed'};
      if(passiveAttackDamageBonus(card('S1-MAG-001'),renegade)!==10||passiveAttackDamageBonus(card('S1-ARC-019'),renegade)!==10||passiveAttackDamageBonus(card('S1-MAG-010'),renegade)!==0) return {ok:false,reason:'Renegade attack profile gate failed'};
      if(passiveAttackDamageBonus(card('S1-WAR-001'),conqueror)!==10||passiveAttackDamageBonus(card('S1-ARC-009'),conqueror)!==0) return {ok:false,reason:'Conqueror attack profile gate failed'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.pvpHumanVsHuman=true;
      s.playerHeroes.CENTER.card_id='S1-ARC-H003';s.playerHeroes.CENTER.exhausted=false;s.playerHand=['S1-ARC-017'];LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;s.aiHeroes[l].maxHp=100;s.aiHeroes[l].statuses=[];});
      beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','CENTER');
      if(!s.pending||s.pending.type!=='exact_two_target_selection') return {ok:false,reason:'Dual Arrow board selection did not open',pending:s.pending};
      chooseHeroFromBoard('AI','LEFT');
      if(!s.pending||Number((s.pending.selected_target_lanes||[]).length)!==1||s.responseWindow) return {ok:false,reason:'Dual Arrow first board click did not wait for second target',pending:s.pending,rw:s.responseWindow};
      chooseHeroFromBoard('AI','RIGHT');
      if(!s.responseWindow||s.responseWindow.target_lane!=='LEFT'||!s.responseWindow.multi_sequence) return {ok:false,reason:'Dual Arrow did not open first per-Hero response after second board click',rw:s.responseWindow,pending:s.pending};
      resolveResponseWindow(null);
      if(!s.responseWindow||s.responseWindow.target_lane!=='RIGHT') return {ok:false,reason:'Dual Arrow did not continue to second selected Hero',rw:s.responseWindow,heroes:s.aiHeroes};
      resolveResponseWindow(null);
      if(s.aiHeroes.LEFT.hp!==60||s.aiHeroes.RIGHT.hp!==60||s.aiHeroes.CENTER.hp!==100||s.pending||s.responseWindow) return {ok:false,reason:'Dual Arrow did not resolve both selected Heroes exactly once',heroes:s.aiHeroes,pending:s.pending,rw:s.responseWindow};

      return {ok:true,version:'v1.39.0',opponentRailsMirroredOnly:true,duplicateArcherLegacyPackages:true,passiveAttackProfiles:true,chargedShotGrandRanger:60,chargedShotOutsideCoverage:110,dualArrowBoardClicks:true,dualArrowTwoResponseWindows:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V1390_PVP_REGRESSION_QA_SELF_TEST=simulateV1390MirrorLegacyPassiveDualArrowAudit;


  function simulateV1400OpponentResourcesManaHeroAbilityAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShared=window.GL_PVP_SHARED_BOARD_ACTIVE;
    SUPPRESS_RENDER=true;matchStarted=true;window.GL_PVP_SHARED_BOARD_ACTIVE=false;
    try{
      var s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.aiMana=30;
      var aiHtml=field('AI',s.aiHeroes,s), playerHtml=field('PLAYER',s.playerHeroes,s);
      var aiLeftStart=aiHtml.indexOf('<div class="zones left">'), aiLeftEnd=aiHtml.indexOf('<article class="lane',aiLeftStart);
      var aiRightStart=aiHtml.indexOf('<div class="zones right">');
      var aiLeft=aiHtml.slice(aiLeftStart,aiLeftEnd), aiRight=aiHtml.slice(aiRightStart);
      if(!(aiLeft.indexOf('Main Deck')>=0 && aiLeft.indexOf('Discard Pile')>aiLeft.indexOf('Main Deck'))) return {ok:false,reason:'Opponent screen-left order is not Main Deck then Discard Pile',html:aiLeft};
      if(!(aiRight.indexOf('Racial Token')>=0 && aiRight.indexOf('Legacy Deck')>aiRight.indexOf('Racial Token') && aiRight.indexOf('Mana Pool')>aiRight.indexOf('Legacy Deck'))) return {ok:false,reason:'Opponent screen-right order is not Racial Token, Legacy Deck, Mana Pool',html:aiRight.slice(0,1600)};
      var playerLeft=playerHtml.slice(playerHtml.indexOf('<div class="zones left">'),playerHtml.indexOf('<div class="zones right">'));
      if(!(playerLeft.indexOf('Mana Pool')>=0 && playerLeft.indexOf('Legacy Deck')>playerLeft.indexOf('Mana Pool') && playerLeft.indexOf('Racial Token')>playerLeft.indexOf('Legacy Deck'))) return {ok:false,reason:'Player resource rail changed unexpectedly'};

      // Archer class abilities: all-position targeting, profile-gated attack bonuses, and draw replacement.
      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Battle';s.round=2;
      s.playerHeroes.LEFT.card_id='S1-ARC-H002';LANE_ORDER.forEach(function(l){s.aiHeroes[l].hp=100;s.aiHeroes[l].maxHp=100;});
      var lanes=legalTargetLanesFor(s,'PLAYER','S1-ARC-006','LEFT');if(lanes.length!==3) return {ok:false,reason:'Sharpshooter did not target all three opponent positions',lanes:lanes};
      s.playerHeroes.LEFT.card_id='S1-ARC-H003';if(passiveAttackDamageBonus(card('S1-ARC-018'),s.playerHeroes.LEFT)!==10||passiveAttackDamageBonus(card('S1-ARC-001'),s.playerHeroes.LEFT)!==0) return {ok:false,reason:'Dead Eye profile/exclusion failed'};
      s.playerHeroes.LEFT.card_id='S1-ARC-H005';if((drawReplacementAbilityForHero(s.playerHeroes.LEFT)||{}).abilityId!=='quick_reload') return {ok:false,reason:'Quick Reload class ability missing'};
      s.playerHeroes.LEFT.card_id='S1-ARC-H006';if((drawReplacementAbilityForHero(s.playerHeroes.LEFT)||{}).abilityId!=='rapid_chamber'||passiveAttackDamageBonus(card('S1-ARC-018'),s.playerHeroes.LEFT)!==10||passiveAttackDamageBonus(card('S1-ARC-019'),s.playerHeroes.LEFT)!==0) return {ok:false,reason:'Rapid Chamber draw/Physical Attack profile failed'};

      // Cleric line: healing modifiers and explicit Saint cleanse choice, even with one status.
      s=buildInitialMatchState();appState=s;s.playerHeroes.LEFT.card_id='S1-CLE-H002';s.playerHeroes.CENTER.hp=20;s.playerHeroes.CENTER.maxHp=100;
      var hr=healHeroIfLegal(s,s.playerHeroes.CENTER,10,card('S1-CLE-005'),s.playerHeroes.LEFT,'Holy Grace audit');if(hr.healed!==30) return {ok:false,reason:'Holy Grace +20 healing failed',result:hr};
      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.playerHeroes.LEFT.card_id='S1-CLE-H003';s.playerHeroes.CENTER.hp=20;s.playerHeroes.CENTER.maxHp=100;s.playerHeroes.CENTER.statuses=[{name:'Poison',duration:2}];
      hr=healHeroIfLegal(s,s.playerHeroes.CENTER,10,card('S1-CLE-005'),s.playerHeroes.LEFT,'Holy Rejuvenation audit');activateNextSaintPurifyChoice(s);
      if(hr.healed!==30||!s.pending||s.pending.type!=='saint_purify_choice'||(s.pending.status_choices||[]).length!==1) return {ok:false,reason:'Holy Rejuvenation heal or mandatory one-status choice failed',pending:s.pending,result:hr};

      // Mage line: attack-connected duration and exact Magical Attack bonus.
      s=buildInitialMatchState();appState=s;s.playerHeroes.LEFT.card_id='S1-MAG-H002';s.aiHeroes.LEFT.statuses=[];var burn=(card('S1-MAG-003').effect||[]).find(function(e){return e.kind==='inflict_status'&&e.status==='Burn';});var baseBurn=durationForEffect(burn,s.playerHeroes.LEFT);applyStatusEffectToHero(burn,s.aiHeroes.LEFT,s.playerHeroes.LEFT,'attack-connected');
      var burnStatus=(s.aiHeroes.LEFT.statuses||[]).find(function(st){return statusName(st)==='Burn';});if(!burnStatus||Number(burnStatus.duration)!==baseBurn+1) return {ok:false,reason:'Magic Mastery duration +1 failed',status:burnStatus,base:baseBurn};
      s.playerHeroes.LEFT.card_id='S1-MAG-H003';if(passiveAttackDamageBonus(card('S1-MAG-001'),s.playerHeroes.LEFT)!==10||passiveAttackDamageBonus(card('S1-MAG-010'),s.playerHeroes.LEFT)!==0) return {ok:false,reason:'Elemental Sovereignty exact Magical Attack profile failed'};

      // Thief line: Poison duration and normal-profile Renegade attack bonus.
      s=buildInitialMatchState();appState=s;s.playerHeroes.LEFT.card_id='S1-THF-H002';s.aiHeroes.LEFT.statuses=[];var poison=(card('S1-THF-013').effect||[]).find(function(e){return e.kind==='inflict_status'&&e.status==='Poison';});var basePoison=durationForEffect(poison,s.playerHeroes.LEFT);applyStatusEffectToHero(poison,s.aiHeroes.LEFT,s.playerHeroes.LEFT,'attack-connected');var poisonStatus=(s.aiHeroes.LEFT.statuses||[]).find(function(st){return statusName(st)==='Poison';});if(!poisonStatus||Number(poisonStatus.duration)!==basePoison+1) return {ok:false,reason:'Venom Mastery duration +1 failed',status:poisonStatus,base:basePoison};
      s.playerHeroes.LEFT.card_id='S1-THF-H003';if(passiveAttackDamageBonus(card('S1-THF-013'),s.playerHeroes.LEFT)!==10||passiveAttackDamageBonus(card('S1-THF-018'),s.playerHeroes.LEFT)!==0) return {ok:false,reason:'Nightshade Venom normal-profile bonus failed'};

      // Hybrid line: optional extra Mana choice must work for either PvP seat, and bonus must resolve from Hero identity.
      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='AI';s.phase='Battle';s.round=2;s.aiMana=10;s.aiHeroes.LEFT.card_id='S1-THF-H005';s.aiHeroes.LEFT.exhausted=false;s.aiHand=['S1-MAG-001'];s.playerHand=[];s.playerHeroes.LEFT.hp=100;s.playerHeroes.LEFT.maxHp=100;
      var surgeAction={card_id:'S1-MAG-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'};
      if(!openMagicalSurgeChoice(s,surgeAction)||!s.pending||s.pending.type!=='optional_magical_surge'||s.pending.decision_side!=='AI') return {ok:false,reason:'Mana Surge optional payment did not open for PvP seat 2',pending:s.pending};
      if(!commitMagicalSurgeChoice(true)) return {ok:false,reason:'Mana Surge confirm did not commit for PvP seat 2',pending:s.pending};
      if(s.responseWindow) resolveResponseWindow(null);
      if(s.playerHeroes.LEFT.hp!==70||s.aiMana!==7||s.aiHand.indexOf('S1-MAG-001')!==-1) return {ok:false,reason:'Mana Surge full flow should pay card cost 2 + extra 1 and deal 30',hp:s.playerHeroes.LEFT.hp,mana:s.aiMana,hand:s.aiHand,logs:s.log.slice(-8)};
      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.aiHeroes.LEFT.card_id='S1-THF-H006';s.aiMana=10;var appliedAction={source_side:'AI',source_lane:'LEFT',use_magical_surge:true};var mb=maybeApplyMagicalSurge(s,appliedAction,s.aiHeroes.LEFT,card('S1-MAG-001'));if(mb!==20||s.aiMana!==9) return {ok:false,reason:'Arcane Surge +20/pay 1 failed',bonus:mb,mana:s.aiMana};

      // Warrior line: Physical reduction, profile-gated Conqueror damage, and active heal once-per-turn lock.
      s=buildInitialMatchState();appState=s;s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.aiHeroes.LEFT.card_id='S1-WAR-H002';s.aiHeroes.LEFT.hp=100;s.aiHeroes.LEFT.maxHp=100;var dr=applyResolvedAttackDamage(s,'S1-WAR-001','PLAYER','CENTER','AI','LEFT',30,'Physical',false,0,false,false);if(dr.passive_block!==10||s.aiHeroes.LEFT.hp!==80) return {ok:false,reason:'Vanquisher Resolve Physical reduction failed',result:dr,hp:s.aiHeroes.LEFT.hp};
      s.aiHeroes.LEFT.card_id='S1-WAR-H003';if(passiveAttackDamageBonus(card('S1-WAR-001'),s.aiHeroes.LEFT)!==10||passiveAttackDamageBonus(card('S1-WAR-008'),s.aiHeroes.LEFT)!==0) return {ok:false,reason:'Arena Dominator attack profile failed'};
      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Deploy';s.mana=5;s.playerHeroes.LEFT.card_id='S1-WAR-H005';s.playerHeroes.LEFT.exhausted=false;s.playerHeroes.CENTER.hp=50;s.playerHeroes.CENTER.maxHp=100;
      if(!resolveActivatedHeroAbility(s,'PLAYER','LEFT','paladin_crusader_heal','CENTER')||s.playerHeroes.CENTER.hp!==60||s.mana!==4||s.playerHeroes.LEFT.class_ability_used_turn!==runtimeTurnStamp(s)) return {ok:false,reason:'Holy Resurgence resolve/usage marker failed',hero:s.playerHeroes.LEFT,target:s.playerHeroes.CENTER,mana:s.mana};
      s.playerHeroes.LEFT.exhausted=false;if(getActivatedHeroAbilities(s,'PLAYER','LEFT').length) return {ok:false,reason:'Holy Resurgence could be reused after artificial ready in same turn'};
      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Deploy';s.mana=5;s.playerHeroes.LEFT.card_id='S1-WAR-H006';s.playerHeroes.LEFT.exhausted=false;s.playerHeroes.CENTER.hp=50;s.playerHeroes.CENTER.maxHp=100;resolveActivatedHeroAbility(s,'PLAYER','LEFT','paladin_crusader_heal','CENTER');if(s.playerHeroes.CENTER.hp!==70) return {ok:false,reason:'Radiant Oblivion heal 20 failed',hp:s.playerHeroes.CENTER.hp};

      // Active racial traits plus the existing response/trigger racial regression suite.
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.racial=1;s.playerHeroes.LEFT.card_id='S1-ARC-H004';s.playerDeck=['S1-WAR-001','S1-WAR-002'];s.playerHand=[];resolveActivatedRacialAbility(s,'PLAYER','LEFT','human_ambition',null);if(s.playerHand.length!==2||s.racial!==0) return {ok:false,reason:'Human Ambition draw 2 failed',hand:s.playerHand,racial:s.racial};
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.racial=1;s.mana=2;s.playerHeroes.LEFT.card_id='S1-MAG-H001';resolveActivatedRacialAbility(s,'PLAYER','LEFT','ancestral_focus',null);if(s.mana!==4||s.racial!==0) return {ok:false,reason:'Ancestral Focus gain 2 Mana failed',mana:s.mana,racial:s.racial};
      var racialAudit=simulateHeroRacialEffectFixAudit();if(!racialAudit||!racialAudit.ok) return {ok:false,reason:'Response/trigger racial suite failed',audit:racialAudit};

      var abilityNames={};Object.keys(CARDS||{}).forEach(function(id){var c=CARDS[id],n=c&&c.class_ability&&c.class_ability.name;if(n)abilityNames[n]=true;});
      // Hybrid source data keeps Mana/Arcane Surge in locked ability text rather than class_ability.name.
      if(card('S1-THF-H005')&&/Mana Surge/i.test(String(card('S1-THF-H005').class_ability&&card('S1-THF-H005').class_ability.text||card('S1-THF-H005').card_text||''))) abilityNames['Mana Surge']=true;
      if(card('S1-THF-H006')&&/Arcane Surge/i.test(String(card('S1-THF-H006').class_ability&&card('S1-THF-H006').class_ability.text||card('S1-THF-H006').card_text||''))) abilityNames['Arcane Surge']=true;
      var required=['Sharpshooter','Dead Eye','Quick Reload','Rapid Chamber','Holy Grace','Holy Rejuvenation','Magic Mastery','Elemental Sovereignty','Venom Mastery','Nightshade Venom','Mana Surge','Arcane Surge','Vanquisher’s Resolve','Arena Dominator','Holy Resurgence','Radiant Oblivion'];
      var missing=required.filter(function(n){return !abilityNames[n];});if(missing.length) return {ok:false,reason:'Class ability source list incomplete',missing:missing,names:Object.keys(abilityNames)};
      return {ok:true,version:'v1.41.0',opponentOrder:true,opponentMainDeckLarger:true,manaSurgeBothSeats:true,classAbilityProfiles:required.length,passiveProfilesAudited:12,activatedProfilesAudited:2,drawReplacementProfilesAudited:2,racialTraitsAudited:6,saintSingleStatusChoice:true,activatedOncePerTurn:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;window.GL_PVP_SHARED_BOARD_ACTIVE=oldShared;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V1400_PVP_HERO_ABILITY_QA_SELF_TEST=simulateV1400OpponentResourcesManaHeroAbilityAudit;

  function simulateV1410DefeatReviveExpClearAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    SUPPRESS_RENDER=true;matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;
    try{
      // Confirmed defeat clears EXP immediately, moves each EXP card to Discard exactly once,
      // and stores an empty-EXP defeated snapshot for Legacy Mode.
      var s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;
      var h=s.playerHeroes.LEFT;h.card_id='S1-WAR-H001';h.hp=0;h.exp_cards=['S1-WAR-001','S1-WAR-002'];h.exp_total=200;
      h.assigned_legacy_card_id='S1-WAR-L001';h.legacy_lineage_id='WAR-CONQUEROR';h.legacy_package_id='LEFT-PACKAGE';
      s.playerLegacy=['S1-WAR-L001'];s.playerLegacyPackageSlots=[{slot:'LEFT',package_id:'LEFT-PACKAGE',legacy:'S1-WAR-L001',progression:'S1-WAR-H001'}];
      if(!applyDefeatCheck(s,'PLAYER','LEFT')||!s.pending||s.pending.type!=='legacy_defeat_choice') return {ok:false,reason:'Defeat did not open Legacy choice',pending:s.pending};
      if(Number(h.exp_total||0)!==0||(h.exp_cards||[]).length) return {ok:false,reason:'Hero EXP was not cleared immediately on defeat',hero:h};
      if(s.playerDiscard.filter(function(id){return id==='S1-WAR-001';}).length!==1||s.playerDiscard.filter(function(id){return id==='S1-WAR-002';}).length!==1) return {ok:false,reason:'Defeated Hero EXP cards did not move to Discard exactly once',discard:s.playerDiscard};
      selectLegacyDefeatChoice(0);if(!commitLegacyDefeatChoice()) return {ok:false,reason:'Legacy replacement could not commit'};
      var legacy=s.playerHeroes.LEFT,snap=legacy.defeated_hero_snapshot||{};
      if(!isLegacyModeHero(legacy)||Number(snap.exp_total||0)!==0||(snap.exp_cards||[]).length) return {ok:false,reason:'Legacy defeated snapshot retained EXP',legacy:legacy};

      // Every revive source restores the Hero with empty EXP, not only Resurrection.
      if(!restoreHeroFromLegacyMode(s,'PLAYER','LEFT',10,'Phoenix Feather')) return {ok:false,reason:'Legacy revive failed'};
      h=s.playerHeroes.LEFT;
      if(h.hp!==10||Number(h.exp_total||0)!==0||(h.exp_cards||[]).length) return {ok:false,reason:'Revived Hero did not return with empty EXP',hero:h};

      // Direct defeated-Hero revive fallback is also empty-EXP and exact-once for older/custom states.
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=20;
      h=s.playerHeroes.LEFT;h.hp=0;h.exp_cards=['S1-WAR-001'];h.exp_total=100;s.playerHand=['S1-ITM-008'];
      beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','LEFT');
      if(h.hp!==10||Number(h.exp_total||0)!==0||(h.exp_cards||[]).length||s.playerDiscard.filter(function(id){return id==='S1-WAR-001';}).length!==1) return {ok:false,reason:'Direct Phoenix Feather revive retained EXP or duplicated movement',hero:h,discard:s.playerDiscard};

      // The third/final Hero also clears EXP before the game-over state is committed.
      s=buildInitialMatchState();appState=s;s.playerHeroes.LEFT=transitionDefeatedHeroToLegacyMode(s,'PLAYER','LEFT','S1-WAR-L001',s.playerHeroes.LEFT)||s.playerHeroes.LEFT;s.playerHeroes.RIGHT=transitionDefeatedHeroToLegacyMode(s,'PLAYER','RIGHT','S1-THF-L001',s.playerHeroes.RIGHT)||s.playerHeroes.RIGHT;
      h=s.playerHeroes.CENTER;h.hp=0;h.exp_cards=['S1-WAR-002'];h.exp_total=100;applyDefeatCheck(s,'PLAYER','CENTER');
      if(Number(h.exp_total||0)!==0||(h.exp_cards||[]).length||s.playerDiscard.filter(function(id){return id==='S1-WAR-002';}).length!==1) return {ok:false,reason:'Final defeated Hero retained EXP',hero:h,discard:s.playerDiscard};

      return {ok:true,version:'v1.41.0',defeatExpZero:true,legacySnapshotExpZero:true,allRevivesExpZero:true,finalHeroExpZero:true,expDiscardExactlyOnce:true};
    }catch(e){return {ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V1410_EXP_CLEAR_QA_SELF_TEST=simulateV1410DefeatReviveExpClearAudit;



  function simulateV570PendingFixAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER,oldShuffle=STARTUP_SHUFFLE_ENABLED;
    try{
      matchStarted=true;STARTUP_SHUFFLE_ENABLED=false;SUPPRESS_RENDER=true;
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Reform';s.playerHand=['S1-WAR-001'];s.tributeUsedThisReform=false;
      s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.playerHeroes.CENTER.exp_cards=[];s.playerHeroes.CENTER.exp_total=0;
      if(!commitTributeToHero(s,{card_id:'S1-WAR-001',hand_index:0,target_lane:'CENTER'}))return{ok:false,reason:'Player Tribute could not commit'};
      var tributeEvt=(s.playerPlayedEvents||[]).find(function(e){return e.label==='TRIBUTE'&&e.card_id==='S1-WAR-001';});
      if(!tributeEvt||!/gained 100 EXP/i.test((tributeEvt.result_lines||[]).join(' ')))return{ok:false,reason:'Player Tribute missing from detailed Card Played',event:tributeEvt};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.round=2;s.phase='Battle';s.mana=10;s.aiHand=[];s.playerHand=['S1-WAR-001'];s.playerHeroes.CENTER.card_id='S1-WAR-H001';s.playerHeroes.CENTER.exhausted=false;s.aiHeroes.CENTER.hp=100;
      if(!commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'}))return{ok:false,reason:'Player attack could not commit'};
      var atkEvt=(s.playerPlayedEvents||[]).find(function(e){return e.label==='ATK'&&e.card_id==='S1-WAR-001';});
      if(!atkEvt||(atkEvt.response_lines||[]).length<1||!(atkEvt.result_lines||[]).some(function(x){return /Final HP damage/i.test(x);}))return{ok:false,reason:'Player Card Played attack detail is incomplete',event:atkEvt};

      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Battle';s.round=2;s.playerHeroes.CENTER.card_id='S1-CLE-H002';s.playerHeroes.CENTER.exhausted=false;s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.hp=10;s.aiHeroes.LEFT.maxHp=80;s.aiHeroes.LEFT.assigned_legacy_card_id='S1-WAR-L001';s.aiHeroes.LEFT.legacy_lineage_id='WAR-CONQUEROR';s.aiHeroes.LEFT.legacy_package_id=null;s.aiHeroes.CENTER.hp=100;s.aiLegacy=['S1-WAR-L001'];
      if(!openMultiTargetResponseWindow(s,{card_id:'S1-CLE-010',source_side:'PLAYER',source_lane:'CENTER',target_side:'AI'},{persistent:false,attachmentSlot:-1},['LEFT','CENTER'],0,0,false,0))return{ok:false,reason:'Holy Blast first Area target did not open'};
      if(!resolveResponseWindow(null))return{ok:false,reason:'Holy Blast first target could not resolve'};
      if(!s.pending||s.pending.type!=='legacy_defeat_choice'||!s.pending.after_legacy_multi_sequence)return{ok:false,reason:'Holy Blast did not preserve Area queue through lethal target / Legacy prompt',pending:s.pending};
      s.pending.selected_index=0;if(!commitLegacyDefeatChoice())return{ok:false,reason:'Holy Blast Legacy replacement could not commit'};
      if(!s.responseWindow||s.responseWindow.target_lane!=='CENTER')return{ok:false,reason:'Holy Blast did not resume at the second original target after Legacy replacement',response:s.responseWindow,pending:s.pending};

      s=buildInitialMatchState();appState=s;s.aiHand=['S1-EVT-007'];s.playerHand=['S1-EVT-009'];s.aiMana=10;s.mana=10;
      if(reactiveCancelOptionsFor(s,'Event','PLAYER','PLAYER').length!==0)return{ok:false,reason:'Player can still Intercept own Tactical Adaptation'};
      if(reactiveCancelOptionsFor(s,'Event','AI','PLAYER').length<1)return{ok:false,reason:'Opponent cannot Intercept Tactical Adaptation'};

      SUPPRESS_RENDER=false;
      s=buildInitialMatchState();appState=s;s.pending={type:'draw_replacement_choice',side:'PLAYER',decision_side:'PLAYER',source_lane:'LEFT',drawn_card_id:'S1-WAR-001',hand_index:0,abilityId:'quick_reload',abilityName:'Quick Reload'};s.playerHand=['S1-WAR-001'];renderDrawReplacementChoice();
      var quickHtml=$('choiceBody').innerHTML||'';if(quickHtml.indexOf('compact-decision-layout')<0||quickHtml.indexOf('choice-instruction')>=0||quickHtml.indexOf('data-draw-replacement-choice="redraw"')<0)return{ok:false,reason:'Quick Reload popup is not compact card-first',html:quickHtml};
      s.pending={type:'optional_magical_surge',side:'PLAYER',decision_side:'PLAYER',card_id:'S1-MAG-001',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'};s.playerHeroes.LEFT.card_id='S1-MAG-H005';renderMagicalSurgeChoice();
      var surgeHtml=$('choiceBody').innerHTML||'';if(surgeHtml.indexOf('compact-decision-layout')<0||surgeHtml.indexOf('choice-instruction')>=0||surgeHtml.indexOf('S1-MAG-H005')<0)return{ok:false,reason:'Mana Surge popup is not compact Hero-card-first',html:surgeHtml};
      if(V58_STATUS_ICON_ASSETS.Poison!=='assets/status-icons/Icon-Poison.png')return{ok:false,reason:'Official status icon assets not active',assets:V58_STATUS_ICON_ASSETS};
      return{ok:true,version:'v5.38',playerCardPlayedDetailed:true,tributeInCardPlayed:true,interceptOwnership:true,holyBlastLegacyResume:true,compactQuickReload:true,compactManaSurge:true,officialStatusIcons:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)};}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;STARTUP_SHUFFLE_ENABLED=oldShuffle;closeChoice();closeResponseWindowUI();}
  }
  function simulateV580UiRefinementAudit(){
    try{
      var oldApp=appState, oldMatch=matchStarted, oldSuppress=SUPPRESS_RENDER;
      var s=buildInitialMatchState(); appState=s; matchStarted=true; SUPPRESS_RENDER=true;
      s.cardsDrawnThisTurn={PLAYER:3,AI:1}; s.playerHeroes.LEFT.card_id='S1-ARC-H005';
      var info=v56HeroInfoLines(s.playerHeroes.LEFT,'PLAYER');
      if(info.indexOf('Draw This Turn: 3')===-1) return {ok:false,reason:'Arbalest draw counter missing from Hero Information',info:info};
      var tooltip=v58StatusTooltipText({name:'Poison',duration:2,sources:[{source_name:'attack-connected'},{source_card_id:'S1-THF-011',source_name:'Poison Mist'}]});
      if(tooltip.indexOf('Duration: 2')===-1||/attack-connected/i.test(tooltip)||tooltip.indexOf('Poison Mist')===-1) return {ok:false,reason:'Compact status tooltip source filtering failed',tooltip:tooltip};
      SUPPRESS_RENDER=false; s.pending={type:'optional_magical_surge',side:'PLAYER',decision_side:'PLAYER',card_id:'S1-MAG-001',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'}; s.playerHeroes.LEFT.card_id='S1-MAG-H005'; renderMagicalSurgeChoice();
      var html=$('choiceBody')&&$('choiceBody').innerHTML||'';
      if(html.indexOf('compact-decision-layout')===-1||html.indexOf('Use Mana Surge')===-1) return {ok:false,reason:'Mana Surge compact popup missing',html:html.slice(0,300)};
      appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
      return {ok:true,version:'v5.35',heroTooltipLayer:true,compactSurge:true,statusTooltipCompact:true,arbalestDrawCounter:true};
    }catch(err){return {ok:false,error:String(err&&err.stack||err)}}
  }
  function simulateV510DoubleCastingRegression(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    function freshDoubleCastingState(heroCardId,allowDifferentTarget){
      var s=buildInitialMatchState();
      appState=s; matchStarted=true; SUPPRESS_RENDER=true;
      s.pvpHumanVsHuman=true; s.turn='PLAYER'; s.phase='Battle';s.round=2; s.mana=30; s.aiMana=30;
      s.playerHeroes.LEFT=heroState(heroCardId||'S1-MAG-H002','PLAYER','LEFT');
      s.playerHeroes.LEFT.hp=100; s.playerHeroes.LEFT.maxHp=(heroCardId==='S1-MAG-H003'?120:100); s.playerHeroes.LEFT.exhausted=false; s.playerHeroes.LEFT.attachments=['S1-MAG-018',null];
      s.activeAttachments=[{attachment_id:'QA:DOUBLE_CASTING',side:'PLAYER',lane:'LEFT',slot:0,card_id:'S1-MAG-018',tick_phase:'END_PHASE',remaining_count:2,counter_mode:'countdown',role:'ongoing',created_phase:'Deploy',created_turn:'PLAYER',created_round:0,active_from_round:0,allow_different_target:!!allowDifferentTarget}];
      s.playerHand=['S1-MAG-003']; s.playerDiscard=[];
      s.aiHand=[];
      return s;
    }
    try{
      var s=freshDoubleCastingState(), before=s.aiHeroes.LEFT.hp;
      commitPlayedCard(s,{card_id:'S1-MAG-003',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'});
      if(!s.responseWindow) return {ok:false,reason:'Double Casting activation 1/2 did not open Response Window'};
      resolveResponseWindow(null);
      var afterFirst=s.aiHeroes.LEFT.hp;
      if(!(afterFirst<before)) return {ok:false,reason:'Double Casting activation 1/2 did not damage the first target',before:before,after:afterFirst};
      if(!s.pending||s.pending.type!=='double_casting_target_selection') return {ok:false,reason:'Double Casting did not require target selection for activation 2/2',pending:s.pending};
      if((s.pending.legal_targets||[]).indexOf('LEFT')<0) return {ok:false,reason:'The same target was not legal for Elementalist activation 2/2',legal:s.pending.legal_targets};
      if((s.pending.legal_targets||[]).indexOf('CENTER')<0) return {ok:false,reason:'Elementalist Double Casting did not allow a different legal target',legal:s.pending.legal_targets};
      var centerBeforeRank2=s.aiHeroes.CENTER.hp;
      commitDoubleCastingSecondTarget(s,s.pending,'CENTER');
      if(!s.responseWindow) return {ok:false,reason:'Double Casting activation 2/2 did not open its own Response Window'};
      resolveResponseWindow(null);
      if(s.aiHeroes.LEFT.hp!==afterFirst||!(s.aiHeroes.CENTER.hp<centerBeforeRank2)) return {ok:false,reason:'Elementalist Double Casting different target did not resolve independently',leftAfterFirst:afterFirst,leftAfterSecond:s.aiHeroes.LEFT.hp,centerBefore:centerBeforeRank2,centerAfter:s.aiHeroes.CENTER.hp};
      if(hasDoubleCasting(s.playerHeroes.LEFT)) return {ok:false,reason:'Double Casting Attachment was not consumed after activation 2/2'};

      s=freshDoubleCastingState('S1-MAG-H003',true); var leftBefore=s.aiHeroes.LEFT.hp, centerBefore=s.aiHeroes.CENTER.hp;
      commitPlayedCard(s,{card_id:'S1-MAG-003',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'});
      resolveResponseWindow(null);
      var leftAfterFirst=s.aiHeroes.LEFT.hp;
      if(!(leftAfterFirst<leftBefore)) return {ok:false,reason:'Different-target activation 1/2 did not damage target 1',before:leftBefore,after:leftAfterFirst};
      if(!s.pending||s.pending.type!=='double_casting_target_selection') return {ok:false,reason:'Different-target Double Casting selection missing'};
      commitDoubleCastingSecondTarget(s,s.pending,'CENTER');
      if(!s.responseWindow) return {ok:false,reason:'Different-target activation 2/2 Response Window missing'};
      resolveResponseWindow(null);
      if(s.aiHeroes.LEFT.hp!==leftAfterFirst||!(s.aiHeroes.CENTER.hp<centerBefore)) return {ok:false,reason:'Double Casting different targets did not resolve independently',leftBefore:leftBefore,leftAfterFirst:leftAfterFirst,leftAfterSecond:s.aiHeroes.LEFT.hp,centerBefore:centerBefore,centerAfter:s.aiHeroes.CENTER.hp};

      s=freshDoubleCastingState('S1-MAG-H003',true);
      s.aiHeroes.LEFT=heroState('S1-WAR-H001','AI','LEFT'); s.aiHeroes.LEFT.hp=20; s.aiHeroes.LEFT.maxHp=80; s.aiHeroes.LEFT.assigned_legacy_card_id='S1-WAR-L001';
      s.aiLegacy=['S1-WAR-L001']; s.aiHeroes.CENTER.hp=100; centerBefore=s.aiHeroes.CENTER.hp;
      commitPlayedCard(s,{card_id:'S1-MAG-003',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'});
      resolveResponseWindow(null);
      if(!s.pending||s.pending.type!=='legacy_defeat_choice'||!s.pending.after_legacy_double_casting) return {ok:false,reason:'Lethal activation 1/2 did not pause for Legacy before activation 2/2',pending:s.pending};
      selectLegacyDefeatChoice(0); commitLegacyDefeatChoice();
      if(!s.pending||s.pending.type!=='double_casting_target_selection') return {ok:false,reason:'Double Casting did not resume target 2/2 selection after Legacy replacement',pending:s.pending};
      commitDoubleCastingSecondTarget(s,s.pending,'CENTER');
      if(!s.responseWindow) return {ok:false,reason:'Post-Legacy activation 2/2 Response Window missing'};
      resolveResponseWindow(null);
      if(!isLegacyModeHero(s.aiHeroes.LEFT)||!(s.aiHeroes.CENTER.hp<centerBefore)) return {ok:false,reason:'Post-Legacy activation 2/2 did not complete correctly',left:s.aiHeroes.LEFT,centerBefore:centerBefore,centerAfter:s.aiHeroes.CENTER.hp};
      return {ok:true,version:'v5.35',elementalistSameTarget:true,elementalLordDifferentTarget:true,separateResponseWindows:true,legacyPauseResume:true,doubleCastingConsumed:true};
    }catch(err){return {ok:false,error:String(err&&err.stack||err)}}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;closeChoice();closeResponseWindowUI();}
  }

  function simulateV511UiFixAudit(){
    try{
      var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
      var s=buildInitialMatchState(); appState=s; matchStarted=true; SUPPRESS_RENDER=true;
      s.aiHeroes.LEFT=transitionDefeatedHeroToLegacyMode(s,'AI','LEFT','S1-WAR-L001',s.aiHeroes.LEFT)||s.aiHeroes.LEFT;
      s.aiHeroes.RIGHT=transitionDefeatedHeroToLegacyMode(s,'AI','RIGHT','S1-THF-L001',s.aiHeroes.RIGHT)||s.aiHeroes.RIGHT;
      s.aiHeroes.CENTER.hp=0; applyDefeatCheck(s,'AI','CENTER');
      if(!s.gameOver||s.winner!=='PLAYER') return {ok:false,reason:'terminal defeat did not end match'};
      var panel=heroPanel('AI','CENTER',s.aiHeroes.CENTER,s);
      if(panel.indexOf('Back-of-Card-Main-Deck.webp')<0||panel.indexOf('HP 0 /')<0||panel.indexOf('--hp-value:0%')<0) return {ok:false,reason:'terminal defeated Hero presentation is not card-back / zero HP',panel:panel};
      var hand=handCard('S1-WAR-001',0,s);
      if(hand.indexOf('hand-art')<0||hand.indexOf('data-preview="S1-WAR-001"')<0) return {ok:false,reason:'Hand card click preview contract missing'};
      appState=oldApp; matchStarted=oldMatch; SUPPRESS_RENDER=oldSuppress;
      var legacyHero={card_id:'S1-WAR-L001',side:'PLAYER',lane:'CENTER',mode:'LEGACY',legacy_mode:true,active_legacy_card_id:'S1-WAR-L001',defeated_hero_snapshot:{card_id:'S1-WAR-H001',hp:0,maxHp:100,exp_cards:[],attachments:[null,null]}};
      var legacyPanel=heroPanel('PLAYER','CENTER',legacyHero,s);
      if(legacyPanel.indexOf('legacy-name-bar')<0||legacyPanel.indexOf("Warrior&#39;s Relic")<0||legacyPanel.indexOf('class="hp-bar"')>=0) return {ok:false,reason:'Legacy active-card name did not replace HP row',panel:legacyPanel};
      s=buildInitialMatchState();appState=s;s.playerHeroes.LEFT.card_id='S1-MAG-H002';s.aiHeroes.LEFT.statuses=[];
      var burn={kind:'inflict_status',status:'Burn',duration_turns:1,raw_duration:'1'};
      applyStatusEffectToHero(burn,s.aiHeroes.LEFT,s.playerHeroes.LEFT,'attack-connected',{card_id:'S1-MAG-003',name:'Fireball',family:'Skill'});
      var burnStatus=(s.aiHeroes.LEFT.statuses||[]).find(function(st){return statusName(st)==='Burn';}),tooltip=v58StatusTooltipText(burnStatus);
      if(tooltip.indexOf('Source: Fireball')<0||tooltip.indexOf('Elf Elementalist')>=0) return {ok:false,reason:'Status tooltip source is not the Skill Card',tooltip:tooltip,status:burnStatus};
      return {ok:true,version:'v5.35',terminalCardBack:true,zeroHp:true,handHoverOnly:true,fieldBackground:true,inwardSlotGlow:true,phaseBlinkRemoved:true,legacyNameBar:true,statusSkillSource:true,cardMotionSound:true};
    }catch(err){return {ok:false,error:String(err&&err.stack||err)}}
  }

  function simulateV511EscapeArrowNoStuckAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      var s=buildInitialMatchState();appState=s;matchStarted=true;SUPPRESS_RENDER=true;
      s.pvpHumanVsHuman=false;s.turn='AI';s.phase='Battle';s.round=2;s.aiMana=30;s.mana=30;
      s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.exhausted=false;s.aiHand=['S1-WAR-001'];
      s.playerHeroes.LEFT.card_id='S1-ARC-H001';s.playerHeroes.LEFT.hp=80;s.playerHeroes.LEFT.maxHp=80;s.playerHeroes.LEFT.exhausted=false;
      s.playerHand=['S1-ARC-003','S1-WAR-002'];s.playerDiscard=[];
      commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});
      var idx=(s.responseWindow&&s.responseWindow.options||[]).findIndex(function(o){return o.card_id==='S1-ARC-003';});
      if(idx<0)return{ok:false,reason:'Escape Arrow response unavailable'};
      s.responseWindow.selected=idx;confirmSelectedResponse();
      if(!s.pending||s.pending.type!=='response_extra_discard_choice')return{ok:false,reason:'Escape Arrow discard choice did not open',pending:s.pending};
      var pick=(s.pending.candidates||[]).findIndex(function(x){return x.card_id==='S1-WAR-002';});
      if(pick<0)return{ok:false,reason:'Escape Arrow discard candidate missing'};
      selectResponseExtraDiscardChoice(pick);
      if(!commitResponseExtraDiscardChoice())return{ok:false,reason:'Escape Arrow discard confirmation did not resolve'};
      if(s.pending||s.responseWindow)return{ok:false,reason:'Escape Arrow left a pending/response frame stuck',pending:s.pending,responseWindow:s.responseWindow};
      if(s.playerHeroes.LEFT.hp!==80)return{ok:false,reason:'Escape Arrow did not Dodge the attack',hp:s.playerHeroes.LEFT.hp};
      if(s.playerHand.length!==0||s.playerDiscard.indexOf('S1-ARC-003')<0||s.playerDiscard.indexOf('S1-WAR-002')<0)return{ok:false,reason:'Escape Arrow cards did not reach Discard',hand:s.playerHand,discard:s.playerDiscard};
      return{ok:true,version:'v5.35',escapeArrowNoStuck:true,extraDiscardSelected:true,responseCompleted:true,parentResponseHidden:true};
    }catch(err){return{ok:false,error:String(err&&err.stack||err)}}
    finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;closeChoice();closeResponseWindowUI();}
  }
  function simulateV512RuntimeRegressionAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      var lightning=card('S1-MAG-014');
      if(!lightning||lightning.casting_delay||lightning.casting||lightning.effect_strings_by_class) return {ok:false,reason:'Lightning Strike retained stale Casting metadata',card:lightning};
      if(!lightning.targeting||!lightning.targeting.requires_selected_target||!lightning.targeting.ignore_area_of_attack) return {ok:false,reason:'Lightning Strike Range target lock missing',targeting:lightning&&lightning.targeting};
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerHeroes.CENTER.card_id='S1-MAG-H003';s.playerHeroes.CENTER.exhausted=false;s.playerHeroes.CENTER.statuses=[];s.playerHand=['S1-MAG-014'];s.aiHand=[];s.aiRacial=0;s.aiHeroes.RIGHT.card_id='S1-ARC-H001';s.aiHeroes.RIGHT.hp=100;s.aiHeroes.RIGHT.maxHp=100;s.aiHeroes.RIGHT.statuses=[];s.aiHeroes.RIGHT.attachments=[null,null];
      beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','CENTER');
      if(!s.pending||s.pending.type!=='target_selection'||(s.pending.legal_targets||[]).indexOf('RIGHT')<0) return {ok:false,reason:'Lightning Strike did not open single-target Range selection',pending:s.pending};
      chooseHeroFromBoard('AI','RIGHT');
      if(s.pending&&s.pending.type==='optional_magical_surge') commitMagicalSurgeChoice(false);
      if(s.responseWindow)resolveResponseWindow(null);
      if(s.aiHeroes.RIGHT.hp!==30||s.playerDiscard.indexOf('S1-MAG-014')<0||(s.playerHeroes.CENTER.attachments||[]).some(Boolean)) return {ok:false,reason:'Lightning Strike did not resolve 70 single-target damage directly',hp:s.aiHeroes.RIGHT.hp,discard:s.playerDiscard,attachments:s.playerHeroes.CENTER.attachments,pending:s.pending};
      s=buildInitialMatchState();appState=s;s.turn='AI';s.phase='Battle';s.round=2;s.aiMana=30;s.mana=30;s.aiControl={resume:'after_player_response',from_phase:'Battle',director:false};s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.exhausted=false;s.aiHand=['S1-WAR-001'];s.playerHeroes.LEFT.card_id='S1-ARC-H001';s.playerHeroes.LEFT.hp=80;s.playerHeroes.LEFT.maxHp=80;s.playerHand=['S1-ARC-003','S1-WAR-002'];
      commitPlayedCard(s,{card_id:'S1-WAR-001',hand_index:0,source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'});var eidx=(s.responseWindow.options||[]).findIndex(function(o){return o.card_id==='S1-ARC-003';});s.responseWindow.selected=eidx;confirmSelectedResponse();var pick=(s.pending.candidates||[]).findIndex(function(x){return x.card_id==='S1-WAR-002';});selectResponseExtraDiscardChoice(pick);if(!commitResponseExtraDiscardChoice()||s.pending||s.responseWindow||s.playerHeroes.LEFT.hp!==80) return {ok:false,reason:'Escape Arrow full continuation remained stuck',pending:s.pending,rw:s.responseWindow,hp:s.playerHeroes.LEFT.hp};
      s=buildInitialMatchState();appState=s;var rank2=s.playerHeroes.LEFT;rank2.card_id='S1-WAR-H002';rank2.maxHp=100;rank2.hp=0;rank2.exp_cards=['S1-WAR-001'];rank2.exp_total=400;applyDefeatCheck(s,'PLAYER','LEFT');if(s.pending&&s.pending.type==='legacy_defeat_choice'){s.pending.selected_index=0;commitLegacyDefeatChoice();}if(!isLegacyModeHero(s.playerHeroes.LEFT)) return {ok:false,reason:'Rank II defeat did not enter Legacy'};restoreHeroFromLegacyMode(s,'PLAYER','LEFT',20,'Resurrection');rank2=s.playerHeroes.LEFT;if(rank2.exp_total!==300||(rank2.exp_cards||[]).length) return {ok:false,reason:'Rank II revive did not preserve inherent 300 EXP floor',hero:rank2};
      var legacy={card_id:'S1-WAR-L001',side:'PLAYER',lane:'CENTER',mode:'LEGACY',legacy_mode:true,active_legacy_card_id:'S1-WAR-L001',defeated_hero_snapshot:{card_id:'S1-WAR-H002',hp:0,maxHp:100,exp_cards:[],exp_total:300,attachments:[null,null]}};var panel=heroPanel('PLAYER','CENTER',legacy,s);if(panel.indexOf('legacy-hero-info')<0||panel.indexOf('S1-WAR-H002')<0)return{ok:false,reason:'Legacy defeated-Hero info control missing',panel:panel};
      return {ok:true,version:'v5.35',lightningStrike:true,escapeArrowContinuation:true,rankExpFloor:true,legacyHeroInfo:true};
    }catch(err){return{ok:false,error:String(err&&err.stack||err)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V512_RUNTIME_REGRESSION_QA_SELF_TEST=simulateV512RuntimeRegressionAudit;
  window.GL_V510_DOUBLE_CASTING_QA_SELF_TEST=simulateV510DoubleCastingRegression;
  window.GL_V511_ESCAPE_ARROW_QA_SELF_TEST=simulateV511EscapeArrowNoStuckAudit;
  window.GL_V511_UI_FIX_QA_SELF_TEST=simulateV511UiFixAudit;
  window.GL_V580_UI_REFINEMENT_QA_SELF_TEST=simulateV580UiRefinementAudit;
  window.GL_V570_PENDING_FIX_QA_SELF_TEST=simulateV570PendingFixAudit;


  function simulateV513AttachmentIntegrationAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      var specs=[
        {id:'S1-ITM-010',hero:'S1-MAG-H001'},
        {id:'S1-ITM-011',hero:'S1-ARC-H001'},
        {id:'S1-ITM-013',hero:'S1-WAR-H001'},
        {id:'S1-ITM-014',hero:'S1-CLE-H001'},
        {id:'S1-ITM-015',hero:'S1-MAG-H001'}
      ];
      for(var i=0;i<specs.length;i++){
        var sp=specs[i],st=buildInitialMatchState();appState=st;st.turn='PLAYER';st.phase='Deploy';st.mana=30;
        st.playerHeroes.LEFT.card_id=sp.hero;st.playerHeroes.LEFT.hp=100;st.playerHeroes.LEFT.maxHp=100;st.playerHeroes.LEFT.exhausted=false;st.playerHeroes.LEFT.attachments=[null,null];st.playerHeroes.LEFT.statuses=[];st.playerHand=[sp.id];st.playerDiscard=[];
        beginPlayFromHand(0);if(!st.pending||st.pending.type!=='target_selection')return{ok:false,reason:sp.id+' did not use one allied Hero host picker',pending:st.pending};
        chooseHeroFromBoard('PLAYER','LEFT');
        if(st.playerHeroes.LEFT.attachments.indexOf(sp.id)<0)return{ok:false,reason:sp.id+' missing from Hero Attachment Slot',hero:st.playerHeroes.LEFT};
        if(!(st.activeAttachments||[]).some(function(a){return a.card_id===sp.id&&a.side==='PLAYER'&&a.lane==='LEFT';}))return{ok:false,reason:sp.id+' missing authoritative activeAttachment record',active:st.activeAttachments};
        if(st.playerDiscard.indexOf(sp.id)>=0)return{ok:false,reason:sp.id+' incorrectly moved to Discard on play',discard:st.playerDiscard};
        if(hasStatus(st.playerHeroes.LEFT,cardName(card(sp.id))))return{ok:false,reason:sp.id+' created a fake Hero status marker',statuses:st.playerHeroes.LEFT.statuses};
      }
      return{ok:true,version:'v5.35',attachmentItems:5,sourceHostNormalized:true,exactZone:true,noFakeStatus:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;closeChoice();closeResponseWindowUI();}
  }
  window.GL_V513_ATTACHMENT_INTEGRATION_QA_SELF_TEST=simulateV513AttachmentIntegrationAudit;

  function simulateV203ResolverAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      // Colossal Dreadbolt must use its per-card-drawn multiplier and cap.
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiHand=[];
      s.playerHeroes.LEFT.card_id='S1-ARC-H006';s.playerHeroes.LEFT.exhausted=false;s.playerHand=['S1-ARC-024'];s.cardsDrawnThisTurn={PLAYER:4,AI:0};s.aiHeroes.LEFT.hp=150;s.aiHeroes.LEFT.maxHp=150;
      var dreadAction={card_id:'S1-ARC-024',source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'};
      var dreadDamage=computedAttackDamageForAction(s,dreadAction,s.playerHeroes.LEFT,s.aiHeroes.LEFT);
      if(dreadDamage!==90)return{ok:false,reason:'Grand Arbalest Dreadbolt draw-4 final damage is not 90 (80 scaled +10 Rapid Chamber)',damage:dreadDamage,log:dreadAction.damage_formula_log};
      s.cardsDrawnThisTurn.PLAYER=7;dreadAction={card_id:'S1-ARC-024',source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'};
      if(computedAttackDamageForAction(s,dreadAction,s.playerHeroes.LEFT,s.aiHeroes.LEFT)!==130)return{ok:false,reason:'Grand Arbalest Dreadbolt capped final damage is not 130 (120 cap +10 Rapid Chamber)'};
      s.playerHeroes.LEFT.card_id='S1-ARC-H005';s.cardsDrawnThisTurn.PLAYER=4;dreadAction={card_id:'S1-ARC-024',source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'};
      if(computedAttackDamageForAction(s,dreadAction,s.playerHeroes.LEFT,s.aiHeroes.LEFT)!==40)return{ok:false,reason:'Arbalest Dreadbolt draw-4 formula is not 40'};

      // Crusader uses the Warrior row of Rage Swing; Gladiator-only Bleed must not apply.
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiHand=[];s.aiRacial=0;
      s.playerHeroes.CENTER.card_id='S1-WAR-H006';s.playerHeroes.CENTER.exhausted=false;s.playerHand=['S1-WAR-009'];s.aiHeroes.RIGHT.hp=100;s.aiHeroes.RIGHT.maxHp=100;s.aiHeroes.RIGHT.statuses=[];
      commitPlayedCard(s,{card_id:'S1-WAR-009',hand_index:0,source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'RIGHT'});
      if(s.responseWindow)resolveResponseWindow(null);
      if(s.aiHeroes.RIGHT.hp!==50)return{ok:false,reason:'Crusader Rage Swing did not deal Warrior-row 50 damage',hp:s.aiHeroes.RIGHT.hp};
      if(hasStatus(s.aiHeroes.RIGHT,'Bleed'))return{ok:false,reason:'Crusader Rage Swing incorrectly applied Gladiator Bleed',statuses:s.aiHeroes.RIGHT.statuses};

      // Normal Items may attach/use a Hero without exhausting it.
      var itemSpecs=['S1-ITM-010','S1-ITM-011','S1-ITM-013','S1-ITM-014','S1-ITM-015'];
      for(var i=0;i<itemSpecs.length;i++){
        s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.playerHeroes.LEFT.card_id=(itemSpecs[i]==='S1-ITM-010'?'S1-MAG-H001':itemSpecs[i]==='S1-ITM-011'?'S1-ARC-H001':itemSpecs[i]==='S1-ITM-014'?'S1-CLE-H001':itemSpecs[i]==='S1-ITM-015'?'S1-MAG-H001':'S1-WAR-H001');s.playerHeroes.LEFT.exhausted=false;s.playerHeroes.LEFT.attachments=[null,null];s.playerHand=[itemSpecs[i]];
        beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','LEFT');
        if(s.playerHeroes.LEFT.exhausted)return{ok:false,reason:itemSpecs[i]+' incorrectly Exhausted its Item user/host'};
        if(s.playerHeroes.LEFT.attachments.indexOf(itemSpecs[i])<0)return{ok:false,reason:itemSpecs[i]+' did not enter Attachment Slot'};
      }
      return{ok:true,version:'v5.35',dreadboltScaling:true,lineageFallback:true,itemNoAutoExhaust:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;closeChoice();closeResponseWindowUI();}
  }
  window.GL_PVP_V203_RESOLVER_QA_SELF_TEST=simulateV203ResolverAudit;

  function simulateV220GameplayUiAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      var r1=window.GL_RULES_RUNTIME.resolveTributeExp(200,200,1,true),r2=window.GL_RULES_RUNTIME.resolveTributeExp(600,200,2,true);
      if(!r1.legal||r1.total!==300||r1.overflow!==100||!r2.legal||r2.total!==700||r2.overflow!==100) return{ok:false,reason:'Ultimate Tribute overflow policy mismatch',r1:r1,r2:r2};

      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;
      s.playerHeroes.LEFT.card_id='S1-WAR-H001';s.playerHeroes.LEFT.hp=30;s.playerHeroes.LEFT.maxHp=80;s.playerHeroes.LEFT.exhausted=false;s.playerHeroes.LEFT.statuses=[];s.playerHeroes.LEFT.attachments=['S1-ITM-010','S1-ITM-011'];s.playerHand=['S1-ITM-002'];s.playerDiscard=[];
      beginPlayFromHand(0);
      if(!s.pending||s.pending.type!=='target_selection'||(s.pending.legal_targets||[]).indexOf('LEFT')<0)return{ok:false,reason:'Greater Health Potion did not allow full-slot target',pending:s.pending};
      chooseHeroFromBoard('PLAYER','LEFT');
      if(s.playerHeroes.LEFT.hp<=30||!s.playerHeroes.LEFT.exhausted||s.playerDiscard.indexOf('S1-ITM-002')<0)return{ok:false,reason:'Greater Health Potion instant resolve mismatch',hero:s.playerHeroes.LEFT,discard:s.playerDiscard};

      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;
      s.playerHeroes.LEFT.card_id='S1-THF-H003';s.playerHeroes.LEFT.exhausted=false;
      s.aiHeroes.LEFT.card_id='S1-MAG-H001';s.aiHeroes.LEFT.hp=80;s.aiHeroes.LEFT.maxHp=80;s.aiHeroes.LEFT.statuses=[{name:'Poison',duration:2}];s.aiHeroes.LEFT.attachments=[null,null];s.aiHand=['S1-MAG-004'];
      s.aiHeroes.CENTER.statuses=[{name:'Poison',duration:1}];s.aiHeroes.CENTER.hp=80;s.aiHeroes.CENTER.maxHp=80;
      resolveVenomDetonation(s,{card_id:'S1-THF-018',source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',commit_token:'TEST:VENOM'},{persistent:false});
      if(!s.responseWindow||s.responseWindow.target_lane!=='LEFT'||s.responseWindow.damage_type!=='Magical'||!s.responseWindow.cannot_dodge||s.responseWindow.cannot_block)return{ok:false,reason:'Venom first per-Hero response mismatch',rw:s.responseWindow};
      if(hasStatus(s.aiHeroes.LEFT,'Poison'))return{ok:false,reason:'Venom did not remove Poison before response'};
      var manaShield=(s.responseWindow.options||[]).findIndex(function(o){return o.card_id==='S1-MAG-004';});
      if(manaShield<0)return{ok:false,reason:'Magical Block missing from Venom response',options:s.responseWindow.options};
      s.responseWindow.selected=manaShield;confirmSelectedResponse();
      if(s.aiHeroes.LEFT.hp!==80)return{ok:false,reason:'Mana Shield did not Block Venom to 0',hp:s.aiHeroes.LEFT.hp};
      if(!s.responseWindow||s.responseWindow.target_lane!=='CENTER')return{ok:false,reason:'Venom did not continue to second Hero',rw:s.responseWindow,pending:s.pending};

      s=buildInitialMatchState();appState=s;s.pvpHumanVsHuman=true;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;
      s.playerHeroes.LEFT.card_id='S1-ARC-H005';s.playerHeroes.LEFT.exhausted=false;s.playerHand=['S1-ARC-023'];
      s.aiHeroes.LEFT.card_id='S1-WAR-H006';s.aiHeroes.LEFT.exhausted=true;s.aiHeroes.LEFT.attachments=[null,null];s.aiHand=['S1-CLE-025'];
      commitPlayedCard(s,{card_id:'S1-ARC-023',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'});
      var blessing=(s.responseWindow&&s.responseWindow.options||[]).findIndex(function(o){return o.card_id==='S1-CLE-025';});
      if(blessing<0)return{ok:false,reason:'Cross Bolt did not offer Blessing of Divinity',rw:s.responseWindow};
      s.responseWindow.selected=blessing;confirmSelectedResponse();
      if(!s.aiHeroes.LEFT.exhausted)return{ok:false,reason:'Defense Response changed existing Exhaust state'};
      return{ok:true,version:'v5.35',ultimateOverflow:true,potionFullSlots:true,venomPerHeroMagicalResponse:true,venomBlockToZero:true,crossBoltBlessingExhaustParity:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;closeChoice();closeResponseWindowUI();}
  }

  function simulateV230FullCardEffectCodingAudit(){
    initCards();
    var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      if(!window.GL_CARD_DEFINITIONS||window.GL_CARD_DEFINITIONS.version!=='v0.12.7'||CARDS.length!==198)return{ok:false,reason:'Runtime Data v0.12.7 / 198-card guard failed'};
      if(!window.GL_EFFECT_RECIPES||window.GL_EFFECT_RECIPES.version!=='v0.11.7'||(window.GL_EFFECT_RECIPES.effect_recipes||[]).length!==198)return{ok:false,reason:'Effect Recipe v0.11.7 / 198-recipe guard failed'};
      var s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;s.aiManaRegen=4;
      var spell=heroState('S1-THF-H005','PLAYER','LEFT'),duelist=heroState('S1-THF-H006','PLAYER','LEFT'),elementalist=heroState('S1-MAG-H002','PLAYER','LEFT'),lord=heroState('S1-MAG-H003','PLAYER','LEFT'),priest=heroState('S1-CLE-H002','PLAYER','LEFT'),saint=heroState('S1-CLE-H003','PLAYER','LEFT'),paladin=heroState('S1-WAR-H005','PLAYER','LEFT'),crusader=heroState('S1-WAR-H006','PLAYER','LEFT');
      if(cardPlayCostForSource(card('S1-MAG-025'),spell,s,'PLAYER')!==4||cardPlayCostForSource(card('S1-MAG-025'),duelist,s,'PLAYER')!==4)return{ok:false,reason:'Mana Void dynamic cost mismatch'};
      if(cardPlayCostForSource(card('S1-MAG-018'),elementalist,s,'PLAYER')!==5||cardPlayCostForSource(card('S1-MAG-018'),lord,s,'PLAYER')!==2)return{ok:false,reason:'Double Casting class cost mismatch'};
      if(cardPlayCostForSource(card('S1-CLE-018'),priest,s,'PLAYER')!==6||cardPlayCostForSource(card('S1-CLE-018'),saint,s,'PLAYER')!==4)return{ok:false,reason:"Heaven's Fury class cost mismatch"};
      if(cardPlayCostForSource(card('S1-MAG-019'),elementalist,s,'PLAYER')!==8||cardPlayCostForSource(card('S1-MAG-019'),lord,s,'PLAYER')!==8)return{ok:false,reason:"Flame Dragon's Wrath cost mismatch"};
      if(responseCostForSource(card('S1-CLE-025'),paladin,s,'PLAYER')!==6||responseCostForSource(card('S1-CLE-025'),crusader,s,'PLAYER')!==4)return{ok:false,reason:'Blessing of Divinity response cost mismatch'};
      if(cardPlayCostForSource(card('S1-CLE-024'),paladin,s,'PLAYER')!==9||cardPlayCostForSource(card('S1-CLE-024'),crusader,s,'PLAYER')!==9)return{ok:false,reason:'Divine Judgement cost mismatch'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerHeroes.LEFT.card_id='S1-MAG-H001';s.playerHeroes.LEFT.exhausted=false;s.playerHand=['S1-MAG-003'];
      try{getLegalActions('PLAYER',s);}catch(e){return{ok:false,reason:'getLegalActions crashed',error:String(e)}}

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.playerHeroes.LEFT.card_id='S1-THF-H005';s.aiHeroes.LEFT.statuses=[];
      var frost=(card('S1-MAG-008').effect||[]).find(function(e){return e.kind==='inflict_status';});applyStatusEffectToHero(frost,s.aiHeroes.LEFT,s.playerHeroes.LEFT,'attack-connected',card('S1-MAG-008'));
      if(!hasStatus(s.aiHeroes.LEFT,'Freeze'))return{ok:false,reason:'Frostbite hybrid lineage lost Freeze'};
      s.aiHeroes.LEFT.statuses=[];var fire=(card('S1-MAG-009').effect||[]).find(function(e){return e.kind==='inflict_status';});applyStatusEffectToHero(fire,s.aiHeroes.LEFT,s.playerHeroes.LEFT,'attack-connected',card('S1-MAG-009'));
      if(!hasStatus(s.aiHeroes.LEFT,'Burn'))return{ok:false,reason:'Fire Blast hybrid lineage lost Burn'};

      s=buildInitialMatchState();appState=s;LANE_ORDER.forEach(function(l){s.playerHeroes[l].hp=40;s.playerHeroes[l].maxHp=100;});s.playerHeroes.RIGHT.card_id='S1-CLE-H002';
      applySourcePostAttackEffects(s,'S1-CLE-010',s.playerHeroes.RIGHT,'PLAYER','RIGHT','AI',null,{damage:0,prevented:true});
      if(LANE_ORDER.some(function(l){return s.playerHeroes[l].hp<=40;}))return{ok:false,reason:'Holy Blast heal still depends on damage'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.aiMana=30;s.playerHeroes.LEFT.card_id='S1-WAR-H002';s.playerHeroes.LEFT.exhausted=false;s.playerHand=['S1-WAR-018'];s.aiHeroes.LEFT.hp=30;s.aiHeroes.LEFT.maxHp=80;
      commitPlayedCard(s,{card_id:'S1-WAR-018',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'});
      if(!s.responseWindow||s.responseWindow.cannot_dodge)return{ok:false,reason:'Gladiator Execute did not open dodge-capable response'};
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerHeroes.LEFT.card_id='S1-WAR-H003';s.playerHeroes.LEFT.exhausted=false;s.playerHand=['S1-WAR-018'];s.aiHeroes.LEFT.hp=30;s.aiHeroes.LEFT.maxHp=80;
      commitPlayedCard(s,{card_id:'S1-WAR-018',hand_index:0,source_side:'PLAYER',source_lane:'LEFT',target_side:'AI',target_lane:'LEFT'});
      if(!s.responseWindow||!s.responseWindow.cannot_dodge||!s.responseWindow.cannot_block)return{ok:false,reason:'Conqueror Execute response restrictions mismatch'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerHeroes.LEFT.card_id='S1-CLE-H002';s.playerHeroes.LEFT.exhausted=true;s.playerHand=['S1-CLE-012'];
      if(!reactiveCancelOptionsFor(s,'DefendSkill','PLAYER','AI').some(function(o){return o.card_id==='S1-CLE-012';}))return{ok:false,reason:'Binding Light cannot cancel opponent committed Defend Skill'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.playerHeroes.CENTER.card_id='S1-ARC-H002';s.playerHeroes.CENTER.exhausted=false;s.playerHand=['S1-ARC-013','S1-ARC-002','S1-ARC-007','S1-ARC-006'];beginPlayFromHand(0);chooseHeroFromBoard('PLAYER','CENTER');
      var tc=(s.pending&&s.pending.candidates||[]).map(function(x){return x.card_id;}).sort();if(tc.join(',')!=='S1-ARC-002,S1-ARC-007')return{ok:false,reason:'Triple Shot candidate filter mismatch',candidates:tc};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.playerHeroes.LEFT.exhausted=true;s.playerHeroes.LEFT.attachments=['S1-ITM-010','S1-ITM-011'];s.playerHeroes.LEFT.statuses=[{name:'Poison',duration:1}];
      if(legalTargetLanesFor(s,'PLAYER','S1-ITM-003',null).indexOf('LEFT')<0||legalTargetLanesFor(s,'PLAYER','S1-ITM-004',null).indexOf('LEFT')<0)return{ok:false,reason:'Instant Item still blocked by full Attachment slots'};
      s.playerHeroes.LEFT.stamina_no_proactive_skill_turn=runtimeTurnStamp(s);s.playerHeroes.LEFT.exhausted=false;if(sourceCompatible(card('S1-WAR-001'),s.playerHeroes.LEFT))return{ok:false,reason:'Stamina Potion granted an additional proactive Skill use'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.playerHand=['S1-EVT-005'];if(validateCardPreconditions(s,'PLAYER','S1-EVT-005',null,null,{}).ok)return{ok:false,reason:'Last Resort illegal board was not prevalidated'};
      s.racial=2;s.playerHand=['S1-EVT-008'];if(validateCardPreconditions(s,'PLAYER','S1-EVT-008',null,null,{}).ok)return{ok:false,reason:'Forged Alliance token cap was not prevalidated'};
      s.playerHand=['S1-EVT-004'];if(validateCardPreconditions(s,'PLAYER','S1-EVT-004','LEFT',null,{}).ok)return{ok:false,reason:'Relentless Leveling missing candidate was not prevalidated'};
      s.playerDiscard=[];if(validateCardPreconditions(s,'PLAYER','S1-EVT-010',null,null,{}).ok)return{ok:false,reason:'Deja vu missing candidate was not prevalidated'};
      s.playerHand=['S1-CLE-013'];if(validateCardPreconditions(s,'PLAYER','S1-CLE-013','RIGHT',null,{}).ok)return{ok:false,reason:"God's Blessing missing discard was not prevalidated"};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.playerHand=['S1-ITM-006'];commitPlayedCard(s,{card_id:'S1-ITM-006',hand_index:0,side:'PLAYER',source_side:'PLAYER'});if(!s.pending||s.pending.type!=='magic_scope_reveal'||s.playerDiscard.indexOf('S1-ITM-006')>=0)return{ok:false,reason:'Magic Scope did not remain pending until close'};
      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Deploy';s.mana=30;s.playerHeroes.RIGHT.card_id='S1-CLE-H002';s.playerHeroes.RIGHT.exhausted=false;s.playerHand=['S1-CLE-013','S1-WAR-001'];commitPlayedCard(s,{card_id:'S1-CLE-013',hand_index:0,side:'PLAYER',source_side:'PLAYER',source_lane:'RIGHT'});if(!s.pending||s.pending.resolve_to!=='discard_then_draw_three')return{ok:false,reason:"God's Blessing explicit discard choice missing"};

      s=buildInitialMatchState();appState=s;s.aiHeroes.LEFT.statuses=[{name:'Burn',duration:1}];s.playerHeroes.LEFT.card_id='S1-MAG-H001';var br=applyResolvedAttackDamage(s,'S1-MAG-003','PLAYER','LEFT','AI','LEFT',50,'Magical',false,20,false,false);if(Number(br.burn_damage||0)!==10||Number(br.hp_damage||0)!==40)return{ok:false,reason:'Burn failed after partial Block',result:br};

      s=buildInitialMatchState();appState=s;s.playerHeroes.LEFT.card_id='S1-MAG-H002';s.aiHeroes.LEFT.statuses=[];applyStatusEffectToHero({kind:'inflict_status',status:'Burn',duration_turns:1},s.aiHeroes.LEFT,s.playerHeroes.LEFT,'direct effect',card('S1-MAG-013'));var bs=(s.aiHeroes.LEFT.statuses||[]).find(function(x){return statusName(x)==='Burn';});if(!bs||Number(bs.duration||0)!==2)return{ok:false,reason:'Elemental Mastery/Sovereignty did not extend opponent non-attack Burn'};

      s=buildInitialMatchState();appState=s;s.turn='PLAYER';s.phase='Battle';s.round=2;s.playerHeroes.LEFT.card_id='S1-CLE-H001';s.playerHeroes.LEFT.attachments=[null,null];s.playerHand=['S1-CLE-003','S1-EVT-009','S1-ITM-012'];var castOpts=responseOptionsFor(s,'PLAYER',{damage:50,damage_type:'Magical',attack_type:'Casting Attack',casting:true,target_lane:'LEFT',source_lane:'LEFT',source_side:'AI',target_side:'PLAYER'});if(castOpts.some(function(o){return o.card_id==='S1-CLE-003'||o.card_id==='S1-EVT-009';}))return{ok:false,reason:'Casting-illegal negate response still offered'};s.playerHeroes.LEFT.card_id='S1-THF-H001';var areaOpts=responseOptionsFor(s,'PLAYER',{damage:50,damage_type:'Physical',attack_type:'Area Attack',area:true,affected_lanes:['LEFT','CENTER'],target_lane:'LEFT',source_lane:'CENTER',source_side:'AI',target_side:'PLAYER'});if(!areaOpts.some(function(o){return o.card_id==='S1-ITM-012'&&o.source_lane==='LEFT';}))return{ok:false,reason:'Spectral Grappling Hook missing for current affected Hero during Area'};

      return{ok:true,version:'v5.35',cardsScanned:198,effectsScanned:198,costs:true,ultimateResponses:true,hybridStatuses:true,holyBlast:true,bindingLight:true,tripleShot:true,instantItems:true,eventPrevalidation:true,magicScope:true,godsBlessing:true,burnPartialBlock:true,elementalDuration:true,responseRestrictions:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;closeChoice();closeResponseWindowUI();}
  }
  function simulateV523MinorUiAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      var s=buildInitialMatchState();appState=s;s.preGame=null;s.turn='AI';s.phase='Deploy';s.aiMana=30;s.aiHand=['S1-EVT-011'];
      LANE_ORDER.forEach(function(lane){s.aiHeroes[lane].exhausted=false;s.aiHeroes[lane].hp=Math.max(1,Number(s.aiHeroes[lane].hp||100));});
      var noAttack=chooseAIAction(s,'Deploy');
      if(noAttack&&noAttack.card_id==='S1-EVT-011')return{ok:false,reason:'AI played Coordination Attack without a guaranteed follow-up Attack'};
      s.aiHand=['S1-EVT-011','S1-WAR-001'];s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.exhausted=false;
      var withAttack=chooseAIAction(s,'Deploy');
      if(!withAttack||withAttack.card_id!=='S1-EVT-011'||!withAttack.ai_setup_plan||withAttack.ai_setup_plan.preferred_attack_card_id!=='S1-WAR-001')return{ok:false,reason:'AI did not bind Coordination Attack to a legal same-turn follow-up',action:withAttack};
      if(typeof bringModalToFront!=='function'||typeof bindModalStackManager!=='function')return{ok:false,reason:'Newest-popup stack manager missing'};
      return{ok:true,version:'v5.35',coordinationRequiresFollowUp:true,newestPopupWins:true,responseReviewUsesCanonicalPreview:true,audioVisualStartSharedFrame:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }
  window.GL_LOCAL_AI_V523_UI_QA_SELF_TEST=simulateV523MinorUiAudit;

  function simulateV523CastingCardPlayedAudit(){
    initCards();var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      function fresh(){var s=buildInitialMatchState();appState=s;s.preGame=null;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=30;s.playerHeroes.CENTER.card_id='S1-MAG-H001';s.playerHeroes.CENTER.hp=80;s.playerHeroes.CENTER.maxHp=80;s.playerHeroes.CENTER.exhausted=false;s.playerHeroes.CENTER.attachments=[null,null];s.playerHeroes.CENTER.statuses=[];s.aiHeroes.CENTER.hp=100;s.aiHeroes.CENTER.maxHp=100;s.aiHeroes.CENTER.statuses=[];s.playerHand=['S1-MAG-020'];return s;}
      var s=fresh();
      if(!commitPlayedCard(s,{card_id:'S1-MAG-020',hand_index:0,source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'}))return{ok:false,reason:'Aether Ball casting commit failed'};
      var start=(s.playerPlayedEvents||[]).find(function(e){return e.label==='CAST'&&e.card_id==='S1-MAG-020';});
      if(!start||!start.casting_action_id)return{ok:false,reason:'Casting start Card Played entry missing',events:s.playerPlayedEvents};
      resolvePendingCastingsFor(s,'PLAYER');
      var resolved=(s.playerPlayedEvents||[]).find(function(e){return e.label==='RESOLVE'&&e.card_id==='S1-MAG-020';});
      if(!resolved||resolved.casting_action_id!==start.casting_action_id||resolved.related_casting_start_event_id!==start.id)return{ok:false,reason:'Casting resolved Card Played entry not linked to start',events:s.playerPlayedEvents};
      if(!(resolved.result_lines||[]).some(function(line){return /left its Attachment Slot/i.test(line);}))return{ok:false,reason:'Casting resolved entry was not emitted on Attachment exit',event:resolved};

      s=fresh();
      if(!commitPlayedCard(s,{card_id:'S1-MAG-020',hand_index:0,source_side:'PLAYER',source_lane:'CENTER',target_side:'AI',target_lane:'CENTER'}))return{ok:false,reason:'Stun cancellation casting commit failed'};
      start=(s.playerPlayedEvents||[]).find(function(e){return e.label==='CAST'&&e.card_id==='S1-MAG-020';});addStatus(s.playerHeroes.CENTER,'Stun',1,'QA cancel Casting');resolvePendingCastingsFor(s,'PLAYER');
      var canceled=(s.playerPlayedEvents||[]).find(function(e){return e.label==='CANCELED'&&e.card_id==='S1-MAG-020';});
      if(!canceled||canceled.casting_action_id!==start.casting_action_id||canceled.related_casting_start_event_id!==start.id)return{ok:false,reason:'Stun cancellation Card Played entry not linked',events:s.playerPlayedEvents};
      if(!(canceled.reason_lines||[]).some(function(line){return /Stun/i.test(line);}))return{ok:false,reason:'Stun cancellation reason missing',event:canceled};
      if((s.playerHeroes.CENTER.attachments||[]).some(function(v){return attachmentCardId(v)==='S1-MAG-020';}))return{ok:false,reason:'Canceled Casting remained in Attachment Slot'};
      return{ok:true,version:'v5.35',castingStartAndResolve:true,castingStartAndCancel:true,stunReason:true,sharedActionInstance:true,attachmentExitTrigger:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }
  window.GL_LOCAL_AI_V523_CASTING_CARD_PLAYED_QA_SELF_TEST=simulateV523CastingCardPlayedAudit;


  function simulateV534TacticalAIPlannerAudit(){
    initCards(); var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      function fresh(){
        var s=buildInitialMatchState();appState=s;s.preGame=null;s.turn='AI';s.phase='Deploy';s.aiMana=30;s.aiPlan=null;
        LANE_ORDER.forEach(function(lane){var h=s.aiHeroes[lane];h.hp=Number(h.maxHp||100);h.exhausted=false;h.statuses=[];h.attachments=[null,null];});
        LANE_ORDER.forEach(function(lane){var h=s.playerHeroes[lane];h.hp=Math.max(1,Number(h.maxHp||100));h.statuses=[];h.attachments=[null,null];});
        return s;
      }
      var s=fresh();s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.maxHp=100;s.aiHeroes.LEFT.hp=40;s.aiHeroes.RIGHT.card_id='S1-CLE-H001';s.aiHeroes.RIGHT.maxHp=80;s.aiHeroes.RIGHT.hp=80;s.aiHand=['S1-ITM-013'];
      var action=chooseAIAction(s,'Deploy');if(action&&action.card_id==='S1-ITM-013')return{ok:false,reason:'Ring of Grace was spent without a same-turn heal follow-up',action:action};

      s=fresh();s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.LEFT.maxHp=100;s.aiHeroes.LEFT.hp=40;s.aiHeroes.RIGHT.card_id='S1-CLE-H001';s.aiHeroes.RIGHT.maxHp=80;s.aiHeroes.RIGHT.hp=80;s.aiHand=['S1-ITM-013','S1-CLE-005'];
      action=chooseAIAction(s,'Deploy');
      if(!action||action.card_id!=='S1-ITM-013'||action.target_lane!=='LEFT'||!action.ai_setup_plan||action.ai_setup_plan.kind!=='HEAL_FOLLOWUP')return{ok:false,reason:'Ring of Grace was not bound to the injured heal recipient',action:action};
      if(action.ai_setup_plan.preferred_source_lane!=='RIGHT'||action.ai_setup_plan.preferred_target_lane!=='LEFT')return{ok:false,reason:'Ring plan confused healer with heal recipient',plan:action.ai_setup_plan};
      if(!executeAIAction(s,action)||!s.aiPlan)return{ok:false,reason:'Ring tactical plan was not stored',plan:s.aiPlan};
      s.phase='Reform';var heal=chooseAIAction(s,'Reform');
      if(!heal||heal.card_id!=='S1-CLE-005'||heal.source_lane!=='RIGHT'||heal.target_lane!=='LEFT')return{ok:false,reason:'Ring follow-up did not heal the Ring host',heal:heal,plan:s.aiPlan};

      s=fresh();s.aiHeroes.LEFT.card_id='S1-THF-H001';s.aiHand=['S1-ITM-011'];action=chooseAIAction(s,'Deploy');if(action&&action.card_id==='S1-ITM-011')return{ok:false,reason:'Poison Vial was spent without an attack follow-up',action:action};
      s.aiHand=['S1-ITM-011','S1-THF-001'];action=chooseAIAction(s,'Deploy');if(!action||action.card_id!=='S1-ITM-011'||action.target_lane!=='LEFT'||!action.ai_setup_plan||action.ai_setup_plan.preferred_source_lane!=='LEFT'||action.ai_setup_plan.preferred_card_id!=='S1-THF-001')return{ok:false,reason:'Poison Vial was not bound to the attached Hero attack',action:action};

      s=fresh();s.aiHeroes.LEFT.card_id='S1-MAG-H001';s.aiHand=['S1-ITM-010','S1-WAR-001'];action=chooseAIAction(s,'Deploy');if(action&&action.card_id==='S1-ITM-010')return{ok:false,reason:'Arcane Scroll accepted a Physical Attack follow-up',action:action};
      s.aiHand=['S1-ITM-010','S1-MAG-003'];action=chooseAIAction(s,'Deploy');if(!action||action.card_id!=='S1-ITM-010'||!action.ai_setup_plan||action.ai_setup_plan.preferred_source_lane!=='LEFT'||action.ai_setup_plan.preferred_card_id!=='S1-MAG-003')return{ok:false,reason:'Arcane Scroll did not bind a Magical Attack by its host',action:action};

      s=fresh();s.aiHeroes.LEFT.card_id='S1-WAR-H001';s.aiHeroes.RIGHT.card_id='S1-CLE-H001';s.aiHand=['S1-CLE-006','S1-WAR-001'];action=chooseAIAction(s,'Deploy');
      if(!action||action.card_id!=='S1-CLE-006'||action.target_lane!=='LEFT'||!action.ai_setup_plan||action.ai_setup_plan.preferred_source_lane!=='LEFT'||action.ai_setup_plan.preferred_card_id!=='S1-WAR-001')return{ok:false,reason:'Blessing of Might did not bind the buffed Hero Physical Attack',action:action};

      s=fresh();s.aiHeroes.LEFT.card_id='S1-MAG-H001';s.aiHeroes.RIGHT.card_id='S1-CLE-H001';s.aiHand=['S1-CLE-007','S1-MAG-003'];action=chooseAIAction(s,'Deploy');
      if(!action||action.card_id!=='S1-CLE-007'||action.target_lane!=='LEFT'||!action.ai_setup_plan||action.ai_setup_plan.preferred_source_lane!=='LEFT'||action.ai_setup_plan.preferred_card_id!=='S1-MAG-003')return{ok:false,reason:'Blessing of Wisdom did not bind the buffed Hero Magical Attack',action:action};

      s=fresh();s.aiHeroes.LEFT.card_id='S1-WAR-H002';s.aiHand=['S1-WAR-013'];action=chooseAIAction(s,'Deploy');if(action&&action.card_id==='S1-WAR-013')return{ok:false,reason:'Enrage was spent without a Physical Attack follow-up',action:action};
      s.aiHand=['S1-WAR-013','S1-WAR-001'];action=chooseAIAction(s,'Deploy');if(!action||action.card_id!=='S1-WAR-013'||!action.ai_setup_plan||action.ai_setup_plan.preferred_source_lane!=='LEFT')return{ok:false,reason:'Enrage did not bind its source Hero Physical Attack',action:action};

      s=fresh();s.round=2;s.aiHeroes.LEFT.card_id='S1-MAG-H002';s.aiHeroes.LEFT.maxHp=100;s.aiHeroes.LEFT.hp=100;s.aiHand=['S1-MAG-018','S1-MAG-003'];action=chooseAIAction(s,'Deploy');
      if(!action||action.card_id!=='S1-MAG-018'||!action.ai_setup_plan||action.ai_setup_plan.not_before_round!==3)return{ok:false,reason:'Elementalist Double Casting did not reserve a next-turn Magical Attack',action:action};
      if(!executeAIAction(s,action)||!s.aiPlan)return{ok:false,reason:'Elementalist Double Casting future plan was not stored',plan:s.aiPlan};
      s.phase='Battle';s.round=2;var earlyDouble=chooseAIAction(s,'Battle');if(earlyDouble&&earlyDouble.card_id==='S1-MAG-003')return{ok:false,reason:'Elementalist spent reserved Double Casting Attack too early',action:earlyDouble,plan:s.aiPlan};
      clearAIPlanAtTurnEnd(s);if(!s.aiPlan)return{ok:false,reason:'Elementalist future Double Casting plan was cleared at turn end'};
      s.round=3;s.phase='Battle';s.aiHeroes.LEFT.exhausted=false;var delayedDouble=chooseAIAction(s,'Battle');if(!delayedDouble||delayedDouble.card_id!=='S1-MAG-003'||delayedDouble.source_lane!=='LEFT')return{ok:false,reason:'Elementalist did not play its reserved Magical Attack next turn',action:delayedDouble};

      s=fresh();s.aiHeroes.LEFT.card_id='S1-MAG-H003';s.aiHeroes.LEFT.maxHp=120;s.aiHeroes.LEFT.hp=120;s.aiHand=['S1-MAG-018','S1-MAG-006'];action=chooseAIAction(s,'Deploy');if(action&&action.card_id==='S1-MAG-018')return{ok:false,reason:'Elemental Lord used Double Casting without a Magical Attack in hand',action:action};
      s.aiHand=['S1-MAG-018','S1-MAG-006','S1-MAG-003'];action=chooseAIAction(s,'Deploy');
      if(!action||action.card_id!=='S1-MAG-018'||!action.ai_setup_plan||action.ai_setup_plan.preferred_card_id!=='S1-MAG-003'||action.ai_setup_plan.preferred_source_lane!=='LEFT'||action.ai_setup_plan.not_before_round!==Number(s.round||1))return{ok:false,reason:'Elemental Lord Double Casting did not reserve the immediate same-source Magical Attack',action:action};
      if(!executeAIAction(s,action)||!s.aiPlan)return{ok:false,reason:'Elemental Lord Double Casting follow-up plan was not stored',plan:s.aiPlan};
      s.phase='Battle';s.round=2;var doubleFollow=chooseAIAction(s,'Battle');if(!doubleFollow||doubleFollow.card_id!=='S1-MAG-003'||doubleFollow.source_lane!=='LEFT')return{ok:false,reason:'AI played something other than the reserved Magical Attack after Elemental Lord Double Casting',follow:doubleFollow};

      s=fresh();s.round=4;s.aiHeroes.LEFT.card_id='S1-MAG-H002';s.aiHeroes.LEFT.maxHp=100;s.aiHeroes.LEFT.hp=100;s.aiHand=['S1-MAG-013','S1-MAG-003'];action=chooseAIAction(s,'Deploy');
      if(!action||action.card_id!=='S1-MAG-013'||!action.ai_setup_plan||action.ai_setup_plan.not_before_round!==5||action.ai_setup_plan.preferred_card_id!=='S1-MAG-003'||action.ai_setup_plan.preferred_source_lane!=='LEFT')return{ok:false,reason:'Wildfire did not reserve the same-Mage next-turn Attack',action:action};
      if(!executeAIAction(s,action)||!s.aiPlan)return{ok:false,reason:'Wildfire future Attack plan was not stored',plan:s.aiPlan};
      s.phase='Battle';s.round=2;var earlyWildfire=chooseAIAction(s,'Battle');if(earlyWildfire&&earlyWildfire.card_id==='S1-MAG-003')return{ok:false,reason:'Wildfire reserved Attack was spent in the setup turn',action:earlyWildfire};
      clearAIPlanAtTurnEnd(s);if(!s.aiPlan)return{ok:false,reason:'Wildfire next-turn plan was cleared at turn end'};
      s.round=5;s.phase='Battle';s.aiHeroes.LEFT.exhausted=false;var wildfireFollow=chooseAIAction(s,'Battle');if(!wildfireFollow||wildfireFollow.card_id!=='S1-MAG-003'||wildfireFollow.source_lane!=='LEFT')return{ok:false,reason:'Wildfire was not followed by the reserved Mage Attack next turn',action:wildfireFollow};

      return{ok:true,version:'v5.35',ringTargetsHealRecipient:true,ringRequiresHeal:true,poisonVialRequiresHostAttack:true,arcaneScrollRequiresHostMagicalAttack:true,blessingMightRequiresBuffedHeroPhysicalAttack:true,blessingWisdomRequiresBuffedHeroMagicalAttack:true,enrageRequiresSourcePhysicalAttack:true,doubleCastingRequiresTimedMagicalAttack:true,doubleCastingAttackFollowsPrintedTiming:true,wildfireRequiresNextTurnAttack:true,futureAttackPlanPreserved:true,plansRevalidateAtFollowUp:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }
  window.GL_LOCAL_AI_V534_TACTICAL_AI_QA_SELF_TEST=simulateV534TacticalAIPlannerAudit;

  function simulateV536FallbackSnapshotDrawAudit(){
    initCards();var oldApp=appState,oldMatch=matchStarted,oldSuppress=SUPPRESS_RENDER;
    try{
      SUPPRESS_RENDER=true;matchStarted=true;
      var heroCards=CARDS.filter(function(c){return cardFamily(c)==='Hero';});
      var responseCards=CARDS.filter(function(c){return isResponseOnly(c)&&requiresSourceHero(c);});
      var sourceCards=CARDS.filter(function(c){return requiresSourceHero(c);});
      var auditedPairs=0,fallbackPairs=0,responseFallbackPairs=0,failures=[];
      function printedClassRows(c){return ((c.printed&&c.printed.rows)||[]).filter(function(row){return row&&row.kind==='class_rank'&&Array.isArray(row.classes)&&row.classes.length;});}
      heroCards.forEach(function(hc){
        var baseHero={card_id:hc.card_id,hp:Number((hc.identity&&hc.identity.hp)||100),maxHp:Number((hc.identity&&hc.identity.hp)||100),exhausted:false,statuses:[],attachments:[null,null]};
        var rank=heroRank(baseHero),current=normalizedClassToken(heroClass(baseHero)),lineage=activeLineageForHero(baseHero).map(normalizedClassToken);
        sourceCards.forEach(function(rc){
          var rows=printedClassRows(rc);if(!rows.length)return;
          var matchingRows=rows.filter(function(row){return Number(row.rank||1)<=rank&&(row.classes||[]).some(function(cls){return lineage.indexOf(normalizedClassToken(cls))!==-1;});});
          if(!matchingRows.length)return;
          var currentHasPrintedRow=matchingRows.some(function(row){return (row.classes||[]).some(function(cls){return normalizedClassToken(cls)===current;});});
          if(currentHasPrintedRow)return;
          var legal=canonicalCardLegalClasses(rc);if(legal.length&&!lineageContainsAnyClass(baseHero,legal))return;
          var req=(rc.requirement||{}),src=(rc.source_requirement||{}),ult=req.ultimate||src.ultimate||{};
          if(ult&&ult.is_ultimate&&((ult.owner_lineage_card_ids||[]).indexOf(hc.card_id)===-1))return;
          auditedPairs++;fallbackPairs++;if(isResponseOnly(rc))responseFallbackPairs++;
          var testHero=clone(baseHero);testHero.exhausted=!!isResponseOnly(rc);
          var legalByMode=isResponseOnly(rc)?sourceCompatibleForResponse(rc,testHero):sourceCompatible(rc,testHero);
          if(!legalByMode)failures.push(hc.card_id+' ('+heroClass(testHero)+') <- '+rc.card_id+' ['+(matchingRows.map(function(r){return r.label;}).join(', '))+']');
        });
      });
      if(failures.length)return{ok:false,reason:'Global Hero printed-row fallback failures',failures:failures.slice(0,20),count:failures.length};
      if(fallbackPairs<1||responseFallbackPairs<1)return{ok:false,reason:'Fallback audit did not discover printed-row fallback pairs',fallbackPairs:fallbackPairs,responseFallbackPairs:responseFallbackPairs};
      var s=buildInitialMatchState();appState=s;s.preGame=null;s.turn='PLAYER';s.phase='Battle';s.round=2;s.mana=9;s.playerHeroes.LEFT.card_id='S1-ARC-H003';s.playerHeroes.LEFT.hp=120;s.playerHeroes.LEFT.maxHp=120;s.playerHeroes.LEFT.exhausted=true;s.playerHeroes.LEFT.statuses=[];s.playerHeroes.LEFT.attachments=[null,null];s.playerHand=['S1-ARC-011'];
      var incoming={damage:40,damage_type:'Magical',attack_type:'Magical Attack',card_id:'S1-MAG-021',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT',cannot_dodge:false,cannot_block:false};
      var opts=responseOptionsFor(s,'PLAYER',incoming);if(!opts.some(function(o){return o.card_id==='S1-ARC-011'&&o.response_kind==='dodge';}))return{ok:false,reason:'Grand Ranger Rank III cannot fallback to Marksman Back Step against Mana Absorption',options:opts};
      s.responseWindow={kind:'incoming_attack',response_owner:'PLAYER',card_id:'S1-MAG-021',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT',damage:40,damage_type:'Magical',attack_type:'Magical Attack',cannot_dodge:false,cannot_block:false,options:[],selected:null};refreshResponseWindowOptions(s,s.responseWindow);if(!s.responseWindow.options.some(function(o){return o.card_id==='S1-ARC-011';}))return{ok:false,reason:'Live Response Window refresh did not restore legal fallback response'};
      var snap=heroEventSnapshot(s,'PLAYER','LEFT'),evt={target_side:'PLAYER',target_lane:'LEFT',target_snapshot:clone(snap)};s.playerHeroes.LEFT.card_id='S1-ARC-L002';if(actionTargetLine(evt).indexOf(snap.name)<0||actionTargetLine(evt).indexOf('Falconer')>=0)return{ok:false,reason:'Card Played target snapshot changed after Legacy replacement',line:actionTargetLine(evt),snapshot:snap};
      var d=buildInitialMatchState();appState=d;d.preGame=null;d.turn='AI';d.phase='End';d.playerHeroes.LEFT.exhausted=true;var beforeHand=d.playerHand.length,beforeMana=d.mana;beginPlayerTurnDrawPhase(d,'QA AI end to Player Draw');if(d.turn!=='PLAYER'||d.phase!=='Deploy'||d.drawPhaseResolvedFor!==null||d.playerHand.length!==beforeHand+1||d.mana!==Math.min(12,beforeMana+d.manaRegen)||d.playerHeroes.LEFT.exhausted)return{ok:false,reason:'Automatic Player Draw-to-Deploy transition mismatch',state:{turn:d.turn,phase:d.phase,drawPhaseResolvedFor:d.drawPhaseResolvedFor,hand:d.playerHand.length,beforeHand:beforeHand,mana:d.mana,beforeMana:beforeMana,exhausted:d.playerHeroes.LEFT.exhausted}};
      d.pending=null;d.phase='Draw';d.drawPhaseResolvedFor='PLAYER';finishDrawReplacementChoice(d,'PLAYER');if(d.phase!=='Deploy')return{ok:false,reason:'Draw replacement did not auto-advance to Deploy after mandatory Draw work completed'};
      return{ok:true,version:'v5.38',heroCards:heroCards.length,responseCards:responseCards.length,sourceCards:sourceCards.length,auditedPairs:auditedPairs,fallbackPairs:fallbackPairs,responseFallbackPairs:responseFallbackPairs,backStepManaAbsorption:true,responseRefresh:true,targetSnapshot:true,automaticDrawEntry:true,drawAutoDeploy:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldApp;matchStarted=oldMatch;SUPPRESS_RENDER=oldSuppress;}
  }
  window.GL_V536_FALLBACK_SNAPSHOT_DRAW_QA_SELF_TEST=simulateV536FallbackSnapshotDrawAudit;


  window.GL_V539_RESPONSE_DIAGNOSTICS_QA_SELF_TEST=function(){
    initCards();var old=appState,oldStarted=matchStarted,oldSuppress=SUPPRESS_RENDER;SUPPRESS_RENDER=true;
    try{
      var st=buildInitialMatchState();appState=st;matchStarted=true;st.turn='AI';st.phase='Battle';st.mana=10;st.playerHeroes.LEFT.card_id='S1-ARC-H003';st.playerHeroes.LEFT.hp=100;st.playerHeroes.LEFT.statuses=[];st.playerHand=['S1-ARC-011','S1-THF-003'];
      var incoming={damage:60,damage_type:'Magical',attack_type:'Single Target',cannot_dodge:false,cannot_block:false,card_id:'S1-MAG-021',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT'};
      var opts=responseOptionsFor(st,'PLAYER',incoming);if(!opts.some(function(o){return o.card_id==='S1-ARC-011';}))return{ok:false,reason:'Back Step fallback missing from legal response options'};
      st.playerHeroes.LEFT.statuses=[{name:'Freeze',duration:1}];opts=responseOptionsFor(st,'PLAYER',incoming);var reasons=responseUnavailableReasonsFor(st,'PLAYER',incoming,'S1-ARC-011',0,opts);if(opts.some(function(o){return o.card_id==='S1-ARC-011';})||!reasons.some(function(x){return /Frozen|Freeze/i.test(x);}))return{ok:false,reason:'Frozen unavailable response reason missing',options:opts,reasons:reasons};
      st.responseWindow={kind:'incoming_attack',response_owner:'PLAYER',card_id:'S1-MAG-021',source_side:'AI',source_lane:'LEFT',target_side:'PLAYER',target_lane:'LEFT',damage:60,damage_type:'Magical',attack_type:'Single Target',cannot_dodge:false,cannot_block:false,options:opts,selected:null};var display=responseDisplayItemsFor(st,st.responseWindow);if(!display.some(function(x){return !x.available&&x.card_id==='S1-ARC-011';}))return{ok:false,reason:'Unavailable Defense card not kept visible',display:display};
      return{ok:true,version:'v5.46',legalFallbackVisible:true,unavailableReasons:true,allResponseCardsVisible:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=old;matchStarted=oldStarted;SUPPRESS_RENDER=oldSuppress;}
  };
  window.GL_V540_MOBILE_UI_QA_SELF_TEST=function(){
    initCards();var oldState=appState,oldStarted=matchStarted,oldSuppress=SUPPRESS_RENDER;SUPPRESS_RENDER=true;
    try{
      var statusHtml=v56StatusControl({card_id:'S1-ARC-H001',hp:80,maxHp:80,statuses:[{name:'Burn',duration:2},{name:'Poison',duration:1}],exhausted:false},'PLAYER');
      appState=buildInitialMatchState();matchStarted=true;appState.turn='PLAYER';appState.phase='Battle';appState.playerHeroes.LEFT.card_id='S1-ARC-H001';appState.racial=1;appState.aiHeroes.LEFT.hp=Math.max(1,Number(appState.aiHeroes.LEFT.maxHp||100)-20);
      var trigger=v540MobileActionTrigger('PLAYER','LEFT',appState.playerHeroes.LEFT,card('S1-ARC-H001'),false);
      return{ok:/mobile-status-summary/.test(statusHtml)&&/Burn/.test(statusHtml)&&/Poison/.test(statusHtml)&&/mobile-hero-action-trigger/.test(trigger),statusAggregated:true,compactAction:true,mobileFooterBelowPhase:true};
    }catch(e){return{ok:false,error:String(e&&e.stack||e)}}finally{appState=oldState;matchStarted=oldStarted;SUPPRESS_RENDER=oldSuppress;}
  };
  window.GL_PVP_V231_FULL_CARD_EFFECT_QA_SELF_TEST=simulateV230FullCardEffectCodingAudit;

  window.GL_PVP_V220_GAMEPLAY_UI_QA_SELF_TEST=simulateV220GameplayUiAudit;

})();
